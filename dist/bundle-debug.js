(() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __commonJS = (cb, mod) => function __require() {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));

  // node_modules/react/cjs/react.development.js
  var require_react_development = __commonJS({
    "node_modules/react/cjs/react.development.js"(exports, module) {
      "use strict";
      if (true) {
        (function() {
          "use strict";
          if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart === "function") {
            __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(new Error());
          }
          var ReactVersion = "18.3.1";
          var REACT_ELEMENT_TYPE = Symbol.for("react.element");
          var REACT_PORTAL_TYPE = Symbol.for("react.portal");
          var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
          var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
          var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
          var REACT_PROVIDER_TYPE = Symbol.for("react.provider");
          var REACT_CONTEXT_TYPE = Symbol.for("react.context");
          var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
          var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
          var REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list");
          var REACT_MEMO_TYPE = Symbol.for("react.memo");
          var REACT_LAZY_TYPE = Symbol.for("react.lazy");
          var REACT_OFFSCREEN_TYPE = Symbol.for("react.offscreen");
          var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
          var FAUX_ITERATOR_SYMBOL = "@@iterator";
          function getIteratorFn(maybeIterable) {
            if (maybeIterable === null || typeof maybeIterable !== "object") {
              return null;
            }
            var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
            if (typeof maybeIterator === "function") {
              return maybeIterator;
            }
            return null;
          }
          var ReactCurrentDispatcher = {
            /**
             * @internal
             * @type {ReactComponent}
             */
            current: null
          };
          var ReactCurrentBatchConfig = {
            transition: null
          };
          var ReactCurrentActQueue = {
            current: null,
            // Used to reproduce behavior of `batchedUpdates` in legacy mode.
            isBatchingLegacy: false,
            didScheduleLegacyUpdate: false
          };
          var ReactCurrentOwner = {
            /**
             * @internal
             * @type {ReactComponent}
             */
            current: null
          };
          var ReactDebugCurrentFrame = {};
          var currentExtraStackFrame = null;
          function setExtraStackFrame(stack) {
            {
              currentExtraStackFrame = stack;
            }
          }
          {
            ReactDebugCurrentFrame.setExtraStackFrame = function(stack) {
              {
                currentExtraStackFrame = stack;
              }
            };
            ReactDebugCurrentFrame.getCurrentStack = null;
            ReactDebugCurrentFrame.getStackAddendum = function() {
              var stack = "";
              if (currentExtraStackFrame) {
                stack += currentExtraStackFrame;
              }
              var impl = ReactDebugCurrentFrame.getCurrentStack;
              if (impl) {
                stack += impl() || "";
              }
              return stack;
            };
          }
          var enableScopeAPI = false;
          var enableCacheElement = false;
          var enableTransitionTracing = false;
          var enableLegacyHidden = false;
          var enableDebugTracing = false;
          var ReactSharedInternals = {
            ReactCurrentDispatcher,
            ReactCurrentBatchConfig,
            ReactCurrentOwner
          };
          {
            ReactSharedInternals.ReactDebugCurrentFrame = ReactDebugCurrentFrame;
            ReactSharedInternals.ReactCurrentActQueue = ReactCurrentActQueue;
          }
          function warn(format) {
            {
              {
                for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
                  args[_key - 1] = arguments[_key];
                }
                printWarning("warn", format, args);
              }
            }
          }
          function error(format) {
            {
              {
                for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
                  args[_key2 - 1] = arguments[_key2];
                }
                printWarning("error", format, args);
              }
            }
          }
          function printWarning(level, format, args) {
            {
              var ReactDebugCurrentFrame2 = ReactSharedInternals.ReactDebugCurrentFrame;
              var stack = ReactDebugCurrentFrame2.getStackAddendum();
              if (stack !== "") {
                format += "%s";
                args = args.concat([stack]);
              }
              var argsWithFormat = args.map(function(item) {
                return String(item);
              });
              argsWithFormat.unshift("Warning: " + format);
              Function.prototype.apply.call(console[level], console, argsWithFormat);
            }
          }
          var didWarnStateUpdateForUnmountedComponent = {};
          function warnNoop(publicInstance, callerName) {
            {
              var _constructor = publicInstance.constructor;
              var componentName = _constructor && (_constructor.displayName || _constructor.name) || "ReactClass";
              var warningKey = componentName + "." + callerName;
              if (didWarnStateUpdateForUnmountedComponent[warningKey]) {
                return;
              }
              error("Can't call %s on a component that is not yet mounted. This is a no-op, but it might indicate a bug in your application. Instead, assign to `this.state` directly or define a `state = {};` class property with the desired state in the %s component.", callerName, componentName);
              didWarnStateUpdateForUnmountedComponent[warningKey] = true;
            }
          }
          var ReactNoopUpdateQueue = {
            /**
             * Checks whether or not this composite component is mounted.
             * @param {ReactClass} publicInstance The instance we want to test.
             * @return {boolean} True if mounted, false otherwise.
             * @protected
             * @final
             */
            isMounted: function(publicInstance) {
              return false;
            },
            /**
             * Forces an update. This should only be invoked when it is known with
             * certainty that we are **not** in a DOM transaction.
             *
             * You may want to call this when you know that some deeper aspect of the
             * component's state has changed but `setState` was not called.
             *
             * This will not invoke `shouldComponentUpdate`, but it will invoke
             * `componentWillUpdate` and `componentDidUpdate`.
             *
             * @param {ReactClass} publicInstance The instance that should rerender.
             * @param {?function} callback Called after component is updated.
             * @param {?string} callerName name of the calling function in the public API.
             * @internal
             */
            enqueueForceUpdate: function(publicInstance, callback, callerName) {
              warnNoop(publicInstance, "forceUpdate");
            },
            /**
             * Replaces all of the state. Always use this or `setState` to mutate state.
             * You should treat `this.state` as immutable.
             *
             * There is no guarantee that `this.state` will be immediately updated, so
             * accessing `this.state` after calling this method may return the old value.
             *
             * @param {ReactClass} publicInstance The instance that should rerender.
             * @param {object} completeState Next state.
             * @param {?function} callback Called after component is updated.
             * @param {?string} callerName name of the calling function in the public API.
             * @internal
             */
            enqueueReplaceState: function(publicInstance, completeState, callback, callerName) {
              warnNoop(publicInstance, "replaceState");
            },
            /**
             * Sets a subset of the state. This only exists because _pendingState is
             * internal. This provides a merging strategy that is not available to deep
             * properties which is confusing. TODO: Expose pendingState or don't use it
             * during the merge.
             *
             * @param {ReactClass} publicInstance The instance that should rerender.
             * @param {object} partialState Next partial state to be merged with state.
             * @param {?function} callback Called after component is updated.
             * @param {?string} Name of the calling function in the public API.
             * @internal
             */
            enqueueSetState: function(publicInstance, partialState, callback, callerName) {
              warnNoop(publicInstance, "setState");
            }
          };
          var assign = Object.assign;
          var emptyObject = {};
          {
            Object.freeze(emptyObject);
          }
          function Component(props, context, updater) {
            this.props = props;
            this.context = context;
            this.refs = emptyObject;
            this.updater = updater || ReactNoopUpdateQueue;
          }
          Component.prototype.isReactComponent = {};
          Component.prototype.setState = function(partialState, callback) {
            if (typeof partialState !== "object" && typeof partialState !== "function" && partialState != null) {
              throw new Error("setState(...): takes an object of state variables to update or a function which returns an object of state variables.");
            }
            this.updater.enqueueSetState(this, partialState, callback, "setState");
          };
          Component.prototype.forceUpdate = function(callback) {
            this.updater.enqueueForceUpdate(this, callback, "forceUpdate");
          };
          {
            var deprecatedAPIs = {
              isMounted: ["isMounted", "Instead, make sure to clean up subscriptions and pending requests in componentWillUnmount to prevent memory leaks."],
              replaceState: ["replaceState", "Refactor your code to use setState instead (see https://github.com/facebook/react/issues/3236)."]
            };
            var defineDeprecationWarning = function(methodName, info) {
              Object.defineProperty(Component.prototype, methodName, {
                get: function() {
                  warn("%s(...) is deprecated in plain JavaScript React classes. %s", info[0], info[1]);
                  return void 0;
                }
              });
            };
            for (var fnName in deprecatedAPIs) {
              if (deprecatedAPIs.hasOwnProperty(fnName)) {
                defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);
              }
            }
          }
          function ComponentDummy() {
          }
          ComponentDummy.prototype = Component.prototype;
          function PureComponent(props, context, updater) {
            this.props = props;
            this.context = context;
            this.refs = emptyObject;
            this.updater = updater || ReactNoopUpdateQueue;
          }
          var pureComponentPrototype = PureComponent.prototype = new ComponentDummy();
          pureComponentPrototype.constructor = PureComponent;
          assign(pureComponentPrototype, Component.prototype);
          pureComponentPrototype.isPureReactComponent = true;
          function createRef() {
            var refObject = {
              current: null
            };
            {
              Object.seal(refObject);
            }
            return refObject;
          }
          var isArrayImpl = Array.isArray;
          function isArray(a) {
            return isArrayImpl(a);
          }
          function typeName(value) {
            {
              var hasToStringTag = typeof Symbol === "function" && Symbol.toStringTag;
              var type = hasToStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
              return type;
            }
          }
          function willCoercionThrow(value) {
            {
              try {
                testStringCoercion(value);
                return false;
              } catch (e) {
                return true;
              }
            }
          }
          function testStringCoercion(value) {
            return "" + value;
          }
          function checkKeyStringCoercion(value) {
            {
              if (willCoercionThrow(value)) {
                error("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
                return testStringCoercion(value);
              }
            }
          }
          function getWrappedName(outerType, innerType, wrapperName) {
            var displayName = outerType.displayName;
            if (displayName) {
              return displayName;
            }
            var functionName = innerType.displayName || innerType.name || "";
            return functionName !== "" ? wrapperName + "(" + functionName + ")" : wrapperName;
          }
          function getContextName(type) {
            return type.displayName || "Context";
          }
          function getComponentNameFromType(type) {
            if (type == null) {
              return null;
            }
            {
              if (typeof type.tag === "number") {
                error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue.");
              }
            }
            if (typeof type === "function") {
              return type.displayName || type.name || null;
            }
            if (typeof type === "string") {
              return type;
            }
            switch (type) {
              case REACT_FRAGMENT_TYPE:
                return "Fragment";
              case REACT_PORTAL_TYPE:
                return "Portal";
              case REACT_PROFILER_TYPE:
                return "Profiler";
              case REACT_STRICT_MODE_TYPE:
                return "StrictMode";
              case REACT_SUSPENSE_TYPE:
                return "Suspense";
              case REACT_SUSPENSE_LIST_TYPE:
                return "SuspenseList";
            }
            if (typeof type === "object") {
              switch (type.$$typeof) {
                case REACT_CONTEXT_TYPE:
                  var context = type;
                  return getContextName(context) + ".Consumer";
                case REACT_PROVIDER_TYPE:
                  var provider = type;
                  return getContextName(provider._context) + ".Provider";
                case REACT_FORWARD_REF_TYPE:
                  return getWrappedName(type, type.render, "ForwardRef");
                case REACT_MEMO_TYPE:
                  var outerName = type.displayName || null;
                  if (outerName !== null) {
                    return outerName;
                  }
                  return getComponentNameFromType(type.type) || "Memo";
                case REACT_LAZY_TYPE: {
                  var lazyComponent = type;
                  var payload = lazyComponent._payload;
                  var init = lazyComponent._init;
                  try {
                    return getComponentNameFromType(init(payload));
                  } catch (x) {
                    return null;
                  }
                }
              }
            }
            return null;
          }
          var hasOwnProperty = Object.prototype.hasOwnProperty;
          var RESERVED_PROPS = {
            key: true,
            ref: true,
            __self: true,
            __source: true
          };
          var specialPropKeyWarningShown, specialPropRefWarningShown, didWarnAboutStringRefs;
          {
            didWarnAboutStringRefs = {};
          }
          function hasValidRef(config) {
            {
              if (hasOwnProperty.call(config, "ref")) {
                var getter = Object.getOwnPropertyDescriptor(config, "ref").get;
                if (getter && getter.isReactWarning) {
                  return false;
                }
              }
            }
            return config.ref !== void 0;
          }
          function hasValidKey(config) {
            {
              if (hasOwnProperty.call(config, "key")) {
                var getter = Object.getOwnPropertyDescriptor(config, "key").get;
                if (getter && getter.isReactWarning) {
                  return false;
                }
              }
            }
            return config.key !== void 0;
          }
          function defineKeyPropWarningGetter(props, displayName) {
            var warnAboutAccessingKey = function() {
              {
                if (!specialPropKeyWarningShown) {
                  specialPropKeyWarningShown = true;
                  error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
                }
              }
            };
            warnAboutAccessingKey.isReactWarning = true;
            Object.defineProperty(props, "key", {
              get: warnAboutAccessingKey,
              configurable: true
            });
          }
          function defineRefPropWarningGetter(props, displayName) {
            var warnAboutAccessingRef = function() {
              {
                if (!specialPropRefWarningShown) {
                  specialPropRefWarningShown = true;
                  error("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
                }
              }
            };
            warnAboutAccessingRef.isReactWarning = true;
            Object.defineProperty(props, "ref", {
              get: warnAboutAccessingRef,
              configurable: true
            });
          }
          function warnIfStringRefCannotBeAutoConverted(config) {
            {
              if (typeof config.ref === "string" && ReactCurrentOwner.current && config.__self && ReactCurrentOwner.current.stateNode !== config.__self) {
                var componentName = getComponentNameFromType(ReactCurrentOwner.current.type);
                if (!didWarnAboutStringRefs[componentName]) {
                  error('Component "%s" contains the string ref "%s". Support for string refs will be removed in a future major release. This case cannot be automatically converted to an arrow function. We ask you to manually fix this case by using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref', componentName, config.ref);
                  didWarnAboutStringRefs[componentName] = true;
                }
              }
            }
          }
          var ReactElement = function(type, key, ref, self, source, owner, props) {
            var element = {
              // This tag allows us to uniquely identify this as a React Element
              $$typeof: REACT_ELEMENT_TYPE,
              // Built-in properties that belong on the element
              type,
              key,
              ref,
              props,
              // Record the component responsible for creating this element.
              _owner: owner
            };
            {
              element._store = {};
              Object.defineProperty(element._store, "validated", {
                configurable: false,
                enumerable: false,
                writable: true,
                value: false
              });
              Object.defineProperty(element, "_self", {
                configurable: false,
                enumerable: false,
                writable: false,
                value: self
              });
              Object.defineProperty(element, "_source", {
                configurable: false,
                enumerable: false,
                writable: false,
                value: source
              });
              if (Object.freeze) {
                Object.freeze(element.props);
                Object.freeze(element);
              }
            }
            return element;
          };
          function createElement(type, config, children) {
            var propName;
            var props = {};
            var key = null;
            var ref = null;
            var self = null;
            var source = null;
            if (config != null) {
              if (hasValidRef(config)) {
                ref = config.ref;
                {
                  warnIfStringRefCannotBeAutoConverted(config);
                }
              }
              if (hasValidKey(config)) {
                {
                  checkKeyStringCoercion(config.key);
                }
                key = "" + config.key;
              }
              self = config.__self === void 0 ? null : config.__self;
              source = config.__source === void 0 ? null : config.__source;
              for (propName in config) {
                if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
                  props[propName] = config[propName];
                }
              }
            }
            var childrenLength = arguments.length - 2;
            if (childrenLength === 1) {
              props.children = children;
            } else if (childrenLength > 1) {
              var childArray = Array(childrenLength);
              for (var i = 0; i < childrenLength; i++) {
                childArray[i] = arguments[i + 2];
              }
              {
                if (Object.freeze) {
                  Object.freeze(childArray);
                }
              }
              props.children = childArray;
            }
            if (type && type.defaultProps) {
              var defaultProps = type.defaultProps;
              for (propName in defaultProps) {
                if (props[propName] === void 0) {
                  props[propName] = defaultProps[propName];
                }
              }
            }
            {
              if (key || ref) {
                var displayName = typeof type === "function" ? type.displayName || type.name || "Unknown" : type;
                if (key) {
                  defineKeyPropWarningGetter(props, displayName);
                }
                if (ref) {
                  defineRefPropWarningGetter(props, displayName);
                }
              }
            }
            return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
          }
          function cloneAndReplaceKey(oldElement, newKey) {
            var newElement = ReactElement(oldElement.type, newKey, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, oldElement.props);
            return newElement;
          }
          function cloneElement(element, config, children) {
            if (element === null || element === void 0) {
              throw new Error("React.cloneElement(...): The argument must be a React element, but you passed " + element + ".");
            }
            var propName;
            var props = assign({}, element.props);
            var key = element.key;
            var ref = element.ref;
            var self = element._self;
            var source = element._source;
            var owner = element._owner;
            if (config != null) {
              if (hasValidRef(config)) {
                ref = config.ref;
                owner = ReactCurrentOwner.current;
              }
              if (hasValidKey(config)) {
                {
                  checkKeyStringCoercion(config.key);
                }
                key = "" + config.key;
              }
              var defaultProps;
              if (element.type && element.type.defaultProps) {
                defaultProps = element.type.defaultProps;
              }
              for (propName in config) {
                if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
                  if (config[propName] === void 0 && defaultProps !== void 0) {
                    props[propName] = defaultProps[propName];
                  } else {
                    props[propName] = config[propName];
                  }
                }
              }
            }
            var childrenLength = arguments.length - 2;
            if (childrenLength === 1) {
              props.children = children;
            } else if (childrenLength > 1) {
              var childArray = Array(childrenLength);
              for (var i = 0; i < childrenLength; i++) {
                childArray[i] = arguments[i + 2];
              }
              props.children = childArray;
            }
            return ReactElement(element.type, key, ref, self, source, owner, props);
          }
          function isValidElement(object) {
            return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
          }
          var SEPARATOR = ".";
          var SUBSEPARATOR = ":";
          function escape(key) {
            var escapeRegex = /[=:]/g;
            var escaperLookup = {
              "=": "=0",
              ":": "=2"
            };
            var escapedString = key.replace(escapeRegex, function(match) {
              return escaperLookup[match];
            });
            return "$" + escapedString;
          }
          var didWarnAboutMaps = false;
          var userProvidedKeyEscapeRegex = /\/+/g;
          function escapeUserProvidedKey(text) {
            return text.replace(userProvidedKeyEscapeRegex, "$&/");
          }
          function getElementKey(element, index) {
            if (typeof element === "object" && element !== null && element.key != null) {
              {
                checkKeyStringCoercion(element.key);
              }
              return escape("" + element.key);
            }
            return index.toString(36);
          }
          function mapIntoArray(children, array, escapedPrefix, nameSoFar, callback) {
            var type = typeof children;
            if (type === "undefined" || type === "boolean") {
              children = null;
            }
            var invokeCallback = false;
            if (children === null) {
              invokeCallback = true;
            } else {
              switch (type) {
                case "string":
                case "number":
                  invokeCallback = true;
                  break;
                case "object":
                  switch (children.$$typeof) {
                    case REACT_ELEMENT_TYPE:
                    case REACT_PORTAL_TYPE:
                      invokeCallback = true;
                  }
              }
            }
            if (invokeCallback) {
              var _child = children;
              var mappedChild = callback(_child);
              var childKey = nameSoFar === "" ? SEPARATOR + getElementKey(_child, 0) : nameSoFar;
              if (isArray(mappedChild)) {
                var escapedChildKey = "";
                if (childKey != null) {
                  escapedChildKey = escapeUserProvidedKey(childKey) + "/";
                }
                mapIntoArray(mappedChild, array, escapedChildKey, "", function(c) {
                  return c;
                });
              } else if (mappedChild != null) {
                if (isValidElement(mappedChild)) {
                  {
                    if (mappedChild.key && (!_child || _child.key !== mappedChild.key)) {
                      checkKeyStringCoercion(mappedChild.key);
                    }
                  }
                  mappedChild = cloneAndReplaceKey(
                    mappedChild,
                    // Keep both the (mapped) and old keys if they differ, just as
                    // traverseAllChildren used to do for objects as children
                    escapedPrefix + // $FlowFixMe Flow incorrectly thinks React.Portal doesn't have a key
                    (mappedChild.key && (!_child || _child.key !== mappedChild.key) ? (
                      // $FlowFixMe Flow incorrectly thinks existing element's key can be a number
                      // eslint-disable-next-line react-internal/safe-string-coercion
                      escapeUserProvidedKey("" + mappedChild.key) + "/"
                    ) : "") + childKey
                  );
                }
                array.push(mappedChild);
              }
              return 1;
            }
            var child;
            var nextName;
            var subtreeCount = 0;
            var nextNamePrefix = nameSoFar === "" ? SEPARATOR : nameSoFar + SUBSEPARATOR;
            if (isArray(children)) {
              for (var i = 0; i < children.length; i++) {
                child = children[i];
                nextName = nextNamePrefix + getElementKey(child, i);
                subtreeCount += mapIntoArray(child, array, escapedPrefix, nextName, callback);
              }
            } else {
              var iteratorFn = getIteratorFn(children);
              if (typeof iteratorFn === "function") {
                var iterableChildren = children;
                {
                  if (iteratorFn === iterableChildren.entries) {
                    if (!didWarnAboutMaps) {
                      warn("Using Maps as children is not supported. Use an array of keyed ReactElements instead.");
                    }
                    didWarnAboutMaps = true;
                  }
                }
                var iterator = iteratorFn.call(iterableChildren);
                var step;
                var ii = 0;
                while (!(step = iterator.next()).done) {
                  child = step.value;
                  nextName = nextNamePrefix + getElementKey(child, ii++);
                  subtreeCount += mapIntoArray(child, array, escapedPrefix, nextName, callback);
                }
              } else if (type === "object") {
                var childrenString = String(children);
                throw new Error("Objects are not valid as a React child (found: " + (childrenString === "[object Object]" ? "object with keys {" + Object.keys(children).join(", ") + "}" : childrenString) + "). If you meant to render a collection of children, use an array instead.");
              }
            }
            return subtreeCount;
          }
          function mapChildren(children, func, context) {
            if (children == null) {
              return children;
            }
            var result = [];
            var count = 0;
            mapIntoArray(children, result, "", "", function(child) {
              return func.call(context, child, count++);
            });
            return result;
          }
          function countChildren(children) {
            var n = 0;
            mapChildren(children, function() {
              n++;
            });
            return n;
          }
          function forEachChildren(children, forEachFunc, forEachContext) {
            mapChildren(children, function() {
              forEachFunc.apply(this, arguments);
            }, forEachContext);
          }
          function toArray(children) {
            return mapChildren(children, function(child) {
              return child;
            }) || [];
          }
          function onlyChild(children) {
            if (!isValidElement(children)) {
              throw new Error("React.Children.only expected to receive a single React element child.");
            }
            return children;
          }
          function createContext(defaultValue) {
            var context = {
              $$typeof: REACT_CONTEXT_TYPE,
              // As a workaround to support multiple concurrent renderers, we categorize
              // some renderers as primary and others as secondary. We only expect
              // there to be two concurrent renderers at most: React Native (primary) and
              // Fabric (secondary); React DOM (primary) and React ART (secondary).
              // Secondary renderers store their context values on separate fields.
              _currentValue: defaultValue,
              _currentValue2: defaultValue,
              // Used to track how many concurrent renderers this context currently
              // supports within in a single renderer. Such as parallel server rendering.
              _threadCount: 0,
              // These are circular
              Provider: null,
              Consumer: null,
              // Add these to use same hidden class in VM as ServerContext
              _defaultValue: null,
              _globalName: null
            };
            context.Provider = {
              $$typeof: REACT_PROVIDER_TYPE,
              _context: context
            };
            var hasWarnedAboutUsingNestedContextConsumers = false;
            var hasWarnedAboutUsingConsumerProvider = false;
            var hasWarnedAboutDisplayNameOnConsumer = false;
            {
              var Consumer = {
                $$typeof: REACT_CONTEXT_TYPE,
                _context: context
              };
              Object.defineProperties(Consumer, {
                Provider: {
                  get: function() {
                    if (!hasWarnedAboutUsingConsumerProvider) {
                      hasWarnedAboutUsingConsumerProvider = true;
                      error("Rendering <Context.Consumer.Provider> is not supported and will be removed in a future major release. Did you mean to render <Context.Provider> instead?");
                    }
                    return context.Provider;
                  },
                  set: function(_Provider) {
                    context.Provider = _Provider;
                  }
                },
                _currentValue: {
                  get: function() {
                    return context._currentValue;
                  },
                  set: function(_currentValue) {
                    context._currentValue = _currentValue;
                  }
                },
                _currentValue2: {
                  get: function() {
                    return context._currentValue2;
                  },
                  set: function(_currentValue2) {
                    context._currentValue2 = _currentValue2;
                  }
                },
                _threadCount: {
                  get: function() {
                    return context._threadCount;
                  },
                  set: function(_threadCount) {
                    context._threadCount = _threadCount;
                  }
                },
                Consumer: {
                  get: function() {
                    if (!hasWarnedAboutUsingNestedContextConsumers) {
                      hasWarnedAboutUsingNestedContextConsumers = true;
                      error("Rendering <Context.Consumer.Consumer> is not supported and will be removed in a future major release. Did you mean to render <Context.Consumer> instead?");
                    }
                    return context.Consumer;
                  }
                },
                displayName: {
                  get: function() {
                    return context.displayName;
                  },
                  set: function(displayName) {
                    if (!hasWarnedAboutDisplayNameOnConsumer) {
                      warn("Setting `displayName` on Context.Consumer has no effect. You should set it directly on the context with Context.displayName = '%s'.", displayName);
                      hasWarnedAboutDisplayNameOnConsumer = true;
                    }
                  }
                }
              });
              context.Consumer = Consumer;
            }
            {
              context._currentRenderer = null;
              context._currentRenderer2 = null;
            }
            return context;
          }
          var Uninitialized = -1;
          var Pending = 0;
          var Resolved = 1;
          var Rejected = 2;
          function lazyInitializer(payload) {
            if (payload._status === Uninitialized) {
              var ctor = payload._result;
              var thenable = ctor();
              thenable.then(function(moduleObject2) {
                if (payload._status === Pending || payload._status === Uninitialized) {
                  var resolved = payload;
                  resolved._status = Resolved;
                  resolved._result = moduleObject2;
                }
              }, function(error2) {
                if (payload._status === Pending || payload._status === Uninitialized) {
                  var rejected = payload;
                  rejected._status = Rejected;
                  rejected._result = error2;
                }
              });
              if (payload._status === Uninitialized) {
                var pending = payload;
                pending._status = Pending;
                pending._result = thenable;
              }
            }
            if (payload._status === Resolved) {
              var moduleObject = payload._result;
              {
                if (moduleObject === void 0) {
                  error("lazy: Expected the result of a dynamic import() call. Instead received: %s\n\nYour code should look like: \n  const MyComponent = lazy(() => import('./MyComponent'))\n\nDid you accidentally put curly braces around the import?", moduleObject);
                }
              }
              {
                if (!("default" in moduleObject)) {
                  error("lazy: Expected the result of a dynamic import() call. Instead received: %s\n\nYour code should look like: \n  const MyComponent = lazy(() => import('./MyComponent'))", moduleObject);
                }
              }
              return moduleObject.default;
            } else {
              throw payload._result;
            }
          }
          function lazy(ctor) {
            var payload = {
              // We use these fields to store the result.
              _status: Uninitialized,
              _result: ctor
            };
            var lazyType = {
              $$typeof: REACT_LAZY_TYPE,
              _payload: payload,
              _init: lazyInitializer
            };
            {
              var defaultProps;
              var propTypes;
              Object.defineProperties(lazyType, {
                defaultProps: {
                  configurable: true,
                  get: function() {
                    return defaultProps;
                  },
                  set: function(newDefaultProps) {
                    error("React.lazy(...): It is not supported to assign `defaultProps` to a lazy component import. Either specify them where the component is defined, or create a wrapping component around it.");
                    defaultProps = newDefaultProps;
                    Object.defineProperty(lazyType, "defaultProps", {
                      enumerable: true
                    });
                  }
                },
                propTypes: {
                  configurable: true,
                  get: function() {
                    return propTypes;
                  },
                  set: function(newPropTypes) {
                    error("React.lazy(...): It is not supported to assign `propTypes` to a lazy component import. Either specify them where the component is defined, or create a wrapping component around it.");
                    propTypes = newPropTypes;
                    Object.defineProperty(lazyType, "propTypes", {
                      enumerable: true
                    });
                  }
                }
              });
            }
            return lazyType;
          }
          function forwardRef(render) {
            {
              if (render != null && render.$$typeof === REACT_MEMO_TYPE) {
                error("forwardRef requires a render function but received a `memo` component. Instead of forwardRef(memo(...)), use memo(forwardRef(...)).");
              } else if (typeof render !== "function") {
                error("forwardRef requires a render function but was given %s.", render === null ? "null" : typeof render);
              } else {
                if (render.length !== 0 && render.length !== 2) {
                  error("forwardRef render functions accept exactly two parameters: props and ref. %s", render.length === 1 ? "Did you forget to use the ref parameter?" : "Any additional parameter will be undefined.");
                }
              }
              if (render != null) {
                if (render.defaultProps != null || render.propTypes != null) {
                  error("forwardRef render functions do not support propTypes or defaultProps. Did you accidentally pass a React component?");
                }
              }
            }
            var elementType = {
              $$typeof: REACT_FORWARD_REF_TYPE,
              render
            };
            {
              var ownName;
              Object.defineProperty(elementType, "displayName", {
                enumerable: false,
                configurable: true,
                get: function() {
                  return ownName;
                },
                set: function(name) {
                  ownName = name;
                  if (!render.name && !render.displayName) {
                    render.displayName = name;
                  }
                }
              });
            }
            return elementType;
          }
          var REACT_MODULE_REFERENCE;
          {
            REACT_MODULE_REFERENCE = Symbol.for("react.module.reference");
          }
          function isValidElementType(type) {
            if (typeof type === "string" || typeof type === "function") {
              return true;
            }
            if (type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || enableDebugTracing || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || enableLegacyHidden || type === REACT_OFFSCREEN_TYPE || enableScopeAPI || enableCacheElement || enableTransitionTracing) {
              return true;
            }
            if (typeof type === "object" && type !== null) {
              if (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || // This needs to include all possible module reference object
              // types supported by any Flight configuration anywhere since
              // we don't know which Flight build this will end up being used
              // with.
              type.$$typeof === REACT_MODULE_REFERENCE || type.getModuleId !== void 0) {
                return true;
              }
            }
            return false;
          }
          function memo(type, compare) {
            {
              if (!isValidElementType(type)) {
                error("memo: The first argument must be a component. Instead received: %s", type === null ? "null" : typeof type);
              }
            }
            var elementType = {
              $$typeof: REACT_MEMO_TYPE,
              type,
              compare: compare === void 0 ? null : compare
            };
            {
              var ownName;
              Object.defineProperty(elementType, "displayName", {
                enumerable: false,
                configurable: true,
                get: function() {
                  return ownName;
                },
                set: function(name) {
                  ownName = name;
                  if (!type.name && !type.displayName) {
                    type.displayName = name;
                  }
                }
              });
            }
            return elementType;
          }
          function resolveDispatcher() {
            var dispatcher = ReactCurrentDispatcher.current;
            {
              if (dispatcher === null) {
                error("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://reactjs.org/link/invalid-hook-call for tips about how to debug and fix this problem.");
              }
            }
            return dispatcher;
          }
          function useContext(Context) {
            var dispatcher = resolveDispatcher();
            {
              if (Context._context !== void 0) {
                var realContext = Context._context;
                if (realContext.Consumer === Context) {
                  error("Calling useContext(Context.Consumer) is not supported, may cause bugs, and will be removed in a future major release. Did you mean to call useContext(Context) instead?");
                } else if (realContext.Provider === Context) {
                  error("Calling useContext(Context.Provider) is not supported. Did you mean to call useContext(Context) instead?");
                }
              }
            }
            return dispatcher.useContext(Context);
          }
          function useState2(initialState) {
            var dispatcher = resolveDispatcher();
            return dispatcher.useState(initialState);
          }
          function useReducer(reducer, initialArg, init) {
            var dispatcher = resolveDispatcher();
            return dispatcher.useReducer(reducer, initialArg, init);
          }
          function useRef2(initialValue) {
            var dispatcher = resolveDispatcher();
            return dispatcher.useRef(initialValue);
          }
          function useEffect2(create, deps) {
            var dispatcher = resolveDispatcher();
            return dispatcher.useEffect(create, deps);
          }
          function useInsertionEffect(create, deps) {
            var dispatcher = resolveDispatcher();
            return dispatcher.useInsertionEffect(create, deps);
          }
          function useLayoutEffect(create, deps) {
            var dispatcher = resolveDispatcher();
            return dispatcher.useLayoutEffect(create, deps);
          }
          function useCallback(callback, deps) {
            var dispatcher = resolveDispatcher();
            return dispatcher.useCallback(callback, deps);
          }
          function useMemo2(create, deps) {
            var dispatcher = resolveDispatcher();
            return dispatcher.useMemo(create, deps);
          }
          function useImperativeHandle(ref, create, deps) {
            var dispatcher = resolveDispatcher();
            return dispatcher.useImperativeHandle(ref, create, deps);
          }
          function useDebugValue(value, formatterFn) {
            {
              var dispatcher = resolveDispatcher();
              return dispatcher.useDebugValue(value, formatterFn);
            }
          }
          function useTransition() {
            var dispatcher = resolveDispatcher();
            return dispatcher.useTransition();
          }
          function useDeferredValue(value) {
            var dispatcher = resolveDispatcher();
            return dispatcher.useDeferredValue(value);
          }
          function useId() {
            var dispatcher = resolveDispatcher();
            return dispatcher.useId();
          }
          function useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
            var dispatcher = resolveDispatcher();
            return dispatcher.useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
          }
          var disabledDepth = 0;
          var prevLog;
          var prevInfo;
          var prevWarn;
          var prevError;
          var prevGroup;
          var prevGroupCollapsed;
          var prevGroupEnd;
          function disabledLog() {
          }
          disabledLog.__reactDisabledLog = true;
          function disableLogs() {
            {
              if (disabledDepth === 0) {
                prevLog = console.log;
                prevInfo = console.info;
                prevWarn = console.warn;
                prevError = console.error;
                prevGroup = console.group;
                prevGroupCollapsed = console.groupCollapsed;
                prevGroupEnd = console.groupEnd;
                var props = {
                  configurable: true,
                  enumerable: true,
                  value: disabledLog,
                  writable: true
                };
                Object.defineProperties(console, {
                  info: props,
                  log: props,
                  warn: props,
                  error: props,
                  group: props,
                  groupCollapsed: props,
                  groupEnd: props
                });
              }
              disabledDepth++;
            }
          }
          function reenableLogs() {
            {
              disabledDepth--;
              if (disabledDepth === 0) {
                var props = {
                  configurable: true,
                  enumerable: true,
                  writable: true
                };
                Object.defineProperties(console, {
                  log: assign({}, props, {
                    value: prevLog
                  }),
                  info: assign({}, props, {
                    value: prevInfo
                  }),
                  warn: assign({}, props, {
                    value: prevWarn
                  }),
                  error: assign({}, props, {
                    value: prevError
                  }),
                  group: assign({}, props, {
                    value: prevGroup
                  }),
                  groupCollapsed: assign({}, props, {
                    value: prevGroupCollapsed
                  }),
                  groupEnd: assign({}, props, {
                    value: prevGroupEnd
                  })
                });
              }
              if (disabledDepth < 0) {
                error("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
              }
            }
          }
          var ReactCurrentDispatcher$1 = ReactSharedInternals.ReactCurrentDispatcher;
          var prefix;
          function describeBuiltInComponentFrame(name, source, ownerFn) {
            {
              if (prefix === void 0) {
                try {
                  throw Error();
                } catch (x) {
                  var match = x.stack.trim().match(/\n( *(at )?)/);
                  prefix = match && match[1] || "";
                }
              }
              return "\n" + prefix + name;
            }
          }
          var reentry = false;
          var componentFrameCache;
          {
            var PossiblyWeakMap = typeof WeakMap === "function" ? WeakMap : Map;
            componentFrameCache = new PossiblyWeakMap();
          }
          function describeNativeComponentFrame(fn, construct) {
            if (!fn || reentry) {
              return "";
            }
            {
              var frame = componentFrameCache.get(fn);
              if (frame !== void 0) {
                return frame;
              }
            }
            var control;
            reentry = true;
            var previousPrepareStackTrace = Error.prepareStackTrace;
            Error.prepareStackTrace = void 0;
            var previousDispatcher;
            {
              previousDispatcher = ReactCurrentDispatcher$1.current;
              ReactCurrentDispatcher$1.current = null;
              disableLogs();
            }
            try {
              if (construct) {
                var Fake = function() {
                  throw Error();
                };
                Object.defineProperty(Fake.prototype, "props", {
                  set: function() {
                    throw Error();
                  }
                });
                if (typeof Reflect === "object" && Reflect.construct) {
                  try {
                    Reflect.construct(Fake, []);
                  } catch (x) {
                    control = x;
                  }
                  Reflect.construct(fn, [], Fake);
                } else {
                  try {
                    Fake.call();
                  } catch (x) {
                    control = x;
                  }
                  fn.call(Fake.prototype);
                }
              } else {
                try {
                  throw Error();
                } catch (x) {
                  control = x;
                }
                fn();
              }
            } catch (sample) {
              if (sample && control && typeof sample.stack === "string") {
                var sampleLines = sample.stack.split("\n");
                var controlLines = control.stack.split("\n");
                var s = sampleLines.length - 1;
                var c = controlLines.length - 1;
                while (s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c]) {
                  c--;
                }
                for (; s >= 1 && c >= 0; s--, c--) {
                  if (sampleLines[s] !== controlLines[c]) {
                    if (s !== 1 || c !== 1) {
                      do {
                        s--;
                        c--;
                        if (c < 0 || sampleLines[s] !== controlLines[c]) {
                          var _frame = "\n" + sampleLines[s].replace(" at new ", " at ");
                          if (fn.displayName && _frame.includes("<anonymous>")) {
                            _frame = _frame.replace("<anonymous>", fn.displayName);
                          }
                          {
                            if (typeof fn === "function") {
                              componentFrameCache.set(fn, _frame);
                            }
                          }
                          return _frame;
                        }
                      } while (s >= 1 && c >= 0);
                    }
                    break;
                  }
                }
              }
            } finally {
              reentry = false;
              {
                ReactCurrentDispatcher$1.current = previousDispatcher;
                reenableLogs();
              }
              Error.prepareStackTrace = previousPrepareStackTrace;
            }
            var name = fn ? fn.displayName || fn.name : "";
            var syntheticFrame = name ? describeBuiltInComponentFrame(name) : "";
            {
              if (typeof fn === "function") {
                componentFrameCache.set(fn, syntheticFrame);
              }
            }
            return syntheticFrame;
          }
          function describeFunctionComponentFrame(fn, source, ownerFn) {
            {
              return describeNativeComponentFrame(fn, false);
            }
          }
          function shouldConstruct(Component2) {
            var prototype = Component2.prototype;
            return !!(prototype && prototype.isReactComponent);
          }
          function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {
            if (type == null) {
              return "";
            }
            if (typeof type === "function") {
              {
                return describeNativeComponentFrame(type, shouldConstruct(type));
              }
            }
            if (typeof type === "string") {
              return describeBuiltInComponentFrame(type);
            }
            switch (type) {
              case REACT_SUSPENSE_TYPE:
                return describeBuiltInComponentFrame("Suspense");
              case REACT_SUSPENSE_LIST_TYPE:
                return describeBuiltInComponentFrame("SuspenseList");
            }
            if (typeof type === "object") {
              switch (type.$$typeof) {
                case REACT_FORWARD_REF_TYPE:
                  return describeFunctionComponentFrame(type.render);
                case REACT_MEMO_TYPE:
                  return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);
                case REACT_LAZY_TYPE: {
                  var lazyComponent = type;
                  var payload = lazyComponent._payload;
                  var init = lazyComponent._init;
                  try {
                    return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
                  } catch (x) {
                  }
                }
              }
            }
            return "";
          }
          var loggedTypeFailures = {};
          var ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
          function setCurrentlyValidatingElement(element) {
            {
              if (element) {
                var owner = element._owner;
                var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
                ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
              } else {
                ReactDebugCurrentFrame$1.setExtraStackFrame(null);
              }
            }
          }
          function checkPropTypes(typeSpecs, values, location, componentName, element) {
            {
              var has = Function.call.bind(hasOwnProperty);
              for (var typeSpecName in typeSpecs) {
                if (has(typeSpecs, typeSpecName)) {
                  var error$1 = void 0;
                  try {
                    if (typeof typeSpecs[typeSpecName] !== "function") {
                      var err = Error((componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
                      err.name = "Invariant Violation";
                      throw err;
                    }
                    error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
                  } catch (ex) {
                    error$1 = ex;
                  }
                  if (error$1 && !(error$1 instanceof Error)) {
                    setCurrentlyValidatingElement(element);
                    error("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", componentName || "React class", location, typeSpecName, typeof error$1);
                    setCurrentlyValidatingElement(null);
                  }
                  if (error$1 instanceof Error && !(error$1.message in loggedTypeFailures)) {
                    loggedTypeFailures[error$1.message] = true;
                    setCurrentlyValidatingElement(element);
                    error("Failed %s type: %s", location, error$1.message);
                    setCurrentlyValidatingElement(null);
                  }
                }
              }
            }
          }
          function setCurrentlyValidatingElement$1(element) {
            {
              if (element) {
                var owner = element._owner;
                var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
                setExtraStackFrame(stack);
              } else {
                setExtraStackFrame(null);
              }
            }
          }
          var propTypesMisspellWarningShown;
          {
            propTypesMisspellWarningShown = false;
          }
          function getDeclarationErrorAddendum() {
            if (ReactCurrentOwner.current) {
              var name = getComponentNameFromType(ReactCurrentOwner.current.type);
              if (name) {
                return "\n\nCheck the render method of `" + name + "`.";
              }
            }
            return "";
          }
          function getSourceInfoErrorAddendum(source) {
            if (source !== void 0) {
              var fileName = source.fileName.replace(/^.*[\\\/]/, "");
              var lineNumber = source.lineNumber;
              return "\n\nCheck your code at " + fileName + ":" + lineNumber + ".";
            }
            return "";
          }
          function getSourceInfoErrorAddendumForProps(elementProps) {
            if (elementProps !== null && elementProps !== void 0) {
              return getSourceInfoErrorAddendum(elementProps.__source);
            }
            return "";
          }
          var ownerHasKeyUseWarning = {};
          function getCurrentComponentErrorInfo(parentType) {
            var info = getDeclarationErrorAddendum();
            if (!info) {
              var parentName = typeof parentType === "string" ? parentType : parentType.displayName || parentType.name;
              if (parentName) {
                info = "\n\nCheck the top-level render call using <" + parentName + ">.";
              }
            }
            return info;
          }
          function validateExplicitKey(element, parentType) {
            if (!element._store || element._store.validated || element.key != null) {
              return;
            }
            element._store.validated = true;
            var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);
            if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
              return;
            }
            ownerHasKeyUseWarning[currentComponentErrorInfo] = true;
            var childOwner = "";
            if (element && element._owner && element._owner !== ReactCurrentOwner.current) {
              childOwner = " It was passed a child from " + getComponentNameFromType(element._owner.type) + ".";
            }
            {
              setCurrentlyValidatingElement$1(element);
              error('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', currentComponentErrorInfo, childOwner);
              setCurrentlyValidatingElement$1(null);
            }
          }
          function validateChildKeys(node, parentType) {
            if (typeof node !== "object") {
              return;
            }
            if (isArray(node)) {
              for (var i = 0; i < node.length; i++) {
                var child = node[i];
                if (isValidElement(child)) {
                  validateExplicitKey(child, parentType);
                }
              }
            } else if (isValidElement(node)) {
              if (node._store) {
                node._store.validated = true;
              }
            } else if (node) {
              var iteratorFn = getIteratorFn(node);
              if (typeof iteratorFn === "function") {
                if (iteratorFn !== node.entries) {
                  var iterator = iteratorFn.call(node);
                  var step;
                  while (!(step = iterator.next()).done) {
                    if (isValidElement(step.value)) {
                      validateExplicitKey(step.value, parentType);
                    }
                  }
                }
              }
            }
          }
          function validatePropTypes(element) {
            {
              var type = element.type;
              if (type === null || type === void 0 || typeof type === "string") {
                return;
              }
              var propTypes;
              if (typeof type === "function") {
                propTypes = type.propTypes;
              } else if (typeof type === "object" && (type.$$typeof === REACT_FORWARD_REF_TYPE || // Note: Memo only checks outer props here.
              // Inner props are checked in the reconciler.
              type.$$typeof === REACT_MEMO_TYPE)) {
                propTypes = type.propTypes;
              } else {
                return;
              }
              if (propTypes) {
                var name = getComponentNameFromType(type);
                checkPropTypes(propTypes, element.props, "prop", name, element);
              } else if (type.PropTypes !== void 0 && !propTypesMisspellWarningShown) {
                propTypesMisspellWarningShown = true;
                var _name = getComponentNameFromType(type);
                error("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", _name || "Unknown");
              }
              if (typeof type.getDefaultProps === "function" && !type.getDefaultProps.isReactClassApproved) {
                error("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
              }
            }
          }
          function validateFragmentProps(fragment) {
            {
              var keys = Object.keys(fragment.props);
              for (var i = 0; i < keys.length; i++) {
                var key = keys[i];
                if (key !== "children" && key !== "key") {
                  setCurrentlyValidatingElement$1(fragment);
                  error("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", key);
                  setCurrentlyValidatingElement$1(null);
                  break;
                }
              }
              if (fragment.ref !== null) {
                setCurrentlyValidatingElement$1(fragment);
                error("Invalid attribute `ref` supplied to `React.Fragment`.");
                setCurrentlyValidatingElement$1(null);
              }
            }
          }
          function createElementWithValidation(type, props, children) {
            var validType = isValidElementType(type);
            if (!validType) {
              var info = "";
              if (type === void 0 || typeof type === "object" && type !== null && Object.keys(type).length === 0) {
                info += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.";
              }
              var sourceInfo = getSourceInfoErrorAddendumForProps(props);
              if (sourceInfo) {
                info += sourceInfo;
              } else {
                info += getDeclarationErrorAddendum();
              }
              var typeString;
              if (type === null) {
                typeString = "null";
              } else if (isArray(type)) {
                typeString = "array";
              } else if (type !== void 0 && type.$$typeof === REACT_ELEMENT_TYPE) {
                typeString = "<" + (getComponentNameFromType(type.type) || "Unknown") + " />";
                info = " Did you accidentally export a JSX literal instead of a component?";
              } else {
                typeString = typeof type;
              }
              {
                error("React.createElement: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", typeString, info);
              }
            }
            var element = createElement.apply(this, arguments);
            if (element == null) {
              return element;
            }
            if (validType) {
              for (var i = 2; i < arguments.length; i++) {
                validateChildKeys(arguments[i], type);
              }
            }
            if (type === REACT_FRAGMENT_TYPE) {
              validateFragmentProps(element);
            } else {
              validatePropTypes(element);
            }
            return element;
          }
          var didWarnAboutDeprecatedCreateFactory = false;
          function createFactoryWithValidation(type) {
            var validatedFactory = createElementWithValidation.bind(null, type);
            validatedFactory.type = type;
            {
              if (!didWarnAboutDeprecatedCreateFactory) {
                didWarnAboutDeprecatedCreateFactory = true;
                warn("React.createFactory() is deprecated and will be removed in a future major release. Consider using JSX or use React.createElement() directly instead.");
              }
              Object.defineProperty(validatedFactory, "type", {
                enumerable: false,
                get: function() {
                  warn("Factory.type is deprecated. Access the class directly before passing it to createFactory.");
                  Object.defineProperty(this, "type", {
                    value: type
                  });
                  return type;
                }
              });
            }
            return validatedFactory;
          }
          function cloneElementWithValidation(element, props, children) {
            var newElement = cloneElement.apply(this, arguments);
            for (var i = 2; i < arguments.length; i++) {
              validateChildKeys(arguments[i], newElement.type);
            }
            validatePropTypes(newElement);
            return newElement;
          }
          function startTransition(scope, options) {
            var prevTransition = ReactCurrentBatchConfig.transition;
            ReactCurrentBatchConfig.transition = {};
            var currentTransition = ReactCurrentBatchConfig.transition;
            {
              ReactCurrentBatchConfig.transition._updatedFibers = /* @__PURE__ */ new Set();
            }
            try {
              scope();
            } finally {
              ReactCurrentBatchConfig.transition = prevTransition;
              {
                if (prevTransition === null && currentTransition._updatedFibers) {
                  var updatedFibersCount = currentTransition._updatedFibers.size;
                  if (updatedFibersCount > 10) {
                    warn("Detected a large number of updates inside startTransition. If this is due to a subscription please re-write it to use React provided hooks. Otherwise concurrent mode guarantees are off the table.");
                  }
                  currentTransition._updatedFibers.clear();
                }
              }
            }
          }
          var didWarnAboutMessageChannel = false;
          var enqueueTaskImpl = null;
          function enqueueTask(task) {
            if (enqueueTaskImpl === null) {
              try {
                var requireString = ("require" + Math.random()).slice(0, 7);
                var nodeRequire = module && module[requireString];
                enqueueTaskImpl = nodeRequire.call(module, "timers").setImmediate;
              } catch (_err) {
                enqueueTaskImpl = function(callback) {
                  {
                    if (didWarnAboutMessageChannel === false) {
                      didWarnAboutMessageChannel = true;
                      if (typeof MessageChannel === "undefined") {
                        error("This browser does not have a MessageChannel implementation, so enqueuing tasks via await act(async () => ...) will fail. Please file an issue at https://github.com/facebook/react/issues if you encounter this warning.");
                      }
                    }
                  }
                  var channel = new MessageChannel();
                  channel.port1.onmessage = callback;
                  channel.port2.postMessage(void 0);
                };
              }
            }
            return enqueueTaskImpl(task);
          }
          var actScopeDepth = 0;
          var didWarnNoAwaitAct = false;
          function act(callback) {
            {
              var prevActScopeDepth = actScopeDepth;
              actScopeDepth++;
              if (ReactCurrentActQueue.current === null) {
                ReactCurrentActQueue.current = [];
              }
              var prevIsBatchingLegacy = ReactCurrentActQueue.isBatchingLegacy;
              var result;
              try {
                ReactCurrentActQueue.isBatchingLegacy = true;
                result = callback();
                if (!prevIsBatchingLegacy && ReactCurrentActQueue.didScheduleLegacyUpdate) {
                  var queue = ReactCurrentActQueue.current;
                  if (queue !== null) {
                    ReactCurrentActQueue.didScheduleLegacyUpdate = false;
                    flushActQueue(queue);
                  }
                }
              } catch (error2) {
                popActScope(prevActScopeDepth);
                throw error2;
              } finally {
                ReactCurrentActQueue.isBatchingLegacy = prevIsBatchingLegacy;
              }
              if (result !== null && typeof result === "object" && typeof result.then === "function") {
                var thenableResult = result;
                var wasAwaited = false;
                var thenable = {
                  then: function(resolve, reject) {
                    wasAwaited = true;
                    thenableResult.then(function(returnValue2) {
                      popActScope(prevActScopeDepth);
                      if (actScopeDepth === 0) {
                        recursivelyFlushAsyncActWork(returnValue2, resolve, reject);
                      } else {
                        resolve(returnValue2);
                      }
                    }, function(error2) {
                      popActScope(prevActScopeDepth);
                      reject(error2);
                    });
                  }
                };
                {
                  if (!didWarnNoAwaitAct && typeof Promise !== "undefined") {
                    Promise.resolve().then(function() {
                    }).then(function() {
                      if (!wasAwaited) {
                        didWarnNoAwaitAct = true;
                        error("You called act(async () => ...) without await. This could lead to unexpected testing behaviour, interleaving multiple act calls and mixing their scopes. You should - await act(async () => ...);");
                      }
                    });
                  }
                }
                return thenable;
              } else {
                var returnValue = result;
                popActScope(prevActScopeDepth);
                if (actScopeDepth === 0) {
                  var _queue = ReactCurrentActQueue.current;
                  if (_queue !== null) {
                    flushActQueue(_queue);
                    ReactCurrentActQueue.current = null;
                  }
                  var _thenable = {
                    then: function(resolve, reject) {
                      if (ReactCurrentActQueue.current === null) {
                        ReactCurrentActQueue.current = [];
                        recursivelyFlushAsyncActWork(returnValue, resolve, reject);
                      } else {
                        resolve(returnValue);
                      }
                    }
                  };
                  return _thenable;
                } else {
                  var _thenable2 = {
                    then: function(resolve, reject) {
                      resolve(returnValue);
                    }
                  };
                  return _thenable2;
                }
              }
            }
          }
          function popActScope(prevActScopeDepth) {
            {
              if (prevActScopeDepth !== actScopeDepth - 1) {
                error("You seem to have overlapping act() calls, this is not supported. Be sure to await previous act() calls before making a new one. ");
              }
              actScopeDepth = prevActScopeDepth;
            }
          }
          function recursivelyFlushAsyncActWork(returnValue, resolve, reject) {
            {
              var queue = ReactCurrentActQueue.current;
              if (queue !== null) {
                try {
                  flushActQueue(queue);
                  enqueueTask(function() {
                    if (queue.length === 0) {
                      ReactCurrentActQueue.current = null;
                      resolve(returnValue);
                    } else {
                      recursivelyFlushAsyncActWork(returnValue, resolve, reject);
                    }
                  });
                } catch (error2) {
                  reject(error2);
                }
              } else {
                resolve(returnValue);
              }
            }
          }
          var isFlushing = false;
          function flushActQueue(queue) {
            {
              if (!isFlushing) {
                isFlushing = true;
                var i = 0;
                try {
                  for (; i < queue.length; i++) {
                    var callback = queue[i];
                    do {
                      callback = callback(true);
                    } while (callback !== null);
                  }
                  queue.length = 0;
                } catch (error2) {
                  queue = queue.slice(i + 1);
                  throw error2;
                } finally {
                  isFlushing = false;
                }
              }
            }
          }
          var createElement$1 = createElementWithValidation;
          var cloneElement$1 = cloneElementWithValidation;
          var createFactory = createFactoryWithValidation;
          var Children = {
            map: mapChildren,
            forEach: forEachChildren,
            count: countChildren,
            toArray,
            only: onlyChild
          };
          exports.Children = Children;
          exports.Component = Component;
          exports.Fragment = REACT_FRAGMENT_TYPE;
          exports.Profiler = REACT_PROFILER_TYPE;
          exports.PureComponent = PureComponent;
          exports.StrictMode = REACT_STRICT_MODE_TYPE;
          exports.Suspense = REACT_SUSPENSE_TYPE;
          exports.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = ReactSharedInternals;
          exports.act = act;
          exports.cloneElement = cloneElement$1;
          exports.createContext = createContext;
          exports.createElement = createElement$1;
          exports.createFactory = createFactory;
          exports.createRef = createRef;
          exports.forwardRef = forwardRef;
          exports.isValidElement = isValidElement;
          exports.lazy = lazy;
          exports.memo = memo;
          exports.startTransition = startTransition;
          exports.unstable_act = act;
          exports.useCallback = useCallback;
          exports.useContext = useContext;
          exports.useDebugValue = useDebugValue;
          exports.useDeferredValue = useDeferredValue;
          exports.useEffect = useEffect2;
          exports.useId = useId;
          exports.useImperativeHandle = useImperativeHandle;
          exports.useInsertionEffect = useInsertionEffect;
          exports.useLayoutEffect = useLayoutEffect;
          exports.useMemo = useMemo2;
          exports.useReducer = useReducer;
          exports.useRef = useRef2;
          exports.useState = useState2;
          exports.useSyncExternalStore = useSyncExternalStore;
          exports.useTransition = useTransition;
          exports.version = ReactVersion;
          if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop === "function") {
            __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(new Error());
          }
        })();
      }
    }
  });

  // node_modules/react/index.js
  var require_react = __commonJS({
    "node_modules/react/index.js"(exports, module) {
      "use strict";
      if (false) {
        module.exports = null;
      } else {
        module.exports = require_react_development();
      }
    }
  });

  // node_modules/scheduler/cjs/scheduler.development.js
  var require_scheduler_development = __commonJS({
    "node_modules/scheduler/cjs/scheduler.development.js"(exports) {
      "use strict";
      if (true) {
        (function() {
          "use strict";
          if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart === "function") {
            __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(new Error());
          }
          var enableSchedulerDebugging = false;
          var enableProfiling = false;
          var frameYieldMs = 5;
          function push(heap, node) {
            var index = heap.length;
            heap.push(node);
            siftUp(heap, node, index);
          }
          function peek(heap) {
            return heap.length === 0 ? null : heap[0];
          }
          function pop(heap) {
            if (heap.length === 0) {
              return null;
            }
            var first = heap[0];
            var last = heap.pop();
            if (last !== first) {
              heap[0] = last;
              siftDown(heap, last, 0);
            }
            return first;
          }
          function siftUp(heap, node, i) {
            var index = i;
            while (index > 0) {
              var parentIndex = index - 1 >>> 1;
              var parent = heap[parentIndex];
              if (compare(parent, node) > 0) {
                heap[parentIndex] = node;
                heap[index] = parent;
                index = parentIndex;
              } else {
                return;
              }
            }
          }
          function siftDown(heap, node, i) {
            var index = i;
            var length = heap.length;
            var halfLength = length >>> 1;
            while (index < halfLength) {
              var leftIndex = (index + 1) * 2 - 1;
              var left = heap[leftIndex];
              var rightIndex = leftIndex + 1;
              var right = heap[rightIndex];
              if (compare(left, node) < 0) {
                if (rightIndex < length && compare(right, left) < 0) {
                  heap[index] = right;
                  heap[rightIndex] = node;
                  index = rightIndex;
                } else {
                  heap[index] = left;
                  heap[leftIndex] = node;
                  index = leftIndex;
                }
              } else if (rightIndex < length && compare(right, node) < 0) {
                heap[index] = right;
                heap[rightIndex] = node;
                index = rightIndex;
              } else {
                return;
              }
            }
          }
          function compare(a, b) {
            var diff = a.sortIndex - b.sortIndex;
            return diff !== 0 ? diff : a.id - b.id;
          }
          var ImmediatePriority = 1;
          var UserBlockingPriority = 2;
          var NormalPriority = 3;
          var LowPriority = 4;
          var IdlePriority = 5;
          function markTaskErrored(task, ms) {
          }
          var hasPerformanceNow = typeof performance === "object" && typeof performance.now === "function";
          if (hasPerformanceNow) {
            var localPerformance = performance;
            exports.unstable_now = function() {
              return localPerformance.now();
            };
          } else {
            var localDate = Date;
            var initialTime = localDate.now();
            exports.unstable_now = function() {
              return localDate.now() - initialTime;
            };
          }
          var maxSigned31BitInt = 1073741823;
          var IMMEDIATE_PRIORITY_TIMEOUT = -1;
          var USER_BLOCKING_PRIORITY_TIMEOUT = 250;
          var NORMAL_PRIORITY_TIMEOUT = 5e3;
          var LOW_PRIORITY_TIMEOUT = 1e4;
          var IDLE_PRIORITY_TIMEOUT = maxSigned31BitInt;
          var taskQueue = [];
          var timerQueue = [];
          var taskIdCounter = 1;
          var currentTask = null;
          var currentPriorityLevel = NormalPriority;
          var isPerformingWork = false;
          var isHostCallbackScheduled = false;
          var isHostTimeoutScheduled = false;
          var localSetTimeout = typeof setTimeout === "function" ? setTimeout : null;
          var localClearTimeout = typeof clearTimeout === "function" ? clearTimeout : null;
          var localSetImmediate = typeof setImmediate !== "undefined" ? setImmediate : null;
          var isInputPending = typeof navigator !== "undefined" && navigator.scheduling !== void 0 && navigator.scheduling.isInputPending !== void 0 ? navigator.scheduling.isInputPending.bind(navigator.scheduling) : null;
          function advanceTimers(currentTime) {
            var timer = peek(timerQueue);
            while (timer !== null) {
              if (timer.callback === null) {
                pop(timerQueue);
              } else if (timer.startTime <= currentTime) {
                pop(timerQueue);
                timer.sortIndex = timer.expirationTime;
                push(taskQueue, timer);
              } else {
                return;
              }
              timer = peek(timerQueue);
            }
          }
          function handleTimeout(currentTime) {
            isHostTimeoutScheduled = false;
            advanceTimers(currentTime);
            if (!isHostCallbackScheduled) {
              if (peek(taskQueue) !== null) {
                isHostCallbackScheduled = true;
                requestHostCallback(flushWork);
              } else {
                var firstTimer = peek(timerQueue);
                if (firstTimer !== null) {
                  requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
                }
              }
            }
          }
          function flushWork(hasTimeRemaining, initialTime2) {
            isHostCallbackScheduled = false;
            if (isHostTimeoutScheduled) {
              isHostTimeoutScheduled = false;
              cancelHostTimeout();
            }
            isPerformingWork = true;
            var previousPriorityLevel = currentPriorityLevel;
            try {
              if (enableProfiling) {
                try {
                  return workLoop(hasTimeRemaining, initialTime2);
                } catch (error) {
                  if (currentTask !== null) {
                    var currentTime = exports.unstable_now();
                    markTaskErrored(currentTask, currentTime);
                    currentTask.isQueued = false;
                  }
                  throw error;
                }
              } else {
                return workLoop(hasTimeRemaining, initialTime2);
              }
            } finally {
              currentTask = null;
              currentPriorityLevel = previousPriorityLevel;
              isPerformingWork = false;
            }
          }
          function workLoop(hasTimeRemaining, initialTime2) {
            var currentTime = initialTime2;
            advanceTimers(currentTime);
            currentTask = peek(taskQueue);
            while (currentTask !== null && !enableSchedulerDebugging) {
              if (currentTask.expirationTime > currentTime && (!hasTimeRemaining || shouldYieldToHost())) {
                break;
              }
              var callback = currentTask.callback;
              if (typeof callback === "function") {
                currentTask.callback = null;
                currentPriorityLevel = currentTask.priorityLevel;
                var didUserCallbackTimeout = currentTask.expirationTime <= currentTime;
                var continuationCallback = callback(didUserCallbackTimeout);
                currentTime = exports.unstable_now();
                if (typeof continuationCallback === "function") {
                  currentTask.callback = continuationCallback;
                } else {
                  if (currentTask === peek(taskQueue)) {
                    pop(taskQueue);
                  }
                }
                advanceTimers(currentTime);
              } else {
                pop(taskQueue);
              }
              currentTask = peek(taskQueue);
            }
            if (currentTask !== null) {
              return true;
            } else {
              var firstTimer = peek(timerQueue);
              if (firstTimer !== null) {
                requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
              }
              return false;
            }
          }
          function unstable_runWithPriority(priorityLevel, eventHandler) {
            switch (priorityLevel) {
              case ImmediatePriority:
              case UserBlockingPriority:
              case NormalPriority:
              case LowPriority:
              case IdlePriority:
                break;
              default:
                priorityLevel = NormalPriority;
            }
            var previousPriorityLevel = currentPriorityLevel;
            currentPriorityLevel = priorityLevel;
            try {
              return eventHandler();
            } finally {
              currentPriorityLevel = previousPriorityLevel;
            }
          }
          function unstable_next(eventHandler) {
            var priorityLevel;
            switch (currentPriorityLevel) {
              case ImmediatePriority:
              case UserBlockingPriority:
              case NormalPriority:
                priorityLevel = NormalPriority;
                break;
              default:
                priorityLevel = currentPriorityLevel;
                break;
            }
            var previousPriorityLevel = currentPriorityLevel;
            currentPriorityLevel = priorityLevel;
            try {
              return eventHandler();
            } finally {
              currentPriorityLevel = previousPriorityLevel;
            }
          }
          function unstable_wrapCallback(callback) {
            var parentPriorityLevel = currentPriorityLevel;
            return function() {
              var previousPriorityLevel = currentPriorityLevel;
              currentPriorityLevel = parentPriorityLevel;
              try {
                return callback.apply(this, arguments);
              } finally {
                currentPriorityLevel = previousPriorityLevel;
              }
            };
          }
          function unstable_scheduleCallback(priorityLevel, callback, options) {
            var currentTime = exports.unstable_now();
            var startTime2;
            if (typeof options === "object" && options !== null) {
              var delay = options.delay;
              if (typeof delay === "number" && delay > 0) {
                startTime2 = currentTime + delay;
              } else {
                startTime2 = currentTime;
              }
            } else {
              startTime2 = currentTime;
            }
            var timeout;
            switch (priorityLevel) {
              case ImmediatePriority:
                timeout = IMMEDIATE_PRIORITY_TIMEOUT;
                break;
              case UserBlockingPriority:
                timeout = USER_BLOCKING_PRIORITY_TIMEOUT;
                break;
              case IdlePriority:
                timeout = IDLE_PRIORITY_TIMEOUT;
                break;
              case LowPriority:
                timeout = LOW_PRIORITY_TIMEOUT;
                break;
              case NormalPriority:
              default:
                timeout = NORMAL_PRIORITY_TIMEOUT;
                break;
            }
            var expirationTime = startTime2 + timeout;
            var newTask = {
              id: taskIdCounter++,
              callback,
              priorityLevel,
              startTime: startTime2,
              expirationTime,
              sortIndex: -1
            };
            if (startTime2 > currentTime) {
              newTask.sortIndex = startTime2;
              push(timerQueue, newTask);
              if (peek(taskQueue) === null && newTask === peek(timerQueue)) {
                if (isHostTimeoutScheduled) {
                  cancelHostTimeout();
                } else {
                  isHostTimeoutScheduled = true;
                }
                requestHostTimeout(handleTimeout, startTime2 - currentTime);
              }
            } else {
              newTask.sortIndex = expirationTime;
              push(taskQueue, newTask);
              if (!isHostCallbackScheduled && !isPerformingWork) {
                isHostCallbackScheduled = true;
                requestHostCallback(flushWork);
              }
            }
            return newTask;
          }
          function unstable_pauseExecution() {
          }
          function unstable_continueExecution() {
            if (!isHostCallbackScheduled && !isPerformingWork) {
              isHostCallbackScheduled = true;
              requestHostCallback(flushWork);
            }
          }
          function unstable_getFirstCallbackNode() {
            return peek(taskQueue);
          }
          function unstable_cancelCallback(task) {
            task.callback = null;
          }
          function unstable_getCurrentPriorityLevel() {
            return currentPriorityLevel;
          }
          var isMessageLoopRunning = false;
          var scheduledHostCallback = null;
          var taskTimeoutID = -1;
          var frameInterval = frameYieldMs;
          var startTime = -1;
          function shouldYieldToHost() {
            var timeElapsed = exports.unstable_now() - startTime;
            if (timeElapsed < frameInterval) {
              return false;
            }
            return true;
          }
          function requestPaint() {
          }
          function forceFrameRate(fps) {
            if (fps < 0 || fps > 125) {
              console["error"]("forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported");
              return;
            }
            if (fps > 0) {
              frameInterval = Math.floor(1e3 / fps);
            } else {
              frameInterval = frameYieldMs;
            }
          }
          var performWorkUntilDeadline = function() {
            if (scheduledHostCallback !== null) {
              var currentTime = exports.unstable_now();
              startTime = currentTime;
              var hasTimeRemaining = true;
              var hasMoreWork = true;
              try {
                hasMoreWork = scheduledHostCallback(hasTimeRemaining, currentTime);
              } finally {
                if (hasMoreWork) {
                  schedulePerformWorkUntilDeadline();
                } else {
                  isMessageLoopRunning = false;
                  scheduledHostCallback = null;
                }
              }
            } else {
              isMessageLoopRunning = false;
            }
          };
          var schedulePerformWorkUntilDeadline;
          if (typeof localSetImmediate === "function") {
            schedulePerformWorkUntilDeadline = function() {
              localSetImmediate(performWorkUntilDeadline);
            };
          } else if (typeof MessageChannel !== "undefined") {
            var channel = new MessageChannel();
            var port = channel.port2;
            channel.port1.onmessage = performWorkUntilDeadline;
            schedulePerformWorkUntilDeadline = function() {
              port.postMessage(null);
            };
          } else {
            schedulePerformWorkUntilDeadline = function() {
              localSetTimeout(performWorkUntilDeadline, 0);
            };
          }
          function requestHostCallback(callback) {
            scheduledHostCallback = callback;
            if (!isMessageLoopRunning) {
              isMessageLoopRunning = true;
              schedulePerformWorkUntilDeadline();
            }
          }
          function requestHostTimeout(callback, ms) {
            taskTimeoutID = localSetTimeout(function() {
              callback(exports.unstable_now());
            }, ms);
          }
          function cancelHostTimeout() {
            localClearTimeout(taskTimeoutID);
            taskTimeoutID = -1;
          }
          var unstable_requestPaint = requestPaint;
          var unstable_Profiling = null;
          exports.unstable_IdlePriority = IdlePriority;
          exports.unstable_ImmediatePriority = ImmediatePriority;
          exports.unstable_LowPriority = LowPriority;
          exports.unstable_NormalPriority = NormalPriority;
          exports.unstable_Profiling = unstable_Profiling;
          exports.unstable_UserBlockingPriority = UserBlockingPriority;
          exports.unstable_cancelCallback = unstable_cancelCallback;
          exports.unstable_continueExecution = unstable_continueExecution;
          exports.unstable_forceFrameRate = forceFrameRate;
          exports.unstable_getCurrentPriorityLevel = unstable_getCurrentPriorityLevel;
          exports.unstable_getFirstCallbackNode = unstable_getFirstCallbackNode;
          exports.unstable_next = unstable_next;
          exports.unstable_pauseExecution = unstable_pauseExecution;
          exports.unstable_requestPaint = unstable_requestPaint;
          exports.unstable_runWithPriority = unstable_runWithPriority;
          exports.unstable_scheduleCallback = unstable_scheduleCallback;
          exports.unstable_shouldYield = shouldYieldToHost;
          exports.unstable_wrapCallback = unstable_wrapCallback;
          if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop === "function") {
            __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(new Error());
          }
        })();
      }
    }
  });

  // node_modules/scheduler/index.js
  var require_scheduler = __commonJS({
    "node_modules/scheduler/index.js"(exports, module) {
      "use strict";
      if (false) {
        module.exports = null;
      } else {
        module.exports = require_scheduler_development();
      }
    }
  });

  // node_modules/react-dom/cjs/react-dom.development.js
  var require_react_dom_development = __commonJS({
    "node_modules/react-dom/cjs/react-dom.development.js"(exports) {
      "use strict";
      if (true) {
        (function() {
          "use strict";
          if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart === "function") {
            __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(new Error());
          }
          var React3 = require_react();
          var Scheduler = require_scheduler();
          var ReactSharedInternals = React3.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
          var suppressWarning = false;
          function setSuppressWarning(newSuppressWarning) {
            {
              suppressWarning = newSuppressWarning;
            }
          }
          function warn(format) {
            {
              if (!suppressWarning) {
                for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
                  args[_key - 1] = arguments[_key];
                }
                printWarning("warn", format, args);
              }
            }
          }
          function error(format) {
            {
              if (!suppressWarning) {
                for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
                  args[_key2 - 1] = arguments[_key2];
                }
                printWarning("error", format, args);
              }
            }
          }
          function printWarning(level, format, args) {
            {
              var ReactDebugCurrentFrame2 = ReactSharedInternals.ReactDebugCurrentFrame;
              var stack = ReactDebugCurrentFrame2.getStackAddendum();
              if (stack !== "") {
                format += "%s";
                args = args.concat([stack]);
              }
              var argsWithFormat = args.map(function(item) {
                return String(item);
              });
              argsWithFormat.unshift("Warning: " + format);
              Function.prototype.apply.call(console[level], console, argsWithFormat);
            }
          }
          var FunctionComponent = 0;
          var ClassComponent = 1;
          var IndeterminateComponent = 2;
          var HostRoot = 3;
          var HostPortal = 4;
          var HostComponent = 5;
          var HostText = 6;
          var Fragment3 = 7;
          var Mode = 8;
          var ContextConsumer = 9;
          var ContextProvider = 10;
          var ForwardRef = 11;
          var Profiler = 12;
          var SuspenseComponent = 13;
          var MemoComponent = 14;
          var SimpleMemoComponent = 15;
          var LazyComponent = 16;
          var IncompleteClassComponent = 17;
          var DehydratedFragment = 18;
          var SuspenseListComponent = 19;
          var ScopeComponent = 21;
          var OffscreenComponent = 22;
          var LegacyHiddenComponent = 23;
          var CacheComponent = 24;
          var TracingMarkerComponent = 25;
          var enableClientRenderFallbackOnTextMismatch = true;
          var enableNewReconciler = false;
          var enableLazyContextPropagation = false;
          var enableLegacyHidden = false;
          var enableSuspenseAvoidThisFallback = false;
          var disableCommentsAsDOMContainers = true;
          var enableCustomElementPropertySupport = false;
          var warnAboutStringRefs = true;
          var enableSchedulingProfiler = true;
          var enableProfilerTimer = true;
          var enableProfilerCommitHooks = true;
          var allNativeEvents = /* @__PURE__ */ new Set();
          var registrationNameDependencies = {};
          var possibleRegistrationNames = {};
          function registerTwoPhaseEvent(registrationName, dependencies) {
            registerDirectEvent(registrationName, dependencies);
            registerDirectEvent(registrationName + "Capture", dependencies);
          }
          function registerDirectEvent(registrationName, dependencies) {
            {
              if (registrationNameDependencies[registrationName]) {
                error("EventRegistry: More than one plugin attempted to publish the same registration name, `%s`.", registrationName);
              }
            }
            registrationNameDependencies[registrationName] = dependencies;
            {
              var lowerCasedName = registrationName.toLowerCase();
              possibleRegistrationNames[lowerCasedName] = registrationName;
              if (registrationName === "onDoubleClick") {
                possibleRegistrationNames.ondblclick = registrationName;
              }
            }
            for (var i = 0; i < dependencies.length; i++) {
              allNativeEvents.add(dependencies[i]);
            }
          }
          var canUseDOM = !!(typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined");
          var hasOwnProperty = Object.prototype.hasOwnProperty;
          function typeName(value) {
            {
              var hasToStringTag = typeof Symbol === "function" && Symbol.toStringTag;
              var type = hasToStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
              return type;
            }
          }
          function willCoercionThrow(value) {
            {
              try {
                testStringCoercion(value);
                return false;
              } catch (e) {
                return true;
              }
            }
          }
          function testStringCoercion(value) {
            return "" + value;
          }
          function checkAttributeStringCoercion(value, attributeName) {
            {
              if (willCoercionThrow(value)) {
                error("The provided `%s` attribute is an unsupported type %s. This value must be coerced to a string before before using it here.", attributeName, typeName(value));
                return testStringCoercion(value);
              }
            }
          }
          function checkKeyStringCoercion(value) {
            {
              if (willCoercionThrow(value)) {
                error("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
                return testStringCoercion(value);
              }
            }
          }
          function checkPropStringCoercion(value, propName) {
            {
              if (willCoercionThrow(value)) {
                error("The provided `%s` prop is an unsupported type %s. This value must be coerced to a string before before using it here.", propName, typeName(value));
                return testStringCoercion(value);
              }
            }
          }
          function checkCSSPropertyStringCoercion(value, propName) {
            {
              if (willCoercionThrow(value)) {
                error("The provided `%s` CSS property is an unsupported type %s. This value must be coerced to a string before before using it here.", propName, typeName(value));
                return testStringCoercion(value);
              }
            }
          }
          function checkHtmlStringCoercion(value) {
            {
              if (willCoercionThrow(value)) {
                error("The provided HTML markup uses a value of unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
                return testStringCoercion(value);
              }
            }
          }
          function checkFormFieldValueStringCoercion(value) {
            {
              if (willCoercionThrow(value)) {
                error("Form field values (value, checked, defaultValue, or defaultChecked props) must be strings, not %s. This value must be coerced to a string before before using it here.", typeName(value));
                return testStringCoercion(value);
              }
            }
          }
          var RESERVED = 0;
          var STRING = 1;
          var BOOLEANISH_STRING = 2;
          var BOOLEAN = 3;
          var OVERLOADED_BOOLEAN = 4;
          var NUMERIC = 5;
          var POSITIVE_NUMERIC = 6;
          var ATTRIBUTE_NAME_START_CHAR = ":A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD";
          var ATTRIBUTE_NAME_CHAR = ATTRIBUTE_NAME_START_CHAR + "\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040";
          var VALID_ATTRIBUTE_NAME_REGEX = new RegExp("^[" + ATTRIBUTE_NAME_START_CHAR + "][" + ATTRIBUTE_NAME_CHAR + "]*$");
          var illegalAttributeNameCache = {};
          var validatedAttributeNameCache = {};
          function isAttributeNameSafe(attributeName) {
            if (hasOwnProperty.call(validatedAttributeNameCache, attributeName)) {
              return true;
            }
            if (hasOwnProperty.call(illegalAttributeNameCache, attributeName)) {
              return false;
            }
            if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName)) {
              validatedAttributeNameCache[attributeName] = true;
              return true;
            }
            illegalAttributeNameCache[attributeName] = true;
            {
              error("Invalid attribute name: `%s`", attributeName);
            }
            return false;
          }
          function shouldIgnoreAttribute(name, propertyInfo, isCustomComponentTag) {
            if (propertyInfo !== null) {
              return propertyInfo.type === RESERVED;
            }
            if (isCustomComponentTag) {
              return false;
            }
            if (name.length > 2 && (name[0] === "o" || name[0] === "O") && (name[1] === "n" || name[1] === "N")) {
              return true;
            }
            return false;
          }
          function shouldRemoveAttributeWithWarning(name, value, propertyInfo, isCustomComponentTag) {
            if (propertyInfo !== null && propertyInfo.type === RESERVED) {
              return false;
            }
            switch (typeof value) {
              case "function":
              // $FlowIssue symbol is perfectly valid here
              case "symbol":
                return true;
              case "boolean": {
                if (isCustomComponentTag) {
                  return false;
                }
                if (propertyInfo !== null) {
                  return !propertyInfo.acceptsBooleans;
                } else {
                  var prefix2 = name.toLowerCase().slice(0, 5);
                  return prefix2 !== "data-" && prefix2 !== "aria-";
                }
              }
              default:
                return false;
            }
          }
          function shouldRemoveAttribute(name, value, propertyInfo, isCustomComponentTag) {
            if (value === null || typeof value === "undefined") {
              return true;
            }
            if (shouldRemoveAttributeWithWarning(name, value, propertyInfo, isCustomComponentTag)) {
              return true;
            }
            if (isCustomComponentTag) {
              return false;
            }
            if (propertyInfo !== null) {
              switch (propertyInfo.type) {
                case BOOLEAN:
                  return !value;
                case OVERLOADED_BOOLEAN:
                  return value === false;
                case NUMERIC:
                  return isNaN(value);
                case POSITIVE_NUMERIC:
                  return isNaN(value) || value < 1;
              }
            }
            return false;
          }
          function getPropertyInfo(name) {
            return properties.hasOwnProperty(name) ? properties[name] : null;
          }
          function PropertyInfoRecord(name, type, mustUseProperty, attributeName, attributeNamespace, sanitizeURL2, removeEmptyString) {
            this.acceptsBooleans = type === BOOLEANISH_STRING || type === BOOLEAN || type === OVERLOADED_BOOLEAN;
            this.attributeName = attributeName;
            this.attributeNamespace = attributeNamespace;
            this.mustUseProperty = mustUseProperty;
            this.propertyName = name;
            this.type = type;
            this.sanitizeURL = sanitizeURL2;
            this.removeEmptyString = removeEmptyString;
          }
          var properties = {};
          var reservedProps = [
            "children",
            "dangerouslySetInnerHTML",
            // TODO: This prevents the assignment of defaultValue to regular
            // elements (not just inputs). Now that ReactDOMInput assigns to the
            // defaultValue property -- do we need this?
            "defaultValue",
            "defaultChecked",
            "innerHTML",
            "suppressContentEditableWarning",
            "suppressHydrationWarning",
            "style"
          ];
          reservedProps.forEach(function(name) {
            properties[name] = new PropertyInfoRecord(
              name,
              RESERVED,
              false,
              // mustUseProperty
              name,
              // attributeName
              null,
              // attributeNamespace
              false,
              // sanitizeURL
              false
            );
          });
          [["acceptCharset", "accept-charset"], ["className", "class"], ["htmlFor", "for"], ["httpEquiv", "http-equiv"]].forEach(function(_ref) {
            var name = _ref[0], attributeName = _ref[1];
            properties[name] = new PropertyInfoRecord(
              name,
              STRING,
              false,
              // mustUseProperty
              attributeName,
              // attributeName
              null,
              // attributeNamespace
              false,
              // sanitizeURL
              false
            );
          });
          ["contentEditable", "draggable", "spellCheck", "value"].forEach(function(name) {
            properties[name] = new PropertyInfoRecord(
              name,
              BOOLEANISH_STRING,
              false,
              // mustUseProperty
              name.toLowerCase(),
              // attributeName
              null,
              // attributeNamespace
              false,
              // sanitizeURL
              false
            );
          });
          ["autoReverse", "externalResourcesRequired", "focusable", "preserveAlpha"].forEach(function(name) {
            properties[name] = new PropertyInfoRecord(
              name,
              BOOLEANISH_STRING,
              false,
              // mustUseProperty
              name,
              // attributeName
              null,
              // attributeNamespace
              false,
              // sanitizeURL
              false
            );
          });
          [
            "allowFullScreen",
            "async",
            // Note: there is a special case that prevents it from being written to the DOM
            // on the client side because the browsers are inconsistent. Instead we call focus().
            "autoFocus",
            "autoPlay",
            "controls",
            "default",
            "defer",
            "disabled",
            "disablePictureInPicture",
            "disableRemotePlayback",
            "formNoValidate",
            "hidden",
            "loop",
            "noModule",
            "noValidate",
            "open",
            "playsInline",
            "readOnly",
            "required",
            "reversed",
            "scoped",
            "seamless",
            // Microdata
            "itemScope"
          ].forEach(function(name) {
            properties[name] = new PropertyInfoRecord(
              name,
              BOOLEAN,
              false,
              // mustUseProperty
              name.toLowerCase(),
              // attributeName
              null,
              // attributeNamespace
              false,
              // sanitizeURL
              false
            );
          });
          [
            "checked",
            // Note: `option.selected` is not updated if `select.multiple` is
            // disabled with `removeAttribute`. We have special logic for handling this.
            "multiple",
            "muted",
            "selected"
            // NOTE: if you add a camelCased prop to this list,
            // you'll need to set attributeName to name.toLowerCase()
            // instead in the assignment below.
          ].forEach(function(name) {
            properties[name] = new PropertyInfoRecord(
              name,
              BOOLEAN,
              true,
              // mustUseProperty
              name,
              // attributeName
              null,
              // attributeNamespace
              false,
              // sanitizeURL
              false
            );
          });
          [
            "capture",
            "download"
            // NOTE: if you add a camelCased prop to this list,
            // you'll need to set attributeName to name.toLowerCase()
            // instead in the assignment below.
          ].forEach(function(name) {
            properties[name] = new PropertyInfoRecord(
              name,
              OVERLOADED_BOOLEAN,
              false,
              // mustUseProperty
              name,
              // attributeName
              null,
              // attributeNamespace
              false,
              // sanitizeURL
              false
            );
          });
          [
            "cols",
            "rows",
            "size",
            "span"
            // NOTE: if you add a camelCased prop to this list,
            // you'll need to set attributeName to name.toLowerCase()
            // instead in the assignment below.
          ].forEach(function(name) {
            properties[name] = new PropertyInfoRecord(
              name,
              POSITIVE_NUMERIC,
              false,
              // mustUseProperty
              name,
              // attributeName
              null,
              // attributeNamespace
              false,
              // sanitizeURL
              false
            );
          });
          ["rowSpan", "start"].forEach(function(name) {
            properties[name] = new PropertyInfoRecord(
              name,
              NUMERIC,
              false,
              // mustUseProperty
              name.toLowerCase(),
              // attributeName
              null,
              // attributeNamespace
              false,
              // sanitizeURL
              false
            );
          });
          var CAMELIZE = /[\-\:]([a-z])/g;
          var capitalize = function(token) {
            return token[1].toUpperCase();
          };
          [
            "accent-height",
            "alignment-baseline",
            "arabic-form",
            "baseline-shift",
            "cap-height",
            "clip-path",
            "clip-rule",
            "color-interpolation",
            "color-interpolation-filters",
            "color-profile",
            "color-rendering",
            "dominant-baseline",
            "enable-background",
            "fill-opacity",
            "fill-rule",
            "flood-color",
            "flood-opacity",
            "font-family",
            "font-size",
            "font-size-adjust",
            "font-stretch",
            "font-style",
            "font-variant",
            "font-weight",
            "glyph-name",
            "glyph-orientation-horizontal",
            "glyph-orientation-vertical",
            "horiz-adv-x",
            "horiz-origin-x",
            "image-rendering",
            "letter-spacing",
            "lighting-color",
            "marker-end",
            "marker-mid",
            "marker-start",
            "overline-position",
            "overline-thickness",
            "paint-order",
            "panose-1",
            "pointer-events",
            "rendering-intent",
            "shape-rendering",
            "stop-color",
            "stop-opacity",
            "strikethrough-position",
            "strikethrough-thickness",
            "stroke-dasharray",
            "stroke-dashoffset",
            "stroke-linecap",
            "stroke-linejoin",
            "stroke-miterlimit",
            "stroke-opacity",
            "stroke-width",
            "text-anchor",
            "text-decoration",
            "text-rendering",
            "underline-position",
            "underline-thickness",
            "unicode-bidi",
            "unicode-range",
            "units-per-em",
            "v-alphabetic",
            "v-hanging",
            "v-ideographic",
            "v-mathematical",
            "vector-effect",
            "vert-adv-y",
            "vert-origin-x",
            "vert-origin-y",
            "word-spacing",
            "writing-mode",
            "xmlns:xlink",
            "x-height"
            // NOTE: if you add a camelCased prop to this list,
            // you'll need to set attributeName to name.toLowerCase()
            // instead in the assignment below.
          ].forEach(function(attributeName) {
            var name = attributeName.replace(CAMELIZE, capitalize);
            properties[name] = new PropertyInfoRecord(
              name,
              STRING,
              false,
              // mustUseProperty
              attributeName,
              null,
              // attributeNamespace
              false,
              // sanitizeURL
              false
            );
          });
          [
            "xlink:actuate",
            "xlink:arcrole",
            "xlink:role",
            "xlink:show",
            "xlink:title",
            "xlink:type"
            // NOTE: if you add a camelCased prop to this list,
            // you'll need to set attributeName to name.toLowerCase()
            // instead in the assignment below.
          ].forEach(function(attributeName) {
            var name = attributeName.replace(CAMELIZE, capitalize);
            properties[name] = new PropertyInfoRecord(
              name,
              STRING,
              false,
              // mustUseProperty
              attributeName,
              "http://www.w3.org/1999/xlink",
              false,
              // sanitizeURL
              false
            );
          });
          [
            "xml:base",
            "xml:lang",
            "xml:space"
            // NOTE: if you add a camelCased prop to this list,
            // you'll need to set attributeName to name.toLowerCase()
            // instead in the assignment below.
          ].forEach(function(attributeName) {
            var name = attributeName.replace(CAMELIZE, capitalize);
            properties[name] = new PropertyInfoRecord(
              name,
              STRING,
              false,
              // mustUseProperty
              attributeName,
              "http://www.w3.org/XML/1998/namespace",
              false,
              // sanitizeURL
              false
            );
          });
          ["tabIndex", "crossOrigin"].forEach(function(attributeName) {
            properties[attributeName] = new PropertyInfoRecord(
              attributeName,
              STRING,
              false,
              // mustUseProperty
              attributeName.toLowerCase(),
              // attributeName
              null,
              // attributeNamespace
              false,
              // sanitizeURL
              false
            );
          });
          var xlinkHref = "xlinkHref";
          properties[xlinkHref] = new PropertyInfoRecord(
            "xlinkHref",
            STRING,
            false,
            // mustUseProperty
            "xlink:href",
            "http://www.w3.org/1999/xlink",
            true,
            // sanitizeURL
            false
          );
          ["src", "href", "action", "formAction"].forEach(function(attributeName) {
            properties[attributeName] = new PropertyInfoRecord(
              attributeName,
              STRING,
              false,
              // mustUseProperty
              attributeName.toLowerCase(),
              // attributeName
              null,
              // attributeNamespace
              true,
              // sanitizeURL
              true
            );
          });
          var isJavaScriptProtocol = /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*\:/i;
          var didWarn = false;
          function sanitizeURL(url) {
            {
              if (!didWarn && isJavaScriptProtocol.test(url)) {
                didWarn = true;
                error("A future version of React will block javascript: URLs as a security precaution. Use event handlers instead if you can. If you need to generate unsafe HTML try using dangerouslySetInnerHTML instead. React was passed %s.", JSON.stringify(url));
              }
            }
          }
          function getValueForProperty(node, name, expected, propertyInfo) {
            {
              if (propertyInfo.mustUseProperty) {
                var propertyName = propertyInfo.propertyName;
                return node[propertyName];
              } else {
                {
                  checkAttributeStringCoercion(expected, name);
                }
                if (propertyInfo.sanitizeURL) {
                  sanitizeURL("" + expected);
                }
                var attributeName = propertyInfo.attributeName;
                var stringValue = null;
                if (propertyInfo.type === OVERLOADED_BOOLEAN) {
                  if (node.hasAttribute(attributeName)) {
                    var value = node.getAttribute(attributeName);
                    if (value === "") {
                      return true;
                    }
                    if (shouldRemoveAttribute(name, expected, propertyInfo, false)) {
                      return value;
                    }
                    if (value === "" + expected) {
                      return expected;
                    }
                    return value;
                  }
                } else if (node.hasAttribute(attributeName)) {
                  if (shouldRemoveAttribute(name, expected, propertyInfo, false)) {
                    return node.getAttribute(attributeName);
                  }
                  if (propertyInfo.type === BOOLEAN) {
                    return expected;
                  }
                  stringValue = node.getAttribute(attributeName);
                }
                if (shouldRemoveAttribute(name, expected, propertyInfo, false)) {
                  return stringValue === null ? expected : stringValue;
                } else if (stringValue === "" + expected) {
                  return expected;
                } else {
                  return stringValue;
                }
              }
            }
          }
          function getValueForAttribute(node, name, expected, isCustomComponentTag) {
            {
              if (!isAttributeNameSafe(name)) {
                return;
              }
              if (!node.hasAttribute(name)) {
                return expected === void 0 ? void 0 : null;
              }
              var value = node.getAttribute(name);
              {
                checkAttributeStringCoercion(expected, name);
              }
              if (value === "" + expected) {
                return expected;
              }
              return value;
            }
          }
          function setValueForProperty(node, name, value, isCustomComponentTag) {
            var propertyInfo = getPropertyInfo(name);
            if (shouldIgnoreAttribute(name, propertyInfo, isCustomComponentTag)) {
              return;
            }
            if (shouldRemoveAttribute(name, value, propertyInfo, isCustomComponentTag)) {
              value = null;
            }
            if (isCustomComponentTag || propertyInfo === null) {
              if (isAttributeNameSafe(name)) {
                var _attributeName = name;
                if (value === null) {
                  node.removeAttribute(_attributeName);
                } else {
                  {
                    checkAttributeStringCoercion(value, name);
                  }
                  node.setAttribute(_attributeName, "" + value);
                }
              }
              return;
            }
            var mustUseProperty = propertyInfo.mustUseProperty;
            if (mustUseProperty) {
              var propertyName = propertyInfo.propertyName;
              if (value === null) {
                var type = propertyInfo.type;
                node[propertyName] = type === BOOLEAN ? false : "";
              } else {
                node[propertyName] = value;
              }
              return;
            }
            var attributeName = propertyInfo.attributeName, attributeNamespace = propertyInfo.attributeNamespace;
            if (value === null) {
              node.removeAttribute(attributeName);
            } else {
              var _type = propertyInfo.type;
              var attributeValue;
              if (_type === BOOLEAN || _type === OVERLOADED_BOOLEAN && value === true) {
                attributeValue = "";
              } else {
                {
                  {
                    checkAttributeStringCoercion(value, attributeName);
                  }
                  attributeValue = "" + value;
                }
                if (propertyInfo.sanitizeURL) {
                  sanitizeURL(attributeValue.toString());
                }
              }
              if (attributeNamespace) {
                node.setAttributeNS(attributeNamespace, attributeName, attributeValue);
              } else {
                node.setAttribute(attributeName, attributeValue);
              }
            }
          }
          var REACT_ELEMENT_TYPE = Symbol.for("react.element");
          var REACT_PORTAL_TYPE = Symbol.for("react.portal");
          var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
          var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
          var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
          var REACT_PROVIDER_TYPE = Symbol.for("react.provider");
          var REACT_CONTEXT_TYPE = Symbol.for("react.context");
          var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
          var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
          var REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list");
          var REACT_MEMO_TYPE = Symbol.for("react.memo");
          var REACT_LAZY_TYPE = Symbol.for("react.lazy");
          var REACT_SCOPE_TYPE = Symbol.for("react.scope");
          var REACT_DEBUG_TRACING_MODE_TYPE = Symbol.for("react.debug_trace_mode");
          var REACT_OFFSCREEN_TYPE = Symbol.for("react.offscreen");
          var REACT_LEGACY_HIDDEN_TYPE = Symbol.for("react.legacy_hidden");
          var REACT_CACHE_TYPE = Symbol.for("react.cache");
          var REACT_TRACING_MARKER_TYPE = Symbol.for("react.tracing_marker");
          var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
          var FAUX_ITERATOR_SYMBOL = "@@iterator";
          function getIteratorFn(maybeIterable) {
            if (maybeIterable === null || typeof maybeIterable !== "object") {
              return null;
            }
            var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
            if (typeof maybeIterator === "function") {
              return maybeIterator;
            }
            return null;
          }
          var assign = Object.assign;
          var disabledDepth = 0;
          var prevLog;
          var prevInfo;
          var prevWarn;
          var prevError;
          var prevGroup;
          var prevGroupCollapsed;
          var prevGroupEnd;
          function disabledLog() {
          }
          disabledLog.__reactDisabledLog = true;
          function disableLogs() {
            {
              if (disabledDepth === 0) {
                prevLog = console.log;
                prevInfo = console.info;
                prevWarn = console.warn;
                prevError = console.error;
                prevGroup = console.group;
                prevGroupCollapsed = console.groupCollapsed;
                prevGroupEnd = console.groupEnd;
                var props = {
                  configurable: true,
                  enumerable: true,
                  value: disabledLog,
                  writable: true
                };
                Object.defineProperties(console, {
                  info: props,
                  log: props,
                  warn: props,
                  error: props,
                  group: props,
                  groupCollapsed: props,
                  groupEnd: props
                });
              }
              disabledDepth++;
            }
          }
          function reenableLogs() {
            {
              disabledDepth--;
              if (disabledDepth === 0) {
                var props = {
                  configurable: true,
                  enumerable: true,
                  writable: true
                };
                Object.defineProperties(console, {
                  log: assign({}, props, {
                    value: prevLog
                  }),
                  info: assign({}, props, {
                    value: prevInfo
                  }),
                  warn: assign({}, props, {
                    value: prevWarn
                  }),
                  error: assign({}, props, {
                    value: prevError
                  }),
                  group: assign({}, props, {
                    value: prevGroup
                  }),
                  groupCollapsed: assign({}, props, {
                    value: prevGroupCollapsed
                  }),
                  groupEnd: assign({}, props, {
                    value: prevGroupEnd
                  })
                });
              }
              if (disabledDepth < 0) {
                error("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
              }
            }
          }
          var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
          var prefix;
          function describeBuiltInComponentFrame(name, source, ownerFn) {
            {
              if (prefix === void 0) {
                try {
                  throw Error();
                } catch (x) {
                  var match = x.stack.trim().match(/\n( *(at )?)/);
                  prefix = match && match[1] || "";
                }
              }
              return "\n" + prefix + name;
            }
          }
          var reentry = false;
          var componentFrameCache;
          {
            var PossiblyWeakMap = typeof WeakMap === "function" ? WeakMap : Map;
            componentFrameCache = new PossiblyWeakMap();
          }
          function describeNativeComponentFrame(fn, construct) {
            if (!fn || reentry) {
              return "";
            }
            {
              var frame = componentFrameCache.get(fn);
              if (frame !== void 0) {
                return frame;
              }
            }
            var control;
            reentry = true;
            var previousPrepareStackTrace = Error.prepareStackTrace;
            Error.prepareStackTrace = void 0;
            var previousDispatcher;
            {
              previousDispatcher = ReactCurrentDispatcher.current;
              ReactCurrentDispatcher.current = null;
              disableLogs();
            }
            try {
              if (construct) {
                var Fake = function() {
                  throw Error();
                };
                Object.defineProperty(Fake.prototype, "props", {
                  set: function() {
                    throw Error();
                  }
                });
                if (typeof Reflect === "object" && Reflect.construct) {
                  try {
                    Reflect.construct(Fake, []);
                  } catch (x) {
                    control = x;
                  }
                  Reflect.construct(fn, [], Fake);
                } else {
                  try {
                    Fake.call();
                  } catch (x) {
                    control = x;
                  }
                  fn.call(Fake.prototype);
                }
              } else {
                try {
                  throw Error();
                } catch (x) {
                  control = x;
                }
                fn();
              }
            } catch (sample) {
              if (sample && control && typeof sample.stack === "string") {
                var sampleLines = sample.stack.split("\n");
                var controlLines = control.stack.split("\n");
                var s = sampleLines.length - 1;
                var c = controlLines.length - 1;
                while (s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c]) {
                  c--;
                }
                for (; s >= 1 && c >= 0; s--, c--) {
                  if (sampleLines[s] !== controlLines[c]) {
                    if (s !== 1 || c !== 1) {
                      do {
                        s--;
                        c--;
                        if (c < 0 || sampleLines[s] !== controlLines[c]) {
                          var _frame = "\n" + sampleLines[s].replace(" at new ", " at ");
                          if (fn.displayName && _frame.includes("<anonymous>")) {
                            _frame = _frame.replace("<anonymous>", fn.displayName);
                          }
                          {
                            if (typeof fn === "function") {
                              componentFrameCache.set(fn, _frame);
                            }
                          }
                          return _frame;
                        }
                      } while (s >= 1 && c >= 0);
                    }
                    break;
                  }
                }
              }
            } finally {
              reentry = false;
              {
                ReactCurrentDispatcher.current = previousDispatcher;
                reenableLogs();
              }
              Error.prepareStackTrace = previousPrepareStackTrace;
            }
            var name = fn ? fn.displayName || fn.name : "";
            var syntheticFrame = name ? describeBuiltInComponentFrame(name) : "";
            {
              if (typeof fn === "function") {
                componentFrameCache.set(fn, syntheticFrame);
              }
            }
            return syntheticFrame;
          }
          function describeClassComponentFrame(ctor, source, ownerFn) {
            {
              return describeNativeComponentFrame(ctor, true);
            }
          }
          function describeFunctionComponentFrame(fn, source, ownerFn) {
            {
              return describeNativeComponentFrame(fn, false);
            }
          }
          function shouldConstruct(Component) {
            var prototype = Component.prototype;
            return !!(prototype && prototype.isReactComponent);
          }
          function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {
            if (type == null) {
              return "";
            }
            if (typeof type === "function") {
              {
                return describeNativeComponentFrame(type, shouldConstruct(type));
              }
            }
            if (typeof type === "string") {
              return describeBuiltInComponentFrame(type);
            }
            switch (type) {
              case REACT_SUSPENSE_TYPE:
                return describeBuiltInComponentFrame("Suspense");
              case REACT_SUSPENSE_LIST_TYPE:
                return describeBuiltInComponentFrame("SuspenseList");
            }
            if (typeof type === "object") {
              switch (type.$$typeof) {
                case REACT_FORWARD_REF_TYPE:
                  return describeFunctionComponentFrame(type.render);
                case REACT_MEMO_TYPE:
                  return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);
                case REACT_LAZY_TYPE: {
                  var lazyComponent = type;
                  var payload = lazyComponent._payload;
                  var init = lazyComponent._init;
                  try {
                    return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
                  } catch (x) {
                  }
                }
              }
            }
            return "";
          }
          function describeFiber(fiber) {
            var owner = fiber._debugOwner ? fiber._debugOwner.type : null;
            var source = fiber._debugSource;
            switch (fiber.tag) {
              case HostComponent:
                return describeBuiltInComponentFrame(fiber.type);
              case LazyComponent:
                return describeBuiltInComponentFrame("Lazy");
              case SuspenseComponent:
                return describeBuiltInComponentFrame("Suspense");
              case SuspenseListComponent:
                return describeBuiltInComponentFrame("SuspenseList");
              case FunctionComponent:
              case IndeterminateComponent:
              case SimpleMemoComponent:
                return describeFunctionComponentFrame(fiber.type);
              case ForwardRef:
                return describeFunctionComponentFrame(fiber.type.render);
              case ClassComponent:
                return describeClassComponentFrame(fiber.type);
              default:
                return "";
            }
          }
          function getStackByFiberInDevAndProd(workInProgress2) {
            try {
              var info = "";
              var node = workInProgress2;
              do {
                info += describeFiber(node);
                node = node.return;
              } while (node);
              return info;
            } catch (x) {
              return "\nError generating stack: " + x.message + "\n" + x.stack;
            }
          }
          function getWrappedName(outerType, innerType, wrapperName) {
            var displayName = outerType.displayName;
            if (displayName) {
              return displayName;
            }
            var functionName = innerType.displayName || innerType.name || "";
            return functionName !== "" ? wrapperName + "(" + functionName + ")" : wrapperName;
          }
          function getContextName(type) {
            return type.displayName || "Context";
          }
          function getComponentNameFromType(type) {
            if (type == null) {
              return null;
            }
            {
              if (typeof type.tag === "number") {
                error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue.");
              }
            }
            if (typeof type === "function") {
              return type.displayName || type.name || null;
            }
            if (typeof type === "string") {
              return type;
            }
            switch (type) {
              case REACT_FRAGMENT_TYPE:
                return "Fragment";
              case REACT_PORTAL_TYPE:
                return "Portal";
              case REACT_PROFILER_TYPE:
                return "Profiler";
              case REACT_STRICT_MODE_TYPE:
                return "StrictMode";
              case REACT_SUSPENSE_TYPE:
                return "Suspense";
              case REACT_SUSPENSE_LIST_TYPE:
                return "SuspenseList";
            }
            if (typeof type === "object") {
              switch (type.$$typeof) {
                case REACT_CONTEXT_TYPE:
                  var context = type;
                  return getContextName(context) + ".Consumer";
                case REACT_PROVIDER_TYPE:
                  var provider = type;
                  return getContextName(provider._context) + ".Provider";
                case REACT_FORWARD_REF_TYPE:
                  return getWrappedName(type, type.render, "ForwardRef");
                case REACT_MEMO_TYPE:
                  var outerName = type.displayName || null;
                  if (outerName !== null) {
                    return outerName;
                  }
                  return getComponentNameFromType(type.type) || "Memo";
                case REACT_LAZY_TYPE: {
                  var lazyComponent = type;
                  var payload = lazyComponent._payload;
                  var init = lazyComponent._init;
                  try {
                    return getComponentNameFromType(init(payload));
                  } catch (x) {
                    return null;
                  }
                }
              }
            }
            return null;
          }
          function getWrappedName$1(outerType, innerType, wrapperName) {
            var functionName = innerType.displayName || innerType.name || "";
            return outerType.displayName || (functionName !== "" ? wrapperName + "(" + functionName + ")" : wrapperName);
          }
          function getContextName$1(type) {
            return type.displayName || "Context";
          }
          function getComponentNameFromFiber(fiber) {
            var tag = fiber.tag, type = fiber.type;
            switch (tag) {
              case CacheComponent:
                return "Cache";
              case ContextConsumer:
                var context = type;
                return getContextName$1(context) + ".Consumer";
              case ContextProvider:
                var provider = type;
                return getContextName$1(provider._context) + ".Provider";
              case DehydratedFragment:
                return "DehydratedFragment";
              case ForwardRef:
                return getWrappedName$1(type, type.render, "ForwardRef");
              case Fragment3:
                return "Fragment";
              case HostComponent:
                return type;
              case HostPortal:
                return "Portal";
              case HostRoot:
                return "Root";
              case HostText:
                return "Text";
              case LazyComponent:
                return getComponentNameFromType(type);
              case Mode:
                if (type === REACT_STRICT_MODE_TYPE) {
                  return "StrictMode";
                }
                return "Mode";
              case OffscreenComponent:
                return "Offscreen";
              case Profiler:
                return "Profiler";
              case ScopeComponent:
                return "Scope";
              case SuspenseComponent:
                return "Suspense";
              case SuspenseListComponent:
                return "SuspenseList";
              case TracingMarkerComponent:
                return "TracingMarker";
              // The display name for this tags come from the user-provided type:
              case ClassComponent:
              case FunctionComponent:
              case IncompleteClassComponent:
              case IndeterminateComponent:
              case MemoComponent:
              case SimpleMemoComponent:
                if (typeof type === "function") {
                  return type.displayName || type.name || null;
                }
                if (typeof type === "string") {
                  return type;
                }
                break;
            }
            return null;
          }
          var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
          var current = null;
          var isRendering = false;
          function getCurrentFiberOwnerNameInDevOrNull() {
            {
              if (current === null) {
                return null;
              }
              var owner = current._debugOwner;
              if (owner !== null && typeof owner !== "undefined") {
                return getComponentNameFromFiber(owner);
              }
            }
            return null;
          }
          function getCurrentFiberStackInDev() {
            {
              if (current === null) {
                return "";
              }
              return getStackByFiberInDevAndProd(current);
            }
          }
          function resetCurrentFiber() {
            {
              ReactDebugCurrentFrame.getCurrentStack = null;
              current = null;
              isRendering = false;
            }
          }
          function setCurrentFiber(fiber) {
            {
              ReactDebugCurrentFrame.getCurrentStack = fiber === null ? null : getCurrentFiberStackInDev;
              current = fiber;
              isRendering = false;
            }
          }
          function getCurrentFiber() {
            {
              return current;
            }
          }
          function setIsRendering(rendering) {
            {
              isRendering = rendering;
            }
          }
          function toString(value) {
            return "" + value;
          }
          function getToStringValue(value) {
            switch (typeof value) {
              case "boolean":
              case "number":
              case "string":
              case "undefined":
                return value;
              case "object":
                {
                  checkFormFieldValueStringCoercion(value);
                }
                return value;
              default:
                return "";
            }
          }
          var hasReadOnlyValue = {
            button: true,
            checkbox: true,
            image: true,
            hidden: true,
            radio: true,
            reset: true,
            submit: true
          };
          function checkControlledValueProps(tagName, props) {
            {
              if (!(hasReadOnlyValue[props.type] || props.onChange || props.onInput || props.readOnly || props.disabled || props.value == null)) {
                error("You provided a `value` prop to a form field without an `onChange` handler. This will render a read-only field. If the field should be mutable use `defaultValue`. Otherwise, set either `onChange` or `readOnly`.");
              }
              if (!(props.onChange || props.readOnly || props.disabled || props.checked == null)) {
                error("You provided a `checked` prop to a form field without an `onChange` handler. This will render a read-only field. If the field should be mutable use `defaultChecked`. Otherwise, set either `onChange` or `readOnly`.");
              }
            }
          }
          function isCheckable(elem) {
            var type = elem.type;
            var nodeName = elem.nodeName;
            return nodeName && nodeName.toLowerCase() === "input" && (type === "checkbox" || type === "radio");
          }
          function getTracker(node) {
            return node._valueTracker;
          }
          function detachTracker(node) {
            node._valueTracker = null;
          }
          function getValueFromNode(node) {
            var value = "";
            if (!node) {
              return value;
            }
            if (isCheckable(node)) {
              value = node.checked ? "true" : "false";
            } else {
              value = node.value;
            }
            return value;
          }
          function trackValueOnNode(node) {
            var valueField = isCheckable(node) ? "checked" : "value";
            var descriptor = Object.getOwnPropertyDescriptor(node.constructor.prototype, valueField);
            {
              checkFormFieldValueStringCoercion(node[valueField]);
            }
            var currentValue = "" + node[valueField];
            if (node.hasOwnProperty(valueField) || typeof descriptor === "undefined" || typeof descriptor.get !== "function" || typeof descriptor.set !== "function") {
              return;
            }
            var get2 = descriptor.get, set2 = descriptor.set;
            Object.defineProperty(node, valueField, {
              configurable: true,
              get: function() {
                return get2.call(this);
              },
              set: function(value) {
                {
                  checkFormFieldValueStringCoercion(value);
                }
                currentValue = "" + value;
                set2.call(this, value);
              }
            });
            Object.defineProperty(node, valueField, {
              enumerable: descriptor.enumerable
            });
            var tracker = {
              getValue: function() {
                return currentValue;
              },
              setValue: function(value) {
                {
                  checkFormFieldValueStringCoercion(value);
                }
                currentValue = "" + value;
              },
              stopTracking: function() {
                detachTracker(node);
                delete node[valueField];
              }
            };
            return tracker;
          }
          function track(node) {
            if (getTracker(node)) {
              return;
            }
            node._valueTracker = trackValueOnNode(node);
          }
          function updateValueIfChanged(node) {
            if (!node) {
              return false;
            }
            var tracker = getTracker(node);
            if (!tracker) {
              return true;
            }
            var lastValue = tracker.getValue();
            var nextValue = getValueFromNode(node);
            if (nextValue !== lastValue) {
              tracker.setValue(nextValue);
              return true;
            }
            return false;
          }
          function getActiveElement(doc) {
            doc = doc || (typeof document !== "undefined" ? document : void 0);
            if (typeof doc === "undefined") {
              return null;
            }
            try {
              return doc.activeElement || doc.body;
            } catch (e) {
              return doc.body;
            }
          }
          var didWarnValueDefaultValue = false;
          var didWarnCheckedDefaultChecked = false;
          var didWarnControlledToUncontrolled = false;
          var didWarnUncontrolledToControlled = false;
          function isControlled(props) {
            var usesChecked = props.type === "checkbox" || props.type === "radio";
            return usesChecked ? props.checked != null : props.value != null;
          }
          function getHostProps(element, props) {
            var node = element;
            var checked = props.checked;
            var hostProps = assign({}, props, {
              defaultChecked: void 0,
              defaultValue: void 0,
              value: void 0,
              checked: checked != null ? checked : node._wrapperState.initialChecked
            });
            return hostProps;
          }
          function initWrapperState(element, props) {
            {
              checkControlledValueProps("input", props);
              if (props.checked !== void 0 && props.defaultChecked !== void 0 && !didWarnCheckedDefaultChecked) {
                error("%s contains an input of type %s with both checked and defaultChecked props. Input elements must be either controlled or uncontrolled (specify either the checked prop, or the defaultChecked prop, but not both). Decide between using a controlled or uncontrolled input element and remove one of these props. More info: https://reactjs.org/link/controlled-components", getCurrentFiberOwnerNameInDevOrNull() || "A component", props.type);
                didWarnCheckedDefaultChecked = true;
              }
              if (props.value !== void 0 && props.defaultValue !== void 0 && !didWarnValueDefaultValue) {
                error("%s contains an input of type %s with both value and defaultValue props. Input elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled input element and remove one of these props. More info: https://reactjs.org/link/controlled-components", getCurrentFiberOwnerNameInDevOrNull() || "A component", props.type);
                didWarnValueDefaultValue = true;
              }
            }
            var node = element;
            var defaultValue = props.defaultValue == null ? "" : props.defaultValue;
            node._wrapperState = {
              initialChecked: props.checked != null ? props.checked : props.defaultChecked,
              initialValue: getToStringValue(props.value != null ? props.value : defaultValue),
              controlled: isControlled(props)
            };
          }
          function updateChecked(element, props) {
            var node = element;
            var checked = props.checked;
            if (checked != null) {
              setValueForProperty(node, "checked", checked, false);
            }
          }
          function updateWrapper(element, props) {
            var node = element;
            {
              var controlled = isControlled(props);
              if (!node._wrapperState.controlled && controlled && !didWarnUncontrolledToControlled) {
                error("A component is changing an uncontrolled input to be controlled. This is likely caused by the value changing from undefined to a defined value, which should not happen. Decide between using a controlled or uncontrolled input element for the lifetime of the component. More info: https://reactjs.org/link/controlled-components");
                didWarnUncontrolledToControlled = true;
              }
              if (node._wrapperState.controlled && !controlled && !didWarnControlledToUncontrolled) {
                error("A component is changing a controlled input to be uncontrolled. This is likely caused by the value changing from a defined to undefined, which should not happen. Decide between using a controlled or uncontrolled input element for the lifetime of the component. More info: https://reactjs.org/link/controlled-components");
                didWarnControlledToUncontrolled = true;
              }
            }
            updateChecked(element, props);
            var value = getToStringValue(props.value);
            var type = props.type;
            if (value != null) {
              if (type === "number") {
                if (value === 0 && node.value === "" || // We explicitly want to coerce to number here if possible.
                // eslint-disable-next-line
                node.value != value) {
                  node.value = toString(value);
                }
              } else if (node.value !== toString(value)) {
                node.value = toString(value);
              }
            } else if (type === "submit" || type === "reset") {
              node.removeAttribute("value");
              return;
            }
            {
              if (props.hasOwnProperty("value")) {
                setDefaultValue(node, props.type, value);
              } else if (props.hasOwnProperty("defaultValue")) {
                setDefaultValue(node, props.type, getToStringValue(props.defaultValue));
              }
            }
            {
              if (props.checked == null && props.defaultChecked != null) {
                node.defaultChecked = !!props.defaultChecked;
              }
            }
          }
          function postMountWrapper(element, props, isHydrating2) {
            var node = element;
            if (props.hasOwnProperty("value") || props.hasOwnProperty("defaultValue")) {
              var type = props.type;
              var isButton = type === "submit" || type === "reset";
              if (isButton && (props.value === void 0 || props.value === null)) {
                return;
              }
              var initialValue = toString(node._wrapperState.initialValue);
              if (!isHydrating2) {
                {
                  if (initialValue !== node.value) {
                    node.value = initialValue;
                  }
                }
              }
              {
                node.defaultValue = initialValue;
              }
            }
            var name = node.name;
            if (name !== "") {
              node.name = "";
            }
            {
              node.defaultChecked = !node.defaultChecked;
              node.defaultChecked = !!node._wrapperState.initialChecked;
            }
            if (name !== "") {
              node.name = name;
            }
          }
          function restoreControlledState(element, props) {
            var node = element;
            updateWrapper(node, props);
            updateNamedCousins(node, props);
          }
          function updateNamedCousins(rootNode, props) {
            var name = props.name;
            if (props.type === "radio" && name != null) {
              var queryRoot = rootNode;
              while (queryRoot.parentNode) {
                queryRoot = queryRoot.parentNode;
              }
              {
                checkAttributeStringCoercion(name, "name");
              }
              var group = queryRoot.querySelectorAll("input[name=" + JSON.stringify("" + name) + '][type="radio"]');
              for (var i = 0; i < group.length; i++) {
                var otherNode = group[i];
                if (otherNode === rootNode || otherNode.form !== rootNode.form) {
                  continue;
                }
                var otherProps = getFiberCurrentPropsFromNode(otherNode);
                if (!otherProps) {
                  throw new Error("ReactDOMInput: Mixing React and non-React radio inputs with the same `name` is not supported.");
                }
                updateValueIfChanged(otherNode);
                updateWrapper(otherNode, otherProps);
              }
            }
          }
          function setDefaultValue(node, type, value) {
            if (
              // Focused number inputs synchronize on blur. See ChangeEventPlugin.js
              type !== "number" || getActiveElement(node.ownerDocument) !== node
            ) {
              if (value == null) {
                node.defaultValue = toString(node._wrapperState.initialValue);
              } else if (node.defaultValue !== toString(value)) {
                node.defaultValue = toString(value);
              }
            }
          }
          var didWarnSelectedSetOnOption = false;
          var didWarnInvalidChild = false;
          var didWarnInvalidInnerHTML = false;
          function validateProps(element, props) {
            {
              if (props.value == null) {
                if (typeof props.children === "object" && props.children !== null) {
                  React3.Children.forEach(props.children, function(child) {
                    if (child == null) {
                      return;
                    }
                    if (typeof child === "string" || typeof child === "number") {
                      return;
                    }
                    if (!didWarnInvalidChild) {
                      didWarnInvalidChild = true;
                      error("Cannot infer the option value of complex children. Pass a `value` prop or use a plain string as children to <option>.");
                    }
                  });
                } else if (props.dangerouslySetInnerHTML != null) {
                  if (!didWarnInvalidInnerHTML) {
                    didWarnInvalidInnerHTML = true;
                    error("Pass a `value` prop if you set dangerouslyInnerHTML so React knows which value should be selected.");
                  }
                }
              }
              if (props.selected != null && !didWarnSelectedSetOnOption) {
                error("Use the `defaultValue` or `value` props on <select> instead of setting `selected` on <option>.");
                didWarnSelectedSetOnOption = true;
              }
            }
          }
          function postMountWrapper$1(element, props) {
            if (props.value != null) {
              element.setAttribute("value", toString(getToStringValue(props.value)));
            }
          }
          var isArrayImpl = Array.isArray;
          function isArray(a) {
            return isArrayImpl(a);
          }
          var didWarnValueDefaultValue$1;
          {
            didWarnValueDefaultValue$1 = false;
          }
          function getDeclarationErrorAddendum() {
            var ownerName = getCurrentFiberOwnerNameInDevOrNull();
            if (ownerName) {
              return "\n\nCheck the render method of `" + ownerName + "`.";
            }
            return "";
          }
          var valuePropNames = ["value", "defaultValue"];
          function checkSelectPropTypes(props) {
            {
              checkControlledValueProps("select", props);
              for (var i = 0; i < valuePropNames.length; i++) {
                var propName = valuePropNames[i];
                if (props[propName] == null) {
                  continue;
                }
                var propNameIsArray = isArray(props[propName]);
                if (props.multiple && !propNameIsArray) {
                  error("The `%s` prop supplied to <select> must be an array if `multiple` is true.%s", propName, getDeclarationErrorAddendum());
                } else if (!props.multiple && propNameIsArray) {
                  error("The `%s` prop supplied to <select> must be a scalar value if `multiple` is false.%s", propName, getDeclarationErrorAddendum());
                }
              }
            }
          }
          function updateOptions(node, multiple, propValue, setDefaultSelected) {
            var options2 = node.options;
            if (multiple) {
              var selectedValues = propValue;
              var selectedValue = {};
              for (var i = 0; i < selectedValues.length; i++) {
                selectedValue["$" + selectedValues[i]] = true;
              }
              for (var _i = 0; _i < options2.length; _i++) {
                var selected = selectedValue.hasOwnProperty("$" + options2[_i].value);
                if (options2[_i].selected !== selected) {
                  options2[_i].selected = selected;
                }
                if (selected && setDefaultSelected) {
                  options2[_i].defaultSelected = true;
                }
              }
            } else {
              var _selectedValue = toString(getToStringValue(propValue));
              var defaultSelected = null;
              for (var _i2 = 0; _i2 < options2.length; _i2++) {
                if (options2[_i2].value === _selectedValue) {
                  options2[_i2].selected = true;
                  if (setDefaultSelected) {
                    options2[_i2].defaultSelected = true;
                  }
                  return;
                }
                if (defaultSelected === null && !options2[_i2].disabled) {
                  defaultSelected = options2[_i2];
                }
              }
              if (defaultSelected !== null) {
                defaultSelected.selected = true;
              }
            }
          }
          function getHostProps$1(element, props) {
            return assign({}, props, {
              value: void 0
            });
          }
          function initWrapperState$1(element, props) {
            var node = element;
            {
              checkSelectPropTypes(props);
            }
            node._wrapperState = {
              wasMultiple: !!props.multiple
            };
            {
              if (props.value !== void 0 && props.defaultValue !== void 0 && !didWarnValueDefaultValue$1) {
                error("Select elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled select element and remove one of these props. More info: https://reactjs.org/link/controlled-components");
                didWarnValueDefaultValue$1 = true;
              }
            }
          }
          function postMountWrapper$2(element, props) {
            var node = element;
            node.multiple = !!props.multiple;
            var value = props.value;
            if (value != null) {
              updateOptions(node, !!props.multiple, value, false);
            } else if (props.defaultValue != null) {
              updateOptions(node, !!props.multiple, props.defaultValue, true);
            }
          }
          function postUpdateWrapper(element, props) {
            var node = element;
            var wasMultiple = node._wrapperState.wasMultiple;
            node._wrapperState.wasMultiple = !!props.multiple;
            var value = props.value;
            if (value != null) {
              updateOptions(node, !!props.multiple, value, false);
            } else if (wasMultiple !== !!props.multiple) {
              if (props.defaultValue != null) {
                updateOptions(node, !!props.multiple, props.defaultValue, true);
              } else {
                updateOptions(node, !!props.multiple, props.multiple ? [] : "", false);
              }
            }
          }
          function restoreControlledState$1(element, props) {
            var node = element;
            var value = props.value;
            if (value != null) {
              updateOptions(node, !!props.multiple, value, false);
            }
          }
          var didWarnValDefaultVal = false;
          function getHostProps$2(element, props) {
            var node = element;
            if (props.dangerouslySetInnerHTML != null) {
              throw new Error("`dangerouslySetInnerHTML` does not make sense on <textarea>.");
            }
            var hostProps = assign({}, props, {
              value: void 0,
              defaultValue: void 0,
              children: toString(node._wrapperState.initialValue)
            });
            return hostProps;
          }
          function initWrapperState$2(element, props) {
            var node = element;
            {
              checkControlledValueProps("textarea", props);
              if (props.value !== void 0 && props.defaultValue !== void 0 && !didWarnValDefaultVal) {
                error("%s contains a textarea with both value and defaultValue props. Textarea elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled textarea and remove one of these props. More info: https://reactjs.org/link/controlled-components", getCurrentFiberOwnerNameInDevOrNull() || "A component");
                didWarnValDefaultVal = true;
              }
            }
            var initialValue = props.value;
            if (initialValue == null) {
              var children = props.children, defaultValue = props.defaultValue;
              if (children != null) {
                {
                  error("Use the `defaultValue` or `value` props instead of setting children on <textarea>.");
                }
                {
                  if (defaultValue != null) {
                    throw new Error("If you supply `defaultValue` on a <textarea>, do not pass children.");
                  }
                  if (isArray(children)) {
                    if (children.length > 1) {
                      throw new Error("<textarea> can only have at most one child.");
                    }
                    children = children[0];
                  }
                  defaultValue = children;
                }
              }
              if (defaultValue == null) {
                defaultValue = "";
              }
              initialValue = defaultValue;
            }
            node._wrapperState = {
              initialValue: getToStringValue(initialValue)
            };
          }
          function updateWrapper$1(element, props) {
            var node = element;
            var value = getToStringValue(props.value);
            var defaultValue = getToStringValue(props.defaultValue);
            if (value != null) {
              var newValue = toString(value);
              if (newValue !== node.value) {
                node.value = newValue;
              }
              if (props.defaultValue == null && node.defaultValue !== newValue) {
                node.defaultValue = newValue;
              }
            }
            if (defaultValue != null) {
              node.defaultValue = toString(defaultValue);
            }
          }
          function postMountWrapper$3(element, props) {
            var node = element;
            var textContent = node.textContent;
            if (textContent === node._wrapperState.initialValue) {
              if (textContent !== "" && textContent !== null) {
                node.value = textContent;
              }
            }
          }
          function restoreControlledState$2(element, props) {
            updateWrapper$1(element, props);
          }
          var HTML_NAMESPACE = "http://www.w3.org/1999/xhtml";
          var MATH_NAMESPACE = "http://www.w3.org/1998/Math/MathML";
          var SVG_NAMESPACE = "http://www.w3.org/2000/svg";
          function getIntrinsicNamespace(type) {
            switch (type) {
              case "svg":
                return SVG_NAMESPACE;
              case "math":
                return MATH_NAMESPACE;
              default:
                return HTML_NAMESPACE;
            }
          }
          function getChildNamespace(parentNamespace, type) {
            if (parentNamespace == null || parentNamespace === HTML_NAMESPACE) {
              return getIntrinsicNamespace(type);
            }
            if (parentNamespace === SVG_NAMESPACE && type === "foreignObject") {
              return HTML_NAMESPACE;
            }
            return parentNamespace;
          }
          var createMicrosoftUnsafeLocalFunction = function(func) {
            if (typeof MSApp !== "undefined" && MSApp.execUnsafeLocalFunction) {
              return function(arg0, arg1, arg2, arg3) {
                MSApp.execUnsafeLocalFunction(function() {
                  return func(arg0, arg1, arg2, arg3);
                });
              };
            } else {
              return func;
            }
          };
          var reusableSVGContainer;
          var setInnerHTML = createMicrosoftUnsafeLocalFunction(function(node, html) {
            if (node.namespaceURI === SVG_NAMESPACE) {
              if (!("innerHTML" in node)) {
                reusableSVGContainer = reusableSVGContainer || document.createElement("div");
                reusableSVGContainer.innerHTML = "<svg>" + html.valueOf().toString() + "</svg>";
                var svgNode = reusableSVGContainer.firstChild;
                while (node.firstChild) {
                  node.removeChild(node.firstChild);
                }
                while (svgNode.firstChild) {
                  node.appendChild(svgNode.firstChild);
                }
                return;
              }
            }
            node.innerHTML = html;
          });
          var ELEMENT_NODE = 1;
          var TEXT_NODE = 3;
          var COMMENT_NODE = 8;
          var DOCUMENT_NODE = 9;
          var DOCUMENT_FRAGMENT_NODE = 11;
          var setTextContent = function(node, text) {
            if (text) {
              var firstChild = node.firstChild;
              if (firstChild && firstChild === node.lastChild && firstChild.nodeType === TEXT_NODE) {
                firstChild.nodeValue = text;
                return;
              }
            }
            node.textContent = text;
          };
          var shorthandToLonghand = {
            animation: ["animationDelay", "animationDirection", "animationDuration", "animationFillMode", "animationIterationCount", "animationName", "animationPlayState", "animationTimingFunction"],
            background: ["backgroundAttachment", "backgroundClip", "backgroundColor", "backgroundImage", "backgroundOrigin", "backgroundPositionX", "backgroundPositionY", "backgroundRepeat", "backgroundSize"],
            backgroundPosition: ["backgroundPositionX", "backgroundPositionY"],
            border: ["borderBottomColor", "borderBottomStyle", "borderBottomWidth", "borderImageOutset", "borderImageRepeat", "borderImageSlice", "borderImageSource", "borderImageWidth", "borderLeftColor", "borderLeftStyle", "borderLeftWidth", "borderRightColor", "borderRightStyle", "borderRightWidth", "borderTopColor", "borderTopStyle", "borderTopWidth"],
            borderBlockEnd: ["borderBlockEndColor", "borderBlockEndStyle", "borderBlockEndWidth"],
            borderBlockStart: ["borderBlockStartColor", "borderBlockStartStyle", "borderBlockStartWidth"],
            borderBottom: ["borderBottomColor", "borderBottomStyle", "borderBottomWidth"],
            borderColor: ["borderBottomColor", "borderLeftColor", "borderRightColor", "borderTopColor"],
            borderImage: ["borderImageOutset", "borderImageRepeat", "borderImageSlice", "borderImageSource", "borderImageWidth"],
            borderInlineEnd: ["borderInlineEndColor", "borderInlineEndStyle", "borderInlineEndWidth"],
            borderInlineStart: ["borderInlineStartColor", "borderInlineStartStyle", "borderInlineStartWidth"],
            borderLeft: ["borderLeftColor", "borderLeftStyle", "borderLeftWidth"],
            borderRadius: ["borderBottomLeftRadius", "borderBottomRightRadius", "borderTopLeftRadius", "borderTopRightRadius"],
            borderRight: ["borderRightColor", "borderRightStyle", "borderRightWidth"],
            borderStyle: ["borderBottomStyle", "borderLeftStyle", "borderRightStyle", "borderTopStyle"],
            borderTop: ["borderTopColor", "borderTopStyle", "borderTopWidth"],
            borderWidth: ["borderBottomWidth", "borderLeftWidth", "borderRightWidth", "borderTopWidth"],
            columnRule: ["columnRuleColor", "columnRuleStyle", "columnRuleWidth"],
            columns: ["columnCount", "columnWidth"],
            flex: ["flexBasis", "flexGrow", "flexShrink"],
            flexFlow: ["flexDirection", "flexWrap"],
            font: ["fontFamily", "fontFeatureSettings", "fontKerning", "fontLanguageOverride", "fontSize", "fontSizeAdjust", "fontStretch", "fontStyle", "fontVariant", "fontVariantAlternates", "fontVariantCaps", "fontVariantEastAsian", "fontVariantLigatures", "fontVariantNumeric", "fontVariantPosition", "fontWeight", "lineHeight"],
            fontVariant: ["fontVariantAlternates", "fontVariantCaps", "fontVariantEastAsian", "fontVariantLigatures", "fontVariantNumeric", "fontVariantPosition"],
            gap: ["columnGap", "rowGap"],
            grid: ["gridAutoColumns", "gridAutoFlow", "gridAutoRows", "gridTemplateAreas", "gridTemplateColumns", "gridTemplateRows"],
            gridArea: ["gridColumnEnd", "gridColumnStart", "gridRowEnd", "gridRowStart"],
            gridColumn: ["gridColumnEnd", "gridColumnStart"],
            gridColumnGap: ["columnGap"],
            gridGap: ["columnGap", "rowGap"],
            gridRow: ["gridRowEnd", "gridRowStart"],
            gridRowGap: ["rowGap"],
            gridTemplate: ["gridTemplateAreas", "gridTemplateColumns", "gridTemplateRows"],
            listStyle: ["listStyleImage", "listStylePosition", "listStyleType"],
            margin: ["marginBottom", "marginLeft", "marginRight", "marginTop"],
            marker: ["markerEnd", "markerMid", "markerStart"],
            mask: ["maskClip", "maskComposite", "maskImage", "maskMode", "maskOrigin", "maskPositionX", "maskPositionY", "maskRepeat", "maskSize"],
            maskPosition: ["maskPositionX", "maskPositionY"],
            outline: ["outlineColor", "outlineStyle", "outlineWidth"],
            overflow: ["overflowX", "overflowY"],
            padding: ["paddingBottom", "paddingLeft", "paddingRight", "paddingTop"],
            placeContent: ["alignContent", "justifyContent"],
            placeItems: ["alignItems", "justifyItems"],
            placeSelf: ["alignSelf", "justifySelf"],
            textDecoration: ["textDecorationColor", "textDecorationLine", "textDecorationStyle"],
            textEmphasis: ["textEmphasisColor", "textEmphasisStyle"],
            transition: ["transitionDelay", "transitionDuration", "transitionProperty", "transitionTimingFunction"],
            wordWrap: ["overflowWrap"]
          };
          var isUnitlessNumber = {
            animationIterationCount: true,
            aspectRatio: true,
            borderImageOutset: true,
            borderImageSlice: true,
            borderImageWidth: true,
            boxFlex: true,
            boxFlexGroup: true,
            boxOrdinalGroup: true,
            columnCount: true,
            columns: true,
            flex: true,
            flexGrow: true,
            flexPositive: true,
            flexShrink: true,
            flexNegative: true,
            flexOrder: true,
            gridArea: true,
            gridRow: true,
            gridRowEnd: true,
            gridRowSpan: true,
            gridRowStart: true,
            gridColumn: true,
            gridColumnEnd: true,
            gridColumnSpan: true,
            gridColumnStart: true,
            fontWeight: true,
            lineClamp: true,
            lineHeight: true,
            opacity: true,
            order: true,
            orphans: true,
            tabSize: true,
            widows: true,
            zIndex: true,
            zoom: true,
            // SVG-related properties
            fillOpacity: true,
            floodOpacity: true,
            stopOpacity: true,
            strokeDasharray: true,
            strokeDashoffset: true,
            strokeMiterlimit: true,
            strokeOpacity: true,
            strokeWidth: true
          };
          function prefixKey(prefix2, key) {
            return prefix2 + key.charAt(0).toUpperCase() + key.substring(1);
          }
          var prefixes = ["Webkit", "ms", "Moz", "O"];
          Object.keys(isUnitlessNumber).forEach(function(prop) {
            prefixes.forEach(function(prefix2) {
              isUnitlessNumber[prefixKey(prefix2, prop)] = isUnitlessNumber[prop];
            });
          });
          function dangerousStyleValue(name, value, isCustomProperty) {
            var isEmpty = value == null || typeof value === "boolean" || value === "";
            if (isEmpty) {
              return "";
            }
            if (!isCustomProperty && typeof value === "number" && value !== 0 && !(isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name])) {
              return value + "px";
            }
            {
              checkCSSPropertyStringCoercion(value, name);
            }
            return ("" + value).trim();
          }
          var uppercasePattern = /([A-Z])/g;
          var msPattern = /^ms-/;
          function hyphenateStyleName(name) {
            return name.replace(uppercasePattern, "-$1").toLowerCase().replace(msPattern, "-ms-");
          }
          var warnValidStyle = function() {
          };
          {
            var badVendoredStyleNamePattern = /^(?:webkit|moz|o)[A-Z]/;
            var msPattern$1 = /^-ms-/;
            var hyphenPattern = /-(.)/g;
            var badStyleValueWithSemicolonPattern = /;\s*$/;
            var warnedStyleNames = {};
            var warnedStyleValues = {};
            var warnedForNaNValue = false;
            var warnedForInfinityValue = false;
            var camelize = function(string) {
              return string.replace(hyphenPattern, function(_, character) {
                return character.toUpperCase();
              });
            };
            var warnHyphenatedStyleName = function(name) {
              if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
                return;
              }
              warnedStyleNames[name] = true;
              error(
                "Unsupported style property %s. Did you mean %s?",
                name,
                // As Andi Smith suggests
                // (http://www.andismith.com/blog/2012/02/modernizr-prefixed/), an `-ms` prefix
                // is converted to lowercase `ms`.
                camelize(name.replace(msPattern$1, "ms-"))
              );
            };
            var warnBadVendoredStyleName = function(name) {
              if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
                return;
              }
              warnedStyleNames[name] = true;
              error("Unsupported vendor-prefixed style property %s. Did you mean %s?", name, name.charAt(0).toUpperCase() + name.slice(1));
            };
            var warnStyleValueWithSemicolon = function(name, value) {
              if (warnedStyleValues.hasOwnProperty(value) && warnedStyleValues[value]) {
                return;
              }
              warnedStyleValues[value] = true;
              error(`Style property values shouldn't contain a semicolon. Try "%s: %s" instead.`, name, value.replace(badStyleValueWithSemicolonPattern, ""));
            };
            var warnStyleValueIsNaN = function(name, value) {
              if (warnedForNaNValue) {
                return;
              }
              warnedForNaNValue = true;
              error("`NaN` is an invalid value for the `%s` css style property.", name);
            };
            var warnStyleValueIsInfinity = function(name, value) {
              if (warnedForInfinityValue) {
                return;
              }
              warnedForInfinityValue = true;
              error("`Infinity` is an invalid value for the `%s` css style property.", name);
            };
            warnValidStyle = function(name, value) {
              if (name.indexOf("-") > -1) {
                warnHyphenatedStyleName(name);
              } else if (badVendoredStyleNamePattern.test(name)) {
                warnBadVendoredStyleName(name);
              } else if (badStyleValueWithSemicolonPattern.test(value)) {
                warnStyleValueWithSemicolon(name, value);
              }
              if (typeof value === "number") {
                if (isNaN(value)) {
                  warnStyleValueIsNaN(name, value);
                } else if (!isFinite(value)) {
                  warnStyleValueIsInfinity(name, value);
                }
              }
            };
          }
          var warnValidStyle$1 = warnValidStyle;
          function createDangerousStringForStyles(styles) {
            {
              var serialized = "";
              var delimiter = "";
              for (var styleName in styles) {
                if (!styles.hasOwnProperty(styleName)) {
                  continue;
                }
                var styleValue = styles[styleName];
                if (styleValue != null) {
                  var isCustomProperty = styleName.indexOf("--") === 0;
                  serialized += delimiter + (isCustomProperty ? styleName : hyphenateStyleName(styleName)) + ":";
                  serialized += dangerousStyleValue(styleName, styleValue, isCustomProperty);
                  delimiter = ";";
                }
              }
              return serialized || null;
            }
          }
          function setValueForStyles(node, styles) {
            var style2 = node.style;
            for (var styleName in styles) {
              if (!styles.hasOwnProperty(styleName)) {
                continue;
              }
              var isCustomProperty = styleName.indexOf("--") === 0;
              {
                if (!isCustomProperty) {
                  warnValidStyle$1(styleName, styles[styleName]);
                }
              }
              var styleValue = dangerousStyleValue(styleName, styles[styleName], isCustomProperty);
              if (styleName === "float") {
                styleName = "cssFloat";
              }
              if (isCustomProperty) {
                style2.setProperty(styleName, styleValue);
              } else {
                style2[styleName] = styleValue;
              }
            }
          }
          function isValueEmpty(value) {
            return value == null || typeof value === "boolean" || value === "";
          }
          function expandShorthandMap(styles) {
            var expanded = {};
            for (var key in styles) {
              var longhands = shorthandToLonghand[key] || [key];
              for (var i = 0; i < longhands.length; i++) {
                expanded[longhands[i]] = key;
              }
            }
            return expanded;
          }
          function validateShorthandPropertyCollisionInDev(styleUpdates, nextStyles) {
            {
              if (!nextStyles) {
                return;
              }
              var expandedUpdates = expandShorthandMap(styleUpdates);
              var expandedStyles = expandShorthandMap(nextStyles);
              var warnedAbout = {};
              for (var key in expandedUpdates) {
                var originalKey = expandedUpdates[key];
                var correctOriginalKey = expandedStyles[key];
                if (correctOriginalKey && originalKey !== correctOriginalKey) {
                  var warningKey = originalKey + "," + correctOriginalKey;
                  if (warnedAbout[warningKey]) {
                    continue;
                  }
                  warnedAbout[warningKey] = true;
                  error("%s a style property during rerender (%s) when a conflicting property is set (%s) can lead to styling bugs. To avoid this, don't mix shorthand and non-shorthand properties for the same value; instead, replace the shorthand with separate values.", isValueEmpty(styleUpdates[originalKey]) ? "Removing" : "Updating", originalKey, correctOriginalKey);
                }
              }
            }
          }
          var omittedCloseTags = {
            area: true,
            base: true,
            br: true,
            col: true,
            embed: true,
            hr: true,
            img: true,
            input: true,
            keygen: true,
            link: true,
            meta: true,
            param: true,
            source: true,
            track: true,
            wbr: true
            // NOTE: menuitem's close tag should be omitted, but that causes problems.
          };
          var voidElementTags = assign({
            menuitem: true
          }, omittedCloseTags);
          var HTML = "__html";
          function assertValidProps(tag, props) {
            if (!props) {
              return;
            }
            if (voidElementTags[tag]) {
              if (props.children != null || props.dangerouslySetInnerHTML != null) {
                throw new Error(tag + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`.");
              }
            }
            if (props.dangerouslySetInnerHTML != null) {
              if (props.children != null) {
                throw new Error("Can only set one of `children` or `props.dangerouslySetInnerHTML`.");
              }
              if (typeof props.dangerouslySetInnerHTML !== "object" || !(HTML in props.dangerouslySetInnerHTML)) {
                throw new Error("`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://reactjs.org/link/dangerously-set-inner-html for more information.");
              }
            }
            {
              if (!props.suppressContentEditableWarning && props.contentEditable && props.children != null) {
                error("A component is `contentEditable` and contains `children` managed by React. It is now your responsibility to guarantee that none of those nodes are unexpectedly modified or duplicated. This is probably not intentional.");
              }
            }
            if (props.style != null && typeof props.style !== "object") {
              throw new Error("The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX.");
            }
          }
          function isCustomComponent(tagName, props) {
            if (tagName.indexOf("-") === -1) {
              return typeof props.is === "string";
            }
            switch (tagName) {
              // These are reserved SVG and MathML elements.
              // We don't mind this list too much because we expect it to never grow.
              // The alternative is to track the namespace in a few places which is convoluted.
              // https://w3c.github.io/webcomponents/spec/custom/#custom-elements-core-concepts
              case "annotation-xml":
              case "color-profile":
              case "font-face":
              case "font-face-src":
              case "font-face-uri":
              case "font-face-format":
              case "font-face-name":
              case "missing-glyph":
                return false;
              default:
                return true;
            }
          }
          var possibleStandardNames = {
            // HTML
            accept: "accept",
            acceptcharset: "acceptCharset",
            "accept-charset": "acceptCharset",
            accesskey: "accessKey",
            action: "action",
            allowfullscreen: "allowFullScreen",
            alt: "alt",
            as: "as",
            async: "async",
            autocapitalize: "autoCapitalize",
            autocomplete: "autoComplete",
            autocorrect: "autoCorrect",
            autofocus: "autoFocus",
            autoplay: "autoPlay",
            autosave: "autoSave",
            capture: "capture",
            cellpadding: "cellPadding",
            cellspacing: "cellSpacing",
            challenge: "challenge",
            charset: "charSet",
            checked: "checked",
            children: "children",
            cite: "cite",
            class: "className",
            classid: "classID",
            classname: "className",
            cols: "cols",
            colspan: "colSpan",
            content: "content",
            contenteditable: "contentEditable",
            contextmenu: "contextMenu",
            controls: "controls",
            controlslist: "controlsList",
            coords: "coords",
            crossorigin: "crossOrigin",
            dangerouslysetinnerhtml: "dangerouslySetInnerHTML",
            data: "data",
            datetime: "dateTime",
            default: "default",
            defaultchecked: "defaultChecked",
            defaultvalue: "defaultValue",
            defer: "defer",
            dir: "dir",
            disabled: "disabled",
            disablepictureinpicture: "disablePictureInPicture",
            disableremoteplayback: "disableRemotePlayback",
            download: "download",
            draggable: "draggable",
            enctype: "encType",
            enterkeyhint: "enterKeyHint",
            for: "htmlFor",
            form: "form",
            formmethod: "formMethod",
            formaction: "formAction",
            formenctype: "formEncType",
            formnovalidate: "formNoValidate",
            formtarget: "formTarget",
            frameborder: "frameBorder",
            headers: "headers",
            height: "height",
            hidden: "hidden",
            high: "high",
            href: "href",
            hreflang: "hrefLang",
            htmlfor: "htmlFor",
            httpequiv: "httpEquiv",
            "http-equiv": "httpEquiv",
            icon: "icon",
            id: "id",
            imagesizes: "imageSizes",
            imagesrcset: "imageSrcSet",
            innerhtml: "innerHTML",
            inputmode: "inputMode",
            integrity: "integrity",
            is: "is",
            itemid: "itemID",
            itemprop: "itemProp",
            itemref: "itemRef",
            itemscope: "itemScope",
            itemtype: "itemType",
            keyparams: "keyParams",
            keytype: "keyType",
            kind: "kind",
            label: "label",
            lang: "lang",
            list: "list",
            loop: "loop",
            low: "low",
            manifest: "manifest",
            marginwidth: "marginWidth",
            marginheight: "marginHeight",
            max: "max",
            maxlength: "maxLength",
            media: "media",
            mediagroup: "mediaGroup",
            method: "method",
            min: "min",
            minlength: "minLength",
            multiple: "multiple",
            muted: "muted",
            name: "name",
            nomodule: "noModule",
            nonce: "nonce",
            novalidate: "noValidate",
            open: "open",
            optimum: "optimum",
            pattern: "pattern",
            placeholder: "placeholder",
            playsinline: "playsInline",
            poster: "poster",
            preload: "preload",
            profile: "profile",
            radiogroup: "radioGroup",
            readonly: "readOnly",
            referrerpolicy: "referrerPolicy",
            rel: "rel",
            required: "required",
            reversed: "reversed",
            role: "role",
            rows: "rows",
            rowspan: "rowSpan",
            sandbox: "sandbox",
            scope: "scope",
            scoped: "scoped",
            scrolling: "scrolling",
            seamless: "seamless",
            selected: "selected",
            shape: "shape",
            size: "size",
            sizes: "sizes",
            span: "span",
            spellcheck: "spellCheck",
            src: "src",
            srcdoc: "srcDoc",
            srclang: "srcLang",
            srcset: "srcSet",
            start: "start",
            step: "step",
            style: "style",
            summary: "summary",
            tabindex: "tabIndex",
            target: "target",
            title: "title",
            type: "type",
            usemap: "useMap",
            value: "value",
            width: "width",
            wmode: "wmode",
            wrap: "wrap",
            // SVG
            about: "about",
            accentheight: "accentHeight",
            "accent-height": "accentHeight",
            accumulate: "accumulate",
            additive: "additive",
            alignmentbaseline: "alignmentBaseline",
            "alignment-baseline": "alignmentBaseline",
            allowreorder: "allowReorder",
            alphabetic: "alphabetic",
            amplitude: "amplitude",
            arabicform: "arabicForm",
            "arabic-form": "arabicForm",
            ascent: "ascent",
            attributename: "attributeName",
            attributetype: "attributeType",
            autoreverse: "autoReverse",
            azimuth: "azimuth",
            basefrequency: "baseFrequency",
            baselineshift: "baselineShift",
            "baseline-shift": "baselineShift",
            baseprofile: "baseProfile",
            bbox: "bbox",
            begin: "begin",
            bias: "bias",
            by: "by",
            calcmode: "calcMode",
            capheight: "capHeight",
            "cap-height": "capHeight",
            clip: "clip",
            clippath: "clipPath",
            "clip-path": "clipPath",
            clippathunits: "clipPathUnits",
            cliprule: "clipRule",
            "clip-rule": "clipRule",
            color: "color",
            colorinterpolation: "colorInterpolation",
            "color-interpolation": "colorInterpolation",
            colorinterpolationfilters: "colorInterpolationFilters",
            "color-interpolation-filters": "colorInterpolationFilters",
            colorprofile: "colorProfile",
            "color-profile": "colorProfile",
            colorrendering: "colorRendering",
            "color-rendering": "colorRendering",
            contentscripttype: "contentScriptType",
            contentstyletype: "contentStyleType",
            cursor: "cursor",
            cx: "cx",
            cy: "cy",
            d: "d",
            datatype: "datatype",
            decelerate: "decelerate",
            descent: "descent",
            diffuseconstant: "diffuseConstant",
            direction: "direction",
            display: "display",
            divisor: "divisor",
            dominantbaseline: "dominantBaseline",
            "dominant-baseline": "dominantBaseline",
            dur: "dur",
            dx: "dx",
            dy: "dy",
            edgemode: "edgeMode",
            elevation: "elevation",
            enablebackground: "enableBackground",
            "enable-background": "enableBackground",
            end: "end",
            exponent: "exponent",
            externalresourcesrequired: "externalResourcesRequired",
            fill: "fill",
            fillopacity: "fillOpacity",
            "fill-opacity": "fillOpacity",
            fillrule: "fillRule",
            "fill-rule": "fillRule",
            filter: "filter",
            filterres: "filterRes",
            filterunits: "filterUnits",
            floodopacity: "floodOpacity",
            "flood-opacity": "floodOpacity",
            floodcolor: "floodColor",
            "flood-color": "floodColor",
            focusable: "focusable",
            fontfamily: "fontFamily",
            "font-family": "fontFamily",
            fontsize: "fontSize",
            "font-size": "fontSize",
            fontsizeadjust: "fontSizeAdjust",
            "font-size-adjust": "fontSizeAdjust",
            fontstretch: "fontStretch",
            "font-stretch": "fontStretch",
            fontstyle: "fontStyle",
            "font-style": "fontStyle",
            fontvariant: "fontVariant",
            "font-variant": "fontVariant",
            fontweight: "fontWeight",
            "font-weight": "fontWeight",
            format: "format",
            from: "from",
            fx: "fx",
            fy: "fy",
            g1: "g1",
            g2: "g2",
            glyphname: "glyphName",
            "glyph-name": "glyphName",
            glyphorientationhorizontal: "glyphOrientationHorizontal",
            "glyph-orientation-horizontal": "glyphOrientationHorizontal",
            glyphorientationvertical: "glyphOrientationVertical",
            "glyph-orientation-vertical": "glyphOrientationVertical",
            glyphref: "glyphRef",
            gradienttransform: "gradientTransform",
            gradientunits: "gradientUnits",
            hanging: "hanging",
            horizadvx: "horizAdvX",
            "horiz-adv-x": "horizAdvX",
            horizoriginx: "horizOriginX",
            "horiz-origin-x": "horizOriginX",
            ideographic: "ideographic",
            imagerendering: "imageRendering",
            "image-rendering": "imageRendering",
            in2: "in2",
            in: "in",
            inlist: "inlist",
            intercept: "intercept",
            k1: "k1",
            k2: "k2",
            k3: "k3",
            k4: "k4",
            k: "k",
            kernelmatrix: "kernelMatrix",
            kernelunitlength: "kernelUnitLength",
            kerning: "kerning",
            keypoints: "keyPoints",
            keysplines: "keySplines",
            keytimes: "keyTimes",
            lengthadjust: "lengthAdjust",
            letterspacing: "letterSpacing",
            "letter-spacing": "letterSpacing",
            lightingcolor: "lightingColor",
            "lighting-color": "lightingColor",
            limitingconeangle: "limitingConeAngle",
            local: "local",
            markerend: "markerEnd",
            "marker-end": "markerEnd",
            markerheight: "markerHeight",
            markermid: "markerMid",
            "marker-mid": "markerMid",
            markerstart: "markerStart",
            "marker-start": "markerStart",
            markerunits: "markerUnits",
            markerwidth: "markerWidth",
            mask: "mask",
            maskcontentunits: "maskContentUnits",
            maskunits: "maskUnits",
            mathematical: "mathematical",
            mode: "mode",
            numoctaves: "numOctaves",
            offset: "offset",
            opacity: "opacity",
            operator: "operator",
            order: "order",
            orient: "orient",
            orientation: "orientation",
            origin: "origin",
            overflow: "overflow",
            overlineposition: "overlinePosition",
            "overline-position": "overlinePosition",
            overlinethickness: "overlineThickness",
            "overline-thickness": "overlineThickness",
            paintorder: "paintOrder",
            "paint-order": "paintOrder",
            panose1: "panose1",
            "panose-1": "panose1",
            pathlength: "pathLength",
            patterncontentunits: "patternContentUnits",
            patterntransform: "patternTransform",
            patternunits: "patternUnits",
            pointerevents: "pointerEvents",
            "pointer-events": "pointerEvents",
            points: "points",
            pointsatx: "pointsAtX",
            pointsaty: "pointsAtY",
            pointsatz: "pointsAtZ",
            prefix: "prefix",
            preservealpha: "preserveAlpha",
            preserveaspectratio: "preserveAspectRatio",
            primitiveunits: "primitiveUnits",
            property: "property",
            r: "r",
            radius: "radius",
            refx: "refX",
            refy: "refY",
            renderingintent: "renderingIntent",
            "rendering-intent": "renderingIntent",
            repeatcount: "repeatCount",
            repeatdur: "repeatDur",
            requiredextensions: "requiredExtensions",
            requiredfeatures: "requiredFeatures",
            resource: "resource",
            restart: "restart",
            result: "result",
            results: "results",
            rotate: "rotate",
            rx: "rx",
            ry: "ry",
            scale: "scale",
            security: "security",
            seed: "seed",
            shaperendering: "shapeRendering",
            "shape-rendering": "shapeRendering",
            slope: "slope",
            spacing: "spacing",
            specularconstant: "specularConstant",
            specularexponent: "specularExponent",
            speed: "speed",
            spreadmethod: "spreadMethod",
            startoffset: "startOffset",
            stddeviation: "stdDeviation",
            stemh: "stemh",
            stemv: "stemv",
            stitchtiles: "stitchTiles",
            stopcolor: "stopColor",
            "stop-color": "stopColor",
            stopopacity: "stopOpacity",
            "stop-opacity": "stopOpacity",
            strikethroughposition: "strikethroughPosition",
            "strikethrough-position": "strikethroughPosition",
            strikethroughthickness: "strikethroughThickness",
            "strikethrough-thickness": "strikethroughThickness",
            string: "string",
            stroke: "stroke",
            strokedasharray: "strokeDasharray",
            "stroke-dasharray": "strokeDasharray",
            strokedashoffset: "strokeDashoffset",
            "stroke-dashoffset": "strokeDashoffset",
            strokelinecap: "strokeLinecap",
            "stroke-linecap": "strokeLinecap",
            strokelinejoin: "strokeLinejoin",
            "stroke-linejoin": "strokeLinejoin",
            strokemiterlimit: "strokeMiterlimit",
            "stroke-miterlimit": "strokeMiterlimit",
            strokewidth: "strokeWidth",
            "stroke-width": "strokeWidth",
            strokeopacity: "strokeOpacity",
            "stroke-opacity": "strokeOpacity",
            suppresscontenteditablewarning: "suppressContentEditableWarning",
            suppresshydrationwarning: "suppressHydrationWarning",
            surfacescale: "surfaceScale",
            systemlanguage: "systemLanguage",
            tablevalues: "tableValues",
            targetx: "targetX",
            targety: "targetY",
            textanchor: "textAnchor",
            "text-anchor": "textAnchor",
            textdecoration: "textDecoration",
            "text-decoration": "textDecoration",
            textlength: "textLength",
            textrendering: "textRendering",
            "text-rendering": "textRendering",
            to: "to",
            transform: "transform",
            typeof: "typeof",
            u1: "u1",
            u2: "u2",
            underlineposition: "underlinePosition",
            "underline-position": "underlinePosition",
            underlinethickness: "underlineThickness",
            "underline-thickness": "underlineThickness",
            unicode: "unicode",
            unicodebidi: "unicodeBidi",
            "unicode-bidi": "unicodeBidi",
            unicoderange: "unicodeRange",
            "unicode-range": "unicodeRange",
            unitsperem: "unitsPerEm",
            "units-per-em": "unitsPerEm",
            unselectable: "unselectable",
            valphabetic: "vAlphabetic",
            "v-alphabetic": "vAlphabetic",
            values: "values",
            vectoreffect: "vectorEffect",
            "vector-effect": "vectorEffect",
            version: "version",
            vertadvy: "vertAdvY",
            "vert-adv-y": "vertAdvY",
            vertoriginx: "vertOriginX",
            "vert-origin-x": "vertOriginX",
            vertoriginy: "vertOriginY",
            "vert-origin-y": "vertOriginY",
            vhanging: "vHanging",
            "v-hanging": "vHanging",
            videographic: "vIdeographic",
            "v-ideographic": "vIdeographic",
            viewbox: "viewBox",
            viewtarget: "viewTarget",
            visibility: "visibility",
            vmathematical: "vMathematical",
            "v-mathematical": "vMathematical",
            vocab: "vocab",
            widths: "widths",
            wordspacing: "wordSpacing",
            "word-spacing": "wordSpacing",
            writingmode: "writingMode",
            "writing-mode": "writingMode",
            x1: "x1",
            x2: "x2",
            x: "x",
            xchannelselector: "xChannelSelector",
            xheight: "xHeight",
            "x-height": "xHeight",
            xlinkactuate: "xlinkActuate",
            "xlink:actuate": "xlinkActuate",
            xlinkarcrole: "xlinkArcrole",
            "xlink:arcrole": "xlinkArcrole",
            xlinkhref: "xlinkHref",
            "xlink:href": "xlinkHref",
            xlinkrole: "xlinkRole",
            "xlink:role": "xlinkRole",
            xlinkshow: "xlinkShow",
            "xlink:show": "xlinkShow",
            xlinktitle: "xlinkTitle",
            "xlink:title": "xlinkTitle",
            xlinktype: "xlinkType",
            "xlink:type": "xlinkType",
            xmlbase: "xmlBase",
            "xml:base": "xmlBase",
            xmllang: "xmlLang",
            "xml:lang": "xmlLang",
            xmlns: "xmlns",
            "xml:space": "xmlSpace",
            xmlnsxlink: "xmlnsXlink",
            "xmlns:xlink": "xmlnsXlink",
            xmlspace: "xmlSpace",
            y1: "y1",
            y2: "y2",
            y: "y",
            ychannelselector: "yChannelSelector",
            z: "z",
            zoomandpan: "zoomAndPan"
          };
          var ariaProperties = {
            "aria-current": 0,
            // state
            "aria-description": 0,
            "aria-details": 0,
            "aria-disabled": 0,
            // state
            "aria-hidden": 0,
            // state
            "aria-invalid": 0,
            // state
            "aria-keyshortcuts": 0,
            "aria-label": 0,
            "aria-roledescription": 0,
            // Widget Attributes
            "aria-autocomplete": 0,
            "aria-checked": 0,
            "aria-expanded": 0,
            "aria-haspopup": 0,
            "aria-level": 0,
            "aria-modal": 0,
            "aria-multiline": 0,
            "aria-multiselectable": 0,
            "aria-orientation": 0,
            "aria-placeholder": 0,
            "aria-pressed": 0,
            "aria-readonly": 0,
            "aria-required": 0,
            "aria-selected": 0,
            "aria-sort": 0,
            "aria-valuemax": 0,
            "aria-valuemin": 0,
            "aria-valuenow": 0,
            "aria-valuetext": 0,
            // Live Region Attributes
            "aria-atomic": 0,
            "aria-busy": 0,
            "aria-live": 0,
            "aria-relevant": 0,
            // Drag-and-Drop Attributes
            "aria-dropeffect": 0,
            "aria-grabbed": 0,
            // Relationship Attributes
            "aria-activedescendant": 0,
            "aria-colcount": 0,
            "aria-colindex": 0,
            "aria-colspan": 0,
            "aria-controls": 0,
            "aria-describedby": 0,
            "aria-errormessage": 0,
            "aria-flowto": 0,
            "aria-labelledby": 0,
            "aria-owns": 0,
            "aria-posinset": 0,
            "aria-rowcount": 0,
            "aria-rowindex": 0,
            "aria-rowspan": 0,
            "aria-setsize": 0
          };
          var warnedProperties = {};
          var rARIA = new RegExp("^(aria)-[" + ATTRIBUTE_NAME_CHAR + "]*$");
          var rARIACamel = new RegExp("^(aria)[A-Z][" + ATTRIBUTE_NAME_CHAR + "]*$");
          function validateProperty(tagName, name) {
            {
              if (hasOwnProperty.call(warnedProperties, name) && warnedProperties[name]) {
                return true;
              }
              if (rARIACamel.test(name)) {
                var ariaName = "aria-" + name.slice(4).toLowerCase();
                var correctName = ariaProperties.hasOwnProperty(ariaName) ? ariaName : null;
                if (correctName == null) {
                  error("Invalid ARIA attribute `%s`. ARIA attributes follow the pattern aria-* and must be lowercase.", name);
                  warnedProperties[name] = true;
                  return true;
                }
                if (name !== correctName) {
                  error("Invalid ARIA attribute `%s`. Did you mean `%s`?", name, correctName);
                  warnedProperties[name] = true;
                  return true;
                }
              }
              if (rARIA.test(name)) {
                var lowerCasedName = name.toLowerCase();
                var standardName = ariaProperties.hasOwnProperty(lowerCasedName) ? lowerCasedName : null;
                if (standardName == null) {
                  warnedProperties[name] = true;
                  return false;
                }
                if (name !== standardName) {
                  error("Unknown ARIA attribute `%s`. Did you mean `%s`?", name, standardName);
                  warnedProperties[name] = true;
                  return true;
                }
              }
            }
            return true;
          }
          function warnInvalidARIAProps(type, props) {
            {
              var invalidProps = [];
              for (var key in props) {
                var isValid = validateProperty(type, key);
                if (!isValid) {
                  invalidProps.push(key);
                }
              }
              var unknownPropString = invalidProps.map(function(prop) {
                return "`" + prop + "`";
              }).join(", ");
              if (invalidProps.length === 1) {
                error("Invalid aria prop %s on <%s> tag. For details, see https://reactjs.org/link/invalid-aria-props", unknownPropString, type);
              } else if (invalidProps.length > 1) {
                error("Invalid aria props %s on <%s> tag. For details, see https://reactjs.org/link/invalid-aria-props", unknownPropString, type);
              }
            }
          }
          function validateProperties(type, props) {
            if (isCustomComponent(type, props)) {
              return;
            }
            warnInvalidARIAProps(type, props);
          }
          var didWarnValueNull = false;
          function validateProperties$1(type, props) {
            {
              if (type !== "input" && type !== "textarea" && type !== "select") {
                return;
              }
              if (props != null && props.value === null && !didWarnValueNull) {
                didWarnValueNull = true;
                if (type === "select" && props.multiple) {
                  error("`value` prop on `%s` should not be null. Consider using an empty array when `multiple` is set to `true` to clear the component or `undefined` for uncontrolled components.", type);
                } else {
                  error("`value` prop on `%s` should not be null. Consider using an empty string to clear the component or `undefined` for uncontrolled components.", type);
                }
              }
            }
          }
          var validateProperty$1 = function() {
          };
          {
            var warnedProperties$1 = {};
            var EVENT_NAME_REGEX = /^on./;
            var INVALID_EVENT_NAME_REGEX = /^on[^A-Z]/;
            var rARIA$1 = new RegExp("^(aria)-[" + ATTRIBUTE_NAME_CHAR + "]*$");
            var rARIACamel$1 = new RegExp("^(aria)[A-Z][" + ATTRIBUTE_NAME_CHAR + "]*$");
            validateProperty$1 = function(tagName, name, value, eventRegistry) {
              if (hasOwnProperty.call(warnedProperties$1, name) && warnedProperties$1[name]) {
                return true;
              }
              var lowerCasedName = name.toLowerCase();
              if (lowerCasedName === "onfocusin" || lowerCasedName === "onfocusout") {
                error("React uses onFocus and onBlur instead of onFocusIn and onFocusOut. All React events are normalized to bubble, so onFocusIn and onFocusOut are not needed/supported by React.");
                warnedProperties$1[name] = true;
                return true;
              }
              if (eventRegistry != null) {
                var registrationNameDependencies2 = eventRegistry.registrationNameDependencies, possibleRegistrationNames2 = eventRegistry.possibleRegistrationNames;
                if (registrationNameDependencies2.hasOwnProperty(name)) {
                  return true;
                }
                var registrationName = possibleRegistrationNames2.hasOwnProperty(lowerCasedName) ? possibleRegistrationNames2[lowerCasedName] : null;
                if (registrationName != null) {
                  error("Invalid event handler property `%s`. Did you mean `%s`?", name, registrationName);
                  warnedProperties$1[name] = true;
                  return true;
                }
                if (EVENT_NAME_REGEX.test(name)) {
                  error("Unknown event handler property `%s`. It will be ignored.", name);
                  warnedProperties$1[name] = true;
                  return true;
                }
              } else if (EVENT_NAME_REGEX.test(name)) {
                if (INVALID_EVENT_NAME_REGEX.test(name)) {
                  error("Invalid event handler property `%s`. React events use the camelCase naming convention, for example `onClick`.", name);
                }
                warnedProperties$1[name] = true;
                return true;
              }
              if (rARIA$1.test(name) || rARIACamel$1.test(name)) {
                return true;
              }
              if (lowerCasedName === "innerhtml") {
                error("Directly setting property `innerHTML` is not permitted. For more information, lookup documentation on `dangerouslySetInnerHTML`.");
                warnedProperties$1[name] = true;
                return true;
              }
              if (lowerCasedName === "aria") {
                error("The `aria` attribute is reserved for future use in React. Pass individual `aria-` attributes instead.");
                warnedProperties$1[name] = true;
                return true;
              }
              if (lowerCasedName === "is" && value !== null && value !== void 0 && typeof value !== "string") {
                error("Received a `%s` for a string attribute `is`. If this is expected, cast the value to a string.", typeof value);
                warnedProperties$1[name] = true;
                return true;
              }
              if (typeof value === "number" && isNaN(value)) {
                error("Received NaN for the `%s` attribute. If this is expected, cast the value to a string.", name);
                warnedProperties$1[name] = true;
                return true;
              }
              var propertyInfo = getPropertyInfo(name);
              var isReserved = propertyInfo !== null && propertyInfo.type === RESERVED;
              if (possibleStandardNames.hasOwnProperty(lowerCasedName)) {
                var standardName = possibleStandardNames[lowerCasedName];
                if (standardName !== name) {
                  error("Invalid DOM property `%s`. Did you mean `%s`?", name, standardName);
                  warnedProperties$1[name] = true;
                  return true;
                }
              } else if (!isReserved && name !== lowerCasedName) {
                error("React does not recognize the `%s` prop on a DOM element. If you intentionally want it to appear in the DOM as a custom attribute, spell it as lowercase `%s` instead. If you accidentally passed it from a parent component, remove it from the DOM element.", name, lowerCasedName);
                warnedProperties$1[name] = true;
                return true;
              }
              if (typeof value === "boolean" && shouldRemoveAttributeWithWarning(name, value, propertyInfo, false)) {
                if (value) {
                  error('Received `%s` for a non-boolean attribute `%s`.\n\nIf you want to write it to the DOM, pass a string instead: %s="%s" or %s={value.toString()}.', value, name, name, value, name);
                } else {
                  error('Received `%s` for a non-boolean attribute `%s`.\n\nIf you want to write it to the DOM, pass a string instead: %s="%s" or %s={value.toString()}.\n\nIf you used to conditionally omit it with %s={condition && value}, pass %s={condition ? value : undefined} instead.', value, name, name, value, name, name, name);
                }
                warnedProperties$1[name] = true;
                return true;
              }
              if (isReserved) {
                return true;
              }
              if (shouldRemoveAttributeWithWarning(name, value, propertyInfo, false)) {
                warnedProperties$1[name] = true;
                return false;
              }
              if ((value === "false" || value === "true") && propertyInfo !== null && propertyInfo.type === BOOLEAN) {
                error("Received the string `%s` for the boolean attribute `%s`. %s Did you mean %s={%s}?", value, name, value === "false" ? "The browser will interpret it as a truthy value." : 'Although this works, it will not work as expected if you pass the string "false".', name, value);
                warnedProperties$1[name] = true;
                return true;
              }
              return true;
            };
          }
          var warnUnknownProperties = function(type, props, eventRegistry) {
            {
              var unknownProps = [];
              for (var key in props) {
                var isValid = validateProperty$1(type, key, props[key], eventRegistry);
                if (!isValid) {
                  unknownProps.push(key);
                }
              }
              var unknownPropString = unknownProps.map(function(prop) {
                return "`" + prop + "`";
              }).join(", ");
              if (unknownProps.length === 1) {
                error("Invalid value for prop %s on <%s> tag. Either remove it from the element, or pass a string or number value to keep it in the DOM. For details, see https://reactjs.org/link/attribute-behavior ", unknownPropString, type);
              } else if (unknownProps.length > 1) {
                error("Invalid values for props %s on <%s> tag. Either remove them from the element, or pass a string or number value to keep them in the DOM. For details, see https://reactjs.org/link/attribute-behavior ", unknownPropString, type);
              }
            }
          };
          function validateProperties$2(type, props, eventRegistry) {
            if (isCustomComponent(type, props)) {
              return;
            }
            warnUnknownProperties(type, props, eventRegistry);
          }
          var IS_EVENT_HANDLE_NON_MANAGED_NODE = 1;
          var IS_NON_DELEGATED = 1 << 1;
          var IS_CAPTURE_PHASE = 1 << 2;
          var SHOULD_NOT_PROCESS_POLYFILL_EVENT_PLUGINS = IS_EVENT_HANDLE_NON_MANAGED_NODE | IS_NON_DELEGATED | IS_CAPTURE_PHASE;
          var currentReplayingEvent = null;
          function setReplayingEvent(event) {
            {
              if (currentReplayingEvent !== null) {
                error("Expected currently replaying event to be null. This error is likely caused by a bug in React. Please file an issue.");
              }
            }
            currentReplayingEvent = event;
          }
          function resetReplayingEvent() {
            {
              if (currentReplayingEvent === null) {
                error("Expected currently replaying event to not be null. This error is likely caused by a bug in React. Please file an issue.");
              }
            }
            currentReplayingEvent = null;
          }
          function isReplayingEvent(event) {
            return event === currentReplayingEvent;
          }
          function getEventTarget(nativeEvent) {
            var target = nativeEvent.target || nativeEvent.srcElement || window;
            if (target.correspondingUseElement) {
              target = target.correspondingUseElement;
            }
            return target.nodeType === TEXT_NODE ? target.parentNode : target;
          }
          var restoreImpl = null;
          var restoreTarget = null;
          var restoreQueue = null;
          function restoreStateOfTarget(target) {
            var internalInstance = getInstanceFromNode(target);
            if (!internalInstance) {
              return;
            }
            if (typeof restoreImpl !== "function") {
              throw new Error("setRestoreImplementation() needs to be called to handle a target for controlled events. This error is likely caused by a bug in React. Please file an issue.");
            }
            var stateNode = internalInstance.stateNode;
            if (stateNode) {
              var _props = getFiberCurrentPropsFromNode(stateNode);
              restoreImpl(internalInstance.stateNode, internalInstance.type, _props);
            }
          }
          function setRestoreImplementation(impl) {
            restoreImpl = impl;
          }
          function enqueueStateRestore(target) {
            if (restoreTarget) {
              if (restoreQueue) {
                restoreQueue.push(target);
              } else {
                restoreQueue = [target];
              }
            } else {
              restoreTarget = target;
            }
          }
          function needsStateRestore() {
            return restoreTarget !== null || restoreQueue !== null;
          }
          function restoreStateIfNeeded() {
            if (!restoreTarget) {
              return;
            }
            var target = restoreTarget;
            var queuedTargets = restoreQueue;
            restoreTarget = null;
            restoreQueue = null;
            restoreStateOfTarget(target);
            if (queuedTargets) {
              for (var i = 0; i < queuedTargets.length; i++) {
                restoreStateOfTarget(queuedTargets[i]);
              }
            }
          }
          var batchedUpdatesImpl = function(fn, bookkeeping) {
            return fn(bookkeeping);
          };
          var flushSyncImpl = function() {
          };
          var isInsideEventHandler = false;
          function finishEventHandler() {
            var controlledComponentsHavePendingUpdates = needsStateRestore();
            if (controlledComponentsHavePendingUpdates) {
              flushSyncImpl();
              restoreStateIfNeeded();
            }
          }
          function batchedUpdates(fn, a, b) {
            if (isInsideEventHandler) {
              return fn(a, b);
            }
            isInsideEventHandler = true;
            try {
              return batchedUpdatesImpl(fn, a, b);
            } finally {
              isInsideEventHandler = false;
              finishEventHandler();
            }
          }
          function setBatchingImplementation(_batchedUpdatesImpl, _discreteUpdatesImpl, _flushSyncImpl) {
            batchedUpdatesImpl = _batchedUpdatesImpl;
            flushSyncImpl = _flushSyncImpl;
          }
          function isInteractive(tag) {
            return tag === "button" || tag === "input" || tag === "select" || tag === "textarea";
          }
          function shouldPreventMouseEvent(name, type, props) {
            switch (name) {
              case "onClick":
              case "onClickCapture":
              case "onDoubleClick":
              case "onDoubleClickCapture":
              case "onMouseDown":
              case "onMouseDownCapture":
              case "onMouseMove":
              case "onMouseMoveCapture":
              case "onMouseUp":
              case "onMouseUpCapture":
              case "onMouseEnter":
                return !!(props.disabled && isInteractive(type));
              default:
                return false;
            }
          }
          function getListener(inst, registrationName) {
            var stateNode = inst.stateNode;
            if (stateNode === null) {
              return null;
            }
            var props = getFiberCurrentPropsFromNode(stateNode);
            if (props === null) {
              return null;
            }
            var listener = props[registrationName];
            if (shouldPreventMouseEvent(registrationName, inst.type, props)) {
              return null;
            }
            if (listener && typeof listener !== "function") {
              throw new Error("Expected `" + registrationName + "` listener to be a function, instead got a value of `" + typeof listener + "` type.");
            }
            return listener;
          }
          var passiveBrowserEventsSupported = false;
          if (canUseDOM) {
            try {
              var options = {};
              Object.defineProperty(options, "passive", {
                get: function() {
                  passiveBrowserEventsSupported = true;
                }
              });
              window.addEventListener("test", options, options);
              window.removeEventListener("test", options, options);
            } catch (e) {
              passiveBrowserEventsSupported = false;
            }
          }
          function invokeGuardedCallbackProd(name, func, context, a, b, c, d, e, f) {
            var funcArgs = Array.prototype.slice.call(arguments, 3);
            try {
              func.apply(context, funcArgs);
            } catch (error2) {
              this.onError(error2);
            }
          }
          var invokeGuardedCallbackImpl = invokeGuardedCallbackProd;
          {
            if (typeof window !== "undefined" && typeof window.dispatchEvent === "function" && typeof document !== "undefined" && typeof document.createEvent === "function") {
              var fakeNode = document.createElement("react");
              invokeGuardedCallbackImpl = function invokeGuardedCallbackDev(name, func, context, a, b, c, d, e, f) {
                if (typeof document === "undefined" || document === null) {
                  throw new Error("The `document` global was defined when React was initialized, but is not defined anymore. This can happen in a test environment if a component schedules an update from an asynchronous callback, but the test has already finished running. To solve this, you can either unmount the component at the end of your test (and ensure that any asynchronous operations get canceled in `componentWillUnmount`), or you can change the test itself to be asynchronous.");
                }
                var evt = document.createEvent("Event");
                var didCall = false;
                var didError = true;
                var windowEvent = window.event;
                var windowEventDescriptor = Object.getOwnPropertyDescriptor(window, "event");
                function restoreAfterDispatch() {
                  fakeNode.removeEventListener(evtType, callCallback2, false);
                  if (typeof window.event !== "undefined" && window.hasOwnProperty("event")) {
                    window.event = windowEvent;
                  }
                }
                var funcArgs = Array.prototype.slice.call(arguments, 3);
                function callCallback2() {
                  didCall = true;
                  restoreAfterDispatch();
                  func.apply(context, funcArgs);
                  didError = false;
                }
                var error2;
                var didSetError = false;
                var isCrossOriginError = false;
                function handleWindowError(event) {
                  error2 = event.error;
                  didSetError = true;
                  if (error2 === null && event.colno === 0 && event.lineno === 0) {
                    isCrossOriginError = true;
                  }
                  if (event.defaultPrevented) {
                    if (error2 != null && typeof error2 === "object") {
                      try {
                        error2._suppressLogging = true;
                      } catch (inner) {
                      }
                    }
                  }
                }
                var evtType = "react-" + (name ? name : "invokeguardedcallback");
                window.addEventListener("error", handleWindowError);
                fakeNode.addEventListener(evtType, callCallback2, false);
                evt.initEvent(evtType, false, false);
                fakeNode.dispatchEvent(evt);
                if (windowEventDescriptor) {
                  Object.defineProperty(window, "event", windowEventDescriptor);
                }
                if (didCall && didError) {
                  if (!didSetError) {
                    error2 = new Error(`An error was thrown inside one of your components, but React doesn't know what it was. This is likely due to browser flakiness. React does its best to preserve the "Pause on exceptions" behavior of the DevTools, which requires some DEV-mode only tricks. It's possible that these don't work in your browser. Try triggering the error in production mode, or switching to a modern browser. If you suspect that this is actually an issue with React, please file an issue.`);
                  } else if (isCrossOriginError) {
                    error2 = new Error("A cross-origin error was thrown. React doesn't have access to the actual error object in development. See https://reactjs.org/link/crossorigin-error for more information.");
                  }
                  this.onError(error2);
                }
                window.removeEventListener("error", handleWindowError);
                if (!didCall) {
                  restoreAfterDispatch();
                  return invokeGuardedCallbackProd.apply(this, arguments);
                }
              };
            }
          }
          var invokeGuardedCallbackImpl$1 = invokeGuardedCallbackImpl;
          var hasError = false;
          var caughtError = null;
          var hasRethrowError = false;
          var rethrowError = null;
          var reporter = {
            onError: function(error2) {
              hasError = true;
              caughtError = error2;
            }
          };
          function invokeGuardedCallback(name, func, context, a, b, c, d, e, f) {
            hasError = false;
            caughtError = null;
            invokeGuardedCallbackImpl$1.apply(reporter, arguments);
          }
          function invokeGuardedCallbackAndCatchFirstError(name, func, context, a, b, c, d, e, f) {
            invokeGuardedCallback.apply(this, arguments);
            if (hasError) {
              var error2 = clearCaughtError();
              if (!hasRethrowError) {
                hasRethrowError = true;
                rethrowError = error2;
              }
            }
          }
          function rethrowCaughtError() {
            if (hasRethrowError) {
              var error2 = rethrowError;
              hasRethrowError = false;
              rethrowError = null;
              throw error2;
            }
          }
          function hasCaughtError() {
            return hasError;
          }
          function clearCaughtError() {
            if (hasError) {
              var error2 = caughtError;
              hasError = false;
              caughtError = null;
              return error2;
            } else {
              throw new Error("clearCaughtError was called but no error was captured. This error is likely caused by a bug in React. Please file an issue.");
            }
          }
          function get(key) {
            return key._reactInternals;
          }
          function has(key) {
            return key._reactInternals !== void 0;
          }
          function set(key, value) {
            key._reactInternals = value;
          }
          var NoFlags = (
            /*                      */
            0
          );
          var PerformedWork = (
            /*                */
            1
          );
          var Placement = (
            /*                    */
            2
          );
          var Update = (
            /*                       */
            4
          );
          var ChildDeletion = (
            /*                */
            16
          );
          var ContentReset = (
            /*                 */
            32
          );
          var Callback = (
            /*                     */
            64
          );
          var DidCapture = (
            /*                   */
            128
          );
          var ForceClientRender = (
            /*            */
            256
          );
          var Ref = (
            /*                          */
            512
          );
          var Snapshot = (
            /*                     */
            1024
          );
          var Passive = (
            /*                      */
            2048
          );
          var Hydrating = (
            /*                    */
            4096
          );
          var Visibility = (
            /*                   */
            8192
          );
          var StoreConsistency = (
            /*             */
            16384
          );
          var LifecycleEffectMask = Passive | Update | Callback | Ref | Snapshot | StoreConsistency;
          var HostEffectMask = (
            /*               */
            32767
          );
          var Incomplete = (
            /*                   */
            32768
          );
          var ShouldCapture = (
            /*                */
            65536
          );
          var ForceUpdateForLegacySuspense = (
            /* */
            131072
          );
          var Forked = (
            /*                       */
            1048576
          );
          var RefStatic = (
            /*                    */
            2097152
          );
          var LayoutStatic = (
            /*                 */
            4194304
          );
          var PassiveStatic = (
            /*                */
            8388608
          );
          var MountLayoutDev = (
            /*               */
            16777216
          );
          var MountPassiveDev = (
            /*              */
            33554432
          );
          var BeforeMutationMask = (
            // TODO: Remove Update flag from before mutation phase by re-landing Visibility
            // flag logic (see #20043)
            Update | Snapshot | 0
          );
          var MutationMask = Placement | Update | ChildDeletion | ContentReset | Ref | Hydrating | Visibility;
          var LayoutMask = Update | Callback | Ref | Visibility;
          var PassiveMask = Passive | ChildDeletion;
          var StaticMask = LayoutStatic | PassiveStatic | RefStatic;
          var ReactCurrentOwner = ReactSharedInternals.ReactCurrentOwner;
          function getNearestMountedFiber(fiber) {
            var node = fiber;
            var nearestMounted = fiber;
            if (!fiber.alternate) {
              var nextNode = node;
              do {
                node = nextNode;
                if ((node.flags & (Placement | Hydrating)) !== NoFlags) {
                  nearestMounted = node.return;
                }
                nextNode = node.return;
              } while (nextNode);
            } else {
              while (node.return) {
                node = node.return;
              }
            }
            if (node.tag === HostRoot) {
              return nearestMounted;
            }
            return null;
          }
          function getSuspenseInstanceFromFiber(fiber) {
            if (fiber.tag === SuspenseComponent) {
              var suspenseState = fiber.memoizedState;
              if (suspenseState === null) {
                var current2 = fiber.alternate;
                if (current2 !== null) {
                  suspenseState = current2.memoizedState;
                }
              }
              if (suspenseState !== null) {
                return suspenseState.dehydrated;
              }
            }
            return null;
          }
          function getContainerFromFiber(fiber) {
            return fiber.tag === HostRoot ? fiber.stateNode.containerInfo : null;
          }
          function isFiberMounted(fiber) {
            return getNearestMountedFiber(fiber) === fiber;
          }
          function isMounted(component) {
            {
              var owner = ReactCurrentOwner.current;
              if (owner !== null && owner.tag === ClassComponent) {
                var ownerFiber = owner;
                var instance = ownerFiber.stateNode;
                if (!instance._warnedAboutRefsInRender) {
                  error("%s is accessing isMounted inside its render() function. render() should be a pure function of props and state. It should never access something that requires stale data from the previous render, such as refs. Move this logic to componentDidMount and componentDidUpdate instead.", getComponentNameFromFiber(ownerFiber) || "A component");
                }
                instance._warnedAboutRefsInRender = true;
              }
            }
            var fiber = get(component);
            if (!fiber) {
              return false;
            }
            return getNearestMountedFiber(fiber) === fiber;
          }
          function assertIsMounted(fiber) {
            if (getNearestMountedFiber(fiber) !== fiber) {
              throw new Error("Unable to find node on an unmounted component.");
            }
          }
          function findCurrentFiberUsingSlowPath(fiber) {
            var alternate = fiber.alternate;
            if (!alternate) {
              var nearestMounted = getNearestMountedFiber(fiber);
              if (nearestMounted === null) {
                throw new Error("Unable to find node on an unmounted component.");
              }
              if (nearestMounted !== fiber) {
                return null;
              }
              return fiber;
            }
            var a = fiber;
            var b = alternate;
            while (true) {
              var parentA = a.return;
              if (parentA === null) {
                break;
              }
              var parentB = parentA.alternate;
              if (parentB === null) {
                var nextParent = parentA.return;
                if (nextParent !== null) {
                  a = b = nextParent;
                  continue;
                }
                break;
              }
              if (parentA.child === parentB.child) {
                var child = parentA.child;
                while (child) {
                  if (child === a) {
                    assertIsMounted(parentA);
                    return fiber;
                  }
                  if (child === b) {
                    assertIsMounted(parentA);
                    return alternate;
                  }
                  child = child.sibling;
                }
                throw new Error("Unable to find node on an unmounted component.");
              }
              if (a.return !== b.return) {
                a = parentA;
                b = parentB;
              } else {
                var didFindChild = false;
                var _child = parentA.child;
                while (_child) {
                  if (_child === a) {
                    didFindChild = true;
                    a = parentA;
                    b = parentB;
                    break;
                  }
                  if (_child === b) {
                    didFindChild = true;
                    b = parentA;
                    a = parentB;
                    break;
                  }
                  _child = _child.sibling;
                }
                if (!didFindChild) {
                  _child = parentB.child;
                  while (_child) {
                    if (_child === a) {
                      didFindChild = true;
                      a = parentB;
                      b = parentA;
                      break;
                    }
                    if (_child === b) {
                      didFindChild = true;
                      b = parentB;
                      a = parentA;
                      break;
                    }
                    _child = _child.sibling;
                  }
                  if (!didFindChild) {
                    throw new Error("Child was not found in either parent set. This indicates a bug in React related to the return pointer. Please file an issue.");
                  }
                }
              }
              if (a.alternate !== b) {
                throw new Error("Return fibers should always be each others' alternates. This error is likely caused by a bug in React. Please file an issue.");
              }
            }
            if (a.tag !== HostRoot) {
              throw new Error("Unable to find node on an unmounted component.");
            }
            if (a.stateNode.current === a) {
              return fiber;
            }
            return alternate;
          }
          function findCurrentHostFiber(parent) {
            var currentParent = findCurrentFiberUsingSlowPath(parent);
            return currentParent !== null ? findCurrentHostFiberImpl(currentParent) : null;
          }
          function findCurrentHostFiberImpl(node) {
            if (node.tag === HostComponent || node.tag === HostText) {
              return node;
            }
            var child = node.child;
            while (child !== null) {
              var match = findCurrentHostFiberImpl(child);
              if (match !== null) {
                return match;
              }
              child = child.sibling;
            }
            return null;
          }
          function findCurrentHostFiberWithNoPortals(parent) {
            var currentParent = findCurrentFiberUsingSlowPath(parent);
            return currentParent !== null ? findCurrentHostFiberWithNoPortalsImpl(currentParent) : null;
          }
          function findCurrentHostFiberWithNoPortalsImpl(node) {
            if (node.tag === HostComponent || node.tag === HostText) {
              return node;
            }
            var child = node.child;
            while (child !== null) {
              if (child.tag !== HostPortal) {
                var match = findCurrentHostFiberWithNoPortalsImpl(child);
                if (match !== null) {
                  return match;
                }
              }
              child = child.sibling;
            }
            return null;
          }
          var scheduleCallback = Scheduler.unstable_scheduleCallback;
          var cancelCallback = Scheduler.unstable_cancelCallback;
          var shouldYield = Scheduler.unstable_shouldYield;
          var requestPaint = Scheduler.unstable_requestPaint;
          var now = Scheduler.unstable_now;
          var getCurrentPriorityLevel = Scheduler.unstable_getCurrentPriorityLevel;
          var ImmediatePriority = Scheduler.unstable_ImmediatePriority;
          var UserBlockingPriority = Scheduler.unstable_UserBlockingPriority;
          var NormalPriority = Scheduler.unstable_NormalPriority;
          var LowPriority = Scheduler.unstable_LowPriority;
          var IdlePriority = Scheduler.unstable_IdlePriority;
          var unstable_yieldValue = Scheduler.unstable_yieldValue;
          var unstable_setDisableYieldValue = Scheduler.unstable_setDisableYieldValue;
          var rendererID = null;
          var injectedHook = null;
          var injectedProfilingHooks = null;
          var hasLoggedError = false;
          var isDevToolsPresent = typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined";
          function injectInternals(internals) {
            if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === "undefined") {
              return false;
            }
            var hook = __REACT_DEVTOOLS_GLOBAL_HOOK__;
            if (hook.isDisabled) {
              return true;
            }
            if (!hook.supportsFiber) {
              {
                error("The installed version of React DevTools is too old and will not work with the current version of React. Please update React DevTools. https://reactjs.org/link/react-devtools");
              }
              return true;
            }
            try {
              if (enableSchedulingProfiler) {
                internals = assign({}, internals, {
                  getLaneLabelMap,
                  injectProfilingHooks
                });
              }
              rendererID = hook.inject(internals);
              injectedHook = hook;
            } catch (err) {
              {
                error("React instrumentation encountered an error: %s.", err);
              }
            }
            if (hook.checkDCE) {
              return true;
            } else {
              return false;
            }
          }
          function onScheduleRoot(root3, children) {
            {
              if (injectedHook && typeof injectedHook.onScheduleFiberRoot === "function") {
                try {
                  injectedHook.onScheduleFiberRoot(rendererID, root3, children);
                } catch (err) {
                  if (!hasLoggedError) {
                    hasLoggedError = true;
                    error("React instrumentation encountered an error: %s", err);
                  }
                }
              }
            }
          }
          function onCommitRoot(root3, eventPriority) {
            if (injectedHook && typeof injectedHook.onCommitFiberRoot === "function") {
              try {
                var didError = (root3.current.flags & DidCapture) === DidCapture;
                if (enableProfilerTimer) {
                  var schedulerPriority;
                  switch (eventPriority) {
                    case DiscreteEventPriority:
                      schedulerPriority = ImmediatePriority;
                      break;
                    case ContinuousEventPriority:
                      schedulerPriority = UserBlockingPriority;
                      break;
                    case DefaultEventPriority:
                      schedulerPriority = NormalPriority;
                      break;
                    case IdleEventPriority:
                      schedulerPriority = IdlePriority;
                      break;
                    default:
                      schedulerPriority = NormalPriority;
                      break;
                  }
                  injectedHook.onCommitFiberRoot(rendererID, root3, schedulerPriority, didError);
                } else {
                  injectedHook.onCommitFiberRoot(rendererID, root3, void 0, didError);
                }
              } catch (err) {
                {
                  if (!hasLoggedError) {
                    hasLoggedError = true;
                    error("React instrumentation encountered an error: %s", err);
                  }
                }
              }
            }
          }
          function onPostCommitRoot(root3) {
            if (injectedHook && typeof injectedHook.onPostCommitFiberRoot === "function") {
              try {
                injectedHook.onPostCommitFiberRoot(rendererID, root3);
              } catch (err) {
                {
                  if (!hasLoggedError) {
                    hasLoggedError = true;
                    error("React instrumentation encountered an error: %s", err);
                  }
                }
              }
            }
          }
          function onCommitUnmount(fiber) {
            if (injectedHook && typeof injectedHook.onCommitFiberUnmount === "function") {
              try {
                injectedHook.onCommitFiberUnmount(rendererID, fiber);
              } catch (err) {
                {
                  if (!hasLoggedError) {
                    hasLoggedError = true;
                    error("React instrumentation encountered an error: %s", err);
                  }
                }
              }
            }
          }
          function setIsStrictModeForDevtools(newIsStrictMode) {
            {
              if (typeof unstable_yieldValue === "function") {
                unstable_setDisableYieldValue(newIsStrictMode);
                setSuppressWarning(newIsStrictMode);
              }
              if (injectedHook && typeof injectedHook.setStrictMode === "function") {
                try {
                  injectedHook.setStrictMode(rendererID, newIsStrictMode);
                } catch (err) {
                  {
                    if (!hasLoggedError) {
                      hasLoggedError = true;
                      error("React instrumentation encountered an error: %s", err);
                    }
                  }
                }
              }
            }
          }
          function injectProfilingHooks(profilingHooks) {
            injectedProfilingHooks = profilingHooks;
          }
          function getLaneLabelMap() {
            {
              var map = /* @__PURE__ */ new Map();
              var lane = 1;
              for (var index2 = 0; index2 < TotalLanes; index2++) {
                var label = getLabelForLane(lane);
                map.set(lane, label);
                lane *= 2;
              }
              return map;
            }
          }
          function markCommitStarted(lanes) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markCommitStarted === "function") {
                injectedProfilingHooks.markCommitStarted(lanes);
              }
            }
          }
          function markCommitStopped() {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markCommitStopped === "function") {
                injectedProfilingHooks.markCommitStopped();
              }
            }
          }
          function markComponentRenderStarted(fiber) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentRenderStarted === "function") {
                injectedProfilingHooks.markComponentRenderStarted(fiber);
              }
            }
          }
          function markComponentRenderStopped() {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentRenderStopped === "function") {
                injectedProfilingHooks.markComponentRenderStopped();
              }
            }
          }
          function markComponentPassiveEffectMountStarted(fiber) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentPassiveEffectMountStarted === "function") {
                injectedProfilingHooks.markComponentPassiveEffectMountStarted(fiber);
              }
            }
          }
          function markComponentPassiveEffectMountStopped() {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentPassiveEffectMountStopped === "function") {
                injectedProfilingHooks.markComponentPassiveEffectMountStopped();
              }
            }
          }
          function markComponentPassiveEffectUnmountStarted(fiber) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentPassiveEffectUnmountStarted === "function") {
                injectedProfilingHooks.markComponentPassiveEffectUnmountStarted(fiber);
              }
            }
          }
          function markComponentPassiveEffectUnmountStopped() {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentPassiveEffectUnmountStopped === "function") {
                injectedProfilingHooks.markComponentPassiveEffectUnmountStopped();
              }
            }
          }
          function markComponentLayoutEffectMountStarted(fiber) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentLayoutEffectMountStarted === "function") {
                injectedProfilingHooks.markComponentLayoutEffectMountStarted(fiber);
              }
            }
          }
          function markComponentLayoutEffectMountStopped() {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentLayoutEffectMountStopped === "function") {
                injectedProfilingHooks.markComponentLayoutEffectMountStopped();
              }
            }
          }
          function markComponentLayoutEffectUnmountStarted(fiber) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentLayoutEffectUnmountStarted === "function") {
                injectedProfilingHooks.markComponentLayoutEffectUnmountStarted(fiber);
              }
            }
          }
          function markComponentLayoutEffectUnmountStopped() {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentLayoutEffectUnmountStopped === "function") {
                injectedProfilingHooks.markComponentLayoutEffectUnmountStopped();
              }
            }
          }
          function markComponentErrored(fiber, thrownValue, lanes) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentErrored === "function") {
                injectedProfilingHooks.markComponentErrored(fiber, thrownValue, lanes);
              }
            }
          }
          function markComponentSuspended(fiber, wakeable, lanes) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentSuspended === "function") {
                injectedProfilingHooks.markComponentSuspended(fiber, wakeable, lanes);
              }
            }
          }
          function markLayoutEffectsStarted(lanes) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markLayoutEffectsStarted === "function") {
                injectedProfilingHooks.markLayoutEffectsStarted(lanes);
              }
            }
          }
          function markLayoutEffectsStopped() {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markLayoutEffectsStopped === "function") {
                injectedProfilingHooks.markLayoutEffectsStopped();
              }
            }
          }
          function markPassiveEffectsStarted(lanes) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markPassiveEffectsStarted === "function") {
                injectedProfilingHooks.markPassiveEffectsStarted(lanes);
              }
            }
          }
          function markPassiveEffectsStopped() {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markPassiveEffectsStopped === "function") {
                injectedProfilingHooks.markPassiveEffectsStopped();
              }
            }
          }
          function markRenderStarted(lanes) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markRenderStarted === "function") {
                injectedProfilingHooks.markRenderStarted(lanes);
              }
            }
          }
          function markRenderYielded() {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markRenderYielded === "function") {
                injectedProfilingHooks.markRenderYielded();
              }
            }
          }
          function markRenderStopped() {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markRenderStopped === "function") {
                injectedProfilingHooks.markRenderStopped();
              }
            }
          }
          function markRenderScheduled(lane) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markRenderScheduled === "function") {
                injectedProfilingHooks.markRenderScheduled(lane);
              }
            }
          }
          function markForceUpdateScheduled(fiber, lane) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markForceUpdateScheduled === "function") {
                injectedProfilingHooks.markForceUpdateScheduled(fiber, lane);
              }
            }
          }
          function markStateUpdateScheduled(fiber, lane) {
            {
              if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markStateUpdateScheduled === "function") {
                injectedProfilingHooks.markStateUpdateScheduled(fiber, lane);
              }
            }
          }
          var NoMode = (
            /*                         */
            0
          );
          var ConcurrentMode = (
            /*                 */
            1
          );
          var ProfileMode = (
            /*                    */
            2
          );
          var StrictLegacyMode = (
            /*               */
            8
          );
          var StrictEffectsMode = (
            /*              */
            16
          );
          var clz32 = Math.clz32 ? Math.clz32 : clz32Fallback;
          var log = Math.log;
          var LN2 = Math.LN2;
          function clz32Fallback(x) {
            var asUint = x >>> 0;
            if (asUint === 0) {
              return 32;
            }
            return 31 - (log(asUint) / LN2 | 0) | 0;
          }
          var TotalLanes = 31;
          var NoLanes = (
            /*                        */
            0
          );
          var NoLane = (
            /*                          */
            0
          );
          var SyncLane = (
            /*                        */
            1
          );
          var InputContinuousHydrationLane = (
            /*    */
            2
          );
          var InputContinuousLane = (
            /*             */
            4
          );
          var DefaultHydrationLane = (
            /*            */
            8
          );
          var DefaultLane = (
            /*                     */
            16
          );
          var TransitionHydrationLane = (
            /*                */
            32
          );
          var TransitionLanes = (
            /*                       */
            4194240
          );
          var TransitionLane1 = (
            /*                        */
            64
          );
          var TransitionLane2 = (
            /*                        */
            128
          );
          var TransitionLane3 = (
            /*                        */
            256
          );
          var TransitionLane4 = (
            /*                        */
            512
          );
          var TransitionLane5 = (
            /*                        */
            1024
          );
          var TransitionLane6 = (
            /*                        */
            2048
          );
          var TransitionLane7 = (
            /*                        */
            4096
          );
          var TransitionLane8 = (
            /*                        */
            8192
          );
          var TransitionLane9 = (
            /*                        */
            16384
          );
          var TransitionLane10 = (
            /*                       */
            32768
          );
          var TransitionLane11 = (
            /*                       */
            65536
          );
          var TransitionLane12 = (
            /*                       */
            131072
          );
          var TransitionLane13 = (
            /*                       */
            262144
          );
          var TransitionLane14 = (
            /*                       */
            524288
          );
          var TransitionLane15 = (
            /*                       */
            1048576
          );
          var TransitionLane16 = (
            /*                       */
            2097152
          );
          var RetryLanes = (
            /*                            */
            130023424
          );
          var RetryLane1 = (
            /*                             */
            4194304
          );
          var RetryLane2 = (
            /*                             */
            8388608
          );
          var RetryLane3 = (
            /*                             */
            16777216
          );
          var RetryLane4 = (
            /*                             */
            33554432
          );
          var RetryLane5 = (
            /*                             */
            67108864
          );
          var SomeRetryLane = RetryLane1;
          var SelectiveHydrationLane = (
            /*          */
            134217728
          );
          var NonIdleLanes = (
            /*                          */
            268435455
          );
          var IdleHydrationLane = (
            /*               */
            268435456
          );
          var IdleLane = (
            /*                        */
            536870912
          );
          var OffscreenLane = (
            /*                   */
            1073741824
          );
          function getLabelForLane(lane) {
            {
              if (lane & SyncLane) {
                return "Sync";
              }
              if (lane & InputContinuousHydrationLane) {
                return "InputContinuousHydration";
              }
              if (lane & InputContinuousLane) {
                return "InputContinuous";
              }
              if (lane & DefaultHydrationLane) {
                return "DefaultHydration";
              }
              if (lane & DefaultLane) {
                return "Default";
              }
              if (lane & TransitionHydrationLane) {
                return "TransitionHydration";
              }
              if (lane & TransitionLanes) {
                return "Transition";
              }
              if (lane & RetryLanes) {
                return "Retry";
              }
              if (lane & SelectiveHydrationLane) {
                return "SelectiveHydration";
              }
              if (lane & IdleHydrationLane) {
                return "IdleHydration";
              }
              if (lane & IdleLane) {
                return "Idle";
              }
              if (lane & OffscreenLane) {
                return "Offscreen";
              }
            }
          }
          var NoTimestamp = -1;
          var nextTransitionLane = TransitionLane1;
          var nextRetryLane = RetryLane1;
          function getHighestPriorityLanes(lanes) {
            switch (getHighestPriorityLane(lanes)) {
              case SyncLane:
                return SyncLane;
              case InputContinuousHydrationLane:
                return InputContinuousHydrationLane;
              case InputContinuousLane:
                return InputContinuousLane;
              case DefaultHydrationLane:
                return DefaultHydrationLane;
              case DefaultLane:
                return DefaultLane;
              case TransitionHydrationLane:
                return TransitionHydrationLane;
              case TransitionLane1:
              case TransitionLane2:
              case TransitionLane3:
              case TransitionLane4:
              case TransitionLane5:
              case TransitionLane6:
              case TransitionLane7:
              case TransitionLane8:
              case TransitionLane9:
              case TransitionLane10:
              case TransitionLane11:
              case TransitionLane12:
              case TransitionLane13:
              case TransitionLane14:
              case TransitionLane15:
              case TransitionLane16:
                return lanes & TransitionLanes;
              case RetryLane1:
              case RetryLane2:
              case RetryLane3:
              case RetryLane4:
              case RetryLane5:
                return lanes & RetryLanes;
              case SelectiveHydrationLane:
                return SelectiveHydrationLane;
              case IdleHydrationLane:
                return IdleHydrationLane;
              case IdleLane:
                return IdleLane;
              case OffscreenLane:
                return OffscreenLane;
              default:
                {
                  error("Should have found matching lanes. This is a bug in React.");
                }
                return lanes;
            }
          }
          function getNextLanes(root3, wipLanes) {
            var pendingLanes = root3.pendingLanes;
            if (pendingLanes === NoLanes) {
              return NoLanes;
            }
            var nextLanes = NoLanes;
            var suspendedLanes = root3.suspendedLanes;
            var pingedLanes = root3.pingedLanes;
            var nonIdlePendingLanes = pendingLanes & NonIdleLanes;
            if (nonIdlePendingLanes !== NoLanes) {
              var nonIdleUnblockedLanes = nonIdlePendingLanes & ~suspendedLanes;
              if (nonIdleUnblockedLanes !== NoLanes) {
                nextLanes = getHighestPriorityLanes(nonIdleUnblockedLanes);
              } else {
                var nonIdlePingedLanes = nonIdlePendingLanes & pingedLanes;
                if (nonIdlePingedLanes !== NoLanes) {
                  nextLanes = getHighestPriorityLanes(nonIdlePingedLanes);
                }
              }
            } else {
              var unblockedLanes = pendingLanes & ~suspendedLanes;
              if (unblockedLanes !== NoLanes) {
                nextLanes = getHighestPriorityLanes(unblockedLanes);
              } else {
                if (pingedLanes !== NoLanes) {
                  nextLanes = getHighestPriorityLanes(pingedLanes);
                }
              }
            }
            if (nextLanes === NoLanes) {
              return NoLanes;
            }
            if (wipLanes !== NoLanes && wipLanes !== nextLanes && // If we already suspended with a delay, then interrupting is fine. Don't
            // bother waiting until the root is complete.
            (wipLanes & suspendedLanes) === NoLanes) {
              var nextLane = getHighestPriorityLane(nextLanes);
              var wipLane = getHighestPriorityLane(wipLanes);
              if (
                // Tests whether the next lane is equal or lower priority than the wip
                // one. This works because the bits decrease in priority as you go left.
                nextLane >= wipLane || // Default priority updates should not interrupt transition updates. The
                // only difference between default updates and transition updates is that
                // default updates do not support refresh transitions.
                nextLane === DefaultLane && (wipLane & TransitionLanes) !== NoLanes
              ) {
                return wipLanes;
              }
            }
            if ((nextLanes & InputContinuousLane) !== NoLanes) {
              nextLanes |= pendingLanes & DefaultLane;
            }
            var entangledLanes = root3.entangledLanes;
            if (entangledLanes !== NoLanes) {
              var entanglements = root3.entanglements;
              var lanes = nextLanes & entangledLanes;
              while (lanes > 0) {
                var index2 = pickArbitraryLaneIndex(lanes);
                var lane = 1 << index2;
                nextLanes |= entanglements[index2];
                lanes &= ~lane;
              }
            }
            return nextLanes;
          }
          function getMostRecentEventTime(root3, lanes) {
            var eventTimes = root3.eventTimes;
            var mostRecentEventTime = NoTimestamp;
            while (lanes > 0) {
              var index2 = pickArbitraryLaneIndex(lanes);
              var lane = 1 << index2;
              var eventTime = eventTimes[index2];
              if (eventTime > mostRecentEventTime) {
                mostRecentEventTime = eventTime;
              }
              lanes &= ~lane;
            }
            return mostRecentEventTime;
          }
          function computeExpirationTime(lane, currentTime) {
            switch (lane) {
              case SyncLane:
              case InputContinuousHydrationLane:
              case InputContinuousLane:
                return currentTime + 250;
              case DefaultHydrationLane:
              case DefaultLane:
              case TransitionHydrationLane:
              case TransitionLane1:
              case TransitionLane2:
              case TransitionLane3:
              case TransitionLane4:
              case TransitionLane5:
              case TransitionLane6:
              case TransitionLane7:
              case TransitionLane8:
              case TransitionLane9:
              case TransitionLane10:
              case TransitionLane11:
              case TransitionLane12:
              case TransitionLane13:
              case TransitionLane14:
              case TransitionLane15:
              case TransitionLane16:
                return currentTime + 5e3;
              case RetryLane1:
              case RetryLane2:
              case RetryLane3:
              case RetryLane4:
              case RetryLane5:
                return NoTimestamp;
              case SelectiveHydrationLane:
              case IdleHydrationLane:
              case IdleLane:
              case OffscreenLane:
                return NoTimestamp;
              default:
                {
                  error("Should have found matching lanes. This is a bug in React.");
                }
                return NoTimestamp;
            }
          }
          function markStarvedLanesAsExpired(root3, currentTime) {
            var pendingLanes = root3.pendingLanes;
            var suspendedLanes = root3.suspendedLanes;
            var pingedLanes = root3.pingedLanes;
            var expirationTimes = root3.expirationTimes;
            var lanes = pendingLanes;
            while (lanes > 0) {
              var index2 = pickArbitraryLaneIndex(lanes);
              var lane = 1 << index2;
              var expirationTime = expirationTimes[index2];
              if (expirationTime === NoTimestamp) {
                if ((lane & suspendedLanes) === NoLanes || (lane & pingedLanes) !== NoLanes) {
                  expirationTimes[index2] = computeExpirationTime(lane, currentTime);
                }
              } else if (expirationTime <= currentTime) {
                root3.expiredLanes |= lane;
              }
              lanes &= ~lane;
            }
          }
          function getHighestPriorityPendingLanes(root3) {
            return getHighestPriorityLanes(root3.pendingLanes);
          }
          function getLanesToRetrySynchronouslyOnError(root3) {
            var everythingButOffscreen = root3.pendingLanes & ~OffscreenLane;
            if (everythingButOffscreen !== NoLanes) {
              return everythingButOffscreen;
            }
            if (everythingButOffscreen & OffscreenLane) {
              return OffscreenLane;
            }
            return NoLanes;
          }
          function includesSyncLane(lanes) {
            return (lanes & SyncLane) !== NoLanes;
          }
          function includesNonIdleWork(lanes) {
            return (lanes & NonIdleLanes) !== NoLanes;
          }
          function includesOnlyRetries(lanes) {
            return (lanes & RetryLanes) === lanes;
          }
          function includesOnlyNonUrgentLanes(lanes) {
            var UrgentLanes = SyncLane | InputContinuousLane | DefaultLane;
            return (lanes & UrgentLanes) === NoLanes;
          }
          function includesOnlyTransitions(lanes) {
            return (lanes & TransitionLanes) === lanes;
          }
          function includesBlockingLane(root3, lanes) {
            var SyncDefaultLanes = InputContinuousHydrationLane | InputContinuousLane | DefaultHydrationLane | DefaultLane;
            return (lanes & SyncDefaultLanes) !== NoLanes;
          }
          function includesExpiredLane(root3, lanes) {
            return (lanes & root3.expiredLanes) !== NoLanes;
          }
          function isTransitionLane(lane) {
            return (lane & TransitionLanes) !== NoLanes;
          }
          function claimNextTransitionLane() {
            var lane = nextTransitionLane;
            nextTransitionLane <<= 1;
            if ((nextTransitionLane & TransitionLanes) === NoLanes) {
              nextTransitionLane = TransitionLane1;
            }
            return lane;
          }
          function claimNextRetryLane() {
            var lane = nextRetryLane;
            nextRetryLane <<= 1;
            if ((nextRetryLane & RetryLanes) === NoLanes) {
              nextRetryLane = RetryLane1;
            }
            return lane;
          }
          function getHighestPriorityLane(lanes) {
            return lanes & -lanes;
          }
          function pickArbitraryLane(lanes) {
            return getHighestPriorityLane(lanes);
          }
          function pickArbitraryLaneIndex(lanes) {
            return 31 - clz32(lanes);
          }
          function laneToIndex(lane) {
            return pickArbitraryLaneIndex(lane);
          }
          function includesSomeLane(a, b) {
            return (a & b) !== NoLanes;
          }
          function isSubsetOfLanes(set2, subset) {
            return (set2 & subset) === subset;
          }
          function mergeLanes(a, b) {
            return a | b;
          }
          function removeLanes(set2, subset) {
            return set2 & ~subset;
          }
          function intersectLanes(a, b) {
            return a & b;
          }
          function laneToLanes(lane) {
            return lane;
          }
          function higherPriorityLane(a, b) {
            return a !== NoLane && a < b ? a : b;
          }
          function createLaneMap(initial) {
            var laneMap = [];
            for (var i = 0; i < TotalLanes; i++) {
              laneMap.push(initial);
            }
            return laneMap;
          }
          function markRootUpdated(root3, updateLane, eventTime) {
            root3.pendingLanes |= updateLane;
            if (updateLane !== IdleLane) {
              root3.suspendedLanes = NoLanes;
              root3.pingedLanes = NoLanes;
            }
            var eventTimes = root3.eventTimes;
            var index2 = laneToIndex(updateLane);
            eventTimes[index2] = eventTime;
          }
          function markRootSuspended(root3, suspendedLanes) {
            root3.suspendedLanes |= suspendedLanes;
            root3.pingedLanes &= ~suspendedLanes;
            var expirationTimes = root3.expirationTimes;
            var lanes = suspendedLanes;
            while (lanes > 0) {
              var index2 = pickArbitraryLaneIndex(lanes);
              var lane = 1 << index2;
              expirationTimes[index2] = NoTimestamp;
              lanes &= ~lane;
            }
          }
          function markRootPinged(root3, pingedLanes, eventTime) {
            root3.pingedLanes |= root3.suspendedLanes & pingedLanes;
          }
          function markRootFinished(root3, remainingLanes) {
            var noLongerPendingLanes = root3.pendingLanes & ~remainingLanes;
            root3.pendingLanes = remainingLanes;
            root3.suspendedLanes = NoLanes;
            root3.pingedLanes = NoLanes;
            root3.expiredLanes &= remainingLanes;
            root3.mutableReadLanes &= remainingLanes;
            root3.entangledLanes &= remainingLanes;
            var entanglements = root3.entanglements;
            var eventTimes = root3.eventTimes;
            var expirationTimes = root3.expirationTimes;
            var lanes = noLongerPendingLanes;
            while (lanes > 0) {
              var index2 = pickArbitraryLaneIndex(lanes);
              var lane = 1 << index2;
              entanglements[index2] = NoLanes;
              eventTimes[index2] = NoTimestamp;
              expirationTimes[index2] = NoTimestamp;
              lanes &= ~lane;
            }
          }
          function markRootEntangled(root3, entangledLanes) {
            var rootEntangledLanes = root3.entangledLanes |= entangledLanes;
            var entanglements = root3.entanglements;
            var lanes = rootEntangledLanes;
            while (lanes) {
              var index2 = pickArbitraryLaneIndex(lanes);
              var lane = 1 << index2;
              if (
                // Is this one of the newly entangled lanes?
                lane & entangledLanes | // Is this lane transitively entangled with the newly entangled lanes?
                entanglements[index2] & entangledLanes
              ) {
                entanglements[index2] |= entangledLanes;
              }
              lanes &= ~lane;
            }
          }
          function getBumpedLaneForHydration(root3, renderLanes2) {
            var renderLane = getHighestPriorityLane(renderLanes2);
            var lane;
            switch (renderLane) {
              case InputContinuousLane:
                lane = InputContinuousHydrationLane;
                break;
              case DefaultLane:
                lane = DefaultHydrationLane;
                break;
              case TransitionLane1:
              case TransitionLane2:
              case TransitionLane3:
              case TransitionLane4:
              case TransitionLane5:
              case TransitionLane6:
              case TransitionLane7:
              case TransitionLane8:
              case TransitionLane9:
              case TransitionLane10:
              case TransitionLane11:
              case TransitionLane12:
              case TransitionLane13:
              case TransitionLane14:
              case TransitionLane15:
              case TransitionLane16:
              case RetryLane1:
              case RetryLane2:
              case RetryLane3:
              case RetryLane4:
              case RetryLane5:
                lane = TransitionHydrationLane;
                break;
              case IdleLane:
                lane = IdleHydrationLane;
                break;
              default:
                lane = NoLane;
                break;
            }
            if ((lane & (root3.suspendedLanes | renderLanes2)) !== NoLane) {
              return NoLane;
            }
            return lane;
          }
          function addFiberToLanesMap(root3, fiber, lanes) {
            if (!isDevToolsPresent) {
              return;
            }
            var pendingUpdatersLaneMap = root3.pendingUpdatersLaneMap;
            while (lanes > 0) {
              var index2 = laneToIndex(lanes);
              var lane = 1 << index2;
              var updaters = pendingUpdatersLaneMap[index2];
              updaters.add(fiber);
              lanes &= ~lane;
            }
          }
          function movePendingFibersToMemoized(root3, lanes) {
            if (!isDevToolsPresent) {
              return;
            }
            var pendingUpdatersLaneMap = root3.pendingUpdatersLaneMap;
            var memoizedUpdaters = root3.memoizedUpdaters;
            while (lanes > 0) {
              var index2 = laneToIndex(lanes);
              var lane = 1 << index2;
              var updaters = pendingUpdatersLaneMap[index2];
              if (updaters.size > 0) {
                updaters.forEach(function(fiber) {
                  var alternate = fiber.alternate;
                  if (alternate === null || !memoizedUpdaters.has(alternate)) {
                    memoizedUpdaters.add(fiber);
                  }
                });
                updaters.clear();
              }
              lanes &= ~lane;
            }
          }
          function getTransitionsForLanes(root3, lanes) {
            {
              return null;
            }
          }
          var DiscreteEventPriority = SyncLane;
          var ContinuousEventPriority = InputContinuousLane;
          var DefaultEventPriority = DefaultLane;
          var IdleEventPriority = IdleLane;
          var currentUpdatePriority = NoLane;
          function getCurrentUpdatePriority() {
            return currentUpdatePriority;
          }
          function setCurrentUpdatePriority(newPriority) {
            currentUpdatePriority = newPriority;
          }
          function runWithPriority(priority, fn) {
            var previousPriority = currentUpdatePriority;
            try {
              currentUpdatePriority = priority;
              return fn();
            } finally {
              currentUpdatePriority = previousPriority;
            }
          }
          function higherEventPriority(a, b) {
            return a !== 0 && a < b ? a : b;
          }
          function lowerEventPriority(a, b) {
            return a === 0 || a > b ? a : b;
          }
          function isHigherEventPriority(a, b) {
            return a !== 0 && a < b;
          }
          function lanesToEventPriority(lanes) {
            var lane = getHighestPriorityLane(lanes);
            if (!isHigherEventPriority(DiscreteEventPriority, lane)) {
              return DiscreteEventPriority;
            }
            if (!isHigherEventPriority(ContinuousEventPriority, lane)) {
              return ContinuousEventPriority;
            }
            if (includesNonIdleWork(lane)) {
              return DefaultEventPriority;
            }
            return IdleEventPriority;
          }
          function isRootDehydrated(root3) {
            var currentState = root3.current.memoizedState;
            return currentState.isDehydrated;
          }
          var _attemptSynchronousHydration;
          function setAttemptSynchronousHydration(fn) {
            _attemptSynchronousHydration = fn;
          }
          function attemptSynchronousHydration(fiber) {
            _attemptSynchronousHydration(fiber);
          }
          var attemptContinuousHydration;
          function setAttemptContinuousHydration(fn) {
            attemptContinuousHydration = fn;
          }
          var attemptHydrationAtCurrentPriority;
          function setAttemptHydrationAtCurrentPriority(fn) {
            attemptHydrationAtCurrentPriority = fn;
          }
          var getCurrentUpdatePriority$1;
          function setGetCurrentUpdatePriority(fn) {
            getCurrentUpdatePriority$1 = fn;
          }
          var attemptHydrationAtPriority;
          function setAttemptHydrationAtPriority(fn) {
            attemptHydrationAtPriority = fn;
          }
          var hasScheduledReplayAttempt = false;
          var queuedDiscreteEvents = [];
          var queuedFocus = null;
          var queuedDrag = null;
          var queuedMouse = null;
          var queuedPointers = /* @__PURE__ */ new Map();
          var queuedPointerCaptures = /* @__PURE__ */ new Map();
          var queuedExplicitHydrationTargets = [];
          var discreteReplayableEvents = [
            "mousedown",
            "mouseup",
            "touchcancel",
            "touchend",
            "touchstart",
            "auxclick",
            "dblclick",
            "pointercancel",
            "pointerdown",
            "pointerup",
            "dragend",
            "dragstart",
            "drop",
            "compositionend",
            "compositionstart",
            "keydown",
            "keypress",
            "keyup",
            "input",
            "textInput",
            // Intentionally camelCase
            "copy",
            "cut",
            "paste",
            "click",
            "change",
            "contextmenu",
            "reset",
            "submit"
          ];
          function isDiscreteEventThatRequiresHydration(eventType) {
            return discreteReplayableEvents.indexOf(eventType) > -1;
          }
          function createQueuedReplayableEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
            return {
              blockedOn,
              domEventName,
              eventSystemFlags,
              nativeEvent,
              targetContainers: [targetContainer]
            };
          }
          function clearIfContinuousEvent(domEventName, nativeEvent) {
            switch (domEventName) {
              case "focusin":
              case "focusout":
                queuedFocus = null;
                break;
              case "dragenter":
              case "dragleave":
                queuedDrag = null;
                break;
              case "mouseover":
              case "mouseout":
                queuedMouse = null;
                break;
              case "pointerover":
              case "pointerout": {
                var pointerId = nativeEvent.pointerId;
                queuedPointers.delete(pointerId);
                break;
              }
              case "gotpointercapture":
              case "lostpointercapture": {
                var _pointerId = nativeEvent.pointerId;
                queuedPointerCaptures.delete(_pointerId);
                break;
              }
            }
          }
          function accumulateOrCreateContinuousQueuedReplayableEvent(existingQueuedEvent, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
            if (existingQueuedEvent === null || existingQueuedEvent.nativeEvent !== nativeEvent) {
              var queuedEvent = createQueuedReplayableEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent);
              if (blockedOn !== null) {
                var _fiber2 = getInstanceFromNode(blockedOn);
                if (_fiber2 !== null) {
                  attemptContinuousHydration(_fiber2);
                }
              }
              return queuedEvent;
            }
            existingQueuedEvent.eventSystemFlags |= eventSystemFlags;
            var targetContainers = existingQueuedEvent.targetContainers;
            if (targetContainer !== null && targetContainers.indexOf(targetContainer) === -1) {
              targetContainers.push(targetContainer);
            }
            return existingQueuedEvent;
          }
          function queueIfContinuousEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
            switch (domEventName) {
              case "focusin": {
                var focusEvent = nativeEvent;
                queuedFocus = accumulateOrCreateContinuousQueuedReplayableEvent(queuedFocus, blockedOn, domEventName, eventSystemFlags, targetContainer, focusEvent);
                return true;
              }
              case "dragenter": {
                var dragEvent = nativeEvent;
                queuedDrag = accumulateOrCreateContinuousQueuedReplayableEvent(queuedDrag, blockedOn, domEventName, eventSystemFlags, targetContainer, dragEvent);
                return true;
              }
              case "mouseover": {
                var mouseEvent = nativeEvent;
                queuedMouse = accumulateOrCreateContinuousQueuedReplayableEvent(queuedMouse, blockedOn, domEventName, eventSystemFlags, targetContainer, mouseEvent);
                return true;
              }
              case "pointerover": {
                var pointerEvent = nativeEvent;
                var pointerId = pointerEvent.pointerId;
                queuedPointers.set(pointerId, accumulateOrCreateContinuousQueuedReplayableEvent(queuedPointers.get(pointerId) || null, blockedOn, domEventName, eventSystemFlags, targetContainer, pointerEvent));
                return true;
              }
              case "gotpointercapture": {
                var _pointerEvent = nativeEvent;
                var _pointerId2 = _pointerEvent.pointerId;
                queuedPointerCaptures.set(_pointerId2, accumulateOrCreateContinuousQueuedReplayableEvent(queuedPointerCaptures.get(_pointerId2) || null, blockedOn, domEventName, eventSystemFlags, targetContainer, _pointerEvent));
                return true;
              }
            }
            return false;
          }
          function attemptExplicitHydrationTarget(queuedTarget) {
            var targetInst = getClosestInstanceFromNode(queuedTarget.target);
            if (targetInst !== null) {
              var nearestMounted = getNearestMountedFiber(targetInst);
              if (nearestMounted !== null) {
                var tag = nearestMounted.tag;
                if (tag === SuspenseComponent) {
                  var instance = getSuspenseInstanceFromFiber(nearestMounted);
                  if (instance !== null) {
                    queuedTarget.blockedOn = instance;
                    attemptHydrationAtPriority(queuedTarget.priority, function() {
                      attemptHydrationAtCurrentPriority(nearestMounted);
                    });
                    return;
                  }
                } else if (tag === HostRoot) {
                  var root3 = nearestMounted.stateNode;
                  if (isRootDehydrated(root3)) {
                    queuedTarget.blockedOn = getContainerFromFiber(nearestMounted);
                    return;
                  }
                }
              }
            }
            queuedTarget.blockedOn = null;
          }
          function queueExplicitHydrationTarget(target) {
            var updatePriority = getCurrentUpdatePriority$1();
            var queuedTarget = {
              blockedOn: null,
              target,
              priority: updatePriority
            };
            var i = 0;
            for (; i < queuedExplicitHydrationTargets.length; i++) {
              if (!isHigherEventPriority(updatePriority, queuedExplicitHydrationTargets[i].priority)) {
                break;
              }
            }
            queuedExplicitHydrationTargets.splice(i, 0, queuedTarget);
            if (i === 0) {
              attemptExplicitHydrationTarget(queuedTarget);
            }
          }
          function attemptReplayContinuousQueuedEvent(queuedEvent) {
            if (queuedEvent.blockedOn !== null) {
              return false;
            }
            var targetContainers = queuedEvent.targetContainers;
            while (targetContainers.length > 0) {
              var targetContainer = targetContainers[0];
              var nextBlockedOn = findInstanceBlockingEvent(queuedEvent.domEventName, queuedEvent.eventSystemFlags, targetContainer, queuedEvent.nativeEvent);
              if (nextBlockedOn === null) {
                {
                  var nativeEvent = queuedEvent.nativeEvent;
                  var nativeEventClone = new nativeEvent.constructor(nativeEvent.type, nativeEvent);
                  setReplayingEvent(nativeEventClone);
                  nativeEvent.target.dispatchEvent(nativeEventClone);
                  resetReplayingEvent();
                }
              } else {
                var _fiber3 = getInstanceFromNode(nextBlockedOn);
                if (_fiber3 !== null) {
                  attemptContinuousHydration(_fiber3);
                }
                queuedEvent.blockedOn = nextBlockedOn;
                return false;
              }
              targetContainers.shift();
            }
            return true;
          }
          function attemptReplayContinuousQueuedEventInMap(queuedEvent, key, map) {
            if (attemptReplayContinuousQueuedEvent(queuedEvent)) {
              map.delete(key);
            }
          }
          function replayUnblockedEvents() {
            hasScheduledReplayAttempt = false;
            if (queuedFocus !== null && attemptReplayContinuousQueuedEvent(queuedFocus)) {
              queuedFocus = null;
            }
            if (queuedDrag !== null && attemptReplayContinuousQueuedEvent(queuedDrag)) {
              queuedDrag = null;
            }
            if (queuedMouse !== null && attemptReplayContinuousQueuedEvent(queuedMouse)) {
              queuedMouse = null;
            }
            queuedPointers.forEach(attemptReplayContinuousQueuedEventInMap);
            queuedPointerCaptures.forEach(attemptReplayContinuousQueuedEventInMap);
          }
          function scheduleCallbackIfUnblocked(queuedEvent, unblocked) {
            if (queuedEvent.blockedOn === unblocked) {
              queuedEvent.blockedOn = null;
              if (!hasScheduledReplayAttempt) {
                hasScheduledReplayAttempt = true;
                Scheduler.unstable_scheduleCallback(Scheduler.unstable_NormalPriority, replayUnblockedEvents);
              }
            }
          }
          function retryIfBlockedOn(unblocked) {
            if (queuedDiscreteEvents.length > 0) {
              scheduleCallbackIfUnblocked(queuedDiscreteEvents[0], unblocked);
              for (var i = 1; i < queuedDiscreteEvents.length; i++) {
                var queuedEvent = queuedDiscreteEvents[i];
                if (queuedEvent.blockedOn === unblocked) {
                  queuedEvent.blockedOn = null;
                }
              }
            }
            if (queuedFocus !== null) {
              scheduleCallbackIfUnblocked(queuedFocus, unblocked);
            }
            if (queuedDrag !== null) {
              scheduleCallbackIfUnblocked(queuedDrag, unblocked);
            }
            if (queuedMouse !== null) {
              scheduleCallbackIfUnblocked(queuedMouse, unblocked);
            }
            var unblock = function(queuedEvent2) {
              return scheduleCallbackIfUnblocked(queuedEvent2, unblocked);
            };
            queuedPointers.forEach(unblock);
            queuedPointerCaptures.forEach(unblock);
            for (var _i = 0; _i < queuedExplicitHydrationTargets.length; _i++) {
              var queuedTarget = queuedExplicitHydrationTargets[_i];
              if (queuedTarget.blockedOn === unblocked) {
                queuedTarget.blockedOn = null;
              }
            }
            while (queuedExplicitHydrationTargets.length > 0) {
              var nextExplicitTarget = queuedExplicitHydrationTargets[0];
              if (nextExplicitTarget.blockedOn !== null) {
                break;
              } else {
                attemptExplicitHydrationTarget(nextExplicitTarget);
                if (nextExplicitTarget.blockedOn === null) {
                  queuedExplicitHydrationTargets.shift();
                }
              }
            }
          }
          var ReactCurrentBatchConfig = ReactSharedInternals.ReactCurrentBatchConfig;
          var _enabled = true;
          function setEnabled(enabled) {
            _enabled = !!enabled;
          }
          function isEnabled() {
            return _enabled;
          }
          function createEventListenerWrapperWithPriority(targetContainer, domEventName, eventSystemFlags) {
            var eventPriority = getEventPriority(domEventName);
            var listenerWrapper;
            switch (eventPriority) {
              case DiscreteEventPriority:
                listenerWrapper = dispatchDiscreteEvent;
                break;
              case ContinuousEventPriority:
                listenerWrapper = dispatchContinuousEvent;
                break;
              case DefaultEventPriority:
              default:
                listenerWrapper = dispatchEvent;
                break;
            }
            return listenerWrapper.bind(null, domEventName, eventSystemFlags, targetContainer);
          }
          function dispatchDiscreteEvent(domEventName, eventSystemFlags, container, nativeEvent) {
            var previousPriority = getCurrentUpdatePriority();
            var prevTransition = ReactCurrentBatchConfig.transition;
            ReactCurrentBatchConfig.transition = null;
            try {
              setCurrentUpdatePriority(DiscreteEventPriority);
              dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
            } finally {
              setCurrentUpdatePriority(previousPriority);
              ReactCurrentBatchConfig.transition = prevTransition;
            }
          }
          function dispatchContinuousEvent(domEventName, eventSystemFlags, container, nativeEvent) {
            var previousPriority = getCurrentUpdatePriority();
            var prevTransition = ReactCurrentBatchConfig.transition;
            ReactCurrentBatchConfig.transition = null;
            try {
              setCurrentUpdatePriority(ContinuousEventPriority);
              dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
            } finally {
              setCurrentUpdatePriority(previousPriority);
              ReactCurrentBatchConfig.transition = prevTransition;
            }
          }
          function dispatchEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
            if (!_enabled) {
              return;
            }
            {
              dispatchEventWithEnableCapturePhaseSelectiveHydrationWithoutDiscreteEventReplay(domEventName, eventSystemFlags, targetContainer, nativeEvent);
            }
          }
          function dispatchEventWithEnableCapturePhaseSelectiveHydrationWithoutDiscreteEventReplay(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
            var blockedOn = findInstanceBlockingEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent);
            if (blockedOn === null) {
              dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, return_targetInst, targetContainer);
              clearIfContinuousEvent(domEventName, nativeEvent);
              return;
            }
            if (queueIfContinuousEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent)) {
              nativeEvent.stopPropagation();
              return;
            }
            clearIfContinuousEvent(domEventName, nativeEvent);
            if (eventSystemFlags & IS_CAPTURE_PHASE && isDiscreteEventThatRequiresHydration(domEventName)) {
              while (blockedOn !== null) {
                var fiber = getInstanceFromNode(blockedOn);
                if (fiber !== null) {
                  attemptSynchronousHydration(fiber);
                }
                var nextBlockedOn = findInstanceBlockingEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent);
                if (nextBlockedOn === null) {
                  dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, return_targetInst, targetContainer);
                }
                if (nextBlockedOn === blockedOn) {
                  break;
                }
                blockedOn = nextBlockedOn;
              }
              if (blockedOn !== null) {
                nativeEvent.stopPropagation();
              }
              return;
            }
            dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, null, targetContainer);
          }
          var return_targetInst = null;
          function findInstanceBlockingEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
            return_targetInst = null;
            var nativeEventTarget = getEventTarget(nativeEvent);
            var targetInst = getClosestInstanceFromNode(nativeEventTarget);
            if (targetInst !== null) {
              var nearestMounted = getNearestMountedFiber(targetInst);
              if (nearestMounted === null) {
                targetInst = null;
              } else {
                var tag = nearestMounted.tag;
                if (tag === SuspenseComponent) {
                  var instance = getSuspenseInstanceFromFiber(nearestMounted);
                  if (instance !== null) {
                    return instance;
                  }
                  targetInst = null;
                } else if (tag === HostRoot) {
                  var root3 = nearestMounted.stateNode;
                  if (isRootDehydrated(root3)) {
                    return getContainerFromFiber(nearestMounted);
                  }
                  targetInst = null;
                } else if (nearestMounted !== targetInst) {
                  targetInst = null;
                }
              }
            }
            return_targetInst = targetInst;
            return null;
          }
          function getEventPriority(domEventName) {
            switch (domEventName) {
              // Used by SimpleEventPlugin:
              case "cancel":
              case "click":
              case "close":
              case "contextmenu":
              case "copy":
              case "cut":
              case "auxclick":
              case "dblclick":
              case "dragend":
              case "dragstart":
              case "drop":
              case "focusin":
              case "focusout":
              case "input":
              case "invalid":
              case "keydown":
              case "keypress":
              case "keyup":
              case "mousedown":
              case "mouseup":
              case "paste":
              case "pause":
              case "play":
              case "pointercancel":
              case "pointerdown":
              case "pointerup":
              case "ratechange":
              case "reset":
              case "resize":
              case "seeked":
              case "submit":
              case "touchcancel":
              case "touchend":
              case "touchstart":
              case "volumechange":
              // Used by polyfills:
              // eslint-disable-next-line no-fallthrough
              case "change":
              case "selectionchange":
              case "textInput":
              case "compositionstart":
              case "compositionend":
              case "compositionupdate":
              // Only enableCreateEventHandleAPI:
              // eslint-disable-next-line no-fallthrough
              case "beforeblur":
              case "afterblur":
              // Not used by React but could be by user code:
              // eslint-disable-next-line no-fallthrough
              case "beforeinput":
              case "blur":
              case "fullscreenchange":
              case "focus":
              case "hashchange":
              case "popstate":
              case "select":
              case "selectstart":
                return DiscreteEventPriority;
              case "drag":
              case "dragenter":
              case "dragexit":
              case "dragleave":
              case "dragover":
              case "mousemove":
              case "mouseout":
              case "mouseover":
              case "pointermove":
              case "pointerout":
              case "pointerover":
              case "scroll":
              case "toggle":
              case "touchmove":
              case "wheel":
              // Not used by React but could be by user code:
              // eslint-disable-next-line no-fallthrough
              case "mouseenter":
              case "mouseleave":
              case "pointerenter":
              case "pointerleave":
                return ContinuousEventPriority;
              case "message": {
                var schedulerPriority = getCurrentPriorityLevel();
                switch (schedulerPriority) {
                  case ImmediatePriority:
                    return DiscreteEventPriority;
                  case UserBlockingPriority:
                    return ContinuousEventPriority;
                  case NormalPriority:
                  case LowPriority:
                    return DefaultEventPriority;
                  case IdlePriority:
                    return IdleEventPriority;
                  default:
                    return DefaultEventPriority;
                }
              }
              default:
                return DefaultEventPriority;
            }
          }
          function addEventBubbleListener(target, eventType, listener) {
            target.addEventListener(eventType, listener, false);
            return listener;
          }
          function addEventCaptureListener(target, eventType, listener) {
            target.addEventListener(eventType, listener, true);
            return listener;
          }
          function addEventCaptureListenerWithPassiveFlag(target, eventType, listener, passive) {
            target.addEventListener(eventType, listener, {
              capture: true,
              passive
            });
            return listener;
          }
          function addEventBubbleListenerWithPassiveFlag(target, eventType, listener, passive) {
            target.addEventListener(eventType, listener, {
              passive
            });
            return listener;
          }
          var root2 = null;
          var startText = null;
          var fallbackText = null;
          function initialize(nativeEventTarget) {
            root2 = nativeEventTarget;
            startText = getText();
            return true;
          }
          function reset() {
            root2 = null;
            startText = null;
            fallbackText = null;
          }
          function getData() {
            if (fallbackText) {
              return fallbackText;
            }
            var start;
            var startValue = startText;
            var startLength = startValue.length;
            var end;
            var endValue = getText();
            var endLength = endValue.length;
            for (start = 0; start < startLength; start++) {
              if (startValue[start] !== endValue[start]) {
                break;
              }
            }
            var minEnd = startLength - start;
            for (end = 1; end <= minEnd; end++) {
              if (startValue[startLength - end] !== endValue[endLength - end]) {
                break;
              }
            }
            var sliceTail = end > 1 ? 1 - end : void 0;
            fallbackText = endValue.slice(start, sliceTail);
            return fallbackText;
          }
          function getText() {
            if ("value" in root2) {
              return root2.value;
            }
            return root2.textContent;
          }
          function getEventCharCode(nativeEvent) {
            var charCode;
            var keyCode = nativeEvent.keyCode;
            if ("charCode" in nativeEvent) {
              charCode = nativeEvent.charCode;
              if (charCode === 0 && keyCode === 13) {
                charCode = 13;
              }
            } else {
              charCode = keyCode;
            }
            if (charCode === 10) {
              charCode = 13;
            }
            if (charCode >= 32 || charCode === 13) {
              return charCode;
            }
            return 0;
          }
          function functionThatReturnsTrue() {
            return true;
          }
          function functionThatReturnsFalse() {
            return false;
          }
          function createSyntheticEvent(Interface) {
            function SyntheticBaseEvent(reactName, reactEventType, targetInst, nativeEvent, nativeEventTarget) {
              this._reactName = reactName;
              this._targetInst = targetInst;
              this.type = reactEventType;
              this.nativeEvent = nativeEvent;
              this.target = nativeEventTarget;
              this.currentTarget = null;
              for (var _propName in Interface) {
                if (!Interface.hasOwnProperty(_propName)) {
                  continue;
                }
                var normalize = Interface[_propName];
                if (normalize) {
                  this[_propName] = normalize(nativeEvent);
                } else {
                  this[_propName] = nativeEvent[_propName];
                }
              }
              var defaultPrevented = nativeEvent.defaultPrevented != null ? nativeEvent.defaultPrevented : nativeEvent.returnValue === false;
              if (defaultPrevented) {
                this.isDefaultPrevented = functionThatReturnsTrue;
              } else {
                this.isDefaultPrevented = functionThatReturnsFalse;
              }
              this.isPropagationStopped = functionThatReturnsFalse;
              return this;
            }
            assign(SyntheticBaseEvent.prototype, {
              preventDefault: function() {
                this.defaultPrevented = true;
                var event = this.nativeEvent;
                if (!event) {
                  return;
                }
                if (event.preventDefault) {
                  event.preventDefault();
                } else if (typeof event.returnValue !== "unknown") {
                  event.returnValue = false;
                }
                this.isDefaultPrevented = functionThatReturnsTrue;
              },
              stopPropagation: function() {
                var event = this.nativeEvent;
                if (!event) {
                  return;
                }
                if (event.stopPropagation) {
                  event.stopPropagation();
                } else if (typeof event.cancelBubble !== "unknown") {
                  event.cancelBubble = true;
                }
                this.isPropagationStopped = functionThatReturnsTrue;
              },
              /**
               * We release all dispatched `SyntheticEvent`s after each event loop, adding
               * them back into the pool. This allows a way to hold onto a reference that
               * won't be added back into the pool.
               */
              persist: function() {
              },
              /**
               * Checks if this event should be released back into the pool.
               *
               * @return {boolean} True if this should not be released, false otherwise.
               */
              isPersistent: functionThatReturnsTrue
            });
            return SyntheticBaseEvent;
          }
          var EventInterface = {
            eventPhase: 0,
            bubbles: 0,
            cancelable: 0,
            timeStamp: function(event) {
              return event.timeStamp || Date.now();
            },
            defaultPrevented: 0,
            isTrusted: 0
          };
          var SyntheticEvent = createSyntheticEvent(EventInterface);
          var UIEventInterface = assign({}, EventInterface, {
            view: 0,
            detail: 0
          });
          var SyntheticUIEvent = createSyntheticEvent(UIEventInterface);
          var lastMovementX;
          var lastMovementY;
          var lastMouseEvent;
          function updateMouseMovementPolyfillState(event) {
            if (event !== lastMouseEvent) {
              if (lastMouseEvent && event.type === "mousemove") {
                lastMovementX = event.screenX - lastMouseEvent.screenX;
                lastMovementY = event.screenY - lastMouseEvent.screenY;
              } else {
                lastMovementX = 0;
                lastMovementY = 0;
              }
              lastMouseEvent = event;
            }
          }
          var MouseEventInterface = assign({}, UIEventInterface, {
            screenX: 0,
            screenY: 0,
            clientX: 0,
            clientY: 0,
            pageX: 0,
            pageY: 0,
            ctrlKey: 0,
            shiftKey: 0,
            altKey: 0,
            metaKey: 0,
            getModifierState: getEventModifierState,
            button: 0,
            buttons: 0,
            relatedTarget: function(event) {
              if (event.relatedTarget === void 0) return event.fromElement === event.srcElement ? event.toElement : event.fromElement;
              return event.relatedTarget;
            },
            movementX: function(event) {
              if ("movementX" in event) {
                return event.movementX;
              }
              updateMouseMovementPolyfillState(event);
              return lastMovementX;
            },
            movementY: function(event) {
              if ("movementY" in event) {
                return event.movementY;
              }
              return lastMovementY;
            }
          });
          var SyntheticMouseEvent = createSyntheticEvent(MouseEventInterface);
          var DragEventInterface = assign({}, MouseEventInterface, {
            dataTransfer: 0
          });
          var SyntheticDragEvent = createSyntheticEvent(DragEventInterface);
          var FocusEventInterface = assign({}, UIEventInterface, {
            relatedTarget: 0
          });
          var SyntheticFocusEvent = createSyntheticEvent(FocusEventInterface);
          var AnimationEventInterface = assign({}, EventInterface, {
            animationName: 0,
            elapsedTime: 0,
            pseudoElement: 0
          });
          var SyntheticAnimationEvent = createSyntheticEvent(AnimationEventInterface);
          var ClipboardEventInterface = assign({}, EventInterface, {
            clipboardData: function(event) {
              return "clipboardData" in event ? event.clipboardData : window.clipboardData;
            }
          });
          var SyntheticClipboardEvent = createSyntheticEvent(ClipboardEventInterface);
          var CompositionEventInterface = assign({}, EventInterface, {
            data: 0
          });
          var SyntheticCompositionEvent = createSyntheticEvent(CompositionEventInterface);
          var SyntheticInputEvent = SyntheticCompositionEvent;
          var normalizeKey = {
            Esc: "Escape",
            Spacebar: " ",
            Left: "ArrowLeft",
            Up: "ArrowUp",
            Right: "ArrowRight",
            Down: "ArrowDown",
            Del: "Delete",
            Win: "OS",
            Menu: "ContextMenu",
            Apps: "ContextMenu",
            Scroll: "ScrollLock",
            MozPrintableKey: "Unidentified"
          };
          var translateToKey = {
            "8": "Backspace",
            "9": "Tab",
            "12": "Clear",
            "13": "Enter",
            "16": "Shift",
            "17": "Control",
            "18": "Alt",
            "19": "Pause",
            "20": "CapsLock",
            "27": "Escape",
            "32": " ",
            "33": "PageUp",
            "34": "PageDown",
            "35": "End",
            "36": "Home",
            "37": "ArrowLeft",
            "38": "ArrowUp",
            "39": "ArrowRight",
            "40": "ArrowDown",
            "45": "Insert",
            "46": "Delete",
            "112": "F1",
            "113": "F2",
            "114": "F3",
            "115": "F4",
            "116": "F5",
            "117": "F6",
            "118": "F7",
            "119": "F8",
            "120": "F9",
            "121": "F10",
            "122": "F11",
            "123": "F12",
            "144": "NumLock",
            "145": "ScrollLock",
            "224": "Meta"
          };
          function getEventKey(nativeEvent) {
            if (nativeEvent.key) {
              var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
              if (key !== "Unidentified") {
                return key;
              }
            }
            if (nativeEvent.type === "keypress") {
              var charCode = getEventCharCode(nativeEvent);
              return charCode === 13 ? "Enter" : String.fromCharCode(charCode);
            }
            if (nativeEvent.type === "keydown" || nativeEvent.type === "keyup") {
              return translateToKey[nativeEvent.keyCode] || "Unidentified";
            }
            return "";
          }
          var modifierKeyToProp = {
            Alt: "altKey",
            Control: "ctrlKey",
            Meta: "metaKey",
            Shift: "shiftKey"
          };
          function modifierStateGetter(keyArg) {
            var syntheticEvent = this;
            var nativeEvent = syntheticEvent.nativeEvent;
            if (nativeEvent.getModifierState) {
              return nativeEvent.getModifierState(keyArg);
            }
            var keyProp = modifierKeyToProp[keyArg];
            return keyProp ? !!nativeEvent[keyProp] : false;
          }
          function getEventModifierState(nativeEvent) {
            return modifierStateGetter;
          }
          var KeyboardEventInterface = assign({}, UIEventInterface, {
            key: getEventKey,
            code: 0,
            location: 0,
            ctrlKey: 0,
            shiftKey: 0,
            altKey: 0,
            metaKey: 0,
            repeat: 0,
            locale: 0,
            getModifierState: getEventModifierState,
            // Legacy Interface
            charCode: function(event) {
              if (event.type === "keypress") {
                return getEventCharCode(event);
              }
              return 0;
            },
            keyCode: function(event) {
              if (event.type === "keydown" || event.type === "keyup") {
                return event.keyCode;
              }
              return 0;
            },
            which: function(event) {
              if (event.type === "keypress") {
                return getEventCharCode(event);
              }
              if (event.type === "keydown" || event.type === "keyup") {
                return event.keyCode;
              }
              return 0;
            }
          });
          var SyntheticKeyboardEvent = createSyntheticEvent(KeyboardEventInterface);
          var PointerEventInterface = assign({}, MouseEventInterface, {
            pointerId: 0,
            width: 0,
            height: 0,
            pressure: 0,
            tangentialPressure: 0,
            tiltX: 0,
            tiltY: 0,
            twist: 0,
            pointerType: 0,
            isPrimary: 0
          });
          var SyntheticPointerEvent = createSyntheticEvent(PointerEventInterface);
          var TouchEventInterface = assign({}, UIEventInterface, {
            touches: 0,
            targetTouches: 0,
            changedTouches: 0,
            altKey: 0,
            metaKey: 0,
            ctrlKey: 0,
            shiftKey: 0,
            getModifierState: getEventModifierState
          });
          var SyntheticTouchEvent = createSyntheticEvent(TouchEventInterface);
          var TransitionEventInterface = assign({}, EventInterface, {
            propertyName: 0,
            elapsedTime: 0,
            pseudoElement: 0
          });
          var SyntheticTransitionEvent = createSyntheticEvent(TransitionEventInterface);
          var WheelEventInterface = assign({}, MouseEventInterface, {
            deltaX: function(event) {
              return "deltaX" in event ? event.deltaX : (
                // Fallback to `wheelDeltaX` for Webkit and normalize (right is positive).
                "wheelDeltaX" in event ? -event.wheelDeltaX : 0
              );
            },
            deltaY: function(event) {
              return "deltaY" in event ? event.deltaY : (
                // Fallback to `wheelDeltaY` for Webkit and normalize (down is positive).
                "wheelDeltaY" in event ? -event.wheelDeltaY : (
                  // Fallback to `wheelDelta` for IE<9 and normalize (down is positive).
                  "wheelDelta" in event ? -event.wheelDelta : 0
                )
              );
            },
            deltaZ: 0,
            // Browsers without "deltaMode" is reporting in raw wheel delta where one
            // notch on the scroll is always +/- 120, roughly equivalent to pixels.
            // A good approximation of DOM_DELTA_LINE (1) is 5% of viewport size or
            // ~40 pixels, for DOM_DELTA_SCREEN (2) it is 87.5% of viewport size.
            deltaMode: 0
          });
          var SyntheticWheelEvent = createSyntheticEvent(WheelEventInterface);
          var END_KEYCODES = [9, 13, 27, 32];
          var START_KEYCODE = 229;
          var canUseCompositionEvent = canUseDOM && "CompositionEvent" in window;
          var documentMode = null;
          if (canUseDOM && "documentMode" in document) {
            documentMode = document.documentMode;
          }
          var canUseTextInputEvent = canUseDOM && "TextEvent" in window && !documentMode;
          var useFallbackCompositionData = canUseDOM && (!canUseCompositionEvent || documentMode && documentMode > 8 && documentMode <= 11);
          var SPACEBAR_CODE = 32;
          var SPACEBAR_CHAR = String.fromCharCode(SPACEBAR_CODE);
          function registerEvents() {
            registerTwoPhaseEvent("onBeforeInput", ["compositionend", "keypress", "textInput", "paste"]);
            registerTwoPhaseEvent("onCompositionEnd", ["compositionend", "focusout", "keydown", "keypress", "keyup", "mousedown"]);
            registerTwoPhaseEvent("onCompositionStart", ["compositionstart", "focusout", "keydown", "keypress", "keyup", "mousedown"]);
            registerTwoPhaseEvent("onCompositionUpdate", ["compositionupdate", "focusout", "keydown", "keypress", "keyup", "mousedown"]);
          }
          var hasSpaceKeypress = false;
          function isKeypressCommand(nativeEvent) {
            return (nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) && // ctrlKey && altKey is equivalent to AltGr, and is not a command.
            !(nativeEvent.ctrlKey && nativeEvent.altKey);
          }
          function getCompositionEventType(domEventName) {
            switch (domEventName) {
              case "compositionstart":
                return "onCompositionStart";
              case "compositionend":
                return "onCompositionEnd";
              case "compositionupdate":
                return "onCompositionUpdate";
            }
          }
          function isFallbackCompositionStart(domEventName, nativeEvent) {
            return domEventName === "keydown" && nativeEvent.keyCode === START_KEYCODE;
          }
          function isFallbackCompositionEnd(domEventName, nativeEvent) {
            switch (domEventName) {
              case "keyup":
                return END_KEYCODES.indexOf(nativeEvent.keyCode) !== -1;
              case "keydown":
                return nativeEvent.keyCode !== START_KEYCODE;
              case "keypress":
              case "mousedown":
              case "focusout":
                return true;
              default:
                return false;
            }
          }
          function getDataFromCustomEvent(nativeEvent) {
            var detail = nativeEvent.detail;
            if (typeof detail === "object" && "data" in detail) {
              return detail.data;
            }
            return null;
          }
          function isUsingKoreanIME(nativeEvent) {
            return nativeEvent.locale === "ko";
          }
          var isComposing = false;
          function extractCompositionEvent(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget) {
            var eventType;
            var fallbackData;
            if (canUseCompositionEvent) {
              eventType = getCompositionEventType(domEventName);
            } else if (!isComposing) {
              if (isFallbackCompositionStart(domEventName, nativeEvent)) {
                eventType = "onCompositionStart";
              }
            } else if (isFallbackCompositionEnd(domEventName, nativeEvent)) {
              eventType = "onCompositionEnd";
            }
            if (!eventType) {
              return null;
            }
            if (useFallbackCompositionData && !isUsingKoreanIME(nativeEvent)) {
              if (!isComposing && eventType === "onCompositionStart") {
                isComposing = initialize(nativeEventTarget);
              } else if (eventType === "onCompositionEnd") {
                if (isComposing) {
                  fallbackData = getData();
                }
              }
            }
            var listeners = accumulateTwoPhaseListeners(targetInst, eventType);
            if (listeners.length > 0) {
              var event = new SyntheticCompositionEvent(eventType, domEventName, null, nativeEvent, nativeEventTarget);
              dispatchQueue.push({
                event,
                listeners
              });
              if (fallbackData) {
                event.data = fallbackData;
              } else {
                var customData = getDataFromCustomEvent(nativeEvent);
                if (customData !== null) {
                  event.data = customData;
                }
              }
            }
          }
          function getNativeBeforeInputChars(domEventName, nativeEvent) {
            switch (domEventName) {
              case "compositionend":
                return getDataFromCustomEvent(nativeEvent);
              case "keypress":
                var which = nativeEvent.which;
                if (which !== SPACEBAR_CODE) {
                  return null;
                }
                hasSpaceKeypress = true;
                return SPACEBAR_CHAR;
              case "textInput":
                var chars = nativeEvent.data;
                if (chars === SPACEBAR_CHAR && hasSpaceKeypress) {
                  return null;
                }
                return chars;
              default:
                return null;
            }
          }
          function getFallbackBeforeInputChars(domEventName, nativeEvent) {
            if (isComposing) {
              if (domEventName === "compositionend" || !canUseCompositionEvent && isFallbackCompositionEnd(domEventName, nativeEvent)) {
                var chars = getData();
                reset();
                isComposing = false;
                return chars;
              }
              return null;
            }
            switch (domEventName) {
              case "paste":
                return null;
              case "keypress":
                if (!isKeypressCommand(nativeEvent)) {
                  if (nativeEvent.char && nativeEvent.char.length > 1) {
                    return nativeEvent.char;
                  } else if (nativeEvent.which) {
                    return String.fromCharCode(nativeEvent.which);
                  }
                }
                return null;
              case "compositionend":
                return useFallbackCompositionData && !isUsingKoreanIME(nativeEvent) ? null : nativeEvent.data;
              default:
                return null;
            }
          }
          function extractBeforeInputEvent(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget) {
            var chars;
            if (canUseTextInputEvent) {
              chars = getNativeBeforeInputChars(domEventName, nativeEvent);
            } else {
              chars = getFallbackBeforeInputChars(domEventName, nativeEvent);
            }
            if (!chars) {
              return null;
            }
            var listeners = accumulateTwoPhaseListeners(targetInst, "onBeforeInput");
            if (listeners.length > 0) {
              var event = new SyntheticInputEvent("onBeforeInput", "beforeinput", null, nativeEvent, nativeEventTarget);
              dispatchQueue.push({
                event,
                listeners
              });
              event.data = chars;
            }
          }
          function extractEvents(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
            extractCompositionEvent(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
            extractBeforeInputEvent(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
          }
          var supportedInputTypes = {
            color: true,
            date: true,
            datetime: true,
            "datetime-local": true,
            email: true,
            month: true,
            number: true,
            password: true,
            range: true,
            search: true,
            tel: true,
            text: true,
            time: true,
            url: true,
            week: true
          };
          function isTextInputElement(elem) {
            var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
            if (nodeName === "input") {
              return !!supportedInputTypes[elem.type];
            }
            if (nodeName === "textarea") {
              return true;
            }
            return false;
          }
          function isEventSupported(eventNameSuffix) {
            if (!canUseDOM) {
              return false;
            }
            var eventName = "on" + eventNameSuffix;
            var isSupported = eventName in document;
            if (!isSupported) {
              var element = document.createElement("div");
              element.setAttribute(eventName, "return;");
              isSupported = typeof element[eventName] === "function";
            }
            return isSupported;
          }
          function registerEvents$1() {
            registerTwoPhaseEvent("onChange", ["change", "click", "focusin", "focusout", "input", "keydown", "keyup", "selectionchange"]);
          }
          function createAndAccumulateChangeEvent(dispatchQueue, inst, nativeEvent, target) {
            enqueueStateRestore(target);
            var listeners = accumulateTwoPhaseListeners(inst, "onChange");
            if (listeners.length > 0) {
              var event = new SyntheticEvent("onChange", "change", null, nativeEvent, target);
              dispatchQueue.push({
                event,
                listeners
              });
            }
          }
          var activeElement = null;
          var activeElementInst = null;
          function shouldUseChangeEvent(elem) {
            var nodeName = elem.nodeName && elem.nodeName.toLowerCase();
            return nodeName === "select" || nodeName === "input" && elem.type === "file";
          }
          function manualDispatchChangeEvent(nativeEvent) {
            var dispatchQueue = [];
            createAndAccumulateChangeEvent(dispatchQueue, activeElementInst, nativeEvent, getEventTarget(nativeEvent));
            batchedUpdates(runEventInBatch, dispatchQueue);
          }
          function runEventInBatch(dispatchQueue) {
            processDispatchQueue(dispatchQueue, 0);
          }
          function getInstIfValueChanged(targetInst) {
            var targetNode = getNodeFromInstance(targetInst);
            if (updateValueIfChanged(targetNode)) {
              return targetInst;
            }
          }
          function getTargetInstForChangeEvent(domEventName, targetInst) {
            if (domEventName === "change") {
              return targetInst;
            }
          }
          var isInputEventSupported = false;
          if (canUseDOM) {
            isInputEventSupported = isEventSupported("input") && (!document.documentMode || document.documentMode > 9);
          }
          function startWatchingForValueChange(target, targetInst) {
            activeElement = target;
            activeElementInst = targetInst;
            activeElement.attachEvent("onpropertychange", handlePropertyChange);
          }
          function stopWatchingForValueChange() {
            if (!activeElement) {
              return;
            }
            activeElement.detachEvent("onpropertychange", handlePropertyChange);
            activeElement = null;
            activeElementInst = null;
          }
          function handlePropertyChange(nativeEvent) {
            if (nativeEvent.propertyName !== "value") {
              return;
            }
            if (getInstIfValueChanged(activeElementInst)) {
              manualDispatchChangeEvent(nativeEvent);
            }
          }
          function handleEventsForInputEventPolyfill(domEventName, target, targetInst) {
            if (domEventName === "focusin") {
              stopWatchingForValueChange();
              startWatchingForValueChange(target, targetInst);
            } else if (domEventName === "focusout") {
              stopWatchingForValueChange();
            }
          }
          function getTargetInstForInputEventPolyfill(domEventName, targetInst) {
            if (domEventName === "selectionchange" || domEventName === "keyup" || domEventName === "keydown") {
              return getInstIfValueChanged(activeElementInst);
            }
          }
          function shouldUseClickEvent(elem) {
            var nodeName = elem.nodeName;
            return nodeName && nodeName.toLowerCase() === "input" && (elem.type === "checkbox" || elem.type === "radio");
          }
          function getTargetInstForClickEvent(domEventName, targetInst) {
            if (domEventName === "click") {
              return getInstIfValueChanged(targetInst);
            }
          }
          function getTargetInstForInputOrChangeEvent(domEventName, targetInst) {
            if (domEventName === "input" || domEventName === "change") {
              return getInstIfValueChanged(targetInst);
            }
          }
          function handleControlledInputBlur(node) {
            var state = node._wrapperState;
            if (!state || !state.controlled || node.type !== "number") {
              return;
            }
            {
              setDefaultValue(node, "number", node.value);
            }
          }
          function extractEvents$1(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
            var targetNode = targetInst ? getNodeFromInstance(targetInst) : window;
            var getTargetInstFunc, handleEventFunc;
            if (shouldUseChangeEvent(targetNode)) {
              getTargetInstFunc = getTargetInstForChangeEvent;
            } else if (isTextInputElement(targetNode)) {
              if (isInputEventSupported) {
                getTargetInstFunc = getTargetInstForInputOrChangeEvent;
              } else {
                getTargetInstFunc = getTargetInstForInputEventPolyfill;
                handleEventFunc = handleEventsForInputEventPolyfill;
              }
            } else if (shouldUseClickEvent(targetNode)) {
              getTargetInstFunc = getTargetInstForClickEvent;
            }
            if (getTargetInstFunc) {
              var inst = getTargetInstFunc(domEventName, targetInst);
              if (inst) {
                createAndAccumulateChangeEvent(dispatchQueue, inst, nativeEvent, nativeEventTarget);
                return;
              }
            }
            if (handleEventFunc) {
              handleEventFunc(domEventName, targetNode, targetInst);
            }
            if (domEventName === "focusout") {
              handleControlledInputBlur(targetNode);
            }
          }
          function registerEvents$2() {
            registerDirectEvent("onMouseEnter", ["mouseout", "mouseover"]);
            registerDirectEvent("onMouseLeave", ["mouseout", "mouseover"]);
            registerDirectEvent("onPointerEnter", ["pointerout", "pointerover"]);
            registerDirectEvent("onPointerLeave", ["pointerout", "pointerover"]);
          }
          function extractEvents$2(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
            var isOverEvent = domEventName === "mouseover" || domEventName === "pointerover";
            var isOutEvent = domEventName === "mouseout" || domEventName === "pointerout";
            if (isOverEvent && !isReplayingEvent(nativeEvent)) {
              var related = nativeEvent.relatedTarget || nativeEvent.fromElement;
              if (related) {
                if (getClosestInstanceFromNode(related) || isContainerMarkedAsRoot(related)) {
                  return;
                }
              }
            }
            if (!isOutEvent && !isOverEvent) {
              return;
            }
            var win;
            if (nativeEventTarget.window === nativeEventTarget) {
              win = nativeEventTarget;
            } else {
              var doc = nativeEventTarget.ownerDocument;
              if (doc) {
                win = doc.defaultView || doc.parentWindow;
              } else {
                win = window;
              }
            }
            var from;
            var to;
            if (isOutEvent) {
              var _related = nativeEvent.relatedTarget || nativeEvent.toElement;
              from = targetInst;
              to = _related ? getClosestInstanceFromNode(_related) : null;
              if (to !== null) {
                var nearestMounted = getNearestMountedFiber(to);
                if (to !== nearestMounted || to.tag !== HostComponent && to.tag !== HostText) {
                  to = null;
                }
              }
            } else {
              from = null;
              to = targetInst;
            }
            if (from === to) {
              return;
            }
            var SyntheticEventCtor = SyntheticMouseEvent;
            var leaveEventType = "onMouseLeave";
            var enterEventType = "onMouseEnter";
            var eventTypePrefix = "mouse";
            if (domEventName === "pointerout" || domEventName === "pointerover") {
              SyntheticEventCtor = SyntheticPointerEvent;
              leaveEventType = "onPointerLeave";
              enterEventType = "onPointerEnter";
              eventTypePrefix = "pointer";
            }
            var fromNode = from == null ? win : getNodeFromInstance(from);
            var toNode = to == null ? win : getNodeFromInstance(to);
            var leave = new SyntheticEventCtor(leaveEventType, eventTypePrefix + "leave", from, nativeEvent, nativeEventTarget);
            leave.target = fromNode;
            leave.relatedTarget = toNode;
            var enter = null;
            var nativeTargetInst = getClosestInstanceFromNode(nativeEventTarget);
            if (nativeTargetInst === targetInst) {
              var enterEvent = new SyntheticEventCtor(enterEventType, eventTypePrefix + "enter", to, nativeEvent, nativeEventTarget);
              enterEvent.target = toNode;
              enterEvent.relatedTarget = fromNode;
              enter = enterEvent;
            }
            accumulateEnterLeaveTwoPhaseListeners(dispatchQueue, leave, enter, from, to);
          }
          function is(x, y) {
            return x === y && (x !== 0 || 1 / x === 1 / y) || x !== x && y !== y;
          }
          var objectIs = typeof Object.is === "function" ? Object.is : is;
          function shallowEqual(objA, objB) {
            if (objectIs(objA, objB)) {
              return true;
            }
            if (typeof objA !== "object" || objA === null || typeof objB !== "object" || objB === null) {
              return false;
            }
            var keysA = Object.keys(objA);
            var keysB = Object.keys(objB);
            if (keysA.length !== keysB.length) {
              return false;
            }
            for (var i = 0; i < keysA.length; i++) {
              var currentKey = keysA[i];
              if (!hasOwnProperty.call(objB, currentKey) || !objectIs(objA[currentKey], objB[currentKey])) {
                return false;
              }
            }
            return true;
          }
          function getLeafNode(node) {
            while (node && node.firstChild) {
              node = node.firstChild;
            }
            return node;
          }
          function getSiblingNode(node) {
            while (node) {
              if (node.nextSibling) {
                return node.nextSibling;
              }
              node = node.parentNode;
            }
          }
          function getNodeForCharacterOffset(root3, offset) {
            var node = getLeafNode(root3);
            var nodeStart = 0;
            var nodeEnd = 0;
            while (node) {
              if (node.nodeType === TEXT_NODE) {
                nodeEnd = nodeStart + node.textContent.length;
                if (nodeStart <= offset && nodeEnd >= offset) {
                  return {
                    node,
                    offset: offset - nodeStart
                  };
                }
                nodeStart = nodeEnd;
              }
              node = getLeafNode(getSiblingNode(node));
            }
          }
          function getOffsets(outerNode) {
            var ownerDocument = outerNode.ownerDocument;
            var win = ownerDocument && ownerDocument.defaultView || window;
            var selection = win.getSelection && win.getSelection();
            if (!selection || selection.rangeCount === 0) {
              return null;
            }
            var anchorNode = selection.anchorNode, anchorOffset = selection.anchorOffset, focusNode = selection.focusNode, focusOffset = selection.focusOffset;
            try {
              anchorNode.nodeType;
              focusNode.nodeType;
            } catch (e) {
              return null;
            }
            return getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode, focusOffset);
          }
          function getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode, focusOffset) {
            var length = 0;
            var start = -1;
            var end = -1;
            var indexWithinAnchor = 0;
            var indexWithinFocus = 0;
            var node = outerNode;
            var parentNode = null;
            outer: while (true) {
              var next = null;
              while (true) {
                if (node === anchorNode && (anchorOffset === 0 || node.nodeType === TEXT_NODE)) {
                  start = length + anchorOffset;
                }
                if (node === focusNode && (focusOffset === 0 || node.nodeType === TEXT_NODE)) {
                  end = length + focusOffset;
                }
                if (node.nodeType === TEXT_NODE) {
                  length += node.nodeValue.length;
                }
                if ((next = node.firstChild) === null) {
                  break;
                }
                parentNode = node;
                node = next;
              }
              while (true) {
                if (node === outerNode) {
                  break outer;
                }
                if (parentNode === anchorNode && ++indexWithinAnchor === anchorOffset) {
                  start = length;
                }
                if (parentNode === focusNode && ++indexWithinFocus === focusOffset) {
                  end = length;
                }
                if ((next = node.nextSibling) !== null) {
                  break;
                }
                node = parentNode;
                parentNode = node.parentNode;
              }
              node = next;
            }
            if (start === -1 || end === -1) {
              return null;
            }
            return {
              start,
              end
            };
          }
          function setOffsets(node, offsets) {
            var doc = node.ownerDocument || document;
            var win = doc && doc.defaultView || window;
            if (!win.getSelection) {
              return;
            }
            var selection = win.getSelection();
            var length = node.textContent.length;
            var start = Math.min(offsets.start, length);
            var end = offsets.end === void 0 ? start : Math.min(offsets.end, length);
            if (!selection.extend && start > end) {
              var temp = end;
              end = start;
              start = temp;
            }
            var startMarker = getNodeForCharacterOffset(node, start);
            var endMarker = getNodeForCharacterOffset(node, end);
            if (startMarker && endMarker) {
              if (selection.rangeCount === 1 && selection.anchorNode === startMarker.node && selection.anchorOffset === startMarker.offset && selection.focusNode === endMarker.node && selection.focusOffset === endMarker.offset) {
                return;
              }
              var range = doc.createRange();
              range.setStart(startMarker.node, startMarker.offset);
              selection.removeAllRanges();
              if (start > end) {
                selection.addRange(range);
                selection.extend(endMarker.node, endMarker.offset);
              } else {
                range.setEnd(endMarker.node, endMarker.offset);
                selection.addRange(range);
              }
            }
          }
          function isTextNode(node) {
            return node && node.nodeType === TEXT_NODE;
          }
          function containsNode(outerNode, innerNode) {
            if (!outerNode || !innerNode) {
              return false;
            } else if (outerNode === innerNode) {
              return true;
            } else if (isTextNode(outerNode)) {
              return false;
            } else if (isTextNode(innerNode)) {
              return containsNode(outerNode, innerNode.parentNode);
            } else if ("contains" in outerNode) {
              return outerNode.contains(innerNode);
            } else if (outerNode.compareDocumentPosition) {
              return !!(outerNode.compareDocumentPosition(innerNode) & 16);
            } else {
              return false;
            }
          }
          function isInDocument(node) {
            return node && node.ownerDocument && containsNode(node.ownerDocument.documentElement, node);
          }
          function isSameOriginFrame(iframe) {
            try {
              return typeof iframe.contentWindow.location.href === "string";
            } catch (err) {
              return false;
            }
          }
          function getActiveElementDeep() {
            var win = window;
            var element = getActiveElement();
            while (element instanceof win.HTMLIFrameElement) {
              if (isSameOriginFrame(element)) {
                win = element.contentWindow;
              } else {
                return element;
              }
              element = getActiveElement(win.document);
            }
            return element;
          }
          function hasSelectionCapabilities(elem) {
            var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
            return nodeName && (nodeName === "input" && (elem.type === "text" || elem.type === "search" || elem.type === "tel" || elem.type === "url" || elem.type === "password") || nodeName === "textarea" || elem.contentEditable === "true");
          }
          function getSelectionInformation() {
            var focusedElem = getActiveElementDeep();
            return {
              focusedElem,
              selectionRange: hasSelectionCapabilities(focusedElem) ? getSelection(focusedElem) : null
            };
          }
          function restoreSelection(priorSelectionInformation) {
            var curFocusedElem = getActiveElementDeep();
            var priorFocusedElem = priorSelectionInformation.focusedElem;
            var priorSelectionRange = priorSelectionInformation.selectionRange;
            if (curFocusedElem !== priorFocusedElem && isInDocument(priorFocusedElem)) {
              if (priorSelectionRange !== null && hasSelectionCapabilities(priorFocusedElem)) {
                setSelection(priorFocusedElem, priorSelectionRange);
              }
              var ancestors = [];
              var ancestor = priorFocusedElem;
              while (ancestor = ancestor.parentNode) {
                if (ancestor.nodeType === ELEMENT_NODE) {
                  ancestors.push({
                    element: ancestor,
                    left: ancestor.scrollLeft,
                    top: ancestor.scrollTop
                  });
                }
              }
              if (typeof priorFocusedElem.focus === "function") {
                priorFocusedElem.focus();
              }
              for (var i = 0; i < ancestors.length; i++) {
                var info = ancestors[i];
                info.element.scrollLeft = info.left;
                info.element.scrollTop = info.top;
              }
            }
          }
          function getSelection(input) {
            var selection;
            if ("selectionStart" in input) {
              selection = {
                start: input.selectionStart,
                end: input.selectionEnd
              };
            } else {
              selection = getOffsets(input);
            }
            return selection || {
              start: 0,
              end: 0
            };
          }
          function setSelection(input, offsets) {
            var start = offsets.start;
            var end = offsets.end;
            if (end === void 0) {
              end = start;
            }
            if ("selectionStart" in input) {
              input.selectionStart = start;
              input.selectionEnd = Math.min(end, input.value.length);
            } else {
              setOffsets(input, offsets);
            }
          }
          var skipSelectionChangeEvent = canUseDOM && "documentMode" in document && document.documentMode <= 11;
          function registerEvents$3() {
            registerTwoPhaseEvent("onSelect", ["focusout", "contextmenu", "dragend", "focusin", "keydown", "keyup", "mousedown", "mouseup", "selectionchange"]);
          }
          var activeElement$1 = null;
          var activeElementInst$1 = null;
          var lastSelection = null;
          var mouseDown = false;
          function getSelection$1(node) {
            if ("selectionStart" in node && hasSelectionCapabilities(node)) {
              return {
                start: node.selectionStart,
                end: node.selectionEnd
              };
            } else {
              var win = node.ownerDocument && node.ownerDocument.defaultView || window;
              var selection = win.getSelection();
              return {
                anchorNode: selection.anchorNode,
                anchorOffset: selection.anchorOffset,
                focusNode: selection.focusNode,
                focusOffset: selection.focusOffset
              };
            }
          }
          function getEventTargetDocument(eventTarget) {
            return eventTarget.window === eventTarget ? eventTarget.document : eventTarget.nodeType === DOCUMENT_NODE ? eventTarget : eventTarget.ownerDocument;
          }
          function constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget) {
            var doc = getEventTargetDocument(nativeEventTarget);
            if (mouseDown || activeElement$1 == null || activeElement$1 !== getActiveElement(doc)) {
              return;
            }
            var currentSelection = getSelection$1(activeElement$1);
            if (!lastSelection || !shallowEqual(lastSelection, currentSelection)) {
              lastSelection = currentSelection;
              var listeners = accumulateTwoPhaseListeners(activeElementInst$1, "onSelect");
              if (listeners.length > 0) {
                var event = new SyntheticEvent("onSelect", "select", null, nativeEvent, nativeEventTarget);
                dispatchQueue.push({
                  event,
                  listeners
                });
                event.target = activeElement$1;
              }
            }
          }
          function extractEvents$3(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
            var targetNode = targetInst ? getNodeFromInstance(targetInst) : window;
            switch (domEventName) {
              // Track the input node that has focus.
              case "focusin":
                if (isTextInputElement(targetNode) || targetNode.contentEditable === "true") {
                  activeElement$1 = targetNode;
                  activeElementInst$1 = targetInst;
                  lastSelection = null;
                }
                break;
              case "focusout":
                activeElement$1 = null;
                activeElementInst$1 = null;
                lastSelection = null;
                break;
              // Don't fire the event while the user is dragging. This matches the
              // semantics of the native select event.
              case "mousedown":
                mouseDown = true;
                break;
              case "contextmenu":
              case "mouseup":
              case "dragend":
                mouseDown = false;
                constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
                break;
              // Chrome and IE fire non-standard event when selection is changed (and
              // sometimes when it hasn't). IE's event fires out of order with respect
              // to key and input events on deletion, so we discard it.
              //
              // Firefox doesn't support selectionchange, so check selection status
              // after each key entry. The selection changes after keydown and before
              // keyup, but we check on keydown as well in the case of holding down a
              // key, when multiple keydown events are fired but only one keyup is.
              // This is also our approach for IE handling, for the reason above.
              case "selectionchange":
                if (skipSelectionChangeEvent) {
                  break;
                }
              // falls through
              case "keydown":
              case "keyup":
                constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
            }
          }
          function makePrefixMap(styleProp, eventName) {
            var prefixes2 = {};
            prefixes2[styleProp.toLowerCase()] = eventName.toLowerCase();
            prefixes2["Webkit" + styleProp] = "webkit" + eventName;
            prefixes2["Moz" + styleProp] = "moz" + eventName;
            return prefixes2;
          }
          var vendorPrefixes = {
            animationend: makePrefixMap("Animation", "AnimationEnd"),
            animationiteration: makePrefixMap("Animation", "AnimationIteration"),
            animationstart: makePrefixMap("Animation", "AnimationStart"),
            transitionend: makePrefixMap("Transition", "TransitionEnd")
          };
          var prefixedEventNames = {};
          var style = {};
          if (canUseDOM) {
            style = document.createElement("div").style;
            if (!("AnimationEvent" in window)) {
              delete vendorPrefixes.animationend.animation;
              delete vendorPrefixes.animationiteration.animation;
              delete vendorPrefixes.animationstart.animation;
            }
            if (!("TransitionEvent" in window)) {
              delete vendorPrefixes.transitionend.transition;
            }
          }
          function getVendorPrefixedEventName(eventName) {
            if (prefixedEventNames[eventName]) {
              return prefixedEventNames[eventName];
            } else if (!vendorPrefixes[eventName]) {
              return eventName;
            }
            var prefixMap = vendorPrefixes[eventName];
            for (var styleProp in prefixMap) {
              if (prefixMap.hasOwnProperty(styleProp) && styleProp in style) {
                return prefixedEventNames[eventName] = prefixMap[styleProp];
              }
            }
            return eventName;
          }
          var ANIMATION_END = getVendorPrefixedEventName("animationend");
          var ANIMATION_ITERATION = getVendorPrefixedEventName("animationiteration");
          var ANIMATION_START = getVendorPrefixedEventName("animationstart");
          var TRANSITION_END = getVendorPrefixedEventName("transitionend");
          var topLevelEventsToReactNames = /* @__PURE__ */ new Map();
          var simpleEventPluginEvents = ["abort", "auxClick", "cancel", "canPlay", "canPlayThrough", "click", "close", "contextMenu", "copy", "cut", "drag", "dragEnd", "dragEnter", "dragExit", "dragLeave", "dragOver", "dragStart", "drop", "durationChange", "emptied", "encrypted", "ended", "error", "gotPointerCapture", "input", "invalid", "keyDown", "keyPress", "keyUp", "load", "loadedData", "loadedMetadata", "loadStart", "lostPointerCapture", "mouseDown", "mouseMove", "mouseOut", "mouseOver", "mouseUp", "paste", "pause", "play", "playing", "pointerCancel", "pointerDown", "pointerMove", "pointerOut", "pointerOver", "pointerUp", "progress", "rateChange", "reset", "resize", "seeked", "seeking", "stalled", "submit", "suspend", "timeUpdate", "touchCancel", "touchEnd", "touchStart", "volumeChange", "scroll", "toggle", "touchMove", "waiting", "wheel"];
          function registerSimpleEvent(domEventName, reactName) {
            topLevelEventsToReactNames.set(domEventName, reactName);
            registerTwoPhaseEvent(reactName, [domEventName]);
          }
          function registerSimpleEvents() {
            for (var i = 0; i < simpleEventPluginEvents.length; i++) {
              var eventName = simpleEventPluginEvents[i];
              var domEventName = eventName.toLowerCase();
              var capitalizedEvent = eventName[0].toUpperCase() + eventName.slice(1);
              registerSimpleEvent(domEventName, "on" + capitalizedEvent);
            }
            registerSimpleEvent(ANIMATION_END, "onAnimationEnd");
            registerSimpleEvent(ANIMATION_ITERATION, "onAnimationIteration");
            registerSimpleEvent(ANIMATION_START, "onAnimationStart");
            registerSimpleEvent("dblclick", "onDoubleClick");
            registerSimpleEvent("focusin", "onFocus");
            registerSimpleEvent("focusout", "onBlur");
            registerSimpleEvent(TRANSITION_END, "onTransitionEnd");
          }
          function extractEvents$4(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
            var reactName = topLevelEventsToReactNames.get(domEventName);
            if (reactName === void 0) {
              return;
            }
            var SyntheticEventCtor = SyntheticEvent;
            var reactEventType = domEventName;
            switch (domEventName) {
              case "keypress":
                if (getEventCharCode(nativeEvent) === 0) {
                  return;
                }
              /* falls through */
              case "keydown":
              case "keyup":
                SyntheticEventCtor = SyntheticKeyboardEvent;
                break;
              case "focusin":
                reactEventType = "focus";
                SyntheticEventCtor = SyntheticFocusEvent;
                break;
              case "focusout":
                reactEventType = "blur";
                SyntheticEventCtor = SyntheticFocusEvent;
                break;
              case "beforeblur":
              case "afterblur":
                SyntheticEventCtor = SyntheticFocusEvent;
                break;
              case "click":
                if (nativeEvent.button === 2) {
                  return;
                }
              /* falls through */
              case "auxclick":
              case "dblclick":
              case "mousedown":
              case "mousemove":
              case "mouseup":
              // TODO: Disabled elements should not respond to mouse events
              /* falls through */
              case "mouseout":
              case "mouseover":
              case "contextmenu":
                SyntheticEventCtor = SyntheticMouseEvent;
                break;
              case "drag":
              case "dragend":
              case "dragenter":
              case "dragexit":
              case "dragleave":
              case "dragover":
              case "dragstart":
              case "drop":
                SyntheticEventCtor = SyntheticDragEvent;
                break;
              case "touchcancel":
              case "touchend":
              case "touchmove":
              case "touchstart":
                SyntheticEventCtor = SyntheticTouchEvent;
                break;
              case ANIMATION_END:
              case ANIMATION_ITERATION:
              case ANIMATION_START:
                SyntheticEventCtor = SyntheticAnimationEvent;
                break;
              case TRANSITION_END:
                SyntheticEventCtor = SyntheticTransitionEvent;
                break;
              case "scroll":
                SyntheticEventCtor = SyntheticUIEvent;
                break;
              case "wheel":
                SyntheticEventCtor = SyntheticWheelEvent;
                break;
              case "copy":
              case "cut":
              case "paste":
                SyntheticEventCtor = SyntheticClipboardEvent;
                break;
              case "gotpointercapture":
              case "lostpointercapture":
              case "pointercancel":
              case "pointerdown":
              case "pointermove":
              case "pointerout":
              case "pointerover":
              case "pointerup":
                SyntheticEventCtor = SyntheticPointerEvent;
                break;
            }
            var inCapturePhase = (eventSystemFlags & IS_CAPTURE_PHASE) !== 0;
            {
              var accumulateTargetOnly = !inCapturePhase && // TODO: ideally, we'd eventually add all events from
              // nonDelegatedEvents list in DOMPluginEventSystem.
              // Then we can remove this special list.
              // This is a breaking change that can wait until React 18.
              domEventName === "scroll";
              var _listeners = accumulateSinglePhaseListeners(targetInst, reactName, nativeEvent.type, inCapturePhase, accumulateTargetOnly);
              if (_listeners.length > 0) {
                var _event = new SyntheticEventCtor(reactName, reactEventType, null, nativeEvent, nativeEventTarget);
                dispatchQueue.push({
                  event: _event,
                  listeners: _listeners
                });
              }
            }
          }
          registerSimpleEvents();
          registerEvents$2();
          registerEvents$1();
          registerEvents$3();
          registerEvents();
          function extractEvents$5(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
            extractEvents$4(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags);
            var shouldProcessPolyfillPlugins = (eventSystemFlags & SHOULD_NOT_PROCESS_POLYFILL_EVENT_PLUGINS) === 0;
            if (shouldProcessPolyfillPlugins) {
              extractEvents$2(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
              extractEvents$1(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
              extractEvents$3(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
              extractEvents(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
            }
          }
          var mediaEventTypes = ["abort", "canplay", "canplaythrough", "durationchange", "emptied", "encrypted", "ended", "error", "loadeddata", "loadedmetadata", "loadstart", "pause", "play", "playing", "progress", "ratechange", "resize", "seeked", "seeking", "stalled", "suspend", "timeupdate", "volumechange", "waiting"];
          var nonDelegatedEvents = new Set(["cancel", "close", "invalid", "load", "scroll", "toggle"].concat(mediaEventTypes));
          function executeDispatch(event, listener, currentTarget) {
            var type = event.type || "unknown-event";
            event.currentTarget = currentTarget;
            invokeGuardedCallbackAndCatchFirstError(type, listener, void 0, event);
            event.currentTarget = null;
          }
          function processDispatchQueueItemsInOrder(event, dispatchListeners, inCapturePhase) {
            var previousInstance;
            if (inCapturePhase) {
              for (var i = dispatchListeners.length - 1; i >= 0; i--) {
                var _dispatchListeners$i = dispatchListeners[i], instance = _dispatchListeners$i.instance, currentTarget = _dispatchListeners$i.currentTarget, listener = _dispatchListeners$i.listener;
                if (instance !== previousInstance && event.isPropagationStopped()) {
                  return;
                }
                executeDispatch(event, listener, currentTarget);
                previousInstance = instance;
              }
            } else {
              for (var _i = 0; _i < dispatchListeners.length; _i++) {
                var _dispatchListeners$_i = dispatchListeners[_i], _instance = _dispatchListeners$_i.instance, _currentTarget = _dispatchListeners$_i.currentTarget, _listener = _dispatchListeners$_i.listener;
                if (_instance !== previousInstance && event.isPropagationStopped()) {
                  return;
                }
                executeDispatch(event, _listener, _currentTarget);
                previousInstance = _instance;
              }
            }
          }
          function processDispatchQueue(dispatchQueue, eventSystemFlags) {
            var inCapturePhase = (eventSystemFlags & IS_CAPTURE_PHASE) !== 0;
            for (var i = 0; i < dispatchQueue.length; i++) {
              var _dispatchQueue$i = dispatchQueue[i], event = _dispatchQueue$i.event, listeners = _dispatchQueue$i.listeners;
              processDispatchQueueItemsInOrder(event, listeners, inCapturePhase);
            }
            rethrowCaughtError();
          }
          function dispatchEventsForPlugins(domEventName, eventSystemFlags, nativeEvent, targetInst, targetContainer) {
            var nativeEventTarget = getEventTarget(nativeEvent);
            var dispatchQueue = [];
            extractEvents$5(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags);
            processDispatchQueue(dispatchQueue, eventSystemFlags);
          }
          function listenToNonDelegatedEvent(domEventName, targetElement) {
            {
              if (!nonDelegatedEvents.has(domEventName)) {
                error('Did not expect a listenToNonDelegatedEvent() call for "%s". This is a bug in React. Please file an issue.', domEventName);
              }
            }
            var isCapturePhaseListener = false;
            var listenerSet = getEventListenerSet(targetElement);
            var listenerSetKey = getListenerSetKey(domEventName, isCapturePhaseListener);
            if (!listenerSet.has(listenerSetKey)) {
              addTrappedEventListener(targetElement, domEventName, IS_NON_DELEGATED, isCapturePhaseListener);
              listenerSet.add(listenerSetKey);
            }
          }
          function listenToNativeEvent(domEventName, isCapturePhaseListener, target) {
            {
              if (nonDelegatedEvents.has(domEventName) && !isCapturePhaseListener) {
                error('Did not expect a listenToNativeEvent() call for "%s" in the bubble phase. This is a bug in React. Please file an issue.', domEventName);
              }
            }
            var eventSystemFlags = 0;
            if (isCapturePhaseListener) {
              eventSystemFlags |= IS_CAPTURE_PHASE;
            }
            addTrappedEventListener(target, domEventName, eventSystemFlags, isCapturePhaseListener);
          }
          var listeningMarker = "_reactListening" + Math.random().toString(36).slice(2);
          function listenToAllSupportedEvents(rootContainerElement) {
            if (!rootContainerElement[listeningMarker]) {
              rootContainerElement[listeningMarker] = true;
              allNativeEvents.forEach(function(domEventName) {
                if (domEventName !== "selectionchange") {
                  if (!nonDelegatedEvents.has(domEventName)) {
                    listenToNativeEvent(domEventName, false, rootContainerElement);
                  }
                  listenToNativeEvent(domEventName, true, rootContainerElement);
                }
              });
              var ownerDocument = rootContainerElement.nodeType === DOCUMENT_NODE ? rootContainerElement : rootContainerElement.ownerDocument;
              if (ownerDocument !== null) {
                if (!ownerDocument[listeningMarker]) {
                  ownerDocument[listeningMarker] = true;
                  listenToNativeEvent("selectionchange", false, ownerDocument);
                }
              }
            }
          }
          function addTrappedEventListener(targetContainer, domEventName, eventSystemFlags, isCapturePhaseListener, isDeferredListenerForLegacyFBSupport) {
            var listener = createEventListenerWrapperWithPriority(targetContainer, domEventName, eventSystemFlags);
            var isPassiveListener = void 0;
            if (passiveBrowserEventsSupported) {
              if (domEventName === "touchstart" || domEventName === "touchmove" || domEventName === "wheel") {
                isPassiveListener = true;
              }
            }
            targetContainer = targetContainer;
            var unsubscribeListener;
            if (isCapturePhaseListener) {
              if (isPassiveListener !== void 0) {
                unsubscribeListener = addEventCaptureListenerWithPassiveFlag(targetContainer, domEventName, listener, isPassiveListener);
              } else {
                unsubscribeListener = addEventCaptureListener(targetContainer, domEventName, listener);
              }
            } else {
              if (isPassiveListener !== void 0) {
                unsubscribeListener = addEventBubbleListenerWithPassiveFlag(targetContainer, domEventName, listener, isPassiveListener);
              } else {
                unsubscribeListener = addEventBubbleListener(targetContainer, domEventName, listener);
              }
            }
          }
          function isMatchingRootContainer(grandContainer, targetContainer) {
            return grandContainer === targetContainer || grandContainer.nodeType === COMMENT_NODE && grandContainer.parentNode === targetContainer;
          }
          function dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, targetInst, targetContainer) {
            var ancestorInst = targetInst;
            if ((eventSystemFlags & IS_EVENT_HANDLE_NON_MANAGED_NODE) === 0 && (eventSystemFlags & IS_NON_DELEGATED) === 0) {
              var targetContainerNode = targetContainer;
              if (targetInst !== null) {
                var node = targetInst;
                mainLoop: while (true) {
                  if (node === null) {
                    return;
                  }
                  var nodeTag = node.tag;
                  if (nodeTag === HostRoot || nodeTag === HostPortal) {
                    var container = node.stateNode.containerInfo;
                    if (isMatchingRootContainer(container, targetContainerNode)) {
                      break;
                    }
                    if (nodeTag === HostPortal) {
                      var grandNode = node.return;
                      while (grandNode !== null) {
                        var grandTag = grandNode.tag;
                        if (grandTag === HostRoot || grandTag === HostPortal) {
                          var grandContainer = grandNode.stateNode.containerInfo;
                          if (isMatchingRootContainer(grandContainer, targetContainerNode)) {
                            return;
                          }
                        }
                        grandNode = grandNode.return;
                      }
                    }
                    while (container !== null) {
                      var parentNode = getClosestInstanceFromNode(container);
                      if (parentNode === null) {
                        return;
                      }
                      var parentTag = parentNode.tag;
                      if (parentTag === HostComponent || parentTag === HostText) {
                        node = ancestorInst = parentNode;
                        continue mainLoop;
                      }
                      container = container.parentNode;
                    }
                  }
                  node = node.return;
                }
              }
            }
            batchedUpdates(function() {
              return dispatchEventsForPlugins(domEventName, eventSystemFlags, nativeEvent, ancestorInst);
            });
          }
          function createDispatchListener(instance, listener, currentTarget) {
            return {
              instance,
              listener,
              currentTarget
            };
          }
          function accumulateSinglePhaseListeners(targetFiber, reactName, nativeEventType, inCapturePhase, accumulateTargetOnly, nativeEvent) {
            var captureName = reactName !== null ? reactName + "Capture" : null;
            var reactEventName = inCapturePhase ? captureName : reactName;
            var listeners = [];
            var instance = targetFiber;
            var lastHostComponent = null;
            while (instance !== null) {
              var _instance2 = instance, stateNode = _instance2.stateNode, tag = _instance2.tag;
              if (tag === HostComponent && stateNode !== null) {
                lastHostComponent = stateNode;
                if (reactEventName !== null) {
                  var listener = getListener(instance, reactEventName);
                  if (listener != null) {
                    listeners.push(createDispatchListener(instance, listener, lastHostComponent));
                  }
                }
              }
              if (accumulateTargetOnly) {
                break;
              }
              instance = instance.return;
            }
            return listeners;
          }
          function accumulateTwoPhaseListeners(targetFiber, reactName) {
            var captureName = reactName + "Capture";
            var listeners = [];
            var instance = targetFiber;
            while (instance !== null) {
              var _instance3 = instance, stateNode = _instance3.stateNode, tag = _instance3.tag;
              if (tag === HostComponent && stateNode !== null) {
                var currentTarget = stateNode;
                var captureListener = getListener(instance, captureName);
                if (captureListener != null) {
                  listeners.unshift(createDispatchListener(instance, captureListener, currentTarget));
                }
                var bubbleListener = getListener(instance, reactName);
                if (bubbleListener != null) {
                  listeners.push(createDispatchListener(instance, bubbleListener, currentTarget));
                }
              }
              instance = instance.return;
            }
            return listeners;
          }
          function getParent(inst) {
            if (inst === null) {
              return null;
            }
            do {
              inst = inst.return;
            } while (inst && inst.tag !== HostComponent);
            if (inst) {
              return inst;
            }
            return null;
          }
          function getLowestCommonAncestor(instA, instB) {
            var nodeA = instA;
            var nodeB = instB;
            var depthA = 0;
            for (var tempA = nodeA; tempA; tempA = getParent(tempA)) {
              depthA++;
            }
            var depthB = 0;
            for (var tempB = nodeB; tempB; tempB = getParent(tempB)) {
              depthB++;
            }
            while (depthA - depthB > 0) {
              nodeA = getParent(nodeA);
              depthA--;
            }
            while (depthB - depthA > 0) {
              nodeB = getParent(nodeB);
              depthB--;
            }
            var depth = depthA;
            while (depth--) {
              if (nodeA === nodeB || nodeB !== null && nodeA === nodeB.alternate) {
                return nodeA;
              }
              nodeA = getParent(nodeA);
              nodeB = getParent(nodeB);
            }
            return null;
          }
          function accumulateEnterLeaveListenersForEvent(dispatchQueue, event, target, common, inCapturePhase) {
            var registrationName = event._reactName;
            var listeners = [];
            var instance = target;
            while (instance !== null) {
              if (instance === common) {
                break;
              }
              var _instance4 = instance, alternate = _instance4.alternate, stateNode = _instance4.stateNode, tag = _instance4.tag;
              if (alternate !== null && alternate === common) {
                break;
              }
              if (tag === HostComponent && stateNode !== null) {
                var currentTarget = stateNode;
                if (inCapturePhase) {
                  var captureListener = getListener(instance, registrationName);
                  if (captureListener != null) {
                    listeners.unshift(createDispatchListener(instance, captureListener, currentTarget));
                  }
                } else if (!inCapturePhase) {
                  var bubbleListener = getListener(instance, registrationName);
                  if (bubbleListener != null) {
                    listeners.push(createDispatchListener(instance, bubbleListener, currentTarget));
                  }
                }
              }
              instance = instance.return;
            }
            if (listeners.length !== 0) {
              dispatchQueue.push({
                event,
                listeners
              });
            }
          }
          function accumulateEnterLeaveTwoPhaseListeners(dispatchQueue, leaveEvent, enterEvent, from, to) {
            var common = from && to ? getLowestCommonAncestor(from, to) : null;
            if (from !== null) {
              accumulateEnterLeaveListenersForEvent(dispatchQueue, leaveEvent, from, common, false);
            }
            if (to !== null && enterEvent !== null) {
              accumulateEnterLeaveListenersForEvent(dispatchQueue, enterEvent, to, common, true);
            }
          }
          function getListenerSetKey(domEventName, capture) {
            return domEventName + "__" + (capture ? "capture" : "bubble");
          }
          var didWarnInvalidHydration = false;
          var DANGEROUSLY_SET_INNER_HTML = "dangerouslySetInnerHTML";
          var SUPPRESS_CONTENT_EDITABLE_WARNING = "suppressContentEditableWarning";
          var SUPPRESS_HYDRATION_WARNING = "suppressHydrationWarning";
          var AUTOFOCUS = "autoFocus";
          var CHILDREN = "children";
          var STYLE = "style";
          var HTML$1 = "__html";
          var warnedUnknownTags;
          var validatePropertiesInDevelopment;
          var warnForPropDifference;
          var warnForExtraAttributes;
          var warnForInvalidEventListener;
          var canDiffStyleForHydrationWarning;
          var normalizeHTML;
          {
            warnedUnknownTags = {
              // There are working polyfills for <dialog>. Let people use it.
              dialog: true,
              // Electron ships a custom <webview> tag to display external web content in
              // an isolated frame and process.
              // This tag is not present in non Electron environments such as JSDom which
              // is often used for testing purposes.
              // @see https://electronjs.org/docs/api/webview-tag
              webview: true
            };
            validatePropertiesInDevelopment = function(type, props) {
              validateProperties(type, props);
              validateProperties$1(type, props);
              validateProperties$2(type, props, {
                registrationNameDependencies,
                possibleRegistrationNames
              });
            };
            canDiffStyleForHydrationWarning = canUseDOM && !document.documentMode;
            warnForPropDifference = function(propName, serverValue, clientValue) {
              if (didWarnInvalidHydration) {
                return;
              }
              var normalizedClientValue = normalizeMarkupForTextOrAttribute(clientValue);
              var normalizedServerValue = normalizeMarkupForTextOrAttribute(serverValue);
              if (normalizedServerValue === normalizedClientValue) {
                return;
              }
              didWarnInvalidHydration = true;
              error("Prop `%s` did not match. Server: %s Client: %s", propName, JSON.stringify(normalizedServerValue), JSON.stringify(normalizedClientValue));
            };
            warnForExtraAttributes = function(attributeNames) {
              if (didWarnInvalidHydration) {
                return;
              }
              didWarnInvalidHydration = true;
              var names = [];
              attributeNames.forEach(function(name) {
                names.push(name);
              });
              error("Extra attributes from the server: %s", names);
            };
            warnForInvalidEventListener = function(registrationName, listener) {
              if (listener === false) {
                error("Expected `%s` listener to be a function, instead got `false`.\n\nIf you used to conditionally omit it with %s={condition && value}, pass %s={condition ? value : undefined} instead.", registrationName, registrationName, registrationName);
              } else {
                error("Expected `%s` listener to be a function, instead got a value of `%s` type.", registrationName, typeof listener);
              }
            };
            normalizeHTML = function(parent, html) {
              var testElement = parent.namespaceURI === HTML_NAMESPACE ? parent.ownerDocument.createElement(parent.tagName) : parent.ownerDocument.createElementNS(parent.namespaceURI, parent.tagName);
              testElement.innerHTML = html;
              return testElement.innerHTML;
            };
          }
          var NORMALIZE_NEWLINES_REGEX = /\r\n?/g;
          var NORMALIZE_NULL_AND_REPLACEMENT_REGEX = /\u0000|\uFFFD/g;
          function normalizeMarkupForTextOrAttribute(markup) {
            {
              checkHtmlStringCoercion(markup);
            }
            var markupString = typeof markup === "string" ? markup : "" + markup;
            return markupString.replace(NORMALIZE_NEWLINES_REGEX, "\n").replace(NORMALIZE_NULL_AND_REPLACEMENT_REGEX, "");
          }
          function checkForUnmatchedText(serverText, clientText, isConcurrentMode, shouldWarnDev) {
            var normalizedClientText = normalizeMarkupForTextOrAttribute(clientText);
            var normalizedServerText = normalizeMarkupForTextOrAttribute(serverText);
            if (normalizedServerText === normalizedClientText) {
              return;
            }
            if (shouldWarnDev) {
              {
                if (!didWarnInvalidHydration) {
                  didWarnInvalidHydration = true;
                  error('Text content did not match. Server: "%s" Client: "%s"', normalizedServerText, normalizedClientText);
                }
              }
            }
            if (isConcurrentMode && enableClientRenderFallbackOnTextMismatch) {
              throw new Error("Text content does not match server-rendered HTML.");
            }
          }
          function getOwnerDocumentFromRootContainer(rootContainerElement) {
            return rootContainerElement.nodeType === DOCUMENT_NODE ? rootContainerElement : rootContainerElement.ownerDocument;
          }
          function noop() {
          }
          function trapClickOnNonInteractiveElement(node) {
            node.onclick = noop;
          }
          function setInitialDOMProperties(tag, domElement, rootContainerElement, nextProps, isCustomComponentTag) {
            for (var propKey in nextProps) {
              if (!nextProps.hasOwnProperty(propKey)) {
                continue;
              }
              var nextProp = nextProps[propKey];
              if (propKey === STYLE) {
                {
                  if (nextProp) {
                    Object.freeze(nextProp);
                  }
                }
                setValueForStyles(domElement, nextProp);
              } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
                var nextHtml = nextProp ? nextProp[HTML$1] : void 0;
                if (nextHtml != null) {
                  setInnerHTML(domElement, nextHtml);
                }
              } else if (propKey === CHILDREN) {
                if (typeof nextProp === "string") {
                  var canSetTextContent = tag !== "textarea" || nextProp !== "";
                  if (canSetTextContent) {
                    setTextContent(domElement, nextProp);
                  }
                } else if (typeof nextProp === "number") {
                  setTextContent(domElement, "" + nextProp);
                }
              } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING) ;
              else if (propKey === AUTOFOCUS) ;
              else if (registrationNameDependencies.hasOwnProperty(propKey)) {
                if (nextProp != null) {
                  if (typeof nextProp !== "function") {
                    warnForInvalidEventListener(propKey, nextProp);
                  }
                  if (propKey === "onScroll") {
                    listenToNonDelegatedEvent("scroll", domElement);
                  }
                }
              } else if (nextProp != null) {
                setValueForProperty(domElement, propKey, nextProp, isCustomComponentTag);
              }
            }
          }
          function updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag) {
            for (var i = 0; i < updatePayload.length; i += 2) {
              var propKey = updatePayload[i];
              var propValue = updatePayload[i + 1];
              if (propKey === STYLE) {
                setValueForStyles(domElement, propValue);
              } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
                setInnerHTML(domElement, propValue);
              } else if (propKey === CHILDREN) {
                setTextContent(domElement, propValue);
              } else {
                setValueForProperty(domElement, propKey, propValue, isCustomComponentTag);
              }
            }
          }
          function createElement(type, props, rootContainerElement, parentNamespace) {
            var isCustomComponentTag;
            var ownerDocument = getOwnerDocumentFromRootContainer(rootContainerElement);
            var domElement;
            var namespaceURI = parentNamespace;
            if (namespaceURI === HTML_NAMESPACE) {
              namespaceURI = getIntrinsicNamespace(type);
            }
            if (namespaceURI === HTML_NAMESPACE) {
              {
                isCustomComponentTag = isCustomComponent(type, props);
                if (!isCustomComponentTag && type !== type.toLowerCase()) {
                  error("<%s /> is using incorrect casing. Use PascalCase for React components, or lowercase for HTML elements.", type);
                }
              }
              if (type === "script") {
                var div = ownerDocument.createElement("div");
                div.innerHTML = "<script><\/script>";
                var firstChild = div.firstChild;
                domElement = div.removeChild(firstChild);
              } else if (typeof props.is === "string") {
                domElement = ownerDocument.createElement(type, {
                  is: props.is
                });
              } else {
                domElement = ownerDocument.createElement(type);
                if (type === "select") {
                  var node = domElement;
                  if (props.multiple) {
                    node.multiple = true;
                  } else if (props.size) {
                    node.size = props.size;
                  }
                }
              }
            } else {
              domElement = ownerDocument.createElementNS(namespaceURI, type);
            }
            {
              if (namespaceURI === HTML_NAMESPACE) {
                if (!isCustomComponentTag && Object.prototype.toString.call(domElement) === "[object HTMLUnknownElement]" && !hasOwnProperty.call(warnedUnknownTags, type)) {
                  warnedUnknownTags[type] = true;
                  error("The tag <%s> is unrecognized in this browser. If you meant to render a React component, start its name with an uppercase letter.", type);
                }
              }
            }
            return domElement;
          }
          function createTextNode(text, rootContainerElement) {
            return getOwnerDocumentFromRootContainer(rootContainerElement).createTextNode(text);
          }
          function setInitialProperties(domElement, tag, rawProps, rootContainerElement) {
            var isCustomComponentTag = isCustomComponent(tag, rawProps);
            {
              validatePropertiesInDevelopment(tag, rawProps);
            }
            var props;
            switch (tag) {
              case "dialog":
                listenToNonDelegatedEvent("cancel", domElement);
                listenToNonDelegatedEvent("close", domElement);
                props = rawProps;
                break;
              case "iframe":
              case "object":
              case "embed":
                listenToNonDelegatedEvent("load", domElement);
                props = rawProps;
                break;
              case "video":
              case "audio":
                for (var i = 0; i < mediaEventTypes.length; i++) {
                  listenToNonDelegatedEvent(mediaEventTypes[i], domElement);
                }
                props = rawProps;
                break;
              case "source":
                listenToNonDelegatedEvent("error", domElement);
                props = rawProps;
                break;
              case "img":
              case "image":
              case "link":
                listenToNonDelegatedEvent("error", domElement);
                listenToNonDelegatedEvent("load", domElement);
                props = rawProps;
                break;
              case "details":
                listenToNonDelegatedEvent("toggle", domElement);
                props = rawProps;
                break;
              case "input":
                initWrapperState(domElement, rawProps);
                props = getHostProps(domElement, rawProps);
                listenToNonDelegatedEvent("invalid", domElement);
                break;
              case "option":
                validateProps(domElement, rawProps);
                props = rawProps;
                break;
              case "select":
                initWrapperState$1(domElement, rawProps);
                props = getHostProps$1(domElement, rawProps);
                listenToNonDelegatedEvent("invalid", domElement);
                break;
              case "textarea":
                initWrapperState$2(domElement, rawProps);
                props = getHostProps$2(domElement, rawProps);
                listenToNonDelegatedEvent("invalid", domElement);
                break;
              default:
                props = rawProps;
            }
            assertValidProps(tag, props);
            setInitialDOMProperties(tag, domElement, rootContainerElement, props, isCustomComponentTag);
            switch (tag) {
              case "input":
                track(domElement);
                postMountWrapper(domElement, rawProps, false);
                break;
              case "textarea":
                track(domElement);
                postMountWrapper$3(domElement);
                break;
              case "option":
                postMountWrapper$1(domElement, rawProps);
                break;
              case "select":
                postMountWrapper$2(domElement, rawProps);
                break;
              default:
                if (typeof props.onClick === "function") {
                  trapClickOnNonInteractiveElement(domElement);
                }
                break;
            }
          }
          function diffProperties(domElement, tag, lastRawProps, nextRawProps, rootContainerElement) {
            {
              validatePropertiesInDevelopment(tag, nextRawProps);
            }
            var updatePayload = null;
            var lastProps;
            var nextProps;
            switch (tag) {
              case "input":
                lastProps = getHostProps(domElement, lastRawProps);
                nextProps = getHostProps(domElement, nextRawProps);
                updatePayload = [];
                break;
              case "select":
                lastProps = getHostProps$1(domElement, lastRawProps);
                nextProps = getHostProps$1(domElement, nextRawProps);
                updatePayload = [];
                break;
              case "textarea":
                lastProps = getHostProps$2(domElement, lastRawProps);
                nextProps = getHostProps$2(domElement, nextRawProps);
                updatePayload = [];
                break;
              default:
                lastProps = lastRawProps;
                nextProps = nextRawProps;
                if (typeof lastProps.onClick !== "function" && typeof nextProps.onClick === "function") {
                  trapClickOnNonInteractiveElement(domElement);
                }
                break;
            }
            assertValidProps(tag, nextProps);
            var propKey;
            var styleName;
            var styleUpdates = null;
            for (propKey in lastProps) {
              if (nextProps.hasOwnProperty(propKey) || !lastProps.hasOwnProperty(propKey) || lastProps[propKey] == null) {
                continue;
              }
              if (propKey === STYLE) {
                var lastStyle = lastProps[propKey];
                for (styleName in lastStyle) {
                  if (lastStyle.hasOwnProperty(styleName)) {
                    if (!styleUpdates) {
                      styleUpdates = {};
                    }
                    styleUpdates[styleName] = "";
                  }
                }
              } else if (propKey === DANGEROUSLY_SET_INNER_HTML || propKey === CHILDREN) ;
              else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING) ;
              else if (propKey === AUTOFOCUS) ;
              else if (registrationNameDependencies.hasOwnProperty(propKey)) {
                if (!updatePayload) {
                  updatePayload = [];
                }
              } else {
                (updatePayload = updatePayload || []).push(propKey, null);
              }
            }
            for (propKey in nextProps) {
              var nextProp = nextProps[propKey];
              var lastProp = lastProps != null ? lastProps[propKey] : void 0;
              if (!nextProps.hasOwnProperty(propKey) || nextProp === lastProp || nextProp == null && lastProp == null) {
                continue;
              }
              if (propKey === STYLE) {
                {
                  if (nextProp) {
                    Object.freeze(nextProp);
                  }
                }
                if (lastProp) {
                  for (styleName in lastProp) {
                    if (lastProp.hasOwnProperty(styleName) && (!nextProp || !nextProp.hasOwnProperty(styleName))) {
                      if (!styleUpdates) {
                        styleUpdates = {};
                      }
                      styleUpdates[styleName] = "";
                    }
                  }
                  for (styleName in nextProp) {
                    if (nextProp.hasOwnProperty(styleName) && lastProp[styleName] !== nextProp[styleName]) {
                      if (!styleUpdates) {
                        styleUpdates = {};
                      }
                      styleUpdates[styleName] = nextProp[styleName];
                    }
                  }
                } else {
                  if (!styleUpdates) {
                    if (!updatePayload) {
                      updatePayload = [];
                    }
                    updatePayload.push(propKey, styleUpdates);
                  }
                  styleUpdates = nextProp;
                }
              } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
                var nextHtml = nextProp ? nextProp[HTML$1] : void 0;
                var lastHtml = lastProp ? lastProp[HTML$1] : void 0;
                if (nextHtml != null) {
                  if (lastHtml !== nextHtml) {
                    (updatePayload = updatePayload || []).push(propKey, nextHtml);
                  }
                }
              } else if (propKey === CHILDREN) {
                if (typeof nextProp === "string" || typeof nextProp === "number") {
                  (updatePayload = updatePayload || []).push(propKey, "" + nextProp);
                }
              } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING) ;
              else if (registrationNameDependencies.hasOwnProperty(propKey)) {
                if (nextProp != null) {
                  if (typeof nextProp !== "function") {
                    warnForInvalidEventListener(propKey, nextProp);
                  }
                  if (propKey === "onScroll") {
                    listenToNonDelegatedEvent("scroll", domElement);
                  }
                }
                if (!updatePayload && lastProp !== nextProp) {
                  updatePayload = [];
                }
              } else {
                (updatePayload = updatePayload || []).push(propKey, nextProp);
              }
            }
            if (styleUpdates) {
              {
                validateShorthandPropertyCollisionInDev(styleUpdates, nextProps[STYLE]);
              }
              (updatePayload = updatePayload || []).push(STYLE, styleUpdates);
            }
            return updatePayload;
          }
          function updateProperties(domElement, updatePayload, tag, lastRawProps, nextRawProps) {
            if (tag === "input" && nextRawProps.type === "radio" && nextRawProps.name != null) {
              updateChecked(domElement, nextRawProps);
            }
            var wasCustomComponentTag = isCustomComponent(tag, lastRawProps);
            var isCustomComponentTag = isCustomComponent(tag, nextRawProps);
            updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag);
            switch (tag) {
              case "input":
                updateWrapper(domElement, nextRawProps);
                break;
              case "textarea":
                updateWrapper$1(domElement, nextRawProps);
                break;
              case "select":
                postUpdateWrapper(domElement, nextRawProps);
                break;
            }
          }
          function getPossibleStandardName(propName) {
            {
              var lowerCasedName = propName.toLowerCase();
              if (!possibleStandardNames.hasOwnProperty(lowerCasedName)) {
                return null;
              }
              return possibleStandardNames[lowerCasedName] || null;
            }
          }
          function diffHydratedProperties(domElement, tag, rawProps, parentNamespace, rootContainerElement, isConcurrentMode, shouldWarnDev) {
            var isCustomComponentTag;
            var extraAttributeNames;
            {
              isCustomComponentTag = isCustomComponent(tag, rawProps);
              validatePropertiesInDevelopment(tag, rawProps);
            }
            switch (tag) {
              case "dialog":
                listenToNonDelegatedEvent("cancel", domElement);
                listenToNonDelegatedEvent("close", domElement);
                break;
              case "iframe":
              case "object":
              case "embed":
                listenToNonDelegatedEvent("load", domElement);
                break;
              case "video":
              case "audio":
                for (var i = 0; i < mediaEventTypes.length; i++) {
                  listenToNonDelegatedEvent(mediaEventTypes[i], domElement);
                }
                break;
              case "source":
                listenToNonDelegatedEvent("error", domElement);
                break;
              case "img":
              case "image":
              case "link":
                listenToNonDelegatedEvent("error", domElement);
                listenToNonDelegatedEvent("load", domElement);
                break;
              case "details":
                listenToNonDelegatedEvent("toggle", domElement);
                break;
              case "input":
                initWrapperState(domElement, rawProps);
                listenToNonDelegatedEvent("invalid", domElement);
                break;
              case "option":
                validateProps(domElement, rawProps);
                break;
              case "select":
                initWrapperState$1(domElement, rawProps);
                listenToNonDelegatedEvent("invalid", domElement);
                break;
              case "textarea":
                initWrapperState$2(domElement, rawProps);
                listenToNonDelegatedEvent("invalid", domElement);
                break;
            }
            assertValidProps(tag, rawProps);
            {
              extraAttributeNames = /* @__PURE__ */ new Set();
              var attributes = domElement.attributes;
              for (var _i = 0; _i < attributes.length; _i++) {
                var name = attributes[_i].name.toLowerCase();
                switch (name) {
                  // Controlled attributes are not validated
                  // TODO: Only ignore them on controlled tags.
                  case "value":
                    break;
                  case "checked":
                    break;
                  case "selected":
                    break;
                  default:
                    extraAttributeNames.add(attributes[_i].name);
                }
              }
            }
            var updatePayload = null;
            for (var propKey in rawProps) {
              if (!rawProps.hasOwnProperty(propKey)) {
                continue;
              }
              var nextProp = rawProps[propKey];
              if (propKey === CHILDREN) {
                if (typeof nextProp === "string") {
                  if (domElement.textContent !== nextProp) {
                    if (rawProps[SUPPRESS_HYDRATION_WARNING] !== true) {
                      checkForUnmatchedText(domElement.textContent, nextProp, isConcurrentMode, shouldWarnDev);
                    }
                    updatePayload = [CHILDREN, nextProp];
                  }
                } else if (typeof nextProp === "number") {
                  if (domElement.textContent !== "" + nextProp) {
                    if (rawProps[SUPPRESS_HYDRATION_WARNING] !== true) {
                      checkForUnmatchedText(domElement.textContent, nextProp, isConcurrentMode, shouldWarnDev);
                    }
                    updatePayload = [CHILDREN, "" + nextProp];
                  }
                }
              } else if (registrationNameDependencies.hasOwnProperty(propKey)) {
                if (nextProp != null) {
                  if (typeof nextProp !== "function") {
                    warnForInvalidEventListener(propKey, nextProp);
                  }
                  if (propKey === "onScroll") {
                    listenToNonDelegatedEvent("scroll", domElement);
                  }
                }
              } else if (shouldWarnDev && true && // Convince Flow we've calculated it (it's DEV-only in this method.)
              typeof isCustomComponentTag === "boolean") {
                var serverValue = void 0;
                var propertyInfo = isCustomComponentTag && enableCustomElementPropertySupport ? null : getPropertyInfo(propKey);
                if (rawProps[SUPPRESS_HYDRATION_WARNING] === true) ;
                else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING || // Controlled attributes are not validated
                // TODO: Only ignore them on controlled tags.
                propKey === "value" || propKey === "checked" || propKey === "selected") ;
                else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
                  var serverHTML = domElement.innerHTML;
                  var nextHtml = nextProp ? nextProp[HTML$1] : void 0;
                  if (nextHtml != null) {
                    var expectedHTML = normalizeHTML(domElement, nextHtml);
                    if (expectedHTML !== serverHTML) {
                      warnForPropDifference(propKey, serverHTML, expectedHTML);
                    }
                  }
                } else if (propKey === STYLE) {
                  extraAttributeNames.delete(propKey);
                  if (canDiffStyleForHydrationWarning) {
                    var expectedStyle = createDangerousStringForStyles(nextProp);
                    serverValue = domElement.getAttribute("style");
                    if (expectedStyle !== serverValue) {
                      warnForPropDifference(propKey, serverValue, expectedStyle);
                    }
                  }
                } else if (isCustomComponentTag && !enableCustomElementPropertySupport) {
                  extraAttributeNames.delete(propKey.toLowerCase());
                  serverValue = getValueForAttribute(domElement, propKey, nextProp);
                  if (nextProp !== serverValue) {
                    warnForPropDifference(propKey, serverValue, nextProp);
                  }
                } else if (!shouldIgnoreAttribute(propKey, propertyInfo, isCustomComponentTag) && !shouldRemoveAttribute(propKey, nextProp, propertyInfo, isCustomComponentTag)) {
                  var isMismatchDueToBadCasing = false;
                  if (propertyInfo !== null) {
                    extraAttributeNames.delete(propertyInfo.attributeName);
                    serverValue = getValueForProperty(domElement, propKey, nextProp, propertyInfo);
                  } else {
                    var ownNamespace = parentNamespace;
                    if (ownNamespace === HTML_NAMESPACE) {
                      ownNamespace = getIntrinsicNamespace(tag);
                    }
                    if (ownNamespace === HTML_NAMESPACE) {
                      extraAttributeNames.delete(propKey.toLowerCase());
                    } else {
                      var standardName = getPossibleStandardName(propKey);
                      if (standardName !== null && standardName !== propKey) {
                        isMismatchDueToBadCasing = true;
                        extraAttributeNames.delete(standardName);
                      }
                      extraAttributeNames.delete(propKey);
                    }
                    serverValue = getValueForAttribute(domElement, propKey, nextProp);
                  }
                  var dontWarnCustomElement = enableCustomElementPropertySupport;
                  if (!dontWarnCustomElement && nextProp !== serverValue && !isMismatchDueToBadCasing) {
                    warnForPropDifference(propKey, serverValue, nextProp);
                  }
                }
              }
            }
            {
              if (shouldWarnDev) {
                if (
                  // $FlowFixMe - Should be inferred as not undefined.
                  extraAttributeNames.size > 0 && rawProps[SUPPRESS_HYDRATION_WARNING] !== true
                ) {
                  warnForExtraAttributes(extraAttributeNames);
                }
              }
            }
            switch (tag) {
              case "input":
                track(domElement);
                postMountWrapper(domElement, rawProps, true);
                break;
              case "textarea":
                track(domElement);
                postMountWrapper$3(domElement);
                break;
              case "select":
              case "option":
                break;
              default:
                if (typeof rawProps.onClick === "function") {
                  trapClickOnNonInteractiveElement(domElement);
                }
                break;
            }
            return updatePayload;
          }
          function diffHydratedText(textNode, text, isConcurrentMode) {
            var isDifferent = textNode.nodeValue !== text;
            return isDifferent;
          }
          function warnForDeletedHydratableElement(parentNode, child) {
            {
              if (didWarnInvalidHydration) {
                return;
              }
              didWarnInvalidHydration = true;
              error("Did not expect server HTML to contain a <%s> in <%s>.", child.nodeName.toLowerCase(), parentNode.nodeName.toLowerCase());
            }
          }
          function warnForDeletedHydratableText(parentNode, child) {
            {
              if (didWarnInvalidHydration) {
                return;
              }
              didWarnInvalidHydration = true;
              error('Did not expect server HTML to contain the text node "%s" in <%s>.', child.nodeValue, parentNode.nodeName.toLowerCase());
            }
          }
          function warnForInsertedHydratedElement(parentNode, tag, props) {
            {
              if (didWarnInvalidHydration) {
                return;
              }
              didWarnInvalidHydration = true;
              error("Expected server HTML to contain a matching <%s> in <%s>.", tag, parentNode.nodeName.toLowerCase());
            }
          }
          function warnForInsertedHydratedText(parentNode, text) {
            {
              if (text === "") {
                return;
              }
              if (didWarnInvalidHydration) {
                return;
              }
              didWarnInvalidHydration = true;
              error('Expected server HTML to contain a matching text node for "%s" in <%s>.', text, parentNode.nodeName.toLowerCase());
            }
          }
          function restoreControlledState$3(domElement, tag, props) {
            switch (tag) {
              case "input":
                restoreControlledState(domElement, props);
                return;
              case "textarea":
                restoreControlledState$2(domElement, props);
                return;
              case "select":
                restoreControlledState$1(domElement, props);
                return;
            }
          }
          var validateDOMNesting = function() {
          };
          var updatedAncestorInfo = function() {
          };
          {
            var specialTags = ["address", "applet", "area", "article", "aside", "base", "basefont", "bgsound", "blockquote", "body", "br", "button", "caption", "center", "col", "colgroup", "dd", "details", "dir", "div", "dl", "dt", "embed", "fieldset", "figcaption", "figure", "footer", "form", "frame", "frameset", "h1", "h2", "h3", "h4", "h5", "h6", "head", "header", "hgroup", "hr", "html", "iframe", "img", "input", "isindex", "li", "link", "listing", "main", "marquee", "menu", "menuitem", "meta", "nav", "noembed", "noframes", "noscript", "object", "ol", "p", "param", "plaintext", "pre", "script", "section", "select", "source", "style", "summary", "table", "tbody", "td", "template", "textarea", "tfoot", "th", "thead", "title", "tr", "track", "ul", "wbr", "xmp"];
            var inScopeTags = [
              "applet",
              "caption",
              "html",
              "table",
              "td",
              "th",
              "marquee",
              "object",
              "template",
              // https://html.spec.whatwg.org/multipage/syntax.html#html-integration-point
              // TODO: Distinguish by namespace here -- for <title>, including it here
              // errs on the side of fewer warnings
              "foreignObject",
              "desc",
              "title"
            ];
            var buttonScopeTags = inScopeTags.concat(["button"]);
            var impliedEndTags = ["dd", "dt", "li", "option", "optgroup", "p", "rp", "rt"];
            var emptyAncestorInfo = {
              current: null,
              formTag: null,
              aTagInScope: null,
              buttonTagInScope: null,
              nobrTagInScope: null,
              pTagInButtonScope: null,
              listItemTagAutoclosing: null,
              dlItemTagAutoclosing: null
            };
            updatedAncestorInfo = function(oldInfo, tag) {
              var ancestorInfo = assign({}, oldInfo || emptyAncestorInfo);
              var info = {
                tag
              };
              if (inScopeTags.indexOf(tag) !== -1) {
                ancestorInfo.aTagInScope = null;
                ancestorInfo.buttonTagInScope = null;
                ancestorInfo.nobrTagInScope = null;
              }
              if (buttonScopeTags.indexOf(tag) !== -1) {
                ancestorInfo.pTagInButtonScope = null;
              }
              if (specialTags.indexOf(tag) !== -1 && tag !== "address" && tag !== "div" && tag !== "p") {
                ancestorInfo.listItemTagAutoclosing = null;
                ancestorInfo.dlItemTagAutoclosing = null;
              }
              ancestorInfo.current = info;
              if (tag === "form") {
                ancestorInfo.formTag = info;
              }
              if (tag === "a") {
                ancestorInfo.aTagInScope = info;
              }
              if (tag === "button") {
                ancestorInfo.buttonTagInScope = info;
              }
              if (tag === "nobr") {
                ancestorInfo.nobrTagInScope = info;
              }
              if (tag === "p") {
                ancestorInfo.pTagInButtonScope = info;
              }
              if (tag === "li") {
                ancestorInfo.listItemTagAutoclosing = info;
              }
              if (tag === "dd" || tag === "dt") {
                ancestorInfo.dlItemTagAutoclosing = info;
              }
              return ancestorInfo;
            };
            var isTagValidWithParent = function(tag, parentTag) {
              switch (parentTag) {
                // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inselect
                case "select":
                  return tag === "option" || tag === "optgroup" || tag === "#text";
                case "optgroup":
                  return tag === "option" || tag === "#text";
                // Strictly speaking, seeing an <option> doesn't mean we're in a <select>
                // but
                case "option":
                  return tag === "#text";
                // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intd
                // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incaption
                // No special behavior since these rules fall back to "in body" mode for
                // all except special table nodes which cause bad parsing behavior anyway.
                // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intr
                case "tr":
                  return tag === "th" || tag === "td" || tag === "style" || tag === "script" || tag === "template";
                // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intbody
                case "tbody":
                case "thead":
                case "tfoot":
                  return tag === "tr" || tag === "style" || tag === "script" || tag === "template";
                // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incolgroup
                case "colgroup":
                  return tag === "col" || tag === "template";
                // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intable
                case "table":
                  return tag === "caption" || tag === "colgroup" || tag === "tbody" || tag === "tfoot" || tag === "thead" || tag === "style" || tag === "script" || tag === "template";
                // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inhead
                case "head":
                  return tag === "base" || tag === "basefont" || tag === "bgsound" || tag === "link" || tag === "meta" || tag === "title" || tag === "noscript" || tag === "noframes" || tag === "style" || tag === "script" || tag === "template";
                // https://html.spec.whatwg.org/multipage/semantics.html#the-html-element
                case "html":
                  return tag === "head" || tag === "body" || tag === "frameset";
                case "frameset":
                  return tag === "frame";
                case "#document":
                  return tag === "html";
              }
              switch (tag) {
                case "h1":
                case "h2":
                case "h3":
                case "h4":
                case "h5":
                case "h6":
                  return parentTag !== "h1" && parentTag !== "h2" && parentTag !== "h3" && parentTag !== "h4" && parentTag !== "h5" && parentTag !== "h6";
                case "rp":
                case "rt":
                  return impliedEndTags.indexOf(parentTag) === -1;
                case "body":
                case "caption":
                case "col":
                case "colgroup":
                case "frameset":
                case "frame":
                case "head":
                case "html":
                case "tbody":
                case "td":
                case "tfoot":
                case "th":
                case "thead":
                case "tr":
                  return parentTag == null;
              }
              return true;
            };
            var findInvalidAncestorForTag = function(tag, ancestorInfo) {
              switch (tag) {
                case "address":
                case "article":
                case "aside":
                case "blockquote":
                case "center":
                case "details":
                case "dialog":
                case "dir":
                case "div":
                case "dl":
                case "fieldset":
                case "figcaption":
                case "figure":
                case "footer":
                case "header":
                case "hgroup":
                case "main":
                case "menu":
                case "nav":
                case "ol":
                case "p":
                case "section":
                case "summary":
                case "ul":
                case "pre":
                case "listing":
                case "table":
                case "hr":
                case "xmp":
                case "h1":
                case "h2":
                case "h3":
                case "h4":
                case "h5":
                case "h6":
                  return ancestorInfo.pTagInButtonScope;
                case "form":
                  return ancestorInfo.formTag || ancestorInfo.pTagInButtonScope;
                case "li":
                  return ancestorInfo.listItemTagAutoclosing;
                case "dd":
                case "dt":
                  return ancestorInfo.dlItemTagAutoclosing;
                case "button":
                  return ancestorInfo.buttonTagInScope;
                case "a":
                  return ancestorInfo.aTagInScope;
                case "nobr":
                  return ancestorInfo.nobrTagInScope;
              }
              return null;
            };
            var didWarn$1 = {};
            validateDOMNesting = function(childTag, childText, ancestorInfo) {
              ancestorInfo = ancestorInfo || emptyAncestorInfo;
              var parentInfo = ancestorInfo.current;
              var parentTag = parentInfo && parentInfo.tag;
              if (childText != null) {
                if (childTag != null) {
                  error("validateDOMNesting: when childText is passed, childTag should be null");
                }
                childTag = "#text";
              }
              var invalidParent = isTagValidWithParent(childTag, parentTag) ? null : parentInfo;
              var invalidAncestor = invalidParent ? null : findInvalidAncestorForTag(childTag, ancestorInfo);
              var invalidParentOrAncestor = invalidParent || invalidAncestor;
              if (!invalidParentOrAncestor) {
                return;
              }
              var ancestorTag = invalidParentOrAncestor.tag;
              var warnKey = !!invalidParent + "|" + childTag + "|" + ancestorTag;
              if (didWarn$1[warnKey]) {
                return;
              }
              didWarn$1[warnKey] = true;
              var tagDisplayName = childTag;
              var whitespaceInfo = "";
              if (childTag === "#text") {
                if (/\S/.test(childText)) {
                  tagDisplayName = "Text nodes";
                } else {
                  tagDisplayName = "Whitespace text nodes";
                  whitespaceInfo = " Make sure you don't have any extra whitespace between tags on each line of your source code.";
                }
              } else {
                tagDisplayName = "<" + childTag + ">";
              }
              if (invalidParent) {
                var info = "";
                if (ancestorTag === "table" && childTag === "tr") {
                  info += " Add a <tbody>, <thead> or <tfoot> to your code to match the DOM tree generated by the browser.";
                }
                error("validateDOMNesting(...): %s cannot appear as a child of <%s>.%s%s", tagDisplayName, ancestorTag, whitespaceInfo, info);
              } else {
                error("validateDOMNesting(...): %s cannot appear as a descendant of <%s>.", tagDisplayName, ancestorTag);
              }
            };
          }
          var SUPPRESS_HYDRATION_WARNING$1 = "suppressHydrationWarning";
          var SUSPENSE_START_DATA = "$";
          var SUSPENSE_END_DATA = "/$";
          var SUSPENSE_PENDING_START_DATA = "$?";
          var SUSPENSE_FALLBACK_START_DATA = "$!";
          var STYLE$1 = "style";
          var eventsEnabled = null;
          var selectionInformation = null;
          function getRootHostContext(rootContainerInstance) {
            var type;
            var namespace;
            var nodeType = rootContainerInstance.nodeType;
            switch (nodeType) {
              case DOCUMENT_NODE:
              case DOCUMENT_FRAGMENT_NODE: {
                type = nodeType === DOCUMENT_NODE ? "#document" : "#fragment";
                var root3 = rootContainerInstance.documentElement;
                namespace = root3 ? root3.namespaceURI : getChildNamespace(null, "");
                break;
              }
              default: {
                var container = nodeType === COMMENT_NODE ? rootContainerInstance.parentNode : rootContainerInstance;
                var ownNamespace = container.namespaceURI || null;
                type = container.tagName;
                namespace = getChildNamespace(ownNamespace, type);
                break;
              }
            }
            {
              var validatedTag = type.toLowerCase();
              var ancestorInfo = updatedAncestorInfo(null, validatedTag);
              return {
                namespace,
                ancestorInfo
              };
            }
          }
          function getChildHostContext(parentHostContext, type, rootContainerInstance) {
            {
              var parentHostContextDev = parentHostContext;
              var namespace = getChildNamespace(parentHostContextDev.namespace, type);
              var ancestorInfo = updatedAncestorInfo(parentHostContextDev.ancestorInfo, type);
              return {
                namespace,
                ancestorInfo
              };
            }
          }
          function getPublicInstance(instance) {
            return instance;
          }
          function prepareForCommit(containerInfo) {
            eventsEnabled = isEnabled();
            selectionInformation = getSelectionInformation();
            var activeInstance = null;
            setEnabled(false);
            return activeInstance;
          }
          function resetAfterCommit(containerInfo) {
            restoreSelection(selectionInformation);
            setEnabled(eventsEnabled);
            eventsEnabled = null;
            selectionInformation = null;
          }
          function createInstance(type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
            var parentNamespace;
            {
              var hostContextDev = hostContext;
              validateDOMNesting(type, null, hostContextDev.ancestorInfo);
              if (typeof props.children === "string" || typeof props.children === "number") {
                var string = "" + props.children;
                var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type);
                validateDOMNesting(null, string, ownAncestorInfo);
              }
              parentNamespace = hostContextDev.namespace;
            }
            var domElement = createElement(type, props, rootContainerInstance, parentNamespace);
            precacheFiberNode(internalInstanceHandle, domElement);
            updateFiberProps(domElement, props);
            return domElement;
          }
          function appendInitialChild(parentInstance, child) {
            parentInstance.appendChild(child);
          }
          function finalizeInitialChildren(domElement, type, props, rootContainerInstance, hostContext) {
            setInitialProperties(domElement, type, props, rootContainerInstance);
            switch (type) {
              case "button":
              case "input":
              case "select":
              case "textarea":
                return !!props.autoFocus;
              case "img":
                return true;
              default:
                return false;
            }
          }
          function prepareUpdate(domElement, type, oldProps, newProps, rootContainerInstance, hostContext) {
            {
              var hostContextDev = hostContext;
              if (typeof newProps.children !== typeof oldProps.children && (typeof newProps.children === "string" || typeof newProps.children === "number")) {
                var string = "" + newProps.children;
                var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type);
                validateDOMNesting(null, string, ownAncestorInfo);
              }
            }
            return diffProperties(domElement, type, oldProps, newProps);
          }
          function shouldSetTextContent(type, props) {
            return type === "textarea" || type === "noscript" || typeof props.children === "string" || typeof props.children === "number" || typeof props.dangerouslySetInnerHTML === "object" && props.dangerouslySetInnerHTML !== null && props.dangerouslySetInnerHTML.__html != null;
          }
          function createTextInstance(text, rootContainerInstance, hostContext, internalInstanceHandle) {
            {
              var hostContextDev = hostContext;
              validateDOMNesting(null, text, hostContextDev.ancestorInfo);
            }
            var textNode = createTextNode(text, rootContainerInstance);
            precacheFiberNode(internalInstanceHandle, textNode);
            return textNode;
          }
          function getCurrentEventPriority() {
            var currentEvent = window.event;
            if (currentEvent === void 0) {
              return DefaultEventPriority;
            }
            return getEventPriority(currentEvent.type);
          }
          var scheduleTimeout = typeof setTimeout === "function" ? setTimeout : void 0;
          var cancelTimeout = typeof clearTimeout === "function" ? clearTimeout : void 0;
          var noTimeout = -1;
          var localPromise = typeof Promise === "function" ? Promise : void 0;
          var scheduleMicrotask = typeof queueMicrotask === "function" ? queueMicrotask : typeof localPromise !== "undefined" ? function(callback) {
            return localPromise.resolve(null).then(callback).catch(handleErrorInNextTick);
          } : scheduleTimeout;
          function handleErrorInNextTick(error2) {
            setTimeout(function() {
              throw error2;
            });
          }
          function commitMount(domElement, type, newProps, internalInstanceHandle) {
            switch (type) {
              case "button":
              case "input":
              case "select":
              case "textarea":
                if (newProps.autoFocus) {
                  domElement.focus();
                }
                return;
              case "img": {
                if (newProps.src) {
                  domElement.src = newProps.src;
                }
                return;
              }
            }
          }
          function commitUpdate(domElement, updatePayload, type, oldProps, newProps, internalInstanceHandle) {
            updateProperties(domElement, updatePayload, type, oldProps, newProps);
            updateFiberProps(domElement, newProps);
          }
          function resetTextContent(domElement) {
            setTextContent(domElement, "");
          }
          function commitTextUpdate(textInstance, oldText, newText) {
            textInstance.nodeValue = newText;
          }
          function appendChild(parentInstance, child) {
            parentInstance.appendChild(child);
          }
          function appendChildToContainer(container, child) {
            var parentNode;
            if (container.nodeType === COMMENT_NODE) {
              parentNode = container.parentNode;
              parentNode.insertBefore(child, container);
            } else {
              parentNode = container;
              parentNode.appendChild(child);
            }
            var reactRootContainer = container._reactRootContainer;
            if ((reactRootContainer === null || reactRootContainer === void 0) && parentNode.onclick === null) {
              trapClickOnNonInteractiveElement(parentNode);
            }
          }
          function insertBefore(parentInstance, child, beforeChild) {
            parentInstance.insertBefore(child, beforeChild);
          }
          function insertInContainerBefore(container, child, beforeChild) {
            if (container.nodeType === COMMENT_NODE) {
              container.parentNode.insertBefore(child, beforeChild);
            } else {
              container.insertBefore(child, beforeChild);
            }
          }
          function removeChild(parentInstance, child) {
            parentInstance.removeChild(child);
          }
          function removeChildFromContainer(container, child) {
            if (container.nodeType === COMMENT_NODE) {
              container.parentNode.removeChild(child);
            } else {
              container.removeChild(child);
            }
          }
          function clearSuspenseBoundary(parentInstance, suspenseInstance) {
            var node = suspenseInstance;
            var depth = 0;
            do {
              var nextNode = node.nextSibling;
              parentInstance.removeChild(node);
              if (nextNode && nextNode.nodeType === COMMENT_NODE) {
                var data = nextNode.data;
                if (data === SUSPENSE_END_DATA) {
                  if (depth === 0) {
                    parentInstance.removeChild(nextNode);
                    retryIfBlockedOn(suspenseInstance);
                    return;
                  } else {
                    depth--;
                  }
                } else if (data === SUSPENSE_START_DATA || data === SUSPENSE_PENDING_START_DATA || data === SUSPENSE_FALLBACK_START_DATA) {
                  depth++;
                }
              }
              node = nextNode;
            } while (node);
            retryIfBlockedOn(suspenseInstance);
          }
          function clearSuspenseBoundaryFromContainer(container, suspenseInstance) {
            if (container.nodeType === COMMENT_NODE) {
              clearSuspenseBoundary(container.parentNode, suspenseInstance);
            } else if (container.nodeType === ELEMENT_NODE) {
              clearSuspenseBoundary(container, suspenseInstance);
            }
            retryIfBlockedOn(container);
          }
          function hideInstance(instance) {
            instance = instance;
            var style2 = instance.style;
            if (typeof style2.setProperty === "function") {
              style2.setProperty("display", "none", "important");
            } else {
              style2.display = "none";
            }
          }
          function hideTextInstance(textInstance) {
            textInstance.nodeValue = "";
          }
          function unhideInstance(instance, props) {
            instance = instance;
            var styleProp = props[STYLE$1];
            var display = styleProp !== void 0 && styleProp !== null && styleProp.hasOwnProperty("display") ? styleProp.display : null;
            instance.style.display = dangerousStyleValue("display", display);
          }
          function unhideTextInstance(textInstance, text) {
            textInstance.nodeValue = text;
          }
          function clearContainer(container) {
            if (container.nodeType === ELEMENT_NODE) {
              container.textContent = "";
            } else if (container.nodeType === DOCUMENT_NODE) {
              if (container.documentElement) {
                container.removeChild(container.documentElement);
              }
            }
          }
          function canHydrateInstance(instance, type, props) {
            if (instance.nodeType !== ELEMENT_NODE || type.toLowerCase() !== instance.nodeName.toLowerCase()) {
              return null;
            }
            return instance;
          }
          function canHydrateTextInstance(instance, text) {
            if (text === "" || instance.nodeType !== TEXT_NODE) {
              return null;
            }
            return instance;
          }
          function canHydrateSuspenseInstance(instance) {
            if (instance.nodeType !== COMMENT_NODE) {
              return null;
            }
            return instance;
          }
          function isSuspenseInstancePending(instance) {
            return instance.data === SUSPENSE_PENDING_START_DATA;
          }
          function isSuspenseInstanceFallback(instance) {
            return instance.data === SUSPENSE_FALLBACK_START_DATA;
          }
          function getSuspenseInstanceFallbackErrorDetails(instance) {
            var dataset = instance.nextSibling && instance.nextSibling.dataset;
            var digest, message, stack;
            if (dataset) {
              digest = dataset.dgst;
              {
                message = dataset.msg;
                stack = dataset.stck;
              }
            }
            {
              return {
                message,
                digest,
                stack
              };
            }
          }
          function registerSuspenseInstanceRetry(instance, callback) {
            instance._reactRetry = callback;
          }
          function getNextHydratable(node) {
            for (; node != null; node = node.nextSibling) {
              var nodeType = node.nodeType;
              if (nodeType === ELEMENT_NODE || nodeType === TEXT_NODE) {
                break;
              }
              if (nodeType === COMMENT_NODE) {
                var nodeData = node.data;
                if (nodeData === SUSPENSE_START_DATA || nodeData === SUSPENSE_FALLBACK_START_DATA || nodeData === SUSPENSE_PENDING_START_DATA) {
                  break;
                }
                if (nodeData === SUSPENSE_END_DATA) {
                  return null;
                }
              }
            }
            return node;
          }
          function getNextHydratableSibling(instance) {
            return getNextHydratable(instance.nextSibling);
          }
          function getFirstHydratableChild(parentInstance) {
            return getNextHydratable(parentInstance.firstChild);
          }
          function getFirstHydratableChildWithinContainer(parentContainer) {
            return getNextHydratable(parentContainer.firstChild);
          }
          function getFirstHydratableChildWithinSuspenseInstance(parentInstance) {
            return getNextHydratable(parentInstance.nextSibling);
          }
          function hydrateInstance(instance, type, props, rootContainerInstance, hostContext, internalInstanceHandle, shouldWarnDev) {
            precacheFiberNode(internalInstanceHandle, instance);
            updateFiberProps(instance, props);
            var parentNamespace;
            {
              var hostContextDev = hostContext;
              parentNamespace = hostContextDev.namespace;
            }
            var isConcurrentMode = (internalInstanceHandle.mode & ConcurrentMode) !== NoMode;
            return diffHydratedProperties(instance, type, props, parentNamespace, rootContainerInstance, isConcurrentMode, shouldWarnDev);
          }
          function hydrateTextInstance(textInstance, text, internalInstanceHandle, shouldWarnDev) {
            precacheFiberNode(internalInstanceHandle, textInstance);
            var isConcurrentMode = (internalInstanceHandle.mode & ConcurrentMode) !== NoMode;
            return diffHydratedText(textInstance, text);
          }
          function hydrateSuspenseInstance(suspenseInstance, internalInstanceHandle) {
            precacheFiberNode(internalInstanceHandle, suspenseInstance);
          }
          function getNextHydratableInstanceAfterSuspenseInstance(suspenseInstance) {
            var node = suspenseInstance.nextSibling;
            var depth = 0;
            while (node) {
              if (node.nodeType === COMMENT_NODE) {
                var data = node.data;
                if (data === SUSPENSE_END_DATA) {
                  if (depth === 0) {
                    return getNextHydratableSibling(node);
                  } else {
                    depth--;
                  }
                } else if (data === SUSPENSE_START_DATA || data === SUSPENSE_FALLBACK_START_DATA || data === SUSPENSE_PENDING_START_DATA) {
                  depth++;
                }
              }
              node = node.nextSibling;
            }
            return null;
          }
          function getParentSuspenseInstance(targetInstance) {
            var node = targetInstance.previousSibling;
            var depth = 0;
            while (node) {
              if (node.nodeType === COMMENT_NODE) {
                var data = node.data;
                if (data === SUSPENSE_START_DATA || data === SUSPENSE_FALLBACK_START_DATA || data === SUSPENSE_PENDING_START_DATA) {
                  if (depth === 0) {
                    return node;
                  } else {
                    depth--;
                  }
                } else if (data === SUSPENSE_END_DATA) {
                  depth++;
                }
              }
              node = node.previousSibling;
            }
            return null;
          }
          function commitHydratedContainer(container) {
            retryIfBlockedOn(container);
          }
          function commitHydratedSuspenseInstance(suspenseInstance) {
            retryIfBlockedOn(suspenseInstance);
          }
          function shouldDeleteUnhydratedTailInstances(parentType) {
            return parentType !== "head" && parentType !== "body";
          }
          function didNotMatchHydratedContainerTextInstance(parentContainer, textInstance, text, isConcurrentMode) {
            var shouldWarnDev = true;
            checkForUnmatchedText(textInstance.nodeValue, text, isConcurrentMode, shouldWarnDev);
          }
          function didNotMatchHydratedTextInstance(parentType, parentProps, parentInstance, textInstance, text, isConcurrentMode) {
            if (parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) {
              var shouldWarnDev = true;
              checkForUnmatchedText(textInstance.nodeValue, text, isConcurrentMode, shouldWarnDev);
            }
          }
          function didNotHydrateInstanceWithinContainer(parentContainer, instance) {
            {
              if (instance.nodeType === ELEMENT_NODE) {
                warnForDeletedHydratableElement(parentContainer, instance);
              } else if (instance.nodeType === COMMENT_NODE) ;
              else {
                warnForDeletedHydratableText(parentContainer, instance);
              }
            }
          }
          function didNotHydrateInstanceWithinSuspenseInstance(parentInstance, instance) {
            {
              var parentNode = parentInstance.parentNode;
              if (parentNode !== null) {
                if (instance.nodeType === ELEMENT_NODE) {
                  warnForDeletedHydratableElement(parentNode, instance);
                } else if (instance.nodeType === COMMENT_NODE) ;
                else {
                  warnForDeletedHydratableText(parentNode, instance);
                }
              }
            }
          }
          function didNotHydrateInstance(parentType, parentProps, parentInstance, instance, isConcurrentMode) {
            {
              if (isConcurrentMode || parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) {
                if (instance.nodeType === ELEMENT_NODE) {
                  warnForDeletedHydratableElement(parentInstance, instance);
                } else if (instance.nodeType === COMMENT_NODE) ;
                else {
                  warnForDeletedHydratableText(parentInstance, instance);
                }
              }
            }
          }
          function didNotFindHydratableInstanceWithinContainer(parentContainer, type, props) {
            {
              warnForInsertedHydratedElement(parentContainer, type);
            }
          }
          function didNotFindHydratableTextInstanceWithinContainer(parentContainer, text) {
            {
              warnForInsertedHydratedText(parentContainer, text);
            }
          }
          function didNotFindHydratableInstanceWithinSuspenseInstance(parentInstance, type, props) {
            {
              var parentNode = parentInstance.parentNode;
              if (parentNode !== null) warnForInsertedHydratedElement(parentNode, type);
            }
          }
          function didNotFindHydratableTextInstanceWithinSuspenseInstance(parentInstance, text) {
            {
              var parentNode = parentInstance.parentNode;
              if (parentNode !== null) warnForInsertedHydratedText(parentNode, text);
            }
          }
          function didNotFindHydratableInstance(parentType, parentProps, parentInstance, type, props, isConcurrentMode) {
            {
              if (isConcurrentMode || parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) {
                warnForInsertedHydratedElement(parentInstance, type);
              }
            }
          }
          function didNotFindHydratableTextInstance(parentType, parentProps, parentInstance, text, isConcurrentMode) {
            {
              if (isConcurrentMode || parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) {
                warnForInsertedHydratedText(parentInstance, text);
              }
            }
          }
          function errorHydratingContainer(parentContainer) {
            {
              error("An error occurred during hydration. The server HTML was replaced with client content in <%s>.", parentContainer.nodeName.toLowerCase());
            }
          }
          function preparePortalMount(portalInstance) {
            listenToAllSupportedEvents(portalInstance);
          }
          var randomKey = Math.random().toString(36).slice(2);
          var internalInstanceKey = "__reactFiber$" + randomKey;
          var internalPropsKey = "__reactProps$" + randomKey;
          var internalContainerInstanceKey = "__reactContainer$" + randomKey;
          var internalEventHandlersKey = "__reactEvents$" + randomKey;
          var internalEventHandlerListenersKey = "__reactListeners$" + randomKey;
          var internalEventHandlesSetKey = "__reactHandles$" + randomKey;
          function detachDeletedInstance(node) {
            delete node[internalInstanceKey];
            delete node[internalPropsKey];
            delete node[internalEventHandlersKey];
            delete node[internalEventHandlerListenersKey];
            delete node[internalEventHandlesSetKey];
          }
          function precacheFiberNode(hostInst, node) {
            node[internalInstanceKey] = hostInst;
          }
          function markContainerAsRoot(hostRoot, node) {
            node[internalContainerInstanceKey] = hostRoot;
          }
          function unmarkContainerAsRoot(node) {
            node[internalContainerInstanceKey] = null;
          }
          function isContainerMarkedAsRoot(node) {
            return !!node[internalContainerInstanceKey];
          }
          function getClosestInstanceFromNode(targetNode) {
            var targetInst = targetNode[internalInstanceKey];
            if (targetInst) {
              return targetInst;
            }
            var parentNode = targetNode.parentNode;
            while (parentNode) {
              targetInst = parentNode[internalContainerInstanceKey] || parentNode[internalInstanceKey];
              if (targetInst) {
                var alternate = targetInst.alternate;
                if (targetInst.child !== null || alternate !== null && alternate.child !== null) {
                  var suspenseInstance = getParentSuspenseInstance(targetNode);
                  while (suspenseInstance !== null) {
                    var targetSuspenseInst = suspenseInstance[internalInstanceKey];
                    if (targetSuspenseInst) {
                      return targetSuspenseInst;
                    }
                    suspenseInstance = getParentSuspenseInstance(suspenseInstance);
                  }
                }
                return targetInst;
              }
              targetNode = parentNode;
              parentNode = targetNode.parentNode;
            }
            return null;
          }
          function getInstanceFromNode(node) {
            var inst = node[internalInstanceKey] || node[internalContainerInstanceKey];
            if (inst) {
              if (inst.tag === HostComponent || inst.tag === HostText || inst.tag === SuspenseComponent || inst.tag === HostRoot) {
                return inst;
              } else {
                return null;
              }
            }
            return null;
          }
          function getNodeFromInstance(inst) {
            if (inst.tag === HostComponent || inst.tag === HostText) {
              return inst.stateNode;
            }
            throw new Error("getNodeFromInstance: Invalid argument.");
          }
          function getFiberCurrentPropsFromNode(node) {
            return node[internalPropsKey] || null;
          }
          function updateFiberProps(node, props) {
            node[internalPropsKey] = props;
          }
          function getEventListenerSet(node) {
            var elementListenerSet = node[internalEventHandlersKey];
            if (elementListenerSet === void 0) {
              elementListenerSet = node[internalEventHandlersKey] = /* @__PURE__ */ new Set();
            }
            return elementListenerSet;
          }
          var loggedTypeFailures = {};
          var ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
          function setCurrentlyValidatingElement(element) {
            {
              if (element) {
                var owner = element._owner;
                var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
                ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
              } else {
                ReactDebugCurrentFrame$1.setExtraStackFrame(null);
              }
            }
          }
          function checkPropTypes(typeSpecs, values, location, componentName, element) {
            {
              var has2 = Function.call.bind(hasOwnProperty);
              for (var typeSpecName in typeSpecs) {
                if (has2(typeSpecs, typeSpecName)) {
                  var error$1 = void 0;
                  try {
                    if (typeof typeSpecs[typeSpecName] !== "function") {
                      var err = Error((componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
                      err.name = "Invariant Violation";
                      throw err;
                    }
                    error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
                  } catch (ex) {
                    error$1 = ex;
                  }
                  if (error$1 && !(error$1 instanceof Error)) {
                    setCurrentlyValidatingElement(element);
                    error("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", componentName || "React class", location, typeSpecName, typeof error$1);
                    setCurrentlyValidatingElement(null);
                  }
                  if (error$1 instanceof Error && !(error$1.message in loggedTypeFailures)) {
                    loggedTypeFailures[error$1.message] = true;
                    setCurrentlyValidatingElement(element);
                    error("Failed %s type: %s", location, error$1.message);
                    setCurrentlyValidatingElement(null);
                  }
                }
              }
            }
          }
          var valueStack = [];
          var fiberStack;
          {
            fiberStack = [];
          }
          var index = -1;
          function createCursor(defaultValue) {
            return {
              current: defaultValue
            };
          }
          function pop(cursor, fiber) {
            if (index < 0) {
              {
                error("Unexpected pop.");
              }
              return;
            }
            {
              if (fiber !== fiberStack[index]) {
                error("Unexpected Fiber popped.");
              }
            }
            cursor.current = valueStack[index];
            valueStack[index] = null;
            {
              fiberStack[index] = null;
            }
            index--;
          }
          function push(cursor, value, fiber) {
            index++;
            valueStack[index] = cursor.current;
            {
              fiberStack[index] = fiber;
            }
            cursor.current = value;
          }
          var warnedAboutMissingGetChildContext;
          {
            warnedAboutMissingGetChildContext = {};
          }
          var emptyContextObject = {};
          {
            Object.freeze(emptyContextObject);
          }
          var contextStackCursor = createCursor(emptyContextObject);
          var didPerformWorkStackCursor = createCursor(false);
          var previousContext = emptyContextObject;
          function getUnmaskedContext(workInProgress2, Component, didPushOwnContextIfProvider) {
            {
              if (didPushOwnContextIfProvider && isContextProvider(Component)) {
                return previousContext;
              }
              return contextStackCursor.current;
            }
          }
          function cacheContext(workInProgress2, unmaskedContext, maskedContext) {
            {
              var instance = workInProgress2.stateNode;
              instance.__reactInternalMemoizedUnmaskedChildContext = unmaskedContext;
              instance.__reactInternalMemoizedMaskedChildContext = maskedContext;
            }
          }
          function getMaskedContext(workInProgress2, unmaskedContext) {
            {
              var type = workInProgress2.type;
              var contextTypes = type.contextTypes;
              if (!contextTypes) {
                return emptyContextObject;
              }
              var instance = workInProgress2.stateNode;
              if (instance && instance.__reactInternalMemoizedUnmaskedChildContext === unmaskedContext) {
                return instance.__reactInternalMemoizedMaskedChildContext;
              }
              var context = {};
              for (var key in contextTypes) {
                context[key] = unmaskedContext[key];
              }
              {
                var name = getComponentNameFromFiber(workInProgress2) || "Unknown";
                checkPropTypes(contextTypes, context, "context", name);
              }
              if (instance) {
                cacheContext(workInProgress2, unmaskedContext, context);
              }
              return context;
            }
          }
          function hasContextChanged() {
            {
              return didPerformWorkStackCursor.current;
            }
          }
          function isContextProvider(type) {
            {
              var childContextTypes = type.childContextTypes;
              return childContextTypes !== null && childContextTypes !== void 0;
            }
          }
          function popContext(fiber) {
            {
              pop(didPerformWorkStackCursor, fiber);
              pop(contextStackCursor, fiber);
            }
          }
          function popTopLevelContextObject(fiber) {
            {
              pop(didPerformWorkStackCursor, fiber);
              pop(contextStackCursor, fiber);
            }
          }
          function pushTopLevelContextObject(fiber, context, didChange) {
            {
              if (contextStackCursor.current !== emptyContextObject) {
                throw new Error("Unexpected context found on stack. This error is likely caused by a bug in React. Please file an issue.");
              }
              push(contextStackCursor, context, fiber);
              push(didPerformWorkStackCursor, didChange, fiber);
            }
          }
          function processChildContext(fiber, type, parentContext) {
            {
              var instance = fiber.stateNode;
              var childContextTypes = type.childContextTypes;
              if (typeof instance.getChildContext !== "function") {
                {
                  var componentName = getComponentNameFromFiber(fiber) || "Unknown";
                  if (!warnedAboutMissingGetChildContext[componentName]) {
                    warnedAboutMissingGetChildContext[componentName] = true;
                    error("%s.childContextTypes is specified but there is no getChildContext() method on the instance. You can either define getChildContext() on %s or remove childContextTypes from it.", componentName, componentName);
                  }
                }
                return parentContext;
              }
              var childContext = instance.getChildContext();
              for (var contextKey in childContext) {
                if (!(contextKey in childContextTypes)) {
                  throw new Error((getComponentNameFromFiber(fiber) || "Unknown") + '.getChildContext(): key "' + contextKey + '" is not defined in childContextTypes.');
                }
              }
              {
                var name = getComponentNameFromFiber(fiber) || "Unknown";
                checkPropTypes(childContextTypes, childContext, "child context", name);
              }
              return assign({}, parentContext, childContext);
            }
          }
          function pushContextProvider(workInProgress2) {
            {
              var instance = workInProgress2.stateNode;
              var memoizedMergedChildContext = instance && instance.__reactInternalMemoizedMergedChildContext || emptyContextObject;
              previousContext = contextStackCursor.current;
              push(contextStackCursor, memoizedMergedChildContext, workInProgress2);
              push(didPerformWorkStackCursor, didPerformWorkStackCursor.current, workInProgress2);
              return true;
            }
          }
          function invalidateContextProvider(workInProgress2, type, didChange) {
            {
              var instance = workInProgress2.stateNode;
              if (!instance) {
                throw new Error("Expected to have an instance by this point. This error is likely caused by a bug in React. Please file an issue.");
              }
              if (didChange) {
                var mergedContext = processChildContext(workInProgress2, type, previousContext);
                instance.__reactInternalMemoizedMergedChildContext = mergedContext;
                pop(didPerformWorkStackCursor, workInProgress2);
                pop(contextStackCursor, workInProgress2);
                push(contextStackCursor, mergedContext, workInProgress2);
                push(didPerformWorkStackCursor, didChange, workInProgress2);
              } else {
                pop(didPerformWorkStackCursor, workInProgress2);
                push(didPerformWorkStackCursor, didChange, workInProgress2);
              }
            }
          }
          function findCurrentUnmaskedContext(fiber) {
            {
              if (!isFiberMounted(fiber) || fiber.tag !== ClassComponent) {
                throw new Error("Expected subtree parent to be a mounted class component. This error is likely caused by a bug in React. Please file an issue.");
              }
              var node = fiber;
              do {
                switch (node.tag) {
                  case HostRoot:
                    return node.stateNode.context;
                  case ClassComponent: {
                    var Component = node.type;
                    if (isContextProvider(Component)) {
                      return node.stateNode.__reactInternalMemoizedMergedChildContext;
                    }
                    break;
                  }
                }
                node = node.return;
              } while (node !== null);
              throw new Error("Found unexpected detached subtree parent. This error is likely caused by a bug in React. Please file an issue.");
            }
          }
          var LegacyRoot = 0;
          var ConcurrentRoot = 1;
          var syncQueue = null;
          var includesLegacySyncCallbacks = false;
          var isFlushingSyncQueue = false;
          function scheduleSyncCallback(callback) {
            if (syncQueue === null) {
              syncQueue = [callback];
            } else {
              syncQueue.push(callback);
            }
          }
          function scheduleLegacySyncCallback(callback) {
            includesLegacySyncCallbacks = true;
            scheduleSyncCallback(callback);
          }
          function flushSyncCallbacksOnlyInLegacyMode() {
            if (includesLegacySyncCallbacks) {
              flushSyncCallbacks();
            }
          }
          function flushSyncCallbacks() {
            if (!isFlushingSyncQueue && syncQueue !== null) {
              isFlushingSyncQueue = true;
              var i = 0;
              var previousUpdatePriority = getCurrentUpdatePriority();
              try {
                var isSync = true;
                var queue = syncQueue;
                setCurrentUpdatePriority(DiscreteEventPriority);
                for (; i < queue.length; i++) {
                  var callback = queue[i];
                  do {
                    callback = callback(isSync);
                  } while (callback !== null);
                }
                syncQueue = null;
                includesLegacySyncCallbacks = false;
              } catch (error2) {
                if (syncQueue !== null) {
                  syncQueue = syncQueue.slice(i + 1);
                }
                scheduleCallback(ImmediatePriority, flushSyncCallbacks);
                throw error2;
              } finally {
                setCurrentUpdatePriority(previousUpdatePriority);
                isFlushingSyncQueue = false;
              }
            }
            return null;
          }
          var forkStack = [];
          var forkStackIndex = 0;
          var treeForkProvider = null;
          var treeForkCount = 0;
          var idStack = [];
          var idStackIndex = 0;
          var treeContextProvider = null;
          var treeContextId = 1;
          var treeContextOverflow = "";
          function isForkedChild(workInProgress2) {
            warnIfNotHydrating();
            return (workInProgress2.flags & Forked) !== NoFlags;
          }
          function getForksAtLevel(workInProgress2) {
            warnIfNotHydrating();
            return treeForkCount;
          }
          function getTreeId() {
            var overflow = treeContextOverflow;
            var idWithLeadingBit = treeContextId;
            var id = idWithLeadingBit & ~getLeadingBit(idWithLeadingBit);
            return id.toString(32) + overflow;
          }
          function pushTreeFork(workInProgress2, totalChildren) {
            warnIfNotHydrating();
            forkStack[forkStackIndex++] = treeForkCount;
            forkStack[forkStackIndex++] = treeForkProvider;
            treeForkProvider = workInProgress2;
            treeForkCount = totalChildren;
          }
          function pushTreeId(workInProgress2, totalChildren, index2) {
            warnIfNotHydrating();
            idStack[idStackIndex++] = treeContextId;
            idStack[idStackIndex++] = treeContextOverflow;
            idStack[idStackIndex++] = treeContextProvider;
            treeContextProvider = workInProgress2;
            var baseIdWithLeadingBit = treeContextId;
            var baseOverflow = treeContextOverflow;
            var baseLength = getBitLength(baseIdWithLeadingBit) - 1;
            var baseId = baseIdWithLeadingBit & ~(1 << baseLength);
            var slot = index2 + 1;
            var length = getBitLength(totalChildren) + baseLength;
            if (length > 30) {
              var numberOfOverflowBits = baseLength - baseLength % 5;
              var newOverflowBits = (1 << numberOfOverflowBits) - 1;
              var newOverflow = (baseId & newOverflowBits).toString(32);
              var restOfBaseId = baseId >> numberOfOverflowBits;
              var restOfBaseLength = baseLength - numberOfOverflowBits;
              var restOfLength = getBitLength(totalChildren) + restOfBaseLength;
              var restOfNewBits = slot << restOfBaseLength;
              var id = restOfNewBits | restOfBaseId;
              var overflow = newOverflow + baseOverflow;
              treeContextId = 1 << restOfLength | id;
              treeContextOverflow = overflow;
            } else {
              var newBits = slot << baseLength;
              var _id = newBits | baseId;
              var _overflow = baseOverflow;
              treeContextId = 1 << length | _id;
              treeContextOverflow = _overflow;
            }
          }
          function pushMaterializedTreeId(workInProgress2) {
            warnIfNotHydrating();
            var returnFiber = workInProgress2.return;
            if (returnFiber !== null) {
              var numberOfForks = 1;
              var slotIndex = 0;
              pushTreeFork(workInProgress2, numberOfForks);
              pushTreeId(workInProgress2, numberOfForks, slotIndex);
            }
          }
          function getBitLength(number) {
            return 32 - clz32(number);
          }
          function getLeadingBit(id) {
            return 1 << getBitLength(id) - 1;
          }
          function popTreeContext(workInProgress2) {
            while (workInProgress2 === treeForkProvider) {
              treeForkProvider = forkStack[--forkStackIndex];
              forkStack[forkStackIndex] = null;
              treeForkCount = forkStack[--forkStackIndex];
              forkStack[forkStackIndex] = null;
            }
            while (workInProgress2 === treeContextProvider) {
              treeContextProvider = idStack[--idStackIndex];
              idStack[idStackIndex] = null;
              treeContextOverflow = idStack[--idStackIndex];
              idStack[idStackIndex] = null;
              treeContextId = idStack[--idStackIndex];
              idStack[idStackIndex] = null;
            }
          }
          function getSuspendedTreeContext() {
            warnIfNotHydrating();
            if (treeContextProvider !== null) {
              return {
                id: treeContextId,
                overflow: treeContextOverflow
              };
            } else {
              return null;
            }
          }
          function restoreSuspendedTreeContext(workInProgress2, suspendedContext) {
            warnIfNotHydrating();
            idStack[idStackIndex++] = treeContextId;
            idStack[idStackIndex++] = treeContextOverflow;
            idStack[idStackIndex++] = treeContextProvider;
            treeContextId = suspendedContext.id;
            treeContextOverflow = suspendedContext.overflow;
            treeContextProvider = workInProgress2;
          }
          function warnIfNotHydrating() {
            {
              if (!getIsHydrating()) {
                error("Expected to be hydrating. This is a bug in React. Please file an issue.");
              }
            }
          }
          var hydrationParentFiber = null;
          var nextHydratableInstance = null;
          var isHydrating = false;
          var didSuspendOrErrorDEV = false;
          var hydrationErrors = null;
          function warnIfHydrating() {
            {
              if (isHydrating) {
                error("We should not be hydrating here. This is a bug in React. Please file a bug.");
              }
            }
          }
          function markDidThrowWhileHydratingDEV() {
            {
              didSuspendOrErrorDEV = true;
            }
          }
          function didSuspendOrErrorWhileHydratingDEV() {
            {
              return didSuspendOrErrorDEV;
            }
          }
          function enterHydrationState(fiber) {
            var parentInstance = fiber.stateNode.containerInfo;
            nextHydratableInstance = getFirstHydratableChildWithinContainer(parentInstance);
            hydrationParentFiber = fiber;
            isHydrating = true;
            hydrationErrors = null;
            didSuspendOrErrorDEV = false;
            return true;
          }
          function reenterHydrationStateFromDehydratedSuspenseInstance(fiber, suspenseInstance, treeContext) {
            nextHydratableInstance = getFirstHydratableChildWithinSuspenseInstance(suspenseInstance);
            hydrationParentFiber = fiber;
            isHydrating = true;
            hydrationErrors = null;
            didSuspendOrErrorDEV = false;
            if (treeContext !== null) {
              restoreSuspendedTreeContext(fiber, treeContext);
            }
            return true;
          }
          function warnUnhydratedInstance(returnFiber, instance) {
            {
              switch (returnFiber.tag) {
                case HostRoot: {
                  didNotHydrateInstanceWithinContainer(returnFiber.stateNode.containerInfo, instance);
                  break;
                }
                case HostComponent: {
                  var isConcurrentMode = (returnFiber.mode & ConcurrentMode) !== NoMode;
                  didNotHydrateInstance(
                    returnFiber.type,
                    returnFiber.memoizedProps,
                    returnFiber.stateNode,
                    instance,
                    // TODO: Delete this argument when we remove the legacy root API.
                    isConcurrentMode
                  );
                  break;
                }
                case SuspenseComponent: {
                  var suspenseState = returnFiber.memoizedState;
                  if (suspenseState.dehydrated !== null) didNotHydrateInstanceWithinSuspenseInstance(suspenseState.dehydrated, instance);
                  break;
                }
              }
            }
          }
          function deleteHydratableInstance(returnFiber, instance) {
            warnUnhydratedInstance(returnFiber, instance);
            var childToDelete = createFiberFromHostInstanceForDeletion();
            childToDelete.stateNode = instance;
            childToDelete.return = returnFiber;
            var deletions = returnFiber.deletions;
            if (deletions === null) {
              returnFiber.deletions = [childToDelete];
              returnFiber.flags |= ChildDeletion;
            } else {
              deletions.push(childToDelete);
            }
          }
          function warnNonhydratedInstance(returnFiber, fiber) {
            {
              if (didSuspendOrErrorDEV) {
                return;
              }
              switch (returnFiber.tag) {
                case HostRoot: {
                  var parentContainer = returnFiber.stateNode.containerInfo;
                  switch (fiber.tag) {
                    case HostComponent:
                      var type = fiber.type;
                      var props = fiber.pendingProps;
                      didNotFindHydratableInstanceWithinContainer(parentContainer, type);
                      break;
                    case HostText:
                      var text = fiber.pendingProps;
                      didNotFindHydratableTextInstanceWithinContainer(parentContainer, text);
                      break;
                  }
                  break;
                }
                case HostComponent: {
                  var parentType = returnFiber.type;
                  var parentProps = returnFiber.memoizedProps;
                  var parentInstance = returnFiber.stateNode;
                  switch (fiber.tag) {
                    case HostComponent: {
                      var _type = fiber.type;
                      var _props = fiber.pendingProps;
                      var isConcurrentMode = (returnFiber.mode & ConcurrentMode) !== NoMode;
                      didNotFindHydratableInstance(
                        parentType,
                        parentProps,
                        parentInstance,
                        _type,
                        _props,
                        // TODO: Delete this argument when we remove the legacy root API.
                        isConcurrentMode
                      );
                      break;
                    }
                    case HostText: {
                      var _text = fiber.pendingProps;
                      var _isConcurrentMode = (returnFiber.mode & ConcurrentMode) !== NoMode;
                      didNotFindHydratableTextInstance(
                        parentType,
                        parentProps,
                        parentInstance,
                        _text,
                        // TODO: Delete this argument when we remove the legacy root API.
                        _isConcurrentMode
                      );
                      break;
                    }
                  }
                  break;
                }
                case SuspenseComponent: {
                  var suspenseState = returnFiber.memoizedState;
                  var _parentInstance = suspenseState.dehydrated;
                  if (_parentInstance !== null) switch (fiber.tag) {
                    case HostComponent:
                      var _type2 = fiber.type;
                      var _props2 = fiber.pendingProps;
                      didNotFindHydratableInstanceWithinSuspenseInstance(_parentInstance, _type2);
                      break;
                    case HostText:
                      var _text2 = fiber.pendingProps;
                      didNotFindHydratableTextInstanceWithinSuspenseInstance(_parentInstance, _text2);
                      break;
                  }
                  break;
                }
                default:
                  return;
              }
            }
          }
          function insertNonHydratedInstance(returnFiber, fiber) {
            fiber.flags = fiber.flags & ~Hydrating | Placement;
            warnNonhydratedInstance(returnFiber, fiber);
          }
          function tryHydrate(fiber, nextInstance) {
            switch (fiber.tag) {
              case HostComponent: {
                var type = fiber.type;
                var props = fiber.pendingProps;
                var instance = canHydrateInstance(nextInstance, type);
                if (instance !== null) {
                  fiber.stateNode = instance;
                  hydrationParentFiber = fiber;
                  nextHydratableInstance = getFirstHydratableChild(instance);
                  return true;
                }
                return false;
              }
              case HostText: {
                var text = fiber.pendingProps;
                var textInstance = canHydrateTextInstance(nextInstance, text);
                if (textInstance !== null) {
                  fiber.stateNode = textInstance;
                  hydrationParentFiber = fiber;
                  nextHydratableInstance = null;
                  return true;
                }
                return false;
              }
              case SuspenseComponent: {
                var suspenseInstance = canHydrateSuspenseInstance(nextInstance);
                if (suspenseInstance !== null) {
                  var suspenseState = {
                    dehydrated: suspenseInstance,
                    treeContext: getSuspendedTreeContext(),
                    retryLane: OffscreenLane
                  };
                  fiber.memoizedState = suspenseState;
                  var dehydratedFragment = createFiberFromDehydratedFragment(suspenseInstance);
                  dehydratedFragment.return = fiber;
                  fiber.child = dehydratedFragment;
                  hydrationParentFiber = fiber;
                  nextHydratableInstance = null;
                  return true;
                }
                return false;
              }
              default:
                return false;
            }
          }
          function shouldClientRenderOnMismatch(fiber) {
            return (fiber.mode & ConcurrentMode) !== NoMode && (fiber.flags & DidCapture) === NoFlags;
          }
          function throwOnHydrationMismatch(fiber) {
            throw new Error("Hydration failed because the initial UI does not match what was rendered on the server.");
          }
          function tryToClaimNextHydratableInstance(fiber) {
            if (!isHydrating) {
              return;
            }
            var nextInstance = nextHydratableInstance;
            if (!nextInstance) {
              if (shouldClientRenderOnMismatch(fiber)) {
                warnNonhydratedInstance(hydrationParentFiber, fiber);
                throwOnHydrationMismatch();
              }
              insertNonHydratedInstance(hydrationParentFiber, fiber);
              isHydrating = false;
              hydrationParentFiber = fiber;
              return;
            }
            var firstAttemptedInstance = nextInstance;
            if (!tryHydrate(fiber, nextInstance)) {
              if (shouldClientRenderOnMismatch(fiber)) {
                warnNonhydratedInstance(hydrationParentFiber, fiber);
                throwOnHydrationMismatch();
              }
              nextInstance = getNextHydratableSibling(firstAttemptedInstance);
              var prevHydrationParentFiber = hydrationParentFiber;
              if (!nextInstance || !tryHydrate(fiber, nextInstance)) {
                insertNonHydratedInstance(hydrationParentFiber, fiber);
                isHydrating = false;
                hydrationParentFiber = fiber;
                return;
              }
              deleteHydratableInstance(prevHydrationParentFiber, firstAttemptedInstance);
            }
          }
          function prepareToHydrateHostInstance(fiber, rootContainerInstance, hostContext) {
            var instance = fiber.stateNode;
            var shouldWarnIfMismatchDev = !didSuspendOrErrorDEV;
            var updatePayload = hydrateInstance(instance, fiber.type, fiber.memoizedProps, rootContainerInstance, hostContext, fiber, shouldWarnIfMismatchDev);
            fiber.updateQueue = updatePayload;
            if (updatePayload !== null) {
              return true;
            }
            return false;
          }
          function prepareToHydrateHostTextInstance(fiber) {
            var textInstance = fiber.stateNode;
            var textContent = fiber.memoizedProps;
            var shouldUpdate = hydrateTextInstance(textInstance, textContent, fiber);
            if (shouldUpdate) {
              var returnFiber = hydrationParentFiber;
              if (returnFiber !== null) {
                switch (returnFiber.tag) {
                  case HostRoot: {
                    var parentContainer = returnFiber.stateNode.containerInfo;
                    var isConcurrentMode = (returnFiber.mode & ConcurrentMode) !== NoMode;
                    didNotMatchHydratedContainerTextInstance(
                      parentContainer,
                      textInstance,
                      textContent,
                      // TODO: Delete this argument when we remove the legacy root API.
                      isConcurrentMode
                    );
                    break;
                  }
                  case HostComponent: {
                    var parentType = returnFiber.type;
                    var parentProps = returnFiber.memoizedProps;
                    var parentInstance = returnFiber.stateNode;
                    var _isConcurrentMode2 = (returnFiber.mode & ConcurrentMode) !== NoMode;
                    didNotMatchHydratedTextInstance(
                      parentType,
                      parentProps,
                      parentInstance,
                      textInstance,
                      textContent,
                      // TODO: Delete this argument when we remove the legacy root API.
                      _isConcurrentMode2
                    );
                    break;
                  }
                }
              }
            }
            return shouldUpdate;
          }
          function prepareToHydrateHostSuspenseInstance(fiber) {
            var suspenseState = fiber.memoizedState;
            var suspenseInstance = suspenseState !== null ? suspenseState.dehydrated : null;
            if (!suspenseInstance) {
              throw new Error("Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue.");
            }
            hydrateSuspenseInstance(suspenseInstance, fiber);
          }
          function skipPastDehydratedSuspenseInstance(fiber) {
            var suspenseState = fiber.memoizedState;
            var suspenseInstance = suspenseState !== null ? suspenseState.dehydrated : null;
            if (!suspenseInstance) {
              throw new Error("Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue.");
            }
            return getNextHydratableInstanceAfterSuspenseInstance(suspenseInstance);
          }
          function popToNextHostParent(fiber) {
            var parent = fiber.return;
            while (parent !== null && parent.tag !== HostComponent && parent.tag !== HostRoot && parent.tag !== SuspenseComponent) {
              parent = parent.return;
            }
            hydrationParentFiber = parent;
          }
          function popHydrationState(fiber) {
            if (fiber !== hydrationParentFiber) {
              return false;
            }
            if (!isHydrating) {
              popToNextHostParent(fiber);
              isHydrating = true;
              return false;
            }
            if (fiber.tag !== HostRoot && (fiber.tag !== HostComponent || shouldDeleteUnhydratedTailInstances(fiber.type) && !shouldSetTextContent(fiber.type, fiber.memoizedProps))) {
              var nextInstance = nextHydratableInstance;
              if (nextInstance) {
                if (shouldClientRenderOnMismatch(fiber)) {
                  warnIfUnhydratedTailNodes(fiber);
                  throwOnHydrationMismatch();
                } else {
                  while (nextInstance) {
                    deleteHydratableInstance(fiber, nextInstance);
                    nextInstance = getNextHydratableSibling(nextInstance);
                  }
                }
              }
            }
            popToNextHostParent(fiber);
            if (fiber.tag === SuspenseComponent) {
              nextHydratableInstance = skipPastDehydratedSuspenseInstance(fiber);
            } else {
              nextHydratableInstance = hydrationParentFiber ? getNextHydratableSibling(fiber.stateNode) : null;
            }
            return true;
          }
          function hasUnhydratedTailNodes() {
            return isHydrating && nextHydratableInstance !== null;
          }
          function warnIfUnhydratedTailNodes(fiber) {
            var nextInstance = nextHydratableInstance;
            while (nextInstance) {
              warnUnhydratedInstance(fiber, nextInstance);
              nextInstance = getNextHydratableSibling(nextInstance);
            }
          }
          function resetHydrationState() {
            hydrationParentFiber = null;
            nextHydratableInstance = null;
            isHydrating = false;
            didSuspendOrErrorDEV = false;
          }
          function upgradeHydrationErrorsToRecoverable() {
            if (hydrationErrors !== null) {
              queueRecoverableErrors(hydrationErrors);
              hydrationErrors = null;
            }
          }
          function getIsHydrating() {
            return isHydrating;
          }
          function queueHydrationError(error2) {
            if (hydrationErrors === null) {
              hydrationErrors = [error2];
            } else {
              hydrationErrors.push(error2);
            }
          }
          var ReactCurrentBatchConfig$1 = ReactSharedInternals.ReactCurrentBatchConfig;
          var NoTransition = null;
          function requestCurrentTransition() {
            return ReactCurrentBatchConfig$1.transition;
          }
          var ReactStrictModeWarnings = {
            recordUnsafeLifecycleWarnings: function(fiber, instance) {
            },
            flushPendingUnsafeLifecycleWarnings: function() {
            },
            recordLegacyContextWarning: function(fiber, instance) {
            },
            flushLegacyContextWarning: function() {
            },
            discardPendingWarnings: function() {
            }
          };
          {
            var findStrictRoot = function(fiber) {
              var maybeStrictRoot = null;
              var node = fiber;
              while (node !== null) {
                if (node.mode & StrictLegacyMode) {
                  maybeStrictRoot = node;
                }
                node = node.return;
              }
              return maybeStrictRoot;
            };
            var setToSortedString = function(set2) {
              var array = [];
              set2.forEach(function(value) {
                array.push(value);
              });
              return array.sort().join(", ");
            };
            var pendingComponentWillMountWarnings = [];
            var pendingUNSAFE_ComponentWillMountWarnings = [];
            var pendingComponentWillReceivePropsWarnings = [];
            var pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
            var pendingComponentWillUpdateWarnings = [];
            var pendingUNSAFE_ComponentWillUpdateWarnings = [];
            var didWarnAboutUnsafeLifecycles = /* @__PURE__ */ new Set();
            ReactStrictModeWarnings.recordUnsafeLifecycleWarnings = function(fiber, instance) {
              if (didWarnAboutUnsafeLifecycles.has(fiber.type)) {
                return;
              }
              if (typeof instance.componentWillMount === "function" && // Don't warn about react-lifecycles-compat polyfilled components.
              instance.componentWillMount.__suppressDeprecationWarning !== true) {
                pendingComponentWillMountWarnings.push(fiber);
              }
              if (fiber.mode & StrictLegacyMode && typeof instance.UNSAFE_componentWillMount === "function") {
                pendingUNSAFE_ComponentWillMountWarnings.push(fiber);
              }
              if (typeof instance.componentWillReceiveProps === "function" && instance.componentWillReceiveProps.__suppressDeprecationWarning !== true) {
                pendingComponentWillReceivePropsWarnings.push(fiber);
              }
              if (fiber.mode & StrictLegacyMode && typeof instance.UNSAFE_componentWillReceiveProps === "function") {
                pendingUNSAFE_ComponentWillReceivePropsWarnings.push(fiber);
              }
              if (typeof instance.componentWillUpdate === "function" && instance.componentWillUpdate.__suppressDeprecationWarning !== true) {
                pendingComponentWillUpdateWarnings.push(fiber);
              }
              if (fiber.mode & StrictLegacyMode && typeof instance.UNSAFE_componentWillUpdate === "function") {
                pendingUNSAFE_ComponentWillUpdateWarnings.push(fiber);
              }
            };
            ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings = function() {
              var componentWillMountUniqueNames = /* @__PURE__ */ new Set();
              if (pendingComponentWillMountWarnings.length > 0) {
                pendingComponentWillMountWarnings.forEach(function(fiber) {
                  componentWillMountUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
                  didWarnAboutUnsafeLifecycles.add(fiber.type);
                });
                pendingComponentWillMountWarnings = [];
              }
              var UNSAFE_componentWillMountUniqueNames = /* @__PURE__ */ new Set();
              if (pendingUNSAFE_ComponentWillMountWarnings.length > 0) {
                pendingUNSAFE_ComponentWillMountWarnings.forEach(function(fiber) {
                  UNSAFE_componentWillMountUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
                  didWarnAboutUnsafeLifecycles.add(fiber.type);
                });
                pendingUNSAFE_ComponentWillMountWarnings = [];
              }
              var componentWillReceivePropsUniqueNames = /* @__PURE__ */ new Set();
              if (pendingComponentWillReceivePropsWarnings.length > 0) {
                pendingComponentWillReceivePropsWarnings.forEach(function(fiber) {
                  componentWillReceivePropsUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
                  didWarnAboutUnsafeLifecycles.add(fiber.type);
                });
                pendingComponentWillReceivePropsWarnings = [];
              }
              var UNSAFE_componentWillReceivePropsUniqueNames = /* @__PURE__ */ new Set();
              if (pendingUNSAFE_ComponentWillReceivePropsWarnings.length > 0) {
                pendingUNSAFE_ComponentWillReceivePropsWarnings.forEach(function(fiber) {
                  UNSAFE_componentWillReceivePropsUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
                  didWarnAboutUnsafeLifecycles.add(fiber.type);
                });
                pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
              }
              var componentWillUpdateUniqueNames = /* @__PURE__ */ new Set();
              if (pendingComponentWillUpdateWarnings.length > 0) {
                pendingComponentWillUpdateWarnings.forEach(function(fiber) {
                  componentWillUpdateUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
                  didWarnAboutUnsafeLifecycles.add(fiber.type);
                });
                pendingComponentWillUpdateWarnings = [];
              }
              var UNSAFE_componentWillUpdateUniqueNames = /* @__PURE__ */ new Set();
              if (pendingUNSAFE_ComponentWillUpdateWarnings.length > 0) {
                pendingUNSAFE_ComponentWillUpdateWarnings.forEach(function(fiber) {
                  UNSAFE_componentWillUpdateUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
                  didWarnAboutUnsafeLifecycles.add(fiber.type);
                });
                pendingUNSAFE_ComponentWillUpdateWarnings = [];
              }
              if (UNSAFE_componentWillMountUniqueNames.size > 0) {
                var sortedNames = setToSortedString(UNSAFE_componentWillMountUniqueNames);
                error("Using UNSAFE_componentWillMount in strict mode is not recommended and may indicate bugs in your code. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move code with side effects to componentDidMount, and set initial state in the constructor.\n\nPlease update the following components: %s", sortedNames);
              }
              if (UNSAFE_componentWillReceivePropsUniqueNames.size > 0) {
                var _sortedNames = setToSortedString(UNSAFE_componentWillReceivePropsUniqueNames);
                error("Using UNSAFE_componentWillReceiveProps in strict mode is not recommended and may indicate bugs in your code. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n* If you're updating state whenever props change, refactor your code to use memoization techniques or move it to static getDerivedStateFromProps. Learn more at: https://reactjs.org/link/derived-state\n\nPlease update the following components: %s", _sortedNames);
              }
              if (UNSAFE_componentWillUpdateUniqueNames.size > 0) {
                var _sortedNames2 = setToSortedString(UNSAFE_componentWillUpdateUniqueNames);
                error("Using UNSAFE_componentWillUpdate in strict mode is not recommended and may indicate bugs in your code. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n\nPlease update the following components: %s", _sortedNames2);
              }
              if (componentWillMountUniqueNames.size > 0) {
                var _sortedNames3 = setToSortedString(componentWillMountUniqueNames);
                warn("componentWillMount has been renamed, and is not recommended for use. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move code with side effects to componentDidMount, and set initial state in the constructor.\n* Rename componentWillMount to UNSAFE_componentWillMount to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run `npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n\nPlease update the following components: %s", _sortedNames3);
              }
              if (componentWillReceivePropsUniqueNames.size > 0) {
                var _sortedNames4 = setToSortedString(componentWillReceivePropsUniqueNames);
                warn("componentWillReceiveProps has been renamed, and is not recommended for use. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n* If you're updating state whenever props change, refactor your code to use memoization techniques or move it to static getDerivedStateFromProps. Learn more at: https://reactjs.org/link/derived-state\n* Rename componentWillReceiveProps to UNSAFE_componentWillReceiveProps to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run `npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n\nPlease update the following components: %s", _sortedNames4);
              }
              if (componentWillUpdateUniqueNames.size > 0) {
                var _sortedNames5 = setToSortedString(componentWillUpdateUniqueNames);
                warn("componentWillUpdate has been renamed, and is not recommended for use. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n* Rename componentWillUpdate to UNSAFE_componentWillUpdate to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run `npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n\nPlease update the following components: %s", _sortedNames5);
              }
            };
            var pendingLegacyContextWarning = /* @__PURE__ */ new Map();
            var didWarnAboutLegacyContext = /* @__PURE__ */ new Set();
            ReactStrictModeWarnings.recordLegacyContextWarning = function(fiber, instance) {
              var strictRoot = findStrictRoot(fiber);
              if (strictRoot === null) {
                error("Expected to find a StrictMode component in a strict mode tree. This error is likely caused by a bug in React. Please file an issue.");
                return;
              }
              if (didWarnAboutLegacyContext.has(fiber.type)) {
                return;
              }
              var warningsForRoot = pendingLegacyContextWarning.get(strictRoot);
              if (fiber.type.contextTypes != null || fiber.type.childContextTypes != null || instance !== null && typeof instance.getChildContext === "function") {
                if (warningsForRoot === void 0) {
                  warningsForRoot = [];
                  pendingLegacyContextWarning.set(strictRoot, warningsForRoot);
                }
                warningsForRoot.push(fiber);
              }
            };
            ReactStrictModeWarnings.flushLegacyContextWarning = function() {
              pendingLegacyContextWarning.forEach(function(fiberArray, strictRoot) {
                if (fiberArray.length === 0) {
                  return;
                }
                var firstFiber = fiberArray[0];
                var uniqueNames = /* @__PURE__ */ new Set();
                fiberArray.forEach(function(fiber) {
                  uniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
                  didWarnAboutLegacyContext.add(fiber.type);
                });
                var sortedNames = setToSortedString(uniqueNames);
                try {
                  setCurrentFiber(firstFiber);
                  error("Legacy context API has been detected within a strict-mode tree.\n\nThe old API will be supported in all 16.x releases, but applications using it should migrate to the new version.\n\nPlease update the following components: %s\n\nLearn more about this warning here: https://reactjs.org/link/legacy-context", sortedNames);
                } finally {
                  resetCurrentFiber();
                }
              });
            };
            ReactStrictModeWarnings.discardPendingWarnings = function() {
              pendingComponentWillMountWarnings = [];
              pendingUNSAFE_ComponentWillMountWarnings = [];
              pendingComponentWillReceivePropsWarnings = [];
              pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
              pendingComponentWillUpdateWarnings = [];
              pendingUNSAFE_ComponentWillUpdateWarnings = [];
              pendingLegacyContextWarning = /* @__PURE__ */ new Map();
            };
          }
          var didWarnAboutMaps;
          var didWarnAboutGenerators;
          var didWarnAboutStringRefs;
          var ownerHasKeyUseWarning;
          var ownerHasFunctionTypeWarning;
          var warnForMissingKey = function(child, returnFiber) {
          };
          {
            didWarnAboutMaps = false;
            didWarnAboutGenerators = false;
            didWarnAboutStringRefs = {};
            ownerHasKeyUseWarning = {};
            ownerHasFunctionTypeWarning = {};
            warnForMissingKey = function(child, returnFiber) {
              if (child === null || typeof child !== "object") {
                return;
              }
              if (!child._store || child._store.validated || child.key != null) {
                return;
              }
              if (typeof child._store !== "object") {
                throw new Error("React Component in warnForMissingKey should have a _store. This error is likely caused by a bug in React. Please file an issue.");
              }
              child._store.validated = true;
              var componentName = getComponentNameFromFiber(returnFiber) || "Component";
              if (ownerHasKeyUseWarning[componentName]) {
                return;
              }
              ownerHasKeyUseWarning[componentName] = true;
              error('Each child in a list should have a unique "key" prop. See https://reactjs.org/link/warning-keys for more information.');
            };
          }
          function isReactClass(type) {
            return type.prototype && type.prototype.isReactComponent;
          }
          function coerceRef(returnFiber, current2, element) {
            var mixedRef = element.ref;
            if (mixedRef !== null && typeof mixedRef !== "function" && typeof mixedRef !== "object") {
              {
                if ((returnFiber.mode & StrictLegacyMode || warnAboutStringRefs) && // We warn in ReactElement.js if owner and self are equal for string refs
                // because these cannot be automatically converted to an arrow function
                // using a codemod. Therefore, we don't have to warn about string refs again.
                !(element._owner && element._self && element._owner.stateNode !== element._self) && // Will already throw with "Function components cannot have string refs"
                !(element._owner && element._owner.tag !== ClassComponent) && // Will already warn with "Function components cannot be given refs"
                !(typeof element.type === "function" && !isReactClass(element.type)) && // Will already throw with "Element ref was specified as a string (someStringRef) but no owner was set"
                element._owner) {
                  var componentName = getComponentNameFromFiber(returnFiber) || "Component";
                  if (!didWarnAboutStringRefs[componentName]) {
                    {
                      error('Component "%s" contains the string ref "%s". Support for string refs will be removed in a future major release. We recommend using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref', componentName, mixedRef);
                    }
                    didWarnAboutStringRefs[componentName] = true;
                  }
                }
              }
              if (element._owner) {
                var owner = element._owner;
                var inst;
                if (owner) {
                  var ownerFiber = owner;
                  if (ownerFiber.tag !== ClassComponent) {
                    throw new Error("Function components cannot have string refs. We recommend using useRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref");
                  }
                  inst = ownerFiber.stateNode;
                }
                if (!inst) {
                  throw new Error("Missing owner for string ref " + mixedRef + ". This error is likely caused by a bug in React. Please file an issue.");
                }
                var resolvedInst = inst;
                {
                  checkPropStringCoercion(mixedRef, "ref");
                }
                var stringRef = "" + mixedRef;
                if (current2 !== null && current2.ref !== null && typeof current2.ref === "function" && current2.ref._stringRef === stringRef) {
                  return current2.ref;
                }
                var ref = function(value) {
                  var refs = resolvedInst.refs;
                  if (value === null) {
                    delete refs[stringRef];
                  } else {
                    refs[stringRef] = value;
                  }
                };
                ref._stringRef = stringRef;
                return ref;
              } else {
                if (typeof mixedRef !== "string") {
                  throw new Error("Expected ref to be a function, a string, an object returned by React.createRef(), or null.");
                }
                if (!element._owner) {
                  throw new Error("Element ref was specified as a string (" + mixedRef + ") but no owner was set. This could happen for one of the following reasons:\n1. You may be adding a ref to a function component\n2. You may be adding a ref to a component that was not created inside a component's render method\n3. You have multiple copies of React loaded\nSee https://reactjs.org/link/refs-must-have-owner for more information.");
                }
              }
            }
            return mixedRef;
          }
          function throwOnInvalidObjectType(returnFiber, newChild) {
            var childString = Object.prototype.toString.call(newChild);
            throw new Error("Objects are not valid as a React child (found: " + (childString === "[object Object]" ? "object with keys {" + Object.keys(newChild).join(", ") + "}" : childString) + "). If you meant to render a collection of children, use an array instead.");
          }
          function warnOnFunctionType(returnFiber) {
            {
              var componentName = getComponentNameFromFiber(returnFiber) || "Component";
              if (ownerHasFunctionTypeWarning[componentName]) {
                return;
              }
              ownerHasFunctionTypeWarning[componentName] = true;
              error("Functions are not valid as a React child. This may happen if you return a Component instead of <Component /> from render. Or maybe you meant to call this function rather than return it.");
            }
          }
          function resolveLazy(lazyType) {
            var payload = lazyType._payload;
            var init = lazyType._init;
            return init(payload);
          }
          function ChildReconciler(shouldTrackSideEffects) {
            function deleteChild(returnFiber, childToDelete) {
              if (!shouldTrackSideEffects) {
                return;
              }
              var deletions = returnFiber.deletions;
              if (deletions === null) {
                returnFiber.deletions = [childToDelete];
                returnFiber.flags |= ChildDeletion;
              } else {
                deletions.push(childToDelete);
              }
            }
            function deleteRemainingChildren(returnFiber, currentFirstChild) {
              if (!shouldTrackSideEffects) {
                return null;
              }
              var childToDelete = currentFirstChild;
              while (childToDelete !== null) {
                deleteChild(returnFiber, childToDelete);
                childToDelete = childToDelete.sibling;
              }
              return null;
            }
            function mapRemainingChildren(returnFiber, currentFirstChild) {
              var existingChildren = /* @__PURE__ */ new Map();
              var existingChild = currentFirstChild;
              while (existingChild !== null) {
                if (existingChild.key !== null) {
                  existingChildren.set(existingChild.key, existingChild);
                } else {
                  existingChildren.set(existingChild.index, existingChild);
                }
                existingChild = existingChild.sibling;
              }
              return existingChildren;
            }
            function useFiber(fiber, pendingProps) {
              var clone = createWorkInProgress(fiber, pendingProps);
              clone.index = 0;
              clone.sibling = null;
              return clone;
            }
            function placeChild(newFiber, lastPlacedIndex, newIndex) {
              newFiber.index = newIndex;
              if (!shouldTrackSideEffects) {
                newFiber.flags |= Forked;
                return lastPlacedIndex;
              }
              var current2 = newFiber.alternate;
              if (current2 !== null) {
                var oldIndex = current2.index;
                if (oldIndex < lastPlacedIndex) {
                  newFiber.flags |= Placement;
                  return lastPlacedIndex;
                } else {
                  return oldIndex;
                }
              } else {
                newFiber.flags |= Placement;
                return lastPlacedIndex;
              }
            }
            function placeSingleChild(newFiber) {
              if (shouldTrackSideEffects && newFiber.alternate === null) {
                newFiber.flags |= Placement;
              }
              return newFiber;
            }
            function updateTextNode(returnFiber, current2, textContent, lanes) {
              if (current2 === null || current2.tag !== HostText) {
                var created = createFiberFromText(textContent, returnFiber.mode, lanes);
                created.return = returnFiber;
                return created;
              } else {
                var existing = useFiber(current2, textContent);
                existing.return = returnFiber;
                return existing;
              }
            }
            function updateElement(returnFiber, current2, element, lanes) {
              var elementType = element.type;
              if (elementType === REACT_FRAGMENT_TYPE) {
                return updateFragment2(returnFiber, current2, element.props.children, lanes, element.key);
              }
              if (current2 !== null) {
                if (current2.elementType === elementType || // Keep this check inline so it only runs on the false path:
                isCompatibleFamilyForHotReloading(current2, element) || // Lazy types should reconcile their resolved type.
                // We need to do this after the Hot Reloading check above,
                // because hot reloading has different semantics than prod because
                // it doesn't resuspend. So we can't let the call below suspend.
                typeof elementType === "object" && elementType !== null && elementType.$$typeof === REACT_LAZY_TYPE && resolveLazy(elementType) === current2.type) {
                  var existing = useFiber(current2, element.props);
                  existing.ref = coerceRef(returnFiber, current2, element);
                  existing.return = returnFiber;
                  {
                    existing._debugSource = element._source;
                    existing._debugOwner = element._owner;
                  }
                  return existing;
                }
              }
              var created = createFiberFromElement(element, returnFiber.mode, lanes);
              created.ref = coerceRef(returnFiber, current2, element);
              created.return = returnFiber;
              return created;
            }
            function updatePortal(returnFiber, current2, portal, lanes) {
              if (current2 === null || current2.tag !== HostPortal || current2.stateNode.containerInfo !== portal.containerInfo || current2.stateNode.implementation !== portal.implementation) {
                var created = createFiberFromPortal(portal, returnFiber.mode, lanes);
                created.return = returnFiber;
                return created;
              } else {
                var existing = useFiber(current2, portal.children || []);
                existing.return = returnFiber;
                return existing;
              }
            }
            function updateFragment2(returnFiber, current2, fragment, lanes, key) {
              if (current2 === null || current2.tag !== Fragment3) {
                var created = createFiberFromFragment(fragment, returnFiber.mode, lanes, key);
                created.return = returnFiber;
                return created;
              } else {
                var existing = useFiber(current2, fragment);
                existing.return = returnFiber;
                return existing;
              }
            }
            function createChild(returnFiber, newChild, lanes) {
              if (typeof newChild === "string" && newChild !== "" || typeof newChild === "number") {
                var created = createFiberFromText("" + newChild, returnFiber.mode, lanes);
                created.return = returnFiber;
                return created;
              }
              if (typeof newChild === "object" && newChild !== null) {
                switch (newChild.$$typeof) {
                  case REACT_ELEMENT_TYPE: {
                    var _created = createFiberFromElement(newChild, returnFiber.mode, lanes);
                    _created.ref = coerceRef(returnFiber, null, newChild);
                    _created.return = returnFiber;
                    return _created;
                  }
                  case REACT_PORTAL_TYPE: {
                    var _created2 = createFiberFromPortal(newChild, returnFiber.mode, lanes);
                    _created2.return = returnFiber;
                    return _created2;
                  }
                  case REACT_LAZY_TYPE: {
                    var payload = newChild._payload;
                    var init = newChild._init;
                    return createChild(returnFiber, init(payload), lanes);
                  }
                }
                if (isArray(newChild) || getIteratorFn(newChild)) {
                  var _created3 = createFiberFromFragment(newChild, returnFiber.mode, lanes, null);
                  _created3.return = returnFiber;
                  return _created3;
                }
                throwOnInvalidObjectType(returnFiber, newChild);
              }
              {
                if (typeof newChild === "function") {
                  warnOnFunctionType(returnFiber);
                }
              }
              return null;
            }
            function updateSlot(returnFiber, oldFiber, newChild, lanes) {
              var key = oldFiber !== null ? oldFiber.key : null;
              if (typeof newChild === "string" && newChild !== "" || typeof newChild === "number") {
                if (key !== null) {
                  return null;
                }
                return updateTextNode(returnFiber, oldFiber, "" + newChild, lanes);
              }
              if (typeof newChild === "object" && newChild !== null) {
                switch (newChild.$$typeof) {
                  case REACT_ELEMENT_TYPE: {
                    if (newChild.key === key) {
                      return updateElement(returnFiber, oldFiber, newChild, lanes);
                    } else {
                      return null;
                    }
                  }
                  case REACT_PORTAL_TYPE: {
                    if (newChild.key === key) {
                      return updatePortal(returnFiber, oldFiber, newChild, lanes);
                    } else {
                      return null;
                    }
                  }
                  case REACT_LAZY_TYPE: {
                    var payload = newChild._payload;
                    var init = newChild._init;
                    return updateSlot(returnFiber, oldFiber, init(payload), lanes);
                  }
                }
                if (isArray(newChild) || getIteratorFn(newChild)) {
                  if (key !== null) {
                    return null;
                  }
                  return updateFragment2(returnFiber, oldFiber, newChild, lanes, null);
                }
                throwOnInvalidObjectType(returnFiber, newChild);
              }
              {
                if (typeof newChild === "function") {
                  warnOnFunctionType(returnFiber);
                }
              }
              return null;
            }
            function updateFromMap(existingChildren, returnFiber, newIdx, newChild, lanes) {
              if (typeof newChild === "string" && newChild !== "" || typeof newChild === "number") {
                var matchedFiber = existingChildren.get(newIdx) || null;
                return updateTextNode(returnFiber, matchedFiber, "" + newChild, lanes);
              }
              if (typeof newChild === "object" && newChild !== null) {
                switch (newChild.$$typeof) {
                  case REACT_ELEMENT_TYPE: {
                    var _matchedFiber = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;
                    return updateElement(returnFiber, _matchedFiber, newChild, lanes);
                  }
                  case REACT_PORTAL_TYPE: {
                    var _matchedFiber2 = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;
                    return updatePortal(returnFiber, _matchedFiber2, newChild, lanes);
                  }
                  case REACT_LAZY_TYPE:
                    var payload = newChild._payload;
                    var init = newChild._init;
                    return updateFromMap(existingChildren, returnFiber, newIdx, init(payload), lanes);
                }
                if (isArray(newChild) || getIteratorFn(newChild)) {
                  var _matchedFiber3 = existingChildren.get(newIdx) || null;
                  return updateFragment2(returnFiber, _matchedFiber3, newChild, lanes, null);
                }
                throwOnInvalidObjectType(returnFiber, newChild);
              }
              {
                if (typeof newChild === "function") {
                  warnOnFunctionType(returnFiber);
                }
              }
              return null;
            }
            function warnOnInvalidKey(child, knownKeys, returnFiber) {
              {
                if (typeof child !== "object" || child === null) {
                  return knownKeys;
                }
                switch (child.$$typeof) {
                  case REACT_ELEMENT_TYPE:
                  case REACT_PORTAL_TYPE:
                    warnForMissingKey(child, returnFiber);
                    var key = child.key;
                    if (typeof key !== "string") {
                      break;
                    }
                    if (knownKeys === null) {
                      knownKeys = /* @__PURE__ */ new Set();
                      knownKeys.add(key);
                      break;
                    }
                    if (!knownKeys.has(key)) {
                      knownKeys.add(key);
                      break;
                    }
                    error("Encountered two children with the same key, `%s`. Keys should be unique so that components maintain their identity across updates. Non-unique keys may cause children to be duplicated and/or omitted \u2014 the behavior is unsupported and could change in a future version.", key);
                    break;
                  case REACT_LAZY_TYPE:
                    var payload = child._payload;
                    var init = child._init;
                    warnOnInvalidKey(init(payload), knownKeys, returnFiber);
                    break;
                }
              }
              return knownKeys;
            }
            function reconcileChildrenArray(returnFiber, currentFirstChild, newChildren, lanes) {
              {
                var knownKeys = null;
                for (var i = 0; i < newChildren.length; i++) {
                  var child = newChildren[i];
                  knownKeys = warnOnInvalidKey(child, knownKeys, returnFiber);
                }
              }
              var resultingFirstChild = null;
              var previousNewFiber = null;
              var oldFiber = currentFirstChild;
              var lastPlacedIndex = 0;
              var newIdx = 0;
              var nextOldFiber = null;
              for (; oldFiber !== null && newIdx < newChildren.length; newIdx++) {
                if (oldFiber.index > newIdx) {
                  nextOldFiber = oldFiber;
                  oldFiber = null;
                } else {
                  nextOldFiber = oldFiber.sibling;
                }
                var newFiber = updateSlot(returnFiber, oldFiber, newChildren[newIdx], lanes);
                if (newFiber === null) {
                  if (oldFiber === null) {
                    oldFiber = nextOldFiber;
                  }
                  break;
                }
                if (shouldTrackSideEffects) {
                  if (oldFiber && newFiber.alternate === null) {
                    deleteChild(returnFiber, oldFiber);
                  }
                }
                lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
                if (previousNewFiber === null) {
                  resultingFirstChild = newFiber;
                } else {
                  previousNewFiber.sibling = newFiber;
                }
                previousNewFiber = newFiber;
                oldFiber = nextOldFiber;
              }
              if (newIdx === newChildren.length) {
                deleteRemainingChildren(returnFiber, oldFiber);
                if (getIsHydrating()) {
                  var numberOfForks = newIdx;
                  pushTreeFork(returnFiber, numberOfForks);
                }
                return resultingFirstChild;
              }
              if (oldFiber === null) {
                for (; newIdx < newChildren.length; newIdx++) {
                  var _newFiber = createChild(returnFiber, newChildren[newIdx], lanes);
                  if (_newFiber === null) {
                    continue;
                  }
                  lastPlacedIndex = placeChild(_newFiber, lastPlacedIndex, newIdx);
                  if (previousNewFiber === null) {
                    resultingFirstChild = _newFiber;
                  } else {
                    previousNewFiber.sibling = _newFiber;
                  }
                  previousNewFiber = _newFiber;
                }
                if (getIsHydrating()) {
                  var _numberOfForks = newIdx;
                  pushTreeFork(returnFiber, _numberOfForks);
                }
                return resultingFirstChild;
              }
              var existingChildren = mapRemainingChildren(returnFiber, oldFiber);
              for (; newIdx < newChildren.length; newIdx++) {
                var _newFiber2 = updateFromMap(existingChildren, returnFiber, newIdx, newChildren[newIdx], lanes);
                if (_newFiber2 !== null) {
                  if (shouldTrackSideEffects) {
                    if (_newFiber2.alternate !== null) {
                      existingChildren.delete(_newFiber2.key === null ? newIdx : _newFiber2.key);
                    }
                  }
                  lastPlacedIndex = placeChild(_newFiber2, lastPlacedIndex, newIdx);
                  if (previousNewFiber === null) {
                    resultingFirstChild = _newFiber2;
                  } else {
                    previousNewFiber.sibling = _newFiber2;
                  }
                  previousNewFiber = _newFiber2;
                }
              }
              if (shouldTrackSideEffects) {
                existingChildren.forEach(function(child2) {
                  return deleteChild(returnFiber, child2);
                });
              }
              if (getIsHydrating()) {
                var _numberOfForks2 = newIdx;
                pushTreeFork(returnFiber, _numberOfForks2);
              }
              return resultingFirstChild;
            }
            function reconcileChildrenIterator(returnFiber, currentFirstChild, newChildrenIterable, lanes) {
              var iteratorFn = getIteratorFn(newChildrenIterable);
              if (typeof iteratorFn !== "function") {
                throw new Error("An object is not an iterable. This error is likely caused by a bug in React. Please file an issue.");
              }
              {
                if (typeof Symbol === "function" && // $FlowFixMe Flow doesn't know about toStringTag
                newChildrenIterable[Symbol.toStringTag] === "Generator") {
                  if (!didWarnAboutGenerators) {
                    error("Using Generators as children is unsupported and will likely yield unexpected results because enumerating a generator mutates it. You may convert it to an array with `Array.from()` or the `[...spread]` operator before rendering. Keep in mind you might need to polyfill these features for older browsers.");
                  }
                  didWarnAboutGenerators = true;
                }
                if (newChildrenIterable.entries === iteratorFn) {
                  if (!didWarnAboutMaps) {
                    error("Using Maps as children is not supported. Use an array of keyed ReactElements instead.");
                  }
                  didWarnAboutMaps = true;
                }
                var _newChildren = iteratorFn.call(newChildrenIterable);
                if (_newChildren) {
                  var knownKeys = null;
                  var _step = _newChildren.next();
                  for (; !_step.done; _step = _newChildren.next()) {
                    var child = _step.value;
                    knownKeys = warnOnInvalidKey(child, knownKeys, returnFiber);
                  }
                }
              }
              var newChildren = iteratorFn.call(newChildrenIterable);
              if (newChildren == null) {
                throw new Error("An iterable object provided no iterator.");
              }
              var resultingFirstChild = null;
              var previousNewFiber = null;
              var oldFiber = currentFirstChild;
              var lastPlacedIndex = 0;
              var newIdx = 0;
              var nextOldFiber = null;
              var step = newChildren.next();
              for (; oldFiber !== null && !step.done; newIdx++, step = newChildren.next()) {
                if (oldFiber.index > newIdx) {
                  nextOldFiber = oldFiber;
                  oldFiber = null;
                } else {
                  nextOldFiber = oldFiber.sibling;
                }
                var newFiber = updateSlot(returnFiber, oldFiber, step.value, lanes);
                if (newFiber === null) {
                  if (oldFiber === null) {
                    oldFiber = nextOldFiber;
                  }
                  break;
                }
                if (shouldTrackSideEffects) {
                  if (oldFiber && newFiber.alternate === null) {
                    deleteChild(returnFiber, oldFiber);
                  }
                }
                lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
                if (previousNewFiber === null) {
                  resultingFirstChild = newFiber;
                } else {
                  previousNewFiber.sibling = newFiber;
                }
                previousNewFiber = newFiber;
                oldFiber = nextOldFiber;
              }
              if (step.done) {
                deleteRemainingChildren(returnFiber, oldFiber);
                if (getIsHydrating()) {
                  var numberOfForks = newIdx;
                  pushTreeFork(returnFiber, numberOfForks);
                }
                return resultingFirstChild;
              }
              if (oldFiber === null) {
                for (; !step.done; newIdx++, step = newChildren.next()) {
                  var _newFiber3 = createChild(returnFiber, step.value, lanes);
                  if (_newFiber3 === null) {
                    continue;
                  }
                  lastPlacedIndex = placeChild(_newFiber3, lastPlacedIndex, newIdx);
                  if (previousNewFiber === null) {
                    resultingFirstChild = _newFiber3;
                  } else {
                    previousNewFiber.sibling = _newFiber3;
                  }
                  previousNewFiber = _newFiber3;
                }
                if (getIsHydrating()) {
                  var _numberOfForks3 = newIdx;
                  pushTreeFork(returnFiber, _numberOfForks3);
                }
                return resultingFirstChild;
              }
              var existingChildren = mapRemainingChildren(returnFiber, oldFiber);
              for (; !step.done; newIdx++, step = newChildren.next()) {
                var _newFiber4 = updateFromMap(existingChildren, returnFiber, newIdx, step.value, lanes);
                if (_newFiber4 !== null) {
                  if (shouldTrackSideEffects) {
                    if (_newFiber4.alternate !== null) {
                      existingChildren.delete(_newFiber4.key === null ? newIdx : _newFiber4.key);
                    }
                  }
                  lastPlacedIndex = placeChild(_newFiber4, lastPlacedIndex, newIdx);
                  if (previousNewFiber === null) {
                    resultingFirstChild = _newFiber4;
                  } else {
                    previousNewFiber.sibling = _newFiber4;
                  }
                  previousNewFiber = _newFiber4;
                }
              }
              if (shouldTrackSideEffects) {
                existingChildren.forEach(function(child2) {
                  return deleteChild(returnFiber, child2);
                });
              }
              if (getIsHydrating()) {
                var _numberOfForks4 = newIdx;
                pushTreeFork(returnFiber, _numberOfForks4);
              }
              return resultingFirstChild;
            }
            function reconcileSingleTextNode(returnFiber, currentFirstChild, textContent, lanes) {
              if (currentFirstChild !== null && currentFirstChild.tag === HostText) {
                deleteRemainingChildren(returnFiber, currentFirstChild.sibling);
                var existing = useFiber(currentFirstChild, textContent);
                existing.return = returnFiber;
                return existing;
              }
              deleteRemainingChildren(returnFiber, currentFirstChild);
              var created = createFiberFromText(textContent, returnFiber.mode, lanes);
              created.return = returnFiber;
              return created;
            }
            function reconcileSingleElement(returnFiber, currentFirstChild, element, lanes) {
              var key = element.key;
              var child = currentFirstChild;
              while (child !== null) {
                if (child.key === key) {
                  var elementType = element.type;
                  if (elementType === REACT_FRAGMENT_TYPE) {
                    if (child.tag === Fragment3) {
                      deleteRemainingChildren(returnFiber, child.sibling);
                      var existing = useFiber(child, element.props.children);
                      existing.return = returnFiber;
                      {
                        existing._debugSource = element._source;
                        existing._debugOwner = element._owner;
                      }
                      return existing;
                    }
                  } else {
                    if (child.elementType === elementType || // Keep this check inline so it only runs on the false path:
                    isCompatibleFamilyForHotReloading(child, element) || // Lazy types should reconcile their resolved type.
                    // We need to do this after the Hot Reloading check above,
                    // because hot reloading has different semantics than prod because
                    // it doesn't resuspend. So we can't let the call below suspend.
                    typeof elementType === "object" && elementType !== null && elementType.$$typeof === REACT_LAZY_TYPE && resolveLazy(elementType) === child.type) {
                      deleteRemainingChildren(returnFiber, child.sibling);
                      var _existing = useFiber(child, element.props);
                      _existing.ref = coerceRef(returnFiber, child, element);
                      _existing.return = returnFiber;
                      {
                        _existing._debugSource = element._source;
                        _existing._debugOwner = element._owner;
                      }
                      return _existing;
                    }
                  }
                  deleteRemainingChildren(returnFiber, child);
                  break;
                } else {
                  deleteChild(returnFiber, child);
                }
                child = child.sibling;
              }
              if (element.type === REACT_FRAGMENT_TYPE) {
                var created = createFiberFromFragment(element.props.children, returnFiber.mode, lanes, element.key);
                created.return = returnFiber;
                return created;
              } else {
                var _created4 = createFiberFromElement(element, returnFiber.mode, lanes);
                _created4.ref = coerceRef(returnFiber, currentFirstChild, element);
                _created4.return = returnFiber;
                return _created4;
              }
            }
            function reconcileSinglePortal(returnFiber, currentFirstChild, portal, lanes) {
              var key = portal.key;
              var child = currentFirstChild;
              while (child !== null) {
                if (child.key === key) {
                  if (child.tag === HostPortal && child.stateNode.containerInfo === portal.containerInfo && child.stateNode.implementation === portal.implementation) {
                    deleteRemainingChildren(returnFiber, child.sibling);
                    var existing = useFiber(child, portal.children || []);
                    existing.return = returnFiber;
                    return existing;
                  } else {
                    deleteRemainingChildren(returnFiber, child);
                    break;
                  }
                } else {
                  deleteChild(returnFiber, child);
                }
                child = child.sibling;
              }
              var created = createFiberFromPortal(portal, returnFiber.mode, lanes);
              created.return = returnFiber;
              return created;
            }
            function reconcileChildFibers2(returnFiber, currentFirstChild, newChild, lanes) {
              var isUnkeyedTopLevelFragment = typeof newChild === "object" && newChild !== null && newChild.type === REACT_FRAGMENT_TYPE && newChild.key === null;
              if (isUnkeyedTopLevelFragment) {
                newChild = newChild.props.children;
              }
              if (typeof newChild === "object" && newChild !== null) {
                switch (newChild.$$typeof) {
                  case REACT_ELEMENT_TYPE:
                    return placeSingleChild(reconcileSingleElement(returnFiber, currentFirstChild, newChild, lanes));
                  case REACT_PORTAL_TYPE:
                    return placeSingleChild(reconcileSinglePortal(returnFiber, currentFirstChild, newChild, lanes));
                  case REACT_LAZY_TYPE:
                    var payload = newChild._payload;
                    var init = newChild._init;
                    return reconcileChildFibers2(returnFiber, currentFirstChild, init(payload), lanes);
                }
                if (isArray(newChild)) {
                  return reconcileChildrenArray(returnFiber, currentFirstChild, newChild, lanes);
                }
                if (getIteratorFn(newChild)) {
                  return reconcileChildrenIterator(returnFiber, currentFirstChild, newChild, lanes);
                }
                throwOnInvalidObjectType(returnFiber, newChild);
              }
              if (typeof newChild === "string" && newChild !== "" || typeof newChild === "number") {
                return placeSingleChild(reconcileSingleTextNode(returnFiber, currentFirstChild, "" + newChild, lanes));
              }
              {
                if (typeof newChild === "function") {
                  warnOnFunctionType(returnFiber);
                }
              }
              return deleteRemainingChildren(returnFiber, currentFirstChild);
            }
            return reconcileChildFibers2;
          }
          var reconcileChildFibers = ChildReconciler(true);
          var mountChildFibers = ChildReconciler(false);
          function cloneChildFibers(current2, workInProgress2) {
            if (current2 !== null && workInProgress2.child !== current2.child) {
              throw new Error("Resuming work not yet implemented.");
            }
            if (workInProgress2.child === null) {
              return;
            }
            var currentChild = workInProgress2.child;
            var newChild = createWorkInProgress(currentChild, currentChild.pendingProps);
            workInProgress2.child = newChild;
            newChild.return = workInProgress2;
            while (currentChild.sibling !== null) {
              currentChild = currentChild.sibling;
              newChild = newChild.sibling = createWorkInProgress(currentChild, currentChild.pendingProps);
              newChild.return = workInProgress2;
            }
            newChild.sibling = null;
          }
          function resetChildFibers(workInProgress2, lanes) {
            var child = workInProgress2.child;
            while (child !== null) {
              resetWorkInProgress(child, lanes);
              child = child.sibling;
            }
          }
          var valueCursor = createCursor(null);
          var rendererSigil;
          {
            rendererSigil = {};
          }
          var currentlyRenderingFiber = null;
          var lastContextDependency = null;
          var lastFullyObservedContext = null;
          var isDisallowedContextReadInDEV = false;
          function resetContextDependencies() {
            currentlyRenderingFiber = null;
            lastContextDependency = null;
            lastFullyObservedContext = null;
            {
              isDisallowedContextReadInDEV = false;
            }
          }
          function enterDisallowedContextReadInDEV() {
            {
              isDisallowedContextReadInDEV = true;
            }
          }
          function exitDisallowedContextReadInDEV() {
            {
              isDisallowedContextReadInDEV = false;
            }
          }
          function pushProvider(providerFiber, context, nextValue) {
            {
              push(valueCursor, context._currentValue, providerFiber);
              context._currentValue = nextValue;
              {
                if (context._currentRenderer !== void 0 && context._currentRenderer !== null && context._currentRenderer !== rendererSigil) {
                  error("Detected multiple renderers concurrently rendering the same context provider. This is currently unsupported.");
                }
                context._currentRenderer = rendererSigil;
              }
            }
          }
          function popProvider(context, providerFiber) {
            var currentValue = valueCursor.current;
            pop(valueCursor, providerFiber);
            {
              {
                context._currentValue = currentValue;
              }
            }
          }
          function scheduleContextWorkOnParentPath(parent, renderLanes2, propagationRoot) {
            var node = parent;
            while (node !== null) {
              var alternate = node.alternate;
              if (!isSubsetOfLanes(node.childLanes, renderLanes2)) {
                node.childLanes = mergeLanes(node.childLanes, renderLanes2);
                if (alternate !== null) {
                  alternate.childLanes = mergeLanes(alternate.childLanes, renderLanes2);
                }
              } else if (alternate !== null && !isSubsetOfLanes(alternate.childLanes, renderLanes2)) {
                alternate.childLanes = mergeLanes(alternate.childLanes, renderLanes2);
              }
              if (node === propagationRoot) {
                break;
              }
              node = node.return;
            }
            {
              if (node !== propagationRoot) {
                error("Expected to find the propagation root when scheduling context work. This error is likely caused by a bug in React. Please file an issue.");
              }
            }
          }
          function propagateContextChange(workInProgress2, context, renderLanes2) {
            {
              propagateContextChange_eager(workInProgress2, context, renderLanes2);
            }
          }
          function propagateContextChange_eager(workInProgress2, context, renderLanes2) {
            var fiber = workInProgress2.child;
            if (fiber !== null) {
              fiber.return = workInProgress2;
            }
            while (fiber !== null) {
              var nextFiber = void 0;
              var list = fiber.dependencies;
              if (list !== null) {
                nextFiber = fiber.child;
                var dependency = list.firstContext;
                while (dependency !== null) {
                  if (dependency.context === context) {
                    if (fiber.tag === ClassComponent) {
                      var lane = pickArbitraryLane(renderLanes2);
                      var update = createUpdate(NoTimestamp, lane);
                      update.tag = ForceUpdate;
                      var updateQueue = fiber.updateQueue;
                      if (updateQueue === null) ;
                      else {
                        var sharedQueue = updateQueue.shared;
                        var pending = sharedQueue.pending;
                        if (pending === null) {
                          update.next = update;
                        } else {
                          update.next = pending.next;
                          pending.next = update;
                        }
                        sharedQueue.pending = update;
                      }
                    }
                    fiber.lanes = mergeLanes(fiber.lanes, renderLanes2);
                    var alternate = fiber.alternate;
                    if (alternate !== null) {
                      alternate.lanes = mergeLanes(alternate.lanes, renderLanes2);
                    }
                    scheduleContextWorkOnParentPath(fiber.return, renderLanes2, workInProgress2);
                    list.lanes = mergeLanes(list.lanes, renderLanes2);
                    break;
                  }
                  dependency = dependency.next;
                }
              } else if (fiber.tag === ContextProvider) {
                nextFiber = fiber.type === workInProgress2.type ? null : fiber.child;
              } else if (fiber.tag === DehydratedFragment) {
                var parentSuspense = fiber.return;
                if (parentSuspense === null) {
                  throw new Error("We just came from a parent so we must have had a parent. This is a bug in React.");
                }
                parentSuspense.lanes = mergeLanes(parentSuspense.lanes, renderLanes2);
                var _alternate = parentSuspense.alternate;
                if (_alternate !== null) {
                  _alternate.lanes = mergeLanes(_alternate.lanes, renderLanes2);
                }
                scheduleContextWorkOnParentPath(parentSuspense, renderLanes2, workInProgress2);
                nextFiber = fiber.sibling;
              } else {
                nextFiber = fiber.child;
              }
              if (nextFiber !== null) {
                nextFiber.return = fiber;
              } else {
                nextFiber = fiber;
                while (nextFiber !== null) {
                  if (nextFiber === workInProgress2) {
                    nextFiber = null;
                    break;
                  }
                  var sibling = nextFiber.sibling;
                  if (sibling !== null) {
                    sibling.return = nextFiber.return;
                    nextFiber = sibling;
                    break;
                  }
                  nextFiber = nextFiber.return;
                }
              }
              fiber = nextFiber;
            }
          }
          function prepareToReadContext(workInProgress2, renderLanes2) {
            currentlyRenderingFiber = workInProgress2;
            lastContextDependency = null;
            lastFullyObservedContext = null;
            var dependencies = workInProgress2.dependencies;
            if (dependencies !== null) {
              {
                var firstContext = dependencies.firstContext;
                if (firstContext !== null) {
                  if (includesSomeLane(dependencies.lanes, renderLanes2)) {
                    markWorkInProgressReceivedUpdate();
                  }
                  dependencies.firstContext = null;
                }
              }
            }
          }
          function readContext(context) {
            {
              if (isDisallowedContextReadInDEV) {
                error("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo().");
              }
            }
            var value = context._currentValue;
            if (lastFullyObservedContext === context) ;
            else {
              var contextItem = {
                context,
                memoizedValue: value,
                next: null
              };
              if (lastContextDependency === null) {
                if (currentlyRenderingFiber === null) {
                  throw new Error("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo().");
                }
                lastContextDependency = contextItem;
                currentlyRenderingFiber.dependencies = {
                  lanes: NoLanes,
                  firstContext: contextItem
                };
              } else {
                lastContextDependency = lastContextDependency.next = contextItem;
              }
            }
            return value;
          }
          var concurrentQueues = null;
          function pushConcurrentUpdateQueue(queue) {
            if (concurrentQueues === null) {
              concurrentQueues = [queue];
            } else {
              concurrentQueues.push(queue);
            }
          }
          function finishQueueingConcurrentUpdates() {
            if (concurrentQueues !== null) {
              for (var i = 0; i < concurrentQueues.length; i++) {
                var queue = concurrentQueues[i];
                var lastInterleavedUpdate = queue.interleaved;
                if (lastInterleavedUpdate !== null) {
                  queue.interleaved = null;
                  var firstInterleavedUpdate = lastInterleavedUpdate.next;
                  var lastPendingUpdate = queue.pending;
                  if (lastPendingUpdate !== null) {
                    var firstPendingUpdate = lastPendingUpdate.next;
                    lastPendingUpdate.next = firstInterleavedUpdate;
                    lastInterleavedUpdate.next = firstPendingUpdate;
                  }
                  queue.pending = lastInterleavedUpdate;
                }
              }
              concurrentQueues = null;
            }
          }
          function enqueueConcurrentHookUpdate(fiber, queue, update, lane) {
            var interleaved = queue.interleaved;
            if (interleaved === null) {
              update.next = update;
              pushConcurrentUpdateQueue(queue);
            } else {
              update.next = interleaved.next;
              interleaved.next = update;
            }
            queue.interleaved = update;
            return markUpdateLaneFromFiberToRoot(fiber, lane);
          }
          function enqueueConcurrentHookUpdateAndEagerlyBailout(fiber, queue, update, lane) {
            var interleaved = queue.interleaved;
            if (interleaved === null) {
              update.next = update;
              pushConcurrentUpdateQueue(queue);
            } else {
              update.next = interleaved.next;
              interleaved.next = update;
            }
            queue.interleaved = update;
          }
          function enqueueConcurrentClassUpdate(fiber, queue, update, lane) {
            var interleaved = queue.interleaved;
            if (interleaved === null) {
              update.next = update;
              pushConcurrentUpdateQueue(queue);
            } else {
              update.next = interleaved.next;
              interleaved.next = update;
            }
            queue.interleaved = update;
            return markUpdateLaneFromFiberToRoot(fiber, lane);
          }
          function enqueueConcurrentRenderForLane(fiber, lane) {
            return markUpdateLaneFromFiberToRoot(fiber, lane);
          }
          var unsafe_markUpdateLaneFromFiberToRoot = markUpdateLaneFromFiberToRoot;
          function markUpdateLaneFromFiberToRoot(sourceFiber, lane) {
            sourceFiber.lanes = mergeLanes(sourceFiber.lanes, lane);
            var alternate = sourceFiber.alternate;
            if (alternate !== null) {
              alternate.lanes = mergeLanes(alternate.lanes, lane);
            }
            {
              if (alternate === null && (sourceFiber.flags & (Placement | Hydrating)) !== NoFlags) {
                warnAboutUpdateOnNotYetMountedFiberInDEV(sourceFiber);
              }
            }
            var node = sourceFiber;
            var parent = sourceFiber.return;
            while (parent !== null) {
              parent.childLanes = mergeLanes(parent.childLanes, lane);
              alternate = parent.alternate;
              if (alternate !== null) {
                alternate.childLanes = mergeLanes(alternate.childLanes, lane);
              } else {
                {
                  if ((parent.flags & (Placement | Hydrating)) !== NoFlags) {
                    warnAboutUpdateOnNotYetMountedFiberInDEV(sourceFiber);
                  }
                }
              }
              node = parent;
              parent = parent.return;
            }
            if (node.tag === HostRoot) {
              var root3 = node.stateNode;
              return root3;
            } else {
              return null;
            }
          }
          var UpdateState = 0;
          var ReplaceState = 1;
          var ForceUpdate = 2;
          var CaptureUpdate = 3;
          var hasForceUpdate = false;
          var didWarnUpdateInsideUpdate;
          var currentlyProcessingQueue;
          {
            didWarnUpdateInsideUpdate = false;
            currentlyProcessingQueue = null;
          }
          function initializeUpdateQueue(fiber) {
            var queue = {
              baseState: fiber.memoizedState,
              firstBaseUpdate: null,
              lastBaseUpdate: null,
              shared: {
                pending: null,
                interleaved: null,
                lanes: NoLanes
              },
              effects: null
            };
            fiber.updateQueue = queue;
          }
          function cloneUpdateQueue(current2, workInProgress2) {
            var queue = workInProgress2.updateQueue;
            var currentQueue = current2.updateQueue;
            if (queue === currentQueue) {
              var clone = {
                baseState: currentQueue.baseState,
                firstBaseUpdate: currentQueue.firstBaseUpdate,
                lastBaseUpdate: currentQueue.lastBaseUpdate,
                shared: currentQueue.shared,
                effects: currentQueue.effects
              };
              workInProgress2.updateQueue = clone;
            }
          }
          function createUpdate(eventTime, lane) {
            var update = {
              eventTime,
              lane,
              tag: UpdateState,
              payload: null,
              callback: null,
              next: null
            };
            return update;
          }
          function enqueueUpdate(fiber, update, lane) {
            var updateQueue = fiber.updateQueue;
            if (updateQueue === null) {
              return null;
            }
            var sharedQueue = updateQueue.shared;
            {
              if (currentlyProcessingQueue === sharedQueue && !didWarnUpdateInsideUpdate) {
                error("An update (setState, replaceState, or forceUpdate) was scheduled from inside an update function. Update functions should be pure, with zero side-effects. Consider using componentDidUpdate or a callback.");
                didWarnUpdateInsideUpdate = true;
              }
            }
            if (isUnsafeClassRenderPhaseUpdate()) {
              var pending = sharedQueue.pending;
              if (pending === null) {
                update.next = update;
              } else {
                update.next = pending.next;
                pending.next = update;
              }
              sharedQueue.pending = update;
              return unsafe_markUpdateLaneFromFiberToRoot(fiber, lane);
            } else {
              return enqueueConcurrentClassUpdate(fiber, sharedQueue, update, lane);
            }
          }
          function entangleTransitions(root3, fiber, lane) {
            var updateQueue = fiber.updateQueue;
            if (updateQueue === null) {
              return;
            }
            var sharedQueue = updateQueue.shared;
            if (isTransitionLane(lane)) {
              var queueLanes = sharedQueue.lanes;
              queueLanes = intersectLanes(queueLanes, root3.pendingLanes);
              var newQueueLanes = mergeLanes(queueLanes, lane);
              sharedQueue.lanes = newQueueLanes;
              markRootEntangled(root3, newQueueLanes);
            }
          }
          function enqueueCapturedUpdate(workInProgress2, capturedUpdate) {
            var queue = workInProgress2.updateQueue;
            var current2 = workInProgress2.alternate;
            if (current2 !== null) {
              var currentQueue = current2.updateQueue;
              if (queue === currentQueue) {
                var newFirst = null;
                var newLast = null;
                var firstBaseUpdate = queue.firstBaseUpdate;
                if (firstBaseUpdate !== null) {
                  var update = firstBaseUpdate;
                  do {
                    var clone = {
                      eventTime: update.eventTime,
                      lane: update.lane,
                      tag: update.tag,
                      payload: update.payload,
                      callback: update.callback,
                      next: null
                    };
                    if (newLast === null) {
                      newFirst = newLast = clone;
                    } else {
                      newLast.next = clone;
                      newLast = clone;
                    }
                    update = update.next;
                  } while (update !== null);
                  if (newLast === null) {
                    newFirst = newLast = capturedUpdate;
                  } else {
                    newLast.next = capturedUpdate;
                    newLast = capturedUpdate;
                  }
                } else {
                  newFirst = newLast = capturedUpdate;
                }
                queue = {
                  baseState: currentQueue.baseState,
                  firstBaseUpdate: newFirst,
                  lastBaseUpdate: newLast,
                  shared: currentQueue.shared,
                  effects: currentQueue.effects
                };
                workInProgress2.updateQueue = queue;
                return;
              }
            }
            var lastBaseUpdate = queue.lastBaseUpdate;
            if (lastBaseUpdate === null) {
              queue.firstBaseUpdate = capturedUpdate;
            } else {
              lastBaseUpdate.next = capturedUpdate;
            }
            queue.lastBaseUpdate = capturedUpdate;
          }
          function getStateFromUpdate(workInProgress2, queue, update, prevState, nextProps, instance) {
            switch (update.tag) {
              case ReplaceState: {
                var payload = update.payload;
                if (typeof payload === "function") {
                  {
                    enterDisallowedContextReadInDEV();
                  }
                  var nextState = payload.call(instance, prevState, nextProps);
                  {
                    if (workInProgress2.mode & StrictLegacyMode) {
                      setIsStrictModeForDevtools(true);
                      try {
                        payload.call(instance, prevState, nextProps);
                      } finally {
                        setIsStrictModeForDevtools(false);
                      }
                    }
                    exitDisallowedContextReadInDEV();
                  }
                  return nextState;
                }
                return payload;
              }
              case CaptureUpdate: {
                workInProgress2.flags = workInProgress2.flags & ~ShouldCapture | DidCapture;
              }
              // Intentional fallthrough
              case UpdateState: {
                var _payload = update.payload;
                var partialState;
                if (typeof _payload === "function") {
                  {
                    enterDisallowedContextReadInDEV();
                  }
                  partialState = _payload.call(instance, prevState, nextProps);
                  {
                    if (workInProgress2.mode & StrictLegacyMode) {
                      setIsStrictModeForDevtools(true);
                      try {
                        _payload.call(instance, prevState, nextProps);
                      } finally {
                        setIsStrictModeForDevtools(false);
                      }
                    }
                    exitDisallowedContextReadInDEV();
                  }
                } else {
                  partialState = _payload;
                }
                if (partialState === null || partialState === void 0) {
                  return prevState;
                }
                return assign({}, prevState, partialState);
              }
              case ForceUpdate: {
                hasForceUpdate = true;
                return prevState;
              }
            }
            return prevState;
          }
          function processUpdateQueue(workInProgress2, props, instance, renderLanes2) {
            var queue = workInProgress2.updateQueue;
            hasForceUpdate = false;
            {
              currentlyProcessingQueue = queue.shared;
            }
            var firstBaseUpdate = queue.firstBaseUpdate;
            var lastBaseUpdate = queue.lastBaseUpdate;
            var pendingQueue = queue.shared.pending;
            if (pendingQueue !== null) {
              queue.shared.pending = null;
              var lastPendingUpdate = pendingQueue;
              var firstPendingUpdate = lastPendingUpdate.next;
              lastPendingUpdate.next = null;
              if (lastBaseUpdate === null) {
                firstBaseUpdate = firstPendingUpdate;
              } else {
                lastBaseUpdate.next = firstPendingUpdate;
              }
              lastBaseUpdate = lastPendingUpdate;
              var current2 = workInProgress2.alternate;
              if (current2 !== null) {
                var currentQueue = current2.updateQueue;
                var currentLastBaseUpdate = currentQueue.lastBaseUpdate;
                if (currentLastBaseUpdate !== lastBaseUpdate) {
                  if (currentLastBaseUpdate === null) {
                    currentQueue.firstBaseUpdate = firstPendingUpdate;
                  } else {
                    currentLastBaseUpdate.next = firstPendingUpdate;
                  }
                  currentQueue.lastBaseUpdate = lastPendingUpdate;
                }
              }
            }
            if (firstBaseUpdate !== null) {
              var newState = queue.baseState;
              var newLanes = NoLanes;
              var newBaseState = null;
              var newFirstBaseUpdate = null;
              var newLastBaseUpdate = null;
              var update = firstBaseUpdate;
              do {
                var updateLane = update.lane;
                var updateEventTime = update.eventTime;
                if (!isSubsetOfLanes(renderLanes2, updateLane)) {
                  var clone = {
                    eventTime: updateEventTime,
                    lane: updateLane,
                    tag: update.tag,
                    payload: update.payload,
                    callback: update.callback,
                    next: null
                  };
                  if (newLastBaseUpdate === null) {
                    newFirstBaseUpdate = newLastBaseUpdate = clone;
                    newBaseState = newState;
                  } else {
                    newLastBaseUpdate = newLastBaseUpdate.next = clone;
                  }
                  newLanes = mergeLanes(newLanes, updateLane);
                } else {
                  if (newLastBaseUpdate !== null) {
                    var _clone = {
                      eventTime: updateEventTime,
                      // This update is going to be committed so we never want uncommit
                      // it. Using NoLane works because 0 is a subset of all bitmasks, so
                      // this will never be skipped by the check above.
                      lane: NoLane,
                      tag: update.tag,
                      payload: update.payload,
                      callback: update.callback,
                      next: null
                    };
                    newLastBaseUpdate = newLastBaseUpdate.next = _clone;
                  }
                  newState = getStateFromUpdate(workInProgress2, queue, update, newState, props, instance);
                  var callback = update.callback;
                  if (callback !== null && // If the update was already committed, we should not queue its
                  // callback again.
                  update.lane !== NoLane) {
                    workInProgress2.flags |= Callback;
                    var effects = queue.effects;
                    if (effects === null) {
                      queue.effects = [update];
                    } else {
                      effects.push(update);
                    }
                  }
                }
                update = update.next;
                if (update === null) {
                  pendingQueue = queue.shared.pending;
                  if (pendingQueue === null) {
                    break;
                  } else {
                    var _lastPendingUpdate = pendingQueue;
                    var _firstPendingUpdate = _lastPendingUpdate.next;
                    _lastPendingUpdate.next = null;
                    update = _firstPendingUpdate;
                    queue.lastBaseUpdate = _lastPendingUpdate;
                    queue.shared.pending = null;
                  }
                }
              } while (true);
              if (newLastBaseUpdate === null) {
                newBaseState = newState;
              }
              queue.baseState = newBaseState;
              queue.firstBaseUpdate = newFirstBaseUpdate;
              queue.lastBaseUpdate = newLastBaseUpdate;
              var lastInterleaved = queue.shared.interleaved;
              if (lastInterleaved !== null) {
                var interleaved = lastInterleaved;
                do {
                  newLanes = mergeLanes(newLanes, interleaved.lane);
                  interleaved = interleaved.next;
                } while (interleaved !== lastInterleaved);
              } else if (firstBaseUpdate === null) {
                queue.shared.lanes = NoLanes;
              }
              markSkippedUpdateLanes(newLanes);
              workInProgress2.lanes = newLanes;
              workInProgress2.memoizedState = newState;
            }
            {
              currentlyProcessingQueue = null;
            }
          }
          function callCallback(callback, context) {
            if (typeof callback !== "function") {
              throw new Error("Invalid argument passed as callback. Expected a function. Instead " + ("received: " + callback));
            }
            callback.call(context);
          }
          function resetHasForceUpdateBeforeProcessing() {
            hasForceUpdate = false;
          }
          function checkHasForceUpdateAfterProcessing() {
            return hasForceUpdate;
          }
          function commitUpdateQueue(finishedWork, finishedQueue, instance) {
            var effects = finishedQueue.effects;
            finishedQueue.effects = null;
            if (effects !== null) {
              for (var i = 0; i < effects.length; i++) {
                var effect = effects[i];
                var callback = effect.callback;
                if (callback !== null) {
                  effect.callback = null;
                  callCallback(callback, instance);
                }
              }
            }
          }
          var NO_CONTEXT = {};
          var contextStackCursor$1 = createCursor(NO_CONTEXT);
          var contextFiberStackCursor = createCursor(NO_CONTEXT);
          var rootInstanceStackCursor = createCursor(NO_CONTEXT);
          function requiredContext(c) {
            if (c === NO_CONTEXT) {
              throw new Error("Expected host context to exist. This error is likely caused by a bug in React. Please file an issue.");
            }
            return c;
          }
          function getRootHostContainer() {
            var rootInstance = requiredContext(rootInstanceStackCursor.current);
            return rootInstance;
          }
          function pushHostContainer(fiber, nextRootInstance) {
            push(rootInstanceStackCursor, nextRootInstance, fiber);
            push(contextFiberStackCursor, fiber, fiber);
            push(contextStackCursor$1, NO_CONTEXT, fiber);
            var nextRootContext = getRootHostContext(nextRootInstance);
            pop(contextStackCursor$1, fiber);
            push(contextStackCursor$1, nextRootContext, fiber);
          }
          function popHostContainer(fiber) {
            pop(contextStackCursor$1, fiber);
            pop(contextFiberStackCursor, fiber);
            pop(rootInstanceStackCursor, fiber);
          }
          function getHostContext() {
            var context = requiredContext(contextStackCursor$1.current);
            return context;
          }
          function pushHostContext(fiber) {
            var rootInstance = requiredContext(rootInstanceStackCursor.current);
            var context = requiredContext(contextStackCursor$1.current);
            var nextContext = getChildHostContext(context, fiber.type);
            if (context === nextContext) {
              return;
            }
            push(contextFiberStackCursor, fiber, fiber);
            push(contextStackCursor$1, nextContext, fiber);
          }
          function popHostContext(fiber) {
            if (contextFiberStackCursor.current !== fiber) {
              return;
            }
            pop(contextStackCursor$1, fiber);
            pop(contextFiberStackCursor, fiber);
          }
          var DefaultSuspenseContext = 0;
          var SubtreeSuspenseContextMask = 1;
          var InvisibleParentSuspenseContext = 1;
          var ForceSuspenseFallback = 2;
          var suspenseStackCursor = createCursor(DefaultSuspenseContext);
          function hasSuspenseContext(parentContext, flag) {
            return (parentContext & flag) !== 0;
          }
          function setDefaultShallowSuspenseContext(parentContext) {
            return parentContext & SubtreeSuspenseContextMask;
          }
          function setShallowSuspenseContext(parentContext, shallowContext) {
            return parentContext & SubtreeSuspenseContextMask | shallowContext;
          }
          function addSubtreeSuspenseContext(parentContext, subtreeContext) {
            return parentContext | subtreeContext;
          }
          function pushSuspenseContext(fiber, newContext) {
            push(suspenseStackCursor, newContext, fiber);
          }
          function popSuspenseContext(fiber) {
            pop(suspenseStackCursor, fiber);
          }
          function shouldCaptureSuspense(workInProgress2, hasInvisibleParent) {
            var nextState = workInProgress2.memoizedState;
            if (nextState !== null) {
              if (nextState.dehydrated !== null) {
                return true;
              }
              return false;
            }
            var props = workInProgress2.memoizedProps;
            {
              return true;
            }
          }
          function findFirstSuspended(row) {
            var node = row;
            while (node !== null) {
              if (node.tag === SuspenseComponent) {
                var state = node.memoizedState;
                if (state !== null) {
                  var dehydrated = state.dehydrated;
                  if (dehydrated === null || isSuspenseInstancePending(dehydrated) || isSuspenseInstanceFallback(dehydrated)) {
                    return node;
                  }
                }
              } else if (node.tag === SuspenseListComponent && // revealOrder undefined can't be trusted because it don't
              // keep track of whether it suspended or not.
              node.memoizedProps.revealOrder !== void 0) {
                var didSuspend = (node.flags & DidCapture) !== NoFlags;
                if (didSuspend) {
                  return node;
                }
              } else if (node.child !== null) {
                node.child.return = node;
                node = node.child;
                continue;
              }
              if (node === row) {
                return null;
              }
              while (node.sibling === null) {
                if (node.return === null || node.return === row) {
                  return null;
                }
                node = node.return;
              }
              node.sibling.return = node.return;
              node = node.sibling;
            }
            return null;
          }
          var NoFlags$1 = (
            /*   */
            0
          );
          var HasEffect = (
            /* */
            1
          );
          var Insertion = (
            /*  */
            2
          );
          var Layout = (
            /*    */
            4
          );
          var Passive$1 = (
            /*   */
            8
          );
          var workInProgressSources = [];
          function resetWorkInProgressVersions() {
            for (var i = 0; i < workInProgressSources.length; i++) {
              var mutableSource = workInProgressSources[i];
              {
                mutableSource._workInProgressVersionPrimary = null;
              }
            }
            workInProgressSources.length = 0;
          }
          function registerMutableSourceForHydration(root3, mutableSource) {
            var getVersion = mutableSource._getVersion;
            var version = getVersion(mutableSource._source);
            if (root3.mutableSourceEagerHydrationData == null) {
              root3.mutableSourceEagerHydrationData = [mutableSource, version];
            } else {
              root3.mutableSourceEagerHydrationData.push(mutableSource, version);
            }
          }
          var ReactCurrentDispatcher$1 = ReactSharedInternals.ReactCurrentDispatcher, ReactCurrentBatchConfig$2 = ReactSharedInternals.ReactCurrentBatchConfig;
          var didWarnAboutMismatchedHooksForComponent;
          var didWarnUncachedGetSnapshot;
          {
            didWarnAboutMismatchedHooksForComponent = /* @__PURE__ */ new Set();
          }
          var renderLanes = NoLanes;
          var currentlyRenderingFiber$1 = null;
          var currentHook = null;
          var workInProgressHook = null;
          var didScheduleRenderPhaseUpdate = false;
          var didScheduleRenderPhaseUpdateDuringThisPass = false;
          var localIdCounter = 0;
          var globalClientIdCounter = 0;
          var RE_RENDER_LIMIT = 25;
          var currentHookNameInDev = null;
          var hookTypesDev = null;
          var hookTypesUpdateIndexDev = -1;
          var ignorePreviousDependencies = false;
          function mountHookTypesDev() {
            {
              var hookName = currentHookNameInDev;
              if (hookTypesDev === null) {
                hookTypesDev = [hookName];
              } else {
                hookTypesDev.push(hookName);
              }
            }
          }
          function updateHookTypesDev() {
            {
              var hookName = currentHookNameInDev;
              if (hookTypesDev !== null) {
                hookTypesUpdateIndexDev++;
                if (hookTypesDev[hookTypesUpdateIndexDev] !== hookName) {
                  warnOnHookMismatchInDev(hookName);
                }
              }
            }
          }
          function checkDepsAreArrayDev(deps) {
            {
              if (deps !== void 0 && deps !== null && !isArray(deps)) {
                error("%s received a final argument that is not an array (instead, received `%s`). When specified, the final argument must be an array.", currentHookNameInDev, typeof deps);
              }
            }
          }
          function warnOnHookMismatchInDev(currentHookName) {
            {
              var componentName = getComponentNameFromFiber(currentlyRenderingFiber$1);
              if (!didWarnAboutMismatchedHooksForComponent.has(componentName)) {
                didWarnAboutMismatchedHooksForComponent.add(componentName);
                if (hookTypesDev !== null) {
                  var table = "";
                  var secondColumnStart = 30;
                  for (var i = 0; i <= hookTypesUpdateIndexDev; i++) {
                    var oldHookName = hookTypesDev[i];
                    var newHookName = i === hookTypesUpdateIndexDev ? currentHookName : oldHookName;
                    var row = i + 1 + ". " + oldHookName;
                    while (row.length < secondColumnStart) {
                      row += " ";
                    }
                    row += newHookName + "\n";
                    table += row;
                  }
                  error("React has detected a change in the order of Hooks called by %s. This will lead to bugs and errors if not fixed. For more information, read the Rules of Hooks: https://reactjs.org/link/rules-of-hooks\n\n   Previous render            Next render\n   ------------------------------------------------------\n%s   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n", componentName, table);
                }
              }
            }
          }
          function throwInvalidHookError() {
            throw new Error("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://reactjs.org/link/invalid-hook-call for tips about how to debug and fix this problem.");
          }
          function areHookInputsEqual(nextDeps, prevDeps) {
            {
              if (ignorePreviousDependencies) {
                return false;
              }
            }
            if (prevDeps === null) {
              {
                error("%s received a final argument during this render, but not during the previous render. Even though the final argument is optional, its type cannot change between renders.", currentHookNameInDev);
              }
              return false;
            }
            {
              if (nextDeps.length !== prevDeps.length) {
                error("The final argument passed to %s changed size between renders. The order and size of this array must remain constant.\n\nPrevious: %s\nIncoming: %s", currentHookNameInDev, "[" + prevDeps.join(", ") + "]", "[" + nextDeps.join(", ") + "]");
              }
            }
            for (var i = 0; i < prevDeps.length && i < nextDeps.length; i++) {
              if (objectIs(nextDeps[i], prevDeps[i])) {
                continue;
              }
              return false;
            }
            return true;
          }
          function renderWithHooks(current2, workInProgress2, Component, props, secondArg, nextRenderLanes) {
            renderLanes = nextRenderLanes;
            currentlyRenderingFiber$1 = workInProgress2;
            {
              hookTypesDev = current2 !== null ? current2._debugHookTypes : null;
              hookTypesUpdateIndexDev = -1;
              ignorePreviousDependencies = current2 !== null && current2.type !== workInProgress2.type;
            }
            workInProgress2.memoizedState = null;
            workInProgress2.updateQueue = null;
            workInProgress2.lanes = NoLanes;
            {
              if (current2 !== null && current2.memoizedState !== null) {
                ReactCurrentDispatcher$1.current = HooksDispatcherOnUpdateInDEV;
              } else if (hookTypesDev !== null) {
                ReactCurrentDispatcher$1.current = HooksDispatcherOnMountWithHookTypesInDEV;
              } else {
                ReactCurrentDispatcher$1.current = HooksDispatcherOnMountInDEV;
              }
            }
            var children = Component(props, secondArg);
            if (didScheduleRenderPhaseUpdateDuringThisPass) {
              var numberOfReRenders = 0;
              do {
                didScheduleRenderPhaseUpdateDuringThisPass = false;
                localIdCounter = 0;
                if (numberOfReRenders >= RE_RENDER_LIMIT) {
                  throw new Error("Too many re-renders. React limits the number of renders to prevent an infinite loop.");
                }
                numberOfReRenders += 1;
                {
                  ignorePreviousDependencies = false;
                }
                currentHook = null;
                workInProgressHook = null;
                workInProgress2.updateQueue = null;
                {
                  hookTypesUpdateIndexDev = -1;
                }
                ReactCurrentDispatcher$1.current = HooksDispatcherOnRerenderInDEV;
                children = Component(props, secondArg);
              } while (didScheduleRenderPhaseUpdateDuringThisPass);
            }
            ReactCurrentDispatcher$1.current = ContextOnlyDispatcher;
            {
              workInProgress2._debugHookTypes = hookTypesDev;
            }
            var didRenderTooFewHooks = currentHook !== null && currentHook.next !== null;
            renderLanes = NoLanes;
            currentlyRenderingFiber$1 = null;
            currentHook = null;
            workInProgressHook = null;
            {
              currentHookNameInDev = null;
              hookTypesDev = null;
              hookTypesUpdateIndexDev = -1;
              if (current2 !== null && (current2.flags & StaticMask) !== (workInProgress2.flags & StaticMask) && // Disable this warning in legacy mode, because legacy Suspense is weird
              // and creates false positives. To make this work in legacy mode, we'd
              // need to mark fibers that commit in an incomplete state, somehow. For
              // now I'll disable the warning that most of the bugs that would trigger
              // it are either exclusive to concurrent mode or exist in both.
              (current2.mode & ConcurrentMode) !== NoMode) {
                error("Internal React error: Expected static flag was missing. Please notify the React team.");
              }
            }
            didScheduleRenderPhaseUpdate = false;
            if (didRenderTooFewHooks) {
              throw new Error("Rendered fewer hooks than expected. This may be caused by an accidental early return statement.");
            }
            return children;
          }
          function checkDidRenderIdHook() {
            var didRenderIdHook = localIdCounter !== 0;
            localIdCounter = 0;
            return didRenderIdHook;
          }
          function bailoutHooks(current2, workInProgress2, lanes) {
            workInProgress2.updateQueue = current2.updateQueue;
            if ((workInProgress2.mode & StrictEffectsMode) !== NoMode) {
              workInProgress2.flags &= ~(MountPassiveDev | MountLayoutDev | Passive | Update);
            } else {
              workInProgress2.flags &= ~(Passive | Update);
            }
            current2.lanes = removeLanes(current2.lanes, lanes);
          }
          function resetHooksAfterThrow() {
            ReactCurrentDispatcher$1.current = ContextOnlyDispatcher;
            if (didScheduleRenderPhaseUpdate) {
              var hook = currentlyRenderingFiber$1.memoizedState;
              while (hook !== null) {
                var queue = hook.queue;
                if (queue !== null) {
                  queue.pending = null;
                }
                hook = hook.next;
              }
              didScheduleRenderPhaseUpdate = false;
            }
            renderLanes = NoLanes;
            currentlyRenderingFiber$1 = null;
            currentHook = null;
            workInProgressHook = null;
            {
              hookTypesDev = null;
              hookTypesUpdateIndexDev = -1;
              currentHookNameInDev = null;
              isUpdatingOpaqueValueInRenderPhase = false;
            }
            didScheduleRenderPhaseUpdateDuringThisPass = false;
            localIdCounter = 0;
          }
          function mountWorkInProgressHook() {
            var hook = {
              memoizedState: null,
              baseState: null,
              baseQueue: null,
              queue: null,
              next: null
            };
            if (workInProgressHook === null) {
              currentlyRenderingFiber$1.memoizedState = workInProgressHook = hook;
            } else {
              workInProgressHook = workInProgressHook.next = hook;
            }
            return workInProgressHook;
          }
          function updateWorkInProgressHook() {
            var nextCurrentHook;
            if (currentHook === null) {
              var current2 = currentlyRenderingFiber$1.alternate;
              if (current2 !== null) {
                nextCurrentHook = current2.memoizedState;
              } else {
                nextCurrentHook = null;
              }
            } else {
              nextCurrentHook = currentHook.next;
            }
            var nextWorkInProgressHook;
            if (workInProgressHook === null) {
              nextWorkInProgressHook = currentlyRenderingFiber$1.memoizedState;
            } else {
              nextWorkInProgressHook = workInProgressHook.next;
            }
            if (nextWorkInProgressHook !== null) {
              workInProgressHook = nextWorkInProgressHook;
              nextWorkInProgressHook = workInProgressHook.next;
              currentHook = nextCurrentHook;
            } else {
              if (nextCurrentHook === null) {
                throw new Error("Rendered more hooks than during the previous render.");
              }
              currentHook = nextCurrentHook;
              var newHook = {
                memoizedState: currentHook.memoizedState,
                baseState: currentHook.baseState,
                baseQueue: currentHook.baseQueue,
                queue: currentHook.queue,
                next: null
              };
              if (workInProgressHook === null) {
                currentlyRenderingFiber$1.memoizedState = workInProgressHook = newHook;
              } else {
                workInProgressHook = workInProgressHook.next = newHook;
              }
            }
            return workInProgressHook;
          }
          function createFunctionComponentUpdateQueue() {
            return {
              lastEffect: null,
              stores: null
            };
          }
          function basicStateReducer(state, action) {
            return typeof action === "function" ? action(state) : action;
          }
          function mountReducer(reducer, initialArg, init) {
            var hook = mountWorkInProgressHook();
            var initialState;
            if (init !== void 0) {
              initialState = init(initialArg);
            } else {
              initialState = initialArg;
            }
            hook.memoizedState = hook.baseState = initialState;
            var queue = {
              pending: null,
              interleaved: null,
              lanes: NoLanes,
              dispatch: null,
              lastRenderedReducer: reducer,
              lastRenderedState: initialState
            };
            hook.queue = queue;
            var dispatch = queue.dispatch = dispatchReducerAction.bind(null, currentlyRenderingFiber$1, queue);
            return [hook.memoizedState, dispatch];
          }
          function updateReducer(reducer, initialArg, init) {
            var hook = updateWorkInProgressHook();
            var queue = hook.queue;
            if (queue === null) {
              throw new Error("Should have a queue. This is likely a bug in React. Please file an issue.");
            }
            queue.lastRenderedReducer = reducer;
            var current2 = currentHook;
            var baseQueue = current2.baseQueue;
            var pendingQueue = queue.pending;
            if (pendingQueue !== null) {
              if (baseQueue !== null) {
                var baseFirst = baseQueue.next;
                var pendingFirst = pendingQueue.next;
                baseQueue.next = pendingFirst;
                pendingQueue.next = baseFirst;
              }
              {
                if (current2.baseQueue !== baseQueue) {
                  error("Internal error: Expected work-in-progress queue to be a clone. This is a bug in React.");
                }
              }
              current2.baseQueue = baseQueue = pendingQueue;
              queue.pending = null;
            }
            if (baseQueue !== null) {
              var first = baseQueue.next;
              var newState = current2.baseState;
              var newBaseState = null;
              var newBaseQueueFirst = null;
              var newBaseQueueLast = null;
              var update = first;
              do {
                var updateLane = update.lane;
                if (!isSubsetOfLanes(renderLanes, updateLane)) {
                  var clone = {
                    lane: updateLane,
                    action: update.action,
                    hasEagerState: update.hasEagerState,
                    eagerState: update.eagerState,
                    next: null
                  };
                  if (newBaseQueueLast === null) {
                    newBaseQueueFirst = newBaseQueueLast = clone;
                    newBaseState = newState;
                  } else {
                    newBaseQueueLast = newBaseQueueLast.next = clone;
                  }
                  currentlyRenderingFiber$1.lanes = mergeLanes(currentlyRenderingFiber$1.lanes, updateLane);
                  markSkippedUpdateLanes(updateLane);
                } else {
                  if (newBaseQueueLast !== null) {
                    var _clone = {
                      // This update is going to be committed so we never want uncommit
                      // it. Using NoLane works because 0 is a subset of all bitmasks, so
                      // this will never be skipped by the check above.
                      lane: NoLane,
                      action: update.action,
                      hasEagerState: update.hasEagerState,
                      eagerState: update.eagerState,
                      next: null
                    };
                    newBaseQueueLast = newBaseQueueLast.next = _clone;
                  }
                  if (update.hasEagerState) {
                    newState = update.eagerState;
                  } else {
                    var action = update.action;
                    newState = reducer(newState, action);
                  }
                }
                update = update.next;
              } while (update !== null && update !== first);
              if (newBaseQueueLast === null) {
                newBaseState = newState;
              } else {
                newBaseQueueLast.next = newBaseQueueFirst;
              }
              if (!objectIs(newState, hook.memoizedState)) {
                markWorkInProgressReceivedUpdate();
              }
              hook.memoizedState = newState;
              hook.baseState = newBaseState;
              hook.baseQueue = newBaseQueueLast;
              queue.lastRenderedState = newState;
            }
            var lastInterleaved = queue.interleaved;
            if (lastInterleaved !== null) {
              var interleaved = lastInterleaved;
              do {
                var interleavedLane = interleaved.lane;
                currentlyRenderingFiber$1.lanes = mergeLanes(currentlyRenderingFiber$1.lanes, interleavedLane);
                markSkippedUpdateLanes(interleavedLane);
                interleaved = interleaved.next;
              } while (interleaved !== lastInterleaved);
            } else if (baseQueue === null) {
              queue.lanes = NoLanes;
            }
            var dispatch = queue.dispatch;
            return [hook.memoizedState, dispatch];
          }
          function rerenderReducer(reducer, initialArg, init) {
            var hook = updateWorkInProgressHook();
            var queue = hook.queue;
            if (queue === null) {
              throw new Error("Should have a queue. This is likely a bug in React. Please file an issue.");
            }
            queue.lastRenderedReducer = reducer;
            var dispatch = queue.dispatch;
            var lastRenderPhaseUpdate = queue.pending;
            var newState = hook.memoizedState;
            if (lastRenderPhaseUpdate !== null) {
              queue.pending = null;
              var firstRenderPhaseUpdate = lastRenderPhaseUpdate.next;
              var update = firstRenderPhaseUpdate;
              do {
                var action = update.action;
                newState = reducer(newState, action);
                update = update.next;
              } while (update !== firstRenderPhaseUpdate);
              if (!objectIs(newState, hook.memoizedState)) {
                markWorkInProgressReceivedUpdate();
              }
              hook.memoizedState = newState;
              if (hook.baseQueue === null) {
                hook.baseState = newState;
              }
              queue.lastRenderedState = newState;
            }
            return [newState, dispatch];
          }
          function mountMutableSource(source, getSnapshot, subscribe) {
            {
              return void 0;
            }
          }
          function updateMutableSource(source, getSnapshot, subscribe) {
            {
              return void 0;
            }
          }
          function mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
            var fiber = currentlyRenderingFiber$1;
            var hook = mountWorkInProgressHook();
            var nextSnapshot;
            var isHydrating2 = getIsHydrating();
            if (isHydrating2) {
              if (getServerSnapshot === void 0) {
                throw new Error("Missing getServerSnapshot, which is required for server-rendered content. Will revert to client rendering.");
              }
              nextSnapshot = getServerSnapshot();
              {
                if (!didWarnUncachedGetSnapshot) {
                  if (nextSnapshot !== getServerSnapshot()) {
                    error("The result of getServerSnapshot should be cached to avoid an infinite loop");
                    didWarnUncachedGetSnapshot = true;
                  }
                }
              }
            } else {
              nextSnapshot = getSnapshot();
              {
                if (!didWarnUncachedGetSnapshot) {
                  var cachedSnapshot = getSnapshot();
                  if (!objectIs(nextSnapshot, cachedSnapshot)) {
                    error("The result of getSnapshot should be cached to avoid an infinite loop");
                    didWarnUncachedGetSnapshot = true;
                  }
                }
              }
              var root3 = getWorkInProgressRoot();
              if (root3 === null) {
                throw new Error("Expected a work-in-progress root. This is a bug in React. Please file an issue.");
              }
              if (!includesBlockingLane(root3, renderLanes)) {
                pushStoreConsistencyCheck(fiber, getSnapshot, nextSnapshot);
              }
            }
            hook.memoizedState = nextSnapshot;
            var inst = {
              value: nextSnapshot,
              getSnapshot
            };
            hook.queue = inst;
            mountEffect(subscribeToStore.bind(null, fiber, inst, subscribe), [subscribe]);
            fiber.flags |= Passive;
            pushEffect(HasEffect | Passive$1, updateStoreInstance.bind(null, fiber, inst, nextSnapshot, getSnapshot), void 0, null);
            return nextSnapshot;
          }
          function updateSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
            var fiber = currentlyRenderingFiber$1;
            var hook = updateWorkInProgressHook();
            var nextSnapshot = getSnapshot();
            {
              if (!didWarnUncachedGetSnapshot) {
                var cachedSnapshot = getSnapshot();
                if (!objectIs(nextSnapshot, cachedSnapshot)) {
                  error("The result of getSnapshot should be cached to avoid an infinite loop");
                  didWarnUncachedGetSnapshot = true;
                }
              }
            }
            var prevSnapshot = hook.memoizedState;
            var snapshotChanged = !objectIs(prevSnapshot, nextSnapshot);
            if (snapshotChanged) {
              hook.memoizedState = nextSnapshot;
              markWorkInProgressReceivedUpdate();
            }
            var inst = hook.queue;
            updateEffect(subscribeToStore.bind(null, fiber, inst, subscribe), [subscribe]);
            if (inst.getSnapshot !== getSnapshot || snapshotChanged || // Check if the susbcribe function changed. We can save some memory by
            // checking whether we scheduled a subscription effect above.
            workInProgressHook !== null && workInProgressHook.memoizedState.tag & HasEffect) {
              fiber.flags |= Passive;
              pushEffect(HasEffect | Passive$1, updateStoreInstance.bind(null, fiber, inst, nextSnapshot, getSnapshot), void 0, null);
              var root3 = getWorkInProgressRoot();
              if (root3 === null) {
                throw new Error("Expected a work-in-progress root. This is a bug in React. Please file an issue.");
              }
              if (!includesBlockingLane(root3, renderLanes)) {
                pushStoreConsistencyCheck(fiber, getSnapshot, nextSnapshot);
              }
            }
            return nextSnapshot;
          }
          function pushStoreConsistencyCheck(fiber, getSnapshot, renderedSnapshot) {
            fiber.flags |= StoreConsistency;
            var check = {
              getSnapshot,
              value: renderedSnapshot
            };
            var componentUpdateQueue = currentlyRenderingFiber$1.updateQueue;
            if (componentUpdateQueue === null) {
              componentUpdateQueue = createFunctionComponentUpdateQueue();
              currentlyRenderingFiber$1.updateQueue = componentUpdateQueue;
              componentUpdateQueue.stores = [check];
            } else {
              var stores = componentUpdateQueue.stores;
              if (stores === null) {
                componentUpdateQueue.stores = [check];
              } else {
                stores.push(check);
              }
            }
          }
          function updateStoreInstance(fiber, inst, nextSnapshot, getSnapshot) {
            inst.value = nextSnapshot;
            inst.getSnapshot = getSnapshot;
            if (checkIfSnapshotChanged(inst)) {
              forceStoreRerender(fiber);
            }
          }
          function subscribeToStore(fiber, inst, subscribe) {
            var handleStoreChange = function() {
              if (checkIfSnapshotChanged(inst)) {
                forceStoreRerender(fiber);
              }
            };
            return subscribe(handleStoreChange);
          }
          function checkIfSnapshotChanged(inst) {
            var latestGetSnapshot = inst.getSnapshot;
            var prevValue = inst.value;
            try {
              var nextValue = latestGetSnapshot();
              return !objectIs(prevValue, nextValue);
            } catch (error2) {
              return true;
            }
          }
          function forceStoreRerender(fiber) {
            var root3 = enqueueConcurrentRenderForLane(fiber, SyncLane);
            if (root3 !== null) {
              scheduleUpdateOnFiber(root3, fiber, SyncLane, NoTimestamp);
            }
          }
          function mountState(initialState) {
            var hook = mountWorkInProgressHook();
            if (typeof initialState === "function") {
              initialState = initialState();
            }
            hook.memoizedState = hook.baseState = initialState;
            var queue = {
              pending: null,
              interleaved: null,
              lanes: NoLanes,
              dispatch: null,
              lastRenderedReducer: basicStateReducer,
              lastRenderedState: initialState
            };
            hook.queue = queue;
            var dispatch = queue.dispatch = dispatchSetState.bind(null, currentlyRenderingFiber$1, queue);
            return [hook.memoizedState, dispatch];
          }
          function updateState(initialState) {
            return updateReducer(basicStateReducer);
          }
          function rerenderState(initialState) {
            return rerenderReducer(basicStateReducer);
          }
          function pushEffect(tag, create, destroy, deps) {
            var effect = {
              tag,
              create,
              destroy,
              deps,
              // Circular
              next: null
            };
            var componentUpdateQueue = currentlyRenderingFiber$1.updateQueue;
            if (componentUpdateQueue === null) {
              componentUpdateQueue = createFunctionComponentUpdateQueue();
              currentlyRenderingFiber$1.updateQueue = componentUpdateQueue;
              componentUpdateQueue.lastEffect = effect.next = effect;
            } else {
              var lastEffect = componentUpdateQueue.lastEffect;
              if (lastEffect === null) {
                componentUpdateQueue.lastEffect = effect.next = effect;
              } else {
                var firstEffect = lastEffect.next;
                lastEffect.next = effect;
                effect.next = firstEffect;
                componentUpdateQueue.lastEffect = effect;
              }
            }
            return effect;
          }
          function mountRef(initialValue) {
            var hook = mountWorkInProgressHook();
            {
              var _ref2 = {
                current: initialValue
              };
              hook.memoizedState = _ref2;
              return _ref2;
            }
          }
          function updateRef(initialValue) {
            var hook = updateWorkInProgressHook();
            return hook.memoizedState;
          }
          function mountEffectImpl(fiberFlags, hookFlags, create, deps) {
            var hook = mountWorkInProgressHook();
            var nextDeps = deps === void 0 ? null : deps;
            currentlyRenderingFiber$1.flags |= fiberFlags;
            hook.memoizedState = pushEffect(HasEffect | hookFlags, create, void 0, nextDeps);
          }
          function updateEffectImpl(fiberFlags, hookFlags, create, deps) {
            var hook = updateWorkInProgressHook();
            var nextDeps = deps === void 0 ? null : deps;
            var destroy = void 0;
            if (currentHook !== null) {
              var prevEffect = currentHook.memoizedState;
              destroy = prevEffect.destroy;
              if (nextDeps !== null) {
                var prevDeps = prevEffect.deps;
                if (areHookInputsEqual(nextDeps, prevDeps)) {
                  hook.memoizedState = pushEffect(hookFlags, create, destroy, nextDeps);
                  return;
                }
              }
            }
            currentlyRenderingFiber$1.flags |= fiberFlags;
            hook.memoizedState = pushEffect(HasEffect | hookFlags, create, destroy, nextDeps);
          }
          function mountEffect(create, deps) {
            if ((currentlyRenderingFiber$1.mode & StrictEffectsMode) !== NoMode) {
              return mountEffectImpl(MountPassiveDev | Passive | PassiveStatic, Passive$1, create, deps);
            } else {
              return mountEffectImpl(Passive | PassiveStatic, Passive$1, create, deps);
            }
          }
          function updateEffect(create, deps) {
            return updateEffectImpl(Passive, Passive$1, create, deps);
          }
          function mountInsertionEffect(create, deps) {
            return mountEffectImpl(Update, Insertion, create, deps);
          }
          function updateInsertionEffect(create, deps) {
            return updateEffectImpl(Update, Insertion, create, deps);
          }
          function mountLayoutEffect(create, deps) {
            var fiberFlags = Update;
            {
              fiberFlags |= LayoutStatic;
            }
            if ((currentlyRenderingFiber$1.mode & StrictEffectsMode) !== NoMode) {
              fiberFlags |= MountLayoutDev;
            }
            return mountEffectImpl(fiberFlags, Layout, create, deps);
          }
          function updateLayoutEffect(create, deps) {
            return updateEffectImpl(Update, Layout, create, deps);
          }
          function imperativeHandleEffect(create, ref) {
            if (typeof ref === "function") {
              var refCallback = ref;
              var _inst = create();
              refCallback(_inst);
              return function() {
                refCallback(null);
              };
            } else if (ref !== null && ref !== void 0) {
              var refObject = ref;
              {
                if (!refObject.hasOwnProperty("current")) {
                  error("Expected useImperativeHandle() first argument to either be a ref callback or React.createRef() object. Instead received: %s.", "an object with keys {" + Object.keys(refObject).join(", ") + "}");
                }
              }
              var _inst2 = create();
              refObject.current = _inst2;
              return function() {
                refObject.current = null;
              };
            }
          }
          function mountImperativeHandle(ref, create, deps) {
            {
              if (typeof create !== "function") {
                error("Expected useImperativeHandle() second argument to be a function that creates a handle. Instead received: %s.", create !== null ? typeof create : "null");
              }
            }
            var effectDeps = deps !== null && deps !== void 0 ? deps.concat([ref]) : null;
            var fiberFlags = Update;
            {
              fiberFlags |= LayoutStatic;
            }
            if ((currentlyRenderingFiber$1.mode & StrictEffectsMode) !== NoMode) {
              fiberFlags |= MountLayoutDev;
            }
            return mountEffectImpl(fiberFlags, Layout, imperativeHandleEffect.bind(null, create, ref), effectDeps);
          }
          function updateImperativeHandle(ref, create, deps) {
            {
              if (typeof create !== "function") {
                error("Expected useImperativeHandle() second argument to be a function that creates a handle. Instead received: %s.", create !== null ? typeof create : "null");
              }
            }
            var effectDeps = deps !== null && deps !== void 0 ? deps.concat([ref]) : null;
            return updateEffectImpl(Update, Layout, imperativeHandleEffect.bind(null, create, ref), effectDeps);
          }
          function mountDebugValue(value, formatterFn) {
          }
          var updateDebugValue = mountDebugValue;
          function mountCallback(callback, deps) {
            var hook = mountWorkInProgressHook();
            var nextDeps = deps === void 0 ? null : deps;
            hook.memoizedState = [callback, nextDeps];
            return callback;
          }
          function updateCallback(callback, deps) {
            var hook = updateWorkInProgressHook();
            var nextDeps = deps === void 0 ? null : deps;
            var prevState = hook.memoizedState;
            if (prevState !== null) {
              if (nextDeps !== null) {
                var prevDeps = prevState[1];
                if (areHookInputsEqual(nextDeps, prevDeps)) {
                  return prevState[0];
                }
              }
            }
            hook.memoizedState = [callback, nextDeps];
            return callback;
          }
          function mountMemo(nextCreate, deps) {
            var hook = mountWorkInProgressHook();
            var nextDeps = deps === void 0 ? null : deps;
            var nextValue = nextCreate();
            hook.memoizedState = [nextValue, nextDeps];
            return nextValue;
          }
          function updateMemo(nextCreate, deps) {
            var hook = updateWorkInProgressHook();
            var nextDeps = deps === void 0 ? null : deps;
            var prevState = hook.memoizedState;
            if (prevState !== null) {
              if (nextDeps !== null) {
                var prevDeps = prevState[1];
                if (areHookInputsEqual(nextDeps, prevDeps)) {
                  return prevState[0];
                }
              }
            }
            var nextValue = nextCreate();
            hook.memoizedState = [nextValue, nextDeps];
            return nextValue;
          }
          function mountDeferredValue(value) {
            var hook = mountWorkInProgressHook();
            hook.memoizedState = value;
            return value;
          }
          function updateDeferredValue(value) {
            var hook = updateWorkInProgressHook();
            var resolvedCurrentHook = currentHook;
            var prevValue = resolvedCurrentHook.memoizedState;
            return updateDeferredValueImpl(hook, prevValue, value);
          }
          function rerenderDeferredValue(value) {
            var hook = updateWorkInProgressHook();
            if (currentHook === null) {
              hook.memoizedState = value;
              return value;
            } else {
              var prevValue = currentHook.memoizedState;
              return updateDeferredValueImpl(hook, prevValue, value);
            }
          }
          function updateDeferredValueImpl(hook, prevValue, value) {
            var shouldDeferValue = !includesOnlyNonUrgentLanes(renderLanes);
            if (shouldDeferValue) {
              if (!objectIs(value, prevValue)) {
                var deferredLane = claimNextTransitionLane();
                currentlyRenderingFiber$1.lanes = mergeLanes(currentlyRenderingFiber$1.lanes, deferredLane);
                markSkippedUpdateLanes(deferredLane);
                hook.baseState = true;
              }
              return prevValue;
            } else {
              if (hook.baseState) {
                hook.baseState = false;
                markWorkInProgressReceivedUpdate();
              }
              hook.memoizedState = value;
              return value;
            }
          }
          function startTransition(setPending, callback, options2) {
            var previousPriority = getCurrentUpdatePriority();
            setCurrentUpdatePriority(higherEventPriority(previousPriority, ContinuousEventPriority));
            setPending(true);
            var prevTransition = ReactCurrentBatchConfig$2.transition;
            ReactCurrentBatchConfig$2.transition = {};
            var currentTransition = ReactCurrentBatchConfig$2.transition;
            {
              ReactCurrentBatchConfig$2.transition._updatedFibers = /* @__PURE__ */ new Set();
            }
            try {
              setPending(false);
              callback();
            } finally {
              setCurrentUpdatePriority(previousPriority);
              ReactCurrentBatchConfig$2.transition = prevTransition;
              {
                if (prevTransition === null && currentTransition._updatedFibers) {
                  var updatedFibersCount = currentTransition._updatedFibers.size;
                  if (updatedFibersCount > 10) {
                    warn("Detected a large number of updates inside startTransition. If this is due to a subscription please re-write it to use React provided hooks. Otherwise concurrent mode guarantees are off the table.");
                  }
                  currentTransition._updatedFibers.clear();
                }
              }
            }
          }
          function mountTransition() {
            var _mountState = mountState(false), isPending = _mountState[0], setPending = _mountState[1];
            var start = startTransition.bind(null, setPending);
            var hook = mountWorkInProgressHook();
            hook.memoizedState = start;
            return [isPending, start];
          }
          function updateTransition() {
            var _updateState = updateState(), isPending = _updateState[0];
            var hook = updateWorkInProgressHook();
            var start = hook.memoizedState;
            return [isPending, start];
          }
          function rerenderTransition() {
            var _rerenderState = rerenderState(), isPending = _rerenderState[0];
            var hook = updateWorkInProgressHook();
            var start = hook.memoizedState;
            return [isPending, start];
          }
          var isUpdatingOpaqueValueInRenderPhase = false;
          function getIsUpdatingOpaqueValueInRenderPhaseInDEV() {
            {
              return isUpdatingOpaqueValueInRenderPhase;
            }
          }
          function mountId() {
            var hook = mountWorkInProgressHook();
            var root3 = getWorkInProgressRoot();
            var identifierPrefix = root3.identifierPrefix;
            var id;
            if (getIsHydrating()) {
              var treeId = getTreeId();
              id = ":" + identifierPrefix + "R" + treeId;
              var localId = localIdCounter++;
              if (localId > 0) {
                id += "H" + localId.toString(32);
              }
              id += ":";
            } else {
              var globalClientId = globalClientIdCounter++;
              id = ":" + identifierPrefix + "r" + globalClientId.toString(32) + ":";
            }
            hook.memoizedState = id;
            return id;
          }
          function updateId() {
            var hook = updateWorkInProgressHook();
            var id = hook.memoizedState;
            return id;
          }
          function dispatchReducerAction(fiber, queue, action) {
            {
              if (typeof arguments[3] === "function") {
                error("State updates from the useState() and useReducer() Hooks don't support the second callback argument. To execute a side effect after rendering, declare it in the component body with useEffect().");
              }
            }
            var lane = requestUpdateLane(fiber);
            var update = {
              lane,
              action,
              hasEagerState: false,
              eagerState: null,
              next: null
            };
            if (isRenderPhaseUpdate(fiber)) {
              enqueueRenderPhaseUpdate(queue, update);
            } else {
              var root3 = enqueueConcurrentHookUpdate(fiber, queue, update, lane);
              if (root3 !== null) {
                var eventTime = requestEventTime();
                scheduleUpdateOnFiber(root3, fiber, lane, eventTime);
                entangleTransitionUpdate(root3, queue, lane);
              }
            }
            markUpdateInDevTools(fiber, lane);
          }
          function dispatchSetState(fiber, queue, action) {
            {
              if (typeof arguments[3] === "function") {
                error("State updates from the useState() and useReducer() Hooks don't support the second callback argument. To execute a side effect after rendering, declare it in the component body with useEffect().");
              }
            }
            var lane = requestUpdateLane(fiber);
            var update = {
              lane,
              action,
              hasEagerState: false,
              eagerState: null,
              next: null
            };
            if (isRenderPhaseUpdate(fiber)) {
              enqueueRenderPhaseUpdate(queue, update);
            } else {
              var alternate = fiber.alternate;
              if (fiber.lanes === NoLanes && (alternate === null || alternate.lanes === NoLanes)) {
                var lastRenderedReducer = queue.lastRenderedReducer;
                if (lastRenderedReducer !== null) {
                  var prevDispatcher;
                  {
                    prevDispatcher = ReactCurrentDispatcher$1.current;
                    ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
                  }
                  try {
                    var currentState = queue.lastRenderedState;
                    var eagerState = lastRenderedReducer(currentState, action);
                    update.hasEagerState = true;
                    update.eagerState = eagerState;
                    if (objectIs(eagerState, currentState)) {
                      enqueueConcurrentHookUpdateAndEagerlyBailout(fiber, queue, update, lane);
                      return;
                    }
                  } catch (error2) {
                  } finally {
                    {
                      ReactCurrentDispatcher$1.current = prevDispatcher;
                    }
                  }
                }
              }
              var root3 = enqueueConcurrentHookUpdate(fiber, queue, update, lane);
              if (root3 !== null) {
                var eventTime = requestEventTime();
                scheduleUpdateOnFiber(root3, fiber, lane, eventTime);
                entangleTransitionUpdate(root3, queue, lane);
              }
            }
            markUpdateInDevTools(fiber, lane);
          }
          function isRenderPhaseUpdate(fiber) {
            var alternate = fiber.alternate;
            return fiber === currentlyRenderingFiber$1 || alternate !== null && alternate === currentlyRenderingFiber$1;
          }
          function enqueueRenderPhaseUpdate(queue, update) {
            didScheduleRenderPhaseUpdateDuringThisPass = didScheduleRenderPhaseUpdate = true;
            var pending = queue.pending;
            if (pending === null) {
              update.next = update;
            } else {
              update.next = pending.next;
              pending.next = update;
            }
            queue.pending = update;
          }
          function entangleTransitionUpdate(root3, queue, lane) {
            if (isTransitionLane(lane)) {
              var queueLanes = queue.lanes;
              queueLanes = intersectLanes(queueLanes, root3.pendingLanes);
              var newQueueLanes = mergeLanes(queueLanes, lane);
              queue.lanes = newQueueLanes;
              markRootEntangled(root3, newQueueLanes);
            }
          }
          function markUpdateInDevTools(fiber, lane, action) {
            {
              markStateUpdateScheduled(fiber, lane);
            }
          }
          var ContextOnlyDispatcher = {
            readContext,
            useCallback: throwInvalidHookError,
            useContext: throwInvalidHookError,
            useEffect: throwInvalidHookError,
            useImperativeHandle: throwInvalidHookError,
            useInsertionEffect: throwInvalidHookError,
            useLayoutEffect: throwInvalidHookError,
            useMemo: throwInvalidHookError,
            useReducer: throwInvalidHookError,
            useRef: throwInvalidHookError,
            useState: throwInvalidHookError,
            useDebugValue: throwInvalidHookError,
            useDeferredValue: throwInvalidHookError,
            useTransition: throwInvalidHookError,
            useMutableSource: throwInvalidHookError,
            useSyncExternalStore: throwInvalidHookError,
            useId: throwInvalidHookError,
            unstable_isNewReconciler: enableNewReconciler
          };
          var HooksDispatcherOnMountInDEV = null;
          var HooksDispatcherOnMountWithHookTypesInDEV = null;
          var HooksDispatcherOnUpdateInDEV = null;
          var HooksDispatcherOnRerenderInDEV = null;
          var InvalidNestedHooksDispatcherOnMountInDEV = null;
          var InvalidNestedHooksDispatcherOnUpdateInDEV = null;
          var InvalidNestedHooksDispatcherOnRerenderInDEV = null;
          {
            var warnInvalidContextAccess = function() {
              error("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo().");
            };
            var warnInvalidHookAccess = function() {
              error("Do not call Hooks inside useEffect(...), useMemo(...), or other built-in Hooks. You can only call Hooks at the top level of your React function. For more information, see https://reactjs.org/link/rules-of-hooks");
            };
            HooksDispatcherOnMountInDEV = {
              readContext: function(context) {
                return readContext(context);
              },
              useCallback: function(callback, deps) {
                currentHookNameInDev = "useCallback";
                mountHookTypesDev();
                checkDepsAreArrayDev(deps);
                return mountCallback(callback, deps);
              },
              useContext: function(context) {
                currentHookNameInDev = "useContext";
                mountHookTypesDev();
                return readContext(context);
              },
              useEffect: function(create, deps) {
                currentHookNameInDev = "useEffect";
                mountHookTypesDev();
                checkDepsAreArrayDev(deps);
                return mountEffect(create, deps);
              },
              useImperativeHandle: function(ref, create, deps) {
                currentHookNameInDev = "useImperativeHandle";
                mountHookTypesDev();
                checkDepsAreArrayDev(deps);
                return mountImperativeHandle(ref, create, deps);
              },
              useInsertionEffect: function(create, deps) {
                currentHookNameInDev = "useInsertionEffect";
                mountHookTypesDev();
                checkDepsAreArrayDev(deps);
                return mountInsertionEffect(create, deps);
              },
              useLayoutEffect: function(create, deps) {
                currentHookNameInDev = "useLayoutEffect";
                mountHookTypesDev();
                checkDepsAreArrayDev(deps);
                return mountLayoutEffect(create, deps);
              },
              useMemo: function(create, deps) {
                currentHookNameInDev = "useMemo";
                mountHookTypesDev();
                checkDepsAreArrayDev(deps);
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
                try {
                  return mountMemo(create, deps);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useReducer: function(reducer, initialArg, init) {
                currentHookNameInDev = "useReducer";
                mountHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
                try {
                  return mountReducer(reducer, initialArg, init);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useRef: function(initialValue) {
                currentHookNameInDev = "useRef";
                mountHookTypesDev();
                return mountRef(initialValue);
              },
              useState: function(initialState) {
                currentHookNameInDev = "useState";
                mountHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
                try {
                  return mountState(initialState);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useDebugValue: function(value, formatterFn) {
                currentHookNameInDev = "useDebugValue";
                mountHookTypesDev();
                return mountDebugValue();
              },
              useDeferredValue: function(value) {
                currentHookNameInDev = "useDeferredValue";
                mountHookTypesDev();
                return mountDeferredValue(value);
              },
              useTransition: function() {
                currentHookNameInDev = "useTransition";
                mountHookTypesDev();
                return mountTransition();
              },
              useMutableSource: function(source, getSnapshot, subscribe) {
                currentHookNameInDev = "useMutableSource";
                mountHookTypesDev();
                return mountMutableSource();
              },
              useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
                currentHookNameInDev = "useSyncExternalStore";
                mountHookTypesDev();
                return mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
              },
              useId: function() {
                currentHookNameInDev = "useId";
                mountHookTypesDev();
                return mountId();
              },
              unstable_isNewReconciler: enableNewReconciler
            };
            HooksDispatcherOnMountWithHookTypesInDEV = {
              readContext: function(context) {
                return readContext(context);
              },
              useCallback: function(callback, deps) {
                currentHookNameInDev = "useCallback";
                updateHookTypesDev();
                return mountCallback(callback, deps);
              },
              useContext: function(context) {
                currentHookNameInDev = "useContext";
                updateHookTypesDev();
                return readContext(context);
              },
              useEffect: function(create, deps) {
                currentHookNameInDev = "useEffect";
                updateHookTypesDev();
                return mountEffect(create, deps);
              },
              useImperativeHandle: function(ref, create, deps) {
                currentHookNameInDev = "useImperativeHandle";
                updateHookTypesDev();
                return mountImperativeHandle(ref, create, deps);
              },
              useInsertionEffect: function(create, deps) {
                currentHookNameInDev = "useInsertionEffect";
                updateHookTypesDev();
                return mountInsertionEffect(create, deps);
              },
              useLayoutEffect: function(create, deps) {
                currentHookNameInDev = "useLayoutEffect";
                updateHookTypesDev();
                return mountLayoutEffect(create, deps);
              },
              useMemo: function(create, deps) {
                currentHookNameInDev = "useMemo";
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
                try {
                  return mountMemo(create, deps);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useReducer: function(reducer, initialArg, init) {
                currentHookNameInDev = "useReducer";
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
                try {
                  return mountReducer(reducer, initialArg, init);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useRef: function(initialValue) {
                currentHookNameInDev = "useRef";
                updateHookTypesDev();
                return mountRef(initialValue);
              },
              useState: function(initialState) {
                currentHookNameInDev = "useState";
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
                try {
                  return mountState(initialState);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useDebugValue: function(value, formatterFn) {
                currentHookNameInDev = "useDebugValue";
                updateHookTypesDev();
                return mountDebugValue();
              },
              useDeferredValue: function(value) {
                currentHookNameInDev = "useDeferredValue";
                updateHookTypesDev();
                return mountDeferredValue(value);
              },
              useTransition: function() {
                currentHookNameInDev = "useTransition";
                updateHookTypesDev();
                return mountTransition();
              },
              useMutableSource: function(source, getSnapshot, subscribe) {
                currentHookNameInDev = "useMutableSource";
                updateHookTypesDev();
                return mountMutableSource();
              },
              useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
                currentHookNameInDev = "useSyncExternalStore";
                updateHookTypesDev();
                return mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
              },
              useId: function() {
                currentHookNameInDev = "useId";
                updateHookTypesDev();
                return mountId();
              },
              unstable_isNewReconciler: enableNewReconciler
            };
            HooksDispatcherOnUpdateInDEV = {
              readContext: function(context) {
                return readContext(context);
              },
              useCallback: function(callback, deps) {
                currentHookNameInDev = "useCallback";
                updateHookTypesDev();
                return updateCallback(callback, deps);
              },
              useContext: function(context) {
                currentHookNameInDev = "useContext";
                updateHookTypesDev();
                return readContext(context);
              },
              useEffect: function(create, deps) {
                currentHookNameInDev = "useEffect";
                updateHookTypesDev();
                return updateEffect(create, deps);
              },
              useImperativeHandle: function(ref, create, deps) {
                currentHookNameInDev = "useImperativeHandle";
                updateHookTypesDev();
                return updateImperativeHandle(ref, create, deps);
              },
              useInsertionEffect: function(create, deps) {
                currentHookNameInDev = "useInsertionEffect";
                updateHookTypesDev();
                return updateInsertionEffect(create, deps);
              },
              useLayoutEffect: function(create, deps) {
                currentHookNameInDev = "useLayoutEffect";
                updateHookTypesDev();
                return updateLayoutEffect(create, deps);
              },
              useMemo: function(create, deps) {
                currentHookNameInDev = "useMemo";
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
                try {
                  return updateMemo(create, deps);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useReducer: function(reducer, initialArg, init) {
                currentHookNameInDev = "useReducer";
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
                try {
                  return updateReducer(reducer, initialArg, init);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useRef: function(initialValue) {
                currentHookNameInDev = "useRef";
                updateHookTypesDev();
                return updateRef();
              },
              useState: function(initialState) {
                currentHookNameInDev = "useState";
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
                try {
                  return updateState(initialState);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useDebugValue: function(value, formatterFn) {
                currentHookNameInDev = "useDebugValue";
                updateHookTypesDev();
                return updateDebugValue();
              },
              useDeferredValue: function(value) {
                currentHookNameInDev = "useDeferredValue";
                updateHookTypesDev();
                return updateDeferredValue(value);
              },
              useTransition: function() {
                currentHookNameInDev = "useTransition";
                updateHookTypesDev();
                return updateTransition();
              },
              useMutableSource: function(source, getSnapshot, subscribe) {
                currentHookNameInDev = "useMutableSource";
                updateHookTypesDev();
                return updateMutableSource();
              },
              useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
                currentHookNameInDev = "useSyncExternalStore";
                updateHookTypesDev();
                return updateSyncExternalStore(subscribe, getSnapshot);
              },
              useId: function() {
                currentHookNameInDev = "useId";
                updateHookTypesDev();
                return updateId();
              },
              unstable_isNewReconciler: enableNewReconciler
            };
            HooksDispatcherOnRerenderInDEV = {
              readContext: function(context) {
                return readContext(context);
              },
              useCallback: function(callback, deps) {
                currentHookNameInDev = "useCallback";
                updateHookTypesDev();
                return updateCallback(callback, deps);
              },
              useContext: function(context) {
                currentHookNameInDev = "useContext";
                updateHookTypesDev();
                return readContext(context);
              },
              useEffect: function(create, deps) {
                currentHookNameInDev = "useEffect";
                updateHookTypesDev();
                return updateEffect(create, deps);
              },
              useImperativeHandle: function(ref, create, deps) {
                currentHookNameInDev = "useImperativeHandle";
                updateHookTypesDev();
                return updateImperativeHandle(ref, create, deps);
              },
              useInsertionEffect: function(create, deps) {
                currentHookNameInDev = "useInsertionEffect";
                updateHookTypesDev();
                return updateInsertionEffect(create, deps);
              },
              useLayoutEffect: function(create, deps) {
                currentHookNameInDev = "useLayoutEffect";
                updateHookTypesDev();
                return updateLayoutEffect(create, deps);
              },
              useMemo: function(create, deps) {
                currentHookNameInDev = "useMemo";
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnRerenderInDEV;
                try {
                  return updateMemo(create, deps);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useReducer: function(reducer, initialArg, init) {
                currentHookNameInDev = "useReducer";
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnRerenderInDEV;
                try {
                  return rerenderReducer(reducer, initialArg, init);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useRef: function(initialValue) {
                currentHookNameInDev = "useRef";
                updateHookTypesDev();
                return updateRef();
              },
              useState: function(initialState) {
                currentHookNameInDev = "useState";
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnRerenderInDEV;
                try {
                  return rerenderState(initialState);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useDebugValue: function(value, formatterFn) {
                currentHookNameInDev = "useDebugValue";
                updateHookTypesDev();
                return updateDebugValue();
              },
              useDeferredValue: function(value) {
                currentHookNameInDev = "useDeferredValue";
                updateHookTypesDev();
                return rerenderDeferredValue(value);
              },
              useTransition: function() {
                currentHookNameInDev = "useTransition";
                updateHookTypesDev();
                return rerenderTransition();
              },
              useMutableSource: function(source, getSnapshot, subscribe) {
                currentHookNameInDev = "useMutableSource";
                updateHookTypesDev();
                return updateMutableSource();
              },
              useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
                currentHookNameInDev = "useSyncExternalStore";
                updateHookTypesDev();
                return updateSyncExternalStore(subscribe, getSnapshot);
              },
              useId: function() {
                currentHookNameInDev = "useId";
                updateHookTypesDev();
                return updateId();
              },
              unstable_isNewReconciler: enableNewReconciler
            };
            InvalidNestedHooksDispatcherOnMountInDEV = {
              readContext: function(context) {
                warnInvalidContextAccess();
                return readContext(context);
              },
              useCallback: function(callback, deps) {
                currentHookNameInDev = "useCallback";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return mountCallback(callback, deps);
              },
              useContext: function(context) {
                currentHookNameInDev = "useContext";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return readContext(context);
              },
              useEffect: function(create, deps) {
                currentHookNameInDev = "useEffect";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return mountEffect(create, deps);
              },
              useImperativeHandle: function(ref, create, deps) {
                currentHookNameInDev = "useImperativeHandle";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return mountImperativeHandle(ref, create, deps);
              },
              useInsertionEffect: function(create, deps) {
                currentHookNameInDev = "useInsertionEffect";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return mountInsertionEffect(create, deps);
              },
              useLayoutEffect: function(create, deps) {
                currentHookNameInDev = "useLayoutEffect";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return mountLayoutEffect(create, deps);
              },
              useMemo: function(create, deps) {
                currentHookNameInDev = "useMemo";
                warnInvalidHookAccess();
                mountHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
                try {
                  return mountMemo(create, deps);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useReducer: function(reducer, initialArg, init) {
                currentHookNameInDev = "useReducer";
                warnInvalidHookAccess();
                mountHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
                try {
                  return mountReducer(reducer, initialArg, init);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useRef: function(initialValue) {
                currentHookNameInDev = "useRef";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return mountRef(initialValue);
              },
              useState: function(initialState) {
                currentHookNameInDev = "useState";
                warnInvalidHookAccess();
                mountHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
                try {
                  return mountState(initialState);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useDebugValue: function(value, formatterFn) {
                currentHookNameInDev = "useDebugValue";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return mountDebugValue();
              },
              useDeferredValue: function(value) {
                currentHookNameInDev = "useDeferredValue";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return mountDeferredValue(value);
              },
              useTransition: function() {
                currentHookNameInDev = "useTransition";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return mountTransition();
              },
              useMutableSource: function(source, getSnapshot, subscribe) {
                currentHookNameInDev = "useMutableSource";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return mountMutableSource();
              },
              useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
                currentHookNameInDev = "useSyncExternalStore";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
              },
              useId: function() {
                currentHookNameInDev = "useId";
                warnInvalidHookAccess();
                mountHookTypesDev();
                return mountId();
              },
              unstable_isNewReconciler: enableNewReconciler
            };
            InvalidNestedHooksDispatcherOnUpdateInDEV = {
              readContext: function(context) {
                warnInvalidContextAccess();
                return readContext(context);
              },
              useCallback: function(callback, deps) {
                currentHookNameInDev = "useCallback";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateCallback(callback, deps);
              },
              useContext: function(context) {
                currentHookNameInDev = "useContext";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return readContext(context);
              },
              useEffect: function(create, deps) {
                currentHookNameInDev = "useEffect";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateEffect(create, deps);
              },
              useImperativeHandle: function(ref, create, deps) {
                currentHookNameInDev = "useImperativeHandle";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateImperativeHandle(ref, create, deps);
              },
              useInsertionEffect: function(create, deps) {
                currentHookNameInDev = "useInsertionEffect";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateInsertionEffect(create, deps);
              },
              useLayoutEffect: function(create, deps) {
                currentHookNameInDev = "useLayoutEffect";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateLayoutEffect(create, deps);
              },
              useMemo: function(create, deps) {
                currentHookNameInDev = "useMemo";
                warnInvalidHookAccess();
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
                try {
                  return updateMemo(create, deps);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useReducer: function(reducer, initialArg, init) {
                currentHookNameInDev = "useReducer";
                warnInvalidHookAccess();
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
                try {
                  return updateReducer(reducer, initialArg, init);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useRef: function(initialValue) {
                currentHookNameInDev = "useRef";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateRef();
              },
              useState: function(initialState) {
                currentHookNameInDev = "useState";
                warnInvalidHookAccess();
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
                try {
                  return updateState(initialState);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useDebugValue: function(value, formatterFn) {
                currentHookNameInDev = "useDebugValue";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateDebugValue();
              },
              useDeferredValue: function(value) {
                currentHookNameInDev = "useDeferredValue";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateDeferredValue(value);
              },
              useTransition: function() {
                currentHookNameInDev = "useTransition";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateTransition();
              },
              useMutableSource: function(source, getSnapshot, subscribe) {
                currentHookNameInDev = "useMutableSource";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateMutableSource();
              },
              useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
                currentHookNameInDev = "useSyncExternalStore";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateSyncExternalStore(subscribe, getSnapshot);
              },
              useId: function() {
                currentHookNameInDev = "useId";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateId();
              },
              unstable_isNewReconciler: enableNewReconciler
            };
            InvalidNestedHooksDispatcherOnRerenderInDEV = {
              readContext: function(context) {
                warnInvalidContextAccess();
                return readContext(context);
              },
              useCallback: function(callback, deps) {
                currentHookNameInDev = "useCallback";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateCallback(callback, deps);
              },
              useContext: function(context) {
                currentHookNameInDev = "useContext";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return readContext(context);
              },
              useEffect: function(create, deps) {
                currentHookNameInDev = "useEffect";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateEffect(create, deps);
              },
              useImperativeHandle: function(ref, create, deps) {
                currentHookNameInDev = "useImperativeHandle";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateImperativeHandle(ref, create, deps);
              },
              useInsertionEffect: function(create, deps) {
                currentHookNameInDev = "useInsertionEffect";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateInsertionEffect(create, deps);
              },
              useLayoutEffect: function(create, deps) {
                currentHookNameInDev = "useLayoutEffect";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateLayoutEffect(create, deps);
              },
              useMemo: function(create, deps) {
                currentHookNameInDev = "useMemo";
                warnInvalidHookAccess();
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
                try {
                  return updateMemo(create, deps);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useReducer: function(reducer, initialArg, init) {
                currentHookNameInDev = "useReducer";
                warnInvalidHookAccess();
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
                try {
                  return rerenderReducer(reducer, initialArg, init);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useRef: function(initialValue) {
                currentHookNameInDev = "useRef";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateRef();
              },
              useState: function(initialState) {
                currentHookNameInDev = "useState";
                warnInvalidHookAccess();
                updateHookTypesDev();
                var prevDispatcher = ReactCurrentDispatcher$1.current;
                ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
                try {
                  return rerenderState(initialState);
                } finally {
                  ReactCurrentDispatcher$1.current = prevDispatcher;
                }
              },
              useDebugValue: function(value, formatterFn) {
                currentHookNameInDev = "useDebugValue";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateDebugValue();
              },
              useDeferredValue: function(value) {
                currentHookNameInDev = "useDeferredValue";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return rerenderDeferredValue(value);
              },
              useTransition: function() {
                currentHookNameInDev = "useTransition";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return rerenderTransition();
              },
              useMutableSource: function(source, getSnapshot, subscribe) {
                currentHookNameInDev = "useMutableSource";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateMutableSource();
              },
              useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
                currentHookNameInDev = "useSyncExternalStore";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateSyncExternalStore(subscribe, getSnapshot);
              },
              useId: function() {
                currentHookNameInDev = "useId";
                warnInvalidHookAccess();
                updateHookTypesDev();
                return updateId();
              },
              unstable_isNewReconciler: enableNewReconciler
            };
          }
          var now$1 = Scheduler.unstable_now;
          var commitTime = 0;
          var layoutEffectStartTime = -1;
          var profilerStartTime = -1;
          var passiveEffectStartTime = -1;
          var currentUpdateIsNested = false;
          var nestedUpdateScheduled = false;
          function isCurrentUpdateNested() {
            return currentUpdateIsNested;
          }
          function markNestedUpdateScheduled() {
            {
              nestedUpdateScheduled = true;
            }
          }
          function resetNestedUpdateFlag() {
            {
              currentUpdateIsNested = false;
              nestedUpdateScheduled = false;
            }
          }
          function syncNestedUpdateFlag() {
            {
              currentUpdateIsNested = nestedUpdateScheduled;
              nestedUpdateScheduled = false;
            }
          }
          function getCommitTime() {
            return commitTime;
          }
          function recordCommitTime() {
            commitTime = now$1();
          }
          function startProfilerTimer(fiber) {
            profilerStartTime = now$1();
            if (fiber.actualStartTime < 0) {
              fiber.actualStartTime = now$1();
            }
          }
          function stopProfilerTimerIfRunning(fiber) {
            profilerStartTime = -1;
          }
          function stopProfilerTimerIfRunningAndRecordDelta(fiber, overrideBaseTime) {
            if (profilerStartTime >= 0) {
              var elapsedTime = now$1() - profilerStartTime;
              fiber.actualDuration += elapsedTime;
              if (overrideBaseTime) {
                fiber.selfBaseDuration = elapsedTime;
              }
              profilerStartTime = -1;
            }
          }
          function recordLayoutEffectDuration(fiber) {
            if (layoutEffectStartTime >= 0) {
              var elapsedTime = now$1() - layoutEffectStartTime;
              layoutEffectStartTime = -1;
              var parentFiber = fiber.return;
              while (parentFiber !== null) {
                switch (parentFiber.tag) {
                  case HostRoot:
                    var root3 = parentFiber.stateNode;
                    root3.effectDuration += elapsedTime;
                    return;
                  case Profiler:
                    var parentStateNode = parentFiber.stateNode;
                    parentStateNode.effectDuration += elapsedTime;
                    return;
                }
                parentFiber = parentFiber.return;
              }
            }
          }
          function recordPassiveEffectDuration(fiber) {
            if (passiveEffectStartTime >= 0) {
              var elapsedTime = now$1() - passiveEffectStartTime;
              passiveEffectStartTime = -1;
              var parentFiber = fiber.return;
              while (parentFiber !== null) {
                switch (parentFiber.tag) {
                  case HostRoot:
                    var root3 = parentFiber.stateNode;
                    if (root3 !== null) {
                      root3.passiveEffectDuration += elapsedTime;
                    }
                    return;
                  case Profiler:
                    var parentStateNode = parentFiber.stateNode;
                    if (parentStateNode !== null) {
                      parentStateNode.passiveEffectDuration += elapsedTime;
                    }
                    return;
                }
                parentFiber = parentFiber.return;
              }
            }
          }
          function startLayoutEffectTimer() {
            layoutEffectStartTime = now$1();
          }
          function startPassiveEffectTimer() {
            passiveEffectStartTime = now$1();
          }
          function transferActualDuration(fiber) {
            var child = fiber.child;
            while (child) {
              fiber.actualDuration += child.actualDuration;
              child = child.sibling;
            }
          }
          function resolveDefaultProps(Component, baseProps) {
            if (Component && Component.defaultProps) {
              var props = assign({}, baseProps);
              var defaultProps = Component.defaultProps;
              for (var propName in defaultProps) {
                if (props[propName] === void 0) {
                  props[propName] = defaultProps[propName];
                }
              }
              return props;
            }
            return baseProps;
          }
          var fakeInternalInstance = {};
          var didWarnAboutStateAssignmentForComponent;
          var didWarnAboutUninitializedState;
          var didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate;
          var didWarnAboutLegacyLifecyclesAndDerivedState;
          var didWarnAboutUndefinedDerivedState;
          var warnOnUndefinedDerivedState;
          var warnOnInvalidCallback;
          var didWarnAboutDirectlyAssigningPropsToState;
          var didWarnAboutContextTypeAndContextTypes;
          var didWarnAboutInvalidateContextType;
          var didWarnAboutLegacyContext$1;
          {
            didWarnAboutStateAssignmentForComponent = /* @__PURE__ */ new Set();
            didWarnAboutUninitializedState = /* @__PURE__ */ new Set();
            didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate = /* @__PURE__ */ new Set();
            didWarnAboutLegacyLifecyclesAndDerivedState = /* @__PURE__ */ new Set();
            didWarnAboutDirectlyAssigningPropsToState = /* @__PURE__ */ new Set();
            didWarnAboutUndefinedDerivedState = /* @__PURE__ */ new Set();
            didWarnAboutContextTypeAndContextTypes = /* @__PURE__ */ new Set();
            didWarnAboutInvalidateContextType = /* @__PURE__ */ new Set();
            didWarnAboutLegacyContext$1 = /* @__PURE__ */ new Set();
            var didWarnOnInvalidCallback = /* @__PURE__ */ new Set();
            warnOnInvalidCallback = function(callback, callerName) {
              if (callback === null || typeof callback === "function") {
                return;
              }
              var key = callerName + "_" + callback;
              if (!didWarnOnInvalidCallback.has(key)) {
                didWarnOnInvalidCallback.add(key);
                error("%s(...): Expected the last optional `callback` argument to be a function. Instead received: %s.", callerName, callback);
              }
            };
            warnOnUndefinedDerivedState = function(type, partialState) {
              if (partialState === void 0) {
                var componentName = getComponentNameFromType(type) || "Component";
                if (!didWarnAboutUndefinedDerivedState.has(componentName)) {
                  didWarnAboutUndefinedDerivedState.add(componentName);
                  error("%s.getDerivedStateFromProps(): A valid state object (or null) must be returned. You have returned undefined.", componentName);
                }
              }
            };
            Object.defineProperty(fakeInternalInstance, "_processChildContext", {
              enumerable: false,
              value: function() {
                throw new Error("_processChildContext is not available in React 16+. This likely means you have multiple copies of React and are attempting to nest a React 15 tree inside a React 16 tree using unstable_renderSubtreeIntoContainer, which isn't supported. Try to make sure you have only one copy of React (and ideally, switch to ReactDOM.createPortal).");
              }
            });
            Object.freeze(fakeInternalInstance);
          }
          function applyDerivedStateFromProps(workInProgress2, ctor, getDerivedStateFromProps, nextProps) {
            var prevState = workInProgress2.memoizedState;
            var partialState = getDerivedStateFromProps(nextProps, prevState);
            {
              if (workInProgress2.mode & StrictLegacyMode) {
                setIsStrictModeForDevtools(true);
                try {
                  partialState = getDerivedStateFromProps(nextProps, prevState);
                } finally {
                  setIsStrictModeForDevtools(false);
                }
              }
              warnOnUndefinedDerivedState(ctor, partialState);
            }
            var memoizedState = partialState === null || partialState === void 0 ? prevState : assign({}, prevState, partialState);
            workInProgress2.memoizedState = memoizedState;
            if (workInProgress2.lanes === NoLanes) {
              var updateQueue = workInProgress2.updateQueue;
              updateQueue.baseState = memoizedState;
            }
          }
          var classComponentUpdater = {
            isMounted,
            enqueueSetState: function(inst, payload, callback) {
              var fiber = get(inst);
              var eventTime = requestEventTime();
              var lane = requestUpdateLane(fiber);
              var update = createUpdate(eventTime, lane);
              update.payload = payload;
              if (callback !== void 0 && callback !== null) {
                {
                  warnOnInvalidCallback(callback, "setState");
                }
                update.callback = callback;
              }
              var root3 = enqueueUpdate(fiber, update, lane);
              if (root3 !== null) {
                scheduleUpdateOnFiber(root3, fiber, lane, eventTime);
                entangleTransitions(root3, fiber, lane);
              }
              {
                markStateUpdateScheduled(fiber, lane);
              }
            },
            enqueueReplaceState: function(inst, payload, callback) {
              var fiber = get(inst);
              var eventTime = requestEventTime();
              var lane = requestUpdateLane(fiber);
              var update = createUpdate(eventTime, lane);
              update.tag = ReplaceState;
              update.payload = payload;
              if (callback !== void 0 && callback !== null) {
                {
                  warnOnInvalidCallback(callback, "replaceState");
                }
                update.callback = callback;
              }
              var root3 = enqueueUpdate(fiber, update, lane);
              if (root3 !== null) {
                scheduleUpdateOnFiber(root3, fiber, lane, eventTime);
                entangleTransitions(root3, fiber, lane);
              }
              {
                markStateUpdateScheduled(fiber, lane);
              }
            },
            enqueueForceUpdate: function(inst, callback) {
              var fiber = get(inst);
              var eventTime = requestEventTime();
              var lane = requestUpdateLane(fiber);
              var update = createUpdate(eventTime, lane);
              update.tag = ForceUpdate;
              if (callback !== void 0 && callback !== null) {
                {
                  warnOnInvalidCallback(callback, "forceUpdate");
                }
                update.callback = callback;
              }
              var root3 = enqueueUpdate(fiber, update, lane);
              if (root3 !== null) {
                scheduleUpdateOnFiber(root3, fiber, lane, eventTime);
                entangleTransitions(root3, fiber, lane);
              }
              {
                markForceUpdateScheduled(fiber, lane);
              }
            }
          };
          function checkShouldComponentUpdate(workInProgress2, ctor, oldProps, newProps, oldState, newState, nextContext) {
            var instance = workInProgress2.stateNode;
            if (typeof instance.shouldComponentUpdate === "function") {
              var shouldUpdate = instance.shouldComponentUpdate(newProps, newState, nextContext);
              {
                if (workInProgress2.mode & StrictLegacyMode) {
                  setIsStrictModeForDevtools(true);
                  try {
                    shouldUpdate = instance.shouldComponentUpdate(newProps, newState, nextContext);
                  } finally {
                    setIsStrictModeForDevtools(false);
                  }
                }
                if (shouldUpdate === void 0) {
                  error("%s.shouldComponentUpdate(): Returned undefined instead of a boolean value. Make sure to return true or false.", getComponentNameFromType(ctor) || "Component");
                }
              }
              return shouldUpdate;
            }
            if (ctor.prototype && ctor.prototype.isPureReactComponent) {
              return !shallowEqual(oldProps, newProps) || !shallowEqual(oldState, newState);
            }
            return true;
          }
          function checkClassInstance(workInProgress2, ctor, newProps) {
            var instance = workInProgress2.stateNode;
            {
              var name = getComponentNameFromType(ctor) || "Component";
              var renderPresent = instance.render;
              if (!renderPresent) {
                if (ctor.prototype && typeof ctor.prototype.render === "function") {
                  error("%s(...): No `render` method found on the returned component instance: did you accidentally return an object from the constructor?", name);
                } else {
                  error("%s(...): No `render` method found on the returned component instance: you may have forgotten to define `render`.", name);
                }
              }
              if (instance.getInitialState && !instance.getInitialState.isReactClassApproved && !instance.state) {
                error("getInitialState was defined on %s, a plain JavaScript class. This is only supported for classes created using React.createClass. Did you mean to define a state property instead?", name);
              }
              if (instance.getDefaultProps && !instance.getDefaultProps.isReactClassApproved) {
                error("getDefaultProps was defined on %s, a plain JavaScript class. This is only supported for classes created using React.createClass. Use a static property to define defaultProps instead.", name);
              }
              if (instance.propTypes) {
                error("propTypes was defined as an instance property on %s. Use a static property to define propTypes instead.", name);
              }
              if (instance.contextType) {
                error("contextType was defined as an instance property on %s. Use a static property to define contextType instead.", name);
              }
              {
                if (ctor.childContextTypes && !didWarnAboutLegacyContext$1.has(ctor) && // Strict Mode has its own warning for legacy context, so we can skip
                // this one.
                (workInProgress2.mode & StrictLegacyMode) === NoMode) {
                  didWarnAboutLegacyContext$1.add(ctor);
                  error("%s uses the legacy childContextTypes API which is no longer supported and will be removed in the next major release. Use React.createContext() instead\n\n.Learn more about this warning here: https://reactjs.org/link/legacy-context", name);
                }
                if (ctor.contextTypes && !didWarnAboutLegacyContext$1.has(ctor) && // Strict Mode has its own warning for legacy context, so we can skip
                // this one.
                (workInProgress2.mode & StrictLegacyMode) === NoMode) {
                  didWarnAboutLegacyContext$1.add(ctor);
                  error("%s uses the legacy contextTypes API which is no longer supported and will be removed in the next major release. Use React.createContext() with static contextType instead.\n\nLearn more about this warning here: https://reactjs.org/link/legacy-context", name);
                }
                if (instance.contextTypes) {
                  error("contextTypes was defined as an instance property on %s. Use a static property to define contextTypes instead.", name);
                }
                if (ctor.contextType && ctor.contextTypes && !didWarnAboutContextTypeAndContextTypes.has(ctor)) {
                  didWarnAboutContextTypeAndContextTypes.add(ctor);
                  error("%s declares both contextTypes and contextType static properties. The legacy contextTypes property will be ignored.", name);
                }
              }
              if (typeof instance.componentShouldUpdate === "function") {
                error("%s has a method called componentShouldUpdate(). Did you mean shouldComponentUpdate()? The name is phrased as a question because the function is expected to return a value.", name);
              }
              if (ctor.prototype && ctor.prototype.isPureReactComponent && typeof instance.shouldComponentUpdate !== "undefined") {
                error("%s has a method called shouldComponentUpdate(). shouldComponentUpdate should not be used when extending React.PureComponent. Please extend React.Component if shouldComponentUpdate is used.", getComponentNameFromType(ctor) || "A pure component");
              }
              if (typeof instance.componentDidUnmount === "function") {
                error("%s has a method called componentDidUnmount(). But there is no such lifecycle method. Did you mean componentWillUnmount()?", name);
              }
              if (typeof instance.componentDidReceiveProps === "function") {
                error("%s has a method called componentDidReceiveProps(). But there is no such lifecycle method. If you meant to update the state in response to changing props, use componentWillReceiveProps(). If you meant to fetch data or run side-effects or mutations after React has updated the UI, use componentDidUpdate().", name);
              }
              if (typeof instance.componentWillRecieveProps === "function") {
                error("%s has a method called componentWillRecieveProps(). Did you mean componentWillReceiveProps()?", name);
              }
              if (typeof instance.UNSAFE_componentWillRecieveProps === "function") {
                error("%s has a method called UNSAFE_componentWillRecieveProps(). Did you mean UNSAFE_componentWillReceiveProps()?", name);
              }
              var hasMutatedProps = instance.props !== newProps;
              if (instance.props !== void 0 && hasMutatedProps) {
                error("%s(...): When calling super() in `%s`, make sure to pass up the same props that your component's constructor was passed.", name, name);
              }
              if (instance.defaultProps) {
                error("Setting defaultProps as an instance property on %s is not supported and will be ignored. Instead, define defaultProps as a static property on %s.", name, name);
              }
              if (typeof instance.getSnapshotBeforeUpdate === "function" && typeof instance.componentDidUpdate !== "function" && !didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.has(ctor)) {
                didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.add(ctor);
                error("%s: getSnapshotBeforeUpdate() should be used with componentDidUpdate(). This component defines getSnapshotBeforeUpdate() only.", getComponentNameFromType(ctor));
              }
              if (typeof instance.getDerivedStateFromProps === "function") {
                error("%s: getDerivedStateFromProps() is defined as an instance method and will be ignored. Instead, declare it as a static method.", name);
              }
              if (typeof instance.getDerivedStateFromError === "function") {
                error("%s: getDerivedStateFromError() is defined as an instance method and will be ignored. Instead, declare it as a static method.", name);
              }
              if (typeof ctor.getSnapshotBeforeUpdate === "function") {
                error("%s: getSnapshotBeforeUpdate() is defined as a static method and will be ignored. Instead, declare it as an instance method.", name);
              }
              var _state = instance.state;
              if (_state && (typeof _state !== "object" || isArray(_state))) {
                error("%s.state: must be set to an object or null", name);
              }
              if (typeof instance.getChildContext === "function" && typeof ctor.childContextTypes !== "object") {
                error("%s.getChildContext(): childContextTypes must be defined in order to use getChildContext().", name);
              }
            }
          }
          function adoptClassInstance(workInProgress2, instance) {
            instance.updater = classComponentUpdater;
            workInProgress2.stateNode = instance;
            set(instance, workInProgress2);
            {
              instance._reactInternalInstance = fakeInternalInstance;
            }
          }
          function constructClassInstance(workInProgress2, ctor, props) {
            var isLegacyContextConsumer = false;
            var unmaskedContext = emptyContextObject;
            var context = emptyContextObject;
            var contextType = ctor.contextType;
            {
              if ("contextType" in ctor) {
                var isValid = (
                  // Allow null for conditional declaration
                  contextType === null || contextType !== void 0 && contextType.$$typeof === REACT_CONTEXT_TYPE && contextType._context === void 0
                );
                if (!isValid && !didWarnAboutInvalidateContextType.has(ctor)) {
                  didWarnAboutInvalidateContextType.add(ctor);
                  var addendum = "";
                  if (contextType === void 0) {
                    addendum = " However, it is set to undefined. This can be caused by a typo or by mixing up named and default imports. This can also happen due to a circular dependency, so try moving the createContext() call to a separate file.";
                  } else if (typeof contextType !== "object") {
                    addendum = " However, it is set to a " + typeof contextType + ".";
                  } else if (contextType.$$typeof === REACT_PROVIDER_TYPE) {
                    addendum = " Did you accidentally pass the Context.Provider instead?";
                  } else if (contextType._context !== void 0) {
                    addendum = " Did you accidentally pass the Context.Consumer instead?";
                  } else {
                    addendum = " However, it is set to an object with keys {" + Object.keys(contextType).join(", ") + "}.";
                  }
                  error("%s defines an invalid contextType. contextType should point to the Context object returned by React.createContext().%s", getComponentNameFromType(ctor) || "Component", addendum);
                }
              }
            }
            if (typeof contextType === "object" && contextType !== null) {
              context = readContext(contextType);
            } else {
              unmaskedContext = getUnmaskedContext(workInProgress2, ctor, true);
              var contextTypes = ctor.contextTypes;
              isLegacyContextConsumer = contextTypes !== null && contextTypes !== void 0;
              context = isLegacyContextConsumer ? getMaskedContext(workInProgress2, unmaskedContext) : emptyContextObject;
            }
            var instance = new ctor(props, context);
            {
              if (workInProgress2.mode & StrictLegacyMode) {
                setIsStrictModeForDevtools(true);
                try {
                  instance = new ctor(props, context);
                } finally {
                  setIsStrictModeForDevtools(false);
                }
              }
            }
            var state = workInProgress2.memoizedState = instance.state !== null && instance.state !== void 0 ? instance.state : null;
            adoptClassInstance(workInProgress2, instance);
            {
              if (typeof ctor.getDerivedStateFromProps === "function" && state === null) {
                var componentName = getComponentNameFromType(ctor) || "Component";
                if (!didWarnAboutUninitializedState.has(componentName)) {
                  didWarnAboutUninitializedState.add(componentName);
                  error("`%s` uses `getDerivedStateFromProps` but its initial state is %s. This is not recommended. Instead, define the initial state by assigning an object to `this.state` in the constructor of `%s`. This ensures that `getDerivedStateFromProps` arguments have a consistent shape.", componentName, instance.state === null ? "null" : "undefined", componentName);
                }
              }
              if (typeof ctor.getDerivedStateFromProps === "function" || typeof instance.getSnapshotBeforeUpdate === "function") {
                var foundWillMountName = null;
                var foundWillReceivePropsName = null;
                var foundWillUpdateName = null;
                if (typeof instance.componentWillMount === "function" && instance.componentWillMount.__suppressDeprecationWarning !== true) {
                  foundWillMountName = "componentWillMount";
                } else if (typeof instance.UNSAFE_componentWillMount === "function") {
                  foundWillMountName = "UNSAFE_componentWillMount";
                }
                if (typeof instance.componentWillReceiveProps === "function" && instance.componentWillReceiveProps.__suppressDeprecationWarning !== true) {
                  foundWillReceivePropsName = "componentWillReceiveProps";
                } else if (typeof instance.UNSAFE_componentWillReceiveProps === "function") {
                  foundWillReceivePropsName = "UNSAFE_componentWillReceiveProps";
                }
                if (typeof instance.componentWillUpdate === "function" && instance.componentWillUpdate.__suppressDeprecationWarning !== true) {
                  foundWillUpdateName = "componentWillUpdate";
                } else if (typeof instance.UNSAFE_componentWillUpdate === "function") {
                  foundWillUpdateName = "UNSAFE_componentWillUpdate";
                }
                if (foundWillMountName !== null || foundWillReceivePropsName !== null || foundWillUpdateName !== null) {
                  var _componentName = getComponentNameFromType(ctor) || "Component";
                  var newApiName = typeof ctor.getDerivedStateFromProps === "function" ? "getDerivedStateFromProps()" : "getSnapshotBeforeUpdate()";
                  if (!didWarnAboutLegacyLifecyclesAndDerivedState.has(_componentName)) {
                    didWarnAboutLegacyLifecyclesAndDerivedState.add(_componentName);
                    error("Unsafe legacy lifecycles will not be called for components using new component APIs.\n\n%s uses %s but also contains the following legacy lifecycles:%s%s%s\n\nThe above lifecycles should be removed. Learn more about this warning here:\nhttps://reactjs.org/link/unsafe-component-lifecycles", _componentName, newApiName, foundWillMountName !== null ? "\n  " + foundWillMountName : "", foundWillReceivePropsName !== null ? "\n  " + foundWillReceivePropsName : "", foundWillUpdateName !== null ? "\n  " + foundWillUpdateName : "");
                  }
                }
              }
            }
            if (isLegacyContextConsumer) {
              cacheContext(workInProgress2, unmaskedContext, context);
            }
            return instance;
          }
          function callComponentWillMount(workInProgress2, instance) {
            var oldState = instance.state;
            if (typeof instance.componentWillMount === "function") {
              instance.componentWillMount();
            }
            if (typeof instance.UNSAFE_componentWillMount === "function") {
              instance.UNSAFE_componentWillMount();
            }
            if (oldState !== instance.state) {
              {
                error("%s.componentWillMount(): Assigning directly to this.state is deprecated (except inside a component's constructor). Use setState instead.", getComponentNameFromFiber(workInProgress2) || "Component");
              }
              classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
            }
          }
          function callComponentWillReceiveProps(workInProgress2, instance, newProps, nextContext) {
            var oldState = instance.state;
            if (typeof instance.componentWillReceiveProps === "function") {
              instance.componentWillReceiveProps(newProps, nextContext);
            }
            if (typeof instance.UNSAFE_componentWillReceiveProps === "function") {
              instance.UNSAFE_componentWillReceiveProps(newProps, nextContext);
            }
            if (instance.state !== oldState) {
              {
                var componentName = getComponentNameFromFiber(workInProgress2) || "Component";
                if (!didWarnAboutStateAssignmentForComponent.has(componentName)) {
                  didWarnAboutStateAssignmentForComponent.add(componentName);
                  error("%s.componentWillReceiveProps(): Assigning directly to this.state is deprecated (except inside a component's constructor). Use setState instead.", componentName);
                }
              }
              classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
            }
          }
          function mountClassInstance(workInProgress2, ctor, newProps, renderLanes2) {
            {
              checkClassInstance(workInProgress2, ctor, newProps);
            }
            var instance = workInProgress2.stateNode;
            instance.props = newProps;
            instance.state = workInProgress2.memoizedState;
            instance.refs = {};
            initializeUpdateQueue(workInProgress2);
            var contextType = ctor.contextType;
            if (typeof contextType === "object" && contextType !== null) {
              instance.context = readContext(contextType);
            } else {
              var unmaskedContext = getUnmaskedContext(workInProgress2, ctor, true);
              instance.context = getMaskedContext(workInProgress2, unmaskedContext);
            }
            {
              if (instance.state === newProps) {
                var componentName = getComponentNameFromType(ctor) || "Component";
                if (!didWarnAboutDirectlyAssigningPropsToState.has(componentName)) {
                  didWarnAboutDirectlyAssigningPropsToState.add(componentName);
                  error("%s: It is not recommended to assign props directly to state because updates to props won't be reflected in state. In most cases, it is better to use props directly.", componentName);
                }
              }
              if (workInProgress2.mode & StrictLegacyMode) {
                ReactStrictModeWarnings.recordLegacyContextWarning(workInProgress2, instance);
              }
              {
                ReactStrictModeWarnings.recordUnsafeLifecycleWarnings(workInProgress2, instance);
              }
            }
            instance.state = workInProgress2.memoizedState;
            var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
            if (typeof getDerivedStateFromProps === "function") {
              applyDerivedStateFromProps(workInProgress2, ctor, getDerivedStateFromProps, newProps);
              instance.state = workInProgress2.memoizedState;
            }
            if (typeof ctor.getDerivedStateFromProps !== "function" && typeof instance.getSnapshotBeforeUpdate !== "function" && (typeof instance.UNSAFE_componentWillMount === "function" || typeof instance.componentWillMount === "function")) {
              callComponentWillMount(workInProgress2, instance);
              processUpdateQueue(workInProgress2, newProps, instance, renderLanes2);
              instance.state = workInProgress2.memoizedState;
            }
            if (typeof instance.componentDidMount === "function") {
              var fiberFlags = Update;
              {
                fiberFlags |= LayoutStatic;
              }
              if ((workInProgress2.mode & StrictEffectsMode) !== NoMode) {
                fiberFlags |= MountLayoutDev;
              }
              workInProgress2.flags |= fiberFlags;
            }
          }
          function resumeMountClassInstance(workInProgress2, ctor, newProps, renderLanes2) {
            var instance = workInProgress2.stateNode;
            var oldProps = workInProgress2.memoizedProps;
            instance.props = oldProps;
            var oldContext = instance.context;
            var contextType = ctor.contextType;
            var nextContext = emptyContextObject;
            if (typeof contextType === "object" && contextType !== null) {
              nextContext = readContext(contextType);
            } else {
              var nextLegacyUnmaskedContext = getUnmaskedContext(workInProgress2, ctor, true);
              nextContext = getMaskedContext(workInProgress2, nextLegacyUnmaskedContext);
            }
            var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
            var hasNewLifecycles = typeof getDerivedStateFromProps === "function" || typeof instance.getSnapshotBeforeUpdate === "function";
            if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillReceiveProps === "function" || typeof instance.componentWillReceiveProps === "function")) {
              if (oldProps !== newProps || oldContext !== nextContext) {
                callComponentWillReceiveProps(workInProgress2, instance, newProps, nextContext);
              }
            }
            resetHasForceUpdateBeforeProcessing();
            var oldState = workInProgress2.memoizedState;
            var newState = instance.state = oldState;
            processUpdateQueue(workInProgress2, newProps, instance, renderLanes2);
            newState = workInProgress2.memoizedState;
            if (oldProps === newProps && oldState === newState && !hasContextChanged() && !checkHasForceUpdateAfterProcessing()) {
              if (typeof instance.componentDidMount === "function") {
                var fiberFlags = Update;
                {
                  fiberFlags |= LayoutStatic;
                }
                if ((workInProgress2.mode & StrictEffectsMode) !== NoMode) {
                  fiberFlags |= MountLayoutDev;
                }
                workInProgress2.flags |= fiberFlags;
              }
              return false;
            }
            if (typeof getDerivedStateFromProps === "function") {
              applyDerivedStateFromProps(workInProgress2, ctor, getDerivedStateFromProps, newProps);
              newState = workInProgress2.memoizedState;
            }
            var shouldUpdate = checkHasForceUpdateAfterProcessing() || checkShouldComponentUpdate(workInProgress2, ctor, oldProps, newProps, oldState, newState, nextContext);
            if (shouldUpdate) {
              if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillMount === "function" || typeof instance.componentWillMount === "function")) {
                if (typeof instance.componentWillMount === "function") {
                  instance.componentWillMount();
                }
                if (typeof instance.UNSAFE_componentWillMount === "function") {
                  instance.UNSAFE_componentWillMount();
                }
              }
              if (typeof instance.componentDidMount === "function") {
                var _fiberFlags = Update;
                {
                  _fiberFlags |= LayoutStatic;
                }
                if ((workInProgress2.mode & StrictEffectsMode) !== NoMode) {
                  _fiberFlags |= MountLayoutDev;
                }
                workInProgress2.flags |= _fiberFlags;
              }
            } else {
              if (typeof instance.componentDidMount === "function") {
                var _fiberFlags2 = Update;
                {
                  _fiberFlags2 |= LayoutStatic;
                }
                if ((workInProgress2.mode & StrictEffectsMode) !== NoMode) {
                  _fiberFlags2 |= MountLayoutDev;
                }
                workInProgress2.flags |= _fiberFlags2;
              }
              workInProgress2.memoizedProps = newProps;
              workInProgress2.memoizedState = newState;
            }
            instance.props = newProps;
            instance.state = newState;
            instance.context = nextContext;
            return shouldUpdate;
          }
          function updateClassInstance(current2, workInProgress2, ctor, newProps, renderLanes2) {
            var instance = workInProgress2.stateNode;
            cloneUpdateQueue(current2, workInProgress2);
            var unresolvedOldProps = workInProgress2.memoizedProps;
            var oldProps = workInProgress2.type === workInProgress2.elementType ? unresolvedOldProps : resolveDefaultProps(workInProgress2.type, unresolvedOldProps);
            instance.props = oldProps;
            var unresolvedNewProps = workInProgress2.pendingProps;
            var oldContext = instance.context;
            var contextType = ctor.contextType;
            var nextContext = emptyContextObject;
            if (typeof contextType === "object" && contextType !== null) {
              nextContext = readContext(contextType);
            } else {
              var nextUnmaskedContext = getUnmaskedContext(workInProgress2, ctor, true);
              nextContext = getMaskedContext(workInProgress2, nextUnmaskedContext);
            }
            var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
            var hasNewLifecycles = typeof getDerivedStateFromProps === "function" || typeof instance.getSnapshotBeforeUpdate === "function";
            if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillReceiveProps === "function" || typeof instance.componentWillReceiveProps === "function")) {
              if (unresolvedOldProps !== unresolvedNewProps || oldContext !== nextContext) {
                callComponentWillReceiveProps(workInProgress2, instance, newProps, nextContext);
              }
            }
            resetHasForceUpdateBeforeProcessing();
            var oldState = workInProgress2.memoizedState;
            var newState = instance.state = oldState;
            processUpdateQueue(workInProgress2, newProps, instance, renderLanes2);
            newState = workInProgress2.memoizedState;
            if (unresolvedOldProps === unresolvedNewProps && oldState === newState && !hasContextChanged() && !checkHasForceUpdateAfterProcessing() && !enableLazyContextPropagation) {
              if (typeof instance.componentDidUpdate === "function") {
                if (unresolvedOldProps !== current2.memoizedProps || oldState !== current2.memoizedState) {
                  workInProgress2.flags |= Update;
                }
              }
              if (typeof instance.getSnapshotBeforeUpdate === "function") {
                if (unresolvedOldProps !== current2.memoizedProps || oldState !== current2.memoizedState) {
                  workInProgress2.flags |= Snapshot;
                }
              }
              return false;
            }
            if (typeof getDerivedStateFromProps === "function") {
              applyDerivedStateFromProps(workInProgress2, ctor, getDerivedStateFromProps, newProps);
              newState = workInProgress2.memoizedState;
            }
            var shouldUpdate = checkHasForceUpdateAfterProcessing() || checkShouldComponentUpdate(workInProgress2, ctor, oldProps, newProps, oldState, newState, nextContext) || // TODO: In some cases, we'll end up checking if context has changed twice,
            // both before and after `shouldComponentUpdate` has been called. Not ideal,
            // but I'm loath to refactor this function. This only happens for memoized
            // components so it's not that common.
            enableLazyContextPropagation;
            if (shouldUpdate) {
              if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillUpdate === "function" || typeof instance.componentWillUpdate === "function")) {
                if (typeof instance.componentWillUpdate === "function") {
                  instance.componentWillUpdate(newProps, newState, nextContext);
                }
                if (typeof instance.UNSAFE_componentWillUpdate === "function") {
                  instance.UNSAFE_componentWillUpdate(newProps, newState, nextContext);
                }
              }
              if (typeof instance.componentDidUpdate === "function") {
                workInProgress2.flags |= Update;
              }
              if (typeof instance.getSnapshotBeforeUpdate === "function") {
                workInProgress2.flags |= Snapshot;
              }
            } else {
              if (typeof instance.componentDidUpdate === "function") {
                if (unresolvedOldProps !== current2.memoizedProps || oldState !== current2.memoizedState) {
                  workInProgress2.flags |= Update;
                }
              }
              if (typeof instance.getSnapshotBeforeUpdate === "function") {
                if (unresolvedOldProps !== current2.memoizedProps || oldState !== current2.memoizedState) {
                  workInProgress2.flags |= Snapshot;
                }
              }
              workInProgress2.memoizedProps = newProps;
              workInProgress2.memoizedState = newState;
            }
            instance.props = newProps;
            instance.state = newState;
            instance.context = nextContext;
            return shouldUpdate;
          }
          function createCapturedValueAtFiber(value, source) {
            return {
              value,
              source,
              stack: getStackByFiberInDevAndProd(source),
              digest: null
            };
          }
          function createCapturedValue(value, digest, stack) {
            return {
              value,
              source: null,
              stack: stack != null ? stack : null,
              digest: digest != null ? digest : null
            };
          }
          function showErrorDialog(boundary, errorInfo) {
            return true;
          }
          function logCapturedError(boundary, errorInfo) {
            try {
              var logError = showErrorDialog(boundary, errorInfo);
              if (logError === false) {
                return;
              }
              var error2 = errorInfo.value;
              if (true) {
                var source = errorInfo.source;
                var stack = errorInfo.stack;
                var componentStack = stack !== null ? stack : "";
                if (error2 != null && error2._suppressLogging) {
                  if (boundary.tag === ClassComponent) {
                    return;
                  }
                  console["error"](error2);
                }
                var componentName = source ? getComponentNameFromFiber(source) : null;
                var componentNameMessage = componentName ? "The above error occurred in the <" + componentName + "> component:" : "The above error occurred in one of your React components:";
                var errorBoundaryMessage;
                if (boundary.tag === HostRoot) {
                  errorBoundaryMessage = "Consider adding an error boundary to your tree to customize error handling behavior.\nVisit https://reactjs.org/link/error-boundaries to learn more about error boundaries.";
                } else {
                  var errorBoundaryName = getComponentNameFromFiber(boundary) || "Anonymous";
                  errorBoundaryMessage = "React will try to recreate this component tree from scratch " + ("using the error boundary you provided, " + errorBoundaryName + ".");
                }
                var combinedMessage = componentNameMessage + "\n" + componentStack + "\n\n" + ("" + errorBoundaryMessage);
                console["error"](combinedMessage);
              } else {
                console["error"](error2);
              }
            } catch (e) {
              setTimeout(function() {
                throw e;
              });
            }
          }
          var PossiblyWeakMap$1 = typeof WeakMap === "function" ? WeakMap : Map;
          function createRootErrorUpdate(fiber, errorInfo, lane) {
            var update = createUpdate(NoTimestamp, lane);
            update.tag = CaptureUpdate;
            update.payload = {
              element: null
            };
            var error2 = errorInfo.value;
            update.callback = function() {
              onUncaughtError(error2);
              logCapturedError(fiber, errorInfo);
            };
            return update;
          }
          function createClassErrorUpdate(fiber, errorInfo, lane) {
            var update = createUpdate(NoTimestamp, lane);
            update.tag = CaptureUpdate;
            var getDerivedStateFromError = fiber.type.getDerivedStateFromError;
            if (typeof getDerivedStateFromError === "function") {
              var error$1 = errorInfo.value;
              update.payload = function() {
                return getDerivedStateFromError(error$1);
              };
              update.callback = function() {
                {
                  markFailedErrorBoundaryForHotReloading(fiber);
                }
                logCapturedError(fiber, errorInfo);
              };
            }
            var inst = fiber.stateNode;
            if (inst !== null && typeof inst.componentDidCatch === "function") {
              update.callback = function callback() {
                {
                  markFailedErrorBoundaryForHotReloading(fiber);
                }
                logCapturedError(fiber, errorInfo);
                if (typeof getDerivedStateFromError !== "function") {
                  markLegacyErrorBoundaryAsFailed(this);
                }
                var error$12 = errorInfo.value;
                var stack = errorInfo.stack;
                this.componentDidCatch(error$12, {
                  componentStack: stack !== null ? stack : ""
                });
                {
                  if (typeof getDerivedStateFromError !== "function") {
                    if (!includesSomeLane(fiber.lanes, SyncLane)) {
                      error("%s: Error boundaries should implement getDerivedStateFromError(). In that method, return a state update to display an error message or fallback UI.", getComponentNameFromFiber(fiber) || "Unknown");
                    }
                  }
                }
              };
            }
            return update;
          }
          function attachPingListener(root3, wakeable, lanes) {
            var pingCache = root3.pingCache;
            var threadIDs;
            if (pingCache === null) {
              pingCache = root3.pingCache = new PossiblyWeakMap$1();
              threadIDs = /* @__PURE__ */ new Set();
              pingCache.set(wakeable, threadIDs);
            } else {
              threadIDs = pingCache.get(wakeable);
              if (threadIDs === void 0) {
                threadIDs = /* @__PURE__ */ new Set();
                pingCache.set(wakeable, threadIDs);
              }
            }
            if (!threadIDs.has(lanes)) {
              threadIDs.add(lanes);
              var ping = pingSuspendedRoot.bind(null, root3, wakeable, lanes);
              {
                if (isDevToolsPresent) {
                  restorePendingUpdaters(root3, lanes);
                }
              }
              wakeable.then(ping, ping);
            }
          }
          function attachRetryListener(suspenseBoundary, root3, wakeable, lanes) {
            var wakeables = suspenseBoundary.updateQueue;
            if (wakeables === null) {
              var updateQueue = /* @__PURE__ */ new Set();
              updateQueue.add(wakeable);
              suspenseBoundary.updateQueue = updateQueue;
            } else {
              wakeables.add(wakeable);
            }
          }
          function resetSuspendedComponent(sourceFiber, rootRenderLanes) {
            var tag = sourceFiber.tag;
            if ((sourceFiber.mode & ConcurrentMode) === NoMode && (tag === FunctionComponent || tag === ForwardRef || tag === SimpleMemoComponent)) {
              var currentSource = sourceFiber.alternate;
              if (currentSource) {
                sourceFiber.updateQueue = currentSource.updateQueue;
                sourceFiber.memoizedState = currentSource.memoizedState;
                sourceFiber.lanes = currentSource.lanes;
              } else {
                sourceFiber.updateQueue = null;
                sourceFiber.memoizedState = null;
              }
            }
          }
          function getNearestSuspenseBoundaryToCapture(returnFiber) {
            var node = returnFiber;
            do {
              if (node.tag === SuspenseComponent && shouldCaptureSuspense(node)) {
                return node;
              }
              node = node.return;
            } while (node !== null);
            return null;
          }
          function markSuspenseBoundaryShouldCapture(suspenseBoundary, returnFiber, sourceFiber, root3, rootRenderLanes) {
            if ((suspenseBoundary.mode & ConcurrentMode) === NoMode) {
              if (suspenseBoundary === returnFiber) {
                suspenseBoundary.flags |= ShouldCapture;
              } else {
                suspenseBoundary.flags |= DidCapture;
                sourceFiber.flags |= ForceUpdateForLegacySuspense;
                sourceFiber.flags &= ~(LifecycleEffectMask | Incomplete);
                if (sourceFiber.tag === ClassComponent) {
                  var currentSourceFiber = sourceFiber.alternate;
                  if (currentSourceFiber === null) {
                    sourceFiber.tag = IncompleteClassComponent;
                  } else {
                    var update = createUpdate(NoTimestamp, SyncLane);
                    update.tag = ForceUpdate;
                    enqueueUpdate(sourceFiber, update, SyncLane);
                  }
                }
                sourceFiber.lanes = mergeLanes(sourceFiber.lanes, SyncLane);
              }
              return suspenseBoundary;
            }
            suspenseBoundary.flags |= ShouldCapture;
            suspenseBoundary.lanes = rootRenderLanes;
            return suspenseBoundary;
          }
          function throwException(root3, returnFiber, sourceFiber, value, rootRenderLanes) {
            sourceFiber.flags |= Incomplete;
            {
              if (isDevToolsPresent) {
                restorePendingUpdaters(root3, rootRenderLanes);
              }
            }
            if (value !== null && typeof value === "object" && typeof value.then === "function") {
              var wakeable = value;
              resetSuspendedComponent(sourceFiber);
              {
                if (getIsHydrating() && sourceFiber.mode & ConcurrentMode) {
                  markDidThrowWhileHydratingDEV();
                }
              }
              var suspenseBoundary = getNearestSuspenseBoundaryToCapture(returnFiber);
              if (suspenseBoundary !== null) {
                suspenseBoundary.flags &= ~ForceClientRender;
                markSuspenseBoundaryShouldCapture(suspenseBoundary, returnFiber, sourceFiber, root3, rootRenderLanes);
                if (suspenseBoundary.mode & ConcurrentMode) {
                  attachPingListener(root3, wakeable, rootRenderLanes);
                }
                attachRetryListener(suspenseBoundary, root3, wakeable);
                return;
              } else {
                if (!includesSyncLane(rootRenderLanes)) {
                  attachPingListener(root3, wakeable, rootRenderLanes);
                  renderDidSuspendDelayIfPossible();
                  return;
                }
                var uncaughtSuspenseError = new Error("A component suspended while responding to synchronous input. This will cause the UI to be replaced with a loading indicator. To fix, updates that suspend should be wrapped with startTransition.");
                value = uncaughtSuspenseError;
              }
            } else {
              if (getIsHydrating() && sourceFiber.mode & ConcurrentMode) {
                markDidThrowWhileHydratingDEV();
                var _suspenseBoundary = getNearestSuspenseBoundaryToCapture(returnFiber);
                if (_suspenseBoundary !== null) {
                  if ((_suspenseBoundary.flags & ShouldCapture) === NoFlags) {
                    _suspenseBoundary.flags |= ForceClientRender;
                  }
                  markSuspenseBoundaryShouldCapture(_suspenseBoundary, returnFiber, sourceFiber, root3, rootRenderLanes);
                  queueHydrationError(createCapturedValueAtFiber(value, sourceFiber));
                  return;
                }
              }
            }
            value = createCapturedValueAtFiber(value, sourceFiber);
            renderDidError(value);
            var workInProgress2 = returnFiber;
            do {
              switch (workInProgress2.tag) {
                case HostRoot: {
                  var _errorInfo = value;
                  workInProgress2.flags |= ShouldCapture;
                  var lane = pickArbitraryLane(rootRenderLanes);
                  workInProgress2.lanes = mergeLanes(workInProgress2.lanes, lane);
                  var update = createRootErrorUpdate(workInProgress2, _errorInfo, lane);
                  enqueueCapturedUpdate(workInProgress2, update);
                  return;
                }
                case ClassComponent:
                  var errorInfo = value;
                  var ctor = workInProgress2.type;
                  var instance = workInProgress2.stateNode;
                  if ((workInProgress2.flags & DidCapture) === NoFlags && (typeof ctor.getDerivedStateFromError === "function" || instance !== null && typeof instance.componentDidCatch === "function" && !isAlreadyFailedLegacyErrorBoundary(instance))) {
                    workInProgress2.flags |= ShouldCapture;
                    var _lane = pickArbitraryLane(rootRenderLanes);
                    workInProgress2.lanes = mergeLanes(workInProgress2.lanes, _lane);
                    var _update = createClassErrorUpdate(workInProgress2, errorInfo, _lane);
                    enqueueCapturedUpdate(workInProgress2, _update);
                    return;
                  }
                  break;
              }
              workInProgress2 = workInProgress2.return;
            } while (workInProgress2 !== null);
          }
          function getSuspendedCache() {
            {
              return null;
            }
          }
          var ReactCurrentOwner$1 = ReactSharedInternals.ReactCurrentOwner;
          var didReceiveUpdate = false;
          var didWarnAboutBadClass;
          var didWarnAboutModulePatternComponent;
          var didWarnAboutContextTypeOnFunctionComponent;
          var didWarnAboutGetDerivedStateOnFunctionComponent;
          var didWarnAboutFunctionRefs;
          var didWarnAboutReassigningProps;
          var didWarnAboutRevealOrder;
          var didWarnAboutTailOptions;
          var didWarnAboutDefaultPropsOnFunctionComponent;
          {
            didWarnAboutBadClass = {};
            didWarnAboutModulePatternComponent = {};
            didWarnAboutContextTypeOnFunctionComponent = {};
            didWarnAboutGetDerivedStateOnFunctionComponent = {};
            didWarnAboutFunctionRefs = {};
            didWarnAboutReassigningProps = false;
            didWarnAboutRevealOrder = {};
            didWarnAboutTailOptions = {};
            didWarnAboutDefaultPropsOnFunctionComponent = {};
          }
          function reconcileChildren(current2, workInProgress2, nextChildren, renderLanes2) {
            if (current2 === null) {
              workInProgress2.child = mountChildFibers(workInProgress2, null, nextChildren, renderLanes2);
            } else {
              workInProgress2.child = reconcileChildFibers(workInProgress2, current2.child, nextChildren, renderLanes2);
            }
          }
          function forceUnmountCurrentAndReconcile(current2, workInProgress2, nextChildren, renderLanes2) {
            workInProgress2.child = reconcileChildFibers(workInProgress2, current2.child, null, renderLanes2);
            workInProgress2.child = reconcileChildFibers(workInProgress2, null, nextChildren, renderLanes2);
          }
          function updateForwardRef(current2, workInProgress2, Component, nextProps, renderLanes2) {
            {
              if (workInProgress2.type !== workInProgress2.elementType) {
                var innerPropTypes = Component.propTypes;
                if (innerPropTypes) {
                  checkPropTypes(
                    innerPropTypes,
                    nextProps,
                    // Resolved props
                    "prop",
                    getComponentNameFromType(Component)
                  );
                }
              }
            }
            var render2 = Component.render;
            var ref = workInProgress2.ref;
            var nextChildren;
            var hasId;
            prepareToReadContext(workInProgress2, renderLanes2);
            {
              markComponentRenderStarted(workInProgress2);
            }
            {
              ReactCurrentOwner$1.current = workInProgress2;
              setIsRendering(true);
              nextChildren = renderWithHooks(current2, workInProgress2, render2, nextProps, ref, renderLanes2);
              hasId = checkDidRenderIdHook();
              if (workInProgress2.mode & StrictLegacyMode) {
                setIsStrictModeForDevtools(true);
                try {
                  nextChildren = renderWithHooks(current2, workInProgress2, render2, nextProps, ref, renderLanes2);
                  hasId = checkDidRenderIdHook();
                } finally {
                  setIsStrictModeForDevtools(false);
                }
              }
              setIsRendering(false);
            }
            {
              markComponentRenderStopped();
            }
            if (current2 !== null && !didReceiveUpdate) {
              bailoutHooks(current2, workInProgress2, renderLanes2);
              return bailoutOnAlreadyFinishedWork(current2, workInProgress2, renderLanes2);
            }
            if (getIsHydrating() && hasId) {
              pushMaterializedTreeId(workInProgress2);
            }
            workInProgress2.flags |= PerformedWork;
            reconcileChildren(current2, workInProgress2, nextChildren, renderLanes2);
            return workInProgress2.child;
          }
          function updateMemoComponent(current2, workInProgress2, Component, nextProps, renderLanes2) {
            if (current2 === null) {
              var type = Component.type;
              if (isSimpleFunctionComponent(type) && Component.compare === null && // SimpleMemoComponent codepath doesn't resolve outer props either.
              Component.defaultProps === void 0) {
                var resolvedType = type;
                {
                  resolvedType = resolveFunctionForHotReloading(type);
                }
                workInProgress2.tag = SimpleMemoComponent;
                workInProgress2.type = resolvedType;
                {
                  validateFunctionComponentInDev(workInProgress2, type);
                }
                return updateSimpleMemoComponent(current2, workInProgress2, resolvedType, nextProps, renderLanes2);
              }
              {
                var innerPropTypes = type.propTypes;
                if (innerPropTypes) {
                  checkPropTypes(
                    innerPropTypes,
                    nextProps,
                    // Resolved props
                    "prop",
                    getComponentNameFromType(type)
                  );
                }
                if (Component.defaultProps !== void 0) {
                  var componentName = getComponentNameFromType(type) || "Unknown";
                  if (!didWarnAboutDefaultPropsOnFunctionComponent[componentName]) {
                    error("%s: Support for defaultProps will be removed from memo components in a future major release. Use JavaScript default parameters instead.", componentName);
                    didWarnAboutDefaultPropsOnFunctionComponent[componentName] = true;
                  }
                }
              }
              var child = createFiberFromTypeAndProps(Component.type, null, nextProps, workInProgress2, workInProgress2.mode, renderLanes2);
              child.ref = workInProgress2.ref;
              child.return = workInProgress2;
              workInProgress2.child = child;
              return child;
            }
            {
              var _type = Component.type;
              var _innerPropTypes = _type.propTypes;
              if (_innerPropTypes) {
                checkPropTypes(
                  _innerPropTypes,
                  nextProps,
                  // Resolved props
                  "prop",
                  getComponentNameFromType(_type)
                );
              }
            }
            var currentChild = current2.child;
            var hasScheduledUpdateOrContext = checkScheduledUpdateOrContext(current2, renderLanes2);
            if (!hasScheduledUpdateOrContext) {
              var prevProps = currentChild.memoizedProps;
              var compare = Component.compare;
              compare = compare !== null ? compare : shallowEqual;
              if (compare(prevProps, nextProps) && current2.ref === workInProgress2.ref) {
                return bailoutOnAlreadyFinishedWork(current2, workInProgress2, renderLanes2);
              }
            }
            workInProgress2.flags |= PerformedWork;
            var newChild = createWorkInProgress(currentChild, nextProps);
            newChild.ref = workInProgress2.ref;
            newChild.return = workInProgress2;
            workInProgress2.child = newChild;
            return newChild;
          }
          function updateSimpleMemoComponent(current2, workInProgress2, Component, nextProps, renderLanes2) {
            {
              if (workInProgress2.type !== workInProgress2.elementType) {
                var outerMemoType = workInProgress2.elementType;
                if (outerMemoType.$$typeof === REACT_LAZY_TYPE) {
                  var lazyComponent = outerMemoType;
                  var payload = lazyComponent._payload;
                  var init = lazyComponent._init;
                  try {
                    outerMemoType = init(payload);
                  } catch (x) {
                    outerMemoType = null;
                  }
                  var outerPropTypes = outerMemoType && outerMemoType.propTypes;
                  if (outerPropTypes) {
                    checkPropTypes(
                      outerPropTypes,
                      nextProps,
                      // Resolved (SimpleMemoComponent has no defaultProps)
                      "prop",
                      getComponentNameFromType(outerMemoType)
                    );
                  }
                }
              }
            }
            if (current2 !== null) {
              var prevProps = current2.memoizedProps;
              if (shallowEqual(prevProps, nextProps) && current2.ref === workInProgress2.ref && // Prevent bailout if the implementation changed due to hot reload.
              workInProgress2.type === current2.type) {
                didReceiveUpdate = false;
                workInProgress2.pendingProps = nextProps = prevProps;
                if (!checkScheduledUpdateOrContext(current2, renderLanes2)) {
                  workInProgress2.lanes = current2.lanes;
                  return bailoutOnAlreadyFinishedWork(current2, workInProgress2, renderLanes2);
                } else if ((current2.flags & ForceUpdateForLegacySuspense) !== NoFlags) {
                  didReceiveUpdate = true;
                }
              }
            }
            return updateFunctionComponent(current2, workInProgress2, Component, nextProps, renderLanes2);
          }
          function updateOffscreenComponent(current2, workInProgress2, renderLanes2) {
            var nextProps = workInProgress2.pendingProps;
            var nextChildren = nextProps.children;
            var prevState = current2 !== null ? current2.memoizedState : null;
            if (nextProps.mode === "hidden" || enableLegacyHidden) {
              if ((workInProgress2.mode & ConcurrentMode) === NoMode) {
                var nextState = {
                  baseLanes: NoLanes,
                  cachePool: null,
                  transitions: null
                };
                workInProgress2.memoizedState = nextState;
                pushRenderLanes(workInProgress2, renderLanes2);
              } else if (!includesSomeLane(renderLanes2, OffscreenLane)) {
                var spawnedCachePool = null;
                var nextBaseLanes;
                if (prevState !== null) {
                  var prevBaseLanes = prevState.baseLanes;
                  nextBaseLanes = mergeLanes(prevBaseLanes, renderLanes2);
                } else {
                  nextBaseLanes = renderLanes2;
                }
                workInProgress2.lanes = workInProgress2.childLanes = laneToLanes(OffscreenLane);
                var _nextState = {
                  baseLanes: nextBaseLanes,
                  cachePool: spawnedCachePool,
                  transitions: null
                };
                workInProgress2.memoizedState = _nextState;
                workInProgress2.updateQueue = null;
                pushRenderLanes(workInProgress2, nextBaseLanes);
                return null;
              } else {
                var _nextState2 = {
                  baseLanes: NoLanes,
                  cachePool: null,
                  transitions: null
                };
                workInProgress2.memoizedState = _nextState2;
                var subtreeRenderLanes2 = prevState !== null ? prevState.baseLanes : renderLanes2;
                pushRenderLanes(workInProgress2, subtreeRenderLanes2);
              }
            } else {
              var _subtreeRenderLanes;
              if (prevState !== null) {
                _subtreeRenderLanes = mergeLanes(prevState.baseLanes, renderLanes2);
                workInProgress2.memoizedState = null;
              } else {
                _subtreeRenderLanes = renderLanes2;
              }
              pushRenderLanes(workInProgress2, _subtreeRenderLanes);
            }
            reconcileChildren(current2, workInProgress2, nextChildren, renderLanes2);
            return workInProgress2.child;
          }
          function updateFragment(current2, workInProgress2, renderLanes2) {
            var nextChildren = workInProgress2.pendingProps;
            reconcileChildren(current2, workInProgress2, nextChildren, renderLanes2);
            return workInProgress2.child;
          }
          function updateMode(current2, workInProgress2, renderLanes2) {
            var nextChildren = workInProgress2.pendingProps.children;
            reconcileChildren(current2, workInProgress2, nextChildren, renderLanes2);
            return workInProgress2.child;
          }
          function updateProfiler(current2, workInProgress2, renderLanes2) {
            {
              workInProgress2.flags |= Update;
              {
                var stateNode = workInProgress2.stateNode;
                stateNode.effectDuration = 0;
                stateNode.passiveEffectDuration = 0;
              }
            }
            var nextProps = workInProgress2.pendingProps;
            var nextChildren = nextProps.children;
            reconcileChildren(current2, workInProgress2, nextChildren, renderLanes2);
            return workInProgress2.child;
          }
          function markRef(current2, workInProgress2) {
            var ref = workInProgress2.ref;
            if (current2 === null && ref !== null || current2 !== null && current2.ref !== ref) {
              workInProgress2.flags |= Ref;
              {
                workInProgress2.flags |= RefStatic;
              }
            }
          }
          function updateFunctionComponent(current2, workInProgress2, Component, nextProps, renderLanes2) {
            {
              if (workInProgress2.type !== workInProgress2.elementType) {
                var innerPropTypes = Component.propTypes;
                if (innerPropTypes) {
                  checkPropTypes(
                    innerPropTypes,
                    nextProps,
                    // Resolved props
                    "prop",
                    getComponentNameFromType(Component)
                  );
                }
              }
            }
            var context;
            {
              var unmaskedContext = getUnmaskedContext(workInProgress2, Component, true);
              context = getMaskedContext(workInProgress2, unmaskedContext);
            }
            var nextChildren;
            var hasId;
            prepareToReadContext(workInProgress2, renderLanes2);
            {
              markComponentRenderStarted(workInProgress2);
            }
            {
              ReactCurrentOwner$1.current = workInProgress2;
              setIsRendering(true);
              nextChildren = renderWithHooks(current2, workInProgress2, Component, nextProps, context, renderLanes2);
              hasId = checkDidRenderIdHook();
              if (workInProgress2.mode & StrictLegacyMode) {
                setIsStrictModeForDevtools(true);
                try {
                  nextChildren = renderWithHooks(current2, workInProgress2, Component, nextProps, context, renderLanes2);
                  hasId = checkDidRenderIdHook();
                } finally {
                  setIsStrictModeForDevtools(false);
                }
              }
              setIsRendering(false);
            }
            {
              markComponentRenderStopped();
            }
            if (current2 !== null && !didReceiveUpdate) {
              bailoutHooks(current2, workInProgress2, renderLanes2);
              return bailoutOnAlreadyFinishedWork(current2, workInProgress2, renderLanes2);
            }
            if (getIsHydrating() && hasId) {
              pushMaterializedTreeId(workInProgress2);
            }
            workInProgress2.flags |= PerformedWork;
            reconcileChildren(current2, workInProgress2, nextChildren, renderLanes2);
            return workInProgress2.child;
          }
          function updateClassComponent(current2, workInProgress2, Component, nextProps, renderLanes2) {
            {
              switch (shouldError(workInProgress2)) {
                case false: {
                  var _instance = workInProgress2.stateNode;
                  var ctor = workInProgress2.type;
                  var tempInstance = new ctor(workInProgress2.memoizedProps, _instance.context);
                  var state = tempInstance.state;
                  _instance.updater.enqueueSetState(_instance, state, null);
                  break;
                }
                case true: {
                  workInProgress2.flags |= DidCapture;
                  workInProgress2.flags |= ShouldCapture;
                  var error$1 = new Error("Simulated error coming from DevTools");
                  var lane = pickArbitraryLane(renderLanes2);
                  workInProgress2.lanes = mergeLanes(workInProgress2.lanes, lane);
                  var update = createClassErrorUpdate(workInProgress2, createCapturedValueAtFiber(error$1, workInProgress2), lane);
                  enqueueCapturedUpdate(workInProgress2, update);
                  break;
                }
              }
              if (workInProgress2.type !== workInProgress2.elementType) {
                var innerPropTypes = Component.propTypes;
                if (innerPropTypes) {
                  checkPropTypes(
                    innerPropTypes,
                    nextProps,
                    // Resolved props
                    "prop",
                    getComponentNameFromType(Component)
                  );
                }
              }
            }
            var hasContext;
            if (isContextProvider(Component)) {
              hasContext = true;
              pushContextProvider(workInProgress2);
            } else {
              hasContext = false;
            }
            prepareToReadContext(workInProgress2, renderLanes2);
            var instance = workInProgress2.stateNode;
            var shouldUpdate;
            if (instance === null) {
              resetSuspendedCurrentOnMountInLegacyMode(current2, workInProgress2);
              constructClassInstance(workInProgress2, Component, nextProps);
              mountClassInstance(workInProgress2, Component, nextProps, renderLanes2);
              shouldUpdate = true;
            } else if (current2 === null) {
              shouldUpdate = resumeMountClassInstance(workInProgress2, Component, nextProps, renderLanes2);
            } else {
              shouldUpdate = updateClassInstance(current2, workInProgress2, Component, nextProps, renderLanes2);
            }
            var nextUnitOfWork = finishClassComponent(current2, workInProgress2, Component, shouldUpdate, hasContext, renderLanes2);
            {
              var inst = workInProgress2.stateNode;
              if (shouldUpdate && inst.props !== nextProps) {
                if (!didWarnAboutReassigningProps) {
                  error("It looks like %s is reassigning its own `this.props` while rendering. This is not supported and can lead to confusing bugs.", getComponentNameFromFiber(workInProgress2) || "a component");
                }
                didWarnAboutReassigningProps = true;
              }
            }
            return nextUnitOfWork;
          }
          function finishClassComponent(current2, workInProgress2, Component, shouldUpdate, hasContext, renderLanes2) {
            markRef(current2, workInProgress2);
            var didCaptureError = (workInProgress2.flags & DidCapture) !== NoFlags;
            if (!shouldUpdate && !didCaptureError) {
              if (hasContext) {
                invalidateContextProvider(workInProgress2, Component, false);
              }
              return bailoutOnAlreadyFinishedWork(current2, workInProgress2, renderLanes2);
            }
            var instance = workInProgress2.stateNode;
            ReactCurrentOwner$1.current = workInProgress2;
            var nextChildren;
            if (didCaptureError && typeof Component.getDerivedStateFromError !== "function") {
              nextChildren = null;
              {
                stopProfilerTimerIfRunning();
              }
            } else {
              {
                markComponentRenderStarted(workInProgress2);
              }
              {
                setIsRendering(true);
                nextChildren = instance.render();
                if (workInProgress2.mode & StrictLegacyMode) {
                  setIsStrictModeForDevtools(true);
                  try {
                    instance.render();
                  } finally {
                    setIsStrictModeForDevtools(false);
                  }
                }
                setIsRendering(false);
              }
              {
                markComponentRenderStopped();
              }
            }
            workInProgress2.flags |= PerformedWork;
            if (current2 !== null && didCaptureError) {
              forceUnmountCurrentAndReconcile(current2, workInProgress2, nextChildren, renderLanes2);
            } else {
              reconcileChildren(current2, workInProgress2, nextChildren, renderLanes2);
            }
            workInProgress2.memoizedState = instance.state;
            if (hasContext) {
              invalidateContextProvider(workInProgress2, Component, true);
            }
            return workInProgress2.child;
          }
          function pushHostRootContext(workInProgress2) {
            var root3 = workInProgress2.stateNode;
            if (root3.pendingContext) {
              pushTopLevelContextObject(workInProgress2, root3.pendingContext, root3.pendingContext !== root3.context);
            } else if (root3.context) {
              pushTopLevelContextObject(workInProgress2, root3.context, false);
            }
            pushHostContainer(workInProgress2, root3.containerInfo);
          }
          function updateHostRoot(current2, workInProgress2, renderLanes2) {
            pushHostRootContext(workInProgress2);
            if (current2 === null) {
              throw new Error("Should have a current fiber. This is a bug in React.");
            }
            var nextProps = workInProgress2.pendingProps;
            var prevState = workInProgress2.memoizedState;
            var prevChildren = prevState.element;
            cloneUpdateQueue(current2, workInProgress2);
            processUpdateQueue(workInProgress2, nextProps, null, renderLanes2);
            var nextState = workInProgress2.memoizedState;
            var root3 = workInProgress2.stateNode;
            var nextChildren = nextState.element;
            if (prevState.isDehydrated) {
              var overrideState = {
                element: nextChildren,
                isDehydrated: false,
                cache: nextState.cache,
                pendingSuspenseBoundaries: nextState.pendingSuspenseBoundaries,
                transitions: nextState.transitions
              };
              var updateQueue = workInProgress2.updateQueue;
              updateQueue.baseState = overrideState;
              workInProgress2.memoizedState = overrideState;
              if (workInProgress2.flags & ForceClientRender) {
                var recoverableError = createCapturedValueAtFiber(new Error("There was an error while hydrating. Because the error happened outside of a Suspense boundary, the entire root will switch to client rendering."), workInProgress2);
                return mountHostRootWithoutHydrating(current2, workInProgress2, nextChildren, renderLanes2, recoverableError);
              } else if (nextChildren !== prevChildren) {
                var _recoverableError = createCapturedValueAtFiber(new Error("This root received an early update, before anything was able hydrate. Switched the entire root to client rendering."), workInProgress2);
                return mountHostRootWithoutHydrating(current2, workInProgress2, nextChildren, renderLanes2, _recoverableError);
              } else {
                enterHydrationState(workInProgress2);
                var child = mountChildFibers(workInProgress2, null, nextChildren, renderLanes2);
                workInProgress2.child = child;
                var node = child;
                while (node) {
                  node.flags = node.flags & ~Placement | Hydrating;
                  node = node.sibling;
                }
              }
            } else {
              resetHydrationState();
              if (nextChildren === prevChildren) {
                return bailoutOnAlreadyFinishedWork(current2, workInProgress2, renderLanes2);
              }
              reconcileChildren(current2, workInProgress2, nextChildren, renderLanes2);
            }
            return workInProgress2.child;
          }
          function mountHostRootWithoutHydrating(current2, workInProgress2, nextChildren, renderLanes2, recoverableError) {
            resetHydrationState();
            queueHydrationError(recoverableError);
            workInProgress2.flags |= ForceClientRender;
            reconcileChildren(current2, workInProgress2, nextChildren, renderLanes2);
            return workInProgress2.child;
          }
          function updateHostComponent(current2, workInProgress2, renderLanes2) {
            pushHostContext(workInProgress2);
            if (current2 === null) {
              tryToClaimNextHydratableInstance(workInProgress2);
            }
            var type = workInProgress2.type;
            var nextProps = workInProgress2.pendingProps;
            var prevProps = current2 !== null ? current2.memoizedProps : null;
            var nextChildren = nextProps.children;
            var isDirectTextChild = shouldSetTextContent(type, nextProps);
            if (isDirectTextChild) {
              nextChildren = null;
            } else if (prevProps !== null && shouldSetTextContent(type, prevProps)) {
              workInProgress2.flags |= ContentReset;
            }
            markRef(current2, workInProgress2);
            reconcileChildren(current2, workInProgress2, nextChildren, renderLanes2);
            return workInProgress2.child;
          }
          function updateHostText(current2, workInProgress2) {
            if (current2 === null) {
              tryToClaimNextHydratableInstance(workInProgress2);
            }
            return null;
          }
          function mountLazyComponent(_current, workInProgress2, elementType, renderLanes2) {
            resetSuspendedCurrentOnMountInLegacyMode(_current, workInProgress2);
            var props = workInProgress2.pendingProps;
            var lazyComponent = elementType;
            var payload = lazyComponent._payload;
            var init = lazyComponent._init;
            var Component = init(payload);
            workInProgress2.type = Component;
            var resolvedTag = workInProgress2.tag = resolveLazyComponentTag(Component);
            var resolvedProps = resolveDefaultProps(Component, props);
            var child;
            switch (resolvedTag) {
              case FunctionComponent: {
                {
                  validateFunctionComponentInDev(workInProgress2, Component);
                  workInProgress2.type = Component = resolveFunctionForHotReloading(Component);
                }
                child = updateFunctionComponent(null, workInProgress2, Component, resolvedProps, renderLanes2);
                return child;
              }
              case ClassComponent: {
                {
                  workInProgress2.type = Component = resolveClassForHotReloading(Component);
                }
                child = updateClassComponent(null, workInProgress2, Component, resolvedProps, renderLanes2);
                return child;
              }
              case ForwardRef: {
                {
                  workInProgress2.type = Component = resolveForwardRefForHotReloading(Component);
                }
                child = updateForwardRef(null, workInProgress2, Component, resolvedProps, renderLanes2);
                return child;
              }
              case MemoComponent: {
                {
                  if (workInProgress2.type !== workInProgress2.elementType) {
                    var outerPropTypes = Component.propTypes;
                    if (outerPropTypes) {
                      checkPropTypes(
                        outerPropTypes,
                        resolvedProps,
                        // Resolved for outer only
                        "prop",
                        getComponentNameFromType(Component)
                      );
                    }
                  }
                }
                child = updateMemoComponent(
                  null,
                  workInProgress2,
                  Component,
                  resolveDefaultProps(Component.type, resolvedProps),
                  // The inner type can have defaults too
                  renderLanes2
                );
                return child;
              }
            }
            var hint = "";
            {
              if (Component !== null && typeof Component === "object" && Component.$$typeof === REACT_LAZY_TYPE) {
                hint = " Did you wrap a component in React.lazy() more than once?";
              }
            }
            throw new Error("Element type is invalid. Received a promise that resolves to: " + Component + ". " + ("Lazy element type must resolve to a class or function." + hint));
          }
          function mountIncompleteClassComponent(_current, workInProgress2, Component, nextProps, renderLanes2) {
            resetSuspendedCurrentOnMountInLegacyMode(_current, workInProgress2);
            workInProgress2.tag = ClassComponent;
            var hasContext;
            if (isContextProvider(Component)) {
              hasContext = true;
              pushContextProvider(workInProgress2);
            } else {
              hasContext = false;
            }
            prepareToReadContext(workInProgress2, renderLanes2);
            constructClassInstance(workInProgress2, Component, nextProps);
            mountClassInstance(workInProgress2, Component, nextProps, renderLanes2);
            return finishClassComponent(null, workInProgress2, Component, true, hasContext, renderLanes2);
          }
          function mountIndeterminateComponent(_current, workInProgress2, Component, renderLanes2) {
            resetSuspendedCurrentOnMountInLegacyMode(_current, workInProgress2);
            var props = workInProgress2.pendingProps;
            var context;
            {
              var unmaskedContext = getUnmaskedContext(workInProgress2, Component, false);
              context = getMaskedContext(workInProgress2, unmaskedContext);
            }
            prepareToReadContext(workInProgress2, renderLanes2);
            var value;
            var hasId;
            {
              markComponentRenderStarted(workInProgress2);
            }
            {
              if (Component.prototype && typeof Component.prototype.render === "function") {
                var componentName = getComponentNameFromType(Component) || "Unknown";
                if (!didWarnAboutBadClass[componentName]) {
                  error("The <%s /> component appears to have a render method, but doesn't extend React.Component. This is likely to cause errors. Change %s to extend React.Component instead.", componentName, componentName);
                  didWarnAboutBadClass[componentName] = true;
                }
              }
              if (workInProgress2.mode & StrictLegacyMode) {
                ReactStrictModeWarnings.recordLegacyContextWarning(workInProgress2, null);
              }
              setIsRendering(true);
              ReactCurrentOwner$1.current = workInProgress2;
              value = renderWithHooks(null, workInProgress2, Component, props, context, renderLanes2);
              hasId = checkDidRenderIdHook();
              setIsRendering(false);
            }
            {
              markComponentRenderStopped();
            }
            workInProgress2.flags |= PerformedWork;
            {
              if (typeof value === "object" && value !== null && typeof value.render === "function" && value.$$typeof === void 0) {
                var _componentName = getComponentNameFromType(Component) || "Unknown";
                if (!didWarnAboutModulePatternComponent[_componentName]) {
                  error("The <%s /> component appears to be a function component that returns a class instance. Change %s to a class that extends React.Component instead. If you can't use a class try assigning the prototype on the function as a workaround. `%s.prototype = React.Component.prototype`. Don't use an arrow function since it cannot be called with `new` by React.", _componentName, _componentName, _componentName);
                  didWarnAboutModulePatternComponent[_componentName] = true;
                }
              }
            }
            if (
              // Run these checks in production only if the flag is off.
              // Eventually we'll delete this branch altogether.
              typeof value === "object" && value !== null && typeof value.render === "function" && value.$$typeof === void 0
            ) {
              {
                var _componentName2 = getComponentNameFromType(Component) || "Unknown";
                if (!didWarnAboutModulePatternComponent[_componentName2]) {
                  error("The <%s /> component appears to be a function component that returns a class instance. Change %s to a class that extends React.Component instead. If you can't use a class try assigning the prototype on the function as a workaround. `%s.prototype = React.Component.prototype`. Don't use an arrow function since it cannot be called with `new` by React.", _componentName2, _componentName2, _componentName2);
                  didWarnAboutModulePatternComponent[_componentName2] = true;
                }
              }
              workInProgress2.tag = ClassComponent;
              workInProgress2.memoizedState = null;
              workInProgress2.updateQueue = null;
              var hasContext = false;
              if (isContextProvider(Component)) {
                hasContext = true;
                pushContextProvider(workInProgress2);
              } else {
                hasContext = false;
              }
              workInProgress2.memoizedState = value.state !== null && value.state !== void 0 ? value.state : null;
              initializeUpdateQueue(workInProgress2);
              adoptClassInstance(workInProgress2, value);
              mountClassInstance(workInProgress2, Component, props, renderLanes2);
              return finishClassComponent(null, workInProgress2, Component, true, hasContext, renderLanes2);
            } else {
              workInProgress2.tag = FunctionComponent;
              {
                if (workInProgress2.mode & StrictLegacyMode) {
                  setIsStrictModeForDevtools(true);
                  try {
                    value = renderWithHooks(null, workInProgress2, Component, props, context, renderLanes2);
                    hasId = checkDidRenderIdHook();
                  } finally {
                    setIsStrictModeForDevtools(false);
                  }
                }
              }
              if (getIsHydrating() && hasId) {
                pushMaterializedTreeId(workInProgress2);
              }
              reconcileChildren(null, workInProgress2, value, renderLanes2);
              {
                validateFunctionComponentInDev(workInProgress2, Component);
              }
              return workInProgress2.child;
            }
          }
          function validateFunctionComponentInDev(workInProgress2, Component) {
            {
              if (Component) {
                if (Component.childContextTypes) {
                  error("%s(...): childContextTypes cannot be defined on a function component.", Component.displayName || Component.name || "Component");
                }
              }
              if (workInProgress2.ref !== null) {
                var info = "";
                var ownerName = getCurrentFiberOwnerNameInDevOrNull();
                if (ownerName) {
                  info += "\n\nCheck the render method of `" + ownerName + "`.";
                }
                var warningKey = ownerName || "";
                var debugSource = workInProgress2._debugSource;
                if (debugSource) {
                  warningKey = debugSource.fileName + ":" + debugSource.lineNumber;
                }
                if (!didWarnAboutFunctionRefs[warningKey]) {
                  didWarnAboutFunctionRefs[warningKey] = true;
                  error("Function components cannot be given refs. Attempts to access this ref will fail. Did you mean to use React.forwardRef()?%s", info);
                }
              }
              if (Component.defaultProps !== void 0) {
                var componentName = getComponentNameFromType(Component) || "Unknown";
                if (!didWarnAboutDefaultPropsOnFunctionComponent[componentName]) {
                  error("%s: Support for defaultProps will be removed from function components in a future major release. Use JavaScript default parameters instead.", componentName);
                  didWarnAboutDefaultPropsOnFunctionComponent[componentName] = true;
                }
              }
              if (typeof Component.getDerivedStateFromProps === "function") {
                var _componentName3 = getComponentNameFromType(Component) || "Unknown";
                if (!didWarnAboutGetDerivedStateOnFunctionComponent[_componentName3]) {
                  error("%s: Function components do not support getDerivedStateFromProps.", _componentName3);
                  didWarnAboutGetDerivedStateOnFunctionComponent[_componentName3] = true;
                }
              }
              if (typeof Component.contextType === "object" && Component.contextType !== null) {
                var _componentName4 = getComponentNameFromType(Component) || "Unknown";
                if (!didWarnAboutContextTypeOnFunctionComponent[_componentName4]) {
                  error("%s: Function components do not support contextType.", _componentName4);
                  didWarnAboutContextTypeOnFunctionComponent[_componentName4] = true;
                }
              }
            }
          }
          var SUSPENDED_MARKER = {
            dehydrated: null,
            treeContext: null,
            retryLane: NoLane
          };
          function mountSuspenseOffscreenState(renderLanes2) {
            return {
              baseLanes: renderLanes2,
              cachePool: getSuspendedCache(),
              transitions: null
            };
          }
          function updateSuspenseOffscreenState(prevOffscreenState, renderLanes2) {
            var cachePool = null;
            return {
              baseLanes: mergeLanes(prevOffscreenState.baseLanes, renderLanes2),
              cachePool,
              transitions: prevOffscreenState.transitions
            };
          }
          function shouldRemainOnFallback(suspenseContext, current2, workInProgress2, renderLanes2) {
            if (current2 !== null) {
              var suspenseState = current2.memoizedState;
              if (suspenseState === null) {
                return false;
              }
            }
            return hasSuspenseContext(suspenseContext, ForceSuspenseFallback);
          }
          function getRemainingWorkInPrimaryTree(current2, renderLanes2) {
            return removeLanes(current2.childLanes, renderLanes2);
          }
          function updateSuspenseComponent(current2, workInProgress2, renderLanes2) {
            var nextProps = workInProgress2.pendingProps;
            {
              if (shouldSuspend(workInProgress2)) {
                workInProgress2.flags |= DidCapture;
              }
            }
            var suspenseContext = suspenseStackCursor.current;
            var showFallback = false;
            var didSuspend = (workInProgress2.flags & DidCapture) !== NoFlags;
            if (didSuspend || shouldRemainOnFallback(suspenseContext, current2)) {
              showFallback = true;
              workInProgress2.flags &= ~DidCapture;
            } else {
              if (current2 === null || current2.memoizedState !== null) {
                {
                  suspenseContext = addSubtreeSuspenseContext(suspenseContext, InvisibleParentSuspenseContext);
                }
              }
            }
            suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
            pushSuspenseContext(workInProgress2, suspenseContext);
            if (current2 === null) {
              tryToClaimNextHydratableInstance(workInProgress2);
              var suspenseState = workInProgress2.memoizedState;
              if (suspenseState !== null) {
                var dehydrated = suspenseState.dehydrated;
                if (dehydrated !== null) {
                  return mountDehydratedSuspenseComponent(workInProgress2, dehydrated);
                }
              }
              var nextPrimaryChildren = nextProps.children;
              var nextFallbackChildren = nextProps.fallback;
              if (showFallback) {
                var fallbackFragment = mountSuspenseFallbackChildren(workInProgress2, nextPrimaryChildren, nextFallbackChildren, renderLanes2);
                var primaryChildFragment = workInProgress2.child;
                primaryChildFragment.memoizedState = mountSuspenseOffscreenState(renderLanes2);
                workInProgress2.memoizedState = SUSPENDED_MARKER;
                return fallbackFragment;
              } else {
                return mountSuspensePrimaryChildren(workInProgress2, nextPrimaryChildren);
              }
            } else {
              var prevState = current2.memoizedState;
              if (prevState !== null) {
                var _dehydrated = prevState.dehydrated;
                if (_dehydrated !== null) {
                  return updateDehydratedSuspenseComponent(current2, workInProgress2, didSuspend, nextProps, _dehydrated, prevState, renderLanes2);
                }
              }
              if (showFallback) {
                var _nextFallbackChildren = nextProps.fallback;
                var _nextPrimaryChildren = nextProps.children;
                var fallbackChildFragment = updateSuspenseFallbackChildren(current2, workInProgress2, _nextPrimaryChildren, _nextFallbackChildren, renderLanes2);
                var _primaryChildFragment2 = workInProgress2.child;
                var prevOffscreenState = current2.child.memoizedState;
                _primaryChildFragment2.memoizedState = prevOffscreenState === null ? mountSuspenseOffscreenState(renderLanes2) : updateSuspenseOffscreenState(prevOffscreenState, renderLanes2);
                _primaryChildFragment2.childLanes = getRemainingWorkInPrimaryTree(current2, renderLanes2);
                workInProgress2.memoizedState = SUSPENDED_MARKER;
                return fallbackChildFragment;
              } else {
                var _nextPrimaryChildren2 = nextProps.children;
                var _primaryChildFragment3 = updateSuspensePrimaryChildren(current2, workInProgress2, _nextPrimaryChildren2, renderLanes2);
                workInProgress2.memoizedState = null;
                return _primaryChildFragment3;
              }
            }
          }
          function mountSuspensePrimaryChildren(workInProgress2, primaryChildren, renderLanes2) {
            var mode = workInProgress2.mode;
            var primaryChildProps = {
              mode: "visible",
              children: primaryChildren
            };
            var primaryChildFragment = mountWorkInProgressOffscreenFiber(primaryChildProps, mode);
            primaryChildFragment.return = workInProgress2;
            workInProgress2.child = primaryChildFragment;
            return primaryChildFragment;
          }
          function mountSuspenseFallbackChildren(workInProgress2, primaryChildren, fallbackChildren, renderLanes2) {
            var mode = workInProgress2.mode;
            var progressedPrimaryFragment = workInProgress2.child;
            var primaryChildProps = {
              mode: "hidden",
              children: primaryChildren
            };
            var primaryChildFragment;
            var fallbackChildFragment;
            if ((mode & ConcurrentMode) === NoMode && progressedPrimaryFragment !== null) {
              primaryChildFragment = progressedPrimaryFragment;
              primaryChildFragment.childLanes = NoLanes;
              primaryChildFragment.pendingProps = primaryChildProps;
              if (workInProgress2.mode & ProfileMode) {
                primaryChildFragment.actualDuration = 0;
                primaryChildFragment.actualStartTime = -1;
                primaryChildFragment.selfBaseDuration = 0;
                primaryChildFragment.treeBaseDuration = 0;
              }
              fallbackChildFragment = createFiberFromFragment(fallbackChildren, mode, renderLanes2, null);
            } else {
              primaryChildFragment = mountWorkInProgressOffscreenFiber(primaryChildProps, mode);
              fallbackChildFragment = createFiberFromFragment(fallbackChildren, mode, renderLanes2, null);
            }
            primaryChildFragment.return = workInProgress2;
            fallbackChildFragment.return = workInProgress2;
            primaryChildFragment.sibling = fallbackChildFragment;
            workInProgress2.child = primaryChildFragment;
            return fallbackChildFragment;
          }
          function mountWorkInProgressOffscreenFiber(offscreenProps, mode, renderLanes2) {
            return createFiberFromOffscreen(offscreenProps, mode, NoLanes, null);
          }
          function updateWorkInProgressOffscreenFiber(current2, offscreenProps) {
            return createWorkInProgress(current2, offscreenProps);
          }
          function updateSuspensePrimaryChildren(current2, workInProgress2, primaryChildren, renderLanes2) {
            var currentPrimaryChildFragment = current2.child;
            var currentFallbackChildFragment = currentPrimaryChildFragment.sibling;
            var primaryChildFragment = updateWorkInProgressOffscreenFiber(currentPrimaryChildFragment, {
              mode: "visible",
              children: primaryChildren
            });
            if ((workInProgress2.mode & ConcurrentMode) === NoMode) {
              primaryChildFragment.lanes = renderLanes2;
            }
            primaryChildFragment.return = workInProgress2;
            primaryChildFragment.sibling = null;
            if (currentFallbackChildFragment !== null) {
              var deletions = workInProgress2.deletions;
              if (deletions === null) {
                workInProgress2.deletions = [currentFallbackChildFragment];
                workInProgress2.flags |= ChildDeletion;
              } else {
                deletions.push(currentFallbackChildFragment);
              }
            }
            workInProgress2.child = primaryChildFragment;
            return primaryChildFragment;
          }
          function updateSuspenseFallbackChildren(current2, workInProgress2, primaryChildren, fallbackChildren, renderLanes2) {
            var mode = workInProgress2.mode;
            var currentPrimaryChildFragment = current2.child;
            var currentFallbackChildFragment = currentPrimaryChildFragment.sibling;
            var primaryChildProps = {
              mode: "hidden",
              children: primaryChildren
            };
            var primaryChildFragment;
            if (
              // In legacy mode, we commit the primary tree as if it successfully
              // completed, even though it's in an inconsistent state.
              (mode & ConcurrentMode) === NoMode && // Make sure we're on the second pass, i.e. the primary child fragment was
              // already cloned. In legacy mode, the only case where this isn't true is
              // when DevTools forces us to display a fallback; we skip the first render
              // pass entirely and go straight to rendering the fallback. (In Concurrent
              // Mode, SuspenseList can also trigger this scenario, but this is a legacy-
              // only codepath.)
              workInProgress2.child !== currentPrimaryChildFragment
            ) {
              var progressedPrimaryFragment = workInProgress2.child;
              primaryChildFragment = progressedPrimaryFragment;
              primaryChildFragment.childLanes = NoLanes;
              primaryChildFragment.pendingProps = primaryChildProps;
              if (workInProgress2.mode & ProfileMode) {
                primaryChildFragment.actualDuration = 0;
                primaryChildFragment.actualStartTime = -1;
                primaryChildFragment.selfBaseDuration = currentPrimaryChildFragment.selfBaseDuration;
                primaryChildFragment.treeBaseDuration = currentPrimaryChildFragment.treeBaseDuration;
              }
              workInProgress2.deletions = null;
            } else {
              primaryChildFragment = updateWorkInProgressOffscreenFiber(currentPrimaryChildFragment, primaryChildProps);
              primaryChildFragment.subtreeFlags = currentPrimaryChildFragment.subtreeFlags & StaticMask;
            }
            var fallbackChildFragment;
            if (currentFallbackChildFragment !== null) {
              fallbackChildFragment = createWorkInProgress(currentFallbackChildFragment, fallbackChildren);
            } else {
              fallbackChildFragment = createFiberFromFragment(fallbackChildren, mode, renderLanes2, null);
              fallbackChildFragment.flags |= Placement;
            }
            fallbackChildFragment.return = workInProgress2;
            primaryChildFragment.return = workInProgress2;
            primaryChildFragment.sibling = fallbackChildFragment;
            workInProgress2.child = primaryChildFragment;
            return fallbackChildFragment;
          }
          function retrySuspenseComponentWithoutHydrating(current2, workInProgress2, renderLanes2, recoverableError) {
            if (recoverableError !== null) {
              queueHydrationError(recoverableError);
            }
            reconcileChildFibers(workInProgress2, current2.child, null, renderLanes2);
            var nextProps = workInProgress2.pendingProps;
            var primaryChildren = nextProps.children;
            var primaryChildFragment = mountSuspensePrimaryChildren(workInProgress2, primaryChildren);
            primaryChildFragment.flags |= Placement;
            workInProgress2.memoizedState = null;
            return primaryChildFragment;
          }
          function mountSuspenseFallbackAfterRetryWithoutHydrating(current2, workInProgress2, primaryChildren, fallbackChildren, renderLanes2) {
            var fiberMode = workInProgress2.mode;
            var primaryChildProps = {
              mode: "visible",
              children: primaryChildren
            };
            var primaryChildFragment = mountWorkInProgressOffscreenFiber(primaryChildProps, fiberMode);
            var fallbackChildFragment = createFiberFromFragment(fallbackChildren, fiberMode, renderLanes2, null);
            fallbackChildFragment.flags |= Placement;
            primaryChildFragment.return = workInProgress2;
            fallbackChildFragment.return = workInProgress2;
            primaryChildFragment.sibling = fallbackChildFragment;
            workInProgress2.child = primaryChildFragment;
            if ((workInProgress2.mode & ConcurrentMode) !== NoMode) {
              reconcileChildFibers(workInProgress2, current2.child, null, renderLanes2);
            }
            return fallbackChildFragment;
          }
          function mountDehydratedSuspenseComponent(workInProgress2, suspenseInstance, renderLanes2) {
            if ((workInProgress2.mode & ConcurrentMode) === NoMode) {
              {
                error("Cannot hydrate Suspense in legacy mode. Switch from ReactDOM.hydrate(element, container) to ReactDOMClient.hydrateRoot(container, <App />).render(element) or remove the Suspense components from the server rendered components.");
              }
              workInProgress2.lanes = laneToLanes(SyncLane);
            } else if (isSuspenseInstanceFallback(suspenseInstance)) {
              workInProgress2.lanes = laneToLanes(DefaultHydrationLane);
            } else {
              workInProgress2.lanes = laneToLanes(OffscreenLane);
            }
            return null;
          }
          function updateDehydratedSuspenseComponent(current2, workInProgress2, didSuspend, nextProps, suspenseInstance, suspenseState, renderLanes2) {
            if (!didSuspend) {
              warnIfHydrating();
              if ((workInProgress2.mode & ConcurrentMode) === NoMode) {
                return retrySuspenseComponentWithoutHydrating(
                  current2,
                  workInProgress2,
                  renderLanes2,
                  // TODO: When we delete legacy mode, we should make this error argument
                  // required — every concurrent mode path that causes hydration to
                  // de-opt to client rendering should have an error message.
                  null
                );
              }
              if (isSuspenseInstanceFallback(suspenseInstance)) {
                var digest, message, stack;
                {
                  var _getSuspenseInstanceF = getSuspenseInstanceFallbackErrorDetails(suspenseInstance);
                  digest = _getSuspenseInstanceF.digest;
                  message = _getSuspenseInstanceF.message;
                  stack = _getSuspenseInstanceF.stack;
                }
                var error2;
                if (message) {
                  error2 = new Error(message);
                } else {
                  error2 = new Error("The server could not finish this Suspense boundary, likely due to an error during server rendering. Switched to client rendering.");
                }
                var capturedValue = createCapturedValue(error2, digest, stack);
                return retrySuspenseComponentWithoutHydrating(current2, workInProgress2, renderLanes2, capturedValue);
              }
              var hasContextChanged2 = includesSomeLane(renderLanes2, current2.childLanes);
              if (didReceiveUpdate || hasContextChanged2) {
                var root3 = getWorkInProgressRoot();
                if (root3 !== null) {
                  var attemptHydrationAtLane = getBumpedLaneForHydration(root3, renderLanes2);
                  if (attemptHydrationAtLane !== NoLane && attemptHydrationAtLane !== suspenseState.retryLane) {
                    suspenseState.retryLane = attemptHydrationAtLane;
                    var eventTime = NoTimestamp;
                    enqueueConcurrentRenderForLane(current2, attemptHydrationAtLane);
                    scheduleUpdateOnFiber(root3, current2, attemptHydrationAtLane, eventTime);
                  }
                }
                renderDidSuspendDelayIfPossible();
                var _capturedValue = createCapturedValue(new Error("This Suspense boundary received an update before it finished hydrating. This caused the boundary to switch to client rendering. The usual way to fix this is to wrap the original update in startTransition."));
                return retrySuspenseComponentWithoutHydrating(current2, workInProgress2, renderLanes2, _capturedValue);
              } else if (isSuspenseInstancePending(suspenseInstance)) {
                workInProgress2.flags |= DidCapture;
                workInProgress2.child = current2.child;
                var retry = retryDehydratedSuspenseBoundary.bind(null, current2);
                registerSuspenseInstanceRetry(suspenseInstance, retry);
                return null;
              } else {
                reenterHydrationStateFromDehydratedSuspenseInstance(workInProgress2, suspenseInstance, suspenseState.treeContext);
                var primaryChildren = nextProps.children;
                var primaryChildFragment = mountSuspensePrimaryChildren(workInProgress2, primaryChildren);
                primaryChildFragment.flags |= Hydrating;
                return primaryChildFragment;
              }
            } else {
              if (workInProgress2.flags & ForceClientRender) {
                workInProgress2.flags &= ~ForceClientRender;
                var _capturedValue2 = createCapturedValue(new Error("There was an error while hydrating this Suspense boundary. Switched to client rendering."));
                return retrySuspenseComponentWithoutHydrating(current2, workInProgress2, renderLanes2, _capturedValue2);
              } else if (workInProgress2.memoizedState !== null) {
                workInProgress2.child = current2.child;
                workInProgress2.flags |= DidCapture;
                return null;
              } else {
                var nextPrimaryChildren = nextProps.children;
                var nextFallbackChildren = nextProps.fallback;
                var fallbackChildFragment = mountSuspenseFallbackAfterRetryWithoutHydrating(current2, workInProgress2, nextPrimaryChildren, nextFallbackChildren, renderLanes2);
                var _primaryChildFragment4 = workInProgress2.child;
                _primaryChildFragment4.memoizedState = mountSuspenseOffscreenState(renderLanes2);
                workInProgress2.memoizedState = SUSPENDED_MARKER;
                return fallbackChildFragment;
              }
            }
          }
          function scheduleSuspenseWorkOnFiber(fiber, renderLanes2, propagationRoot) {
            fiber.lanes = mergeLanes(fiber.lanes, renderLanes2);
            var alternate = fiber.alternate;
            if (alternate !== null) {
              alternate.lanes = mergeLanes(alternate.lanes, renderLanes2);
            }
            scheduleContextWorkOnParentPath(fiber.return, renderLanes2, propagationRoot);
          }
          function propagateSuspenseContextChange(workInProgress2, firstChild, renderLanes2) {
            var node = firstChild;
            while (node !== null) {
              if (node.tag === SuspenseComponent) {
                var state = node.memoizedState;
                if (state !== null) {
                  scheduleSuspenseWorkOnFiber(node, renderLanes2, workInProgress2);
                }
              } else if (node.tag === SuspenseListComponent) {
                scheduleSuspenseWorkOnFiber(node, renderLanes2, workInProgress2);
              } else if (node.child !== null) {
                node.child.return = node;
                node = node.child;
                continue;
              }
              if (node === workInProgress2) {
                return;
              }
              while (node.sibling === null) {
                if (node.return === null || node.return === workInProgress2) {
                  return;
                }
                node = node.return;
              }
              node.sibling.return = node.return;
              node = node.sibling;
            }
          }
          function findLastContentRow(firstChild) {
            var row = firstChild;
            var lastContentRow = null;
            while (row !== null) {
              var currentRow = row.alternate;
              if (currentRow !== null && findFirstSuspended(currentRow) === null) {
                lastContentRow = row;
              }
              row = row.sibling;
            }
            return lastContentRow;
          }
          function validateRevealOrder(revealOrder) {
            {
              if (revealOrder !== void 0 && revealOrder !== "forwards" && revealOrder !== "backwards" && revealOrder !== "together" && !didWarnAboutRevealOrder[revealOrder]) {
                didWarnAboutRevealOrder[revealOrder] = true;
                if (typeof revealOrder === "string") {
                  switch (revealOrder.toLowerCase()) {
                    case "together":
                    case "forwards":
                    case "backwards": {
                      error('"%s" is not a valid value for revealOrder on <SuspenseList />. Use lowercase "%s" instead.', revealOrder, revealOrder.toLowerCase());
                      break;
                    }
                    case "forward":
                    case "backward": {
                      error('"%s" is not a valid value for revealOrder on <SuspenseList />. React uses the -s suffix in the spelling. Use "%ss" instead.', revealOrder, revealOrder.toLowerCase());
                      break;
                    }
                    default:
                      error('"%s" is not a supported revealOrder on <SuspenseList />. Did you mean "together", "forwards" or "backwards"?', revealOrder);
                      break;
                  }
                } else {
                  error('%s is not a supported value for revealOrder on <SuspenseList />. Did you mean "together", "forwards" or "backwards"?', revealOrder);
                }
              }
            }
          }
          function validateTailOptions(tailMode, revealOrder) {
            {
              if (tailMode !== void 0 && !didWarnAboutTailOptions[tailMode]) {
                if (tailMode !== "collapsed" && tailMode !== "hidden") {
                  didWarnAboutTailOptions[tailMode] = true;
                  error('"%s" is not a supported value for tail on <SuspenseList />. Did you mean "collapsed" or "hidden"?', tailMode);
                } else if (revealOrder !== "forwards" && revealOrder !== "backwards") {
                  didWarnAboutTailOptions[tailMode] = true;
                  error('<SuspenseList tail="%s" /> is only valid if revealOrder is "forwards" or "backwards". Did you mean to specify revealOrder="forwards"?', tailMode);
                }
              }
            }
          }
          function validateSuspenseListNestedChild(childSlot, index2) {
            {
              var isAnArray = isArray(childSlot);
              var isIterable = !isAnArray && typeof getIteratorFn(childSlot) === "function";
              if (isAnArray || isIterable) {
                var type = isAnArray ? "array" : "iterable";
                error("A nested %s was passed to row #%s in <SuspenseList />. Wrap it in an additional SuspenseList to configure its revealOrder: <SuspenseList revealOrder=...> ... <SuspenseList revealOrder=...>{%s}</SuspenseList> ... </SuspenseList>", type, index2, type);
                return false;
              }
            }
            return true;
          }
          function validateSuspenseListChildren(children, revealOrder) {
            {
              if ((revealOrder === "forwards" || revealOrder === "backwards") && children !== void 0 && children !== null && children !== false) {
                if (isArray(children)) {
                  for (var i = 0; i < children.length; i++) {
                    if (!validateSuspenseListNestedChild(children[i], i)) {
                      return;
                    }
                  }
                } else {
                  var iteratorFn = getIteratorFn(children);
                  if (typeof iteratorFn === "function") {
                    var childrenIterator = iteratorFn.call(children);
                    if (childrenIterator) {
                      var step = childrenIterator.next();
                      var _i = 0;
                      for (; !step.done; step = childrenIterator.next()) {
                        if (!validateSuspenseListNestedChild(step.value, _i)) {
                          return;
                        }
                        _i++;
                      }
                    }
                  } else {
                    error('A single row was passed to a <SuspenseList revealOrder="%s" />. This is not useful since it needs multiple rows. Did you mean to pass multiple children or an array?', revealOrder);
                  }
                }
              }
            }
          }
          function initSuspenseListRenderState(workInProgress2, isBackwards, tail, lastContentRow, tailMode) {
            var renderState = workInProgress2.memoizedState;
            if (renderState === null) {
              workInProgress2.memoizedState = {
                isBackwards,
                rendering: null,
                renderingStartTime: 0,
                last: lastContentRow,
                tail,
                tailMode
              };
            } else {
              renderState.isBackwards = isBackwards;
              renderState.rendering = null;
              renderState.renderingStartTime = 0;
              renderState.last = lastContentRow;
              renderState.tail = tail;
              renderState.tailMode = tailMode;
            }
          }
          function updateSuspenseListComponent(current2, workInProgress2, renderLanes2) {
            var nextProps = workInProgress2.pendingProps;
            var revealOrder = nextProps.revealOrder;
            var tailMode = nextProps.tail;
            var newChildren = nextProps.children;
            validateRevealOrder(revealOrder);
            validateTailOptions(tailMode, revealOrder);
            validateSuspenseListChildren(newChildren, revealOrder);
            reconcileChildren(current2, workInProgress2, newChildren, renderLanes2);
            var suspenseContext = suspenseStackCursor.current;
            var shouldForceFallback = hasSuspenseContext(suspenseContext, ForceSuspenseFallback);
            if (shouldForceFallback) {
              suspenseContext = setShallowSuspenseContext(suspenseContext, ForceSuspenseFallback);
              workInProgress2.flags |= DidCapture;
            } else {
              var didSuspendBefore = current2 !== null && (current2.flags & DidCapture) !== NoFlags;
              if (didSuspendBefore) {
                propagateSuspenseContextChange(workInProgress2, workInProgress2.child, renderLanes2);
              }
              suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
            }
            pushSuspenseContext(workInProgress2, suspenseContext);
            if ((workInProgress2.mode & ConcurrentMode) === NoMode) {
              workInProgress2.memoizedState = null;
            } else {
              switch (revealOrder) {
                case "forwards": {
                  var lastContentRow = findLastContentRow(workInProgress2.child);
                  var tail;
                  if (lastContentRow === null) {
                    tail = workInProgress2.child;
                    workInProgress2.child = null;
                  } else {
                    tail = lastContentRow.sibling;
                    lastContentRow.sibling = null;
                  }
                  initSuspenseListRenderState(
                    workInProgress2,
                    false,
                    // isBackwards
                    tail,
                    lastContentRow,
                    tailMode
                  );
                  break;
                }
                case "backwards": {
                  var _tail = null;
                  var row = workInProgress2.child;
                  workInProgress2.child = null;
                  while (row !== null) {
                    var currentRow = row.alternate;
                    if (currentRow !== null && findFirstSuspended(currentRow) === null) {
                      workInProgress2.child = row;
                      break;
                    }
                    var nextRow = row.sibling;
                    row.sibling = _tail;
                    _tail = row;
                    row = nextRow;
                  }
                  initSuspenseListRenderState(
                    workInProgress2,
                    true,
                    // isBackwards
                    _tail,
                    null,
                    // last
                    tailMode
                  );
                  break;
                }
                case "together": {
                  initSuspenseListRenderState(
                    workInProgress2,
                    false,
                    // isBackwards
                    null,
                    // tail
                    null,
                    // last
                    void 0
                  );
                  break;
                }
                default: {
                  workInProgress2.memoizedState = null;
                }
              }
            }
            return workInProgress2.child;
          }
          function updatePortalComponent(current2, workInProgress2, renderLanes2) {
            pushHostContainer(workInProgress2, workInProgress2.stateNode.containerInfo);
            var nextChildren = workInProgress2.pendingProps;
            if (current2 === null) {
              workInProgress2.child = reconcileChildFibers(workInProgress2, null, nextChildren, renderLanes2);
            } else {
              reconcileChildren(current2, workInProgress2, nextChildren, renderLanes2);
            }
            return workInProgress2.child;
          }
          var hasWarnedAboutUsingNoValuePropOnContextProvider = false;
          function updateContextProvider(current2, workInProgress2, renderLanes2) {
            var providerType = workInProgress2.type;
            var context = providerType._context;
            var newProps = workInProgress2.pendingProps;
            var oldProps = workInProgress2.memoizedProps;
            var newValue = newProps.value;
            {
              if (!("value" in newProps)) {
                if (!hasWarnedAboutUsingNoValuePropOnContextProvider) {
                  hasWarnedAboutUsingNoValuePropOnContextProvider = true;
                  error("The `value` prop is required for the `<Context.Provider>`. Did you misspell it or forget to pass it?");
                }
              }
              var providerPropTypes = workInProgress2.type.propTypes;
              if (providerPropTypes) {
                checkPropTypes(providerPropTypes, newProps, "prop", "Context.Provider");
              }
            }
            pushProvider(workInProgress2, context, newValue);
            {
              if (oldProps !== null) {
                var oldValue = oldProps.value;
                if (objectIs(oldValue, newValue)) {
                  if (oldProps.children === newProps.children && !hasContextChanged()) {
                    return bailoutOnAlreadyFinishedWork(current2, workInProgress2, renderLanes2);
                  }
                } else {
                  propagateContextChange(workInProgress2, context, renderLanes2);
                }
              }
            }
            var newChildren = newProps.children;
            reconcileChildren(current2, workInProgress2, newChildren, renderLanes2);
            return workInProgress2.child;
          }
          var hasWarnedAboutUsingContextAsConsumer = false;
          function updateContextConsumer(current2, workInProgress2, renderLanes2) {
            var context = workInProgress2.type;
            {
              if (context._context === void 0) {
                if (context !== context.Consumer) {
                  if (!hasWarnedAboutUsingContextAsConsumer) {
                    hasWarnedAboutUsingContextAsConsumer = true;
                    error("Rendering <Context> directly is not supported and will be removed in a future major release. Did you mean to render <Context.Consumer> instead?");
                  }
                }
              } else {
                context = context._context;
              }
            }
            var newProps = workInProgress2.pendingProps;
            var render2 = newProps.children;
            {
              if (typeof render2 !== "function") {
                error("A context consumer was rendered with multiple children, or a child that isn't a function. A context consumer expects a single child that is a function. If you did pass a function, make sure there is no trailing or leading whitespace around it.");
              }
            }
            prepareToReadContext(workInProgress2, renderLanes2);
            var newValue = readContext(context);
            {
              markComponentRenderStarted(workInProgress2);
            }
            var newChildren;
            {
              ReactCurrentOwner$1.current = workInProgress2;
              setIsRendering(true);
              newChildren = render2(newValue);
              setIsRendering(false);
            }
            {
              markComponentRenderStopped();
            }
            workInProgress2.flags |= PerformedWork;
            reconcileChildren(current2, workInProgress2, newChildren, renderLanes2);
            return workInProgress2.child;
          }
          function markWorkInProgressReceivedUpdate() {
            didReceiveUpdate = true;
          }
          function resetSuspendedCurrentOnMountInLegacyMode(current2, workInProgress2) {
            if ((workInProgress2.mode & ConcurrentMode) === NoMode) {
              if (current2 !== null) {
                current2.alternate = null;
                workInProgress2.alternate = null;
                workInProgress2.flags |= Placement;
              }
            }
          }
          function bailoutOnAlreadyFinishedWork(current2, workInProgress2, renderLanes2) {
            if (current2 !== null) {
              workInProgress2.dependencies = current2.dependencies;
            }
            {
              stopProfilerTimerIfRunning();
            }
            markSkippedUpdateLanes(workInProgress2.lanes);
            if (!includesSomeLane(renderLanes2, workInProgress2.childLanes)) {
              {
                return null;
              }
            }
            cloneChildFibers(current2, workInProgress2);
            return workInProgress2.child;
          }
          function remountFiber(current2, oldWorkInProgress, newWorkInProgress) {
            {
              var returnFiber = oldWorkInProgress.return;
              if (returnFiber === null) {
                throw new Error("Cannot swap the root fiber.");
              }
              current2.alternate = null;
              oldWorkInProgress.alternate = null;
              newWorkInProgress.index = oldWorkInProgress.index;
              newWorkInProgress.sibling = oldWorkInProgress.sibling;
              newWorkInProgress.return = oldWorkInProgress.return;
              newWorkInProgress.ref = oldWorkInProgress.ref;
              if (oldWorkInProgress === returnFiber.child) {
                returnFiber.child = newWorkInProgress;
              } else {
                var prevSibling = returnFiber.child;
                if (prevSibling === null) {
                  throw new Error("Expected parent to have a child.");
                }
                while (prevSibling.sibling !== oldWorkInProgress) {
                  prevSibling = prevSibling.sibling;
                  if (prevSibling === null) {
                    throw new Error("Expected to find the previous sibling.");
                  }
                }
                prevSibling.sibling = newWorkInProgress;
              }
              var deletions = returnFiber.deletions;
              if (deletions === null) {
                returnFiber.deletions = [current2];
                returnFiber.flags |= ChildDeletion;
              } else {
                deletions.push(current2);
              }
              newWorkInProgress.flags |= Placement;
              return newWorkInProgress;
            }
          }
          function checkScheduledUpdateOrContext(current2, renderLanes2) {
            var updateLanes = current2.lanes;
            if (includesSomeLane(updateLanes, renderLanes2)) {
              return true;
            }
            return false;
          }
          function attemptEarlyBailoutIfNoScheduledUpdate(current2, workInProgress2, renderLanes2) {
            switch (workInProgress2.tag) {
              case HostRoot:
                pushHostRootContext(workInProgress2);
                var root3 = workInProgress2.stateNode;
                resetHydrationState();
                break;
              case HostComponent:
                pushHostContext(workInProgress2);
                break;
              case ClassComponent: {
                var Component = workInProgress2.type;
                if (isContextProvider(Component)) {
                  pushContextProvider(workInProgress2);
                }
                break;
              }
              case HostPortal:
                pushHostContainer(workInProgress2, workInProgress2.stateNode.containerInfo);
                break;
              case ContextProvider: {
                var newValue = workInProgress2.memoizedProps.value;
                var context = workInProgress2.type._context;
                pushProvider(workInProgress2, context, newValue);
                break;
              }
              case Profiler:
                {
                  var hasChildWork = includesSomeLane(renderLanes2, workInProgress2.childLanes);
                  if (hasChildWork) {
                    workInProgress2.flags |= Update;
                  }
                  {
                    var stateNode = workInProgress2.stateNode;
                    stateNode.effectDuration = 0;
                    stateNode.passiveEffectDuration = 0;
                  }
                }
                break;
              case SuspenseComponent: {
                var state = workInProgress2.memoizedState;
                if (state !== null) {
                  if (state.dehydrated !== null) {
                    pushSuspenseContext(workInProgress2, setDefaultShallowSuspenseContext(suspenseStackCursor.current));
                    workInProgress2.flags |= DidCapture;
                    return null;
                  }
                  var primaryChildFragment = workInProgress2.child;
                  var primaryChildLanes = primaryChildFragment.childLanes;
                  if (includesSomeLane(renderLanes2, primaryChildLanes)) {
                    return updateSuspenseComponent(current2, workInProgress2, renderLanes2);
                  } else {
                    pushSuspenseContext(workInProgress2, setDefaultShallowSuspenseContext(suspenseStackCursor.current));
                    var child = bailoutOnAlreadyFinishedWork(current2, workInProgress2, renderLanes2);
                    if (child !== null) {
                      return child.sibling;
                    } else {
                      return null;
                    }
                  }
                } else {
                  pushSuspenseContext(workInProgress2, setDefaultShallowSuspenseContext(suspenseStackCursor.current));
                }
                break;
              }
              case SuspenseListComponent: {
                var didSuspendBefore = (current2.flags & DidCapture) !== NoFlags;
                var _hasChildWork = includesSomeLane(renderLanes2, workInProgress2.childLanes);
                if (didSuspendBefore) {
                  if (_hasChildWork) {
                    return updateSuspenseListComponent(current2, workInProgress2, renderLanes2);
                  }
                  workInProgress2.flags |= DidCapture;
                }
                var renderState = workInProgress2.memoizedState;
                if (renderState !== null) {
                  renderState.rendering = null;
                  renderState.tail = null;
                  renderState.lastEffect = null;
                }
                pushSuspenseContext(workInProgress2, suspenseStackCursor.current);
                if (_hasChildWork) {
                  break;
                } else {
                  return null;
                }
              }
              case OffscreenComponent:
              case LegacyHiddenComponent: {
                workInProgress2.lanes = NoLanes;
                return updateOffscreenComponent(current2, workInProgress2, renderLanes2);
              }
            }
            return bailoutOnAlreadyFinishedWork(current2, workInProgress2, renderLanes2);
          }
          function beginWork(current2, workInProgress2, renderLanes2) {
            {
              if (workInProgress2._debugNeedsRemount && current2 !== null) {
                return remountFiber(current2, workInProgress2, createFiberFromTypeAndProps(workInProgress2.type, workInProgress2.key, workInProgress2.pendingProps, workInProgress2._debugOwner || null, workInProgress2.mode, workInProgress2.lanes));
              }
            }
            if (current2 !== null) {
              var oldProps = current2.memoizedProps;
              var newProps = workInProgress2.pendingProps;
              if (oldProps !== newProps || hasContextChanged() || // Force a re-render if the implementation changed due to hot reload:
              workInProgress2.type !== current2.type) {
                didReceiveUpdate = true;
              } else {
                var hasScheduledUpdateOrContext = checkScheduledUpdateOrContext(current2, renderLanes2);
                if (!hasScheduledUpdateOrContext && // If this is the second pass of an error or suspense boundary, there
                // may not be work scheduled on `current`, so we check for this flag.
                (workInProgress2.flags & DidCapture) === NoFlags) {
                  didReceiveUpdate = false;
                  return attemptEarlyBailoutIfNoScheduledUpdate(current2, workInProgress2, renderLanes2);
                }
                if ((current2.flags & ForceUpdateForLegacySuspense) !== NoFlags) {
                  didReceiveUpdate = true;
                } else {
                  didReceiveUpdate = false;
                }
              }
            } else {
              didReceiveUpdate = false;
              if (getIsHydrating() && isForkedChild(workInProgress2)) {
                var slotIndex = workInProgress2.index;
                var numberOfForks = getForksAtLevel();
                pushTreeId(workInProgress2, numberOfForks, slotIndex);
              }
            }
            workInProgress2.lanes = NoLanes;
            switch (workInProgress2.tag) {
              case IndeterminateComponent: {
                return mountIndeterminateComponent(current2, workInProgress2, workInProgress2.type, renderLanes2);
              }
              case LazyComponent: {
                var elementType = workInProgress2.elementType;
                return mountLazyComponent(current2, workInProgress2, elementType, renderLanes2);
              }
              case FunctionComponent: {
                var Component = workInProgress2.type;
                var unresolvedProps = workInProgress2.pendingProps;
                var resolvedProps = workInProgress2.elementType === Component ? unresolvedProps : resolveDefaultProps(Component, unresolvedProps);
                return updateFunctionComponent(current2, workInProgress2, Component, resolvedProps, renderLanes2);
              }
              case ClassComponent: {
                var _Component = workInProgress2.type;
                var _unresolvedProps = workInProgress2.pendingProps;
                var _resolvedProps = workInProgress2.elementType === _Component ? _unresolvedProps : resolveDefaultProps(_Component, _unresolvedProps);
                return updateClassComponent(current2, workInProgress2, _Component, _resolvedProps, renderLanes2);
              }
              case HostRoot:
                return updateHostRoot(current2, workInProgress2, renderLanes2);
              case HostComponent:
                return updateHostComponent(current2, workInProgress2, renderLanes2);
              case HostText:
                return updateHostText(current2, workInProgress2);
              case SuspenseComponent:
                return updateSuspenseComponent(current2, workInProgress2, renderLanes2);
              case HostPortal:
                return updatePortalComponent(current2, workInProgress2, renderLanes2);
              case ForwardRef: {
                var type = workInProgress2.type;
                var _unresolvedProps2 = workInProgress2.pendingProps;
                var _resolvedProps2 = workInProgress2.elementType === type ? _unresolvedProps2 : resolveDefaultProps(type, _unresolvedProps2);
                return updateForwardRef(current2, workInProgress2, type, _resolvedProps2, renderLanes2);
              }
              case Fragment3:
                return updateFragment(current2, workInProgress2, renderLanes2);
              case Mode:
                return updateMode(current2, workInProgress2, renderLanes2);
              case Profiler:
                return updateProfiler(current2, workInProgress2, renderLanes2);
              case ContextProvider:
                return updateContextProvider(current2, workInProgress2, renderLanes2);
              case ContextConsumer:
                return updateContextConsumer(current2, workInProgress2, renderLanes2);
              case MemoComponent: {
                var _type2 = workInProgress2.type;
                var _unresolvedProps3 = workInProgress2.pendingProps;
                var _resolvedProps3 = resolveDefaultProps(_type2, _unresolvedProps3);
                {
                  if (workInProgress2.type !== workInProgress2.elementType) {
                    var outerPropTypes = _type2.propTypes;
                    if (outerPropTypes) {
                      checkPropTypes(
                        outerPropTypes,
                        _resolvedProps3,
                        // Resolved for outer only
                        "prop",
                        getComponentNameFromType(_type2)
                      );
                    }
                  }
                }
                _resolvedProps3 = resolveDefaultProps(_type2.type, _resolvedProps3);
                return updateMemoComponent(current2, workInProgress2, _type2, _resolvedProps3, renderLanes2);
              }
              case SimpleMemoComponent: {
                return updateSimpleMemoComponent(current2, workInProgress2, workInProgress2.type, workInProgress2.pendingProps, renderLanes2);
              }
              case IncompleteClassComponent: {
                var _Component2 = workInProgress2.type;
                var _unresolvedProps4 = workInProgress2.pendingProps;
                var _resolvedProps4 = workInProgress2.elementType === _Component2 ? _unresolvedProps4 : resolveDefaultProps(_Component2, _unresolvedProps4);
                return mountIncompleteClassComponent(current2, workInProgress2, _Component2, _resolvedProps4, renderLanes2);
              }
              case SuspenseListComponent: {
                return updateSuspenseListComponent(current2, workInProgress2, renderLanes2);
              }
              case ScopeComponent: {
                break;
              }
              case OffscreenComponent: {
                return updateOffscreenComponent(current2, workInProgress2, renderLanes2);
              }
            }
            throw new Error("Unknown unit of work tag (" + workInProgress2.tag + "). This error is likely caused by a bug in React. Please file an issue.");
          }
          function markUpdate(workInProgress2) {
            workInProgress2.flags |= Update;
          }
          function markRef$1(workInProgress2) {
            workInProgress2.flags |= Ref;
            {
              workInProgress2.flags |= RefStatic;
            }
          }
          var appendAllChildren;
          var updateHostContainer;
          var updateHostComponent$1;
          var updateHostText$1;
          {
            appendAllChildren = function(parent, workInProgress2, needsVisibilityToggle, isHidden) {
              var node = workInProgress2.child;
              while (node !== null) {
                if (node.tag === HostComponent || node.tag === HostText) {
                  appendInitialChild(parent, node.stateNode);
                } else if (node.tag === HostPortal) ;
                else if (node.child !== null) {
                  node.child.return = node;
                  node = node.child;
                  continue;
                }
                if (node === workInProgress2) {
                  return;
                }
                while (node.sibling === null) {
                  if (node.return === null || node.return === workInProgress2) {
                    return;
                  }
                  node = node.return;
                }
                node.sibling.return = node.return;
                node = node.sibling;
              }
            };
            updateHostContainer = function(current2, workInProgress2) {
            };
            updateHostComponent$1 = function(current2, workInProgress2, type, newProps, rootContainerInstance) {
              var oldProps = current2.memoizedProps;
              if (oldProps === newProps) {
                return;
              }
              var instance = workInProgress2.stateNode;
              var currentHostContext = getHostContext();
              var updatePayload = prepareUpdate(instance, type, oldProps, newProps, rootContainerInstance, currentHostContext);
              workInProgress2.updateQueue = updatePayload;
              if (updatePayload) {
                markUpdate(workInProgress2);
              }
            };
            updateHostText$1 = function(current2, workInProgress2, oldText, newText) {
              if (oldText !== newText) {
                markUpdate(workInProgress2);
              }
            };
          }
          function cutOffTailIfNeeded(renderState, hasRenderedATailFallback) {
            if (getIsHydrating()) {
              return;
            }
            switch (renderState.tailMode) {
              case "hidden": {
                var tailNode = renderState.tail;
                var lastTailNode = null;
                while (tailNode !== null) {
                  if (tailNode.alternate !== null) {
                    lastTailNode = tailNode;
                  }
                  tailNode = tailNode.sibling;
                }
                if (lastTailNode === null) {
                  renderState.tail = null;
                } else {
                  lastTailNode.sibling = null;
                }
                break;
              }
              case "collapsed": {
                var _tailNode = renderState.tail;
                var _lastTailNode = null;
                while (_tailNode !== null) {
                  if (_tailNode.alternate !== null) {
                    _lastTailNode = _tailNode;
                  }
                  _tailNode = _tailNode.sibling;
                }
                if (_lastTailNode === null) {
                  if (!hasRenderedATailFallback && renderState.tail !== null) {
                    renderState.tail.sibling = null;
                  } else {
                    renderState.tail = null;
                  }
                } else {
                  _lastTailNode.sibling = null;
                }
                break;
              }
            }
          }
          function bubbleProperties(completedWork) {
            var didBailout = completedWork.alternate !== null && completedWork.alternate.child === completedWork.child;
            var newChildLanes = NoLanes;
            var subtreeFlags = NoFlags;
            if (!didBailout) {
              if ((completedWork.mode & ProfileMode) !== NoMode) {
                var actualDuration = completedWork.actualDuration;
                var treeBaseDuration = completedWork.selfBaseDuration;
                var child = completedWork.child;
                while (child !== null) {
                  newChildLanes = mergeLanes(newChildLanes, mergeLanes(child.lanes, child.childLanes));
                  subtreeFlags |= child.subtreeFlags;
                  subtreeFlags |= child.flags;
                  actualDuration += child.actualDuration;
                  treeBaseDuration += child.treeBaseDuration;
                  child = child.sibling;
                }
                completedWork.actualDuration = actualDuration;
                completedWork.treeBaseDuration = treeBaseDuration;
              } else {
                var _child = completedWork.child;
                while (_child !== null) {
                  newChildLanes = mergeLanes(newChildLanes, mergeLanes(_child.lanes, _child.childLanes));
                  subtreeFlags |= _child.subtreeFlags;
                  subtreeFlags |= _child.flags;
                  _child.return = completedWork;
                  _child = _child.sibling;
                }
              }
              completedWork.subtreeFlags |= subtreeFlags;
            } else {
              if ((completedWork.mode & ProfileMode) !== NoMode) {
                var _treeBaseDuration = completedWork.selfBaseDuration;
                var _child2 = completedWork.child;
                while (_child2 !== null) {
                  newChildLanes = mergeLanes(newChildLanes, mergeLanes(_child2.lanes, _child2.childLanes));
                  subtreeFlags |= _child2.subtreeFlags & StaticMask;
                  subtreeFlags |= _child2.flags & StaticMask;
                  _treeBaseDuration += _child2.treeBaseDuration;
                  _child2 = _child2.sibling;
                }
                completedWork.treeBaseDuration = _treeBaseDuration;
              } else {
                var _child3 = completedWork.child;
                while (_child3 !== null) {
                  newChildLanes = mergeLanes(newChildLanes, mergeLanes(_child3.lanes, _child3.childLanes));
                  subtreeFlags |= _child3.subtreeFlags & StaticMask;
                  subtreeFlags |= _child3.flags & StaticMask;
                  _child3.return = completedWork;
                  _child3 = _child3.sibling;
                }
              }
              completedWork.subtreeFlags |= subtreeFlags;
            }
            completedWork.childLanes = newChildLanes;
            return didBailout;
          }
          function completeDehydratedSuspenseBoundary(current2, workInProgress2, nextState) {
            if (hasUnhydratedTailNodes() && (workInProgress2.mode & ConcurrentMode) !== NoMode && (workInProgress2.flags & DidCapture) === NoFlags) {
              warnIfUnhydratedTailNodes(workInProgress2);
              resetHydrationState();
              workInProgress2.flags |= ForceClientRender | Incomplete | ShouldCapture;
              return false;
            }
            var wasHydrated = popHydrationState(workInProgress2);
            if (nextState !== null && nextState.dehydrated !== null) {
              if (current2 === null) {
                if (!wasHydrated) {
                  throw new Error("A dehydrated suspense component was completed without a hydrated node. This is probably a bug in React.");
                }
                prepareToHydrateHostSuspenseInstance(workInProgress2);
                bubbleProperties(workInProgress2);
                {
                  if ((workInProgress2.mode & ProfileMode) !== NoMode) {
                    var isTimedOutSuspense = nextState !== null;
                    if (isTimedOutSuspense) {
                      var primaryChildFragment = workInProgress2.child;
                      if (primaryChildFragment !== null) {
                        workInProgress2.treeBaseDuration -= primaryChildFragment.treeBaseDuration;
                      }
                    }
                  }
                }
                return false;
              } else {
                resetHydrationState();
                if ((workInProgress2.flags & DidCapture) === NoFlags) {
                  workInProgress2.memoizedState = null;
                }
                workInProgress2.flags |= Update;
                bubbleProperties(workInProgress2);
                {
                  if ((workInProgress2.mode & ProfileMode) !== NoMode) {
                    var _isTimedOutSuspense = nextState !== null;
                    if (_isTimedOutSuspense) {
                      var _primaryChildFragment = workInProgress2.child;
                      if (_primaryChildFragment !== null) {
                        workInProgress2.treeBaseDuration -= _primaryChildFragment.treeBaseDuration;
                      }
                    }
                  }
                }
                return false;
              }
            } else {
              upgradeHydrationErrorsToRecoverable();
              return true;
            }
          }
          function completeWork(current2, workInProgress2, renderLanes2) {
            var newProps = workInProgress2.pendingProps;
            popTreeContext(workInProgress2);
            switch (workInProgress2.tag) {
              case IndeterminateComponent:
              case LazyComponent:
              case SimpleMemoComponent:
              case FunctionComponent:
              case ForwardRef:
              case Fragment3:
              case Mode:
              case Profiler:
              case ContextConsumer:
              case MemoComponent:
                bubbleProperties(workInProgress2);
                return null;
              case ClassComponent: {
                var Component = workInProgress2.type;
                if (isContextProvider(Component)) {
                  popContext(workInProgress2);
                }
                bubbleProperties(workInProgress2);
                return null;
              }
              case HostRoot: {
                var fiberRoot = workInProgress2.stateNode;
                popHostContainer(workInProgress2);
                popTopLevelContextObject(workInProgress2);
                resetWorkInProgressVersions();
                if (fiberRoot.pendingContext) {
                  fiberRoot.context = fiberRoot.pendingContext;
                  fiberRoot.pendingContext = null;
                }
                if (current2 === null || current2.child === null) {
                  var wasHydrated = popHydrationState(workInProgress2);
                  if (wasHydrated) {
                    markUpdate(workInProgress2);
                  } else {
                    if (current2 !== null) {
                      var prevState = current2.memoizedState;
                      if (
                        // Check if this is a client root
                        !prevState.isDehydrated || // Check if we reverted to client rendering (e.g. due to an error)
                        (workInProgress2.flags & ForceClientRender) !== NoFlags
                      ) {
                        workInProgress2.flags |= Snapshot;
                        upgradeHydrationErrorsToRecoverable();
                      }
                    }
                  }
                }
                updateHostContainer(current2, workInProgress2);
                bubbleProperties(workInProgress2);
                return null;
              }
              case HostComponent: {
                popHostContext(workInProgress2);
                var rootContainerInstance = getRootHostContainer();
                var type = workInProgress2.type;
                if (current2 !== null && workInProgress2.stateNode != null) {
                  updateHostComponent$1(current2, workInProgress2, type, newProps, rootContainerInstance);
                  if (current2.ref !== workInProgress2.ref) {
                    markRef$1(workInProgress2);
                  }
                } else {
                  if (!newProps) {
                    if (workInProgress2.stateNode === null) {
                      throw new Error("We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.");
                    }
                    bubbleProperties(workInProgress2);
                    return null;
                  }
                  var currentHostContext = getHostContext();
                  var _wasHydrated = popHydrationState(workInProgress2);
                  if (_wasHydrated) {
                    if (prepareToHydrateHostInstance(workInProgress2, rootContainerInstance, currentHostContext)) {
                      markUpdate(workInProgress2);
                    }
                  } else {
                    var instance = createInstance(type, newProps, rootContainerInstance, currentHostContext, workInProgress2);
                    appendAllChildren(instance, workInProgress2, false, false);
                    workInProgress2.stateNode = instance;
                    if (finalizeInitialChildren(instance, type, newProps, rootContainerInstance)) {
                      markUpdate(workInProgress2);
                    }
                  }
                  if (workInProgress2.ref !== null) {
                    markRef$1(workInProgress2);
                  }
                }
                bubbleProperties(workInProgress2);
                return null;
              }
              case HostText: {
                var newText = newProps;
                if (current2 && workInProgress2.stateNode != null) {
                  var oldText = current2.memoizedProps;
                  updateHostText$1(current2, workInProgress2, oldText, newText);
                } else {
                  if (typeof newText !== "string") {
                    if (workInProgress2.stateNode === null) {
                      throw new Error("We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.");
                    }
                  }
                  var _rootContainerInstance = getRootHostContainer();
                  var _currentHostContext = getHostContext();
                  var _wasHydrated2 = popHydrationState(workInProgress2);
                  if (_wasHydrated2) {
                    if (prepareToHydrateHostTextInstance(workInProgress2)) {
                      markUpdate(workInProgress2);
                    }
                  } else {
                    workInProgress2.stateNode = createTextInstance(newText, _rootContainerInstance, _currentHostContext, workInProgress2);
                  }
                }
                bubbleProperties(workInProgress2);
                return null;
              }
              case SuspenseComponent: {
                popSuspenseContext(workInProgress2);
                var nextState = workInProgress2.memoizedState;
                if (current2 === null || current2.memoizedState !== null && current2.memoizedState.dehydrated !== null) {
                  var fallthroughToNormalSuspensePath = completeDehydratedSuspenseBoundary(current2, workInProgress2, nextState);
                  if (!fallthroughToNormalSuspensePath) {
                    if (workInProgress2.flags & ShouldCapture) {
                      return workInProgress2;
                    } else {
                      return null;
                    }
                  }
                }
                if ((workInProgress2.flags & DidCapture) !== NoFlags) {
                  workInProgress2.lanes = renderLanes2;
                  if ((workInProgress2.mode & ProfileMode) !== NoMode) {
                    transferActualDuration(workInProgress2);
                  }
                  return workInProgress2;
                }
                var nextDidTimeout = nextState !== null;
                var prevDidTimeout = current2 !== null && current2.memoizedState !== null;
                if (nextDidTimeout !== prevDidTimeout) {
                  if (nextDidTimeout) {
                    var _offscreenFiber2 = workInProgress2.child;
                    _offscreenFiber2.flags |= Visibility;
                    if ((workInProgress2.mode & ConcurrentMode) !== NoMode) {
                      var hasInvisibleChildContext = current2 === null && (workInProgress2.memoizedProps.unstable_avoidThisFallback !== true || !enableSuspenseAvoidThisFallback);
                      if (hasInvisibleChildContext || hasSuspenseContext(suspenseStackCursor.current, InvisibleParentSuspenseContext)) {
                        renderDidSuspend();
                      } else {
                        renderDidSuspendDelayIfPossible();
                      }
                    }
                  }
                }
                var wakeables = workInProgress2.updateQueue;
                if (wakeables !== null) {
                  workInProgress2.flags |= Update;
                }
                bubbleProperties(workInProgress2);
                {
                  if ((workInProgress2.mode & ProfileMode) !== NoMode) {
                    if (nextDidTimeout) {
                      var primaryChildFragment = workInProgress2.child;
                      if (primaryChildFragment !== null) {
                        workInProgress2.treeBaseDuration -= primaryChildFragment.treeBaseDuration;
                      }
                    }
                  }
                }
                return null;
              }
              case HostPortal:
                popHostContainer(workInProgress2);
                updateHostContainer(current2, workInProgress2);
                if (current2 === null) {
                  preparePortalMount(workInProgress2.stateNode.containerInfo);
                }
                bubbleProperties(workInProgress2);
                return null;
              case ContextProvider:
                var context = workInProgress2.type._context;
                popProvider(context, workInProgress2);
                bubbleProperties(workInProgress2);
                return null;
              case IncompleteClassComponent: {
                var _Component = workInProgress2.type;
                if (isContextProvider(_Component)) {
                  popContext(workInProgress2);
                }
                bubbleProperties(workInProgress2);
                return null;
              }
              case SuspenseListComponent: {
                popSuspenseContext(workInProgress2);
                var renderState = workInProgress2.memoizedState;
                if (renderState === null) {
                  bubbleProperties(workInProgress2);
                  return null;
                }
                var didSuspendAlready = (workInProgress2.flags & DidCapture) !== NoFlags;
                var renderedTail = renderState.rendering;
                if (renderedTail === null) {
                  if (!didSuspendAlready) {
                    var cannotBeSuspended = renderHasNotSuspendedYet() && (current2 === null || (current2.flags & DidCapture) === NoFlags);
                    if (!cannotBeSuspended) {
                      var row = workInProgress2.child;
                      while (row !== null) {
                        var suspended = findFirstSuspended(row);
                        if (suspended !== null) {
                          didSuspendAlready = true;
                          workInProgress2.flags |= DidCapture;
                          cutOffTailIfNeeded(renderState, false);
                          var newThenables = suspended.updateQueue;
                          if (newThenables !== null) {
                            workInProgress2.updateQueue = newThenables;
                            workInProgress2.flags |= Update;
                          }
                          workInProgress2.subtreeFlags = NoFlags;
                          resetChildFibers(workInProgress2, renderLanes2);
                          pushSuspenseContext(workInProgress2, setShallowSuspenseContext(suspenseStackCursor.current, ForceSuspenseFallback));
                          return workInProgress2.child;
                        }
                        row = row.sibling;
                      }
                    }
                    if (renderState.tail !== null && now() > getRenderTargetTime()) {
                      workInProgress2.flags |= DidCapture;
                      didSuspendAlready = true;
                      cutOffTailIfNeeded(renderState, false);
                      workInProgress2.lanes = SomeRetryLane;
                    }
                  } else {
                    cutOffTailIfNeeded(renderState, false);
                  }
                } else {
                  if (!didSuspendAlready) {
                    var _suspended = findFirstSuspended(renderedTail);
                    if (_suspended !== null) {
                      workInProgress2.flags |= DidCapture;
                      didSuspendAlready = true;
                      var _newThenables = _suspended.updateQueue;
                      if (_newThenables !== null) {
                        workInProgress2.updateQueue = _newThenables;
                        workInProgress2.flags |= Update;
                      }
                      cutOffTailIfNeeded(renderState, true);
                      if (renderState.tail === null && renderState.tailMode === "hidden" && !renderedTail.alternate && !getIsHydrating()) {
                        bubbleProperties(workInProgress2);
                        return null;
                      }
                    } else if (
                      // The time it took to render last row is greater than the remaining
                      // time we have to render. So rendering one more row would likely
                      // exceed it.
                      now() * 2 - renderState.renderingStartTime > getRenderTargetTime() && renderLanes2 !== OffscreenLane
                    ) {
                      workInProgress2.flags |= DidCapture;
                      didSuspendAlready = true;
                      cutOffTailIfNeeded(renderState, false);
                      workInProgress2.lanes = SomeRetryLane;
                    }
                  }
                  if (renderState.isBackwards) {
                    renderedTail.sibling = workInProgress2.child;
                    workInProgress2.child = renderedTail;
                  } else {
                    var previousSibling = renderState.last;
                    if (previousSibling !== null) {
                      previousSibling.sibling = renderedTail;
                    } else {
                      workInProgress2.child = renderedTail;
                    }
                    renderState.last = renderedTail;
                  }
                }
                if (renderState.tail !== null) {
                  var next = renderState.tail;
                  renderState.rendering = next;
                  renderState.tail = next.sibling;
                  renderState.renderingStartTime = now();
                  next.sibling = null;
                  var suspenseContext = suspenseStackCursor.current;
                  if (didSuspendAlready) {
                    suspenseContext = setShallowSuspenseContext(suspenseContext, ForceSuspenseFallback);
                  } else {
                    suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
                  }
                  pushSuspenseContext(workInProgress2, suspenseContext);
                  return next;
                }
                bubbleProperties(workInProgress2);
                return null;
              }
              case ScopeComponent: {
                break;
              }
              case OffscreenComponent:
              case LegacyHiddenComponent: {
                popRenderLanes(workInProgress2);
                var _nextState = workInProgress2.memoizedState;
                var nextIsHidden = _nextState !== null;
                if (current2 !== null) {
                  var _prevState = current2.memoizedState;
                  var prevIsHidden = _prevState !== null;
                  if (prevIsHidden !== nextIsHidden && // LegacyHidden doesn't do any hiding — it only pre-renders.
                  !enableLegacyHidden) {
                    workInProgress2.flags |= Visibility;
                  }
                }
                if (!nextIsHidden || (workInProgress2.mode & ConcurrentMode) === NoMode) {
                  bubbleProperties(workInProgress2);
                } else {
                  if (includesSomeLane(subtreeRenderLanes, OffscreenLane)) {
                    bubbleProperties(workInProgress2);
                    {
                      if (workInProgress2.subtreeFlags & (Placement | Update)) {
                        workInProgress2.flags |= Visibility;
                      }
                    }
                  }
                }
                return null;
              }
              case CacheComponent: {
                return null;
              }
              case TracingMarkerComponent: {
                return null;
              }
            }
            throw new Error("Unknown unit of work tag (" + workInProgress2.tag + "). This error is likely caused by a bug in React. Please file an issue.");
          }
          function unwindWork(current2, workInProgress2, renderLanes2) {
            popTreeContext(workInProgress2);
            switch (workInProgress2.tag) {
              case ClassComponent: {
                var Component = workInProgress2.type;
                if (isContextProvider(Component)) {
                  popContext(workInProgress2);
                }
                var flags = workInProgress2.flags;
                if (flags & ShouldCapture) {
                  workInProgress2.flags = flags & ~ShouldCapture | DidCapture;
                  if ((workInProgress2.mode & ProfileMode) !== NoMode) {
                    transferActualDuration(workInProgress2);
                  }
                  return workInProgress2;
                }
                return null;
              }
              case HostRoot: {
                var root3 = workInProgress2.stateNode;
                popHostContainer(workInProgress2);
                popTopLevelContextObject(workInProgress2);
                resetWorkInProgressVersions();
                var _flags = workInProgress2.flags;
                if ((_flags & ShouldCapture) !== NoFlags && (_flags & DidCapture) === NoFlags) {
                  workInProgress2.flags = _flags & ~ShouldCapture | DidCapture;
                  return workInProgress2;
                }
                return null;
              }
              case HostComponent: {
                popHostContext(workInProgress2);
                return null;
              }
              case SuspenseComponent: {
                popSuspenseContext(workInProgress2);
                var suspenseState = workInProgress2.memoizedState;
                if (suspenseState !== null && suspenseState.dehydrated !== null) {
                  if (workInProgress2.alternate === null) {
                    throw new Error("Threw in newly mounted dehydrated component. This is likely a bug in React. Please file an issue.");
                  }
                  resetHydrationState();
                }
                var _flags2 = workInProgress2.flags;
                if (_flags2 & ShouldCapture) {
                  workInProgress2.flags = _flags2 & ~ShouldCapture | DidCapture;
                  if ((workInProgress2.mode & ProfileMode) !== NoMode) {
                    transferActualDuration(workInProgress2);
                  }
                  return workInProgress2;
                }
                return null;
              }
              case SuspenseListComponent: {
                popSuspenseContext(workInProgress2);
                return null;
              }
              case HostPortal:
                popHostContainer(workInProgress2);
                return null;
              case ContextProvider:
                var context = workInProgress2.type._context;
                popProvider(context, workInProgress2);
                return null;
              case OffscreenComponent:
              case LegacyHiddenComponent:
                popRenderLanes(workInProgress2);
                return null;
              case CacheComponent:
                return null;
              default:
                return null;
            }
          }
          function unwindInterruptedWork(current2, interruptedWork, renderLanes2) {
            popTreeContext(interruptedWork);
            switch (interruptedWork.tag) {
              case ClassComponent: {
                var childContextTypes = interruptedWork.type.childContextTypes;
                if (childContextTypes !== null && childContextTypes !== void 0) {
                  popContext(interruptedWork);
                }
                break;
              }
              case HostRoot: {
                var root3 = interruptedWork.stateNode;
                popHostContainer(interruptedWork);
                popTopLevelContextObject(interruptedWork);
                resetWorkInProgressVersions();
                break;
              }
              case HostComponent: {
                popHostContext(interruptedWork);
                break;
              }
              case HostPortal:
                popHostContainer(interruptedWork);
                break;
              case SuspenseComponent:
                popSuspenseContext(interruptedWork);
                break;
              case SuspenseListComponent:
                popSuspenseContext(interruptedWork);
                break;
              case ContextProvider:
                var context = interruptedWork.type._context;
                popProvider(context, interruptedWork);
                break;
              case OffscreenComponent:
              case LegacyHiddenComponent:
                popRenderLanes(interruptedWork);
                break;
            }
          }
          var didWarnAboutUndefinedSnapshotBeforeUpdate = null;
          {
            didWarnAboutUndefinedSnapshotBeforeUpdate = /* @__PURE__ */ new Set();
          }
          var offscreenSubtreeIsHidden = false;
          var offscreenSubtreeWasHidden = false;
          var PossiblyWeakSet = typeof WeakSet === "function" ? WeakSet : Set;
          var nextEffect = null;
          var inProgressLanes = null;
          var inProgressRoot = null;
          function reportUncaughtErrorInDEV(error2) {
            {
              invokeGuardedCallback(null, function() {
                throw error2;
              });
              clearCaughtError();
            }
          }
          var callComponentWillUnmountWithTimer = function(current2, instance) {
            instance.props = current2.memoizedProps;
            instance.state = current2.memoizedState;
            if (current2.mode & ProfileMode) {
              try {
                startLayoutEffectTimer();
                instance.componentWillUnmount();
              } finally {
                recordLayoutEffectDuration(current2);
              }
            } else {
              instance.componentWillUnmount();
            }
          };
          function safelyCallCommitHookLayoutEffectListMount(current2, nearestMountedAncestor) {
            try {
              commitHookEffectListMount(Layout, current2);
            } catch (error2) {
              captureCommitPhaseError(current2, nearestMountedAncestor, error2);
            }
          }
          function safelyCallComponentWillUnmount(current2, nearestMountedAncestor, instance) {
            try {
              callComponentWillUnmountWithTimer(current2, instance);
            } catch (error2) {
              captureCommitPhaseError(current2, nearestMountedAncestor, error2);
            }
          }
          function safelyCallComponentDidMount(current2, nearestMountedAncestor, instance) {
            try {
              instance.componentDidMount();
            } catch (error2) {
              captureCommitPhaseError(current2, nearestMountedAncestor, error2);
            }
          }
          function safelyAttachRef(current2, nearestMountedAncestor) {
            try {
              commitAttachRef(current2);
            } catch (error2) {
              captureCommitPhaseError(current2, nearestMountedAncestor, error2);
            }
          }
          function safelyDetachRef(current2, nearestMountedAncestor) {
            var ref = current2.ref;
            if (ref !== null) {
              if (typeof ref === "function") {
                var retVal;
                try {
                  if (enableProfilerTimer && enableProfilerCommitHooks && current2.mode & ProfileMode) {
                    try {
                      startLayoutEffectTimer();
                      retVal = ref(null);
                    } finally {
                      recordLayoutEffectDuration(current2);
                    }
                  } else {
                    retVal = ref(null);
                  }
                } catch (error2) {
                  captureCommitPhaseError(current2, nearestMountedAncestor, error2);
                }
                {
                  if (typeof retVal === "function") {
                    error("Unexpected return value from a callback ref in %s. A callback ref should not return a function.", getComponentNameFromFiber(current2));
                  }
                }
              } else {
                ref.current = null;
              }
            }
          }
          function safelyCallDestroy(current2, nearestMountedAncestor, destroy) {
            try {
              destroy();
            } catch (error2) {
              captureCommitPhaseError(current2, nearestMountedAncestor, error2);
            }
          }
          var focusedInstanceHandle = null;
          var shouldFireAfterActiveInstanceBlur = false;
          function commitBeforeMutationEffects(root3, firstChild) {
            focusedInstanceHandle = prepareForCommit(root3.containerInfo);
            nextEffect = firstChild;
            commitBeforeMutationEffects_begin();
            var shouldFire = shouldFireAfterActiveInstanceBlur;
            shouldFireAfterActiveInstanceBlur = false;
            focusedInstanceHandle = null;
            return shouldFire;
          }
          function commitBeforeMutationEffects_begin() {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              var child = fiber.child;
              if ((fiber.subtreeFlags & BeforeMutationMask) !== NoFlags && child !== null) {
                child.return = fiber;
                nextEffect = child;
              } else {
                commitBeforeMutationEffects_complete();
              }
            }
          }
          function commitBeforeMutationEffects_complete() {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              setCurrentFiber(fiber);
              try {
                commitBeforeMutationEffectsOnFiber(fiber);
              } catch (error2) {
                captureCommitPhaseError(fiber, fiber.return, error2);
              }
              resetCurrentFiber();
              var sibling = fiber.sibling;
              if (sibling !== null) {
                sibling.return = fiber.return;
                nextEffect = sibling;
                return;
              }
              nextEffect = fiber.return;
            }
          }
          function commitBeforeMutationEffectsOnFiber(finishedWork) {
            var current2 = finishedWork.alternate;
            var flags = finishedWork.flags;
            if ((flags & Snapshot) !== NoFlags) {
              setCurrentFiber(finishedWork);
              switch (finishedWork.tag) {
                case FunctionComponent:
                case ForwardRef:
                case SimpleMemoComponent: {
                  break;
                }
                case ClassComponent: {
                  if (current2 !== null) {
                    var prevProps = current2.memoizedProps;
                    var prevState = current2.memoizedState;
                    var instance = finishedWork.stateNode;
                    {
                      if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                        if (instance.props !== finishedWork.memoizedProps) {
                          error("Expected %s props to match memoized props before getSnapshotBeforeUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
                        }
                        if (instance.state !== finishedWork.memoizedState) {
                          error("Expected %s state to match memoized state before getSnapshotBeforeUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
                        }
                      }
                    }
                    var snapshot = instance.getSnapshotBeforeUpdate(finishedWork.elementType === finishedWork.type ? prevProps : resolveDefaultProps(finishedWork.type, prevProps), prevState);
                    {
                      var didWarnSet = didWarnAboutUndefinedSnapshotBeforeUpdate;
                      if (snapshot === void 0 && !didWarnSet.has(finishedWork.type)) {
                        didWarnSet.add(finishedWork.type);
                        error("%s.getSnapshotBeforeUpdate(): A snapshot value (or null) must be returned. You have returned undefined.", getComponentNameFromFiber(finishedWork));
                      }
                    }
                    instance.__reactInternalSnapshotBeforeUpdate = snapshot;
                  }
                  break;
                }
                case HostRoot: {
                  {
                    var root3 = finishedWork.stateNode;
                    clearContainer(root3.containerInfo);
                  }
                  break;
                }
                case HostComponent:
                case HostText:
                case HostPortal:
                case IncompleteClassComponent:
                  break;
                default: {
                  throw new Error("This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.");
                }
              }
              resetCurrentFiber();
            }
          }
          function commitHookEffectListUnmount(flags, finishedWork, nearestMountedAncestor) {
            var updateQueue = finishedWork.updateQueue;
            var lastEffect = updateQueue !== null ? updateQueue.lastEffect : null;
            if (lastEffect !== null) {
              var firstEffect = lastEffect.next;
              var effect = firstEffect;
              do {
                if ((effect.tag & flags) === flags) {
                  var destroy = effect.destroy;
                  effect.destroy = void 0;
                  if (destroy !== void 0) {
                    {
                      if ((flags & Passive$1) !== NoFlags$1) {
                        markComponentPassiveEffectUnmountStarted(finishedWork);
                      } else if ((flags & Layout) !== NoFlags$1) {
                        markComponentLayoutEffectUnmountStarted(finishedWork);
                      }
                    }
                    {
                      if ((flags & Insertion) !== NoFlags$1) {
                        setIsRunningInsertionEffect(true);
                      }
                    }
                    safelyCallDestroy(finishedWork, nearestMountedAncestor, destroy);
                    {
                      if ((flags & Insertion) !== NoFlags$1) {
                        setIsRunningInsertionEffect(false);
                      }
                    }
                    {
                      if ((flags & Passive$1) !== NoFlags$1) {
                        markComponentPassiveEffectUnmountStopped();
                      } else if ((flags & Layout) !== NoFlags$1) {
                        markComponentLayoutEffectUnmountStopped();
                      }
                    }
                  }
                }
                effect = effect.next;
              } while (effect !== firstEffect);
            }
          }
          function commitHookEffectListMount(flags, finishedWork) {
            var updateQueue = finishedWork.updateQueue;
            var lastEffect = updateQueue !== null ? updateQueue.lastEffect : null;
            if (lastEffect !== null) {
              var firstEffect = lastEffect.next;
              var effect = firstEffect;
              do {
                if ((effect.tag & flags) === flags) {
                  {
                    if ((flags & Passive$1) !== NoFlags$1) {
                      markComponentPassiveEffectMountStarted(finishedWork);
                    } else if ((flags & Layout) !== NoFlags$1) {
                      markComponentLayoutEffectMountStarted(finishedWork);
                    }
                  }
                  var create = effect.create;
                  {
                    if ((flags & Insertion) !== NoFlags$1) {
                      setIsRunningInsertionEffect(true);
                    }
                  }
                  effect.destroy = create();
                  {
                    if ((flags & Insertion) !== NoFlags$1) {
                      setIsRunningInsertionEffect(false);
                    }
                  }
                  {
                    if ((flags & Passive$1) !== NoFlags$1) {
                      markComponentPassiveEffectMountStopped();
                    } else if ((flags & Layout) !== NoFlags$1) {
                      markComponentLayoutEffectMountStopped();
                    }
                  }
                  {
                    var destroy = effect.destroy;
                    if (destroy !== void 0 && typeof destroy !== "function") {
                      var hookName = void 0;
                      if ((effect.tag & Layout) !== NoFlags) {
                        hookName = "useLayoutEffect";
                      } else if ((effect.tag & Insertion) !== NoFlags) {
                        hookName = "useInsertionEffect";
                      } else {
                        hookName = "useEffect";
                      }
                      var addendum = void 0;
                      if (destroy === null) {
                        addendum = " You returned null. If your effect does not require clean up, return undefined (or nothing).";
                      } else if (typeof destroy.then === "function") {
                        addendum = "\n\nIt looks like you wrote " + hookName + "(async () => ...) or returned a Promise. Instead, write the async function inside your effect and call it immediately:\n\n" + hookName + "(() => {\n  async function fetchData() {\n    // You can await here\n    const response = await MyAPI.getData(someId);\n    // ...\n  }\n  fetchData();\n}, [someId]); // Or [] if effect doesn't need props or state\n\nLearn more about data fetching with Hooks: https://reactjs.org/link/hooks-data-fetching";
                      } else {
                        addendum = " You returned: " + destroy;
                      }
                      error("%s must not return anything besides a function, which is used for clean-up.%s", hookName, addendum);
                    }
                  }
                }
                effect = effect.next;
              } while (effect !== firstEffect);
            }
          }
          function commitPassiveEffectDurations(finishedRoot, finishedWork) {
            {
              if ((finishedWork.flags & Update) !== NoFlags) {
                switch (finishedWork.tag) {
                  case Profiler: {
                    var passiveEffectDuration = finishedWork.stateNode.passiveEffectDuration;
                    var _finishedWork$memoize = finishedWork.memoizedProps, id = _finishedWork$memoize.id, onPostCommit = _finishedWork$memoize.onPostCommit;
                    var commitTime2 = getCommitTime();
                    var phase = finishedWork.alternate === null ? "mount" : "update";
                    {
                      if (isCurrentUpdateNested()) {
                        phase = "nested-update";
                      }
                    }
                    if (typeof onPostCommit === "function") {
                      onPostCommit(id, phase, passiveEffectDuration, commitTime2);
                    }
                    var parentFiber = finishedWork.return;
                    outer: while (parentFiber !== null) {
                      switch (parentFiber.tag) {
                        case HostRoot:
                          var root3 = parentFiber.stateNode;
                          root3.passiveEffectDuration += passiveEffectDuration;
                          break outer;
                        case Profiler:
                          var parentStateNode = parentFiber.stateNode;
                          parentStateNode.passiveEffectDuration += passiveEffectDuration;
                          break outer;
                      }
                      parentFiber = parentFiber.return;
                    }
                    break;
                  }
                }
              }
            }
          }
          function commitLayoutEffectOnFiber(finishedRoot, current2, finishedWork, committedLanes) {
            if ((finishedWork.flags & LayoutMask) !== NoFlags) {
              switch (finishedWork.tag) {
                case FunctionComponent:
                case ForwardRef:
                case SimpleMemoComponent: {
                  if (!offscreenSubtreeWasHidden) {
                    if (finishedWork.mode & ProfileMode) {
                      try {
                        startLayoutEffectTimer();
                        commitHookEffectListMount(Layout | HasEffect, finishedWork);
                      } finally {
                        recordLayoutEffectDuration(finishedWork);
                      }
                    } else {
                      commitHookEffectListMount(Layout | HasEffect, finishedWork);
                    }
                  }
                  break;
                }
                case ClassComponent: {
                  var instance = finishedWork.stateNode;
                  if (finishedWork.flags & Update) {
                    if (!offscreenSubtreeWasHidden) {
                      if (current2 === null) {
                        {
                          if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                            if (instance.props !== finishedWork.memoizedProps) {
                              error("Expected %s props to match memoized props before componentDidMount. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
                            }
                            if (instance.state !== finishedWork.memoizedState) {
                              error("Expected %s state to match memoized state before componentDidMount. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
                            }
                          }
                        }
                        if (finishedWork.mode & ProfileMode) {
                          try {
                            startLayoutEffectTimer();
                            instance.componentDidMount();
                          } finally {
                            recordLayoutEffectDuration(finishedWork);
                          }
                        } else {
                          instance.componentDidMount();
                        }
                      } else {
                        var prevProps = finishedWork.elementType === finishedWork.type ? current2.memoizedProps : resolveDefaultProps(finishedWork.type, current2.memoizedProps);
                        var prevState = current2.memoizedState;
                        {
                          if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                            if (instance.props !== finishedWork.memoizedProps) {
                              error("Expected %s props to match memoized props before componentDidUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
                            }
                            if (instance.state !== finishedWork.memoizedState) {
                              error("Expected %s state to match memoized state before componentDidUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
                            }
                          }
                        }
                        if (finishedWork.mode & ProfileMode) {
                          try {
                            startLayoutEffectTimer();
                            instance.componentDidUpdate(prevProps, prevState, instance.__reactInternalSnapshotBeforeUpdate);
                          } finally {
                            recordLayoutEffectDuration(finishedWork);
                          }
                        } else {
                          instance.componentDidUpdate(prevProps, prevState, instance.__reactInternalSnapshotBeforeUpdate);
                        }
                      }
                    }
                  }
                  var updateQueue = finishedWork.updateQueue;
                  if (updateQueue !== null) {
                    {
                      if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                        if (instance.props !== finishedWork.memoizedProps) {
                          error("Expected %s props to match memoized props before processing the update queue. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
                        }
                        if (instance.state !== finishedWork.memoizedState) {
                          error("Expected %s state to match memoized state before processing the update queue. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
                        }
                      }
                    }
                    commitUpdateQueue(finishedWork, updateQueue, instance);
                  }
                  break;
                }
                case HostRoot: {
                  var _updateQueue = finishedWork.updateQueue;
                  if (_updateQueue !== null) {
                    var _instance = null;
                    if (finishedWork.child !== null) {
                      switch (finishedWork.child.tag) {
                        case HostComponent:
                          _instance = getPublicInstance(finishedWork.child.stateNode);
                          break;
                        case ClassComponent:
                          _instance = finishedWork.child.stateNode;
                          break;
                      }
                    }
                    commitUpdateQueue(finishedWork, _updateQueue, _instance);
                  }
                  break;
                }
                case HostComponent: {
                  var _instance2 = finishedWork.stateNode;
                  if (current2 === null && finishedWork.flags & Update) {
                    var type = finishedWork.type;
                    var props = finishedWork.memoizedProps;
                    commitMount(_instance2, type, props);
                  }
                  break;
                }
                case HostText: {
                  break;
                }
                case HostPortal: {
                  break;
                }
                case Profiler: {
                  {
                    var _finishedWork$memoize2 = finishedWork.memoizedProps, onCommit = _finishedWork$memoize2.onCommit, onRender = _finishedWork$memoize2.onRender;
                    var effectDuration = finishedWork.stateNode.effectDuration;
                    var commitTime2 = getCommitTime();
                    var phase = current2 === null ? "mount" : "update";
                    {
                      if (isCurrentUpdateNested()) {
                        phase = "nested-update";
                      }
                    }
                    if (typeof onRender === "function") {
                      onRender(finishedWork.memoizedProps.id, phase, finishedWork.actualDuration, finishedWork.treeBaseDuration, finishedWork.actualStartTime, commitTime2);
                    }
                    {
                      if (typeof onCommit === "function") {
                        onCommit(finishedWork.memoizedProps.id, phase, effectDuration, commitTime2);
                      }
                      enqueuePendingPassiveProfilerEffect(finishedWork);
                      var parentFiber = finishedWork.return;
                      outer: while (parentFiber !== null) {
                        switch (parentFiber.tag) {
                          case HostRoot:
                            var root3 = parentFiber.stateNode;
                            root3.effectDuration += effectDuration;
                            break outer;
                          case Profiler:
                            var parentStateNode = parentFiber.stateNode;
                            parentStateNode.effectDuration += effectDuration;
                            break outer;
                        }
                        parentFiber = parentFiber.return;
                      }
                    }
                  }
                  break;
                }
                case SuspenseComponent: {
                  commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
                  break;
                }
                case SuspenseListComponent:
                case IncompleteClassComponent:
                case ScopeComponent:
                case OffscreenComponent:
                case LegacyHiddenComponent:
                case TracingMarkerComponent: {
                  break;
                }
                default:
                  throw new Error("This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.");
              }
            }
            if (!offscreenSubtreeWasHidden) {
              {
                if (finishedWork.flags & Ref) {
                  commitAttachRef(finishedWork);
                }
              }
            }
          }
          function reappearLayoutEffectsOnFiber(node) {
            switch (node.tag) {
              case FunctionComponent:
              case ForwardRef:
              case SimpleMemoComponent: {
                if (node.mode & ProfileMode) {
                  try {
                    startLayoutEffectTimer();
                    safelyCallCommitHookLayoutEffectListMount(node, node.return);
                  } finally {
                    recordLayoutEffectDuration(node);
                  }
                } else {
                  safelyCallCommitHookLayoutEffectListMount(node, node.return);
                }
                break;
              }
              case ClassComponent: {
                var instance = node.stateNode;
                if (typeof instance.componentDidMount === "function") {
                  safelyCallComponentDidMount(node, node.return, instance);
                }
                safelyAttachRef(node, node.return);
                break;
              }
              case HostComponent: {
                safelyAttachRef(node, node.return);
                break;
              }
            }
          }
          function hideOrUnhideAllChildren(finishedWork, isHidden) {
            var hostSubtreeRoot = null;
            {
              var node = finishedWork;
              while (true) {
                if (node.tag === HostComponent) {
                  if (hostSubtreeRoot === null) {
                    hostSubtreeRoot = node;
                    try {
                      var instance = node.stateNode;
                      if (isHidden) {
                        hideInstance(instance);
                      } else {
                        unhideInstance(node.stateNode, node.memoizedProps);
                      }
                    } catch (error2) {
                      captureCommitPhaseError(finishedWork, finishedWork.return, error2);
                    }
                  }
                } else if (node.tag === HostText) {
                  if (hostSubtreeRoot === null) {
                    try {
                      var _instance3 = node.stateNode;
                      if (isHidden) {
                        hideTextInstance(_instance3);
                      } else {
                        unhideTextInstance(_instance3, node.memoizedProps);
                      }
                    } catch (error2) {
                      captureCommitPhaseError(finishedWork, finishedWork.return, error2);
                    }
                  }
                } else if ((node.tag === OffscreenComponent || node.tag === LegacyHiddenComponent) && node.memoizedState !== null && node !== finishedWork) ;
                else if (node.child !== null) {
                  node.child.return = node;
                  node = node.child;
                  continue;
                }
                if (node === finishedWork) {
                  return;
                }
                while (node.sibling === null) {
                  if (node.return === null || node.return === finishedWork) {
                    return;
                  }
                  if (hostSubtreeRoot === node) {
                    hostSubtreeRoot = null;
                  }
                  node = node.return;
                }
                if (hostSubtreeRoot === node) {
                  hostSubtreeRoot = null;
                }
                node.sibling.return = node.return;
                node = node.sibling;
              }
            }
          }
          function commitAttachRef(finishedWork) {
            var ref = finishedWork.ref;
            if (ref !== null) {
              var instance = finishedWork.stateNode;
              var instanceToUse;
              switch (finishedWork.tag) {
                case HostComponent:
                  instanceToUse = getPublicInstance(instance);
                  break;
                default:
                  instanceToUse = instance;
              }
              if (typeof ref === "function") {
                var retVal;
                if (finishedWork.mode & ProfileMode) {
                  try {
                    startLayoutEffectTimer();
                    retVal = ref(instanceToUse);
                  } finally {
                    recordLayoutEffectDuration(finishedWork);
                  }
                } else {
                  retVal = ref(instanceToUse);
                }
                {
                  if (typeof retVal === "function") {
                    error("Unexpected return value from a callback ref in %s. A callback ref should not return a function.", getComponentNameFromFiber(finishedWork));
                  }
                }
              } else {
                {
                  if (!ref.hasOwnProperty("current")) {
                    error("Unexpected ref object provided for %s. Use either a ref-setter function or React.createRef().", getComponentNameFromFiber(finishedWork));
                  }
                }
                ref.current = instanceToUse;
              }
            }
          }
          function detachFiberMutation(fiber) {
            var alternate = fiber.alternate;
            if (alternate !== null) {
              alternate.return = null;
            }
            fiber.return = null;
          }
          function detachFiberAfterEffects(fiber) {
            var alternate = fiber.alternate;
            if (alternate !== null) {
              fiber.alternate = null;
              detachFiberAfterEffects(alternate);
            }
            {
              fiber.child = null;
              fiber.deletions = null;
              fiber.sibling = null;
              if (fiber.tag === HostComponent) {
                var hostInstance = fiber.stateNode;
                if (hostInstance !== null) {
                  detachDeletedInstance(hostInstance);
                }
              }
              fiber.stateNode = null;
              {
                fiber._debugOwner = null;
              }
              {
                fiber.return = null;
                fiber.dependencies = null;
                fiber.memoizedProps = null;
                fiber.memoizedState = null;
                fiber.pendingProps = null;
                fiber.stateNode = null;
                fiber.updateQueue = null;
              }
            }
          }
          function getHostParentFiber(fiber) {
            var parent = fiber.return;
            while (parent !== null) {
              if (isHostParent(parent)) {
                return parent;
              }
              parent = parent.return;
            }
            throw new Error("Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.");
          }
          function isHostParent(fiber) {
            return fiber.tag === HostComponent || fiber.tag === HostRoot || fiber.tag === HostPortal;
          }
          function getHostSibling(fiber) {
            var node = fiber;
            siblings: while (true) {
              while (node.sibling === null) {
                if (node.return === null || isHostParent(node.return)) {
                  return null;
                }
                node = node.return;
              }
              node.sibling.return = node.return;
              node = node.sibling;
              while (node.tag !== HostComponent && node.tag !== HostText && node.tag !== DehydratedFragment) {
                if (node.flags & Placement) {
                  continue siblings;
                }
                if (node.child === null || node.tag === HostPortal) {
                  continue siblings;
                } else {
                  node.child.return = node;
                  node = node.child;
                }
              }
              if (!(node.flags & Placement)) {
                return node.stateNode;
              }
            }
          }
          function commitPlacement(finishedWork) {
            var parentFiber = getHostParentFiber(finishedWork);
            switch (parentFiber.tag) {
              case HostComponent: {
                var parent = parentFiber.stateNode;
                if (parentFiber.flags & ContentReset) {
                  resetTextContent(parent);
                  parentFiber.flags &= ~ContentReset;
                }
                var before = getHostSibling(finishedWork);
                insertOrAppendPlacementNode(finishedWork, before, parent);
                break;
              }
              case HostRoot:
              case HostPortal: {
                var _parent = parentFiber.stateNode.containerInfo;
                var _before = getHostSibling(finishedWork);
                insertOrAppendPlacementNodeIntoContainer(finishedWork, _before, _parent);
                break;
              }
              // eslint-disable-next-line-no-fallthrough
              default:
                throw new Error("Invalid host parent fiber. This error is likely caused by a bug in React. Please file an issue.");
            }
          }
          function insertOrAppendPlacementNodeIntoContainer(node, before, parent) {
            var tag = node.tag;
            var isHost = tag === HostComponent || tag === HostText;
            if (isHost) {
              var stateNode = node.stateNode;
              if (before) {
                insertInContainerBefore(parent, stateNode, before);
              } else {
                appendChildToContainer(parent, stateNode);
              }
            } else if (tag === HostPortal) ;
            else {
              var child = node.child;
              if (child !== null) {
                insertOrAppendPlacementNodeIntoContainer(child, before, parent);
                var sibling = child.sibling;
                while (sibling !== null) {
                  insertOrAppendPlacementNodeIntoContainer(sibling, before, parent);
                  sibling = sibling.sibling;
                }
              }
            }
          }
          function insertOrAppendPlacementNode(node, before, parent) {
            var tag = node.tag;
            var isHost = tag === HostComponent || tag === HostText;
            if (isHost) {
              var stateNode = node.stateNode;
              if (before) {
                insertBefore(parent, stateNode, before);
              } else {
                appendChild(parent, stateNode);
              }
            } else if (tag === HostPortal) ;
            else {
              var child = node.child;
              if (child !== null) {
                insertOrAppendPlacementNode(child, before, parent);
                var sibling = child.sibling;
                while (sibling !== null) {
                  insertOrAppendPlacementNode(sibling, before, parent);
                  sibling = sibling.sibling;
                }
              }
            }
          }
          var hostParent = null;
          var hostParentIsContainer = false;
          function commitDeletionEffects(root3, returnFiber, deletedFiber) {
            {
              var parent = returnFiber;
              findParent: while (parent !== null) {
                switch (parent.tag) {
                  case HostComponent: {
                    hostParent = parent.stateNode;
                    hostParentIsContainer = false;
                    break findParent;
                  }
                  case HostRoot: {
                    hostParent = parent.stateNode.containerInfo;
                    hostParentIsContainer = true;
                    break findParent;
                  }
                  case HostPortal: {
                    hostParent = parent.stateNode.containerInfo;
                    hostParentIsContainer = true;
                    break findParent;
                  }
                }
                parent = parent.return;
              }
              if (hostParent === null) {
                throw new Error("Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.");
              }
              commitDeletionEffectsOnFiber(root3, returnFiber, deletedFiber);
              hostParent = null;
              hostParentIsContainer = false;
            }
            detachFiberMutation(deletedFiber);
          }
          function recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, parent) {
            var child = parent.child;
            while (child !== null) {
              commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, child);
              child = child.sibling;
            }
          }
          function commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, deletedFiber) {
            onCommitUnmount(deletedFiber);
            switch (deletedFiber.tag) {
              case HostComponent: {
                if (!offscreenSubtreeWasHidden) {
                  safelyDetachRef(deletedFiber, nearestMountedAncestor);
                }
              }
              // eslint-disable-next-line-no-fallthrough
              case HostText: {
                {
                  var prevHostParent = hostParent;
                  var prevHostParentIsContainer = hostParentIsContainer;
                  hostParent = null;
                  recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                  hostParent = prevHostParent;
                  hostParentIsContainer = prevHostParentIsContainer;
                  if (hostParent !== null) {
                    if (hostParentIsContainer) {
                      removeChildFromContainer(hostParent, deletedFiber.stateNode);
                    } else {
                      removeChild(hostParent, deletedFiber.stateNode);
                    }
                  }
                }
                return;
              }
              case DehydratedFragment: {
                {
                  if (hostParent !== null) {
                    if (hostParentIsContainer) {
                      clearSuspenseBoundaryFromContainer(hostParent, deletedFiber.stateNode);
                    } else {
                      clearSuspenseBoundary(hostParent, deletedFiber.stateNode);
                    }
                  }
                }
                return;
              }
              case HostPortal: {
                {
                  var _prevHostParent = hostParent;
                  var _prevHostParentIsContainer = hostParentIsContainer;
                  hostParent = deletedFiber.stateNode.containerInfo;
                  hostParentIsContainer = true;
                  recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                  hostParent = _prevHostParent;
                  hostParentIsContainer = _prevHostParentIsContainer;
                }
                return;
              }
              case FunctionComponent:
              case ForwardRef:
              case MemoComponent:
              case SimpleMemoComponent: {
                if (!offscreenSubtreeWasHidden) {
                  var updateQueue = deletedFiber.updateQueue;
                  if (updateQueue !== null) {
                    var lastEffect = updateQueue.lastEffect;
                    if (lastEffect !== null) {
                      var firstEffect = lastEffect.next;
                      var effect = firstEffect;
                      do {
                        var _effect = effect, destroy = _effect.destroy, tag = _effect.tag;
                        if (destroy !== void 0) {
                          if ((tag & Insertion) !== NoFlags$1) {
                            safelyCallDestroy(deletedFiber, nearestMountedAncestor, destroy);
                          } else if ((tag & Layout) !== NoFlags$1) {
                            {
                              markComponentLayoutEffectUnmountStarted(deletedFiber);
                            }
                            if (deletedFiber.mode & ProfileMode) {
                              startLayoutEffectTimer();
                              safelyCallDestroy(deletedFiber, nearestMountedAncestor, destroy);
                              recordLayoutEffectDuration(deletedFiber);
                            } else {
                              safelyCallDestroy(deletedFiber, nearestMountedAncestor, destroy);
                            }
                            {
                              markComponentLayoutEffectUnmountStopped();
                            }
                          }
                        }
                        effect = effect.next;
                      } while (effect !== firstEffect);
                    }
                  }
                }
                recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                return;
              }
              case ClassComponent: {
                if (!offscreenSubtreeWasHidden) {
                  safelyDetachRef(deletedFiber, nearestMountedAncestor);
                  var instance = deletedFiber.stateNode;
                  if (typeof instance.componentWillUnmount === "function") {
                    safelyCallComponentWillUnmount(deletedFiber, nearestMountedAncestor, instance);
                  }
                }
                recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                return;
              }
              case ScopeComponent: {
                recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                return;
              }
              case OffscreenComponent: {
                if (
                  // TODO: Remove this dead flag
                  deletedFiber.mode & ConcurrentMode
                ) {
                  var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
                  offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden || deletedFiber.memoizedState !== null;
                  recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                  offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
                } else {
                  recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                }
                break;
              }
              default: {
                recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                return;
              }
            }
          }
          function commitSuspenseCallback(finishedWork) {
            var newState = finishedWork.memoizedState;
          }
          function commitSuspenseHydrationCallbacks(finishedRoot, finishedWork) {
            var newState = finishedWork.memoizedState;
            if (newState === null) {
              var current2 = finishedWork.alternate;
              if (current2 !== null) {
                var prevState = current2.memoizedState;
                if (prevState !== null) {
                  var suspenseInstance = prevState.dehydrated;
                  if (suspenseInstance !== null) {
                    commitHydratedSuspenseInstance(suspenseInstance);
                  }
                }
              }
            }
          }
          function attachSuspenseRetryListeners(finishedWork) {
            var wakeables = finishedWork.updateQueue;
            if (wakeables !== null) {
              finishedWork.updateQueue = null;
              var retryCache = finishedWork.stateNode;
              if (retryCache === null) {
                retryCache = finishedWork.stateNode = new PossiblyWeakSet();
              }
              wakeables.forEach(function(wakeable) {
                var retry = resolveRetryWakeable.bind(null, finishedWork, wakeable);
                if (!retryCache.has(wakeable)) {
                  retryCache.add(wakeable);
                  {
                    if (isDevToolsPresent) {
                      if (inProgressLanes !== null && inProgressRoot !== null) {
                        restorePendingUpdaters(inProgressRoot, inProgressLanes);
                      } else {
                        throw Error("Expected finished root and lanes to be set. This is a bug in React.");
                      }
                    }
                  }
                  wakeable.then(retry, retry);
                }
              });
            }
          }
          function commitMutationEffects(root3, finishedWork, committedLanes) {
            inProgressLanes = committedLanes;
            inProgressRoot = root3;
            setCurrentFiber(finishedWork);
            commitMutationEffectsOnFiber(finishedWork, root3);
            setCurrentFiber(finishedWork);
            inProgressLanes = null;
            inProgressRoot = null;
          }
          function recursivelyTraverseMutationEffects(root3, parentFiber, lanes) {
            var deletions = parentFiber.deletions;
            if (deletions !== null) {
              for (var i = 0; i < deletions.length; i++) {
                var childToDelete = deletions[i];
                try {
                  commitDeletionEffects(root3, parentFiber, childToDelete);
                } catch (error2) {
                  captureCommitPhaseError(childToDelete, parentFiber, error2);
                }
              }
            }
            var prevDebugFiber = getCurrentFiber();
            if (parentFiber.subtreeFlags & MutationMask) {
              var child = parentFiber.child;
              while (child !== null) {
                setCurrentFiber(child);
                commitMutationEffectsOnFiber(child, root3);
                child = child.sibling;
              }
            }
            setCurrentFiber(prevDebugFiber);
          }
          function commitMutationEffectsOnFiber(finishedWork, root3, lanes) {
            var current2 = finishedWork.alternate;
            var flags = finishedWork.flags;
            switch (finishedWork.tag) {
              case FunctionComponent:
              case ForwardRef:
              case MemoComponent:
              case SimpleMemoComponent: {
                recursivelyTraverseMutationEffects(root3, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Update) {
                  try {
                    commitHookEffectListUnmount(Insertion | HasEffect, finishedWork, finishedWork.return);
                    commitHookEffectListMount(Insertion | HasEffect, finishedWork);
                  } catch (error2) {
                    captureCommitPhaseError(finishedWork, finishedWork.return, error2);
                  }
                  if (finishedWork.mode & ProfileMode) {
                    try {
                      startLayoutEffectTimer();
                      commitHookEffectListUnmount(Layout | HasEffect, finishedWork, finishedWork.return);
                    } catch (error2) {
                      captureCommitPhaseError(finishedWork, finishedWork.return, error2);
                    }
                    recordLayoutEffectDuration(finishedWork);
                  } else {
                    try {
                      commitHookEffectListUnmount(Layout | HasEffect, finishedWork, finishedWork.return);
                    } catch (error2) {
                      captureCommitPhaseError(finishedWork, finishedWork.return, error2);
                    }
                  }
                }
                return;
              }
              case ClassComponent: {
                recursivelyTraverseMutationEffects(root3, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Ref) {
                  if (current2 !== null) {
                    safelyDetachRef(current2, current2.return);
                  }
                }
                return;
              }
              case HostComponent: {
                recursivelyTraverseMutationEffects(root3, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Ref) {
                  if (current2 !== null) {
                    safelyDetachRef(current2, current2.return);
                  }
                }
                {
                  if (finishedWork.flags & ContentReset) {
                    var instance = finishedWork.stateNode;
                    try {
                      resetTextContent(instance);
                    } catch (error2) {
                      captureCommitPhaseError(finishedWork, finishedWork.return, error2);
                    }
                  }
                  if (flags & Update) {
                    var _instance4 = finishedWork.stateNode;
                    if (_instance4 != null) {
                      var newProps = finishedWork.memoizedProps;
                      var oldProps = current2 !== null ? current2.memoizedProps : newProps;
                      var type = finishedWork.type;
                      var updatePayload = finishedWork.updateQueue;
                      finishedWork.updateQueue = null;
                      if (updatePayload !== null) {
                        try {
                          commitUpdate(_instance4, updatePayload, type, oldProps, newProps, finishedWork);
                        } catch (error2) {
                          captureCommitPhaseError(finishedWork, finishedWork.return, error2);
                        }
                      }
                    }
                  }
                }
                return;
              }
              case HostText: {
                recursivelyTraverseMutationEffects(root3, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Update) {
                  {
                    if (finishedWork.stateNode === null) {
                      throw new Error("This should have a text node initialized. This error is likely caused by a bug in React. Please file an issue.");
                    }
                    var textInstance = finishedWork.stateNode;
                    var newText = finishedWork.memoizedProps;
                    var oldText = current2 !== null ? current2.memoizedProps : newText;
                    try {
                      commitTextUpdate(textInstance, oldText, newText);
                    } catch (error2) {
                      captureCommitPhaseError(finishedWork, finishedWork.return, error2);
                    }
                  }
                }
                return;
              }
              case HostRoot: {
                recursivelyTraverseMutationEffects(root3, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Update) {
                  {
                    if (current2 !== null) {
                      var prevRootState = current2.memoizedState;
                      if (prevRootState.isDehydrated) {
                        try {
                          commitHydratedContainer(root3.containerInfo);
                        } catch (error2) {
                          captureCommitPhaseError(finishedWork, finishedWork.return, error2);
                        }
                      }
                    }
                  }
                }
                return;
              }
              case HostPortal: {
                recursivelyTraverseMutationEffects(root3, finishedWork);
                commitReconciliationEffects(finishedWork);
                return;
              }
              case SuspenseComponent: {
                recursivelyTraverseMutationEffects(root3, finishedWork);
                commitReconciliationEffects(finishedWork);
                var offscreenFiber = finishedWork.child;
                if (offscreenFiber.flags & Visibility) {
                  var offscreenInstance = offscreenFiber.stateNode;
                  var newState = offscreenFiber.memoizedState;
                  var isHidden = newState !== null;
                  offscreenInstance.isHidden = isHidden;
                  if (isHidden) {
                    var wasHidden = offscreenFiber.alternate !== null && offscreenFiber.alternate.memoizedState !== null;
                    if (!wasHidden) {
                      markCommitTimeOfFallback();
                    }
                  }
                }
                if (flags & Update) {
                  try {
                    commitSuspenseCallback(finishedWork);
                  } catch (error2) {
                    captureCommitPhaseError(finishedWork, finishedWork.return, error2);
                  }
                  attachSuspenseRetryListeners(finishedWork);
                }
                return;
              }
              case OffscreenComponent: {
                var _wasHidden = current2 !== null && current2.memoizedState !== null;
                if (
                  // TODO: Remove this dead flag
                  finishedWork.mode & ConcurrentMode
                ) {
                  var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
                  offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden || _wasHidden;
                  recursivelyTraverseMutationEffects(root3, finishedWork);
                  offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
                } else {
                  recursivelyTraverseMutationEffects(root3, finishedWork);
                }
                commitReconciliationEffects(finishedWork);
                if (flags & Visibility) {
                  var _offscreenInstance = finishedWork.stateNode;
                  var _newState = finishedWork.memoizedState;
                  var _isHidden = _newState !== null;
                  var offscreenBoundary = finishedWork;
                  _offscreenInstance.isHidden = _isHidden;
                  {
                    if (_isHidden) {
                      if (!_wasHidden) {
                        if ((offscreenBoundary.mode & ConcurrentMode) !== NoMode) {
                          nextEffect = offscreenBoundary;
                          var offscreenChild = offscreenBoundary.child;
                          while (offscreenChild !== null) {
                            nextEffect = offscreenChild;
                            disappearLayoutEffects_begin(offscreenChild);
                            offscreenChild = offscreenChild.sibling;
                          }
                        }
                      }
                    }
                  }
                  {
                    hideOrUnhideAllChildren(offscreenBoundary, _isHidden);
                  }
                }
                return;
              }
              case SuspenseListComponent: {
                recursivelyTraverseMutationEffects(root3, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Update) {
                  attachSuspenseRetryListeners(finishedWork);
                }
                return;
              }
              case ScopeComponent: {
                return;
              }
              default: {
                recursivelyTraverseMutationEffects(root3, finishedWork);
                commitReconciliationEffects(finishedWork);
                return;
              }
            }
          }
          function commitReconciliationEffects(finishedWork) {
            var flags = finishedWork.flags;
            if (flags & Placement) {
              try {
                commitPlacement(finishedWork);
              } catch (error2) {
                captureCommitPhaseError(finishedWork, finishedWork.return, error2);
              }
              finishedWork.flags &= ~Placement;
            }
            if (flags & Hydrating) {
              finishedWork.flags &= ~Hydrating;
            }
          }
          function commitLayoutEffects(finishedWork, root3, committedLanes) {
            inProgressLanes = committedLanes;
            inProgressRoot = root3;
            nextEffect = finishedWork;
            commitLayoutEffects_begin(finishedWork, root3, committedLanes);
            inProgressLanes = null;
            inProgressRoot = null;
          }
          function commitLayoutEffects_begin(subtreeRoot, root3, committedLanes) {
            var isModernRoot = (subtreeRoot.mode & ConcurrentMode) !== NoMode;
            while (nextEffect !== null) {
              var fiber = nextEffect;
              var firstChild = fiber.child;
              if (fiber.tag === OffscreenComponent && isModernRoot) {
                var isHidden = fiber.memoizedState !== null;
                var newOffscreenSubtreeIsHidden = isHidden || offscreenSubtreeIsHidden;
                if (newOffscreenSubtreeIsHidden) {
                  commitLayoutMountEffects_complete(subtreeRoot, root3, committedLanes);
                  continue;
                } else {
                  var current2 = fiber.alternate;
                  var wasHidden = current2 !== null && current2.memoizedState !== null;
                  var newOffscreenSubtreeWasHidden = wasHidden || offscreenSubtreeWasHidden;
                  var prevOffscreenSubtreeIsHidden = offscreenSubtreeIsHidden;
                  var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
                  offscreenSubtreeIsHidden = newOffscreenSubtreeIsHidden;
                  offscreenSubtreeWasHidden = newOffscreenSubtreeWasHidden;
                  if (offscreenSubtreeWasHidden && !prevOffscreenSubtreeWasHidden) {
                    nextEffect = fiber;
                    reappearLayoutEffects_begin(fiber);
                  }
                  var child = firstChild;
                  while (child !== null) {
                    nextEffect = child;
                    commitLayoutEffects_begin(
                      child,
                      // New root; bubble back up to here and stop.
                      root3,
                      committedLanes
                    );
                    child = child.sibling;
                  }
                  nextEffect = fiber;
                  offscreenSubtreeIsHidden = prevOffscreenSubtreeIsHidden;
                  offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
                  commitLayoutMountEffects_complete(subtreeRoot, root3, committedLanes);
                  continue;
                }
              }
              if ((fiber.subtreeFlags & LayoutMask) !== NoFlags && firstChild !== null) {
                firstChild.return = fiber;
                nextEffect = firstChild;
              } else {
                commitLayoutMountEffects_complete(subtreeRoot, root3, committedLanes);
              }
            }
          }
          function commitLayoutMountEffects_complete(subtreeRoot, root3, committedLanes) {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              if ((fiber.flags & LayoutMask) !== NoFlags) {
                var current2 = fiber.alternate;
                setCurrentFiber(fiber);
                try {
                  commitLayoutEffectOnFiber(root3, current2, fiber, committedLanes);
                } catch (error2) {
                  captureCommitPhaseError(fiber, fiber.return, error2);
                }
                resetCurrentFiber();
              }
              if (fiber === subtreeRoot) {
                nextEffect = null;
                return;
              }
              var sibling = fiber.sibling;
              if (sibling !== null) {
                sibling.return = fiber.return;
                nextEffect = sibling;
                return;
              }
              nextEffect = fiber.return;
            }
          }
          function disappearLayoutEffects_begin(subtreeRoot) {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              var firstChild = fiber.child;
              switch (fiber.tag) {
                case FunctionComponent:
                case ForwardRef:
                case MemoComponent:
                case SimpleMemoComponent: {
                  if (fiber.mode & ProfileMode) {
                    try {
                      startLayoutEffectTimer();
                      commitHookEffectListUnmount(Layout, fiber, fiber.return);
                    } finally {
                      recordLayoutEffectDuration(fiber);
                    }
                  } else {
                    commitHookEffectListUnmount(Layout, fiber, fiber.return);
                  }
                  break;
                }
                case ClassComponent: {
                  safelyDetachRef(fiber, fiber.return);
                  var instance = fiber.stateNode;
                  if (typeof instance.componentWillUnmount === "function") {
                    safelyCallComponentWillUnmount(fiber, fiber.return, instance);
                  }
                  break;
                }
                case HostComponent: {
                  safelyDetachRef(fiber, fiber.return);
                  break;
                }
                case OffscreenComponent: {
                  var isHidden = fiber.memoizedState !== null;
                  if (isHidden) {
                    disappearLayoutEffects_complete(subtreeRoot);
                    continue;
                  }
                  break;
                }
              }
              if (firstChild !== null) {
                firstChild.return = fiber;
                nextEffect = firstChild;
              } else {
                disappearLayoutEffects_complete(subtreeRoot);
              }
            }
          }
          function disappearLayoutEffects_complete(subtreeRoot) {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              if (fiber === subtreeRoot) {
                nextEffect = null;
                return;
              }
              var sibling = fiber.sibling;
              if (sibling !== null) {
                sibling.return = fiber.return;
                nextEffect = sibling;
                return;
              }
              nextEffect = fiber.return;
            }
          }
          function reappearLayoutEffects_begin(subtreeRoot) {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              var firstChild = fiber.child;
              if (fiber.tag === OffscreenComponent) {
                var isHidden = fiber.memoizedState !== null;
                if (isHidden) {
                  reappearLayoutEffects_complete(subtreeRoot);
                  continue;
                }
              }
              if (firstChild !== null) {
                firstChild.return = fiber;
                nextEffect = firstChild;
              } else {
                reappearLayoutEffects_complete(subtreeRoot);
              }
            }
          }
          function reappearLayoutEffects_complete(subtreeRoot) {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              setCurrentFiber(fiber);
              try {
                reappearLayoutEffectsOnFiber(fiber);
              } catch (error2) {
                captureCommitPhaseError(fiber, fiber.return, error2);
              }
              resetCurrentFiber();
              if (fiber === subtreeRoot) {
                nextEffect = null;
                return;
              }
              var sibling = fiber.sibling;
              if (sibling !== null) {
                sibling.return = fiber.return;
                nextEffect = sibling;
                return;
              }
              nextEffect = fiber.return;
            }
          }
          function commitPassiveMountEffects(root3, finishedWork, committedLanes, committedTransitions) {
            nextEffect = finishedWork;
            commitPassiveMountEffects_begin(finishedWork, root3, committedLanes, committedTransitions);
          }
          function commitPassiveMountEffects_begin(subtreeRoot, root3, committedLanes, committedTransitions) {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              var firstChild = fiber.child;
              if ((fiber.subtreeFlags & PassiveMask) !== NoFlags && firstChild !== null) {
                firstChild.return = fiber;
                nextEffect = firstChild;
              } else {
                commitPassiveMountEffects_complete(subtreeRoot, root3, committedLanes, committedTransitions);
              }
            }
          }
          function commitPassiveMountEffects_complete(subtreeRoot, root3, committedLanes, committedTransitions) {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              if ((fiber.flags & Passive) !== NoFlags) {
                setCurrentFiber(fiber);
                try {
                  commitPassiveMountOnFiber(root3, fiber, committedLanes, committedTransitions);
                } catch (error2) {
                  captureCommitPhaseError(fiber, fiber.return, error2);
                }
                resetCurrentFiber();
              }
              if (fiber === subtreeRoot) {
                nextEffect = null;
                return;
              }
              var sibling = fiber.sibling;
              if (sibling !== null) {
                sibling.return = fiber.return;
                nextEffect = sibling;
                return;
              }
              nextEffect = fiber.return;
            }
          }
          function commitPassiveMountOnFiber(finishedRoot, finishedWork, committedLanes, committedTransitions) {
            switch (finishedWork.tag) {
              case FunctionComponent:
              case ForwardRef:
              case SimpleMemoComponent: {
                if (finishedWork.mode & ProfileMode) {
                  startPassiveEffectTimer();
                  try {
                    commitHookEffectListMount(Passive$1 | HasEffect, finishedWork);
                  } finally {
                    recordPassiveEffectDuration(finishedWork);
                  }
                } else {
                  commitHookEffectListMount(Passive$1 | HasEffect, finishedWork);
                }
                break;
              }
            }
          }
          function commitPassiveUnmountEffects(firstChild) {
            nextEffect = firstChild;
            commitPassiveUnmountEffects_begin();
          }
          function commitPassiveUnmountEffects_begin() {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              var child = fiber.child;
              if ((nextEffect.flags & ChildDeletion) !== NoFlags) {
                var deletions = fiber.deletions;
                if (deletions !== null) {
                  for (var i = 0; i < deletions.length; i++) {
                    var fiberToDelete = deletions[i];
                    nextEffect = fiberToDelete;
                    commitPassiveUnmountEffectsInsideOfDeletedTree_begin(fiberToDelete, fiber);
                  }
                  {
                    var previousFiber = fiber.alternate;
                    if (previousFiber !== null) {
                      var detachedChild = previousFiber.child;
                      if (detachedChild !== null) {
                        previousFiber.child = null;
                        do {
                          var detachedSibling = detachedChild.sibling;
                          detachedChild.sibling = null;
                          detachedChild = detachedSibling;
                        } while (detachedChild !== null);
                      }
                    }
                  }
                  nextEffect = fiber;
                }
              }
              if ((fiber.subtreeFlags & PassiveMask) !== NoFlags && child !== null) {
                child.return = fiber;
                nextEffect = child;
              } else {
                commitPassiveUnmountEffects_complete();
              }
            }
          }
          function commitPassiveUnmountEffects_complete() {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              if ((fiber.flags & Passive) !== NoFlags) {
                setCurrentFiber(fiber);
                commitPassiveUnmountOnFiber(fiber);
                resetCurrentFiber();
              }
              var sibling = fiber.sibling;
              if (sibling !== null) {
                sibling.return = fiber.return;
                nextEffect = sibling;
                return;
              }
              nextEffect = fiber.return;
            }
          }
          function commitPassiveUnmountOnFiber(finishedWork) {
            switch (finishedWork.tag) {
              case FunctionComponent:
              case ForwardRef:
              case SimpleMemoComponent: {
                if (finishedWork.mode & ProfileMode) {
                  startPassiveEffectTimer();
                  commitHookEffectListUnmount(Passive$1 | HasEffect, finishedWork, finishedWork.return);
                  recordPassiveEffectDuration(finishedWork);
                } else {
                  commitHookEffectListUnmount(Passive$1 | HasEffect, finishedWork, finishedWork.return);
                }
                break;
              }
            }
          }
          function commitPassiveUnmountEffectsInsideOfDeletedTree_begin(deletedSubtreeRoot, nearestMountedAncestor) {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              setCurrentFiber(fiber);
              commitPassiveUnmountInsideDeletedTreeOnFiber(fiber, nearestMountedAncestor);
              resetCurrentFiber();
              var child = fiber.child;
              if (child !== null) {
                child.return = fiber;
                nextEffect = child;
              } else {
                commitPassiveUnmountEffectsInsideOfDeletedTree_complete(deletedSubtreeRoot);
              }
            }
          }
          function commitPassiveUnmountEffectsInsideOfDeletedTree_complete(deletedSubtreeRoot) {
            while (nextEffect !== null) {
              var fiber = nextEffect;
              var sibling = fiber.sibling;
              var returnFiber = fiber.return;
              {
                detachFiberAfterEffects(fiber);
                if (fiber === deletedSubtreeRoot) {
                  nextEffect = null;
                  return;
                }
              }
              if (sibling !== null) {
                sibling.return = returnFiber;
                nextEffect = sibling;
                return;
              }
              nextEffect = returnFiber;
            }
          }
          function commitPassiveUnmountInsideDeletedTreeOnFiber(current2, nearestMountedAncestor) {
            switch (current2.tag) {
              case FunctionComponent:
              case ForwardRef:
              case SimpleMemoComponent: {
                if (current2.mode & ProfileMode) {
                  startPassiveEffectTimer();
                  commitHookEffectListUnmount(Passive$1, current2, nearestMountedAncestor);
                  recordPassiveEffectDuration(current2);
                } else {
                  commitHookEffectListUnmount(Passive$1, current2, nearestMountedAncestor);
                }
                break;
              }
            }
          }
          function invokeLayoutEffectMountInDEV(fiber) {
            {
              switch (fiber.tag) {
                case FunctionComponent:
                case ForwardRef:
                case SimpleMemoComponent: {
                  try {
                    commitHookEffectListMount(Layout | HasEffect, fiber);
                  } catch (error2) {
                    captureCommitPhaseError(fiber, fiber.return, error2);
                  }
                  break;
                }
                case ClassComponent: {
                  var instance = fiber.stateNode;
                  try {
                    instance.componentDidMount();
                  } catch (error2) {
                    captureCommitPhaseError(fiber, fiber.return, error2);
                  }
                  break;
                }
              }
            }
          }
          function invokePassiveEffectMountInDEV(fiber) {
            {
              switch (fiber.tag) {
                case FunctionComponent:
                case ForwardRef:
                case SimpleMemoComponent: {
                  try {
                    commitHookEffectListMount(Passive$1 | HasEffect, fiber);
                  } catch (error2) {
                    captureCommitPhaseError(fiber, fiber.return, error2);
                  }
                  break;
                }
              }
            }
          }
          function invokeLayoutEffectUnmountInDEV(fiber) {
            {
              switch (fiber.tag) {
                case FunctionComponent:
                case ForwardRef:
                case SimpleMemoComponent: {
                  try {
                    commitHookEffectListUnmount(Layout | HasEffect, fiber, fiber.return);
                  } catch (error2) {
                    captureCommitPhaseError(fiber, fiber.return, error2);
                  }
                  break;
                }
                case ClassComponent: {
                  var instance = fiber.stateNode;
                  if (typeof instance.componentWillUnmount === "function") {
                    safelyCallComponentWillUnmount(fiber, fiber.return, instance);
                  }
                  break;
                }
              }
            }
          }
          function invokePassiveEffectUnmountInDEV(fiber) {
            {
              switch (fiber.tag) {
                case FunctionComponent:
                case ForwardRef:
                case SimpleMemoComponent: {
                  try {
                    commitHookEffectListUnmount(Passive$1 | HasEffect, fiber, fiber.return);
                  } catch (error2) {
                    captureCommitPhaseError(fiber, fiber.return, error2);
                  }
                }
              }
            }
          }
          var COMPONENT_TYPE = 0;
          var HAS_PSEUDO_CLASS_TYPE = 1;
          var ROLE_TYPE = 2;
          var TEST_NAME_TYPE = 3;
          var TEXT_TYPE = 4;
          if (typeof Symbol === "function" && Symbol.for) {
            var symbolFor = Symbol.for;
            COMPONENT_TYPE = symbolFor("selector.component");
            HAS_PSEUDO_CLASS_TYPE = symbolFor("selector.has_pseudo_class");
            ROLE_TYPE = symbolFor("selector.role");
            TEST_NAME_TYPE = symbolFor("selector.test_id");
            TEXT_TYPE = symbolFor("selector.text");
          }
          var commitHooks = [];
          function onCommitRoot$1() {
            {
              commitHooks.forEach(function(commitHook) {
                return commitHook();
              });
            }
          }
          var ReactCurrentActQueue = ReactSharedInternals.ReactCurrentActQueue;
          function isLegacyActEnvironment(fiber) {
            {
              var isReactActEnvironmentGlobal = (
                // $FlowExpectedError – Flow doesn't know about IS_REACT_ACT_ENVIRONMENT global
                typeof IS_REACT_ACT_ENVIRONMENT !== "undefined" ? IS_REACT_ACT_ENVIRONMENT : void 0
              );
              var jestIsDefined = typeof jest !== "undefined";
              return jestIsDefined && isReactActEnvironmentGlobal !== false;
            }
          }
          function isConcurrentActEnvironment() {
            {
              var isReactActEnvironmentGlobal = (
                // $FlowExpectedError – Flow doesn't know about IS_REACT_ACT_ENVIRONMENT global
                typeof IS_REACT_ACT_ENVIRONMENT !== "undefined" ? IS_REACT_ACT_ENVIRONMENT : void 0
              );
              if (!isReactActEnvironmentGlobal && ReactCurrentActQueue.current !== null) {
                error("The current testing environment is not configured to support act(...)");
              }
              return isReactActEnvironmentGlobal;
            }
          }
          var ceil = Math.ceil;
          var ReactCurrentDispatcher$2 = ReactSharedInternals.ReactCurrentDispatcher, ReactCurrentOwner$2 = ReactSharedInternals.ReactCurrentOwner, ReactCurrentBatchConfig$3 = ReactSharedInternals.ReactCurrentBatchConfig, ReactCurrentActQueue$1 = ReactSharedInternals.ReactCurrentActQueue;
          var NoContext = (
            /*             */
            0
          );
          var BatchedContext = (
            /*               */
            1
          );
          var RenderContext = (
            /*                */
            2
          );
          var CommitContext = (
            /*                */
            4
          );
          var RootInProgress = 0;
          var RootFatalErrored = 1;
          var RootErrored = 2;
          var RootSuspended = 3;
          var RootSuspendedWithDelay = 4;
          var RootCompleted = 5;
          var RootDidNotComplete = 6;
          var executionContext = NoContext;
          var workInProgressRoot = null;
          var workInProgress = null;
          var workInProgressRootRenderLanes = NoLanes;
          var subtreeRenderLanes = NoLanes;
          var subtreeRenderLanesCursor = createCursor(NoLanes);
          var workInProgressRootExitStatus = RootInProgress;
          var workInProgressRootFatalError = null;
          var workInProgressRootIncludedLanes = NoLanes;
          var workInProgressRootSkippedLanes = NoLanes;
          var workInProgressRootInterleavedUpdatedLanes = NoLanes;
          var workInProgressRootPingedLanes = NoLanes;
          var workInProgressRootConcurrentErrors = null;
          var workInProgressRootRecoverableErrors = null;
          var globalMostRecentFallbackTime = 0;
          var FALLBACK_THROTTLE_MS = 500;
          var workInProgressRootRenderTargetTime = Infinity;
          var RENDER_TIMEOUT_MS = 500;
          var workInProgressTransitions = null;
          function resetRenderTimer() {
            workInProgressRootRenderTargetTime = now() + RENDER_TIMEOUT_MS;
          }
          function getRenderTargetTime() {
            return workInProgressRootRenderTargetTime;
          }
          var hasUncaughtError = false;
          var firstUncaughtError = null;
          var legacyErrorBoundariesThatAlreadyFailed = null;
          var rootDoesHavePassiveEffects = false;
          var rootWithPendingPassiveEffects = null;
          var pendingPassiveEffectsLanes = NoLanes;
          var pendingPassiveProfilerEffects = [];
          var pendingPassiveTransitions = null;
          var NESTED_UPDATE_LIMIT = 50;
          var nestedUpdateCount = 0;
          var rootWithNestedUpdates = null;
          var isFlushingPassiveEffects = false;
          var didScheduleUpdateDuringPassiveEffects = false;
          var NESTED_PASSIVE_UPDATE_LIMIT = 50;
          var nestedPassiveUpdateCount = 0;
          var rootWithPassiveNestedUpdates = null;
          var currentEventTime = NoTimestamp;
          var currentEventTransitionLane = NoLanes;
          var isRunningInsertionEffect = false;
          function getWorkInProgressRoot() {
            return workInProgressRoot;
          }
          function requestEventTime() {
            if ((executionContext & (RenderContext | CommitContext)) !== NoContext) {
              return now();
            }
            if (currentEventTime !== NoTimestamp) {
              return currentEventTime;
            }
            currentEventTime = now();
            return currentEventTime;
          }
          function requestUpdateLane(fiber) {
            var mode = fiber.mode;
            if ((mode & ConcurrentMode) === NoMode) {
              return SyncLane;
            } else if ((executionContext & RenderContext) !== NoContext && workInProgressRootRenderLanes !== NoLanes) {
              return pickArbitraryLane(workInProgressRootRenderLanes);
            }
            var isTransition = requestCurrentTransition() !== NoTransition;
            if (isTransition) {
              if (ReactCurrentBatchConfig$3.transition !== null) {
                var transition = ReactCurrentBatchConfig$3.transition;
                if (!transition._updatedFibers) {
                  transition._updatedFibers = /* @__PURE__ */ new Set();
                }
                transition._updatedFibers.add(fiber);
              }
              if (currentEventTransitionLane === NoLane) {
                currentEventTransitionLane = claimNextTransitionLane();
              }
              return currentEventTransitionLane;
            }
            var updateLane = getCurrentUpdatePriority();
            if (updateLane !== NoLane) {
              return updateLane;
            }
            var eventLane = getCurrentEventPriority();
            return eventLane;
          }
          function requestRetryLane(fiber) {
            var mode = fiber.mode;
            if ((mode & ConcurrentMode) === NoMode) {
              return SyncLane;
            }
            return claimNextRetryLane();
          }
          function scheduleUpdateOnFiber(root3, fiber, lane, eventTime) {
            checkForNestedUpdates();
            {
              if (isRunningInsertionEffect) {
                error("useInsertionEffect must not schedule updates.");
              }
            }
            {
              if (isFlushingPassiveEffects) {
                didScheduleUpdateDuringPassiveEffects = true;
              }
            }
            markRootUpdated(root3, lane, eventTime);
            if ((executionContext & RenderContext) !== NoLanes && root3 === workInProgressRoot) {
              warnAboutRenderPhaseUpdatesInDEV(fiber);
            } else {
              {
                if (isDevToolsPresent) {
                  addFiberToLanesMap(root3, fiber, lane);
                }
              }
              warnIfUpdatesNotWrappedWithActDEV(fiber);
              if (root3 === workInProgressRoot) {
                if ((executionContext & RenderContext) === NoContext) {
                  workInProgressRootInterleavedUpdatedLanes = mergeLanes(workInProgressRootInterleavedUpdatedLanes, lane);
                }
                if (workInProgressRootExitStatus === RootSuspendedWithDelay) {
                  markRootSuspended$1(root3, workInProgressRootRenderLanes);
                }
              }
              ensureRootIsScheduled(root3, eventTime);
              if (lane === SyncLane && executionContext === NoContext && (fiber.mode & ConcurrentMode) === NoMode && // Treat `act` as if it's inside `batchedUpdates`, even in legacy mode.
              !ReactCurrentActQueue$1.isBatchingLegacy) {
                resetRenderTimer();
                flushSyncCallbacksOnlyInLegacyMode();
              }
            }
          }
          function scheduleInitialHydrationOnRoot(root3, lane, eventTime) {
            var current2 = root3.current;
            current2.lanes = lane;
            markRootUpdated(root3, lane, eventTime);
            ensureRootIsScheduled(root3, eventTime);
          }
          function isUnsafeClassRenderPhaseUpdate(fiber) {
            return (
              // TODO: Remove outdated deferRenderPhaseUpdateToNextBatch experiment. We
              // decided not to enable it.
              (executionContext & RenderContext) !== NoContext
            );
          }
          function ensureRootIsScheduled(root3, currentTime) {
            var existingCallbackNode = root3.callbackNode;
            markStarvedLanesAsExpired(root3, currentTime);
            var nextLanes = getNextLanes(root3, root3 === workInProgressRoot ? workInProgressRootRenderLanes : NoLanes);
            if (nextLanes === NoLanes) {
              if (existingCallbackNode !== null) {
                cancelCallback$1(existingCallbackNode);
              }
              root3.callbackNode = null;
              root3.callbackPriority = NoLane;
              return;
            }
            var newCallbackPriority = getHighestPriorityLane(nextLanes);
            var existingCallbackPriority = root3.callbackPriority;
            if (existingCallbackPriority === newCallbackPriority && // Special case related to `act`. If the currently scheduled task is a
            // Scheduler task, rather than an `act` task, cancel it and re-scheduled
            // on the `act` queue.
            !(ReactCurrentActQueue$1.current !== null && existingCallbackNode !== fakeActCallbackNode)) {
              {
                if (existingCallbackNode == null && existingCallbackPriority !== SyncLane) {
                  error("Expected scheduled callback to exist. This error is likely caused by a bug in React. Please file an issue.");
                }
              }
              return;
            }
            if (existingCallbackNode != null) {
              cancelCallback$1(existingCallbackNode);
            }
            var newCallbackNode;
            if (newCallbackPriority === SyncLane) {
              if (root3.tag === LegacyRoot) {
                if (ReactCurrentActQueue$1.isBatchingLegacy !== null) {
                  ReactCurrentActQueue$1.didScheduleLegacyUpdate = true;
                }
                scheduleLegacySyncCallback(performSyncWorkOnRoot.bind(null, root3));
              } else {
                scheduleSyncCallback(performSyncWorkOnRoot.bind(null, root3));
              }
              {
                if (ReactCurrentActQueue$1.current !== null) {
                  ReactCurrentActQueue$1.current.push(flushSyncCallbacks);
                } else {
                  scheduleMicrotask(function() {
                    if ((executionContext & (RenderContext | CommitContext)) === NoContext) {
                      flushSyncCallbacks();
                    }
                  });
                }
              }
              newCallbackNode = null;
            } else {
              var schedulerPriorityLevel;
              switch (lanesToEventPriority(nextLanes)) {
                case DiscreteEventPriority:
                  schedulerPriorityLevel = ImmediatePriority;
                  break;
                case ContinuousEventPriority:
                  schedulerPriorityLevel = UserBlockingPriority;
                  break;
                case DefaultEventPriority:
                  schedulerPriorityLevel = NormalPriority;
                  break;
                case IdleEventPriority:
                  schedulerPriorityLevel = IdlePriority;
                  break;
                default:
                  schedulerPriorityLevel = NormalPriority;
                  break;
              }
              newCallbackNode = scheduleCallback$1(schedulerPriorityLevel, performConcurrentWorkOnRoot.bind(null, root3));
            }
            root3.callbackPriority = newCallbackPriority;
            root3.callbackNode = newCallbackNode;
          }
          function performConcurrentWorkOnRoot(root3, didTimeout) {
            {
              resetNestedUpdateFlag();
            }
            currentEventTime = NoTimestamp;
            currentEventTransitionLane = NoLanes;
            if ((executionContext & (RenderContext | CommitContext)) !== NoContext) {
              throw new Error("Should not already be working.");
            }
            var originalCallbackNode = root3.callbackNode;
            var didFlushPassiveEffects = flushPassiveEffects();
            if (didFlushPassiveEffects) {
              if (root3.callbackNode !== originalCallbackNode) {
                return null;
              }
            }
            var lanes = getNextLanes(root3, root3 === workInProgressRoot ? workInProgressRootRenderLanes : NoLanes);
            if (lanes === NoLanes) {
              return null;
            }
            var shouldTimeSlice = !includesBlockingLane(root3, lanes) && !includesExpiredLane(root3, lanes) && !didTimeout;
            var exitStatus = shouldTimeSlice ? renderRootConcurrent(root3, lanes) : renderRootSync(root3, lanes);
            if (exitStatus !== RootInProgress) {
              if (exitStatus === RootErrored) {
                var errorRetryLanes = getLanesToRetrySynchronouslyOnError(root3);
                if (errorRetryLanes !== NoLanes) {
                  lanes = errorRetryLanes;
                  exitStatus = recoverFromConcurrentError(root3, errorRetryLanes);
                }
              }
              if (exitStatus === RootFatalErrored) {
                var fatalError = workInProgressRootFatalError;
                prepareFreshStack(root3, NoLanes);
                markRootSuspended$1(root3, lanes);
                ensureRootIsScheduled(root3, now());
                throw fatalError;
              }
              if (exitStatus === RootDidNotComplete) {
                markRootSuspended$1(root3, lanes);
              } else {
                var renderWasConcurrent = !includesBlockingLane(root3, lanes);
                var finishedWork = root3.current.alternate;
                if (renderWasConcurrent && !isRenderConsistentWithExternalStores(finishedWork)) {
                  exitStatus = renderRootSync(root3, lanes);
                  if (exitStatus === RootErrored) {
                    var _errorRetryLanes = getLanesToRetrySynchronouslyOnError(root3);
                    if (_errorRetryLanes !== NoLanes) {
                      lanes = _errorRetryLanes;
                      exitStatus = recoverFromConcurrentError(root3, _errorRetryLanes);
                    }
                  }
                  if (exitStatus === RootFatalErrored) {
                    var _fatalError = workInProgressRootFatalError;
                    prepareFreshStack(root3, NoLanes);
                    markRootSuspended$1(root3, lanes);
                    ensureRootIsScheduled(root3, now());
                    throw _fatalError;
                  }
                }
                root3.finishedWork = finishedWork;
                root3.finishedLanes = lanes;
                finishConcurrentRender(root3, exitStatus, lanes);
              }
            }
            ensureRootIsScheduled(root3, now());
            if (root3.callbackNode === originalCallbackNode) {
              return performConcurrentWorkOnRoot.bind(null, root3);
            }
            return null;
          }
          function recoverFromConcurrentError(root3, errorRetryLanes) {
            var errorsFromFirstAttempt = workInProgressRootConcurrentErrors;
            if (isRootDehydrated(root3)) {
              var rootWorkInProgress = prepareFreshStack(root3, errorRetryLanes);
              rootWorkInProgress.flags |= ForceClientRender;
              {
                errorHydratingContainer(root3.containerInfo);
              }
            }
            var exitStatus = renderRootSync(root3, errorRetryLanes);
            if (exitStatus !== RootErrored) {
              var errorsFromSecondAttempt = workInProgressRootRecoverableErrors;
              workInProgressRootRecoverableErrors = errorsFromFirstAttempt;
              if (errorsFromSecondAttempt !== null) {
                queueRecoverableErrors(errorsFromSecondAttempt);
              }
            }
            return exitStatus;
          }
          function queueRecoverableErrors(errors) {
            if (workInProgressRootRecoverableErrors === null) {
              workInProgressRootRecoverableErrors = errors;
            } else {
              workInProgressRootRecoverableErrors.push.apply(workInProgressRootRecoverableErrors, errors);
            }
          }
          function finishConcurrentRender(root3, exitStatus, lanes) {
            switch (exitStatus) {
              case RootInProgress:
              case RootFatalErrored: {
                throw new Error("Root did not complete. This is a bug in React.");
              }
              // Flow knows about invariant, so it complains if I add a break
              // statement, but eslint doesn't know about invariant, so it complains
              // if I do. eslint-disable-next-line no-fallthrough
              case RootErrored: {
                commitRoot(root3, workInProgressRootRecoverableErrors, workInProgressTransitions);
                break;
              }
              case RootSuspended: {
                markRootSuspended$1(root3, lanes);
                if (includesOnlyRetries(lanes) && // do not delay if we're inside an act() scope
                !shouldForceFlushFallbacksInDEV()) {
                  var msUntilTimeout = globalMostRecentFallbackTime + FALLBACK_THROTTLE_MS - now();
                  if (msUntilTimeout > 10) {
                    var nextLanes = getNextLanes(root3, NoLanes);
                    if (nextLanes !== NoLanes) {
                      break;
                    }
                    var suspendedLanes = root3.suspendedLanes;
                    if (!isSubsetOfLanes(suspendedLanes, lanes)) {
                      var eventTime = requestEventTime();
                      markRootPinged(root3, suspendedLanes);
                      break;
                    }
                    root3.timeoutHandle = scheduleTimeout(commitRoot.bind(null, root3, workInProgressRootRecoverableErrors, workInProgressTransitions), msUntilTimeout);
                    break;
                  }
                }
                commitRoot(root3, workInProgressRootRecoverableErrors, workInProgressTransitions);
                break;
              }
              case RootSuspendedWithDelay: {
                markRootSuspended$1(root3, lanes);
                if (includesOnlyTransitions(lanes)) {
                  break;
                }
                if (!shouldForceFlushFallbacksInDEV()) {
                  var mostRecentEventTime = getMostRecentEventTime(root3, lanes);
                  var eventTimeMs = mostRecentEventTime;
                  var timeElapsedMs = now() - eventTimeMs;
                  var _msUntilTimeout = jnd(timeElapsedMs) - timeElapsedMs;
                  if (_msUntilTimeout > 10) {
                    root3.timeoutHandle = scheduleTimeout(commitRoot.bind(null, root3, workInProgressRootRecoverableErrors, workInProgressTransitions), _msUntilTimeout);
                    break;
                  }
                }
                commitRoot(root3, workInProgressRootRecoverableErrors, workInProgressTransitions);
                break;
              }
              case RootCompleted: {
                commitRoot(root3, workInProgressRootRecoverableErrors, workInProgressTransitions);
                break;
              }
              default: {
                throw new Error("Unknown root exit status.");
              }
            }
          }
          function isRenderConsistentWithExternalStores(finishedWork) {
            var node = finishedWork;
            while (true) {
              if (node.flags & StoreConsistency) {
                var updateQueue = node.updateQueue;
                if (updateQueue !== null) {
                  var checks = updateQueue.stores;
                  if (checks !== null) {
                    for (var i = 0; i < checks.length; i++) {
                      var check = checks[i];
                      var getSnapshot = check.getSnapshot;
                      var renderedValue = check.value;
                      try {
                        if (!objectIs(getSnapshot(), renderedValue)) {
                          return false;
                        }
                      } catch (error2) {
                        return false;
                      }
                    }
                  }
                }
              }
              var child = node.child;
              if (node.subtreeFlags & StoreConsistency && child !== null) {
                child.return = node;
                node = child;
                continue;
              }
              if (node === finishedWork) {
                return true;
              }
              while (node.sibling === null) {
                if (node.return === null || node.return === finishedWork) {
                  return true;
                }
                node = node.return;
              }
              node.sibling.return = node.return;
              node = node.sibling;
            }
            return true;
          }
          function markRootSuspended$1(root3, suspendedLanes) {
            suspendedLanes = removeLanes(suspendedLanes, workInProgressRootPingedLanes);
            suspendedLanes = removeLanes(suspendedLanes, workInProgressRootInterleavedUpdatedLanes);
            markRootSuspended(root3, suspendedLanes);
          }
          function performSyncWorkOnRoot(root3) {
            {
              syncNestedUpdateFlag();
            }
            if ((executionContext & (RenderContext | CommitContext)) !== NoContext) {
              throw new Error("Should not already be working.");
            }
            flushPassiveEffects();
            var lanes = getNextLanes(root3, NoLanes);
            if (!includesSomeLane(lanes, SyncLane)) {
              ensureRootIsScheduled(root3, now());
              return null;
            }
            var exitStatus = renderRootSync(root3, lanes);
            if (root3.tag !== LegacyRoot && exitStatus === RootErrored) {
              var errorRetryLanes = getLanesToRetrySynchronouslyOnError(root3);
              if (errorRetryLanes !== NoLanes) {
                lanes = errorRetryLanes;
                exitStatus = recoverFromConcurrentError(root3, errorRetryLanes);
              }
            }
            if (exitStatus === RootFatalErrored) {
              var fatalError = workInProgressRootFatalError;
              prepareFreshStack(root3, NoLanes);
              markRootSuspended$1(root3, lanes);
              ensureRootIsScheduled(root3, now());
              throw fatalError;
            }
            if (exitStatus === RootDidNotComplete) {
              throw new Error("Root did not complete. This is a bug in React.");
            }
            var finishedWork = root3.current.alternate;
            root3.finishedWork = finishedWork;
            root3.finishedLanes = lanes;
            commitRoot(root3, workInProgressRootRecoverableErrors, workInProgressTransitions);
            ensureRootIsScheduled(root3, now());
            return null;
          }
          function flushRoot(root3, lanes) {
            if (lanes !== NoLanes) {
              markRootEntangled(root3, mergeLanes(lanes, SyncLane));
              ensureRootIsScheduled(root3, now());
              if ((executionContext & (RenderContext | CommitContext)) === NoContext) {
                resetRenderTimer();
                flushSyncCallbacks();
              }
            }
          }
          function batchedUpdates$1(fn, a) {
            var prevExecutionContext = executionContext;
            executionContext |= BatchedContext;
            try {
              return fn(a);
            } finally {
              executionContext = prevExecutionContext;
              if (executionContext === NoContext && // Treat `act` as if it's inside `batchedUpdates`, even in legacy mode.
              !ReactCurrentActQueue$1.isBatchingLegacy) {
                resetRenderTimer();
                flushSyncCallbacksOnlyInLegacyMode();
              }
            }
          }
          function discreteUpdates(fn, a, b, c, d) {
            var previousPriority = getCurrentUpdatePriority();
            var prevTransition = ReactCurrentBatchConfig$3.transition;
            try {
              ReactCurrentBatchConfig$3.transition = null;
              setCurrentUpdatePriority(DiscreteEventPriority);
              return fn(a, b, c, d);
            } finally {
              setCurrentUpdatePriority(previousPriority);
              ReactCurrentBatchConfig$3.transition = prevTransition;
              if (executionContext === NoContext) {
                resetRenderTimer();
              }
            }
          }
          function flushSync(fn) {
            if (rootWithPendingPassiveEffects !== null && rootWithPendingPassiveEffects.tag === LegacyRoot && (executionContext & (RenderContext | CommitContext)) === NoContext) {
              flushPassiveEffects();
            }
            var prevExecutionContext = executionContext;
            executionContext |= BatchedContext;
            var prevTransition = ReactCurrentBatchConfig$3.transition;
            var previousPriority = getCurrentUpdatePriority();
            try {
              ReactCurrentBatchConfig$3.transition = null;
              setCurrentUpdatePriority(DiscreteEventPriority);
              if (fn) {
                return fn();
              } else {
                return void 0;
              }
            } finally {
              setCurrentUpdatePriority(previousPriority);
              ReactCurrentBatchConfig$3.transition = prevTransition;
              executionContext = prevExecutionContext;
              if ((executionContext & (RenderContext | CommitContext)) === NoContext) {
                flushSyncCallbacks();
              }
            }
          }
          function isAlreadyRendering() {
            return (executionContext & (RenderContext | CommitContext)) !== NoContext;
          }
          function pushRenderLanes(fiber, lanes) {
            push(subtreeRenderLanesCursor, subtreeRenderLanes, fiber);
            subtreeRenderLanes = mergeLanes(subtreeRenderLanes, lanes);
            workInProgressRootIncludedLanes = mergeLanes(workInProgressRootIncludedLanes, lanes);
          }
          function popRenderLanes(fiber) {
            subtreeRenderLanes = subtreeRenderLanesCursor.current;
            pop(subtreeRenderLanesCursor, fiber);
          }
          function prepareFreshStack(root3, lanes) {
            root3.finishedWork = null;
            root3.finishedLanes = NoLanes;
            var timeoutHandle = root3.timeoutHandle;
            if (timeoutHandle !== noTimeout) {
              root3.timeoutHandle = noTimeout;
              cancelTimeout(timeoutHandle);
            }
            if (workInProgress !== null) {
              var interruptedWork = workInProgress.return;
              while (interruptedWork !== null) {
                var current2 = interruptedWork.alternate;
                unwindInterruptedWork(current2, interruptedWork);
                interruptedWork = interruptedWork.return;
              }
            }
            workInProgressRoot = root3;
            var rootWorkInProgress = createWorkInProgress(root3.current, null);
            workInProgress = rootWorkInProgress;
            workInProgressRootRenderLanes = subtreeRenderLanes = workInProgressRootIncludedLanes = lanes;
            workInProgressRootExitStatus = RootInProgress;
            workInProgressRootFatalError = null;
            workInProgressRootSkippedLanes = NoLanes;
            workInProgressRootInterleavedUpdatedLanes = NoLanes;
            workInProgressRootPingedLanes = NoLanes;
            workInProgressRootConcurrentErrors = null;
            workInProgressRootRecoverableErrors = null;
            finishQueueingConcurrentUpdates();
            {
              ReactStrictModeWarnings.discardPendingWarnings();
            }
            return rootWorkInProgress;
          }
          function handleError(root3, thrownValue) {
            do {
              var erroredWork = workInProgress;
              try {
                resetContextDependencies();
                resetHooksAfterThrow();
                resetCurrentFiber();
                ReactCurrentOwner$2.current = null;
                if (erroredWork === null || erroredWork.return === null) {
                  workInProgressRootExitStatus = RootFatalErrored;
                  workInProgressRootFatalError = thrownValue;
                  workInProgress = null;
                  return;
                }
                if (enableProfilerTimer && erroredWork.mode & ProfileMode) {
                  stopProfilerTimerIfRunningAndRecordDelta(erroredWork, true);
                }
                if (enableSchedulingProfiler) {
                  markComponentRenderStopped();
                  if (thrownValue !== null && typeof thrownValue === "object" && typeof thrownValue.then === "function") {
                    var wakeable = thrownValue;
                    markComponentSuspended(erroredWork, wakeable, workInProgressRootRenderLanes);
                  } else {
                    markComponentErrored(erroredWork, thrownValue, workInProgressRootRenderLanes);
                  }
                }
                throwException(root3, erroredWork.return, erroredWork, thrownValue, workInProgressRootRenderLanes);
                completeUnitOfWork(erroredWork);
              } catch (yetAnotherThrownValue) {
                thrownValue = yetAnotherThrownValue;
                if (workInProgress === erroredWork && erroredWork !== null) {
                  erroredWork = erroredWork.return;
                  workInProgress = erroredWork;
                } else {
                  erroredWork = workInProgress;
                }
                continue;
              }
              return;
            } while (true);
          }
          function pushDispatcher() {
            var prevDispatcher = ReactCurrentDispatcher$2.current;
            ReactCurrentDispatcher$2.current = ContextOnlyDispatcher;
            if (prevDispatcher === null) {
              return ContextOnlyDispatcher;
            } else {
              return prevDispatcher;
            }
          }
          function popDispatcher(prevDispatcher) {
            ReactCurrentDispatcher$2.current = prevDispatcher;
          }
          function markCommitTimeOfFallback() {
            globalMostRecentFallbackTime = now();
          }
          function markSkippedUpdateLanes(lane) {
            workInProgressRootSkippedLanes = mergeLanes(lane, workInProgressRootSkippedLanes);
          }
          function renderDidSuspend() {
            if (workInProgressRootExitStatus === RootInProgress) {
              workInProgressRootExitStatus = RootSuspended;
            }
          }
          function renderDidSuspendDelayIfPossible() {
            if (workInProgressRootExitStatus === RootInProgress || workInProgressRootExitStatus === RootSuspended || workInProgressRootExitStatus === RootErrored) {
              workInProgressRootExitStatus = RootSuspendedWithDelay;
            }
            if (workInProgressRoot !== null && (includesNonIdleWork(workInProgressRootSkippedLanes) || includesNonIdleWork(workInProgressRootInterleavedUpdatedLanes))) {
              markRootSuspended$1(workInProgressRoot, workInProgressRootRenderLanes);
            }
          }
          function renderDidError(error2) {
            if (workInProgressRootExitStatus !== RootSuspendedWithDelay) {
              workInProgressRootExitStatus = RootErrored;
            }
            if (workInProgressRootConcurrentErrors === null) {
              workInProgressRootConcurrentErrors = [error2];
            } else {
              workInProgressRootConcurrentErrors.push(error2);
            }
          }
          function renderHasNotSuspendedYet() {
            return workInProgressRootExitStatus === RootInProgress;
          }
          function renderRootSync(root3, lanes) {
            var prevExecutionContext = executionContext;
            executionContext |= RenderContext;
            var prevDispatcher = pushDispatcher();
            if (workInProgressRoot !== root3 || workInProgressRootRenderLanes !== lanes) {
              {
                if (isDevToolsPresent) {
                  var memoizedUpdaters = root3.memoizedUpdaters;
                  if (memoizedUpdaters.size > 0) {
                    restorePendingUpdaters(root3, workInProgressRootRenderLanes);
                    memoizedUpdaters.clear();
                  }
                  movePendingFibersToMemoized(root3, lanes);
                }
              }
              workInProgressTransitions = getTransitionsForLanes();
              prepareFreshStack(root3, lanes);
            }
            {
              markRenderStarted(lanes);
            }
            do {
              try {
                workLoopSync();
                break;
              } catch (thrownValue) {
                handleError(root3, thrownValue);
              }
            } while (true);
            resetContextDependencies();
            executionContext = prevExecutionContext;
            popDispatcher(prevDispatcher);
            if (workInProgress !== null) {
              throw new Error("Cannot commit an incomplete root. This error is likely caused by a bug in React. Please file an issue.");
            }
            {
              markRenderStopped();
            }
            workInProgressRoot = null;
            workInProgressRootRenderLanes = NoLanes;
            return workInProgressRootExitStatus;
          }
          function workLoopSync() {
            while (workInProgress !== null) {
              performUnitOfWork(workInProgress);
            }
          }
          function renderRootConcurrent(root3, lanes) {
            var prevExecutionContext = executionContext;
            executionContext |= RenderContext;
            var prevDispatcher = pushDispatcher();
            if (workInProgressRoot !== root3 || workInProgressRootRenderLanes !== lanes) {
              {
                if (isDevToolsPresent) {
                  var memoizedUpdaters = root3.memoizedUpdaters;
                  if (memoizedUpdaters.size > 0) {
                    restorePendingUpdaters(root3, workInProgressRootRenderLanes);
                    memoizedUpdaters.clear();
                  }
                  movePendingFibersToMemoized(root3, lanes);
                }
              }
              workInProgressTransitions = getTransitionsForLanes();
              resetRenderTimer();
              prepareFreshStack(root3, lanes);
            }
            {
              markRenderStarted(lanes);
            }
            do {
              try {
                workLoopConcurrent();
                break;
              } catch (thrownValue) {
                handleError(root3, thrownValue);
              }
            } while (true);
            resetContextDependencies();
            popDispatcher(prevDispatcher);
            executionContext = prevExecutionContext;
            if (workInProgress !== null) {
              {
                markRenderYielded();
              }
              return RootInProgress;
            } else {
              {
                markRenderStopped();
              }
              workInProgressRoot = null;
              workInProgressRootRenderLanes = NoLanes;
              return workInProgressRootExitStatus;
            }
          }
          function workLoopConcurrent() {
            while (workInProgress !== null && !shouldYield()) {
              performUnitOfWork(workInProgress);
            }
          }
          function performUnitOfWork(unitOfWork) {
            var current2 = unitOfWork.alternate;
            setCurrentFiber(unitOfWork);
            var next;
            if ((unitOfWork.mode & ProfileMode) !== NoMode) {
              startProfilerTimer(unitOfWork);
              next = beginWork$1(current2, unitOfWork, subtreeRenderLanes);
              stopProfilerTimerIfRunningAndRecordDelta(unitOfWork, true);
            } else {
              next = beginWork$1(current2, unitOfWork, subtreeRenderLanes);
            }
            resetCurrentFiber();
            unitOfWork.memoizedProps = unitOfWork.pendingProps;
            if (next === null) {
              completeUnitOfWork(unitOfWork);
            } else {
              workInProgress = next;
            }
            ReactCurrentOwner$2.current = null;
          }
          function completeUnitOfWork(unitOfWork) {
            var completedWork = unitOfWork;
            do {
              var current2 = completedWork.alternate;
              var returnFiber = completedWork.return;
              if ((completedWork.flags & Incomplete) === NoFlags) {
                setCurrentFiber(completedWork);
                var next = void 0;
                if ((completedWork.mode & ProfileMode) === NoMode) {
                  next = completeWork(current2, completedWork, subtreeRenderLanes);
                } else {
                  startProfilerTimer(completedWork);
                  next = completeWork(current2, completedWork, subtreeRenderLanes);
                  stopProfilerTimerIfRunningAndRecordDelta(completedWork, false);
                }
                resetCurrentFiber();
                if (next !== null) {
                  workInProgress = next;
                  return;
                }
              } else {
                var _next = unwindWork(current2, completedWork);
                if (_next !== null) {
                  _next.flags &= HostEffectMask;
                  workInProgress = _next;
                  return;
                }
                if ((completedWork.mode & ProfileMode) !== NoMode) {
                  stopProfilerTimerIfRunningAndRecordDelta(completedWork, false);
                  var actualDuration = completedWork.actualDuration;
                  var child = completedWork.child;
                  while (child !== null) {
                    actualDuration += child.actualDuration;
                    child = child.sibling;
                  }
                  completedWork.actualDuration = actualDuration;
                }
                if (returnFiber !== null) {
                  returnFiber.flags |= Incomplete;
                  returnFiber.subtreeFlags = NoFlags;
                  returnFiber.deletions = null;
                } else {
                  workInProgressRootExitStatus = RootDidNotComplete;
                  workInProgress = null;
                  return;
                }
              }
              var siblingFiber = completedWork.sibling;
              if (siblingFiber !== null) {
                workInProgress = siblingFiber;
                return;
              }
              completedWork = returnFiber;
              workInProgress = completedWork;
            } while (completedWork !== null);
            if (workInProgressRootExitStatus === RootInProgress) {
              workInProgressRootExitStatus = RootCompleted;
            }
          }
          function commitRoot(root3, recoverableErrors, transitions) {
            var previousUpdateLanePriority = getCurrentUpdatePriority();
            var prevTransition = ReactCurrentBatchConfig$3.transition;
            try {
              ReactCurrentBatchConfig$3.transition = null;
              setCurrentUpdatePriority(DiscreteEventPriority);
              commitRootImpl(root3, recoverableErrors, transitions, previousUpdateLanePriority);
            } finally {
              ReactCurrentBatchConfig$3.transition = prevTransition;
              setCurrentUpdatePriority(previousUpdateLanePriority);
            }
            return null;
          }
          function commitRootImpl(root3, recoverableErrors, transitions, renderPriorityLevel) {
            do {
              flushPassiveEffects();
            } while (rootWithPendingPassiveEffects !== null);
            flushRenderPhaseStrictModeWarningsInDEV();
            if ((executionContext & (RenderContext | CommitContext)) !== NoContext) {
              throw new Error("Should not already be working.");
            }
            var finishedWork = root3.finishedWork;
            var lanes = root3.finishedLanes;
            {
              markCommitStarted(lanes);
            }
            if (finishedWork === null) {
              {
                markCommitStopped();
              }
              return null;
            } else {
              {
                if (lanes === NoLanes) {
                  error("root.finishedLanes should not be empty during a commit. This is a bug in React.");
                }
              }
            }
            root3.finishedWork = null;
            root3.finishedLanes = NoLanes;
            if (finishedWork === root3.current) {
              throw new Error("Cannot commit the same tree as before. This error is likely caused by a bug in React. Please file an issue.");
            }
            root3.callbackNode = null;
            root3.callbackPriority = NoLane;
            var remainingLanes = mergeLanes(finishedWork.lanes, finishedWork.childLanes);
            markRootFinished(root3, remainingLanes);
            if (root3 === workInProgressRoot) {
              workInProgressRoot = null;
              workInProgress = null;
              workInProgressRootRenderLanes = NoLanes;
            }
            if ((finishedWork.subtreeFlags & PassiveMask) !== NoFlags || (finishedWork.flags & PassiveMask) !== NoFlags) {
              if (!rootDoesHavePassiveEffects) {
                rootDoesHavePassiveEffects = true;
                pendingPassiveTransitions = transitions;
                scheduleCallback$1(NormalPriority, function() {
                  flushPassiveEffects();
                  return null;
                });
              }
            }
            var subtreeHasEffects = (finishedWork.subtreeFlags & (BeforeMutationMask | MutationMask | LayoutMask | PassiveMask)) !== NoFlags;
            var rootHasEffect = (finishedWork.flags & (BeforeMutationMask | MutationMask | LayoutMask | PassiveMask)) !== NoFlags;
            if (subtreeHasEffects || rootHasEffect) {
              var prevTransition = ReactCurrentBatchConfig$3.transition;
              ReactCurrentBatchConfig$3.transition = null;
              var previousPriority = getCurrentUpdatePriority();
              setCurrentUpdatePriority(DiscreteEventPriority);
              var prevExecutionContext = executionContext;
              executionContext |= CommitContext;
              ReactCurrentOwner$2.current = null;
              var shouldFireAfterActiveInstanceBlur2 = commitBeforeMutationEffects(root3, finishedWork);
              {
                recordCommitTime();
              }
              commitMutationEffects(root3, finishedWork, lanes);
              resetAfterCommit(root3.containerInfo);
              root3.current = finishedWork;
              {
                markLayoutEffectsStarted(lanes);
              }
              commitLayoutEffects(finishedWork, root3, lanes);
              {
                markLayoutEffectsStopped();
              }
              requestPaint();
              executionContext = prevExecutionContext;
              setCurrentUpdatePriority(previousPriority);
              ReactCurrentBatchConfig$3.transition = prevTransition;
            } else {
              root3.current = finishedWork;
              {
                recordCommitTime();
              }
            }
            var rootDidHavePassiveEffects = rootDoesHavePassiveEffects;
            if (rootDoesHavePassiveEffects) {
              rootDoesHavePassiveEffects = false;
              rootWithPendingPassiveEffects = root3;
              pendingPassiveEffectsLanes = lanes;
            } else {
              {
                nestedPassiveUpdateCount = 0;
                rootWithPassiveNestedUpdates = null;
              }
            }
            remainingLanes = root3.pendingLanes;
            if (remainingLanes === NoLanes) {
              legacyErrorBoundariesThatAlreadyFailed = null;
            }
            {
              if (!rootDidHavePassiveEffects) {
                commitDoubleInvokeEffectsInDEV(root3.current, false);
              }
            }
            onCommitRoot(finishedWork.stateNode, renderPriorityLevel);
            {
              if (isDevToolsPresent) {
                root3.memoizedUpdaters.clear();
              }
            }
            {
              onCommitRoot$1();
            }
            ensureRootIsScheduled(root3, now());
            if (recoverableErrors !== null) {
              var onRecoverableError = root3.onRecoverableError;
              for (var i = 0; i < recoverableErrors.length; i++) {
                var recoverableError = recoverableErrors[i];
                var componentStack = recoverableError.stack;
                var digest = recoverableError.digest;
                onRecoverableError(recoverableError.value, {
                  componentStack,
                  digest
                });
              }
            }
            if (hasUncaughtError) {
              hasUncaughtError = false;
              var error$1 = firstUncaughtError;
              firstUncaughtError = null;
              throw error$1;
            }
            if (includesSomeLane(pendingPassiveEffectsLanes, SyncLane) && root3.tag !== LegacyRoot) {
              flushPassiveEffects();
            }
            remainingLanes = root3.pendingLanes;
            if (includesSomeLane(remainingLanes, SyncLane)) {
              {
                markNestedUpdateScheduled();
              }
              if (root3 === rootWithNestedUpdates) {
                nestedUpdateCount++;
              } else {
                nestedUpdateCount = 0;
                rootWithNestedUpdates = root3;
              }
            } else {
              nestedUpdateCount = 0;
            }
            flushSyncCallbacks();
            {
              markCommitStopped();
            }
            return null;
          }
          function flushPassiveEffects() {
            if (rootWithPendingPassiveEffects !== null) {
              var renderPriority = lanesToEventPriority(pendingPassiveEffectsLanes);
              var priority = lowerEventPriority(DefaultEventPriority, renderPriority);
              var prevTransition = ReactCurrentBatchConfig$3.transition;
              var previousPriority = getCurrentUpdatePriority();
              try {
                ReactCurrentBatchConfig$3.transition = null;
                setCurrentUpdatePriority(priority);
                return flushPassiveEffectsImpl();
              } finally {
                setCurrentUpdatePriority(previousPriority);
                ReactCurrentBatchConfig$3.transition = prevTransition;
              }
            }
            return false;
          }
          function enqueuePendingPassiveProfilerEffect(fiber) {
            {
              pendingPassiveProfilerEffects.push(fiber);
              if (!rootDoesHavePassiveEffects) {
                rootDoesHavePassiveEffects = true;
                scheduleCallback$1(NormalPriority, function() {
                  flushPassiveEffects();
                  return null;
                });
              }
            }
          }
          function flushPassiveEffectsImpl() {
            if (rootWithPendingPassiveEffects === null) {
              return false;
            }
            var transitions = pendingPassiveTransitions;
            pendingPassiveTransitions = null;
            var root3 = rootWithPendingPassiveEffects;
            var lanes = pendingPassiveEffectsLanes;
            rootWithPendingPassiveEffects = null;
            pendingPassiveEffectsLanes = NoLanes;
            if ((executionContext & (RenderContext | CommitContext)) !== NoContext) {
              throw new Error("Cannot flush passive effects while already rendering.");
            }
            {
              isFlushingPassiveEffects = true;
              didScheduleUpdateDuringPassiveEffects = false;
            }
            {
              markPassiveEffectsStarted(lanes);
            }
            var prevExecutionContext = executionContext;
            executionContext |= CommitContext;
            commitPassiveUnmountEffects(root3.current);
            commitPassiveMountEffects(root3, root3.current, lanes, transitions);
            {
              var profilerEffects = pendingPassiveProfilerEffects;
              pendingPassiveProfilerEffects = [];
              for (var i = 0; i < profilerEffects.length; i++) {
                var _fiber = profilerEffects[i];
                commitPassiveEffectDurations(root3, _fiber);
              }
            }
            {
              markPassiveEffectsStopped();
            }
            {
              commitDoubleInvokeEffectsInDEV(root3.current, true);
            }
            executionContext = prevExecutionContext;
            flushSyncCallbacks();
            {
              if (didScheduleUpdateDuringPassiveEffects) {
                if (root3 === rootWithPassiveNestedUpdates) {
                  nestedPassiveUpdateCount++;
                } else {
                  nestedPassiveUpdateCount = 0;
                  rootWithPassiveNestedUpdates = root3;
                }
              } else {
                nestedPassiveUpdateCount = 0;
              }
              isFlushingPassiveEffects = false;
              didScheduleUpdateDuringPassiveEffects = false;
            }
            onPostCommitRoot(root3);
            {
              var stateNode = root3.current.stateNode;
              stateNode.effectDuration = 0;
              stateNode.passiveEffectDuration = 0;
            }
            return true;
          }
          function isAlreadyFailedLegacyErrorBoundary(instance) {
            return legacyErrorBoundariesThatAlreadyFailed !== null && legacyErrorBoundariesThatAlreadyFailed.has(instance);
          }
          function markLegacyErrorBoundaryAsFailed(instance) {
            if (legacyErrorBoundariesThatAlreadyFailed === null) {
              legacyErrorBoundariesThatAlreadyFailed = /* @__PURE__ */ new Set([instance]);
            } else {
              legacyErrorBoundariesThatAlreadyFailed.add(instance);
            }
          }
          function prepareToThrowUncaughtError(error2) {
            if (!hasUncaughtError) {
              hasUncaughtError = true;
              firstUncaughtError = error2;
            }
          }
          var onUncaughtError = prepareToThrowUncaughtError;
          function captureCommitPhaseErrorOnRoot(rootFiber, sourceFiber, error2) {
            var errorInfo = createCapturedValueAtFiber(error2, sourceFiber);
            var update = createRootErrorUpdate(rootFiber, errorInfo, SyncLane);
            var root3 = enqueueUpdate(rootFiber, update, SyncLane);
            var eventTime = requestEventTime();
            if (root3 !== null) {
              markRootUpdated(root3, SyncLane, eventTime);
              ensureRootIsScheduled(root3, eventTime);
            }
          }
          function captureCommitPhaseError(sourceFiber, nearestMountedAncestor, error$1) {
            {
              reportUncaughtErrorInDEV(error$1);
              setIsRunningInsertionEffect(false);
            }
            if (sourceFiber.tag === HostRoot) {
              captureCommitPhaseErrorOnRoot(sourceFiber, sourceFiber, error$1);
              return;
            }
            var fiber = null;
            {
              fiber = nearestMountedAncestor;
            }
            while (fiber !== null) {
              if (fiber.tag === HostRoot) {
                captureCommitPhaseErrorOnRoot(fiber, sourceFiber, error$1);
                return;
              } else if (fiber.tag === ClassComponent) {
                var ctor = fiber.type;
                var instance = fiber.stateNode;
                if (typeof ctor.getDerivedStateFromError === "function" || typeof instance.componentDidCatch === "function" && !isAlreadyFailedLegacyErrorBoundary(instance)) {
                  var errorInfo = createCapturedValueAtFiber(error$1, sourceFiber);
                  var update = createClassErrorUpdate(fiber, errorInfo, SyncLane);
                  var root3 = enqueueUpdate(fiber, update, SyncLane);
                  var eventTime = requestEventTime();
                  if (root3 !== null) {
                    markRootUpdated(root3, SyncLane, eventTime);
                    ensureRootIsScheduled(root3, eventTime);
                  }
                  return;
                }
              }
              fiber = fiber.return;
            }
            {
              error("Internal React error: Attempted to capture a commit phase error inside a detached tree. This indicates a bug in React. Likely causes include deleting the same fiber more than once, committing an already-finished tree, or an inconsistent return pointer.\n\nError message:\n\n%s", error$1);
            }
          }
          function pingSuspendedRoot(root3, wakeable, pingedLanes) {
            var pingCache = root3.pingCache;
            if (pingCache !== null) {
              pingCache.delete(wakeable);
            }
            var eventTime = requestEventTime();
            markRootPinged(root3, pingedLanes);
            warnIfSuspenseResolutionNotWrappedWithActDEV(root3);
            if (workInProgressRoot === root3 && isSubsetOfLanes(workInProgressRootRenderLanes, pingedLanes)) {
              if (workInProgressRootExitStatus === RootSuspendedWithDelay || workInProgressRootExitStatus === RootSuspended && includesOnlyRetries(workInProgressRootRenderLanes) && now() - globalMostRecentFallbackTime < FALLBACK_THROTTLE_MS) {
                prepareFreshStack(root3, NoLanes);
              } else {
                workInProgressRootPingedLanes = mergeLanes(workInProgressRootPingedLanes, pingedLanes);
              }
            }
            ensureRootIsScheduled(root3, eventTime);
          }
          function retryTimedOutBoundary(boundaryFiber, retryLane) {
            if (retryLane === NoLane) {
              retryLane = requestRetryLane(boundaryFiber);
            }
            var eventTime = requestEventTime();
            var root3 = enqueueConcurrentRenderForLane(boundaryFiber, retryLane);
            if (root3 !== null) {
              markRootUpdated(root3, retryLane, eventTime);
              ensureRootIsScheduled(root3, eventTime);
            }
          }
          function retryDehydratedSuspenseBoundary(boundaryFiber) {
            var suspenseState = boundaryFiber.memoizedState;
            var retryLane = NoLane;
            if (suspenseState !== null) {
              retryLane = suspenseState.retryLane;
            }
            retryTimedOutBoundary(boundaryFiber, retryLane);
          }
          function resolveRetryWakeable(boundaryFiber, wakeable) {
            var retryLane = NoLane;
            var retryCache;
            switch (boundaryFiber.tag) {
              case SuspenseComponent:
                retryCache = boundaryFiber.stateNode;
                var suspenseState = boundaryFiber.memoizedState;
                if (suspenseState !== null) {
                  retryLane = suspenseState.retryLane;
                }
                break;
              case SuspenseListComponent:
                retryCache = boundaryFiber.stateNode;
                break;
              default:
                throw new Error("Pinged unknown suspense boundary type. This is probably a bug in React.");
            }
            if (retryCache !== null) {
              retryCache.delete(wakeable);
            }
            retryTimedOutBoundary(boundaryFiber, retryLane);
          }
          function jnd(timeElapsed) {
            return timeElapsed < 120 ? 120 : timeElapsed < 480 ? 480 : timeElapsed < 1080 ? 1080 : timeElapsed < 1920 ? 1920 : timeElapsed < 3e3 ? 3e3 : timeElapsed < 4320 ? 4320 : ceil(timeElapsed / 1960) * 1960;
          }
          function checkForNestedUpdates() {
            if (nestedUpdateCount > NESTED_UPDATE_LIMIT) {
              nestedUpdateCount = 0;
              rootWithNestedUpdates = null;
              throw new Error("Maximum update depth exceeded. This can happen when a component repeatedly calls setState inside componentWillUpdate or componentDidUpdate. React limits the number of nested updates to prevent infinite loops.");
            }
            {
              if (nestedPassiveUpdateCount > NESTED_PASSIVE_UPDATE_LIMIT) {
                nestedPassiveUpdateCount = 0;
                rootWithPassiveNestedUpdates = null;
                error("Maximum update depth exceeded. This can happen when a component calls setState inside useEffect, but useEffect either doesn't have a dependency array, or one of the dependencies changes on every render.");
              }
            }
          }
          function flushRenderPhaseStrictModeWarningsInDEV() {
            {
              ReactStrictModeWarnings.flushLegacyContextWarning();
              {
                ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings();
              }
            }
          }
          function commitDoubleInvokeEffectsInDEV(fiber, hasPassiveEffects) {
            {
              setCurrentFiber(fiber);
              invokeEffectsInDev(fiber, MountLayoutDev, invokeLayoutEffectUnmountInDEV);
              if (hasPassiveEffects) {
                invokeEffectsInDev(fiber, MountPassiveDev, invokePassiveEffectUnmountInDEV);
              }
              invokeEffectsInDev(fiber, MountLayoutDev, invokeLayoutEffectMountInDEV);
              if (hasPassiveEffects) {
                invokeEffectsInDev(fiber, MountPassiveDev, invokePassiveEffectMountInDEV);
              }
              resetCurrentFiber();
            }
          }
          function invokeEffectsInDev(firstChild, fiberFlags, invokeEffectFn) {
            {
              var current2 = firstChild;
              var subtreeRoot = null;
              while (current2 !== null) {
                var primarySubtreeFlag = current2.subtreeFlags & fiberFlags;
                if (current2 !== subtreeRoot && current2.child !== null && primarySubtreeFlag !== NoFlags) {
                  current2 = current2.child;
                } else {
                  if ((current2.flags & fiberFlags) !== NoFlags) {
                    invokeEffectFn(current2);
                  }
                  if (current2.sibling !== null) {
                    current2 = current2.sibling;
                  } else {
                    current2 = subtreeRoot = current2.return;
                  }
                }
              }
            }
          }
          var didWarnStateUpdateForNotYetMountedComponent = null;
          function warnAboutUpdateOnNotYetMountedFiberInDEV(fiber) {
            {
              if ((executionContext & RenderContext) !== NoContext) {
                return;
              }
              if (!(fiber.mode & ConcurrentMode)) {
                return;
              }
              var tag = fiber.tag;
              if (tag !== IndeterminateComponent && tag !== HostRoot && tag !== ClassComponent && tag !== FunctionComponent && tag !== ForwardRef && tag !== MemoComponent && tag !== SimpleMemoComponent) {
                return;
              }
              var componentName = getComponentNameFromFiber(fiber) || "ReactComponent";
              if (didWarnStateUpdateForNotYetMountedComponent !== null) {
                if (didWarnStateUpdateForNotYetMountedComponent.has(componentName)) {
                  return;
                }
                didWarnStateUpdateForNotYetMountedComponent.add(componentName);
              } else {
                didWarnStateUpdateForNotYetMountedComponent = /* @__PURE__ */ new Set([componentName]);
              }
              var previousFiber = current;
              try {
                setCurrentFiber(fiber);
                error("Can't perform a React state update on a component that hasn't mounted yet. This indicates that you have a side-effect in your render function that asynchronously later calls tries to update the component. Move this work to useEffect instead.");
              } finally {
                if (previousFiber) {
                  setCurrentFiber(fiber);
                } else {
                  resetCurrentFiber();
                }
              }
            }
          }
          var beginWork$1;
          {
            var dummyFiber = null;
            beginWork$1 = function(current2, unitOfWork, lanes) {
              var originalWorkInProgressCopy = assignFiberPropertiesInDEV(dummyFiber, unitOfWork);
              try {
                return beginWork(current2, unitOfWork, lanes);
              } catch (originalError) {
                if (didSuspendOrErrorWhileHydratingDEV() || originalError !== null && typeof originalError === "object" && typeof originalError.then === "function") {
                  throw originalError;
                }
                resetContextDependencies();
                resetHooksAfterThrow();
                unwindInterruptedWork(current2, unitOfWork);
                assignFiberPropertiesInDEV(unitOfWork, originalWorkInProgressCopy);
                if (unitOfWork.mode & ProfileMode) {
                  startProfilerTimer(unitOfWork);
                }
                invokeGuardedCallback(null, beginWork, null, current2, unitOfWork, lanes);
                if (hasCaughtError()) {
                  var replayError = clearCaughtError();
                  if (typeof replayError === "object" && replayError !== null && replayError._suppressLogging && typeof originalError === "object" && originalError !== null && !originalError._suppressLogging) {
                    originalError._suppressLogging = true;
                  }
                }
                throw originalError;
              }
            };
          }
          var didWarnAboutUpdateInRender = false;
          var didWarnAboutUpdateInRenderForAnotherComponent;
          {
            didWarnAboutUpdateInRenderForAnotherComponent = /* @__PURE__ */ new Set();
          }
          function warnAboutRenderPhaseUpdatesInDEV(fiber) {
            {
              if (isRendering && !getIsUpdatingOpaqueValueInRenderPhaseInDEV()) {
                switch (fiber.tag) {
                  case FunctionComponent:
                  case ForwardRef:
                  case SimpleMemoComponent: {
                    var renderingComponentName = workInProgress && getComponentNameFromFiber(workInProgress) || "Unknown";
                    var dedupeKey = renderingComponentName;
                    if (!didWarnAboutUpdateInRenderForAnotherComponent.has(dedupeKey)) {
                      didWarnAboutUpdateInRenderForAnotherComponent.add(dedupeKey);
                      var setStateComponentName = getComponentNameFromFiber(fiber) || "Unknown";
                      error("Cannot update a component (`%s`) while rendering a different component (`%s`). To locate the bad setState() call inside `%s`, follow the stack trace as described in https://reactjs.org/link/setstate-in-render", setStateComponentName, renderingComponentName, renderingComponentName);
                    }
                    break;
                  }
                  case ClassComponent: {
                    if (!didWarnAboutUpdateInRender) {
                      error("Cannot update during an existing state transition (such as within `render`). Render methods should be a pure function of props and state.");
                      didWarnAboutUpdateInRender = true;
                    }
                    break;
                  }
                }
              }
            }
          }
          function restorePendingUpdaters(root3, lanes) {
            {
              if (isDevToolsPresent) {
                var memoizedUpdaters = root3.memoizedUpdaters;
                memoizedUpdaters.forEach(function(schedulingFiber) {
                  addFiberToLanesMap(root3, schedulingFiber, lanes);
                });
              }
            }
          }
          var fakeActCallbackNode = {};
          function scheduleCallback$1(priorityLevel, callback) {
            {
              var actQueue = ReactCurrentActQueue$1.current;
              if (actQueue !== null) {
                actQueue.push(callback);
                return fakeActCallbackNode;
              } else {
                return scheduleCallback(priorityLevel, callback);
              }
            }
          }
          function cancelCallback$1(callbackNode) {
            if (callbackNode === fakeActCallbackNode) {
              return;
            }
            return cancelCallback(callbackNode);
          }
          function shouldForceFlushFallbacksInDEV() {
            return ReactCurrentActQueue$1.current !== null;
          }
          function warnIfUpdatesNotWrappedWithActDEV(fiber) {
            {
              if (fiber.mode & ConcurrentMode) {
                if (!isConcurrentActEnvironment()) {
                  return;
                }
              } else {
                if (!isLegacyActEnvironment()) {
                  return;
                }
                if (executionContext !== NoContext) {
                  return;
                }
                if (fiber.tag !== FunctionComponent && fiber.tag !== ForwardRef && fiber.tag !== SimpleMemoComponent) {
                  return;
                }
              }
              if (ReactCurrentActQueue$1.current === null) {
                var previousFiber = current;
                try {
                  setCurrentFiber(fiber);
                  error("An update to %s inside a test was not wrapped in act(...).\n\nWhen testing, code that causes React state updates should be wrapped into act(...):\n\nact(() => {\n  /* fire events that update state */\n});\n/* assert on the output */\n\nThis ensures that you're testing the behavior the user would see in the browser. Learn more at https://reactjs.org/link/wrap-tests-with-act", getComponentNameFromFiber(fiber));
                } finally {
                  if (previousFiber) {
                    setCurrentFiber(fiber);
                  } else {
                    resetCurrentFiber();
                  }
                }
              }
            }
          }
          function warnIfSuspenseResolutionNotWrappedWithActDEV(root3) {
            {
              if (root3.tag !== LegacyRoot && isConcurrentActEnvironment() && ReactCurrentActQueue$1.current === null) {
                error("A suspended resource finished loading inside a test, but the event was not wrapped in act(...).\n\nWhen testing, code that resolves suspended data should be wrapped into act(...):\n\nact(() => {\n  /* finish loading suspended data */\n});\n/* assert on the output */\n\nThis ensures that you're testing the behavior the user would see in the browser. Learn more at https://reactjs.org/link/wrap-tests-with-act");
              }
            }
          }
          function setIsRunningInsertionEffect(isRunning) {
            {
              isRunningInsertionEffect = isRunning;
            }
          }
          var resolveFamily = null;
          var failedBoundaries = null;
          var setRefreshHandler = function(handler) {
            {
              resolveFamily = handler;
            }
          };
          function resolveFunctionForHotReloading(type) {
            {
              if (resolveFamily === null) {
                return type;
              }
              var family = resolveFamily(type);
              if (family === void 0) {
                return type;
              }
              return family.current;
            }
          }
          function resolveClassForHotReloading(type) {
            return resolveFunctionForHotReloading(type);
          }
          function resolveForwardRefForHotReloading(type) {
            {
              if (resolveFamily === null) {
                return type;
              }
              var family = resolveFamily(type);
              if (family === void 0) {
                if (type !== null && type !== void 0 && typeof type.render === "function") {
                  var currentRender = resolveFunctionForHotReloading(type.render);
                  if (type.render !== currentRender) {
                    var syntheticType = {
                      $$typeof: REACT_FORWARD_REF_TYPE,
                      render: currentRender
                    };
                    if (type.displayName !== void 0) {
                      syntheticType.displayName = type.displayName;
                    }
                    return syntheticType;
                  }
                }
                return type;
              }
              return family.current;
            }
          }
          function isCompatibleFamilyForHotReloading(fiber, element) {
            {
              if (resolveFamily === null) {
                return false;
              }
              var prevType = fiber.elementType;
              var nextType = element.type;
              var needsCompareFamilies = false;
              var $$typeofNextType = typeof nextType === "object" && nextType !== null ? nextType.$$typeof : null;
              switch (fiber.tag) {
                case ClassComponent: {
                  if (typeof nextType === "function") {
                    needsCompareFamilies = true;
                  }
                  break;
                }
                case FunctionComponent: {
                  if (typeof nextType === "function") {
                    needsCompareFamilies = true;
                  } else if ($$typeofNextType === REACT_LAZY_TYPE) {
                    needsCompareFamilies = true;
                  }
                  break;
                }
                case ForwardRef: {
                  if ($$typeofNextType === REACT_FORWARD_REF_TYPE) {
                    needsCompareFamilies = true;
                  } else if ($$typeofNextType === REACT_LAZY_TYPE) {
                    needsCompareFamilies = true;
                  }
                  break;
                }
                case MemoComponent:
                case SimpleMemoComponent: {
                  if ($$typeofNextType === REACT_MEMO_TYPE) {
                    needsCompareFamilies = true;
                  } else if ($$typeofNextType === REACT_LAZY_TYPE) {
                    needsCompareFamilies = true;
                  }
                  break;
                }
                default:
                  return false;
              }
              if (needsCompareFamilies) {
                var prevFamily = resolveFamily(prevType);
                if (prevFamily !== void 0 && prevFamily === resolveFamily(nextType)) {
                  return true;
                }
              }
              return false;
            }
          }
          function markFailedErrorBoundaryForHotReloading(fiber) {
            {
              if (resolveFamily === null) {
                return;
              }
              if (typeof WeakSet !== "function") {
                return;
              }
              if (failedBoundaries === null) {
                failedBoundaries = /* @__PURE__ */ new WeakSet();
              }
              failedBoundaries.add(fiber);
            }
          }
          var scheduleRefresh = function(root3, update) {
            {
              if (resolveFamily === null) {
                return;
              }
              var staleFamilies = update.staleFamilies, updatedFamilies = update.updatedFamilies;
              flushPassiveEffects();
              flushSync(function() {
                scheduleFibersWithFamiliesRecursively(root3.current, updatedFamilies, staleFamilies);
              });
            }
          };
          var scheduleRoot = function(root3, element) {
            {
              if (root3.context !== emptyContextObject) {
                return;
              }
              flushPassiveEffects();
              flushSync(function() {
                updateContainer(element, root3, null, null);
              });
            }
          };
          function scheduleFibersWithFamiliesRecursively(fiber, updatedFamilies, staleFamilies) {
            {
              var alternate = fiber.alternate, child = fiber.child, sibling = fiber.sibling, tag = fiber.tag, type = fiber.type;
              var candidateType = null;
              switch (tag) {
                case FunctionComponent:
                case SimpleMemoComponent:
                case ClassComponent:
                  candidateType = type;
                  break;
                case ForwardRef:
                  candidateType = type.render;
                  break;
              }
              if (resolveFamily === null) {
                throw new Error("Expected resolveFamily to be set during hot reload.");
              }
              var needsRender = false;
              var needsRemount = false;
              if (candidateType !== null) {
                var family = resolveFamily(candidateType);
                if (family !== void 0) {
                  if (staleFamilies.has(family)) {
                    needsRemount = true;
                  } else if (updatedFamilies.has(family)) {
                    if (tag === ClassComponent) {
                      needsRemount = true;
                    } else {
                      needsRender = true;
                    }
                  }
                }
              }
              if (failedBoundaries !== null) {
                if (failedBoundaries.has(fiber) || alternate !== null && failedBoundaries.has(alternate)) {
                  needsRemount = true;
                }
              }
              if (needsRemount) {
                fiber._debugNeedsRemount = true;
              }
              if (needsRemount || needsRender) {
                var _root = enqueueConcurrentRenderForLane(fiber, SyncLane);
                if (_root !== null) {
                  scheduleUpdateOnFiber(_root, fiber, SyncLane, NoTimestamp);
                }
              }
              if (child !== null && !needsRemount) {
                scheduleFibersWithFamiliesRecursively(child, updatedFamilies, staleFamilies);
              }
              if (sibling !== null) {
                scheduleFibersWithFamiliesRecursively(sibling, updatedFamilies, staleFamilies);
              }
            }
          }
          var findHostInstancesForRefresh = function(root3, families) {
            {
              var hostInstances = /* @__PURE__ */ new Set();
              var types = new Set(families.map(function(family) {
                return family.current;
              }));
              findHostInstancesForMatchingFibersRecursively(root3.current, types, hostInstances);
              return hostInstances;
            }
          };
          function findHostInstancesForMatchingFibersRecursively(fiber, types, hostInstances) {
            {
              var child = fiber.child, sibling = fiber.sibling, tag = fiber.tag, type = fiber.type;
              var candidateType = null;
              switch (tag) {
                case FunctionComponent:
                case SimpleMemoComponent:
                case ClassComponent:
                  candidateType = type;
                  break;
                case ForwardRef:
                  candidateType = type.render;
                  break;
              }
              var didMatch = false;
              if (candidateType !== null) {
                if (types.has(candidateType)) {
                  didMatch = true;
                }
              }
              if (didMatch) {
                findHostInstancesForFiberShallowly(fiber, hostInstances);
              } else {
                if (child !== null) {
                  findHostInstancesForMatchingFibersRecursively(child, types, hostInstances);
                }
              }
              if (sibling !== null) {
                findHostInstancesForMatchingFibersRecursively(sibling, types, hostInstances);
              }
            }
          }
          function findHostInstancesForFiberShallowly(fiber, hostInstances) {
            {
              var foundHostInstances = findChildHostInstancesForFiberShallowly(fiber, hostInstances);
              if (foundHostInstances) {
                return;
              }
              var node = fiber;
              while (true) {
                switch (node.tag) {
                  case HostComponent:
                    hostInstances.add(node.stateNode);
                    return;
                  case HostPortal:
                    hostInstances.add(node.stateNode.containerInfo);
                    return;
                  case HostRoot:
                    hostInstances.add(node.stateNode.containerInfo);
                    return;
                }
                if (node.return === null) {
                  throw new Error("Expected to reach root first.");
                }
                node = node.return;
              }
            }
          }
          function findChildHostInstancesForFiberShallowly(fiber, hostInstances) {
            {
              var node = fiber;
              var foundHostInstances = false;
              while (true) {
                if (node.tag === HostComponent) {
                  foundHostInstances = true;
                  hostInstances.add(node.stateNode);
                } else if (node.child !== null) {
                  node.child.return = node;
                  node = node.child;
                  continue;
                }
                if (node === fiber) {
                  return foundHostInstances;
                }
                while (node.sibling === null) {
                  if (node.return === null || node.return === fiber) {
                    return foundHostInstances;
                  }
                  node = node.return;
                }
                node.sibling.return = node.return;
                node = node.sibling;
              }
            }
            return false;
          }
          var hasBadMapPolyfill;
          {
            hasBadMapPolyfill = false;
            try {
              var nonExtensibleObject = Object.preventExtensions({});
              /* @__PURE__ */ new Map([[nonExtensibleObject, null]]);
              /* @__PURE__ */ new Set([nonExtensibleObject]);
            } catch (e) {
              hasBadMapPolyfill = true;
            }
          }
          function FiberNode(tag, pendingProps, key, mode) {
            this.tag = tag;
            this.key = key;
            this.elementType = null;
            this.type = null;
            this.stateNode = null;
            this.return = null;
            this.child = null;
            this.sibling = null;
            this.index = 0;
            this.ref = null;
            this.pendingProps = pendingProps;
            this.memoizedProps = null;
            this.updateQueue = null;
            this.memoizedState = null;
            this.dependencies = null;
            this.mode = mode;
            this.flags = NoFlags;
            this.subtreeFlags = NoFlags;
            this.deletions = null;
            this.lanes = NoLanes;
            this.childLanes = NoLanes;
            this.alternate = null;
            {
              this.actualDuration = Number.NaN;
              this.actualStartTime = Number.NaN;
              this.selfBaseDuration = Number.NaN;
              this.treeBaseDuration = Number.NaN;
              this.actualDuration = 0;
              this.actualStartTime = -1;
              this.selfBaseDuration = 0;
              this.treeBaseDuration = 0;
            }
            {
              this._debugSource = null;
              this._debugOwner = null;
              this._debugNeedsRemount = false;
              this._debugHookTypes = null;
              if (!hasBadMapPolyfill && typeof Object.preventExtensions === "function") {
                Object.preventExtensions(this);
              }
            }
          }
          var createFiber = function(tag, pendingProps, key, mode) {
            return new FiberNode(tag, pendingProps, key, mode);
          };
          function shouldConstruct$1(Component) {
            var prototype = Component.prototype;
            return !!(prototype && prototype.isReactComponent);
          }
          function isSimpleFunctionComponent(type) {
            return typeof type === "function" && !shouldConstruct$1(type) && type.defaultProps === void 0;
          }
          function resolveLazyComponentTag(Component) {
            if (typeof Component === "function") {
              return shouldConstruct$1(Component) ? ClassComponent : FunctionComponent;
            } else if (Component !== void 0 && Component !== null) {
              var $$typeof = Component.$$typeof;
              if ($$typeof === REACT_FORWARD_REF_TYPE) {
                return ForwardRef;
              }
              if ($$typeof === REACT_MEMO_TYPE) {
                return MemoComponent;
              }
            }
            return IndeterminateComponent;
          }
          function createWorkInProgress(current2, pendingProps) {
            var workInProgress2 = current2.alternate;
            if (workInProgress2 === null) {
              workInProgress2 = createFiber(current2.tag, pendingProps, current2.key, current2.mode);
              workInProgress2.elementType = current2.elementType;
              workInProgress2.type = current2.type;
              workInProgress2.stateNode = current2.stateNode;
              {
                workInProgress2._debugSource = current2._debugSource;
                workInProgress2._debugOwner = current2._debugOwner;
                workInProgress2._debugHookTypes = current2._debugHookTypes;
              }
              workInProgress2.alternate = current2;
              current2.alternate = workInProgress2;
            } else {
              workInProgress2.pendingProps = pendingProps;
              workInProgress2.type = current2.type;
              workInProgress2.flags = NoFlags;
              workInProgress2.subtreeFlags = NoFlags;
              workInProgress2.deletions = null;
              {
                workInProgress2.actualDuration = 0;
                workInProgress2.actualStartTime = -1;
              }
            }
            workInProgress2.flags = current2.flags & StaticMask;
            workInProgress2.childLanes = current2.childLanes;
            workInProgress2.lanes = current2.lanes;
            workInProgress2.child = current2.child;
            workInProgress2.memoizedProps = current2.memoizedProps;
            workInProgress2.memoizedState = current2.memoizedState;
            workInProgress2.updateQueue = current2.updateQueue;
            var currentDependencies = current2.dependencies;
            workInProgress2.dependencies = currentDependencies === null ? null : {
              lanes: currentDependencies.lanes,
              firstContext: currentDependencies.firstContext
            };
            workInProgress2.sibling = current2.sibling;
            workInProgress2.index = current2.index;
            workInProgress2.ref = current2.ref;
            {
              workInProgress2.selfBaseDuration = current2.selfBaseDuration;
              workInProgress2.treeBaseDuration = current2.treeBaseDuration;
            }
            {
              workInProgress2._debugNeedsRemount = current2._debugNeedsRemount;
              switch (workInProgress2.tag) {
                case IndeterminateComponent:
                case FunctionComponent:
                case SimpleMemoComponent:
                  workInProgress2.type = resolveFunctionForHotReloading(current2.type);
                  break;
                case ClassComponent:
                  workInProgress2.type = resolveClassForHotReloading(current2.type);
                  break;
                case ForwardRef:
                  workInProgress2.type = resolveForwardRefForHotReloading(current2.type);
                  break;
              }
            }
            return workInProgress2;
          }
          function resetWorkInProgress(workInProgress2, renderLanes2) {
            workInProgress2.flags &= StaticMask | Placement;
            var current2 = workInProgress2.alternate;
            if (current2 === null) {
              workInProgress2.childLanes = NoLanes;
              workInProgress2.lanes = renderLanes2;
              workInProgress2.child = null;
              workInProgress2.subtreeFlags = NoFlags;
              workInProgress2.memoizedProps = null;
              workInProgress2.memoizedState = null;
              workInProgress2.updateQueue = null;
              workInProgress2.dependencies = null;
              workInProgress2.stateNode = null;
              {
                workInProgress2.selfBaseDuration = 0;
                workInProgress2.treeBaseDuration = 0;
              }
            } else {
              workInProgress2.childLanes = current2.childLanes;
              workInProgress2.lanes = current2.lanes;
              workInProgress2.child = current2.child;
              workInProgress2.subtreeFlags = NoFlags;
              workInProgress2.deletions = null;
              workInProgress2.memoizedProps = current2.memoizedProps;
              workInProgress2.memoizedState = current2.memoizedState;
              workInProgress2.updateQueue = current2.updateQueue;
              workInProgress2.type = current2.type;
              var currentDependencies = current2.dependencies;
              workInProgress2.dependencies = currentDependencies === null ? null : {
                lanes: currentDependencies.lanes,
                firstContext: currentDependencies.firstContext
              };
              {
                workInProgress2.selfBaseDuration = current2.selfBaseDuration;
                workInProgress2.treeBaseDuration = current2.treeBaseDuration;
              }
            }
            return workInProgress2;
          }
          function createHostRootFiber(tag, isStrictMode, concurrentUpdatesByDefaultOverride) {
            var mode;
            if (tag === ConcurrentRoot) {
              mode = ConcurrentMode;
              if (isStrictMode === true) {
                mode |= StrictLegacyMode;
                {
                  mode |= StrictEffectsMode;
                }
              }
            } else {
              mode = NoMode;
            }
            if (isDevToolsPresent) {
              mode |= ProfileMode;
            }
            return createFiber(HostRoot, null, null, mode);
          }
          function createFiberFromTypeAndProps(type, key, pendingProps, owner, mode, lanes) {
            var fiberTag = IndeterminateComponent;
            var resolvedType = type;
            if (typeof type === "function") {
              if (shouldConstruct$1(type)) {
                fiberTag = ClassComponent;
                {
                  resolvedType = resolveClassForHotReloading(resolvedType);
                }
              } else {
                {
                  resolvedType = resolveFunctionForHotReloading(resolvedType);
                }
              }
            } else if (typeof type === "string") {
              fiberTag = HostComponent;
            } else {
              getTag: switch (type) {
                case REACT_FRAGMENT_TYPE:
                  return createFiberFromFragment(pendingProps.children, mode, lanes, key);
                case REACT_STRICT_MODE_TYPE:
                  fiberTag = Mode;
                  mode |= StrictLegacyMode;
                  if ((mode & ConcurrentMode) !== NoMode) {
                    mode |= StrictEffectsMode;
                  }
                  break;
                case REACT_PROFILER_TYPE:
                  return createFiberFromProfiler(pendingProps, mode, lanes, key);
                case REACT_SUSPENSE_TYPE:
                  return createFiberFromSuspense(pendingProps, mode, lanes, key);
                case REACT_SUSPENSE_LIST_TYPE:
                  return createFiberFromSuspenseList(pendingProps, mode, lanes, key);
                case REACT_OFFSCREEN_TYPE:
                  return createFiberFromOffscreen(pendingProps, mode, lanes, key);
                case REACT_LEGACY_HIDDEN_TYPE:
                // eslint-disable-next-line no-fallthrough
                case REACT_SCOPE_TYPE:
                // eslint-disable-next-line no-fallthrough
                case REACT_CACHE_TYPE:
                // eslint-disable-next-line no-fallthrough
                case REACT_TRACING_MARKER_TYPE:
                // eslint-disable-next-line no-fallthrough
                case REACT_DEBUG_TRACING_MODE_TYPE:
                // eslint-disable-next-line no-fallthrough
                default: {
                  if (typeof type === "object" && type !== null) {
                    switch (type.$$typeof) {
                      case REACT_PROVIDER_TYPE:
                        fiberTag = ContextProvider;
                        break getTag;
                      case REACT_CONTEXT_TYPE:
                        fiberTag = ContextConsumer;
                        break getTag;
                      case REACT_FORWARD_REF_TYPE:
                        fiberTag = ForwardRef;
                        {
                          resolvedType = resolveForwardRefForHotReloading(resolvedType);
                        }
                        break getTag;
                      case REACT_MEMO_TYPE:
                        fiberTag = MemoComponent;
                        break getTag;
                      case REACT_LAZY_TYPE:
                        fiberTag = LazyComponent;
                        resolvedType = null;
                        break getTag;
                    }
                  }
                  var info = "";
                  {
                    if (type === void 0 || typeof type === "object" && type !== null && Object.keys(type).length === 0) {
                      info += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.";
                    }
                    var ownerName = owner ? getComponentNameFromFiber(owner) : null;
                    if (ownerName) {
                      info += "\n\nCheck the render method of `" + ownerName + "`.";
                    }
                  }
                  throw new Error("Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) " + ("but got: " + (type == null ? type : typeof type) + "." + info));
                }
              }
            }
            var fiber = createFiber(fiberTag, pendingProps, key, mode);
            fiber.elementType = type;
            fiber.type = resolvedType;
            fiber.lanes = lanes;
            {
              fiber._debugOwner = owner;
            }
            return fiber;
          }
          function createFiberFromElement(element, mode, lanes) {
            var owner = null;
            {
              owner = element._owner;
            }
            var type = element.type;
            var key = element.key;
            var pendingProps = element.props;
            var fiber = createFiberFromTypeAndProps(type, key, pendingProps, owner, mode, lanes);
            {
              fiber._debugSource = element._source;
              fiber._debugOwner = element._owner;
            }
            return fiber;
          }
          function createFiberFromFragment(elements, mode, lanes, key) {
            var fiber = createFiber(Fragment3, elements, key, mode);
            fiber.lanes = lanes;
            return fiber;
          }
          function createFiberFromProfiler(pendingProps, mode, lanes, key) {
            {
              if (typeof pendingProps.id !== "string") {
                error('Profiler must specify an "id" of type `string` as a prop. Received the type `%s` instead.', typeof pendingProps.id);
              }
            }
            var fiber = createFiber(Profiler, pendingProps, key, mode | ProfileMode);
            fiber.elementType = REACT_PROFILER_TYPE;
            fiber.lanes = lanes;
            {
              fiber.stateNode = {
                effectDuration: 0,
                passiveEffectDuration: 0
              };
            }
            return fiber;
          }
          function createFiberFromSuspense(pendingProps, mode, lanes, key) {
            var fiber = createFiber(SuspenseComponent, pendingProps, key, mode);
            fiber.elementType = REACT_SUSPENSE_TYPE;
            fiber.lanes = lanes;
            return fiber;
          }
          function createFiberFromSuspenseList(pendingProps, mode, lanes, key) {
            var fiber = createFiber(SuspenseListComponent, pendingProps, key, mode);
            fiber.elementType = REACT_SUSPENSE_LIST_TYPE;
            fiber.lanes = lanes;
            return fiber;
          }
          function createFiberFromOffscreen(pendingProps, mode, lanes, key) {
            var fiber = createFiber(OffscreenComponent, pendingProps, key, mode);
            fiber.elementType = REACT_OFFSCREEN_TYPE;
            fiber.lanes = lanes;
            var primaryChildInstance = {
              isHidden: false
            };
            fiber.stateNode = primaryChildInstance;
            return fiber;
          }
          function createFiberFromText(content, mode, lanes) {
            var fiber = createFiber(HostText, content, null, mode);
            fiber.lanes = lanes;
            return fiber;
          }
          function createFiberFromHostInstanceForDeletion() {
            var fiber = createFiber(HostComponent, null, null, NoMode);
            fiber.elementType = "DELETED";
            return fiber;
          }
          function createFiberFromDehydratedFragment(dehydratedNode) {
            var fiber = createFiber(DehydratedFragment, null, null, NoMode);
            fiber.stateNode = dehydratedNode;
            return fiber;
          }
          function createFiberFromPortal(portal, mode, lanes) {
            var pendingProps = portal.children !== null ? portal.children : [];
            var fiber = createFiber(HostPortal, pendingProps, portal.key, mode);
            fiber.lanes = lanes;
            fiber.stateNode = {
              containerInfo: portal.containerInfo,
              pendingChildren: null,
              // Used by persistent updates
              implementation: portal.implementation
            };
            return fiber;
          }
          function assignFiberPropertiesInDEV(target, source) {
            if (target === null) {
              target = createFiber(IndeterminateComponent, null, null, NoMode);
            }
            target.tag = source.tag;
            target.key = source.key;
            target.elementType = source.elementType;
            target.type = source.type;
            target.stateNode = source.stateNode;
            target.return = source.return;
            target.child = source.child;
            target.sibling = source.sibling;
            target.index = source.index;
            target.ref = source.ref;
            target.pendingProps = source.pendingProps;
            target.memoizedProps = source.memoizedProps;
            target.updateQueue = source.updateQueue;
            target.memoizedState = source.memoizedState;
            target.dependencies = source.dependencies;
            target.mode = source.mode;
            target.flags = source.flags;
            target.subtreeFlags = source.subtreeFlags;
            target.deletions = source.deletions;
            target.lanes = source.lanes;
            target.childLanes = source.childLanes;
            target.alternate = source.alternate;
            {
              target.actualDuration = source.actualDuration;
              target.actualStartTime = source.actualStartTime;
              target.selfBaseDuration = source.selfBaseDuration;
              target.treeBaseDuration = source.treeBaseDuration;
            }
            target._debugSource = source._debugSource;
            target._debugOwner = source._debugOwner;
            target._debugNeedsRemount = source._debugNeedsRemount;
            target._debugHookTypes = source._debugHookTypes;
            return target;
          }
          function FiberRootNode(containerInfo, tag, hydrate2, identifierPrefix, onRecoverableError) {
            this.tag = tag;
            this.containerInfo = containerInfo;
            this.pendingChildren = null;
            this.current = null;
            this.pingCache = null;
            this.finishedWork = null;
            this.timeoutHandle = noTimeout;
            this.context = null;
            this.pendingContext = null;
            this.callbackNode = null;
            this.callbackPriority = NoLane;
            this.eventTimes = createLaneMap(NoLanes);
            this.expirationTimes = createLaneMap(NoTimestamp);
            this.pendingLanes = NoLanes;
            this.suspendedLanes = NoLanes;
            this.pingedLanes = NoLanes;
            this.expiredLanes = NoLanes;
            this.mutableReadLanes = NoLanes;
            this.finishedLanes = NoLanes;
            this.entangledLanes = NoLanes;
            this.entanglements = createLaneMap(NoLanes);
            this.identifierPrefix = identifierPrefix;
            this.onRecoverableError = onRecoverableError;
            {
              this.mutableSourceEagerHydrationData = null;
            }
            {
              this.effectDuration = 0;
              this.passiveEffectDuration = 0;
            }
            {
              this.memoizedUpdaters = /* @__PURE__ */ new Set();
              var pendingUpdatersLaneMap = this.pendingUpdatersLaneMap = [];
              for (var _i = 0; _i < TotalLanes; _i++) {
                pendingUpdatersLaneMap.push(/* @__PURE__ */ new Set());
              }
            }
            {
              switch (tag) {
                case ConcurrentRoot:
                  this._debugRootType = hydrate2 ? "hydrateRoot()" : "createRoot()";
                  break;
                case LegacyRoot:
                  this._debugRootType = hydrate2 ? "hydrate()" : "render()";
                  break;
              }
            }
          }
          function createFiberRoot(containerInfo, tag, hydrate2, initialChildren, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError, transitionCallbacks) {
            var root3 = new FiberRootNode(containerInfo, tag, hydrate2, identifierPrefix, onRecoverableError);
            var uninitializedFiber = createHostRootFiber(tag, isStrictMode);
            root3.current = uninitializedFiber;
            uninitializedFiber.stateNode = root3;
            {
              var _initialState = {
                element: initialChildren,
                isDehydrated: hydrate2,
                cache: null,
                // not enabled yet
                transitions: null,
                pendingSuspenseBoundaries: null
              };
              uninitializedFiber.memoizedState = _initialState;
            }
            initializeUpdateQueue(uninitializedFiber);
            return root3;
          }
          var ReactVersion = "18.3.1";
          function createPortal(children, containerInfo, implementation) {
            var key = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : null;
            {
              checkKeyStringCoercion(key);
            }
            return {
              // This tag allow us to uniquely identify this as a React Portal
              $$typeof: REACT_PORTAL_TYPE,
              key: key == null ? null : "" + key,
              children,
              containerInfo,
              implementation
            };
          }
          var didWarnAboutNestedUpdates;
          var didWarnAboutFindNodeInStrictMode;
          {
            didWarnAboutNestedUpdates = false;
            didWarnAboutFindNodeInStrictMode = {};
          }
          function getContextForSubtree(parentComponent) {
            if (!parentComponent) {
              return emptyContextObject;
            }
            var fiber = get(parentComponent);
            var parentContext = findCurrentUnmaskedContext(fiber);
            if (fiber.tag === ClassComponent) {
              var Component = fiber.type;
              if (isContextProvider(Component)) {
                return processChildContext(fiber, Component, parentContext);
              }
            }
            return parentContext;
          }
          function findHostInstanceWithWarning(component, methodName) {
            {
              var fiber = get(component);
              if (fiber === void 0) {
                if (typeof component.render === "function") {
                  throw new Error("Unable to find node on an unmounted component.");
                } else {
                  var keys = Object.keys(component).join(",");
                  throw new Error("Argument appears to not be a ReactComponent. Keys: " + keys);
                }
              }
              var hostFiber = findCurrentHostFiber(fiber);
              if (hostFiber === null) {
                return null;
              }
              if (hostFiber.mode & StrictLegacyMode) {
                var componentName = getComponentNameFromFiber(fiber) || "Component";
                if (!didWarnAboutFindNodeInStrictMode[componentName]) {
                  didWarnAboutFindNodeInStrictMode[componentName] = true;
                  var previousFiber = current;
                  try {
                    setCurrentFiber(hostFiber);
                    if (fiber.mode & StrictLegacyMode) {
                      error("%s is deprecated in StrictMode. %s was passed an instance of %s which is inside StrictMode. Instead, add a ref directly to the element you want to reference. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-find-node", methodName, methodName, componentName);
                    } else {
                      error("%s is deprecated in StrictMode. %s was passed an instance of %s which renders StrictMode children. Instead, add a ref directly to the element you want to reference. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-find-node", methodName, methodName, componentName);
                    }
                  } finally {
                    if (previousFiber) {
                      setCurrentFiber(previousFiber);
                    } else {
                      resetCurrentFiber();
                    }
                  }
                }
              }
              return hostFiber.stateNode;
            }
          }
          function createContainer(containerInfo, tag, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError, transitionCallbacks) {
            var hydrate2 = false;
            var initialChildren = null;
            return createFiberRoot(containerInfo, tag, hydrate2, initialChildren, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError);
          }
          function createHydrationContainer(initialChildren, callback, containerInfo, tag, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError, transitionCallbacks) {
            var hydrate2 = true;
            var root3 = createFiberRoot(containerInfo, tag, hydrate2, initialChildren, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError);
            root3.context = getContextForSubtree(null);
            var current2 = root3.current;
            var eventTime = requestEventTime();
            var lane = requestUpdateLane(current2);
            var update = createUpdate(eventTime, lane);
            update.callback = callback !== void 0 && callback !== null ? callback : null;
            enqueueUpdate(current2, update, lane);
            scheduleInitialHydrationOnRoot(root3, lane, eventTime);
            return root3;
          }
          function updateContainer(element, container, parentComponent, callback) {
            {
              onScheduleRoot(container, element);
            }
            var current$1 = container.current;
            var eventTime = requestEventTime();
            var lane = requestUpdateLane(current$1);
            {
              markRenderScheduled(lane);
            }
            var context = getContextForSubtree(parentComponent);
            if (container.context === null) {
              container.context = context;
            } else {
              container.pendingContext = context;
            }
            {
              if (isRendering && current !== null && !didWarnAboutNestedUpdates) {
                didWarnAboutNestedUpdates = true;
                error("Render methods should be a pure function of props and state; triggering nested component updates from render is not allowed. If necessary, trigger nested updates in componentDidUpdate.\n\nCheck the render method of %s.", getComponentNameFromFiber(current) || "Unknown");
              }
            }
            var update = createUpdate(eventTime, lane);
            update.payload = {
              element
            };
            callback = callback === void 0 ? null : callback;
            if (callback !== null) {
              {
                if (typeof callback !== "function") {
                  error("render(...): Expected the last optional `callback` argument to be a function. Instead received: %s.", callback);
                }
              }
              update.callback = callback;
            }
            var root3 = enqueueUpdate(current$1, update, lane);
            if (root3 !== null) {
              scheduleUpdateOnFiber(root3, current$1, lane, eventTime);
              entangleTransitions(root3, current$1, lane);
            }
            return lane;
          }
          function getPublicRootInstance(container) {
            var containerFiber = container.current;
            if (!containerFiber.child) {
              return null;
            }
            switch (containerFiber.child.tag) {
              case HostComponent:
                return getPublicInstance(containerFiber.child.stateNode);
              default:
                return containerFiber.child.stateNode;
            }
          }
          function attemptSynchronousHydration$1(fiber) {
            switch (fiber.tag) {
              case HostRoot: {
                var root3 = fiber.stateNode;
                if (isRootDehydrated(root3)) {
                  var lanes = getHighestPriorityPendingLanes(root3);
                  flushRoot(root3, lanes);
                }
                break;
              }
              case SuspenseComponent: {
                flushSync(function() {
                  var root4 = enqueueConcurrentRenderForLane(fiber, SyncLane);
                  if (root4 !== null) {
                    var eventTime = requestEventTime();
                    scheduleUpdateOnFiber(root4, fiber, SyncLane, eventTime);
                  }
                });
                var retryLane = SyncLane;
                markRetryLaneIfNotHydrated(fiber, retryLane);
                break;
              }
            }
          }
          function markRetryLaneImpl(fiber, retryLane) {
            var suspenseState = fiber.memoizedState;
            if (suspenseState !== null && suspenseState.dehydrated !== null) {
              suspenseState.retryLane = higherPriorityLane(suspenseState.retryLane, retryLane);
            }
          }
          function markRetryLaneIfNotHydrated(fiber, retryLane) {
            markRetryLaneImpl(fiber, retryLane);
            var alternate = fiber.alternate;
            if (alternate) {
              markRetryLaneImpl(alternate, retryLane);
            }
          }
          function attemptContinuousHydration$1(fiber) {
            if (fiber.tag !== SuspenseComponent) {
              return;
            }
            var lane = SelectiveHydrationLane;
            var root3 = enqueueConcurrentRenderForLane(fiber, lane);
            if (root3 !== null) {
              var eventTime = requestEventTime();
              scheduleUpdateOnFiber(root3, fiber, lane, eventTime);
            }
            markRetryLaneIfNotHydrated(fiber, lane);
          }
          function attemptHydrationAtCurrentPriority$1(fiber) {
            if (fiber.tag !== SuspenseComponent) {
              return;
            }
            var lane = requestUpdateLane(fiber);
            var root3 = enqueueConcurrentRenderForLane(fiber, lane);
            if (root3 !== null) {
              var eventTime = requestEventTime();
              scheduleUpdateOnFiber(root3, fiber, lane, eventTime);
            }
            markRetryLaneIfNotHydrated(fiber, lane);
          }
          function findHostInstanceWithNoPortals(fiber) {
            var hostFiber = findCurrentHostFiberWithNoPortals(fiber);
            if (hostFiber === null) {
              return null;
            }
            return hostFiber.stateNode;
          }
          var shouldErrorImpl = function(fiber) {
            return null;
          };
          function shouldError(fiber) {
            return shouldErrorImpl(fiber);
          }
          var shouldSuspendImpl = function(fiber) {
            return false;
          };
          function shouldSuspend(fiber) {
            return shouldSuspendImpl(fiber);
          }
          var overrideHookState = null;
          var overrideHookStateDeletePath = null;
          var overrideHookStateRenamePath = null;
          var overrideProps = null;
          var overridePropsDeletePath = null;
          var overridePropsRenamePath = null;
          var scheduleUpdate = null;
          var setErrorHandler = null;
          var setSuspenseHandler = null;
          {
            var copyWithDeleteImpl = function(obj, path, index2) {
              var key = path[index2];
              var updated = isArray(obj) ? obj.slice() : assign({}, obj);
              if (index2 + 1 === path.length) {
                if (isArray(updated)) {
                  updated.splice(key, 1);
                } else {
                  delete updated[key];
                }
                return updated;
              }
              updated[key] = copyWithDeleteImpl(obj[key], path, index2 + 1);
              return updated;
            };
            var copyWithDelete = function(obj, path) {
              return copyWithDeleteImpl(obj, path, 0);
            };
            var copyWithRenameImpl = function(obj, oldPath, newPath, index2) {
              var oldKey = oldPath[index2];
              var updated = isArray(obj) ? obj.slice() : assign({}, obj);
              if (index2 + 1 === oldPath.length) {
                var newKey = newPath[index2];
                updated[newKey] = updated[oldKey];
                if (isArray(updated)) {
                  updated.splice(oldKey, 1);
                } else {
                  delete updated[oldKey];
                }
              } else {
                updated[oldKey] = copyWithRenameImpl(
                  // $FlowFixMe number or string is fine here
                  obj[oldKey],
                  oldPath,
                  newPath,
                  index2 + 1
                );
              }
              return updated;
            };
            var copyWithRename = function(obj, oldPath, newPath) {
              if (oldPath.length !== newPath.length) {
                warn("copyWithRename() expects paths of the same length");
                return;
              } else {
                for (var i = 0; i < newPath.length - 1; i++) {
                  if (oldPath[i] !== newPath[i]) {
                    warn("copyWithRename() expects paths to be the same except for the deepest key");
                    return;
                  }
                }
              }
              return copyWithRenameImpl(obj, oldPath, newPath, 0);
            };
            var copyWithSetImpl = function(obj, path, index2, value) {
              if (index2 >= path.length) {
                return value;
              }
              var key = path[index2];
              var updated = isArray(obj) ? obj.slice() : assign({}, obj);
              updated[key] = copyWithSetImpl(obj[key], path, index2 + 1, value);
              return updated;
            };
            var copyWithSet = function(obj, path, value) {
              return copyWithSetImpl(obj, path, 0, value);
            };
            var findHook = function(fiber, id) {
              var currentHook2 = fiber.memoizedState;
              while (currentHook2 !== null && id > 0) {
                currentHook2 = currentHook2.next;
                id--;
              }
              return currentHook2;
            };
            overrideHookState = function(fiber, id, path, value) {
              var hook = findHook(fiber, id);
              if (hook !== null) {
                var newState = copyWithSet(hook.memoizedState, path, value);
                hook.memoizedState = newState;
                hook.baseState = newState;
                fiber.memoizedProps = assign({}, fiber.memoizedProps);
                var root3 = enqueueConcurrentRenderForLane(fiber, SyncLane);
                if (root3 !== null) {
                  scheduleUpdateOnFiber(root3, fiber, SyncLane, NoTimestamp);
                }
              }
            };
            overrideHookStateDeletePath = function(fiber, id, path) {
              var hook = findHook(fiber, id);
              if (hook !== null) {
                var newState = copyWithDelete(hook.memoizedState, path);
                hook.memoizedState = newState;
                hook.baseState = newState;
                fiber.memoizedProps = assign({}, fiber.memoizedProps);
                var root3 = enqueueConcurrentRenderForLane(fiber, SyncLane);
                if (root3 !== null) {
                  scheduleUpdateOnFiber(root3, fiber, SyncLane, NoTimestamp);
                }
              }
            };
            overrideHookStateRenamePath = function(fiber, id, oldPath, newPath) {
              var hook = findHook(fiber, id);
              if (hook !== null) {
                var newState = copyWithRename(hook.memoizedState, oldPath, newPath);
                hook.memoizedState = newState;
                hook.baseState = newState;
                fiber.memoizedProps = assign({}, fiber.memoizedProps);
                var root3 = enqueueConcurrentRenderForLane(fiber, SyncLane);
                if (root3 !== null) {
                  scheduleUpdateOnFiber(root3, fiber, SyncLane, NoTimestamp);
                }
              }
            };
            overrideProps = function(fiber, path, value) {
              fiber.pendingProps = copyWithSet(fiber.memoizedProps, path, value);
              if (fiber.alternate) {
                fiber.alternate.pendingProps = fiber.pendingProps;
              }
              var root3 = enqueueConcurrentRenderForLane(fiber, SyncLane);
              if (root3 !== null) {
                scheduleUpdateOnFiber(root3, fiber, SyncLane, NoTimestamp);
              }
            };
            overridePropsDeletePath = function(fiber, path) {
              fiber.pendingProps = copyWithDelete(fiber.memoizedProps, path);
              if (fiber.alternate) {
                fiber.alternate.pendingProps = fiber.pendingProps;
              }
              var root3 = enqueueConcurrentRenderForLane(fiber, SyncLane);
              if (root3 !== null) {
                scheduleUpdateOnFiber(root3, fiber, SyncLane, NoTimestamp);
              }
            };
            overridePropsRenamePath = function(fiber, oldPath, newPath) {
              fiber.pendingProps = copyWithRename(fiber.memoizedProps, oldPath, newPath);
              if (fiber.alternate) {
                fiber.alternate.pendingProps = fiber.pendingProps;
              }
              var root3 = enqueueConcurrentRenderForLane(fiber, SyncLane);
              if (root3 !== null) {
                scheduleUpdateOnFiber(root3, fiber, SyncLane, NoTimestamp);
              }
            };
            scheduleUpdate = function(fiber) {
              var root3 = enqueueConcurrentRenderForLane(fiber, SyncLane);
              if (root3 !== null) {
                scheduleUpdateOnFiber(root3, fiber, SyncLane, NoTimestamp);
              }
            };
            setErrorHandler = function(newShouldErrorImpl) {
              shouldErrorImpl = newShouldErrorImpl;
            };
            setSuspenseHandler = function(newShouldSuspendImpl) {
              shouldSuspendImpl = newShouldSuspendImpl;
            };
          }
          function findHostInstanceByFiber(fiber) {
            var hostFiber = findCurrentHostFiber(fiber);
            if (hostFiber === null) {
              return null;
            }
            return hostFiber.stateNode;
          }
          function emptyFindFiberByHostInstance(instance) {
            return null;
          }
          function getCurrentFiberForDevTools() {
            return current;
          }
          function injectIntoDevTools(devToolsConfig) {
            var findFiberByHostInstance = devToolsConfig.findFiberByHostInstance;
            var ReactCurrentDispatcher2 = ReactSharedInternals.ReactCurrentDispatcher;
            return injectInternals({
              bundleType: devToolsConfig.bundleType,
              version: devToolsConfig.version,
              rendererPackageName: devToolsConfig.rendererPackageName,
              rendererConfig: devToolsConfig.rendererConfig,
              overrideHookState,
              overrideHookStateDeletePath,
              overrideHookStateRenamePath,
              overrideProps,
              overridePropsDeletePath,
              overridePropsRenamePath,
              setErrorHandler,
              setSuspenseHandler,
              scheduleUpdate,
              currentDispatcherRef: ReactCurrentDispatcher2,
              findHostInstanceByFiber,
              findFiberByHostInstance: findFiberByHostInstance || emptyFindFiberByHostInstance,
              // React Refresh
              findHostInstancesForRefresh,
              scheduleRefresh,
              scheduleRoot,
              setRefreshHandler,
              // Enables DevTools to append owner stacks to error messages in DEV mode.
              getCurrentFiber: getCurrentFiberForDevTools,
              // Enables DevTools to detect reconciler version rather than renderer version
              // which may not match for third party renderers.
              reconcilerVersion: ReactVersion
            });
          }
          var defaultOnRecoverableError = typeof reportError === "function" ? (
            // In modern browsers, reportError will dispatch an error event,
            // emulating an uncaught JavaScript error.
            reportError
          ) : function(error2) {
            console["error"](error2);
          };
          function ReactDOMRoot(internalRoot) {
            this._internalRoot = internalRoot;
          }
          ReactDOMHydrationRoot.prototype.render = ReactDOMRoot.prototype.render = function(children) {
            var root3 = this._internalRoot;
            if (root3 === null) {
              throw new Error("Cannot update an unmounted root.");
            }
            {
              if (typeof arguments[1] === "function") {
                error("render(...): does not support the second callback argument. To execute a side effect after rendering, declare it in a component body with useEffect().");
              } else if (isValidContainer(arguments[1])) {
                error("You passed a container to the second argument of root.render(...). You don't need to pass it again since you already passed it to create the root.");
              } else if (typeof arguments[1] !== "undefined") {
                error("You passed a second argument to root.render(...) but it only accepts one argument.");
              }
              var container = root3.containerInfo;
              if (container.nodeType !== COMMENT_NODE) {
                var hostInstance = findHostInstanceWithNoPortals(root3.current);
                if (hostInstance) {
                  if (hostInstance.parentNode !== container) {
                    error("render(...): It looks like the React-rendered content of the root container was removed without using React. This is not supported and will cause errors. Instead, call root.unmount() to empty a root's container.");
                  }
                }
              }
            }
            updateContainer(children, root3, null, null);
          };
          ReactDOMHydrationRoot.prototype.unmount = ReactDOMRoot.prototype.unmount = function() {
            {
              if (typeof arguments[0] === "function") {
                error("unmount(...): does not support a callback argument. To execute a side effect after rendering, declare it in a component body with useEffect().");
              }
            }
            var root3 = this._internalRoot;
            if (root3 !== null) {
              this._internalRoot = null;
              var container = root3.containerInfo;
              {
                if (isAlreadyRendering()) {
                  error("Attempted to synchronously unmount a root while React was already rendering. React cannot finish unmounting the root until the current render has completed, which may lead to a race condition.");
                }
              }
              flushSync(function() {
                updateContainer(null, root3, null, null);
              });
              unmarkContainerAsRoot(container);
            }
          };
          function createRoot2(container, options2) {
            if (!isValidContainer(container)) {
              throw new Error("createRoot(...): Target container is not a DOM element.");
            }
            warnIfReactDOMContainerInDEV(container);
            var isStrictMode = false;
            var concurrentUpdatesByDefaultOverride = false;
            var identifierPrefix = "";
            var onRecoverableError = defaultOnRecoverableError;
            var transitionCallbacks = null;
            if (options2 !== null && options2 !== void 0) {
              {
                if (options2.hydrate) {
                  warn("hydrate through createRoot is deprecated. Use ReactDOMClient.hydrateRoot(container, <App />) instead.");
                } else {
                  if (typeof options2 === "object" && options2 !== null && options2.$$typeof === REACT_ELEMENT_TYPE) {
                    error("You passed a JSX element to createRoot. You probably meant to call root.render instead. Example usage:\n\n  let root = createRoot(domContainer);\n  root.render(<App />);");
                  }
                }
              }
              if (options2.unstable_strictMode === true) {
                isStrictMode = true;
              }
              if (options2.identifierPrefix !== void 0) {
                identifierPrefix = options2.identifierPrefix;
              }
              if (options2.onRecoverableError !== void 0) {
                onRecoverableError = options2.onRecoverableError;
              }
              if (options2.transitionCallbacks !== void 0) {
                transitionCallbacks = options2.transitionCallbacks;
              }
            }
            var root3 = createContainer(container, ConcurrentRoot, null, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError);
            markContainerAsRoot(root3.current, container);
            var rootContainerElement = container.nodeType === COMMENT_NODE ? container.parentNode : container;
            listenToAllSupportedEvents(rootContainerElement);
            return new ReactDOMRoot(root3);
          }
          function ReactDOMHydrationRoot(internalRoot) {
            this._internalRoot = internalRoot;
          }
          function scheduleHydration(target) {
            if (target) {
              queueExplicitHydrationTarget(target);
            }
          }
          ReactDOMHydrationRoot.prototype.unstable_scheduleHydration = scheduleHydration;
          function hydrateRoot(container, initialChildren, options2) {
            if (!isValidContainer(container)) {
              throw new Error("hydrateRoot(...): Target container is not a DOM element.");
            }
            warnIfReactDOMContainerInDEV(container);
            {
              if (initialChildren === void 0) {
                error("Must provide initial children as second argument to hydrateRoot. Example usage: hydrateRoot(domContainer, <App />)");
              }
            }
            var hydrationCallbacks = options2 != null ? options2 : null;
            var mutableSources = options2 != null && options2.hydratedSources || null;
            var isStrictMode = false;
            var concurrentUpdatesByDefaultOverride = false;
            var identifierPrefix = "";
            var onRecoverableError = defaultOnRecoverableError;
            if (options2 !== null && options2 !== void 0) {
              if (options2.unstable_strictMode === true) {
                isStrictMode = true;
              }
              if (options2.identifierPrefix !== void 0) {
                identifierPrefix = options2.identifierPrefix;
              }
              if (options2.onRecoverableError !== void 0) {
                onRecoverableError = options2.onRecoverableError;
              }
            }
            var root3 = createHydrationContainer(initialChildren, null, container, ConcurrentRoot, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError);
            markContainerAsRoot(root3.current, container);
            listenToAllSupportedEvents(container);
            if (mutableSources) {
              for (var i = 0; i < mutableSources.length; i++) {
                var mutableSource = mutableSources[i];
                registerMutableSourceForHydration(root3, mutableSource);
              }
            }
            return new ReactDOMHydrationRoot(root3);
          }
          function isValidContainer(node) {
            return !!(node && (node.nodeType === ELEMENT_NODE || node.nodeType === DOCUMENT_NODE || node.nodeType === DOCUMENT_FRAGMENT_NODE || !disableCommentsAsDOMContainers));
          }
          function isValidContainerLegacy(node) {
            return !!(node && (node.nodeType === ELEMENT_NODE || node.nodeType === DOCUMENT_NODE || node.nodeType === DOCUMENT_FRAGMENT_NODE || node.nodeType === COMMENT_NODE && node.nodeValue === " react-mount-point-unstable "));
          }
          function warnIfReactDOMContainerInDEV(container) {
            {
              if (container.nodeType === ELEMENT_NODE && container.tagName && container.tagName.toUpperCase() === "BODY") {
                error("createRoot(): Creating roots directly with document.body is discouraged, since its children are often manipulated by third-party scripts and browser extensions. This may lead to subtle reconciliation issues. Try using a container element created for your app.");
              }
              if (isContainerMarkedAsRoot(container)) {
                if (container._reactRootContainer) {
                  error("You are calling ReactDOMClient.createRoot() on a container that was previously passed to ReactDOM.render(). This is not supported.");
                } else {
                  error("You are calling ReactDOMClient.createRoot() on a container that has already been passed to createRoot() before. Instead, call root.render() on the existing root instead if you want to update it.");
                }
              }
            }
          }
          var ReactCurrentOwner$3 = ReactSharedInternals.ReactCurrentOwner;
          var topLevelUpdateWarnings;
          {
            topLevelUpdateWarnings = function(container) {
              if (container._reactRootContainer && container.nodeType !== COMMENT_NODE) {
                var hostInstance = findHostInstanceWithNoPortals(container._reactRootContainer.current);
                if (hostInstance) {
                  if (hostInstance.parentNode !== container) {
                    error("render(...): It looks like the React-rendered content of this container was removed without using React. This is not supported and will cause errors. Instead, call ReactDOM.unmountComponentAtNode to empty a container.");
                  }
                }
              }
              var isRootRenderedBySomeReact = !!container._reactRootContainer;
              var rootEl = getReactRootElementInContainer(container);
              var hasNonRootReactChild = !!(rootEl && getInstanceFromNode(rootEl));
              if (hasNonRootReactChild && !isRootRenderedBySomeReact) {
                error("render(...): Replacing React-rendered children with a new root component. If you intended to update the children of this node, you should instead have the existing children update their state and render the new components instead of calling ReactDOM.render.");
              }
              if (container.nodeType === ELEMENT_NODE && container.tagName && container.tagName.toUpperCase() === "BODY") {
                error("render(): Rendering components directly into document.body is discouraged, since its children are often manipulated by third-party scripts and browser extensions. This may lead to subtle reconciliation issues. Try rendering into a container element created for your app.");
              }
            };
          }
          function getReactRootElementInContainer(container) {
            if (!container) {
              return null;
            }
            if (container.nodeType === DOCUMENT_NODE) {
              return container.documentElement;
            } else {
              return container.firstChild;
            }
          }
          function noopOnRecoverableError() {
          }
          function legacyCreateRootFromDOMContainer(container, initialChildren, parentComponent, callback, isHydrationContainer) {
            if (isHydrationContainer) {
              if (typeof callback === "function") {
                var originalCallback = callback;
                callback = function() {
                  var instance = getPublicRootInstance(root3);
                  originalCallback.call(instance);
                };
              }
              var root3 = createHydrationContainer(
                initialChildren,
                callback,
                container,
                LegacyRoot,
                null,
                // hydrationCallbacks
                false,
                // isStrictMode
                false,
                // concurrentUpdatesByDefaultOverride,
                "",
                // identifierPrefix
                noopOnRecoverableError
              );
              container._reactRootContainer = root3;
              markContainerAsRoot(root3.current, container);
              var rootContainerElement = container.nodeType === COMMENT_NODE ? container.parentNode : container;
              listenToAllSupportedEvents(rootContainerElement);
              flushSync();
              return root3;
            } else {
              var rootSibling;
              while (rootSibling = container.lastChild) {
                container.removeChild(rootSibling);
              }
              if (typeof callback === "function") {
                var _originalCallback = callback;
                callback = function() {
                  var instance = getPublicRootInstance(_root);
                  _originalCallback.call(instance);
                };
              }
              var _root = createContainer(
                container,
                LegacyRoot,
                null,
                // hydrationCallbacks
                false,
                // isStrictMode
                false,
                // concurrentUpdatesByDefaultOverride,
                "",
                // identifierPrefix
                noopOnRecoverableError
              );
              container._reactRootContainer = _root;
              markContainerAsRoot(_root.current, container);
              var _rootContainerElement = container.nodeType === COMMENT_NODE ? container.parentNode : container;
              listenToAllSupportedEvents(_rootContainerElement);
              flushSync(function() {
                updateContainer(initialChildren, _root, parentComponent, callback);
              });
              return _root;
            }
          }
          function warnOnInvalidCallback$1(callback, callerName) {
            {
              if (callback !== null && typeof callback !== "function") {
                error("%s(...): Expected the last optional `callback` argument to be a function. Instead received: %s.", callerName, callback);
              }
            }
          }
          function legacyRenderSubtreeIntoContainer(parentComponent, children, container, forceHydrate, callback) {
            {
              topLevelUpdateWarnings(container);
              warnOnInvalidCallback$1(callback === void 0 ? null : callback, "render");
            }
            var maybeRoot = container._reactRootContainer;
            var root3;
            if (!maybeRoot) {
              root3 = legacyCreateRootFromDOMContainer(container, children, parentComponent, callback, forceHydrate);
            } else {
              root3 = maybeRoot;
              if (typeof callback === "function") {
                var originalCallback = callback;
                callback = function() {
                  var instance = getPublicRootInstance(root3);
                  originalCallback.call(instance);
                };
              }
              updateContainer(children, root3, parentComponent, callback);
            }
            return getPublicRootInstance(root3);
          }
          var didWarnAboutFindDOMNode = false;
          function findDOMNode(componentOrElement) {
            {
              if (!didWarnAboutFindDOMNode) {
                didWarnAboutFindDOMNode = true;
                error("findDOMNode is deprecated and will be removed in the next major release. Instead, add a ref directly to the element you want to reference. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-find-node");
              }
              var owner = ReactCurrentOwner$3.current;
              if (owner !== null && owner.stateNode !== null) {
                var warnedAboutRefsInRender = owner.stateNode._warnedAboutRefsInRender;
                if (!warnedAboutRefsInRender) {
                  error("%s is accessing findDOMNode inside its render(). render() should be a pure function of props and state. It should never access something that requires stale data from the previous render, such as refs. Move this logic to componentDidMount and componentDidUpdate instead.", getComponentNameFromType(owner.type) || "A component");
                }
                owner.stateNode._warnedAboutRefsInRender = true;
              }
            }
            if (componentOrElement == null) {
              return null;
            }
            if (componentOrElement.nodeType === ELEMENT_NODE) {
              return componentOrElement;
            }
            {
              return findHostInstanceWithWarning(componentOrElement, "findDOMNode");
            }
          }
          function hydrate(element, container, callback) {
            {
              error("ReactDOM.hydrate is no longer supported in React 18. Use hydrateRoot instead. Until you switch to the new API, your app will behave as if it's running React 17. Learn more: https://reactjs.org/link/switch-to-createroot");
            }
            if (!isValidContainerLegacy(container)) {
              throw new Error("Target container is not a DOM element.");
            }
            {
              var isModernRoot = isContainerMarkedAsRoot(container) && container._reactRootContainer === void 0;
              if (isModernRoot) {
                error("You are calling ReactDOM.hydrate() on a container that was previously passed to ReactDOMClient.createRoot(). This is not supported. Did you mean to call hydrateRoot(container, element)?");
              }
            }
            return legacyRenderSubtreeIntoContainer(null, element, container, true, callback);
          }
          function render(element, container, callback) {
            {
              error("ReactDOM.render is no longer supported in React 18. Use createRoot instead. Until you switch to the new API, your app will behave as if it's running React 17. Learn more: https://reactjs.org/link/switch-to-createroot");
            }
            if (!isValidContainerLegacy(container)) {
              throw new Error("Target container is not a DOM element.");
            }
            {
              var isModernRoot = isContainerMarkedAsRoot(container) && container._reactRootContainer === void 0;
              if (isModernRoot) {
                error("You are calling ReactDOM.render() on a container that was previously passed to ReactDOMClient.createRoot(). This is not supported. Did you mean to call root.render(element)?");
              }
            }
            return legacyRenderSubtreeIntoContainer(null, element, container, false, callback);
          }
          function unstable_renderSubtreeIntoContainer(parentComponent, element, containerNode, callback) {
            {
              error("ReactDOM.unstable_renderSubtreeIntoContainer() is no longer supported in React 18. Consider using a portal instead. Until you switch to the createRoot API, your app will behave as if it's running React 17. Learn more: https://reactjs.org/link/switch-to-createroot");
            }
            if (!isValidContainerLegacy(containerNode)) {
              throw new Error("Target container is not a DOM element.");
            }
            if (parentComponent == null || !has(parentComponent)) {
              throw new Error("parentComponent must be a valid React Component");
            }
            return legacyRenderSubtreeIntoContainer(parentComponent, element, containerNode, false, callback);
          }
          var didWarnAboutUnmountComponentAtNode = false;
          function unmountComponentAtNode(container) {
            {
              if (!didWarnAboutUnmountComponentAtNode) {
                didWarnAboutUnmountComponentAtNode = true;
                error("unmountComponentAtNode is deprecated and will be removed in the next major release. Switch to the createRoot API. Learn more: https://reactjs.org/link/switch-to-createroot");
              }
            }
            if (!isValidContainerLegacy(container)) {
              throw new Error("unmountComponentAtNode(...): Target container is not a DOM element.");
            }
            {
              var isModernRoot = isContainerMarkedAsRoot(container) && container._reactRootContainer === void 0;
              if (isModernRoot) {
                error("You are calling ReactDOM.unmountComponentAtNode() on a container that was previously passed to ReactDOMClient.createRoot(). This is not supported. Did you mean to call root.unmount()?");
              }
            }
            if (container._reactRootContainer) {
              {
                var rootEl = getReactRootElementInContainer(container);
                var renderedByDifferentReact = rootEl && !getInstanceFromNode(rootEl);
                if (renderedByDifferentReact) {
                  error("unmountComponentAtNode(): The node you're attempting to unmount was rendered by another copy of React.");
                }
              }
              flushSync(function() {
                legacyRenderSubtreeIntoContainer(null, null, container, false, function() {
                  container._reactRootContainer = null;
                  unmarkContainerAsRoot(container);
                });
              });
              return true;
            } else {
              {
                var _rootEl = getReactRootElementInContainer(container);
                var hasNonRootReactChild = !!(_rootEl && getInstanceFromNode(_rootEl));
                var isContainerReactRoot = container.nodeType === ELEMENT_NODE && isValidContainerLegacy(container.parentNode) && !!container.parentNode._reactRootContainer;
                if (hasNonRootReactChild) {
                  error("unmountComponentAtNode(): The node you're attempting to unmount was rendered by React and is not a top-level container. %s", isContainerReactRoot ? "You may have accidentally passed in a React root node instead of its container." : "Instead, have the parent component update its state and rerender in order to remove this component.");
                }
              }
              return false;
            }
          }
          setAttemptSynchronousHydration(attemptSynchronousHydration$1);
          setAttemptContinuousHydration(attemptContinuousHydration$1);
          setAttemptHydrationAtCurrentPriority(attemptHydrationAtCurrentPriority$1);
          setGetCurrentUpdatePriority(getCurrentUpdatePriority);
          setAttemptHydrationAtPriority(runWithPriority);
          {
            if (typeof Map !== "function" || // $FlowIssue Flow incorrectly thinks Map has no prototype
            Map.prototype == null || typeof Map.prototype.forEach !== "function" || typeof Set !== "function" || // $FlowIssue Flow incorrectly thinks Set has no prototype
            Set.prototype == null || typeof Set.prototype.clear !== "function" || typeof Set.prototype.forEach !== "function") {
              error("React depends on Map and Set built-in types. Make sure that you load a polyfill in older browsers. https://reactjs.org/link/react-polyfills");
            }
          }
          setRestoreImplementation(restoreControlledState$3);
          setBatchingImplementation(batchedUpdates$1, discreteUpdates, flushSync);
          function createPortal$1(children, container) {
            var key = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null;
            if (!isValidContainer(container)) {
              throw new Error("Target container is not a DOM element.");
            }
            return createPortal(children, container, null, key);
          }
          function renderSubtreeIntoContainer(parentComponent, element, containerNode, callback) {
            return unstable_renderSubtreeIntoContainer(parentComponent, element, containerNode, callback);
          }
          var Internals = {
            usingClientEntryPoint: false,
            // Keep in sync with ReactTestUtils.js.
            // This is an array for better minification.
            Events: [getInstanceFromNode, getNodeFromInstance, getFiberCurrentPropsFromNode, enqueueStateRestore, restoreStateIfNeeded, batchedUpdates$1]
          };
          function createRoot$1(container, options2) {
            {
              if (!Internals.usingClientEntryPoint && true) {
                error('You are importing createRoot from "react-dom" which is not supported. You should instead import it from "react-dom/client".');
              }
            }
            return createRoot2(container, options2);
          }
          function hydrateRoot$1(container, initialChildren, options2) {
            {
              if (!Internals.usingClientEntryPoint && true) {
                error('You are importing hydrateRoot from "react-dom" which is not supported. You should instead import it from "react-dom/client".');
              }
            }
            return hydrateRoot(container, initialChildren, options2);
          }
          function flushSync$1(fn) {
            {
              if (isAlreadyRendering()) {
                error("flushSync was called from inside a lifecycle method. React cannot flush when React is already rendering. Consider moving this call to a scheduler task or micro task.");
              }
            }
            return flushSync(fn);
          }
          var foundDevTools = injectIntoDevTools({
            findFiberByHostInstance: getClosestInstanceFromNode,
            bundleType: 1,
            version: ReactVersion,
            rendererPackageName: "react-dom"
          });
          {
            if (!foundDevTools && canUseDOM && window.top === window.self) {
              if (navigator.userAgent.indexOf("Chrome") > -1 && navigator.userAgent.indexOf("Edge") === -1 || navigator.userAgent.indexOf("Firefox") > -1) {
                var protocol = window.location.protocol;
                if (/^(https?|file):$/.test(protocol)) {
                  console.info("%cDownload the React DevTools for a better development experience: https://reactjs.org/link/react-devtools" + (protocol === "file:" ? "\nYou might need to use a local HTTP server (instead of file://): https://reactjs.org/link/react-devtools-faq" : ""), "font-weight:bold");
                }
              }
            }
          }
          exports.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = Internals;
          exports.createPortal = createPortal$1;
          exports.createRoot = createRoot$1;
          exports.findDOMNode = findDOMNode;
          exports.flushSync = flushSync$1;
          exports.hydrate = hydrate;
          exports.hydrateRoot = hydrateRoot$1;
          exports.render = render;
          exports.unmountComponentAtNode = unmountComponentAtNode;
          exports.unstable_batchedUpdates = batchedUpdates$1;
          exports.unstable_renderSubtreeIntoContainer = renderSubtreeIntoContainer;
          exports.version = ReactVersion;
          if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop === "function") {
            __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(new Error());
          }
        })();
      }
    }
  });

  // node_modules/react-dom/index.js
  var require_react_dom = __commonJS({
    "node_modules/react-dom/index.js"(exports, module) {
      "use strict";
      if (false) {
        checkDCE();
        module.exports = null;
      } else {
        module.exports = require_react_dom_development();
      }
    }
  });

  // node_modules/react-dom/client.js
  var require_client = __commonJS({
    "node_modules/react-dom/client.js"(exports) {
      "use strict";
      var m = require_react_dom();
      if (false) {
        exports.createRoot = m.createRoot;
        exports.hydrateRoot = m.hydrateRoot;
      } else {
        i = m.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
        exports.createRoot = function(c, o) {
          i.usingClientEntryPoint = true;
          try {
            return m.createRoot(c, o);
          } finally {
            i.usingClientEntryPoint = false;
          }
        };
        exports.hydrateRoot = function(c, h, o) {
          i.usingClientEntryPoint = true;
          try {
            return m.hydrateRoot(c, h, o);
          } finally {
            i.usingClientEntryPoint = false;
          }
        };
      }
      var i;
    }
  });

  // node_modules/react/cjs/react-jsx-runtime.development.js
  var require_react_jsx_runtime_development = __commonJS({
    "node_modules/react/cjs/react-jsx-runtime.development.js"(exports) {
      "use strict";
      if (true) {
        (function() {
          "use strict";
          var React3 = require_react();
          var REACT_ELEMENT_TYPE = Symbol.for("react.element");
          var REACT_PORTAL_TYPE = Symbol.for("react.portal");
          var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
          var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
          var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
          var REACT_PROVIDER_TYPE = Symbol.for("react.provider");
          var REACT_CONTEXT_TYPE = Symbol.for("react.context");
          var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
          var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
          var REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list");
          var REACT_MEMO_TYPE = Symbol.for("react.memo");
          var REACT_LAZY_TYPE = Symbol.for("react.lazy");
          var REACT_OFFSCREEN_TYPE = Symbol.for("react.offscreen");
          var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
          var FAUX_ITERATOR_SYMBOL = "@@iterator";
          function getIteratorFn(maybeIterable) {
            if (maybeIterable === null || typeof maybeIterable !== "object") {
              return null;
            }
            var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
            if (typeof maybeIterator === "function") {
              return maybeIterator;
            }
            return null;
          }
          var ReactSharedInternals = React3.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
          function error(format) {
            {
              {
                for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
                  args[_key2 - 1] = arguments[_key2];
                }
                printWarning("error", format, args);
              }
            }
          }
          function printWarning(level, format, args) {
            {
              var ReactDebugCurrentFrame2 = ReactSharedInternals.ReactDebugCurrentFrame;
              var stack = ReactDebugCurrentFrame2.getStackAddendum();
              if (stack !== "") {
                format += "%s";
                args = args.concat([stack]);
              }
              var argsWithFormat = args.map(function(item) {
                return String(item);
              });
              argsWithFormat.unshift("Warning: " + format);
              Function.prototype.apply.call(console[level], console, argsWithFormat);
            }
          }
          var enableScopeAPI = false;
          var enableCacheElement = false;
          var enableTransitionTracing = false;
          var enableLegacyHidden = false;
          var enableDebugTracing = false;
          var REACT_MODULE_REFERENCE;
          {
            REACT_MODULE_REFERENCE = Symbol.for("react.module.reference");
          }
          function isValidElementType(type) {
            if (typeof type === "string" || typeof type === "function") {
              return true;
            }
            if (type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || enableDebugTracing || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || enableLegacyHidden || type === REACT_OFFSCREEN_TYPE || enableScopeAPI || enableCacheElement || enableTransitionTracing) {
              return true;
            }
            if (typeof type === "object" && type !== null) {
              if (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || // This needs to include all possible module reference object
              // types supported by any Flight configuration anywhere since
              // we don't know which Flight build this will end up being used
              // with.
              type.$$typeof === REACT_MODULE_REFERENCE || type.getModuleId !== void 0) {
                return true;
              }
            }
            return false;
          }
          function getWrappedName(outerType, innerType, wrapperName) {
            var displayName = outerType.displayName;
            if (displayName) {
              return displayName;
            }
            var functionName = innerType.displayName || innerType.name || "";
            return functionName !== "" ? wrapperName + "(" + functionName + ")" : wrapperName;
          }
          function getContextName(type) {
            return type.displayName || "Context";
          }
          function getComponentNameFromType(type) {
            if (type == null) {
              return null;
            }
            {
              if (typeof type.tag === "number") {
                error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue.");
              }
            }
            if (typeof type === "function") {
              return type.displayName || type.name || null;
            }
            if (typeof type === "string") {
              return type;
            }
            switch (type) {
              case REACT_FRAGMENT_TYPE:
                return "Fragment";
              case REACT_PORTAL_TYPE:
                return "Portal";
              case REACT_PROFILER_TYPE:
                return "Profiler";
              case REACT_STRICT_MODE_TYPE:
                return "StrictMode";
              case REACT_SUSPENSE_TYPE:
                return "Suspense";
              case REACT_SUSPENSE_LIST_TYPE:
                return "SuspenseList";
            }
            if (typeof type === "object") {
              switch (type.$$typeof) {
                case REACT_CONTEXT_TYPE:
                  var context = type;
                  return getContextName(context) + ".Consumer";
                case REACT_PROVIDER_TYPE:
                  var provider = type;
                  return getContextName(provider._context) + ".Provider";
                case REACT_FORWARD_REF_TYPE:
                  return getWrappedName(type, type.render, "ForwardRef");
                case REACT_MEMO_TYPE:
                  var outerName = type.displayName || null;
                  if (outerName !== null) {
                    return outerName;
                  }
                  return getComponentNameFromType(type.type) || "Memo";
                case REACT_LAZY_TYPE: {
                  var lazyComponent = type;
                  var payload = lazyComponent._payload;
                  var init = lazyComponent._init;
                  try {
                    return getComponentNameFromType(init(payload));
                  } catch (x) {
                    return null;
                  }
                }
              }
            }
            return null;
          }
          var assign = Object.assign;
          var disabledDepth = 0;
          var prevLog;
          var prevInfo;
          var prevWarn;
          var prevError;
          var prevGroup;
          var prevGroupCollapsed;
          var prevGroupEnd;
          function disabledLog() {
          }
          disabledLog.__reactDisabledLog = true;
          function disableLogs() {
            {
              if (disabledDepth === 0) {
                prevLog = console.log;
                prevInfo = console.info;
                prevWarn = console.warn;
                prevError = console.error;
                prevGroup = console.group;
                prevGroupCollapsed = console.groupCollapsed;
                prevGroupEnd = console.groupEnd;
                var props = {
                  configurable: true,
                  enumerable: true,
                  value: disabledLog,
                  writable: true
                };
                Object.defineProperties(console, {
                  info: props,
                  log: props,
                  warn: props,
                  error: props,
                  group: props,
                  groupCollapsed: props,
                  groupEnd: props
                });
              }
              disabledDepth++;
            }
          }
          function reenableLogs() {
            {
              disabledDepth--;
              if (disabledDepth === 0) {
                var props = {
                  configurable: true,
                  enumerable: true,
                  writable: true
                };
                Object.defineProperties(console, {
                  log: assign({}, props, {
                    value: prevLog
                  }),
                  info: assign({}, props, {
                    value: prevInfo
                  }),
                  warn: assign({}, props, {
                    value: prevWarn
                  }),
                  error: assign({}, props, {
                    value: prevError
                  }),
                  group: assign({}, props, {
                    value: prevGroup
                  }),
                  groupCollapsed: assign({}, props, {
                    value: prevGroupCollapsed
                  }),
                  groupEnd: assign({}, props, {
                    value: prevGroupEnd
                  })
                });
              }
              if (disabledDepth < 0) {
                error("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
              }
            }
          }
          var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
          var prefix;
          function describeBuiltInComponentFrame(name, source, ownerFn) {
            {
              if (prefix === void 0) {
                try {
                  throw Error();
                } catch (x) {
                  var match = x.stack.trim().match(/\n( *(at )?)/);
                  prefix = match && match[1] || "";
                }
              }
              return "\n" + prefix + name;
            }
          }
          var reentry = false;
          var componentFrameCache;
          {
            var PossiblyWeakMap = typeof WeakMap === "function" ? WeakMap : Map;
            componentFrameCache = new PossiblyWeakMap();
          }
          function describeNativeComponentFrame(fn, construct) {
            if (!fn || reentry) {
              return "";
            }
            {
              var frame = componentFrameCache.get(fn);
              if (frame !== void 0) {
                return frame;
              }
            }
            var control;
            reentry = true;
            var previousPrepareStackTrace = Error.prepareStackTrace;
            Error.prepareStackTrace = void 0;
            var previousDispatcher;
            {
              previousDispatcher = ReactCurrentDispatcher.current;
              ReactCurrentDispatcher.current = null;
              disableLogs();
            }
            try {
              if (construct) {
                var Fake = function() {
                  throw Error();
                };
                Object.defineProperty(Fake.prototype, "props", {
                  set: function() {
                    throw Error();
                  }
                });
                if (typeof Reflect === "object" && Reflect.construct) {
                  try {
                    Reflect.construct(Fake, []);
                  } catch (x) {
                    control = x;
                  }
                  Reflect.construct(fn, [], Fake);
                } else {
                  try {
                    Fake.call();
                  } catch (x) {
                    control = x;
                  }
                  fn.call(Fake.prototype);
                }
              } else {
                try {
                  throw Error();
                } catch (x) {
                  control = x;
                }
                fn();
              }
            } catch (sample) {
              if (sample && control && typeof sample.stack === "string") {
                var sampleLines = sample.stack.split("\n");
                var controlLines = control.stack.split("\n");
                var s = sampleLines.length - 1;
                var c = controlLines.length - 1;
                while (s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c]) {
                  c--;
                }
                for (; s >= 1 && c >= 0; s--, c--) {
                  if (sampleLines[s] !== controlLines[c]) {
                    if (s !== 1 || c !== 1) {
                      do {
                        s--;
                        c--;
                        if (c < 0 || sampleLines[s] !== controlLines[c]) {
                          var _frame = "\n" + sampleLines[s].replace(" at new ", " at ");
                          if (fn.displayName && _frame.includes("<anonymous>")) {
                            _frame = _frame.replace("<anonymous>", fn.displayName);
                          }
                          {
                            if (typeof fn === "function") {
                              componentFrameCache.set(fn, _frame);
                            }
                          }
                          return _frame;
                        }
                      } while (s >= 1 && c >= 0);
                    }
                    break;
                  }
                }
              }
            } finally {
              reentry = false;
              {
                ReactCurrentDispatcher.current = previousDispatcher;
                reenableLogs();
              }
              Error.prepareStackTrace = previousPrepareStackTrace;
            }
            var name = fn ? fn.displayName || fn.name : "";
            var syntheticFrame = name ? describeBuiltInComponentFrame(name) : "";
            {
              if (typeof fn === "function") {
                componentFrameCache.set(fn, syntheticFrame);
              }
            }
            return syntheticFrame;
          }
          function describeFunctionComponentFrame(fn, source, ownerFn) {
            {
              return describeNativeComponentFrame(fn, false);
            }
          }
          function shouldConstruct(Component) {
            var prototype = Component.prototype;
            return !!(prototype && prototype.isReactComponent);
          }
          function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {
            if (type == null) {
              return "";
            }
            if (typeof type === "function") {
              {
                return describeNativeComponentFrame(type, shouldConstruct(type));
              }
            }
            if (typeof type === "string") {
              return describeBuiltInComponentFrame(type);
            }
            switch (type) {
              case REACT_SUSPENSE_TYPE:
                return describeBuiltInComponentFrame("Suspense");
              case REACT_SUSPENSE_LIST_TYPE:
                return describeBuiltInComponentFrame("SuspenseList");
            }
            if (typeof type === "object") {
              switch (type.$$typeof) {
                case REACT_FORWARD_REF_TYPE:
                  return describeFunctionComponentFrame(type.render);
                case REACT_MEMO_TYPE:
                  return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);
                case REACT_LAZY_TYPE: {
                  var lazyComponent = type;
                  var payload = lazyComponent._payload;
                  var init = lazyComponent._init;
                  try {
                    return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
                  } catch (x) {
                  }
                }
              }
            }
            return "";
          }
          var hasOwnProperty = Object.prototype.hasOwnProperty;
          var loggedTypeFailures = {};
          var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
          function setCurrentlyValidatingElement(element) {
            {
              if (element) {
                var owner = element._owner;
                var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
                ReactDebugCurrentFrame.setExtraStackFrame(stack);
              } else {
                ReactDebugCurrentFrame.setExtraStackFrame(null);
              }
            }
          }
          function checkPropTypes(typeSpecs, values, location, componentName, element) {
            {
              var has = Function.call.bind(hasOwnProperty);
              for (var typeSpecName in typeSpecs) {
                if (has(typeSpecs, typeSpecName)) {
                  var error$1 = void 0;
                  try {
                    if (typeof typeSpecs[typeSpecName] !== "function") {
                      var err = Error((componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
                      err.name = "Invariant Violation";
                      throw err;
                    }
                    error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
                  } catch (ex) {
                    error$1 = ex;
                  }
                  if (error$1 && !(error$1 instanceof Error)) {
                    setCurrentlyValidatingElement(element);
                    error("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", componentName || "React class", location, typeSpecName, typeof error$1);
                    setCurrentlyValidatingElement(null);
                  }
                  if (error$1 instanceof Error && !(error$1.message in loggedTypeFailures)) {
                    loggedTypeFailures[error$1.message] = true;
                    setCurrentlyValidatingElement(element);
                    error("Failed %s type: %s", location, error$1.message);
                    setCurrentlyValidatingElement(null);
                  }
                }
              }
            }
          }
          var isArrayImpl = Array.isArray;
          function isArray(a) {
            return isArrayImpl(a);
          }
          function typeName(value) {
            {
              var hasToStringTag = typeof Symbol === "function" && Symbol.toStringTag;
              var type = hasToStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
              return type;
            }
          }
          function willCoercionThrow(value) {
            {
              try {
                testStringCoercion(value);
                return false;
              } catch (e) {
                return true;
              }
            }
          }
          function testStringCoercion(value) {
            return "" + value;
          }
          function checkKeyStringCoercion(value) {
            {
              if (willCoercionThrow(value)) {
                error("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
                return testStringCoercion(value);
              }
            }
          }
          var ReactCurrentOwner = ReactSharedInternals.ReactCurrentOwner;
          var RESERVED_PROPS = {
            key: true,
            ref: true,
            __self: true,
            __source: true
          };
          var specialPropKeyWarningShown;
          var specialPropRefWarningShown;
          var didWarnAboutStringRefs;
          {
            didWarnAboutStringRefs = {};
          }
          function hasValidRef(config) {
            {
              if (hasOwnProperty.call(config, "ref")) {
                var getter = Object.getOwnPropertyDescriptor(config, "ref").get;
                if (getter && getter.isReactWarning) {
                  return false;
                }
              }
            }
            return config.ref !== void 0;
          }
          function hasValidKey(config) {
            {
              if (hasOwnProperty.call(config, "key")) {
                var getter = Object.getOwnPropertyDescriptor(config, "key").get;
                if (getter && getter.isReactWarning) {
                  return false;
                }
              }
            }
            return config.key !== void 0;
          }
          function warnIfStringRefCannotBeAutoConverted(config, self) {
            {
              if (typeof config.ref === "string" && ReactCurrentOwner.current && self && ReactCurrentOwner.current.stateNode !== self) {
                var componentName = getComponentNameFromType(ReactCurrentOwner.current.type);
                if (!didWarnAboutStringRefs[componentName]) {
                  error('Component "%s" contains the string ref "%s". Support for string refs will be removed in a future major release. This case cannot be automatically converted to an arrow function. We ask you to manually fix this case by using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref', getComponentNameFromType(ReactCurrentOwner.current.type), config.ref);
                  didWarnAboutStringRefs[componentName] = true;
                }
              }
            }
          }
          function defineKeyPropWarningGetter(props, displayName) {
            {
              var warnAboutAccessingKey = function() {
                if (!specialPropKeyWarningShown) {
                  specialPropKeyWarningShown = true;
                  error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
                }
              };
              warnAboutAccessingKey.isReactWarning = true;
              Object.defineProperty(props, "key", {
                get: warnAboutAccessingKey,
                configurable: true
              });
            }
          }
          function defineRefPropWarningGetter(props, displayName) {
            {
              var warnAboutAccessingRef = function() {
                if (!specialPropRefWarningShown) {
                  specialPropRefWarningShown = true;
                  error("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
                }
              };
              warnAboutAccessingRef.isReactWarning = true;
              Object.defineProperty(props, "ref", {
                get: warnAboutAccessingRef,
                configurable: true
              });
            }
          }
          var ReactElement = function(type, key, ref, self, source, owner, props) {
            var element = {
              // This tag allows us to uniquely identify this as a React Element
              $$typeof: REACT_ELEMENT_TYPE,
              // Built-in properties that belong on the element
              type,
              key,
              ref,
              props,
              // Record the component responsible for creating this element.
              _owner: owner
            };
            {
              element._store = {};
              Object.defineProperty(element._store, "validated", {
                configurable: false,
                enumerable: false,
                writable: true,
                value: false
              });
              Object.defineProperty(element, "_self", {
                configurable: false,
                enumerable: false,
                writable: false,
                value: self
              });
              Object.defineProperty(element, "_source", {
                configurable: false,
                enumerable: false,
                writable: false,
                value: source
              });
              if (Object.freeze) {
                Object.freeze(element.props);
                Object.freeze(element);
              }
            }
            return element;
          };
          function jsxDEV(type, config, maybeKey, source, self) {
            {
              var propName;
              var props = {};
              var key = null;
              var ref = null;
              if (maybeKey !== void 0) {
                {
                  checkKeyStringCoercion(maybeKey);
                }
                key = "" + maybeKey;
              }
              if (hasValidKey(config)) {
                {
                  checkKeyStringCoercion(config.key);
                }
                key = "" + config.key;
              }
              if (hasValidRef(config)) {
                ref = config.ref;
                warnIfStringRefCannotBeAutoConverted(config, self);
              }
              for (propName in config) {
                if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
                  props[propName] = config[propName];
                }
              }
              if (type && type.defaultProps) {
                var defaultProps = type.defaultProps;
                for (propName in defaultProps) {
                  if (props[propName] === void 0) {
                    props[propName] = defaultProps[propName];
                  }
                }
              }
              if (key || ref) {
                var displayName = typeof type === "function" ? type.displayName || type.name || "Unknown" : type;
                if (key) {
                  defineKeyPropWarningGetter(props, displayName);
                }
                if (ref) {
                  defineRefPropWarningGetter(props, displayName);
                }
              }
              return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
            }
          }
          var ReactCurrentOwner$1 = ReactSharedInternals.ReactCurrentOwner;
          var ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
          function setCurrentlyValidatingElement$1(element) {
            {
              if (element) {
                var owner = element._owner;
                var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
                ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
              } else {
                ReactDebugCurrentFrame$1.setExtraStackFrame(null);
              }
            }
          }
          var propTypesMisspellWarningShown;
          {
            propTypesMisspellWarningShown = false;
          }
          function isValidElement(object) {
            {
              return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
            }
          }
          function getDeclarationErrorAddendum() {
            {
              if (ReactCurrentOwner$1.current) {
                var name = getComponentNameFromType(ReactCurrentOwner$1.current.type);
                if (name) {
                  return "\n\nCheck the render method of `" + name + "`.";
                }
              }
              return "";
            }
          }
          function getSourceInfoErrorAddendum(source) {
            {
              if (source !== void 0) {
                var fileName = source.fileName.replace(/^.*[\\\/]/, "");
                var lineNumber = source.lineNumber;
                return "\n\nCheck your code at " + fileName + ":" + lineNumber + ".";
              }
              return "";
            }
          }
          var ownerHasKeyUseWarning = {};
          function getCurrentComponentErrorInfo(parentType) {
            {
              var info = getDeclarationErrorAddendum();
              if (!info) {
                var parentName = typeof parentType === "string" ? parentType : parentType.displayName || parentType.name;
                if (parentName) {
                  info = "\n\nCheck the top-level render call using <" + parentName + ">.";
                }
              }
              return info;
            }
          }
          function validateExplicitKey(element, parentType) {
            {
              if (!element._store || element._store.validated || element.key != null) {
                return;
              }
              element._store.validated = true;
              var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);
              if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
                return;
              }
              ownerHasKeyUseWarning[currentComponentErrorInfo] = true;
              var childOwner = "";
              if (element && element._owner && element._owner !== ReactCurrentOwner$1.current) {
                childOwner = " It was passed a child from " + getComponentNameFromType(element._owner.type) + ".";
              }
              setCurrentlyValidatingElement$1(element);
              error('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', currentComponentErrorInfo, childOwner);
              setCurrentlyValidatingElement$1(null);
            }
          }
          function validateChildKeys(node, parentType) {
            {
              if (typeof node !== "object") {
                return;
              }
              if (isArray(node)) {
                for (var i = 0; i < node.length; i++) {
                  var child = node[i];
                  if (isValidElement(child)) {
                    validateExplicitKey(child, parentType);
                  }
                }
              } else if (isValidElement(node)) {
                if (node._store) {
                  node._store.validated = true;
                }
              } else if (node) {
                var iteratorFn = getIteratorFn(node);
                if (typeof iteratorFn === "function") {
                  if (iteratorFn !== node.entries) {
                    var iterator = iteratorFn.call(node);
                    var step;
                    while (!(step = iterator.next()).done) {
                      if (isValidElement(step.value)) {
                        validateExplicitKey(step.value, parentType);
                      }
                    }
                  }
                }
              }
            }
          }
          function validatePropTypes(element) {
            {
              var type = element.type;
              if (type === null || type === void 0 || typeof type === "string") {
                return;
              }
              var propTypes;
              if (typeof type === "function") {
                propTypes = type.propTypes;
              } else if (typeof type === "object" && (type.$$typeof === REACT_FORWARD_REF_TYPE || // Note: Memo only checks outer props here.
              // Inner props are checked in the reconciler.
              type.$$typeof === REACT_MEMO_TYPE)) {
                propTypes = type.propTypes;
              } else {
                return;
              }
              if (propTypes) {
                var name = getComponentNameFromType(type);
                checkPropTypes(propTypes, element.props, "prop", name, element);
              } else if (type.PropTypes !== void 0 && !propTypesMisspellWarningShown) {
                propTypesMisspellWarningShown = true;
                var _name = getComponentNameFromType(type);
                error("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", _name || "Unknown");
              }
              if (typeof type.getDefaultProps === "function" && !type.getDefaultProps.isReactClassApproved) {
                error("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
              }
            }
          }
          function validateFragmentProps(fragment) {
            {
              var keys = Object.keys(fragment.props);
              for (var i = 0; i < keys.length; i++) {
                var key = keys[i];
                if (key !== "children" && key !== "key") {
                  setCurrentlyValidatingElement$1(fragment);
                  error("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", key);
                  setCurrentlyValidatingElement$1(null);
                  break;
                }
              }
              if (fragment.ref !== null) {
                setCurrentlyValidatingElement$1(fragment);
                error("Invalid attribute `ref` supplied to `React.Fragment`.");
                setCurrentlyValidatingElement$1(null);
              }
            }
          }
          var didWarnAboutKeySpread = {};
          function jsxWithValidation(type, props, key, isStaticChildren, source, self) {
            {
              var validType = isValidElementType(type);
              if (!validType) {
                var info = "";
                if (type === void 0 || typeof type === "object" && type !== null && Object.keys(type).length === 0) {
                  info += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.";
                }
                var sourceInfo = getSourceInfoErrorAddendum(source);
                if (sourceInfo) {
                  info += sourceInfo;
                } else {
                  info += getDeclarationErrorAddendum();
                }
                var typeString;
                if (type === null) {
                  typeString = "null";
                } else if (isArray(type)) {
                  typeString = "array";
                } else if (type !== void 0 && type.$$typeof === REACT_ELEMENT_TYPE) {
                  typeString = "<" + (getComponentNameFromType(type.type) || "Unknown") + " />";
                  info = " Did you accidentally export a JSX literal instead of a component?";
                } else {
                  typeString = typeof type;
                }
                error("React.jsx: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", typeString, info);
              }
              var element = jsxDEV(type, props, key, source, self);
              if (element == null) {
                return element;
              }
              if (validType) {
                var children = props.children;
                if (children !== void 0) {
                  if (isStaticChildren) {
                    if (isArray(children)) {
                      for (var i = 0; i < children.length; i++) {
                        validateChildKeys(children[i], type);
                      }
                      if (Object.freeze) {
                        Object.freeze(children);
                      }
                    } else {
                      error("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
                    }
                  } else {
                    validateChildKeys(children, type);
                  }
                }
              }
              {
                if (hasOwnProperty.call(props, "key")) {
                  var componentName = getComponentNameFromType(type);
                  var keys = Object.keys(props).filter(function(k) {
                    return k !== "key";
                  });
                  var beforeExample = keys.length > 0 ? "{key: someKey, " + keys.join(": ..., ") + ": ...}" : "{key: someKey}";
                  if (!didWarnAboutKeySpread[componentName + beforeExample]) {
                    var afterExample = keys.length > 0 ? "{" + keys.join(": ..., ") + ": ...}" : "{}";
                    error('A props object containing a "key" prop is being spread into JSX:\n  let props = %s;\n  <%s {...props} />\nReact keys must be passed directly to JSX without using spread:\n  let props = %s;\n  <%s key={someKey} {...props} />', beforeExample, componentName, afterExample, componentName);
                    didWarnAboutKeySpread[componentName + beforeExample] = true;
                  }
                }
              }
              if (type === REACT_FRAGMENT_TYPE) {
                validateFragmentProps(element);
              } else {
                validatePropTypes(element);
              }
              return element;
            }
          }
          function jsxWithValidationStatic(type, props, key) {
            {
              return jsxWithValidation(type, props, key, true);
            }
          }
          function jsxWithValidationDynamic(type, props, key) {
            {
              return jsxWithValidation(type, props, key, false);
            }
          }
          var jsx3 = jsxWithValidationDynamic;
          var jsxs2 = jsxWithValidationStatic;
          exports.Fragment = REACT_FRAGMENT_TYPE;
          exports.jsx = jsx3;
          exports.jsxs = jsxs2;
        })();
      }
    }
  });

  // node_modules/react/jsx-runtime.js
  var require_jsx_runtime = __commonJS({
    "node_modules/react/jsx-runtime.js"(exports, module) {
      "use strict";
      if (false) {
        module.exports = null;
      } else {
        module.exports = require_react_jsx_runtime_development();
      }
    }
  });

  // src/main.jsx
  var import_react2 = __toESM(require_react());
  var import_client = __toESM(require_client());

  // src/Engine.tsx
  var import_react = __toESM(require_react());
  var import_jsx_runtime = __toESM(require_jsx_runtime());
  var MAP_STATE_COORDS = {
    AL: [1.81, 2.79],
    AR: [0.47, 2.01],
    AZ: [-3.52, 2.17],
    CA: [-4.75, 0.62],
    CO: [-1.9, -0.93],
    CT: [5.04, -1.55],
    DE: [4.75, -0.47],
    FL: [3.32, 4.65],
    GA: [2.76, 2.17],
    IA: [0.28, -0.93],
    ID: [-3.8, -2.48],
    IL: [1.23, -0.47],
    IN: [1.99, -0.47],
    KS: [-0.95, 0.62],
    LA: [0.19, 3.57],
    MA: [5.22, -2.64],
    MD: [4.18, -0.16],
    MI: [2.19, -2.48],
    MN: [0.28, -3.57],
    MO: [0.28, 0.62],
    NC: [3.99, 1.08],
    ND: [-0.95, -4.03],
    NJ: [4.46, -0.93],
    NV: [-3.8, -0.93],
    NY: [4.09, -2.79],
    OH: [2.85, -0.78],
    OK: [-0.95, 2.17],
    OR: [-4.75, -2.48],
    PA: [3.8, -1.24],
    SC: [3.61, 2.01],
    TN: [2.09, 1.08],
    TX: [-1.42, 3.72],
    UT: [-2.85, -0.93],
    VA: [3.71, 0.31],
    WA: [-4.75, -4.03],
    WI: [0.95, -2.48],
    WV: [3.14, 0],
    BC: [-4.75, -5.7],
    AB: [-2.85, -5.7],
    ON: [2.19, -5.4]
  };
  var MAP_DIMS = ["engagement", "margin", "staffing", "community"];
  function mapClamp(v) {
    return Math.max(0.02, Math.min(1, v));
  }
  function buildMapNodes(states, railData, leads) {
    const conflictedCenters = new Set((railData && railData.conflicts || []).flatMap((c) => {
      const recs = railData.recommendations || [];
      const ra = recs.find((r) => r.id === c.a), rb = recs.find((r) => r.id === c.b);
      return [ra, rb].filter(Boolean).flatMap((r) => r.targetIds.filter((t) => typeof t === "string" && t.length > 2));
    }));
    const leadsByRegion = {};
    (leads || []).filter((l) => l.stage0 < 5).forEach((l) => {
      leadsByRegion[l.region] = (leadsByRegion[l.region] || 0) + 1;
    });
    return Object.keys(states).filter((st) => MAP_STATE_COORDS[st]).map((st) => {
      const cs = states[st];
      const n = cs.length;
      const avgHealth = cs.reduce((a, c) => a + c.health, 0) / n / 100;
      const avgConv = cs.reduce((a, c) => a + c.conv, 0) / n;
      const avgChem = cs.reduce((a, c) => a + c.chem, 0) / n;
      const avgRet = cs.reduce((a, c) => a + c.ret, 0) / n;
      const avgEb = cs.reduce((a, c) => a + c.eb, 0) / n;
      const marginDim = mapClamp((avgEb + 4) / 18);
      const conditions = cs.map((c) => conditionOf(c).label);
      const worst = conditions.includes("at-risk") ? "at-risk" : conditions.includes("watch") ? "watch" : "thriving";
      const hex = worst === "at-risk" ? 14693685 : worst === "watch" ? 14263584 : 3129183;
      const conflicted = cs.some((c) => conflictedCenters.has(c.name));
      const centerNames = new Set(cs.map((c) => c.name));
      const verifiedN = cs.filter((c) => c.verified).length;
      return {
        id: st,
        label: st,
        pos: MAP_STATE_COORDS[st],
        state: worst,
        hex,
        n,
        centerNames,
        verifiedN,
        vals: { engagement: mapClamp(avgConv), margin: marginDim, staffing: mapClamp(avgChem), community: mapClamp(avgRet) },
        conflicted,
        leadCount: leadsByRegion[st] || 0,
        repCenter: [...cs].sort((a, b) => a.health - b.health)[0]
      };
    });
  }
  function mtVal(c, d) {
    return c.vals[d];
  }
  function mtHealth(c) {
    return MAP_DIMS.reduce((s, d) => s + mtVal(c, d), 0) / MAP_DIMS.length;
  }
  function Map3D({ mapNodes, mEdges, clusters, approvedPosture, railData, selectedState, onStateClick, scenario, centers, states }) {
    return null;
  }
  var MAP_PROPAGATION_GAP_MIN = 0.12;
  function buildPropagationEdges(mapNodes) {
    const sorted = [...mapNodes].sort((a, b) => mtHealth(b) - mtHealth(a));
    if (sorted.length < 2) return [];
    const k = Math.min(3, Math.floor(sorted.length / 2));
    const strong = sorted.slice(0, k), weak = sorted.slice(-k);
    const edges = [];
    const seen = /* @__PURE__ */ new Set();
    weak.forEach((w) => {
      let best = null, bestDist = Infinity;
      strong.forEach((s) => {
        const gap = mtHealth(s) - mtHealth(w);
        if (gap < MAP_PROPAGATION_GAP_MIN) return;
        const d = Math.hypot(s.pos[0] - w.pos[0], s.pos[1] - w.pos[1]);
        if (d < bestDist) {
          bestDist = d;
          best = s;
        }
      });
      if (!best) return;
      const key = [best.id, w.id].sort().join("-");
      if (seen.has(key)) return;
      seen.add(key);
      edges.push({ a: best, b: w, gap: +(mtHealth(best) - mtHealth(w)).toFixed(2) });
    });
    return edges;
  }
  function CommandTableTab({ centers, states, opt, setOpt, railData, leads, jumpTo }) {
    const mountRef = (0, import_react.useRef)(null);
    const [sel, setSel] = (0, import_react.useState)(null);
    const [sel2, setSel2] = (0, import_react.useState)(null);
    const [viewMode, setViewMode] = (0, import_react.useState)("3d");
    const currentWeek = opt && typeof opt.week === "number" ? opt.week : 0;
    const [week, setWeek] = (0, import_react.useState)(currentWeek);
    (0, import_react.useEffect)(() => {
      setWeek(currentWeek);
    }, [currentWeek]);
    const weekRef = (0, import_react.useRef)(currentWeek);
    const selRef = (0, import_react.useRef)(null);
    const apiRef = (0, import_react.useRef)({});
    const [webglFailed, setWebglFailed] = (0, import_react.useState)(false);
    const mapNodes = (0, import_react.useMemo)(() => buildMapNodes(states, railData || { recommendations: [], conflicts: [] }, leads), [states, railData, leads]);
    const nodesRef = (0, import_react.useRef)(mapNodes);
    nodesRef.current = mapNodes;
    const edges = (0, import_react.useMemo)(() => buildPropagationEdges(mapNodes), [mapNodes]);
    const edgesRef = (0, import_react.useRef)(edges);
    edgesRef.current = edges;
    (0, import_react.useEffect)(() => {
      weekRef.current = week;
      apiRef.current.w && apiRef.current.w(week);
    }, [week]);
    const DEFAULT_ORBIT = { theta: 0.9, phi: 1.02, radius: 24 };
    (0, import_react.useEffect)(() => {
      setWebglFailed(true);
    }, []);
    const sc = mapNodes.find((c) => c.id === sel);
    const sc2 = mapNodes.find((c) => c.id === sel2);
    const fallbackList = [...mapNodes].sort((a, b) => mtHealth(b) - mtHealth(a));
    const scProposals = sc ? (railData && railData.recommendations || []).filter((r) => r.targetIds.some((t) => sc.centerNames.has(t))) : [];
    const LEGEND = [["thriving", 3129183], ["watch", 14263584], ["at-risk", 14693685]];
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginBottom: 6 }, children: [
        "Every node below is a real state from the canonical network (",
        centers.length,
        " centers across ",
        mapNodes.length,
        " territories shown, including three Canadian provinces) \u2014 not a separate dataset. Beams connect the weakest territories to their nearest strong neighbor by the same top-versus-bottom gap test the Network Propagation agent runs within a state, applied here across states (the agent itself only ever proposes within one state, so this is the map's own cross-territory read, not a direct agent feed); a cyan marker flags a territory with candidates active in the Growth pipeline."
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 6, marginBottom: 6, flexWrap: "wrap", alignItems: "center" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setViewMode("3d"), "aria-pressed": viewMode === "3d", style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "4px 10px", cursor: "pointer", border: `1px solid ${viewMode === "3d" ? INK : RULE}`, background: viewMode === "3d" ? INK : "#fff", color: viewMode === "3d" ? "#fff" : MUT }, children: "3D view" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setViewMode("table"), "aria-pressed": viewMode === "table", style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "4px 10px", cursor: "pointer", border: `1px solid ${viewMode === "table" ? INK : RULE}`, background: viewMode === "table" ? INK : "#fff", color: viewMode === "table" ? "#fff" : MUT }, children: "Table view" }),
        viewMode === "3d" && !webglFailed && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => apiRef.current.reset && apiRef.current.reset(), style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "4px 10px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: MUT }, children: "Reset view" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { marginLeft: "auto", display: "flex", gap: 10, alignItems: "center" }, children: [
          LEGEND.map(([label, hex]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { display: "flex", alignItems: "center", gap: 4, fontFamily: "Helvetica", fontSize: 8.5, color: MUT, textTransform: "capitalize" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 8, height: 8, borderRadius: "50%", background: "#" + hex.toString(16).padStart(6, "0"), display: "inline-block" } }),
            label
          ] }, label)),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { display: "flex", alignItems: "center", gap: 4, fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 0, height: 0, borderLeft: "4px solid transparent", borderRight: "4px solid transparent", borderBottom: "7px solid #6fe8ff", display: "inline-block" } }),
            "pipeline active"
          ] })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { role: "listbox", "aria-label": "Select a state to view detail", style: { display: "flex", flexWrap: "wrap", gap: 4, marginBottom: 8 }, children: [...mapNodes].sort((a, b) => a.id.localeCompare(b.id)).map((c) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { role: "option", "aria-selected": sel === c.id, onClick: () => {
        selRef.current = selRef.current === c.id ? null : c.id;
        setSel(selRef.current);
      }, title: c.label + " \u2014 " + c.state + (c.verifiedN === 0 ? " (no verified units)" : ""), style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, padding: "2px 6px", cursor: "pointer", border: `1px solid ${sel === c.id ? INK : c.conflicted ? AC : RULE}`, background: sel === c.id ? INK : "#fff", color: sel === c.id ? "#fff" : c.conflicted ? AC : MUT }, children: [
        c.label,
        c.conflicted ? " \u26A0" : "",
        c.verifiedN === 0 ? " ~" : ""
      ] }, c.id)) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { position: "relative" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { ref: mountRef, style: { width: "100%", minHeight: 460, borderRadius: 6, overflow: "hidden", border: "1px solid #1c2650", background: "#05060e", display: viewMode === "3d" && !webglFailed ? "block" : "none" } }),
        (viewMode === "table" || webglFailed) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: 16, fontFamily: "Helvetica", background: webglFailed ? "#05060e" : "#fff", color: webglFailed ? "#cdd6f4" : INK, border: webglFailed ? "none" : `1px solid ${RULE}`, borderRadius: 6 }, children: [
          webglFailed && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 11, fontWeight: 700, marginBottom: 8 }, children: "3D rendering isn't available in this environment \u2014 showing the same data as a table instead." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("table", { style: { width: "100%", borderCollapse: "collapse", fontSize: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("thead", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("tr", { style: { textAlign: "left", color: webglFailed ? "#8a97c4" : MUT }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { style: { padding: "4px 6px" }, children: "State" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { children: "Units" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { children: "Verified" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { children: "Condition" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { children: "Composite" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { children: "Pipeline" })
            ] }) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tbody", { children: fallbackList.map((c) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("tr", { onClick: () => {
              selRef.current = selRef.current === c.id ? null : c.id;
              setSel(selRef.current);
            }, style: { borderTop: webglFailed ? "1px solid #1c2650" : `1px solid ${RULE}`, cursor: "pointer", background: sel === c.id ? webglFailed ? "#141d3d" : "#f5f5f5" : "transparent" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("td", { style: { padding: "4px 6px", fontWeight: 700 }, children: [
                c.label,
                c.conflicted ? " \u26A0" : ""
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("td", { style: { padding: "4px 6px" }, children: [
                c.n,
                c.verifiedN === 0 ? " ~" : ""
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", { style: { padding: "4px 6px" }, children: c.verifiedN }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", { style: { padding: "4px 6px", textTransform: "capitalize" }, children: c.state }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", { style: { padding: "4px 6px" }, children: mtHealth(c).toFixed(2) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", { style: { padding: "4px 6px" }, children: c.leadCount || "\u2014" })
            ] }, c.id)) })
          ] })
        ] }),
        sc && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { position: viewMode === "3d" ? "absolute" : "static", top: 10, right: 10, width: 260, marginTop: viewMode === "table" ? 10 : 0, background: viewMode === "3d" ? "rgba(8,10,24,0.92)" : "#fff", border: viewMode === "3d" ? "1px solid #2b3d78" : `1px solid ${RULE}`, borderRadius: 6, padding: 12, fontFamily: "Helvetica", color: viewMode === "3d" ? "#cdd6f4" : INK }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { fontSize: 12, color: viewMode === "3d" ? "#e8ecff" : INK }, children: [
              sc.label,
              " \u2014 ",
              sc.state
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
              selRef.current = null;
              setSel(null);
            }, "aria-label": "Close state detail", style: { cursor: "pointer", color: viewMode === "3d" ? "#5a6a9e" : MUT, background: "none", border: "none", padding: 0 }, children: "\u2715" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9, color: viewMode === "3d" ? "#8a97c4" : MUT, margin: "3px 0 7px" }, children: [
            sc.n,
            " unit",
            sc.n > 1 ? "s" : "",
            " in this state (",
            sc.verifiedN,
            " verified, ",
            sc.n - sc.verifiedN,
            " modeled)",
            sc.conflicted ? " \xB7 open cross-agent conflict" : "",
            sc.leadCount > 0 ? " \xB7 " + sc.leadCount + " candidate(s) in pipeline" : ""
          ] }),
          MAP_DIMS.map((d) => {
            const v = mtVal(sc, d);
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { margin: "6px 0" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", fontSize: 9 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { textTransform: "capitalize", color: viewMode === "3d" ? "#aab4dd" : MUT }, children: d }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: viewMode === "3d" ? "#e8ecff" : INK }, children: v.toFixed(2) })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 4, background: viewMode === "3d" ? "#141d3d" : "#eee", borderRadius: 2 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 4, width: v * 100 + "%", borderRadius: 2, background: v < 0.35 ? "#e03535" : v < 0.6 ? "#d9a520" : "#2fbf5f" } }) })
            ] }, d);
          }),
          scProposals.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 8, paddingTop: 6, borderTop: viewMode === "3d" ? "1px solid #2b3d78" : `1px solid ${RULE}` }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: viewMode === "3d" ? "#8a97c4" : MUT, marginBottom: 3 }, children: [
              scProposals.length,
              " open proposal",
              scProposals.length > 1 ? "s" : "",
              " in this state"
            ] }),
            scProposals.slice(0, 3).map((r) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: viewMode === "3d" ? "#cdd6f4" : "#444", marginBottom: 2 }, children: [
              "\u2022 ",
              r.agent,
              ": ",
              r.governance.allowed ? "cleared" : "held"
            ] }, r.id))
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, marginTop: 8, flexWrap: "wrap" }, children: [
            jumpTo && sc.repCenter && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => jumpTo("lenses", sc.repCenter.name, "reviewing " + sc.label + " from the network map \u2014 " + sc.repCenter.name + " is this state's weakest unit"), style: { fontFamily: "Helvetica", fontSize: 9, color: "#6fe8ff", cursor: "pointer", background: "none", border: "none", padding: 0, textDecoration: "underline" }, children: [
              "view ",
              sc.repCenter.name,
              " in Six Lenses \u2192"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setSel2(sel2 === sc.id ? null : sel2 || mapNodes.find((m) => m.id !== sc.id)?.id), style: { fontFamily: "Helvetica", fontSize: 9, color: viewMode === "3d" ? "#6fe8ff" : AC, cursor: "pointer", background: "none", border: "none", padding: 0, textDecoration: "underline" }, children: sel2 ? "stop comparing" : "compare with another state" }),
            jumpTo && sc.repCenter && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => jumpTo("network", sc.repCenter.name, "reviewing " + sc.label + "'s cluster from the network map"), style: { fontFamily: "Helvetica", fontSize: 9, color: viewMode === "3d" ? "#6fe8ff" : AC, cursor: "pointer", background: "none", border: "none", padding: 0, textDecoration: "underline" }, children: [
              "view ",
              sc.label,
              " cluster in Network \u2192"
            ] }),
            jumpTo && sc.leadCount > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => jumpTo("leads", null, sc.leadCount + " candidate(s) active in " + sc.label + ", seen from the network map"), style: { fontFamily: "Helvetica", fontSize: 9, color: viewMode === "3d" ? "#6fe8ff" : AC, cursor: "pointer", background: "none", border: "none", padding: 0, textDecoration: "underline" }, children: [
              "view ",
              sc.label,
              " pipeline in Growth \u2192"
            ] })
          ] })
        ] })
      ] }),
      sc && sel2 && sc2 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 8, border: `1px solid ${RULE}`, background: "#fff", padding: "9px 12px" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 6 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT }, children: [
            "Comparing ",
            sc.label,
            " vs ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("select", { value: sel2, onChange: (e) => setSel2(e.target.value), "aria-label": "Choose comparison state", style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700 }, children: mapNodes.filter((m) => m.id !== sc.id).map((m) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: m.id, children: m.label }, m.id)) })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setSel2(null), "aria-label": "Close comparison", style: { fontFamily: "Helvetica", fontSize: 11, color: MUT, cursor: "pointer", background: "none", border: "none", padding: 0 }, children: "\u2715" })
        ] }),
        MAP_DIMS.map((d) => {
          const v1 = mtVal(sc, d), v2 = mtVal(sc2, d), delta = v1 - v2;
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, margin: "4px 0", fontFamily: "Helvetica", fontSize: 9.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 80, textTransform: "capitalize", color: MUT }, children: d }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 40, textAlign: "right", fontWeight: 700 }, children: v1.toFixed(2) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1, height: 4, background: "#eee", borderRadius: 2, position: "relative" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", left: "50%", height: 4, width: Math.abs(delta) * 50 + "%", transform: delta >= 0 ? "translateX(0)" : "translateX(-100%)", background: delta >= 0 ? GRN : AC, borderRadius: 2 } }) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 40, fontWeight: 700 }, children: v2.toFixed(2) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { width: 52, fontSize: 8.5, color: delta >= 0 ? GRN : AC }, children: [
              delta >= 0 ? "+" : "",
              delta.toFixed(2)
            ] })
          ] }, d);
        })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 8, padding: "8px 12px", background: "#0a0d1f", border: "1px solid #1c2650", borderRadius: 6, fontFamily: "Helvetica" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", fontSize: 9, color: "#8a97c4", marginBottom: 4 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: "#e8ecff" }, children: [
            "Week ",
            week,
            week === currentWeek ? " (current)" : " (preview \u2014 does not change the shared clock)"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
            edges.length,
            " active propagation link",
            edges.length === 1 ? "" : "s",
            " this cycle"
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { type: "range", min: "0", max: currentWeek, step: "1", value: week, onChange: (e) => setWeek(parseInt(e.target.value)), "aria-label": "Preview an earlier week, currently showing week " + week, style: { width: "100%" } })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#888", marginTop: 5 }, children: "drag = orbit \xB7 scroll = zoom \xB7 click a state (or use the list above) = unit readout \xB7 reads the live canonical state, same as every other view" })
    ] });
  }
  var INK = "#111";
  var MUT = "#666";
  var AC = "#8b0000";
  var RULE = "#ccc";
  var W = [
    { n: 1, p: "acquire", s: "Community channel activated", o: "System", sys: "CRM", d: "Select channel: after-school STEM, Girl Scouts, Girls Who Code, holiday camp, farmers market, sports cross-promo, library event, or STEM market positioning." },
    { n: 2, p: "acquire", s: "Event hosted or partnership launched", o: "Director", sys: "Local", d: "Director or owner establishes the relationship and schedules the event. This is local, in-person work." },
    { n: 3, p: "acquire", s: "Lead captured", o: "System", sys: "CRM", d: "Name, child's age, contact info, source channel. Captured at event via form, QR code, or sign-up sheet." },
    { n: 4, p: "acquire", s: "Lead entered in pipeline", o: "System", sys: "CRM", d: "Auto-created if digital capture. Manual entry if paper sign-up. Tagged with source channel for attribution." },
    { n: 5, p: "acquire", s: "Welcome email sent", o: "System", sys: "SendGrid", d: "Auto-triggered on lead creation. Includes: what Code Ninjas is, what to expect at a trial, scheduling link." },
    { n: 6, p: "acquire", s: "Parent responds", o: "Parent", sys: "\u2014", d: "Calls, emails, clicks scheduling link, or walks in. This is the moment of intent." },
    { n: 7, p: "acquire", s: "Availability checked", o: "System", sys: "MyStudio", d: "System shows open trial slots, Sensei availability, and room capacity for the requested day/time." },
    { n: 8, p: "acquire", s: "Tour scheduled", o: "Director", sys: "MyStudio", d: "If parent wants to see the center first. Not all leads need a tour \u2014 some go straight to trial booking." },
    { n: 9, p: "acquire", s: "Tour reminder sent", o: "System", sys: "SendGrid", d: "24-hour reminder with directions, what to expect, and a reschedule link." },
    { n: 10, p: "acquire", s: "Tour conducted", o: "Director", sys: "\u2014", d: "Director walks parent through the center. Shows the curriculum, explains belts, introduces a Sensei, lets the child see other kids coding." },
    { n: 11, p: "acquire", s: "Trial offered", o: "Director", sys: "\u2014", d: "At end of tour: 'Would your child like to try a session?' Free or low-cost. Low commitment, high signal." },
    { n: 12, p: "acquire", s: "Trial booked", o: "System", sys: "MyStudio", d: "Slot reserved. Sensei assigned based on availability and student age/level match." },
    { n: 13, p: "acquire", s: "Trial confirmation sent", o: "System", sys: "SendGrid", d: "Immediate confirmation with date, time, what to bring, parking info." },
    { n: 14, p: "acquire", s: "Trial reminders sent", o: "System", sys: "SendGrid + SMS", d: "24-hour and 1-hour reminders. Reduces no-show rate on trials \u2014 the highest-leverage touchpoint." },
    { n: 15, p: "acquire", s: "Student arrives, checked in", o: "System", sys: "MyStudio", d: "Check-in logged. Attendance record created. First-visit flag set." },
    { n: 16, p: "acquire", s: "Trial session delivered", o: "Sensei", sys: "Learning Platform", d: "Student builds something real in 60 minutes. Sensei reads the room \u2014 is the child engaged? Frustrated? Bored? Thrilled? This is the product demo." },
    { n: 17, p: "acquire", s: "Post-trial note sent", o: "System", sys: "App + SendGrid", d: "Same 4-part structure as regular notes. 'Your child built X today. They were excited about Y. Next time we would work on Z.'" },
    { n: 18, p: "acquire", s: "Day-3 follow-up", o: "System", sys: "SendGrid", d: "Auto-email: 'How did [child's name] feel about the session? Any questions about the program?'" },
    { n: 19, p: "acquire", s: "Day-7 follow-up", o: "Director", sys: "Phone/Email", d: "Personal outreach from Director. Not a sales call \u2014 a check-in. 'Did they mention the session at home? Would they like to come back?'" },
    { n: 20, p: "acquire", s: "GATE: Enrollment decision", o: "Director", sys: "MyStudio", d: "Parent decides. Director discusses pricing tiers, schedule options, membership commitment. This is a financial decision \u2014 human-owned.", gate: true, gr: "Financial materiality" },
    { n: 21, p: "acquire", s: "Paperwork completed", o: "Parent", sys: "MyStudio", d: "Enrollment form, liability waiver, emergency contacts, billing authorization." },
    { n: 22, p: "acquire", s: "Billing set up", o: "System", sys: "MyStudio", d: "Recurring billing configured. First payment processed. Membership tier recorded." },
    { n: 23, p: "acquire", s: "Welcome packet sent", o: "System", sys: "App + SendGrid", d: "Welcome to Code Ninjas. Your child's Sensei is [name]. First session is [date]. Here's what to expect." },
    { n: 24, p: "acquire", s: "First regular session scheduled", o: "System", sys: "MyStudio", d: "Recurring weekly slot booked. Calendar invite sent to parent." },
    { n: 25, p: "deliver", s: "Weekly schedule built", o: "System", sys: "MyStudio", d: "All sessions for the week populated. Sensei assignments confirmed. Room capacity validated." },
    { n: 26, p: "deliver", s: "Sensei-to-student ratios checked", o: "System", sys: "MyStudio", d: "Target: 1:8. If any session exceeds ratio, flag for Director to add coverage or cap enrollment." },
    { n: 27, p: "deliver", s: "Cancellations and makeups processed", o: "System", sys: "MyStudio", d: "Cancelled sessions freed. Makeup slots offered. Waitlisted students notified of openings." },
    { n: 28, p: "deliver", s: "Student brief pulled", o: "System", sys: "Learning Platform", d: "Auto-generated: current belt level, last project state (what they built, where they stopped), flags (stuck, bored, ahead), parent requests." },
    { n: 29, p: "deliver", s: "Curriculum checkpoint reviewed", o: "System", sys: "Learning Platform", d: "Is the student approaching a belt test? Any new challenges available at their level? Curriculum updates since last session?" },
    { n: 30, p: "deliver", s: "Sensei reads brief, plans approach", o: "Sensei", sys: "\u2014", d: "30-second scan. Decision: continue last project, introduce new concept, or pivot based on expected energy. Agent prepares; human decides." },
    { n: 31, p: "deliver", s: "Student arrives, checked in", o: "System", sys: "MyStudio", d: "Attendance logged. Arrival time recorded. Late arrivals flagged if pattern forms." },
    { n: 32, p: "deliver", s: "Welcome and reconnect", o: "Sensei", sys: "\u2014", d: "'Last time you got the scoring working \u2014 want to pick up from there or try something new?' Acknowledge by name. Reference last session. Give agency.", t: "2 min" },
    { n: 33, p: "deliver", s: "Student selects direction", o: "Student", sys: "\u2014", d: "Student chooses: continue current project, start a new challenge, or explore a concept. Student-driven, not teacher-directed.", t: "1 min" },
    { n: 34, p: "deliver", s: "Student codes", o: "Student", sys: "Learning Platform", d: "The core 40 minutes. Student works on their project. Sensei circulates, observes, and intervenes only when stuck 2+ minutes or the student asks for help.", t: "40 min" },
    { n: 35, p: "deliver", s: "Question-driven debugging", o: "Sensei", sys: "\u2014", d: "When stuck: 'What did you expect to happen? What actually happened? Can you find the line where the behavior changes?' Ask before telling. Scaffold, don't solve.", t: "ongoing" },
    { n: 36, p: "deliver", s: "Mini-teach if concept gap", o: "Sensei", sys: "\u2014", d: "Student hits a concept they've never seen (loops, functions, variables). Sensei gives a 3\u20135 minute targeted lesson. Short, specific, then back to building.", t: "3\u20135 min" },
    { n: 37, p: "deliver", s: "Save project", o: "Student", sys: "Learning Platform", d: "Student saves their work. Project state persisted so the next session picks up exactly where they left off." },
    { n: 38, p: "deliver", s: "Show and celebrate", o: "Student", sys: "\u2014", d: "Last 5 minutes. Student runs their project, shows it to the Sensei or another student. 'Look what I made.' This is the visible-progress retention signal.", t: "5 min" },
    { n: 39, p: "deliver", s: "Progress marker logged", o: "System", sys: "Learning Platform", d: "Session completed. Belt-pathway progress updated. If belt checkpoint completed, flag for ceremony scheduling." },
    { n: 40, p: "deliver", s: "Belt checkpoint?", o: "System", sys: "Learning Platform", d: "Decision point: did the student complete a belt test? If yes \u2192 flag for ceremony. If no \u2192 continue on pathway.", decision: true },
    { n: 41, p: "retain", s: "System drafts session note", o: "System", sys: "Learning Platform", d: "Auto-generated from session data. Follows the 4-part structure: (1) lead with a strength, (2) 'I noticed\u2026' observation, (3) what's next, (4) invitation to respond." },
    { n: 42, p: "retain", s: "GATE: Sensei reviews and personalizes", o: "Sensei", sys: "App", d: "60\u201390 seconds per student. Sensei reads draft, adds personal observations ('she was really proud of the scoring system'), corrects anything wrong, approves for send.", gate: true, gr: "Data integrity" },
    { n: 43, p: "retain", s: "Note sent to parent", o: "System", sys: "App + SendGrid", d: "Delivered within 30 minutes of session end. App notification + email. Timely delivery signals: we pay attention, we care." },
    { n: 44, p: "retain", s: "Belt advancement?", o: "System", sys: "App", d: "Decision point: did the student earn a new belt? If yes \u2192 celebration message to parent, ceremony scheduling triggered.", decision: true },
    { n: 45, p: "retain", s: "Belt celebration sent", o: "System", sys: "App + SendGrid", d: "'Your ninja just earned their [color] belt!' Photo opportunity. Ceremony date. Invite friends and family \u2014 this is a referral event." },
    { n: 46, p: "retain", s: "Student absent?", o: "System", sys: "MyStudio", d: "Decision point: did the student not show up?", decision: true },
    { n: 47, p: "retain", s: "Same-day reschedule text", o: "System", sys: "Twilio", d: "Auto-sent within 2 hours of missed session. 'We missed [name] today \u2014 here are available makeup slots this week.'" },
    { n: 48, p: "retain", s: "2nd consecutive no-show?", o: "System", sys: "MyStudio", d: "Decision point: is this the second consecutive miss?", decision: true },
    { n: 49, p: "retain", s: "Director flagged for personal outreach", o: "System", sys: "CRM", d: "At-risk flag set. Director sees it in their morning dashboard. This is no longer an automated recovery \u2014 it's a relationship." },
    { n: 50, p: "retain", s: "GATE: At-risk intervention", o: "Director", sys: "Phone/Email", d: "Director personally contacts the family. 'Is everything okay? Can we adjust the schedule? Is there anything about the program we should change?' Empathy, flexibility, listening.", gate: true, gr: "Franchisee consent" },
    { n: 51, p: "retain", s: "Monthly progress report generated", o: "System", sys: "Learning Platform", d: "Summary: sessions attended, belt progress, projects completed, skills developed. Sent to parent on the 1st of each month." },
    { n: 52, p: "retain", s: "Re-enrollment nudge", o: "System", sys: "SendGrid", d: "30 days before membership lapses: 'Your ninja has earned [belt] and is working on [project]. Renew to keep the momentum going.'" },
    { n: 53, p: "retain", s: "Referral prompt", o: "System", sys: "App", d: "After belt ceremonies, after positive notes, after milestones: 'Know a family who might love Code Ninjas? Share a free trial.'" },
    { n: 54, p: "retain", s: "Referral received", o: "System", sys: "CRM", d: "New lead created, tagged as referral, attributed to referring family. \u2192 Back to step 1. The loop closes.", back: true },
    { n: 55, p: "operate", s: "Staffing need identified", o: "Director", sys: "MyStudio", d: "Session load increasing, Sensei leaving, or ratio exceeding 1:8. The hiring pipeline must always be warm." },
    { n: 56, p: "operate", s: "Job posted", o: "Director", sys: "Indeed", d: "Part-time Sensei role. Coding skills + ability to work with kids. Often college students or high schoolers." },
    { n: 57, p: "operate", s: "Candidates screened", o: "Director", sys: "Indeed", d: "Resume review, brief phone screen. Looking for: coding ability, patience, communication, reliability." },
    { n: 58, p: "operate", s: "GATE: Interview and hire decision", o: "Director", sys: "\u2014", d: "In-person interview. Trial teaching session with a real student. Director decides: hire or pass.", gate: true, gr: "Child safety" },
    { n: 59, p: "operate", s: "Background check", o: "System", sys: "Background service", d: "Required for anyone working with children. Must clear before first session." },
    { n: 60, p: "operate", s: "Training completed", o: "Director + Sensei", sys: "Internal", d: "Curriculum (CREATE/JR), customer service standards, child safety protocols, MyStudio operations, session-note process." },
    { n: 61, p: "operate", s: "Monthly P&L pulled", o: "System", sys: "MyStudio + QuickBooks", d: "Revenue: active students \xD7 tier \xD7 billing period. Expenses: rent, payroll, curriculum license, marketing, supplies." },
    { n: 62, p: "operate", s: "Revenue vs expense reviewed", o: "Owner", sys: "QuickBooks", d: "Is the unit profitable? Where are the leaks? Payroll too high for enrollment? Marketing spend returning trials?" },
    { n: 63, p: "operate", s: "GATE: Financial decisions", o: "Owner", sys: "\u2014", d: "Pricing changes, cost cuts, marketing budget adjustments, staffing level changes. Material financial decisions are human-owned.", gate: true, gr: "Unit economics floor" },
    { n: 64, p: "operate", s: "Royalty calculated", o: "System", sys: "MyStudio", d: "8% of gross revenue (royalty) + 2% of gross revenue (brand fund) = 10% to corporate monthly." },
    { n: 65, p: "operate", s: "Weekly enrollment report", o: "System", sys: "MyStudio \u2192 Corporate", d: "Active students, new enrollments, cancellations, trial bookings. Auto-generated and submitted." },
    { n: 66, p: "operate", s: "Monthly P&L submitted", o: "Owner", sys: "Corporate portal", d: "Required per Franchise Agreement. FBC reviews for health monitoring." },
    { n: 67, p: "operate", s: "Brand compliance checked", o: "System", sys: "Corporate", d: "Center appearance, curriculum delivery, marketing guidelines, territory adherence. Automated where possible." },
    { n: 68, p: "operate", s: "Deviations flagged to FBC", o: "System", sys: "Corporate", d: "Non-compliance items routed to Franchise Business Coach for review and outreach. Not auto-enforced." },
    { n: 69, p: "operate", s: "FBC outreach if needed", o: "FBC", sys: "Phone/Email", d: "Health metric crosses threshold \u2192 outreach ('We noticed X \u2014 how can we help?') \u2192 response feeds the unit's support plan. Systemic issues \u2192 escalate to network tier for a structural fix.", t: "" }
  ];
  var PHASE_META = {
    acquire: { name: "ACQUIRE", range: "Steps 1\u201324", count: 24, desc: "Community channel to enrolled student. Every handoff from first event to first billing cycle." },
    deliver: { name: "DELIVER", range: "Steps 25\u201340", count: 16, desc: "The weekly session loop. Schedule \u2192 prep \u2192 deliver \u2192 log. The core product." },
    retain: { name: "RETAIN", range: "Steps 41\u201354", count: 14, desc: "Post-session notes, parent trust, no-show recovery, at-risk intervention, referral. Where revenue is kept or lost." },
    operate: { name: "OPERATE", range: "Steps 55\u201369", count: 15, desc: "Staffing, finance, compliance, reporting. The foundation under everything." }
  };
  var ORG_ROLES = {
    acquire: ["Director \u2014 tours, enrollment gate, day-7 follow-up, pricing conversation", "System \u2014 CRM, MyStudio, SendGrid automation for lead capture through billing setup", "Parent \u2014 the decision-maker at the enrollment gate", "Sensei \u2014 delivers the trial session that determines conversion"],
    deliver: ["Sensei \u2014 session delivery, question-driven instruction, brief review, save & show", "Student \u2014 project ownership, direction selection, the person doing the learning", "System \u2014 scheduling, prep briefs, progress markers, capacity checks", "Director \u2014 ratio oversight, coverage decisions"],
    retain: ["System \u2014 note drafting, no-show recovery, referral prompts, progress reports", "Sensei \u2014 note review gate, personalization, session quality", "Director \u2014 at-risk intervention gate, personal family outreach", "Parent \u2014 trust recipient, re-enrollment decision, referral source"],
    operate: ["Owner \u2014 P&L ownership, financial decision gate, strategic direction", "Director \u2014 staffing gate, hiring, training, daily operations", "System \u2014 reporting, royalty calculation, compliance monitoring", "FBC \u2014 regional oversight, at-risk unit support, best-practice deployment"]
  };
  var AGILE = {
    acquire: { wip: [99, 3, 2, 99], kanban: [["3 leads from STEM event", "2 from referral"], ["1 tour today", "2 trials this week"], ["1 day-7 follow-up due", "1 enrollment packet out"], ["2 enrolled this week"]], sprint: "Acquire Sprint: Convert 3 of 5 active trials, host 1 Girl Scouts workshop, capture 10 leads", velocity: [4, 3, 5, 4, 6, 5, 7, 6], outcome: "Trial\u2192enrollment conversion (Metrics tab)" },
    deliver: { wip: [99, 6, 4, 99], kanban: [["3 briefs to pull", "1 makeup to schedule"], ["5 sessions in progress", "2 belt tests today"], ["4 notes in draft", "1 belt ceremony to schedule"], ["12 sessions completed", "8 notes sent <30 min"]], sprint: "Deliver Sprint: 15 sessions at 95%+ notes, 1 belt ceremony, prep coverage 100%", velocity: [12, 14, 15, 13, 16, 14, 15, 17], outcome: "Notes completion & belt velocity (Metrics tab)" },
    retain: { wip: [99, 3, 3, 99], kanban: [["2 notes pending review", "1 no-show to recover"], ["3 progress reports generating", "1 at-risk outreach in progress"], ["1 re-enrollment nudge queued", "2 referral prompts sent"], ["4 families retained", "1 referral converted"]], sprint: "Retain Sprint: 95% notes on time, recover 3 no-shows same-day, 1 at-risk save", velocity: [3, 4, 5, 4, 6, 5, 7, 6], outcome: "At-risk resolution & retention (Metrics tab)" },
    operate: { wip: [99, 2, 2, 99], kanban: [["1 Sensei posting active", "P&L data pulling"], ["2 candidates interviewing", "Monthly review in progress"], ["1 background check pending", "Compliance report reviewing"], ["1 Sensei hired + training", "P&L submitted"]], sprint: "Operate Sprint: Hire 1 Sensei, submit monthly P&L, resolve 1 compliance flag", velocity: [2, 2, 3, 2, 3, 3, 4, 3], outcome: "Unit net margin (Metrics tab)" }
  };
  var NET_TIERS = [
    { n: "Network", s: "All 350+ centers", d: "Curriculum, brand standards, pricing, FDD, royalty, Best Practice Library, development pipeline." },
    { n: "Region", s: "8\u201312 regions", d: "Performance benchmarks, FBC oversight, expansion targets, franchisee health aggregation." },
    { n: "Cluster", s: "3\u20138 centers per metro", d: "Shared events, Sensei coverage-pool sharing, cross-promo, territory coordination." },
    { n: "Unit", s: "Single center", d: "Session ops, student experience, local marketing, P&L, staffing, parent relationships." }
  ];
  var NET_STATES = [
    { s: "TX", n: "Texas", c: 34, h: [0.32, 0.37, 0.31] },
    { s: "CA", n: "California", c: 30, h: [0.36, 0.34, 0.3] },
    { s: "FL", n: "Florida", c: 22, h: [0.3, 0.4, 0.3] },
    { s: "VA", n: "Virginia", c: 14, h: [0.38, 0.36, 0.26] },
    { s: "NC", n: "North Carolina", c: 12, h: [0.34, 0.38, 0.28] },
    { s: "NJ", n: "New Jersey", c: 11, h: [0.31, 0.39, 0.3] },
    { s: "NY", n: "New York", c: 11, h: [0.33, 0.37, 0.3] },
    { s: "IL", n: "Illinois", c: 10, h: [0.35, 0.36, 0.29] },
    { s: "GA", n: "Georgia", c: 9, h: [0.4, 0.34, 0.26] },
    { s: "PA", n: "Pennsylvania", c: 9, h: [0.32, 0.4, 0.28] },
    { s: "MD", n: "Maryland", c: 8, h: [0.34, 0.38, 0.28] },
    { s: "WA", n: "Washington", c: 8, h: [0.37, 0.35, 0.28] },
    { s: "AZ", n: "Arizona", c: 7, h: [0.33, 0.39, 0.28] },
    { s: "MA", n: "Massachusetts", c: 7, h: [0.36, 0.36, 0.28] },
    { s: "CO", n: "Colorado", c: 6, h: [0.38, 0.35, 0.27] },
    { s: "OH", n: "Ohio", c: 6, h: [0.31, 0.41, 0.28] },
    { s: "MI", n: "Michigan", c: 6, h: [0.3, 0.4, 0.3] },
    { s: "MN", n: "Minnesota", c: 5, h: [0.35, 0.37, 0.28] },
    { s: "TN", n: "Tennessee", c: 5, h: [0.34, 0.38, 0.28] },
    { s: "MO", n: "Missouri", c: 5, h: [0.33, 0.38, 0.29] },
    { s: "WI", n: "Wisconsin", c: 4, h: [0.36, 0.36, 0.28] },
    { s: "IN", n: "Indiana", c: 4, h: [0.32, 0.4, 0.28] },
    { s: "OR", n: "Oregon", c: 4, h: [0.37, 0.36, 0.27] },
    { s: "NV", n: "Nevada", c: 4, h: [0.31, 0.39, 0.3] },
    { s: "UT", n: "Utah", c: 4, h: [0.39, 0.34, 0.27] },
    { s: "CT", n: "Connecticut", c: 4, h: [0.34, 0.38, 0.28] },
    { s: "SC", n: "South Carolina", c: 4, h: [0.35, 0.37, 0.28] },
    { s: "OK", n: "Oklahoma", c: 3, h: [0.33, 0.38, 0.29] },
    { s: "LA", n: "Louisiana", c: 3, h: [0.3, 0.4, 0.3] },
    { s: "KS", n: "Kansas", c: 3, h: [0.34, 0.38, 0.28] },
    { s: "IA", n: "Iowa", c: 3, h: [0.35, 0.37, 0.28] },
    { s: "AL", n: "Alabama", c: 3, h: [0.32, 0.39, 0.29] },
    { s: "AR", n: "Arkansas", c: 2, h: [0.33, 0.38, 0.29] },
    { s: "ID", n: "Idaho", c: 2, h: [0.38, 0.35, 0.27] },
    { s: "ND", n: "North Dakota", c: 1, h: [0.36, 0.37, 0.27] },
    { s: "WV", n: "West Virginia", c: 2, h: [0.31, 0.4, 0.29] },
    { s: "DE", n: "Delaware", c: 1, h: [0.34, 0.38, 0.28] }
  ];
  var HCOL = ["#2f7a3f", "#b58900", "#8b0000"];
  var CONNECTIVITY = [
    { n: 1, t: "Best Practice Propagation", d: "Validated pattern \u2192 anonymized \u2192 Best Practice Library \u2192 all units. New centers start from proven patterns.", owner: "Director of Franchise Development", accept: "Validated wins codified to the Library \u226430 days; adoption tracked.", guard: "Data integrity \u2014 only verified, reproducible patterns propagate; no anecdote promoted to standard." },
    { n: 2, t: "Cluster Events Engine", d: "Centers in a metro coordinate community events. Cannibalization constraint applies.", owner: "Cluster lead (senior Center Director)", accept: "Shared events run with participating-center sign-off; leads attributed fairly.", guard: "Cannibalization + Franchisee consent \u2014 no shared event that starves or obligates a unit." },
    { n: 3, t: "Sensei Coverage-Pool Sharing", d: "Part-time Senseis float between cluster centers. Student context travels with the system.", owner: "Cluster lead", accept: "Shared Senseis background-checked at every center they serve; coverage logged.", guard: "Child safety \u2014 clearance required at each site before first session." },
    { n: 4, t: "Cross-Unit Referral", d: "Family relocates \u2192 belt level, project, parent history transfer intact.", owner: "Center Director (sending + receiving)", accept: "Full student record transfers within 48h of relocation.", guard: "Data integrity \u2014 record transfers complete and accurate; nothing dropped or altered." },
    { n: 5, t: "Network Health Dashboard", d: "Heat map of all centers by health. At-risk units flagged for FBC intervention.", owner: "Director of Franchise Development", accept: "Every center scored monthly; below-floor units flagged within the cycle.", guard: "Engagement integrity \u2014 health scores built from honest engagement data, never inflated." },
    { n: 6, t: "Curriculum Cascade", d: "Updates deploy simultaneously. Non-deployment flagged at day 30. Feedback not suppressed.", owner: "Network curriculum lead", accept: "Updates live network-wide; non-deployment flagged at day 30.", guard: "Audit trail \u2014 deployment status logged per center; feedback preserved, not suppressed." },
    { n: 7, t: "Competition Pipeline", d: "Students surfaced for FIRST LEGO League, Congressional App Challenge, hackathons, showcases.", owner: "Center Director + lead Sensei", accept: "Eligible students surfaced each season; FLL coach safety-gated.", guard: "Child safety + Engagement integrity \u2014 coaches cleared; only real achievement surfaced." },
    { n: 8, t: "Franchisee Peer Network", d: "Monthly cluster calls, quarterly regional forums. Two-directional. Feedback tracked to resolution.", owner: "Director of Franchise Development", accept: "Cadence held; every raised issue tracked to a logged resolution.", guard: "Franchisee consent \u2014 participation invited, not compelled; feedback never used punitively." },
    { n: 9, t: "Financial Benchmarking", d: "Standardized P&L. Network benchmarks. Below 25th percentile triggers FBC outreach and a support-plan revision.", owner: "Director of Franchise Development + FBC", accept: "Standardized P&L collected; sub-25th-percentile units routed to a support-plan revision within the cycle.", guard: "Financial materiality + Data integrity \u2014 figures reserved to humans; benchmarks from real submitted data." },
    { n: 10, t: "Brand Compliance", d: "Automated monitoring. Deviations flagged to FBC. Systemic issues addressed with support, not punishment.", owner: "FBC (Franchise Business Consultant)", accept: "Deviations flagged; systemic issues met with a support plan first.", guard: "Audit trail \u2014 every flag and resolution logged with provenance; support before punishment." }
  ];
  var DASHBOARD = [
    { m: "Avg students/center", v: "~43", t: "target: 60", c: true },
    { m: "Trial \u2192 enrollment", v: "~35%", t: "target: 50%" },
    { m: "Franchisee satisfaction", v: "2.5/5", t: "profitability: 1.6/5", c: true },
    { m: "Notes completion", v: "varies", t: "target: 95%+" },
    { m: "No-show recovery", v: "varies", t: "target: 80%+" },
    { m: "Referral rate", v: "varies", t: "target: 15%+" },
    { m: "Multi-year retention", v: "varies", t: "target: 70% at 12mo" },
    { m: "Unit payback", v: "8\u201310 yr", t: "target: 3\u20134 yr", c: true }
  ];
  var ACTION_PLANS = [
    { r: 1, n: "Unit-Value Program", d: "Improve unit economics at existing centers. $269 premium-tier pricing, community pipeline, retention back-edge. ~$1.36M/yr additional royalty at 400 units. Lowest risk.", risk: "Low", floor: "None \u2014 do this first" },
    { r: 2, n: "Selective Expansion", d: "25\u201350 new units in verified Tier 1 markets. Candidate underwriting gate. Cannibalization hard constraint.", risk: "Medium", floor: "Median unit above breakeven" },
    { r: 3, n: "Resale & Turnaround", d: "At-risk units resold to stronger operators. Turnaround protocol: retention sprint + cluster support.", risk: "Medium", floor: "Turnaround plan approved" },
    { r: 4, n: "Status Quo", d: "Hold footprint. Focus entirely on unit profitability. Safest, but slowest \u2014 the baseline to beat.", risk: "Lowest", floor: "Default" }
  ];
  function financialRange(point, uncertaintyPct) {
    const lo = +(point * (1 - uncertaintyPct)).toFixed(2);
    const hi = +(point * (1 + uncertaintyPct)).toFixed(2);
    return { point, lo, hi, uncertaintyPct };
  }
  function adoptionRamp(monthsOpen) {
    const m = Math.max(0, monthsOpen);
    return Math.min(1, 1 - Math.exp(-m / 14));
  }
  function rampMilestones() {
    return [0, 6, 12, 24, 36].map((m) => ({ m, pct: Math.round(adoptionRamp(m) * 100) }));
  }
  function franchiseeDeltaOf(plan) {
    const table = {
      "Unit-Value Program": { unitMarginDelta_k: 14, payback_yr: "-1.2 yr", note: "premium-tier pricing + retention lift flow to the unit before they flow to the system" },
      "Selective Expansion": { unitMarginDelta_k: 0, payback_yr: "unchanged", note: "new units carry their own ramp; existing units see no economic change from this lever" },
      "Resale & Turnaround": { unitMarginDelta_k: -4, payback_yr: "reset at sale", note: "incoming operator inherits a support-plan floor, not a clean slate" },
      "Status Quo": { unitMarginDelta_k: 0, payback_yr: "unchanged", note: "no unit-level change either direction" }
    };
    return table[plan.n] || { unitMarginDelta_k: 0, payback_yr: "n/a", note: "" };
  }
  function rationaleOf(rec) {
    const contract = ACTION_CONTRACTS[rec.actionType];
    const window2 = contract ? contract.minFreshnessDays : null;
    if (rec.governance.allowed) {
      return "Cleared: data " + rec.evidence.dataFreshnessDays + "d old" + (window2 ? ", within the " + window2 + "-day window" : "") + "; " + rec.approverRole + " may act.";
    }
    const reasons = rec.governance.heldOn && rec.governance.heldOn.length ? rec.governance.heldOn.join(", ") : "contract not satisfied";
    return "Held: " + reasons + ".";
  }
  function decisionLedgerExport(decisions, railData) {
    const recs = railData && railData.recommendations || [];
    const rows = Object.keys(decisions).map((id) => {
      const rec = recs.find((r) => r.id === id);
      return {
        id,
        decision: decisions[id],
        agent: rec ? rec.agent : null,
        actionType: rec ? rec.actionType : null,
        title: rec ? rec.title : null,
        approverRole: rec ? rec.approverRole : null,
        governanceAllowed: rec ? rec.governance.allowed : null,
        heldOn: rec ? rec.governance.heldOn : null
      };
    });
    return { exportedAt: "week " + (rows.length ? rows.length : 0), schema: "cn-decision-ledger/1.0", rows };
  }
  function FDDReconciliationPanel({ centers }) {
    const AUTHORITATIVE = 244;
    const VERIFIED = centers.filter((c) => c.verified).length;
    const modeled = centers.length;
    const illustrative = modeled - VERIFIED;
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${AC}`, background: "#fff", padding: "8px 11px", marginTop: 6 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 4 }, children: "FDD Item 20 reconciliation" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 14, flexWrap: "wrap", marginBottom: 4 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800, color: INK }, children: AUTHORITATIVE }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "authoritative units (FDD Item 20)" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800, color: GRN }, children: VERIFIED }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "verified from public records" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800, color: MUT }, children: illustrative }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "modeled beyond the verified set" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800, color: VIO }, children: modeled }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "total in this model" })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, lineHeight: 1.4 }, children: [
        "The ",
        AUTHORITATIVE,
        "-unit FDD figure and the ",
        VERIFIED,
        " publicly verified centers are not superseded by the ",
        modeled,
        "-unit modeled network above \u2014 the ",
        illustrative,
        " additional units exist to give the operating model enough scale to demonstrate cross-cluster propagation and network-level governance, and are labeled as such everywhere they appear."
      ] })
    ] });
  }
  function downloadJSON(obj, filename) {
    try {
      const blob = new Blob([JSON.stringify(obj, null, 2)], { type: "application/json" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = filename;
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
      return true;
    } catch (e) {
      return false;
    }
  }
  function downloadText(text, filename) {
    try {
      const blob = new Blob([text], { type: "text/plain" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = filename;
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
      return true;
    } catch (e) {
      return false;
    }
  }
  function toCSV(rows, columns) {
    const esc = (v) => {
      const s = v === null || v === void 0 ? "" : String(v);
      return /[",\n]/.test(s) ? '"' + s.replace(/"/g, '""') + '"' : s;
    };
    const header = columns.map((c) => esc(c.label)).join(",");
    const body = rows.map((r) => columns.map((c) => esc(c.get(r))).join(",")).join("\n");
    return header + "\n" + body;
  }
  function downloadCSV(text, filename) {
    try {
      const blob = new Blob([text], { type: "text/csv" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = filename;
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
      return true;
    } catch (e) {
      return false;
    }
  }
  var CENTER_CSV_COLS = [
    { label: "Center", get: (c) => c.name },
    { label: "State", get: (c) => c.st },
    { label: "Verified", get: (c) => c.verified ? "yes" : "no" },
    { label: "Health", get: (c) => c.health },
    { label: "Condition", get: (c) => conditionOf(c).label },
    { label: "Students", get: (c) => c.students },
    { label: "EBITDA (k)", get: (c) => c.eb },
    { label: "Retention", get: (c) => +c.ret.toFixed(2) },
    { label: "Chemistry", get: (c) => +c.chem.toFixed(2) },
    { label: "Conversion", get: (c) => +c.conv.toFixed(2) }
  ];
  var RECOMMENDATION_CSV_COLS = [
    { label: "ID", get: (r) => r.id },
    { label: "Agent", get: (r) => r.agent },
    { label: "Action type", get: (r) => r.actionType },
    { label: "Title", get: (r) => r.title },
    { label: "Approver role", get: (r) => r.approverRole },
    { label: "Status", get: (r) => r.governance.allowed ? "allowed" : "held" },
    { label: "Held on", get: (r) => r.governance.heldOn || "" }
  ];
  var SUPPORT_PLAN_LABELS = {
    retention: "Retention outreach sequence",
    staffing: "Staffing stabilization plan",
    unitvalue: "Unit-economics program",
    conversion: "Trial-conversion review",
    monitor: "Standard monitoring cadence"
  };
  function successRateData(dyn) {
    const committed = dyn && dyn.committed || {}, completed = dyn && dyn.completed || {};
    const byPlan = {};
    Object.entries(committed).forEach(([name, pid]) => {
      if (!byPlan[pid]) byPlan[pid] = { id: pid, n: SUPPORT_PLAN_LABELS[pid] || pid, committed: 0, completed: 0, centers: [] };
      byPlan[pid].committed++;
      byPlan[pid].centers.push(name);
      if (completed[name] === pid) byPlan[pid].completed++;
    });
    const rows = Object.values(byPlan).sort((a, b) => b.committed - a.committed).map((r) => ({ ...r, rate: r.committed ? Math.round(100 * r.completed / r.committed) : 0 }));
    const totalCommitted = rows.reduce((a, r) => a + r.committed, 0);
    const totalCompleted = rows.reduce((a, r) => a + r.completed, 0);
    return { rows, totalCommitted, totalCompleted, overallRate: totalCommitted ? Math.round(100 * totalCompleted / totalCommitted) : 0 };
  }
  function buildSummaryText(centers, states, railData, dyn, opt, ledger) {
    const recs = railData.recommendations;
    const conflicts = railData.conflicts || [];
    const ageOf = (c) => opt.fresh[c.name] !== void 0 ? (opt.week - opt.fresh[c.name]) * 7 : hash(c.name) % 38 + opt.week * 2;
    const confs = centers.map((c) => qCoherence(ageOf(c)));
    const avgConf = +(confs.reduce((a, v) => a + v, 0) / confs.length).toFixed(2);
    const simpleAvgHealth = +(centers.reduce((a, c) => a + c.health, 0) / centers.length).toFixed(1);
    const totalConf = confs.reduce((a, v) => a + v, 0);
    const weightedAvgHealth = +(centers.reduce((a, c, idx) => a + c.health * confs[idx], 0) / totalConf).toFixed(1);
    const freshPct = Math.round(100 * centers.filter((c) => ageOf(c) <= 28).length / centers.length);
    const allowedN = recs.filter((r) => r.governance.allowed).length, heldN = recs.length - allowedN;
    const lines = [];
    lines.push("FRANCHISE OPERATIONS ENGINE \u2014 SUMMARY EXPORT");
    lines.push("Proposed Policy Framework \u2014 Candidate Submission");
    lines.push("Generated from the live canonical state, week " + opt.week);
    lines.push("");
    lines.push("NETWORK HEALTH");
    lines.push("  Simple average: " + simpleAvgHealth + " | Confidence-weighted: " + weightedAvgHealth);
    lines.push("  Average measurement confidence: " + avgConf + " | Units within 28-day window: " + freshPct + "%");
    lines.push("  Modeled network: " + centers.length + " centers across " + Object.keys(states).length + " states");
    lines.push("");
    lines.push("GOVERNANCE THIS CYCLE");
    lines.push("  " + recs.length + " total proposals \u2014 " + allowedN + " allowed, " + heldN + " held");
    lines.push("  Cross-agent conflicts: " + conflicts.length + (conflicts.length ? "" : " (checked, none open)"));
    conflicts.slice(0, 10).forEach((c) => lines.push("    - " + c.reason));
    lines.push("  Support paths committed: " + Object.keys(dyn.committed || {}).length + " | completed: " + Object.keys(dyn.completed || {}).length);
    lines.push("");
    lines.push("STANDING RULES");
    lines.push("  1. Preserve the whole network state before deriving any operational view.");
    lines.push("  2. Derive a view only where it serves diagnosis, prioritization, or governed action.");
    lines.push("  3. The governance boundary is where a proposal becomes real.");
    lines.push("  4. Every commitment is human-owned.");
    lines.push("");
    lines.push("RECENT ACTIVITY (last " + Math.min(40, (ledger || []).length) + " logged actions)");
    (ledger || []).slice(0, 15).forEach((e) => lines.push("  [" + e.tab + "] " + e.actor + ": " + e.text));
    if (!(ledger || []).length) lines.push("  no actions logged yet this session");
    lines.push("");
    lines.push("This export is a static snapshot for offline review. The interactive model recomputes every figure live and is the authoritative view.");
    return lines.join("\n");
  }
  var CENTER_CHAIN = [
    "Student engagement (agency, visible progress, responsive instruction)",
    "Session notes (4-part structure \u2192 parent's only window)",
    "Learning pathways (belt system \u2192 4\u20135 year runway)",
    "Multi-year retention (70%+ target \u2192 sunk acquisition cost preserved)",
    "Parent trust (notes + ceremonies + responsiveness \u2192 4.9\u2605)",
    "Student-driven learning (question-driven \u2192 Congressional App 1st place)",
    "Teacher improvement (notes gate forces reflection \u2192 better sessions)",
    "Referral (happy parents \u2192 zero-cost leads \u2192 community pipeline)",
    "Unit profitability (all of the above \u2192 the door worth opening)"
  ];
  var PLAN_90 = [
    { phase: "Days 1\u201330 \u2014 Listen & Baseline", focus: "Understand before changing anything.", items: [
      "Read every franchisee health score; identify the bottom-decile units by profitability and the top-decile by retention.",
      "Ride along with 3 Franchise Business Coaches; sit in on at-risk franchisee calls without speaking.",
      "Interview 10 franchisees across the satisfaction spectrum \u2014 what does corporate get right, what does it get wrong.",
      "Reconcile FDD Item 20 (244) against the 350+ public figure; establish the authoritative unit count.",
      "Confirm the CRM binding and map the actual development pipeline as it runs today."
    ] },
    { phase: "Days 31\u201360 \u2014 Diagnose & Pilot", focus: "Find the binding constraint; test one fix.", items: [
      "Segment the network: which units are below the unit-economics floor, and why.",
      "Identify the 2\u20133 proven practices already working at top units that aren't propagating; codify the highest-leverage one.",
      "Pilot the unit-value initiative at 3 volunteer centers: premium pricing tier + community-pipeline activation + retention back-edge.",
      "Stand up the Network Health Dashboard's leading indicators so intervention is proactive.",
      "Define the candidate readiness rubric with the Development Function."
    ] },
    { phase: "Days 61\u201390 \u2014 Systematize & Scale", focus: "Turn what worked into what's standard.", items: [
      "Measure the pilot: did per-unit revenue and retention move. If yes, package as a Best Practice Library entry.",
      "Present the growth thesis to leadership: strengthen unit economics across the existing 244 before adding the next 50.",
      "Establish the cluster events engine in one metro; prove shared community pipeline without cannibalization.",
      "Set the quarterly cadence: franchisee forums, health reviews, best-practice releases.",
      "Hand the FBCs a repeatable at-risk intervention protocol."
    ] }
  ];
  var METRICS_FULL = [
    { name: "Trial-to-enrollment conversion", lead: true, watch: "Weekly, per center", method: "Enrollments \xF7 trials booked (MyStudio)", trigger: "Below 35% for 2 weeks \u2192 Director reviews tour & day-7 follow-up", predicts: "Revenue growth" },
    { name: "Notes completion within 30 min", lead: true, watch: "Daily, per Sensei", method: "Notes sent on time \xF7 sessions delivered", trigger: "Below 90% \u2192 Sensei coaching; below 70% \u2192 staffing review", predicts: "Retention & parent trust" },
    { name: "No-show recovery rate", lead: true, watch: "Daily", method: "Same-day reschedules \xF7 missed sessions", trigger: "Below 70% \u2192 check auto-reschedule automation", predicts: "Churn" },
    { name: "At-risk resolution rate", lead: true, watch: "Weekly", method: "Saved families \xF7 flagged at-risk", trigger: "Below 50% \u2192 Director intervention review", predicts: "Multi-year retention" },
    { name: "Belt-progression velocity", lead: true, watch: "Monthly, per cohort", method: "Avg belts advanced \xF7 months enrolled", trigger: "Stalling \u2192 curriculum or engagement problem", predicts: "Long-term retention & referral" },
    { name: "Community-pipeline inflow", lead: true, watch: "Weekly, per channel", method: "Trials booked by source channel", trigger: "Dry channel \u2192 reactivate partnership effort", predicts: "Enrollment pipeline health" },
    { name: "Active students per center", lead: false, watch: "Monthly", method: "Current enrolled (MyStudio)", trigger: "Below 60 \u2192 unit below profitability floor", predicts: "\u2014 (this is the outcome)" },
    { name: "Unit net margin", lead: false, watch: "Monthly", method: "Revenue \u2212 all costs incl. royalty", trigger: "Below 25th percentile 2 months \u2192 FBC support", predicts: "\u2014 (this is the outcome)" }
  ];
  var FAILURE_MODES = [
    { mode: "Silent churn", detect: "Retention drops but no complaints \u2014 students just stop coming.", cause: "No-show recovery not firing, or notes going stale.", recover: "Instrument no-show recovery + notes completion as daily leading indicators; the at-risk gate (step 50) catches families before they're gone." },
    { mode: "Agent over-reach", detect: "An automated action commits a pricing change or refund it shouldn't.", cause: "A reserved task wasn't gated, or confidence threshold too low.", recover: "Financial materiality guardrail reserves all pricing/refund actions to humans. Audit trail makes any breach traceable." },
    { mode: "Understaffing cascade", detect: "One Sensei callout \u2192 reschedules \u2192 cold prep briefs \u2192 at-risk flags.", cause: "No coverage pool; hiring pipeline ran dry.", recover: "Sensei coverage-pool sharing + always-warm hiring pipeline (step 55)." },
    { mode: "Growth without support", detect: "New units open but franchisee satisfaction drops.", cause: "Door count outpaced FBC capacity.", recover: "Support-capacity guardrail blocks expansion the network can't service." },
    { mode: "Fabricated confidence", detect: "A report asserts a number the data can't support.", cause: "System inferred an absent value instead of flagging it.", recover: "Data-integrity guardrail: flag incomplete data, never infer. FDD Item 20 stays authoritative until reconciled." }
  ];
  var CRIT_STEPS = [1, 7, 20, 36, 42, 50, 63];
  var GRN = "#2f7a3f";
  var AMB = "#b8860b";
  var VIO = "#7a6bd8";
  var BG = "#fdfdfb";
  function hash(s) {
    let h = 0;
    for (let i = 0; i < s.length; i++) {
      h = h * 31 + s.charCodeAt(i) & 2147483647;
    }
    return h;
  }
  function rng(seed) {
    let x = seed || 1;
    return () => {
      x = x * 1103515245 + 12345 & 2147483647;
      return x / 2147483647;
    };
  }
  var pct = (v) => Math.round(v * 100);
  var fmt = (v) => (v >= 0 ? "" : "\u2212") + Math.abs(v).toFixed(1);
  var CA = ["Agoura Hills", "Aliso Viejo", "Arcadia", "Bakersfield", "Cerritos", "Chino", "Chino Hills", "Chula Vista", "Costa Mesa", "Cupertino", "Diamond Bar", "El Segundo", "Elk Grove", "Encinitas", "Encino", "Folsom", "Fountain Valley", "Fremont 1", "Fremont 2", "Garden Grove", "Hacienda Heights", "Irvine 1", "Irvine 2", "La Canada", "La Habra", "Ladera Ranch", "Livermore", "Los Alamitos", "Los Angeles", "Murrieta", "Northridge", "Placentia", "Pleasanton", "Rancho Cucamonga", "Rocklin", "Roseville", "Rolling Hills Estates", "San Diego RB", "San Diego MM", "San Diego 3", "San Gabriel", "San Jose 1", "San Jose 2", "San Ramon", "Santa Clarita", "Sherman Oaks", "Simi Valley", "Stevenson Ranch", "Sunnyvale", "Torrance", "Tracy", "Union City", "Walnut Creek", "West Covina"];
  var OTHER = [["McKinney", "TX"], ["Pearland", "TX"], ["Chesapeake", "VA"], ["Chantilly", "VA"], ["Fairfax", "VA"], ["Virginia Beach", "VA"], ["Leesburg", "VA"], ["South Tulsa", "OK"], ["Little Rock", "AR"], ["St. Joseph", "MO"]];
  var MODEL_ST = [["TX", 38], ["FL", 26], ["NJ", 18], ["NY", 16], ["IL", 14], ["WA", 12], ["AZ", 11], ["GA", 11], ["NC", 10], ["OH", 9], ["PA", 9], ["MI", 8], ["CO", 7], ["MN", 6], ["MD", 8], ["MA", 7], ["TN", 5], ["WI", 4], ["IN", 4], ["OR", 4], ["NV", 4], ["UT", 4], ["CT", 4], ["SC", 4], ["LA", 3], ["KS", 3], ["IA", 3], ["AL", 3], ["ID", 2], ["WV", 2], ["ND", 1], ["DE", 1], ["ON", 12], ["BC", 6], ["AB", 5]];
  function mkCenter(name, st, verified) {
    const h = hash(name + st), r = rng(h);
    const conv = 0.35 + r() * 0.3, ret = 0.74 + r() * 0.2, chem = 0.35 + r() * 0.45, capR = 0.26 + r() * 0.16;
    const students = 60 + Math.floor(r() * 160), eb = +(students * 0.289 * (0.35 + r() * 0.3) - (6 + r() * 14)).toFixed(1);
    const momentum = (r() > 0.45 ? "+" : "\u2212") + (1 + Math.floor(r() * 5));
    const health = Math.round(40 + conv * 30 + ret * 25 + chem * 10 - (capR > 0.38 ? 6 : 0) + (eb > 4 ? 8 : eb < 0 ? -8 : 0));
    const compliance = rng(hash(name + st + "cmp"))() > 0.03;
    const staffCleared = rng(hash(name + st + "stf"))() > 0.02;
    const wires = {};
    [["funnel", ["trials", "closeout", "referrals"]], ["dojo", ["belt pace", "stuck count", "ceremony"]], ["staff", ["ratio", "chemistry", "CIT pipeline"]], ["cash", ["tuition", "labor", "EBITDA"]]].forEach(([w, gs]) => {
      wires[w] = gs.map((n) => {
        const g = rng(hash(name + w + n));
        const days = Math.floor(g() * 38);
        return { n, v: Math.round(35 + g() * 60), days, state: days > 21 ? "super" : "meas" };
      });
    });
    return { name: st && st !== "CA" ? name + " " + st : name, st: st || "CA", verified, conv, ret, chem, capR, eb, momentum, health, students, compliance, staffCleared, wires };
  }
  function buildCenters() {
    const out = CA.map((n) => mkCenter(n, "CA", true));
    OTHER.forEach(([n, st]) => out.push(mkCenter(n, st, true)));
    MODEL_ST.forEach(([st, k]) => {
      for (let i = 1; i <= k; i++) out.push(mkCenter(st + " Unit " + i, st, false));
    });
    return out;
  }
  function engageOf(c) {
    const r = rng(hash(c.name + "eng"));
    const pre = 0.6 + r() * 0.38, post = 0.55 + r() * 0.4, badges = Math.floor(r() * 9), match = 0.5 + r() * 0.48;
    const path = [0.32 + r() * 0.2, 0.3 + r() * 0.15, 0];
    path[2] = Math.max(0.05, 1 - path[0] - path[1]);
    const prog30 = 0.45 + r() * 0.5, commit = [0.45 + r() * 0.2, 0.25 + r() * 0.15, 0];
    commit[2] = Math.max(0.04, 1 - commit[0] - commit[1]);
    const refer = 0.15 + r() * 0.45, buzz = 0.4 + r() * 0.55;
    const score = +(0.2 * pre + 0.2 * post + 0.15 * match + 0.15 * prog30 + 0.15 * (commit[1] + commit[2]) + 0.15 * buzz).toFixed(2);
    return { pre, post, badges, match, path, prog30, commit, refer, buzz, score };
  }
  var tierOf = (o) => o >= 88 ? "TOP" : o >= 80 ? "STRONG" : o >= 68 ? "STABLE" : "PRIORITY";
  var PATH = [["jr", "JR"], ["white", "White"], ["yellow", "Yellow"], ["orange", "Orange"], ["green", "Green"], ["blue", "Blue"], ["purple", "Purple"], ["brown", "Brown"], ["red", "Red"], ["black", "Black"]];
  var COHORT = { jr: [420, 38, 9], white: [730, 96, 14], yellow: [560, 71, 24], orange: [470, 88, 11], green: [350, 52, 19], blue: [260, 31, 8], purple: [170, 22, 26], brown: [110, 14, 6], red: [70, 9, 17], black: [38, 4, 3] };
  var PROJECTS = [
    { id: "aicore", n: "Premium tier $269", stages: [["Design", 0, 4], ["Pilot", 1, 9], ["Measure", 2, 26], ["Wave", 0, 2], ["Scale", 0, 0]] },
    { id: "schools", n: "School pipeline", stages: [["Map", 0, 3], ["Pilot", 1, 12], ["Measure", 2, 31], ["Scale", 0, 0]] },
    { id: "boxscore", n: "Progress report", stages: [["Design", 0, 2], ["Ship", 1, 5], ["Measure", 1, 2], ["Network", 0, 0]] },
    { id: "cit", n: "CIT draft", stages: [["Identify", 0, 6], ["Train", 1, 8], ["Deploy", 0, 0]] },
    { id: "fll", n: "FIRST LEGO League", stages: [["Register", 1, 4], ["Form teams", 0, 0], ["Scrimmage", 0, 0], ["Qualifier", 0, 0]] }
  ];
  var CAL = [
    ["Measurement", [[3, "QBR wave A", 1], [10, "Reports due", 1], [31, "QBR wave B", 0], [59, "QBR wave C", 0]]],
    ["Premium tier", [[5, "Wave-2 open", 1], [19, "Pilot readout", 0], [47, "Wave-3 gate", 0], [75, "Scale review", 0]]],
    ["Events", [[7, "PNO", 0], [30, "Division jam", 0], [67, "PNO", 0], [84, "Network finals", 0]]],
    ["FLL (final FIRST\xB7LEGO season)", [[1, "Register \u2014 OPEN now", 1], [33, "Kickoff Aug 4", 1], [62, "Team build + scrimmage", 0], [88, "Prep regionals (Dec)", 0]]],
    ["Community", [[14, "School STEM night", 0], [21, "Demo day", 0], [42, "Festival table", 0], [63, "Partnership fair", 0]]],
    ["Trades", [[9, "Match run", 0], [39, "30d re-measure", 1], [69, "Match run", 0]]],
    ["Governance", [[1, "Constitution freeze", 1], [45, "Changelog audit", 0], [90, "Quarter close", 0]]]
  ];
  var GRIEV = [["profit", "Profitability", 1.6], ["support", "Support", 1.9], ["leads", "Leads", 2.3], ["terr", "Territory", 2.6], ["tech", "Tech", 2.5], ["comms", "Comms", 2.2]].map(([id, n, sat]) => ({ id, n, sat, sev: +((5 - sat) / 5).toFixed(2) }));
  var MECHS = [["growthM", "Growth model", "growth"], ["transferM", "Transfer board", "transfers"], ["capM", "Cap governor", "team"], ["measM", "Measure cadence", "optimizer"], ["eventsM", "Events engine", "calendar"], ["boxM", "Progress report", "mastery"], ["govM", "Five governors", "portfolio"], ["agendaM", "Agenda+log", "agenda"]].map(([id, n, tab]) => ({ id, n, tab }));
  var PEDGES = [["growthM", "profit", 0.9], ["capM", "profit", 0.7], ["boxM", "profit", 0.6], ["transferM", "profit", 0.5], ["measM", "support", 0.9], ["agendaM", "support", 0.7], ["transferM", "support", 0.6], ["eventsM", "leads", 0.8], ["boxM", "leads", 0.4], ["govM", "terr", 0.95], ["measM", "tech", 0.5], ["transferM", "tech", 0.5], ["agendaM", "comms", 0.8], ["measM", "comms", 0.6]];
  var LEV = MECHS.map((m) => ({ ...m, lev: +PEDGES.filter((e) => e[0] === m.id).reduce((a, [, g, w]) => a + w * GRIEV.find((x) => x.id === g).sev, 0).toFixed(2) })).sort((a, b) => b.lev - a.lev);
  function forecast(c) {
    const mom = (c.momentum[0] === "+" ? 1 : -1) * parseInt(c.momentum.slice(1));
    const slope = +(mom * 0.22 + (c.chem - 0.55) * 3.2 + (c.ret - 0.82) * 4.5).toFixed(2);
    const proj = [1, 2, 3].map((m) => +(c.eb + slope * m).toFixed(1));
    const capDrift = c.chem < 0.5 ? 6e-3 : -2e-3;
    const ebBreach = c.eb > 0 && slope < 0 ? Math.ceil(c.eb / -slope * 30) : c.eb <= 0 ? 0 : null;
    const capBreach = c.capR < 0.4 && capDrift > 0 ? Math.ceil((0.4 - c.capR) / capDrift * 30) : c.capR >= 0.4 ? 0 : null;
    const cls = slope > 0.3 ? "rising" : slope < -0.3 ? "deteriorating" : "holding";
    return { slope, proj, ebBreach, capBreach, cls };
  }
  function alertsOf(centers) {
    const out = [];
    centers.forEach((c) => {
      const f = forecast(c);
      const healthScore = c.health;
      const churnRate = c.momentum.startsWith("-") ? Math.abs(parseInt(c.momentum.slice(1))) : 0;
      const instructorUtilization = c.chem;
      const capApproaching = c.capR > 0.85;
      const arpu = c.eb * 100 / (c.students || 1);
      if (healthScore < 40 || churnRate > 40 || instructorUtilization >= 1) {
        out.push({ c, eta: 1, ev: "CRITICAL", severity: "CRITICAL", agent: "governance", play: "immediate intervention" });
      } else if (healthScore < 55 || churnRate > 30 || instructorUtilization > 0.92) {
        out.push({ c, eta: 3, ev: "HIGH", severity: "HIGH", agent: "governance", play: "playbook + check-in" });
      } else if (f.cls === "deteriorating" || capApproaching || healthScore < 70) {
        out.push({ c, eta: 7, ev: "MEDIUM", severity: "MEDIUM", agent: "governance", play: "dashboard notification" });
      }
      if (f.ebBreach !== null && f.ebBreach <= 90) out.push({ c, eta: f.ebBreach, ev: "EBITDA<0", agent: "pipeline", play: "margin diagnostic" });
      if (f.capBreach !== null && f.capBreach <= 90) out.push({ c, eta: f.capBreach, ev: "labor>40%", agent: "cap", play: "utilization" });
      if (c.chem < 0.45 && f.cls !== "rising") out.push({ c, eta: 45, ev: "sensei exit risk", agent: "staffing", play: "retain+CIT" });
    });
    return out.sort((a, b) => a.eta - b.eta).slice(0, 50);
  }
  function seedABTests(centers) {
    const tests = [];
    if (centers.length < 20) return [];
    const smallMarket = centers.filter((c) => c.students < 60).slice(0, 15);
    const mediumMarket = centers.filter((c) => c.students >= 60 && c.students < 120).slice(0, 20);
    const largeMarket = centers.filter((c) => c.students >= 120).slice(0, 20);
    if (smallMarket.length >= 15) {
      const testCenters = smallMarket.sort(() => Math.random() - 0.5).slice(0, 15);
      tests.push({
        test_id: "pricing_small_219",
        hypothesis: "Reduce Classic pricing from $229 to $219 in small markets \u2192 enrollment +3-5%",
        centers: testCenters.map((c) => c.name),
        metric: "enrollment_change",
        duration_weeks: 4,
        control_centers: mediumMarket.slice(0, 15).map((c) => c.name),
        started_week: 0,
        status: "active"
      });
    }
    if (centers.length >= 8) {
      const roboticsCenters = centers.filter((c) => !c.name.includes("Small")).sort(() => Math.random() - 0.5).slice(0, 8);
      tests.push({
        test_id: "robotics_first_cohort",
        hypothesis: "Enroll cohorts in Robotics first (vs. Scratch) \u2192 completion rate +4-6%",
        centers: roboticsCenters.map((c) => c.name),
        metric: "completion_rate",
        duration_weeks: 12,
        control_centers: centers.filter((c) => !roboticsCenters.includes(c)).slice(0, 8).map((c) => c.name),
        started_week: 0,
        status: "active"
      });
    }
    if (mediumMarket.length >= 20) {
      const messagingCenters = mediumMarket.slice(0, 40);
      tests.push({
        test_id: "elite_messaging_v2",
        hypothesis: "New messaging for Elite tier (project-based, career focus) \u2192 tier migration +3%",
        centers: messagingCenters.map((c) => c.name),
        metric: "tier_migration_rate",
        duration_weeks: 6,
        control_centers: centers.filter((c) => !messagingCenters.includes(c)).slice(0, 40).map((c) => c.name),
        started_week: 0,
        status: "active"
      });
    }
    return tests;
  }
  var AGENT_LEX = { score: "Measurement", transfer: "Best practice", wave: "Rollout", peer: "Peer review", fcst: "Forecast", gate: "Approval", view: "Reporting", cap: "Labor governor", pipeline: "Enrollment", staffing: "Staff retention" };
  var VERB_LEX = { measure: "Measure", transfer: "Transfer", schedule: "Schedule", ship: "Send", hold: "Escalate", review: "Cross-review", promote: "Adopt", retire: "Remove" };
  var LEAD_NAMES = ["Ramirez Group", "Okafor Holdings", "Chen Family Trust", "Delgado Ventures", "Patel Education LLC", "Boudreaux Partners", "Nguyen Capital", "Whitfield & Co.", "Silva Learning", "Kowalski Group", "Abara Holdings", "Fitzgerald Trust", "Moreno Ventures", "Yamamoto LLC", "Brennan Partners", "Adeyemi Group", "Costa Family", "Larsson Capital", "Haddad Holdings", "Ivanova Trust"];
  var LEAD_SRC = ["Franchise portal", "Discovery Day referral", "Existing-franchisee referral", "Trade show", "Resale inquiry", "Multi-unit expansion", "Broker network", "Web inquiry"];
  var LEAD_REGION = ["TX", "FL", "ON", "GA", "NC", "AZ", "WA", "NJ", "open territory"];
  var STAGES6 = ["Intro call", "Discovery", "FDD review", "Due diligence", "Discovery Day", "Signed"];
  var CANADIAN_PROVINCES = ["ON", "BC", "AB"];
  function marketOf(region) {
    return CANADIAN_PROVINCES.includes(region) ? "Canada" : "US";
  }
  function disclosureLawOf(region) {
    if (region === "ON") return "Arthur Wishart Act (Ontario) \u2014 Disclosure Document, 14-day minimum";
    if (region === "BC") return "Franchises Act (British Columbia) \u2014 Disclosure Document, 14-day minimum";
    if (region === "AB") return "Franchises Act (Alberta) \u2014 Disclosure Document, 14-day minimum";
    return "FTC Franchise Rule (US) \u2014 FDD, 14-day minimum";
  }
  function buildLeads() {
    return LEAD_NAMES.map((n, i) => {
      const r = rng(hash(n + "lead"));
      const liquidity = Math.round(150 + r() * 600);
      const net = Math.round(liquidity * (1.5 + r() * 3));
      const multi = r() > 0.6, proven = r() > 0.72;
      const fit = Math.min(99, Math.round(38 + r() * 58 + (proven ? 12 : 0) + (multi ? 6 : 0)));
      const region = LEAD_REGION[i % LEAD_REGION.length], src = LEAD_SRC[i % LEAD_SRC.length];
      const stage0 = Math.floor(r() * 5);
      const dealType = proven ? "conversion" : "new";
      const market = marketOf(region);
      return {
        id: i,
        n,
        liquidity,
        net,
        multi,
        proven,
        fit,
        region,
        src,
        stage0,
        hot: fit >= 78,
        dealType,
        market,
        note: proven ? "proven operator \u2014 ramp inheritance applies" : multi ? "multi-unit intent" : liquidity < 220 ? "liquidity light \u2014 verify financing" : "single-unit prospect"
      };
    }).sort((a, b) => b.fit - a.fit);
  }
  var QSTATES = ["thriving", "watch", "at-risk"];
  var ESSENTIAL_TABS = ["quantum", "exec", "franchise", "rail", "board", "alignment", "compliance", "blockers", "table", "whitespace", "leads", "deals", "workload", "team", "lenses", "network", "portfolio", "financials", "growthfin", "onboarding", "audit", "agenda", "risk", "fdd"];
  var QCOL = ["#5cb87c", "#d9a62e", "#d96a6a"];
  var QCLUSTERS = [["CA", "OR", "WA", "NV"], ["TX", "OK", "LA"], ["NY", "NJ", "CT", "MA"], ["FL", "GA", "NC", "SC"], ["IL", "IN", "OH", "MI"], ["VA", "MD", "PA"]];
  function qAmp(c) {
    const e = engageOf(c);
    const good = Math.max(0.05, 0.5 * e.score + 0.5 * c.ret);
    const bad = Math.max(0.05, 0.5 * (1 - e.score) + 0.5 * (1 - c.ret));
    const watch = Math.max(0.08, 1 - Math.abs(good - bad));
    const s = good + watch + bad;
    return [good / s, watch / s, bad / s];
  }
  function qStateAmp(cs) {
    if (!cs || !cs.length) return [0.34, 0.33, 0.33];
    const a = [0, 0, 0];
    cs.forEach((c) => {
      const q = qAmp(c);
      a[0] += q[0];
      a[1] += q[1];
      a[2] += q[2];
    });
    const s = a[0] + a[1] + a[2];
    return [a[0] / s, a[1] / s, a[2] / s];
  }
  function qCoherence(days) {
    return Math.max(0.12, Math.exp(-days / 42));
  }
  function qResolve(amp) {
    return amp.indexOf(Math.max(...amp));
  }
  function qZone(c) {
    const occ = Math.min(1, c.students / 160);
    return occ >= 0.72 ? { zone: "self-stabilizing", occ, note: "near capacity \u2014 performance holds steady" } : occ >= 0.5 ? { zone: "transitional", occ, note: "partial stability" } : { zone: "drifting", occ, note: "low occupancy \u2014 state drifts between measurements" };
  }
  function qTau(c) {
    const mom = (c.momentum[0] === "+" ? 1 : -1) * parseInt(c.momentum.slice(1));
    const stress = (c.eb < 0 ? 1.6 : c.eb < 4 ? 0.8 : 0) + (mom < 0 ? 0.9 : 0) + (c.capR > 0.38 ? 0.5 : 0);
    return +(0.7 + stress + hash(c.name + "tau") % 10 / 12).toFixed(1);
  }
  function qCrit(c) {
    return +(c.eb - 0).toFixed(1);
  }
  var QPAIRS = [["Premium tier", "CIT pipeline", 0.62], ["Premium tier", "School pipeline", 0.34], ["CIT pipeline", "FIRST LEGO League", 0.55], ["Premium tier", "FIRST LEGO League", -0.18], ["School pipeline", "FIRST LEGO League", 0.41], ["CIT pipeline", "School pipeline", 0.22]];
  function qPair(c) {
    const i = hash(c.name + "pair") % QPAIRS.length;
    return QPAIRS[i];
  }
  function qBalance(c) {
    const gain = +(0.02 + engageOf(c).score * 0.02).toFixed(3);
    const cost = +(0.02 + (c.capR > 0.38 ? 0.012 : 4e-3)).toFixed(3);
    return { gain, cost, net: +(gain - cost).toFixed(3) };
  }
  function qDiag(c) {
    return { zone: qZone(c), tau: qTau(c), crit: qCrit(c), pair: qPair(c), bal: qBalance(c) };
  }
  var QFLOORS = { engagement: 0.35, margin_ebitda_k: 6, staleness_days: 14 };
  var SCENARIO_DELTAS = {
    optimistic: { conv: 0.05, ret: 0.03, chem: 0.02, eb: 0.6 },
    realistic: { conv: 0, ret: 0, chem: 0, eb: 0 },
    pessimistic: { conv: -0.05, ret: -0.03, chem: -0.02, eb: -0.6 }
  };
  var WEEKLY_DRIFT_STEP = { conv: 15e-4, ret: 1e-3, chem: 8e-4, eb: 0.02 };
  var postureSign = (p) => p === "optimistic" ? 1 : p === "pessimistic" ? -1 : 0;
  function weekCompound(posture, week) {
    const sign = postureSign(posture), w = week || 0;
    return { conv: sign * WEEKLY_DRIFT_STEP.conv * w, ret: sign * WEEKLY_DRIFT_STEP.ret * w, chem: sign * WEEKLY_DRIFT_STEP.chem * w, eb: sign * WEEKLY_DRIFT_STEP.eb * w };
  }
  function computeCentersForPosture(base, adj, posture, week) {
    const sd = SCENARIO_DELTAS[posture] || SCENARIO_DELTAS.realistic;
    const wk = weekCompound(posture, week);
    return base.map((c) => {
      const d = adj[c.name];
      const conv0 = c.conv + sd.conv + wk.conv, ret0 = c.ret + sd.ret + wk.ret, chem0 = c.chem + sd.chem + wk.chem, eb0 = c.eb + sd.eb + wk.eb;
      if (!d && posture === "realistic") return c;
      const conv = Math.min(0.9, Math.max(0.1, conv0 + (d && d.conv || 0)));
      const ret = Math.min(0.98, Math.max(0.5, ret0 + (d && d.ret || 0)));
      const chem = Math.min(0.95, Math.max(0.1, chem0 + (d && d.chem || 0)));
      const eb = +(eb0 + (d && d.eb || 0)).toFixed(1);
      const health = Math.round(40 + conv * 30 + ret * 25 + chem * 10 - (c.capR > 0.38 ? 6 : 0) + (eb > 4 ? 8 : eb < 0 ? -8 : 0));
      return { ...c, conv, ret, chem, eb, health, adjusted: true, posture };
    });
  }
  function computeOverrideView(rawCenters, adj, posture, week, leads) {
    const c = computeCentersForPosture(rawCenters, adj, posture, week);
    const st = {};
    c.forEach((x) => {
      (st[x.st] = st[x.st] || []).push(x);
    });
    return { centers: c, states: st, railData: runAllAgents(c, st, leads) };
  }
  function severityOf(distance, scale) {
    if (distance >= 0) return "clear";
    const ratio = Math.abs(distance) / scale;
    return ratio < 0.15 ? "narrow miss" : ratio < 0.5 ? "held" : "far off";
  }
  function qGovernors(c, daysSinceMeasure, stalenessFloorDays) {
    const eng = engageOf(c).score, mar = c.eb, cleared = c.compliance !== false && c.staffCleared !== false, d = daysSinceMeasure;
    const floor = stalenessFloorDays == null ? QFLOORS.staleness_days : stalenessFloorDays;
    const marDist = typeof mar === "number" ? +(mar - QFLOORS.margin_ebitda_k).toFixed(2) : null;
    const engDist = typeof eng === "number" ? +(eng - QFLOORS.engagement).toFixed(3) : null;
    const dataDist = typeof d === "number" ? +(floor - d).toFixed(1) : null;
    return [
      { id: "financial_materiality", label: "Financial materiality", pass: typeof mar === "number" && mar >= QFLOORS.margin_ebitda_k, distance: marDist, severity: marDist == null ? null : severityOf(marDist, QFLOORS.margin_ebitda_k || 1), detail: mar >= QFLOORS.margin_ebitda_k ? "margin $" + mar + "k \u2265 $" + QFLOORS.margin_ebitda_k + "k floor (+$" + marDist + "k)" : "margin $" + mar + "k below $" + QFLOORS.margin_ebitda_k + "k floor ($" + marDist + "k)" },
      { id: "engagement_integrity", label: "Engagement integrity", pass: typeof eng === "number" && eng >= QFLOORS.engagement, distance: engDist, severity: engDist == null ? null : severityOf(engDist, QFLOORS.engagement || 1), detail: eng >= QFLOORS.engagement ? "engagement " + eng.toFixed(2) + " \u2265 " + QFLOORS.engagement : "engagement " + eng.toFixed(2) + " below " + QFLOORS.engagement + " floor" },
      { id: "child_safety", label: "Child safety", pass: cleared === true, distance: cleared === true ? 0 : -1, severity: cleared === true ? "clear" : "far off", detail: cleared === true ? "staff cleared" : "clearance not confirmed \u2014 hold" },
      { id: "data_integrity", label: "Data integrity", pass: typeof d === "number" && d >= 0 && d <= floor, distance: dataDist, severity: dataDist == null ? null : severityOf(dataDist, floor || 1), detail: typeof d === "number" && d <= floor ? "measured " + d + "d ago \u2264 " + floor + "d" : "stale " + d + "d \u2014 measure before acting" }
    ];
  }
  function qGate(c, daysSinceMeasure) {
    const v = qGovernors(c, daysSinceMeasure);
    const held = v.filter((g) => !g.pass);
    return { allow: held.length === 0, verdicts: v, held };
  }
  function dataConfidenceOf(daysSinceMeasure) {
    return qCoherence(daysSinceMeasure == null ? 30 : daysSinceMeasure);
  }
  function uncertaintyBandOf(c) {
    const amp = qAmp(c);
    const spread = Math.max(...amp) - Math.min(...amp);
    return spread >= 0.45 ? "low" : spread >= 0.22 ? "medium" : "high";
  }
  function peerConnectionStrengthOf(c, states) {
    const cluster = QCLUSTERS.find((cl) => cl.includes(c.st));
    if (!cluster) return 0.2;
    const peers = cluster.filter((st) => st !== c.st).flatMap((st) => states[st] || []);
    if (!peers.length) return 0.3;
    const peerAvg = peers.reduce((a, p) => a + p.health, 0) / peers.length;
    return +Math.max(0.1, Math.min(1, peerAvg / 100)).toFixed(2);
  }
  function patternStabilityIndexOf(c) {
    const m = [c.conv / 0.65, c.ret / 0.94, c.chem / 0.8];
    const mu = (m[0] + m[1] + m[2]) / 3;
    const varr = m.reduce((s, x) => s + (x - mu) * (x - mu), 0) / 3;
    return +Math.max(0, Math.min(1, 1 - varr * 6)).toFixed(2);
  }
  function networkSupportIndexOf(c, states) {
    const peerStrength = peerConnectionStrengthOf(c, states);
    const activePrograms = CONNECTIVITY.filter((x) => x.n).length;
    return +Math.max(0.1, Math.min(1, peerStrength * 0.7 + Math.min(1, activePrograms / CONNECTIVITY.length) * 0.3)).toFixed(2);
  }
  function structureScoreOf(c, daysSinceMeasure) {
    const v = qGovernors(c, daysSinceMeasure);
    return +(v.filter((g) => g.pass).length / v.length).toFixed(2);
  }
  function operationalSignalsOf(c, states, daysSinceMeasure) {
    return {
      dataConfidence: dataConfidenceOf(daysSinceMeasure),
      uncertaintyBand: uncertaintyBandOf(c),
      peerConnectionStrength: peerConnectionStrengthOf(c, states),
      patternStabilityIndex: patternStabilityIndexOf(c),
      networkSupportIndex: networkSupportIndexOf(c, states),
      structureScore: structureScoreOf(c, daysSinceMeasure)
    };
  }
  var ACTION_CONTRACTS = {
    "expansion": {
      requiredMetrics: ["readinessScore", "netMargin", "students", "supportCapacity"],
      minFreshnessDays: 28,
      // ~4 weeks
      guardrails: ["financial_materiality", "data_integrity", "structure"],
      approverRole: "Director"
    },
    "pricing-change": {
      requiredMetrics: ["netMargin", "students", "priceCeiling"],
      minFreshnessDays: 28,
      guardrails: ["financial_materiality", "data_integrity"],
      approverRole: "Director"
    },
    "support-plan": {
      requiredMetrics: ["healthScore", "engagementIndex", "staffingStress"],
      minFreshnessDays: 45,
      guardrails: ["engagement_integrity", "child_safety", "data_integrity"],
      approverRole: "FBC"
    },
    "outreach": {
      requiredMetrics: ["retentionRate", "engagementIndex"],
      minFreshnessDays: 60,
      guardrails: ["engagement_integrity", "child_safety"],
      approverRole: "Owner"
    }
  };
  function checkContract(actionType, c, daysSinceMeasure) {
    const contract = ACTION_CONTRACTS[actionType];
    if (!contract) return { allowed: false, reason: "unknown action type: " + actionType };
    const verdicts = qGovernors(c, daysSinceMeasure, contract.minFreshnessDays).filter((g) => contract.guardrails.includes(g.id));
    const freshOk = typeof daysSinceMeasure === "number" && daysSinceMeasure <= contract.minFreshnessDays;
    const held = verdicts.filter((g) => !g.pass);
    const allowed = held.length === 0 && freshOk;
    return {
      allowed,
      approverRole: contract.approverRole,
      verdicts,
      freshnessOk: freshOk,
      freshnessLimit: contract.minFreshnessDays,
      heldOn: held.map((g) => g.label).concat(freshOk ? [] : ["data freshness"])
    };
  }
  function defaultDaysSinceMeasure(c) {
    return hash(c.name) % 38;
  }
  function confidenceBandOf(dataConfidence) {
    return dataConfidence >= 0.6 ? "high" : dataConfidence >= 0.35 ? "medium" : "low";
  }
  function riskBandOf(structureScore, patternStabilityIndex) {
    const avg = (structureScore + patternStabilityIndex) / 2;
    return avg >= 0.75 ? "low" : avg >= 0.45 ? "medium" : "high";
  }
  var AGENT = {
    growth: "Franchise Growth",
    health: "Center Performance",
    vitality: "Team Vitality",
    hub: "Best Practices Hub"
  };
  var AGENT_LIST = Object.values(AGENT);
  var _recSeq = 0;
  function buildRecommendation({ agent, scope, targetIds, title, summary, actionType, center, states, daysSinceMeasure, suggestedActionPlan, agentTag, extraHold, confidenceScore, scenarioViability, propagationConfidence }) {
    const sig = operationalSignalsOf(center, states, daysSinceMeasure);
    const contract = checkContract(actionType, center, daysSinceMeasure);
    const extraBlocked = !!(extraHold && extraHold.blocked);
    const allowed = contract.allowed && !extraBlocked;
    const heldOn = extraBlocked ? contract.heldOn.concat([extraHold.reason]) : contract.heldOn;
    const safetyKernel = {
      neverAllow: [],
      requiresEscalation: []
    };
    if (actionType === "expansion" && extraHold?.liquidity < 150) {
      safetyKernel.requiresEscalation.push("Liquidity near floor \u2014 requires Director approval, cannot auto-approve");
    }
    if (actionType === "support-plan" && daysSinceMeasure > 60) {
      safetyKernel.requiresEscalation.push("Stale data (" + daysSinceMeasure + " days) \u2014 measurement required before execution");
    }
    return {
      id: "rec-" + _recSeq++ + "-" + agent.replace(/\s+/g, "").toLowerCase(),
      agent,
      scope,
      targetIds,
      title,
      summary,
      actionType,
      evidence: {
        dataFreshnessDays: daysSinceMeasure,
        operationalSignals: sig,
        contractVerdicts: contract.verdicts
      },
      confidenceBand: confidenceBandOf(sig.dataConfidence),
      riskBand: riskBandOf(sig.structureScore, sig.patternStabilityIndex),
      requiresGate: true,
      approverRole: contract.approverRole,
      suggestedActionPlan: suggestedActionPlan || null,
      agentTags: [agentTag],
      supplyGate: extraHold || null,
      governance: { allowed, heldOn, freshnessOk: contract.freshnessOk },
      // NEW: Formal execution state + safety kernel
      executionState: "proposed",
      // proposed→approved→executing→completed→verified
      auditTrail: [],
      // [{action,ts,actor,reason}]
      safetyKernel,
      // cannot be bypassed
      // NEW: Agent-specific enhancements from papers
      confidenceScore: confidenceScore || 0.7,
      // feedback-loop confidence (Retention)
      scenarioViability: scenarioViability || null,
      // scenario forecasts (Unit Health)
      propagationConfidence: propagationConfidence || 0.6
      // pair-learning confidence (Network Propagation)
    };
  }
  var SOLVER_EXPLANATIONS = {
    leadRanking: {
      name: "Smart Expansion Picker",
      summary: "Ranks expansion opportunities by fit, money available, and operator track record \u2014 respects territory limits to avoid cannibalizing each other",
      logic: [
        "Rule 1: Fit score (0-100) \u2014 how well the operator matches this territory's needs",
        "Rule 2: Liquidity check ($200k minimum) \u2014 operator has cash reserves to weather startup",
        "Rule 3: Proven operator? Get a discount on the cash requirement (they have a track record)",
        "Rule 4: Territory capacity \u2014 each region can absorb 1-3 new locations per cycle without cannibalization",
        "Pick: Best leads ranked 1-12, subject to all rules"
      ]
    },
    fbcAllocation: {
      name: "Triage by Urgency (Health-Based)",
      summary: "Assigns 10 FBC support slots to the centers in deepest trouble \u2014 scores by health decline speed + margin depth",
      logic: [
        "Measure: How bad is each center right now? Health score (0-100) + margin ($k)",
        "Urgency: Centers losing health faster get priority (momentum matters)",
        "Severity score: 60% health + 40% margin loss = overall risk",
        "Sort by urgency (worst first), fill 10 FBC slots with top 10",
        "Hold: Other at-risk centers wait their turn (no capacity)",
        "Benefit: Saves the sickest centers first, not just whoever called first"
      ]
    },
    conflictResolver: {
      name: "Priority Resolver (When Proposals Clash)",
      summary: "When Growth wants to expand into a territory AND Unit Health says that territory is failing, decides which matters more right now",
      logic: [
        "The clash: Growth says 'expand into Boston' but Health says 'Boston is collapsing'",
        "Scoring: Growth proposals worth 100 points, Health interventions worth 80, Retention risk interventions worth 60",
        "Decision: Pick the higher-value proposal, defer the lower one until conditions change",
        "Rationale: Can't grow into a sinking ship; fix the center first, expand after it stabilizes",
        "Result: Director sees the conflict, the recommendation, and why one wins"
      ]
    }
  };
  function solveLeadRankingQUBO(leads, regionCapacities, states) {
    const N = leads.length;
    const regions = Object.keys(regionCapacities || {});
    const scores = leads.map((l, i) => {
      const regionCap = regionCapacities[l.region] || 3;
      const baseScore = l.fit;
      const liquidityPenalty = l.liquidity < GROWTH_QUALITY_LIQUIDITY_MIN ? -50 : 0;
      const fitPenalty = l.fit < (l.multi ? GROWTH_MULTI_FIT_MIN : GROWTH_QUALITY_FIT_MIN) ? -100 : 0;
      const provenBonus = l.proven ? 5 : 0;
      return { idx: i, lead: l, score: baseScore + liquidityPenalty + fitPenalty + provenBonus };
    });
    const sorted = scores.sort((a, b) => b.score - a.score);
    const regionUsed = {};
    const selected = [];
    sorted.forEach((s) => {
      const region = s.lead.region;
      const used = regionUsed[region] || 0;
      const cap = regionCapacities && regionCapacities[region] || 3;
      if (used < cap) {
        regionUsed[region] = used + 1;
        selected.push(s.lead);
      }
    });
    return selected.slice(0, 12);
  }
  function solveFBCAllocationRQAOA(centersAtRisk, capacityLimit = 10) {
    const scored = centersAtRisk.map((c) => {
      const healthScore = (100 - c.health) / 100;
      const marginSeverity = Math.max(0, (0 - c.eb) / 10);
      const urgencyWeight = healthScore * 0.6 + marginSeverity * 0.4;
      return { center: c, urgency: urgencyWeight, days: c.daysSinceMeasure || 0 };
    });
    const sorted = scored.sort((a, b) => b.urgency - a.urgency);
    const allocated = sorted.slice(0, Math.min(capacityLimit, sorted.length));
    return allocated;
  }
  function solveConflictIndependentSet(recs, conflicts) {
    if (!conflicts || conflicts.length === 0) return [];
    const conflictGraph = {};
    conflicts.forEach((c) => {
      const w = c.weight || 0.5;
      if (!conflictGraph[c.a]) conflictGraph[c.a] = [];
      if (!conflictGraph[c.b]) conflictGraph[c.b] = [];
      conflictGraph[c.a].push({ neighbor: c.b, weight: w });
      conflictGraph[c.b].push({ neighbor: c.a, weight: w });
    });
    const agentPriority = { [AGENT.growth]: 100, [AGENT.health]: 80, [AGENT.vitality]: 60, [AGENT.hub]: 40 };
    const nodeWeight = recs.reduce((m, r) => {
      const base = agentPriority[r.agent] || 50;
      const conf = r.confidenceScore || 0.7;
      m[r.id] = base * conf;
      return m;
    }, {});
    const selected = /* @__PURE__ */ new Set();
    const blocked = /* @__PURE__ */ new Set();
    const sorted = Object.keys(conflictGraph).sort((a, b) => (nodeWeight[b] || 0) - (nodeWeight[a] || 0));
    sorted.forEach((recId) => {
      if (blocked.has(recId) || selected.has(recId)) return;
      selected.add(recId);
      (conflictGraph[recId] || []).forEach(({ neighbor, weight }) => {
        if (weight >= 0.9) {
          blocked.add(neighbor);
        } else {
          const ratio = (nodeWeight[neighbor] || 0) / Math.max(1, nodeWeight[recId] || 1);
          if (ratio < 0.6) blocked.add(neighbor);
        }
      });
    });
    return conflicts.map((c) => {
      const aSelected = selected.has(c.a);
      const bSelected = selected.has(c.b);
      const aRec = recs.find((r) => r.id === c.a);
      const bRec = recs.find((r) => r.id === c.b);
      const winner = aSelected && !bSelected ? c.a : !aSelected && bSelected ? c.b : aSelected && bSelected ? c.a : null;
      const loser = winner === c.a ? c.b : winner === c.b ? c.a : null;
      const aAgentName = aRec ? aRec.agent : "Unknown";
      const bAgentName = bRec ? bRec.agent : "Unknown";
      const isHard = (c.weight || 0.5) >= 0.9;
      return {
        ...c,
        winner,
        loser,
        winnerAgent: winner === c.a ? aAgentName : winner === c.b ? bAgentName : null,
        loserAgent: winner === c.a ? bAgentName : winner === c.b ? aAgentName : null,
        edgeType: isHard ? "hard" : "soft",
        resolution: winner ? "\u2713 " + (winner === c.a ? aAgentName : bAgentName) + " proposal approved (weight " + (nodeWeight[winner] || 0).toFixed(0) + "). \u23F8 " + (winner === c.a ? bAgentName : aAgentName) + " deferred." : aSelected && bSelected ? "Both approved \u2014 soft conflict edge (" + (c.weight || 0.5).toFixed(1) + "), coexistence permitted." : "Both held for manual review."
      };
    });
  }
  var GROWTH_QUALITY_FIT_MIN = 70;
  var GROWTH_QUALITY_LIQUIDITY_MIN = 200;
  function territoryCapacityOf(headroom) {
    return Math.max(1, Math.round(headroom * 10));
  }
  var GROWTH_PROVEN_LIQUIDITY_DISCOUNT = 0.7;
  var GROWTH_MULTI_FIT_MIN = 75;
  var territoryAllocationWeights = {};
  function contextualTerritoryCapacity(region, baseCapacity, centers) {
    const t = territoryAllocationWeights[region] || { weight: 1 };
    const avgHealth = centers.reduce((s, c) => s + c.health, 0) / (centers.length || 1);
    const healthMultiplier = avgHealth > 70 ? 1.2 : avgHealth > 55 ? 1 : 0.7;
    const weightedCapacity = baseCapacity * t.weight * healthMultiplier;
    return Math.max(1, Math.round(weightedCapacity));
  }
  function growthAgentRecommend(centers, states, leads, daysFn = defaultDaysSinceMeasure) {
    const out = [];
    const candidates = [...leads].filter((l) => l.stage0 < 5);
    const regionCapacities = Object.keys(states).reduce((m, region) => {
      const regionCenters = states[region] || [];
      const netState = NET_STATES.find((s) => s.s === region);
      const baseHeadroom = netState ? netState.h[0] : 0.3;
      const baseCapacity = territoryCapacityOf(baseHeadroom);
      m[region] = contextualTerritoryCapacity(region, baseCapacity, regionCenters);
      return m;
    }, {});
    const pool = solveLeadRankingQUBO(candidates, regionCapacities, states);
    const regionSlotsUsed = {};
    pool.forEach((l) => {
      const region = l.region;
      const regionCenters = states[region] || [];
      if (!regionCenters.length) return;
      const netState = NET_STATES.find((s) => s.s === region);
      const baseHeadroom = netState ? netState.h[0] : 0.3;
      const baseCapacity = territoryCapacityOf(baseHeadroom);
      const capacity = contextualTerritoryCapacity(region, baseCapacity, regionCenters);
      const used = regionSlotsUsed[region] || 0;
      regionSlotsUsed[region] = used + 1;
      const withinSupply = used < capacity;
      const overlapPenalty = +(used * 0.06).toFixed(3);
      const headroom = Math.max(0.04, +(baseHeadroom - overlapPenalty).toFixed(3));
      const overlapFlag = overlapPenalty > 0;
      const liquidityFloor = l.proven ? +(GROWTH_QUALITY_LIQUIDITY_MIN * GROWTH_PROVEN_LIQUIDITY_DISCOUNT).toFixed(0) : GROWTH_QUALITY_LIQUIDITY_MIN;
      const fitFloor = l.multi ? GROWTH_MULTI_FIT_MIN : GROWTH_QUALITY_FIT_MIN;
      const qualityOk = l.fit >= fitFloor && l.liquidity >= liquidityFloor;
      const gateReason = !qualityOk ? l.fit < fitFloor ? "below fit threshold" + (l.multi ? " (multi-unit bar)" : "") : "liquidity below quality floor" + (l.proven ? " (proven-operator discounted floor)" : "") : !withinSupply ? "territory candidate-supply ceiling reached this cycle" : null;
      const gateBlocked = !qualityOk || !withinSupply;
      const anchor = [...regionCenters].sort((a, b) => b.health - a.health)[0];
      const worstDays = Math.max(...regionCenters.map(daysFn));
      const underwritingNote = l.proven ? " \xB7 proven operator, liquidity floor discounted to $" + liquidityFloor + "k" : l.multi ? " \xB7 multi-unit intent, fit floor raised to " + fitFloor : "";
      out.push(buildRecommendation({
        agent: AGENT.growth,
        scope: "territory",
        targetIds: [l.id, region, anchor.name],
        title: region + ": " + l.n + " (" + l.fit + " fit) into a " + (headroom >= 0.35 ? "high-headroom" : overlapFlag ? "overlap-constrained" : "moderate-headroom") + " territory",
        summary: l.note + " \xB7 " + regionCenters.length + " existing unit(s) in " + region + " \xB7 territory headroom " + Math.round(headroom * 100) + "%" + (overlapFlag ? " (adjusted for " + used + " concurrent candidate(s) already proposed into this territory this cycle)" : "") + underwritingNote + (gateBlocked ? " \xB7 " + gateReason : ""),
        actionType: "expansion",
        center: anchor,
        states,
        daysSinceMeasure: worstDays,
        suggestedActionPlan: ACTION_PLANS.find((p) => p.n === "Selective Expansion"),
        agentTag: "expansion-target",
        extraHold: { blocked: gateBlocked, reason: gateReason, capacity, used: used + 1, overlapPenalty, qualityOk, withinSupply, liquidityFloor, fitFloor }
      }));
    });
    return out;
  }
  var ROLE_CAPACITY = { FBC: 10, Owner: 8, Director: 12 };
  function evaluateScenarioViability(center, posture, week) {
    const sd = SCENARIO_DELTAS[posture] || SCENARIO_DELTAS.realistic;
    const wk = weekCompound(posture, week);
    const projHealth = Math.max(0, Math.min(100, center.health + (sd.chem + wk.chem) * 50));
    const projEb = Math.max(-20, Math.min(50, center.eb + (sd.eb + wk.eb)));
    const onFloor = projHealth >= 55 || projEb >= QFLOORS.margin_ebitda_k;
    return { posture, health: projHealth, eb: projEb, viable: onFloor, margin: projEb - QFLOORS.margin_ebitda_k };
  }
  function unitHealthAgentRecommend(centers, daysFn = defaultDaysSinceMeasure, capacityLedger) {
    const ledger = capacityLedger || {};
    const out = [];
    const atRisk = centers.filter((c) => c.eb < QFLOORS.margin_ebitda_k || c.health < 55).map((c) => ({ ...c, daysSinceMeasure: daysFn(c) }));
    const allocated = solveFBCAllocationRQAOA(atRisk, ROLE_CAPACITY.FBC);
    const allocatedSet = new Set(allocated.map((a) => a.center.name));
    atRisk.forEach((c) => {
      const days = c.daysSinceMeasure;
      const chronic = c.eb < 0 && c.momentum[0] !== "+";
      const actionPlan = ACTION_PLANS.find((p) => p.n === (chronic ? "Resale & Turnaround" : "Unit-Value Program"));
      const isAllocated = allocatedSet.has(c.name);
      const used = (ledger.FBC || 0) + 1;
      const cap = ROLE_CAPACITY.FBC;
      ledger.FBC = used - 1 + (isAllocated ? 1 : 0);
      const scenarioViability = ["realistic", "optimistic", "pessimistic"].map((p) => evaluateScenarioViability(c, p, 0));
      const robustAcrossScenarios = scenarioViability.every((v) => v.viable);
      const viabilityTag = robustAcrossScenarios ? "robust" : "scenario-dependent" + (scenarioViability.find((v) => v.posture === "pessimistic")?.viable === false ? " (fails under Pessimistic)" : "");
      out.push(buildRecommendation({
        agent: AGENT.health,
        scope: "unit",
        targetIds: [c.name],
        title: c.name + ": " + (chronic ? "chronic underperformance \u2014 turnaround review" : "below-floor margin \u2014 support plan"),
        summary: "margin $" + c.eb + "k \xB7 health " + c.health + " \xB7 momentum " + c.momentum + (chronic ? " \xB7 sustained decline, not a single-period dip" : " \xB7 likely recoverable with a support plan") + (isAllocated ? "" : " \xB7 deferred by health-severity optimization (" + cap + " FBC slots allocated to higher-severity cases)") + " [" + viabilityTag + "]",
        actionType: "support-plan",
        center: c,
        states: {},
        daysSinceMeasure: days,
        suggestedActionPlan: actionPlan,
        agentTag: "support-needed",
        scenarioViability,
        // NEW: multi-posture forecast
        extraHold: { blocked: !isAllocated, reason: isAllocated ? null : "Deferred by RQAOA severity weighting \u2014 other centers have higher health loss risk", capacity: cap, used }
      }));
    });
    return out;
  }
  var retentionProposalHistory = {};
  function retentionAgentRecommend(centers, daysFn = defaultDaysSinceMeasure, capacityLedger) {
    const ledger = capacityLedger || {};
    const out = [];
    centers.filter((c) => c.chem < 0.45 || c.ret < 0.8).forEach((c) => {
      const days = daysFn(c);
      const used = ledger.Owner || 0;
      const cap = ROLE_CAPACITY.Owner;
      const withinCapacity = used < cap;
      ledger.Owner = used + 1;
      const h = retentionProposalHistory[c.name] || { confidenceScore: 0.7, decayFactor: 1 };
      const confidenceScore = h.confidenceScore * h.decayFactor;
      out.push(buildRecommendation({
        agent: AGENT.vitality,
        scope: "unit",
        targetIds: [c.name],
        title: c.name + ": silent-churn risk",
        summary: "staff chemistry " + c.chem.toFixed(2) + " \xB7 12mo retention " + Math.round(c.ret * 100) + "% \xB7 risk builds quietly between measurement cycles" + (withinCapacity ? "" : " \xB7 Owner outreach capacity ceiling reached this cycle (" + cap + " concurrent)") + (confidenceScore < 0.6 ? " [low historical success]" : ""),
        actionType: "outreach",
        center: c,
        states: {},
        daysSinceMeasure: days,
        suggestedActionPlan: null,
        agentTag: "silent-churn-risk",
        confidenceScore,
        // NEW: feedback-loop learned confidence
        extraHold: { blocked: !withinCapacity, reason: withinCapacity ? null : "Owner outreach capacity ceiling reached this cycle", capacity: cap, used: used + 1 }
      }));
    });
    return out;
  }
  var PROPAGATION_MIN_SAMPLE = 5;
  var propagationPairWeights = {};
  function networkPropagationAgentRecommend(centers, states, daysFn = defaultDaysSinceMeasure) {
    const out = [];
    const guard = CONNECTIVITY.find((x) => x.t === "Best Practice Propagation");
    Object.keys(states).forEach((st) => {
      const cs = states[st];
      if (cs.length < PROPAGATION_MIN_SAMPLE) return;
      const sorted = [...cs].sort((a, b) => b.health - a.health);
      const k = Math.min(3, Math.floor(cs.length / 2));
      const topAvg = sorted.slice(0, k).reduce((a, c) => a + c.health, 0) / k;
      const bottomAvg = sorted.slice(-k).reduce((a, c) => a + c.health, 0) / k;
      const gap = Math.round(topAvg - bottomAvg);
      if (gap < 25) return;
      const sampleConfidence = cs.length >= 8 ? "high" : cs.length >= 6 ? "medium" : "low";
      const source = sorted[0], target = sorted[sorted.length - 1];
      const days = Math.max(daysFn(source), daysFn(target));
      const pairKey = [source.name, target.name].sort().join("|");
      const pairWeight = propagationPairWeights[pairKey]?.weight || 1;
      const propagationConfidence = 0.6 * pairWeight;
      out.push(buildRecommendation({
        agent: AGENT.hub,
        scope: "cluster",
        targetIds: [source.name, target.name],
        title: st + ": propagate " + source.name + "'s pattern toward " + target.name,
        summary: gap + "-point health gap (top-" + k + "/bottom-" + k + " avg) within " + st + " \xB7 sample " + cs.length + " units, " + sampleConfidence + " confidence \xB7 guardrail: " + (guard ? guard.guard : "data integrity \u2014 verified pattern only") + (pairWeight < 0.9 ? " [learning from prior pairs]" : ""),
        actionType: "support-plan",
        center: target,
        states,
        daysSinceMeasure: days,
        suggestedActionPlan: ACTION_PLANS.find((p) => p.n === "Unit-Value Program"),
        agentTag: "propagation-candidate",
        propagationConfidence
        // NEW: learned confidence from pair history
      }));
    });
    return out;
  }
  function applyGovernance(recommendations) {
    const actionable = recommendations.filter((r) => r.governance.allowed);
    const held = recommendations.filter((r) => !r.governance.allowed);
    return { actionable, held, summary: actionable.length + " ready for review, " + held.length + " held pending fresher data or a cleared guardrail" };
  }
  function detectConflictsTier1(recs) {
    const unitIndex = {};
    recs.forEach((r) => {
      (r.targetIds || []).filter((t) => t && t.length > 2 && t !== r.scope).forEach((unit) => {
        if (!unitIndex[unit]) unitIndex[unit] = [];
        unitIndex[unit].push(r.id);
      });
    });
    const flagged = [];
    Object.values(unitIndex).forEach((ids) => {
      for (let i = 0; i < ids.length; i++) {
        for (let j = i + 1; j < ids.length; j++) {
          const ra = recs.find((r) => r.id === ids[i]);
          const rb = recs.find((r) => r.id === ids[j]);
          if (ra && rb && ra.agent !== rb.agent) {
            flagged.push({ a: ra.id, b: rb.id, recA: ra, recB: rb });
          }
        }
      }
    });
    return flagged;
  }
  var ACTION_COMPAT_MATRIX = {
    "expansion|support-plan": 1,
    // Growth expanding on a center that needs FBC = hard block
    "expansion|outreach": 0.7,
    // Growth expanding on a churn-risk center = strong caution
    "support-plan|outreach": 0.3,
    // FBC + owner outreach on same center = soft overlap only
    "outreach|rollout": 0.3,
    // Vitality outreach + hub rollout = low conflict
    "expansion|rollout": 0.5
    // Expanding into a territory that hub is fixing = medium
  };
  function conflictEdgeWeight(recA, recB) {
    const pair = [recA.actionType, recB.actionType].sort().join("|");
    return ACTION_COMPAT_MATRIX[pair] || 0.5;
  }
  function detectConflictsTier2(flagged, recs) {
    return flagged.map(({ a, b, recA, recB }) => {
      const weight = conflictEdgeWeight(recA, recB);
      const severity = weight >= 0.9 ? "high" : weight >= 0.55 ? "medium" : "low";
      let reason;
      const growthRec = [recA, recB].find((r) => r.agent === AGENT.growth);
      const otherRec = [recA, recB].find((r) => r !== growthRec);
      if (growthRec && otherRec) {
        const anchorName = (growthRec.targetIds || [])[2] || otherRec.targetIds[0] || "the unit";
        reason = AGENT.growth + " proposes expanding " + ((growthRec.targetIds || [])[1] || "a territory") + " anchored on " + anchorName + ", which " + otherRec.agent + " has flagged: " + otherRec.title;
      } else {
        const sharedUnit = (recA.targetIds || []).find((t) => t && (recB.targetIds || []).includes(t)) || "the unit";
        reason = recA.agent + " and " + recB.agent + " both propose actions on " + sharedUnit + " \u2014 weight " + weight.toFixed(2);
      }
      return { a, b, reason, severity, weight };
    });
  }
  function detectConflicts(recs) {
    const flagged = detectConflictsTier1(recs);
    const rawConflicts = detectConflictsTier2(flagged, recs);
    return solveConflictIndependentSet(recs, rawConflicts);
  }
  var DAG_NODES = [
    { id: "growth", fn: (c, s, l, d, _led) => growthAgentRecommend(c, s, l, d), deps: [] },
    { id: "health", fn: (c, _s, _l, d, led) => unitHealthAgentRecommend(c, d, led), deps: [] },
    { id: "vitality", fn: (c, _s, _l, d, led) => retentionAgentRecommend(c, d, led), deps: [] },
    { id: "hub", fn: (c, s, _l, d, _led) => networkPropagationAgentRecommend(c, s, d), deps: ["health"] }
  ];
  function runDAG(centers, states, leads, daysFn) {
    const capacityLedger = {};
    const results = {};
    const cancelled = /* @__PURE__ */ new Set();
    const order = ["growth", "health", "vitality", "hub"];
    order.forEach((nodeId) => {
      if (cancelled.has(nodeId)) return;
      const node = DAG_NODES.find((n) => n.id === nodeId);
      if (!node) return;
      const depRecs = node.deps.flatMap((d) => results[d] || []);
      const emitted = node.fn(centers, states, leads, daysFn, capacityLedger, depRecs);
      results[nodeId] = emitted;
      if (nodeId === "health" && emitted.length === 0) {
        cancelled.add("hub");
      }
    });
    return Object.values(results).flat();
  }
  function partitionByConflicts(recs, conflicts) {
    const contested = /* @__PURE__ */ new Set();
    conflicts.forEach((c) => {
      contested.add(c.a);
      contested.add(c.b);
    });
    const isolated = recs.filter((r) => !contested.has(r.id));
    const cluster = recs.filter((r) => contested.has(r.id));
    return { isolated, cluster };
  }
  function runAllAgents(centers, states, leads, daysFn = defaultDaysSinceMeasure) {
    const recs = runDAG(centers, states, leads, daysFn);
    const conflicts = detectConflicts(recs);
    const { isolated, cluster } = partitionByConflicts(recs, conflicts);
    isolated.forEach((r) => {
      r._conflictFree = true;
    });
    cluster.forEach((r) => {
      r._conflictFree = false;
    });
    const dagMeta = {
      isolatedCount: isolated.length,
      clusterCount: cluster.length,
      cancelledNodes: Array.from(
        DAG_NODES.map((n) => n.id).filter((id) => {
          return false;
        })
      )
    };
    return { recommendations: recs, conflicts, dagMeta, ...applyGovernance(recs) };
  }
  function qLastMeasSeg(stList, st, seg) {
    for (let s = seg; s >= Math.max(0, seg - 40); s--) {
      if (stList[hash("m" + String(s)) % stList.length] === st) return s;
    }
    return null;
  }
  function qArc(cx, cy, r, a0, a1) {
    const x0 = cx + r * Math.cos(a0), y0 = cy + r * Math.sin(a0), x1 = cx + r * Math.cos(a1), y1 = cy + r * Math.sin(a1);
    const lg = a1 - a0 > Math.PI ? 1 : 0;
    return "M " + cx + " " + cy + " L " + x0.toFixed(1) + " " + y0.toFixed(1) + " A " + r + " " + r + " 0 " + lg + " 1 " + x1.toFixed(1) + " " + y1.toFixed(1) + " Z";
  }
  var BAND_COL = { high: GRN, medium: AMB, low: AC };
  var RISK_COL = { low: GRN, medium: AMB, high: AC };
  function plainVerdictSummary(rec) {
    const verdicts = rec.evidence.contractVerdicts || [];
    const failed = verdicts.filter((v) => !v.pass);
    const staleFlag = !rec.governance.freshnessOk;
    if (failed.length === 0 && !staleFlag) return { text: "All guardrails clear \u2014 ready for Director review.", tone: GRN };
    const parts = failed.map((v) => v.label.toLowerCase());
    if (staleFlag) parts.push("data freshness");
    return { text: "Held on: " + parts.join(", ") + ".", tone: AMB };
  }
  var RecommendationCard = import_react.default.memo(function RecommendationCard2({ rec, decision, history, conflicted, onDecide, jumpTo, postureApproved }) {
    const [open, setOpen] = (0, import_react.useState)(false);
    const postureGate = rec.scope === "territory" && !postureApproved;
    const allowed = rec.governance.allowed && !postureGate;
    const primaryUnit = rec.targetIds && rec.targetIds.find((id) => id && id.length > 2 && id !== rec.scope) || null;
    const histLabel = { approved: "Approved", measure: "Re-measure", ignored: "Ignored" };
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${allowed ? GRN : AMB}`, background: "#fff", padding: "9px 11px", marginBottom: 7 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "flex-start", gap: 8, flexWrap: "wrap" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: 1, minWidth: 220 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 6, alignItems: "center", marginBottom: 2, flexWrap: "wrap" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, border: `1px solid ${RULE}`, padding: "1px 5px" }, children: rec.agent }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: BAND_COL[rec.confidenceBand] }, children: [
              rec.confidenceBand,
              " confidence"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: RISK_COL[rec.riskBand] }, children: [
              rec.riskBand,
              " risk"
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700 }, children: [
            conflicted && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { title: "This proposal conflicts with another agent proposal on the same unit", style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: "#fff", background: AC, padding: "1px 5px", marginRight: 6, cursor: "help" }, children: "CONFLICT" }),
            rec.title,
            primaryUnit && jumpTo && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { onClick: () => jumpTo("lenses", primaryUnit, "following up on \u201C" + rec.title + "\u201D from " + rec.agent), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 400, color: AC, cursor: "pointer", textDecoration: "underline", marginLeft: 8 }, children: "view in Six Lenses \u2192" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#444", marginTop: 2, lineHeight: 1.4 }, children: rec.summary }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: allowed ? GRN : AMB, marginTop: 3, fontStyle: "italic" }, children: rationaleOf(rec) }),
          history && history.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 3 }, children: [
            "history: ",
            history.map((h) => histLabel[h.status] || h.status).join(" \u2192 ")
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { textAlign: "right", minWidth: 110 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, letterSpacing: 0.4, textTransform: "uppercase" }, children: "approver (human-owned)" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700 }, children: rec.approverRole }),
          rec.suggestedActionPlan && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 2 }, children: [
            "\u2192 ",
            rec.suggestedActionPlan.n
          ] })
        ] })
      ] }),
      (() => {
        const s = plainVerdictSummary(rec);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 6, fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: s.tone }, children: s.text });
      })(),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 4, display: "flex", gap: 5, flexWrap: "wrap" }, children: [
        rec.evidence.contractVerdicts.map((v, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { title: v.detail + (v.severity && v.severity !== "clear" ? " (" + v.severity + ")" : ""), style: { fontFamily: "Helvetica", fontSize: 9, padding: "2px 6px", border: `1px solid ${v.pass ? GRN : AC}`, color: v.pass ? GRN : AC, cursor: "help" }, children: [
          v.pass ? "\u2713" : "\u2715",
          " ",
          v.label,
          !v.pass && v.severity === "far off" ? " \u2014 far off" : !v.pass && v.severity === "narrow miss" ? " \u2014 narrow miss" : ""
        ] }, i)),
        !rec.evidence.contractVerdicts.length && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: "no gated guardrails for this action type" }),
        !rec.governance.freshnessOk && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9, padding: "2px 6px", border: `1px solid ${AMB}`, color: AMB }, children: "\u2715 data freshness" }),
        postureGate && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { onClick: () => jumpTo && jumpTo("quantum"), title: "Governance-clear, but expansion commitments are held until a network posture is approved in Quantum PM", style: { fontFamily: "Helvetica", fontSize: 9, padding: "2px 6px", border: `1px solid ${VIO}`, color: VIO, cursor: jumpTo ? "pointer" : "help", textDecoration: jumpTo ? "underline" : "none" }, children: "\u23F8 network posture not approved \u2014 Quantum PM \u2192" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginTop: 7 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setOpen((o) => !o), "aria-expanded": open, style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, cursor: "pointer", textDecoration: "underline", background: "none", border: "none", padding: 0 }, children: [
          open ? "hide" : "view",
          " signals"
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 5 }, children: decision ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: decision === "approved" ? GRN : decision === "measure" ? AMB : MUT }, children: decision === "approved" ? "Approved" : decision === "measure" ? "Queued for re-measurement" : "Ignored" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { disabled: !allowed, onClick: () => onDecide(rec, "approved"), "aria-label": "Approve: " + rec.title, style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "3px 9px", cursor: allowed ? "pointer" : "not-allowed", border: `1px solid ${allowed ? GRN : RULE}`, background: allowed ? GRN : "#f2f2f2", color: allowed ? "#fff" : MUT }, children: "Approve" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => onDecide(rec, "measure"), "aria-label": "Queue for re-measurement: " + rec.title, style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "3px 9px", cursor: "pointer", border: `1px solid ${AMB}`, background: "#fff", color: AMB }, children: "Measure again" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => onDecide(rec, "ignored"), "aria-label": "Ignore: " + rec.title, style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "3px 9px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: MUT }, children: "Ignore" })
        ] }) })
      ] }),
      open && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 7, paddingTop: 6, borderTop: `1px solid ${RULE}`, display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(120px,1fr))", gap: 6 }, children: [
        Object.entries(rec.evidence.operationalSignals).map(([k, v]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, textTransform: "uppercase", letterSpacing: 0.4 }, children: k.replace(/([A-Z])/g, " $1") }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700 }, children: typeof v === "number" ? v.toFixed(2) : v })
        ] }, k)),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, textTransform: "uppercase", letterSpacing: 0.4 }, children: "Data age" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700 }, children: [
            rec.evidence.dataFreshnessDays,
            "d"
          ] })
        ] })
      ] })
    ] });
  }, (prev, next) => prev.rec === next.rec && prev.decision === next.decision && prev.conflicted === next.conflicted && prev.history === next.history);
  var LENS_DEFS = [
    ["student", "Student", "#2f6fa0"],
    ["parent", "Parent", "#7a4fa0"],
    ["franchisee", "Franchisee", "#2f7a3f"],
    ["corporate", "Corporate", "#8b0000"],
    ["financial", "Financial", "#a06b1f"],
    ["system", "System", MUT]
  ];
  function royaltyOf(c) {
    const grossRev = +(c.students * 0.289).toFixed(1);
    return { grossRev, royalty: +(grossRev * 0.08).toFixed(1), brandFund: +(grossRev * 0.02).toFixed(1) };
  }
  function myStudioPayloadOf(centers) {
    return centers.map((c) => ({
      center_id: c.name,
      state: c.st,
      verified_public_record: !!c.verified,
      enrollment: {
        active_students: c.students,
        belt_distribution_note: "per-cohort belt counts modeled at network level (COHORT table), not yet broken out per center in this export"
      },
      engagement: {
        composite_engagement_score: +engageOf(c).score.toFixed(2),
        trial_conversion_rate: +c.conv.toFixed(2)
      },
      retention: {
        retention_rate: +c.ret.toFixed(2),
        staff_chemistry_index: +c.chem.toFixed(2)
      },
      schema: "illustrative \u2014 pending live MyStudio API field contract"
    }));
  }
  function quickBooksPayloadOf(centers) {
    return centers.map((c) => {
      const r = royaltyOf(c);
      return {
        center_id: c.name,
        state: c.st,
        period: "modeled \u2014 no fiscal period bound yet",
        gross_revenue_k: r.grossRev,
        royalty_due_k: r.royalty,
        brand_fund_due_k: r.brandFund,
        ebitda_k: c.eb,
        condition: conditionOf(c).label,
        schema: "illustrative \u2014 pending live QuickBooks GL account mapping"
      };
    });
  }
  function clusterExpansionGateOf(clusterCenters) {
    const top = [...clusterCenters].sort((a, b) => b.health - a.health)[0];
    const worstDays = Math.max(...clusterCenters.map((c) => hash(c.name) % 38));
    return checkContract("expansion", top, worstDays);
  }
  function conditionOf(c) {
    const idx = qResolve(qAmp(c));
    return { label: QSTATES[idx], color: QCOL[idx] };
  }
  var LensStudent = import_react.default.memo(function LensStudent2({ center }) {
    const e = engageOf(center), f = forecast(center), tau = qTau(center);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: "#2f6fa0", marginBottom: 3 }, children: "Student \u2014 experience & progression" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#555", lineHeight: 1.4, marginBottom: 8 }, children: "What this lens is for: how the student actually experiences the program week to week \u2014 session quality, belt progress, and how fast a shocked routine recovers." }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(140px,1fr))", gap: 8, marginBottom: 8 }, children: [["Engagement score", e.score, "0-1, composite"], ["30-day progression", Math.round(e.prog30 * 100) + "%", ""], ["Badges earned", e.badges, "this cohort"], ["Recovery time", tau + "w", tau <= 2 ? "fast" : tau <= 3 ? "watch" : "slow"]].map(([v, l, x], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800 }, children: v }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: l }),
        x && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: x })
      ] }, i)) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#444", lineHeight: 1.5 }, children: [
        "Commitment split: ",
        Math.round(e.commit[0] * 100),
        "% casual, ",
        Math.round(e.commit[1] * 100),
        "% engaged, ",
        Math.round(e.commit[2] * 100),
        "% advocate. Trend is ",
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: f.cls }),
        " on a ",
        f.slope > 0 ? "+" : "",
        f.slope,
        " monthly slope. A slow recovery time here is the earliest signal of the retention risk the Parent and Franchisee lenses price in."
      ] })
    ] });
  }, (prev, next) => prev.center === next.center);
  var LensParent = import_react.default.memo(function LensParent2({ center }) {
    const e = engageOf(center);
    const notesMetric = METRICS_FULL.find((m) => m.name.includes("Notes completion"));
    const noShowMetric = METRICS_FULL.find((m) => m.name.includes("No-show recovery"));
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: "#7a4fa0", marginBottom: 3 }, children: "Parent \u2014 trust & responsiveness" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#555", lineHeight: 1.4, marginBottom: 8 }, children: "What this lens is for: the trust signals a parent feels directly \u2014 timely notes, responsiveness, attendance, and whether they re-enroll or refer." }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(140px,1fr))", gap: 8, marginBottom: 8 }, children: [["12-mo retention", Math.round(center.ret * 100) + "%", "re-enrollment proxy"], ["Referral rate", Math.round(e.refer * 100) + "%", ""], ["Session buzz", Math.round(e.buzz * 100) + "%", "post-session sentiment"]].map(([v, l, x], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800 }, children: v }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: l }),
        x && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: x })
      ] }, i)) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#444", lineHeight: 1.5 }, children: [
        "Network-wide floors this center is held to: ",
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: notesMetric.name }),
        " (",
        notesMetric.trigger,
        ") and ",
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: noShowMetric.name }),
        " (",
        noShowMetric.trigger,
        "). Parent trust is a lagging read on both \u2014 by the time it drops, the leading indicators already moved."
      ] })
    ] });
  }, (prev, next) => prev.center === next.center);
  var LensFranchisee = import_react.default.memo(function LensFranchisee2({ center, states, days }) {
    const f = forecast(center), contract = checkContract("support-plan", center, days);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: "#2f7a3f", marginBottom: 3 }, children: "Franchisee \u2014 unit operations" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#555", lineHeight: 1.4, marginBottom: 8 }, children: "What this lens is for: what the person running the center sees day to day \u2014 trials, staffing, active students, margin, and whether a support plan is warranted." }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(140px,1fr))", gap: 8, marginBottom: 8 }, children: [["Active students", center.students, center.students < 60 ? "below profitability floor (60)" : "above floor"], ["Trial\u2192enrollment", Math.round(center.conv * 100) + "%", ""], ["Staff chemistry", center.chem.toFixed(2), center.chem < 0.45 ? "churn-risk range" : "stable"], ["Labor % of revenue", Math.round(center.capR * 100) + "%", center.capR >= 0.4 ? "at/above governor" : "within governor"]].map(([v, l, x], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800 }, children: v }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: l }),
        x && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: x })
      ] }, i)) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${contract.allowed ? GRN : AMB}`, padding: "7px 9px", fontFamily: "Helvetica", fontSize: 9.5, color: "#333", lineHeight: 1.5 }, children: [
        "Support plan: trend is ",
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: f.cls }),
        ". ",
        contract.allowed ? "Cleared to propose a support plan for Director/FBC review." : "Held \u2014 " + contract.heldOn.join(", ") + "."
      ] })
    ] });
  }, (prev, next) => prev.center === next.center && prev.days === next.days);
  var LensCorporate = import_react.default.memo(function LensCorporate2({ center, states, selectedCluster }) {
    const clusterCenters = states[selectedCluster] || [center];
    const avgHealth = Math.round(clusterCenters.reduce((a, c) => a + c.health, 0) / clusterCenters.length);
    const belowFloor = clusterCenters.filter((c) => c.eb < QFLOORS.margin_ebitda_k).length;
    const netSupport = networkSupportIndexOf(center, states);
    const expansion = clusterExpansionGateOf(clusterCenters);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AC, marginBottom: 3 }, children: "Corporate \u2014 network & propagation" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#555", lineHeight: 1.4, marginBottom: 8 }, children: "What this lens is for: the network-level view \u2014 cluster health, support capacity, and whether growth or best-practice rollout is actually earned yet." }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(140px,1fr))", gap: 8, marginBottom: 8 }, children: [[selectedCluster + " cluster health", avgHealth, clusterCenters.length + " units"], ["Below margin floor", belowFloor, "of " + clusterCenters.length], ["Support capacity", netSupport.toFixed(2), "0-1, peer + FBC proxy"], ["Expansion gate", expansion.allowed ? "clear" : "held", expansion.allowed ? "" : expansion.heldOn.join(", ")]].map(([v, l, x], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800, color: i === 3 ? expansion.allowed ? GRN : AMB : INK }, children: v }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: l }),
        x && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: x })
      ] }, i)) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#444", lineHeight: 1.5 }, children: "Best-practice propagation and expansion are both gated behind this same support-capacity read \u2014 a cluster doesn't get more units, or more of its practices pushed network-wide, faster than its FBC and peer network can actually absorb." })
    ] });
  }, (prev, next) => prev.center === next.center && prev.states === next.states && prev.selectedCluster === next.selectedCluster);
  var LensFinancial = import_react.default.memo(function LensFinancial2({ center, days }) {
    const { grossRev, royalty, brandFund } = royaltyOf(center);
    const tau = qTau(center);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: "#a06b1f", marginBottom: 3 }, children: "Financial \u2014 unit economics" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#555", lineHeight: 1.4, marginBottom: 8 }, children: "What this lens is for: the money \u2014 margin against the floor, royalty and brand-fund impact, and how far the unit is from a durable payback trajectory." }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(140px,1fr))", gap: 8, marginBottom: 8 }, children: [["Unit margin", "$" + center.eb + "k", center.eb < QFLOORS.margin_ebitda_k ? "below $" + QFLOORS.margin_ebitda_k + "k floor" : "above floor"], ["Modeled gross revenue", "$" + grossRev + "k", "students \xD7 modeled monthly rate"], ["Royalty (8%)", "$" + royalty + "k", ""], ["Brand fund (2%)", "$" + brandFund + "k", ""]].map(([v, l, x], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800, color: i === 0 && center.eb < QFLOORS.margin_ebitda_k ? AC : INK }, children: v }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: l }),
        x && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: x })
      ] }, i)) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#444", lineHeight: 1.5 }, children: [
        "Recovery time is ",
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
          tau,
          "w"
        ] }),
        " at current trajectory \u2014 the same number driving the Student lens above, reused here rather than recomputed, because margin recovery and student-experience recovery are the same underlying signal read twice."
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 6 }, children: "Gross revenue is modeled from enrollment at this file's standing per-student rate assumption \u2014 illustrative, not a substitute for MyStudio/QuickBooks reconciliation." })
    ] });
  }, (prev, next) => prev.center === next.center && prev.days === next.days);
  function LensSystem({ center, states, days }) {
    const sig = operationalSignalsOf(center, states, days);
    const gov = qGovernors(center, days);
    const fbMatch = FB_BOUNDARY.centers.find((x) => x.id === center.name);
    const [jsonOpen, setJsonOpen] = (0, import_react.useState)(false);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 3 }, children: "System \u2014 canonical state & guardrails" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#555", lineHeight: 1.4, marginBottom: 8 }, children: "What this lens is for: the audit trail \u2014 the raw signals and guardrail verdicts behind every claim made in the other five lenses, for diagnosis and traceability." }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(120px,1fr))", gap: 6, marginBottom: 8 }, children: Object.entries(sig).map(([k, v]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, textTransform: "uppercase" }, children: k.replace(/([A-Z])/g, " $1") }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 13, fontWeight: 700 }, children: typeof v === "number" ? v.toFixed(2) : v })
      ] }, k)) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 5, flexWrap: "wrap", marginBottom: 8 }, children: gov.map((g, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { title: g.detail, style: { fontFamily: "Helvetica", fontSize: 9, padding: "2px 6px", border: `1px solid ${g.pass ? GRN : AC}`, color: g.pass ? GRN : AC, cursor: "help" }, children: [
        g.pass ? "\u2713" : "\u2715",
        " ",
        g.label
      ] }, i)) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginBottom: 4 }, children: fbMatch ? "Cross-reference: this center id also exists in the separate FB_BOUNDARY proof-of-concept dataset (not the same canonical source as above)." : "No matching id in the separate FB_BOUNDARY dataset \u2014 expected, since it models a different illustrative center set." }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => setJsonOpen((o) => !o), style: { fontFamily: "Helvetica", fontSize: 9, color: AC, cursor: "pointer", textDecoration: "underline", marginBottom: jsonOpen ? 4 : 0 }, children: [
        jsonOpen ? "hide" : "show",
        " diagnostic / audit JSON for this center \u2192"
      ] }),
      jsonOpen && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginBottom: 4, lineHeight: 1.5 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Reading this:" }),
        " health/eb are the same margin and health numbers shown above in plain language \u2014 this is the raw record they're computed from. daysSinceMeasure is data age; signals are the six operational reads used to set confidence and risk bands elsewhere on this screen."
      ] }),
      jsonOpen && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("pre", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: "#333", background: "#f7f6f2", border: `1px solid ${RULE}`, padding: "6px 8px", overflowX: "auto", margin: 0 }, children: JSON.stringify({ name: center.name, st: center.st, health: center.health, eb: center.eb, daysSinceMeasure: days, signals: sig }, null, 1) })
    ] });
  }
  var LENS_COMPONENTS = { student: LensStudent, parent: LensParent, franchisee: LensFranchisee, corporate: LensCorporate, financial: LensFinancial, system: LensSystem };
  function SixLensTab({ centers, states, leads, railData, opt, setOpt, logL, focusCenter, jumpReason, clearJumpReason }) {
    const stateKeys = Object.keys(states);
    const focusSt = focusCenter ? (centers.find((c) => c.name === focusCenter) || {}).st : null;
    const [selSt, setSelSt] = (0, import_react.useState)(focusSt && stateKeys.includes(focusSt) ? focusSt : stateKeys[0]);
    const clusterCenters = states[selSt] || [];
    const [selName, setSelName] = (0, import_react.useState)(focusCenter && focusSt === selSt ? focusCenter : clusterCenters[0]?.name || null);
    const center = clusterCenters.find((c) => c.name === selName) || clusterCenters[0] || centers[0];
    const [lens, setLens] = (0, import_react.useState)("student");
    const [showPerf, setShowPerf] = (0, import_react.useState)(false);
    const mountStart = (0, import_react.useRef)(typeof performance !== "undefined" ? performance.now() : 0);
    const [mountMs, setMountMs] = (0, import_react.useState)(null);
    (0, import_react.useEffect)(() => {
      const t = typeof performance !== "undefined" ? performance.now() - mountStart.current : 0;
      setMountMs(+t.toFixed(1));
    }, []);
    const days = opt.fresh[center.name] !== void 0 ? (opt.week - opt.fresh[center.name]) * 7 : hash(center.name) % 38 + opt.week * 2;
    const centerRecs = railData.recommendations.filter((r) => r.targetIds.includes(center.name));
    const activeSignal = [...centerRecs].sort((a, b) => a.governance.allowed === b.governance.allowed ? 0 : a.governance.allowed ? -1 : 1)[0] || null;
    const lensConflictIds = new Set((railData.conflicts || []).flatMap((c) => [c.a, c.b]));
    const lensConflicted = activeSignal ? lensConflictIds.has(activeSignal.id) : false;
    const lensConflictReason = lensConflicted ? (railData.conflicts || []).find((c) => c.a === activeSignal.id || c.b === activeSignal.id) : null;
    const f = forecast(center), tau = qTau(center), gov = qGovernors(center, days);
    const held = gov.filter((g) => !g.pass);
    const cond = conditionOf(center);
    const coherence = qCoherence(days);
    const expansion = clusterExpansionGateOf(clusterCenters);
    const supportPosture = activeSignal && activeSignal.governance.allowed ? "support plan proposed" : held.length ? "hold \u2014 guardrail open" : cond.label === "at-risk" ? "monitor closely" : "monitor";
    const LensBody = LENS_COMPONENTS[lens];
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StateViewBanner, { thisView: "Six Lenses" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8, fontStyle: "italic" }, children: "Six lanes, one truth: student, parent, franchisee, corporate, financial, and system, all synchronized to the same center, the same week, and the same active signal. The underlying discipline is sense-and-respond \u2014 observe state, identify the binding constraint, propose one bounded pilot, measure, and only then propagate what the data validates. The engine's job stops at diagnosis, prioritization, and support planning; pricing, staffing, compliance, and contracts stay human-owned." }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, marginBottom: 6 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setShowPerf((s) => !s), "aria-pressed": showPerf, "aria-label": "Toggle render timing display", style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, cursor: "pointer", textDecoration: "underline", background: "none", border: "none", padding: 0 }, children: [
          showPerf ? "hide" : "show",
          " render timing"
        ] }),
        showPerf && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, padding: "2px 7px", border: `1px solid ${mountMs !== null && mountMs < 200 ? GRN : AMB}`, color: mountMs !== null && mountMs < 200 ? GRN : AMB }, children: [
          "mount: ",
          mountMs === null ? "measuring\u2026" : mountMs + "ms"
        ] })
      ] }),
      jumpReason && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${AC}`, borderLeft: `3px solid ${AC}`, background: "#fdf3f2", padding: "6px 10px", marginBottom: 8, display: "flex", justifyContent: "space-between", alignItems: "center" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#5a2020" }, children: [
          "Here because: ",
          jumpReason
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: clearJumpReason, "aria-label": "Dismiss navigation context", style: { fontFamily: "Helvetica", fontSize: 11, color: MUT, cursor: "pointer", background: "none", border: "none", padding: 0 }, children: "\xD7" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", flexWrap: "wrap", gap: 8, alignItems: "center", border: `1px solid ${RULE}`, background: "#fff", padding: "7px 10px", marginBottom: 8 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("select", { value: selSt, onChange: (ev) => {
          setSelSt(ev.target.value);
          setSelName(null);
        }, style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, border: `1px solid ${RULE}`, padding: "3px 6px" }, children: stateKeys.map((st) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("option", { value: st, children: [
          st,
          " \xB7 ",
          states[st].length,
          " units"
        ] }, st)) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("select", { value: center.name, onChange: (ev) => setSelName(ev.target.value), style: { fontFamily: "Helvetica", fontSize: 10, border: `1px solid ${RULE}`, padding: "3px 6px", flex: "1 1 160px" }, children: clusterCenters.map((c) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("option", { value: c.name, children: [
          c.name,
          " \xB7 health ",
          c.health
        ] }, c.name)) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 3, fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setOpt((o) => ({ ...o, week: Math.max(0, o.week - 1) })), style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, padding: "1px 7px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff" }, children: "\u2212" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
            "week ",
            opt.week
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setOpt((o) => ({ ...o, week: o.week + 1 })), style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, padding: "1px 7px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff" }, children: "+" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: cond.color }, children: cond.label }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: [
          "measurement confidence ",
          coherence.toFixed(2),
          " \xB7 recovery ",
          tau,
          "w \xB7 measured ",
          days,
          "d ago"
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { marginLeft: "auto", fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: held.length ? AC : GRN }, children: held.length ? held[0].label + (held.length > 1 ? " +" + (held.length - 1) + " more" : "") : "all guardrails clear" }),
        days > 14 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
          setOpt((o) => ({ ...o, fresh: { ...o.fresh, [center.name]: o.week } }));
          logL && logL("lenses", "System", "Re-measured " + center.name);
        }, style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "3px 9px", cursor: "pointer", border: `1px solid ${AMB}`, background: "#fff", color: AMB }, children: "Measure now" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", padding: "9px 12px", marginBottom: 8 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 2 }, children: [
          "What matters now \u2014 ",
          center.name
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginBottom: 6 }, children: "This is a deliberate single-center deep-dive. For the network-wide highest-priority item, see the Overview tab's Today panel instead." }),
        activeSignal ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 12, fontWeight: 700 }, children: activeSignal.title }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#444", margin: "3px 0 6px", lineHeight: 1.5 }, children: activeSignal.summary }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#555", lineHeight: 1.5, marginBottom: 6 }, children: [
            "Why it matters \u2014 ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "students:" }),
            " a ",
            cond.label,
            " read means engagement and belt progress ",
            cond.label === "at-risk" ? "are already slipping" : "are holding",
            ". ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Parents:" }),
            " ",
            center.ret >= 0.85 ? "retention is healthy, so trust hasn't been tested yet" : "retention is soft \u2014 this is what a parent would notice first",
            ". ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Economics:" }),
            " margin is ",
            center.eb < QFLOORS.margin_ebitda_k ? "below the $" + QFLOORS.margin_ebitda_k + "k floor" : "above floor",
            " at $",
            center.eb,
            "k."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: [
            "Trace: ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
              activeSignal.agent,
              " agent"
            ] }),
            " \u2192 ",
            activeSignal.evidence.contractVerdicts.length ? activeSignal.evidence.contractVerdicts.map((v) => v.pass ? v.label : v.label + (v.severity && v.severity !== "clear" ? " (" + v.severity + ")" : "")).join(", ") : "no gated guardrails for this action",
            " \u2192 ",
            activeSignal.suggestedActionPlan ? activeSignal.suggestedActionPlan.n : "no action plan attached",
            " (human-owned) \u2192 approver: ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: activeSignal.approverRole })
          ] }),
          activeSignal.supplyGate && activeSignal.supplyGate.blocked && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: AMB, marginTop: 2 }, children: [
            "Capacity hold: ",
            activeSignal.supplyGate.reason
          ] }),
          lensConflicted && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: AC, marginTop: 2, fontWeight: 700 }, children: [
            "\u26A0 ",
            lensConflictReason ? lensConflictReason.reason : "This proposal conflicts with another agent\u2019s proposal on the same unit",
            " \u2014 see Decision Rail before acting."
          ] })
        ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: MUT }, children: [
          "No proposed action is currently open for this center \u2014 trend is ",
          f.cls,
          ", recovery time ",
          tau,
          "w, condition ",
          cond.label,
          "."
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, marginBottom: 8, flexWrap: "wrap" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "2 1 300px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 4, marginBottom: 6, flexWrap: "wrap" }, children: LENS_DEFS.map(([id, label, c]) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setLens(id), style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "5px 11px", cursor: "pointer", border: `1px solid ${lens === id ? c : RULE}`, background: lens === id ? c : "#fff", color: lens === id ? "#fff" : c }, children: label }, id)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LensBody, { center, states, selectedCluster: selSt, days, week: opt.week, activeSignal }) })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 200px", border: `1px solid ${RULE}`, background: "#fff", padding: "9px 11px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 6 }, children: "Financial & corporate at a glance" }),
          (() => {
            const { royalty } = royaltyOf(center);
            const belowFloor = (states[selSt] || []).filter((c) => c.eb < QFLOORS.margin_ebitda_k).length;
            return [["Students vs floor", center.students + " / 60", center.students < 60 ? AC : GRN], ["Margin", center.eb + "k / " + QFLOORS.margin_ebitda_k + "k floor", center.eb < QFLOORS.margin_ebitda_k ? AC : GRN], ["Royalty impact", "$" + royalty + "k/mo", INK], ["Cluster below floor", belowFloor + " units", belowFloor ? AMB : GRN], ["Support posture", supportPosture, supportPosture === "monitor" ? GRN : AMB]].map(([l, v, c2], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", padding: "4px 0", borderTop: i ? `1px solid #f0f0f0` : "none" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: l }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 10, color: c2 }, children: v })
            ] }, i));
          })(),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 6, paddingTop: 6, borderTop: `1px solid ${RULE}` }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: "Expansion readiness" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 10, color: expansion.allowed ? GRN : MUT }, children: expansion.allowed ? "clear to propose" : "not yet \u2014 " + expansion.heldOn.join(", ") })
          ] }) })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginBottom: 10 }, children: [
        "Six lenses, one canonical state: every panel above reads the same ",
        center.name,
        " object, the same week, and the same active signal \u2014 nothing here is a separate copy of the numbers."
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SixLensProof, {})
    ] });
  }
  function StateViewBanner({ thisView }) {
    const views = [
      ["Six Lenses", "one unit, six roles"],
      ["Operations Board", "every intervention, in parallel"],
      ["Operations Dynamics", "uncertainty & review cadence"]
    ];
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fbfbf9", padding: "7px 11px", marginBottom: 8 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.7, textTransform: "uppercase", color: MUT, marginBottom: 3 }, children: [
        "One canonical state \xB7 several operating views \xB7 you are in: ",
        thisView
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 6, flexWrap: "wrap" }, children: views.map(([n, d]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, padding: "2px 7px", border: `1px solid ${thisView === n ? INK : RULE}`, background: thisView === n ? INK : "#fff", color: thisView === n ? "#fff" : MUT }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: n }),
        " \u2014 ",
        d
      ] }, n)) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 4 }, children: "These are not three datasets. They are three questions asked of the same modeled network \u2014 change the state in one and the others reflect it, because there is only one state to change." })
    ] });
  }
  function SixLensProof() {
    const [open, setOpen] = (0, import_react.useState)(false);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fafafa", padding: "9px 11px" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => setOpen((o) => !o), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, cursor: "pointer" }, children: [
        open ? "hide" : "show",
        " canonical state / system proof \u2192"
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 3 }, children: "Separate from the per-center diagnostic above: this is the full serialized network object every view in this section derives from \u2014 for debugging and audit, not the first thing a reviewer should read." }),
      open && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 6, lineHeight: 1.5 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Reading this:" }),
        " schema/week identify the record version and measurement cycle. clusters group centers by coupling strength; centers each carry a state (thriving/watch/at-risk), a set of dimension values (vals), and their week-over-week movement (mom). This is the record itself, not a summary of it \u2014 every number in the other views traces back to something here."
      ] }),
      open && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("pre", { style: { fontFamily: FB_MONO, fontSize: 9, color: "#333", background: "#fff", border: `1px solid ${RULE}`, padding: "8px 10px", overflowX: "auto", marginTop: 6, maxHeight: 280, overflowY: "auto" }, children: JSON.stringify(FB_BOUNDARY, null, 1) })
    ] });
  }
  function boardStatusOf(rec, decisions) {
    const d = decisions[rec.id];
    if (d === "approved") return "approved";
    if (d === "measure") return "remeasure";
    if (d === "ignored") return "aside";
    return rec.governance.allowed ? "proposed" : "held";
  }
  var BOARD_COLS = [
    ["proposed", "Proposed \u2014 pending Director review", GRN],
    ["held", "Held \u2014 guardrail or data", AMB],
    ["remeasure", "Queued for re-measurement", AMB],
    ["approved", "Approved \u2014 human-owned", INK],
    ["aside", "Set aside", MUT]
  ];
  function supportPathsOf(c) {
    const out = [];
    if (c.ret < 0.82) out.push({ id: "retention", n: "Retention outreach sequence", d: "Structured parent contact on missed sessions; no-show recovery protocol.", window: 60, risk: "Low" });
    if (c.chem < 0.55) out.push({ id: "staffing", n: "Staffing stabilization plan", d: "Coverage review, senior-instructor pairing, scheduled check-ins.", window: 45, risk: "Medium" });
    if (c.eb < QFLOORS.margin_ebitda_k) out.push({ id: "unitvalue", n: "Unit-economics program", d: "Premium-tier pricing review and community pipeline activation.", window: 45, risk: "Low" });
    if (c.conv < 0.4) out.push({ id: "conversion", n: "Trial-conversion review", d: "First-session experience audit; enrollment-day follow-through.", window: 28, risk: "Low" });
    if (!out.length) out.push({ id: "monitor", n: "Standard monitoring cadence", d: "No intervention indicated; hold to the regular review cycle.", window: 28, risk: "Low" });
    return out.slice(0, 3);
  }
  function OperationsDynamicsTab({ centers, states, opt, setOpt, logL, dyn, setDyn, focusCenter, railData, jumpTo }) {
    const [showPerfDyn, setShowPerfDyn] = (0, import_react.useState)(false);
    const mountStartDyn = (0, import_react.useRef)(typeof performance !== "undefined" ? performance.now() : 0);
    const [mountMsDyn, setMountMsDyn] = (0, import_react.useState)(null);
    (0, import_react.useEffect)(() => {
      const t = typeof performance !== "undefined" ? performance.now() - mountStartDyn.current : 0;
      setMountMsDyn(+t.toFixed(1));
    }, []);
    const stateKeys = Object.keys(states);
    const focusSt = focusCenter ? (centers.find((c) => c.name === focusCenter) || {}).st : null;
    const [selSt, setSelSt] = (0, import_react.useState)(focusSt && stateKeys.includes(focusSt) ? focusSt : stateKeys[0]);
    const stCenters = states[selSt] || [];
    const [selName, setSelName] = (0, import_react.useState)(focusCenter && focusSt === selSt ? focusCenter : null);
    const [couplingScope, setCouplingScope] = (0, import_react.useState)("state");
    const [supportDrag, setSupportDrag] = (0, import_react.useState)(0.1);
    const [adoptionSpeed, setAdoptionSpeed] = (0, import_react.useState)(1);
    const [guardrailDays, setGuardrailDays] = (0, import_react.useState)(28);
    const center = stCenters.find((c) => c.name === selName) || stCenters[0] || centers[0];
    const dynConflictIds = new Set((railData && railData.conflicts || []).flatMap((c) => [c.a, c.b]));
    const dynRecs = (railData && railData.recommendations || []).filter((r) => r.targetIds.includes(center.name));
    const dynConflicted = dynRecs.some((r) => dynConflictIds.has(r.id));
    const ageOf = (c) => opt.fresh[c.name] !== void 0 ? (opt.week - opt.fresh[c.name]) * 7 : hash(c.name) % 38 + opt.week * 2;
    const amp = qAmp(center), age = ageOf(center), conf = qCoherence(age);
    const paths = supportPathsOf(center);
    const netConf = +(centers.reduce((a, c) => a + qCoherence(ageOf(c)), 0) / centers.length).toFixed(2);
    const supportIdx = networkSupportIndexOf(center, states);
    const review = (c) => {
      const a = qAmp(c), idx = qResolve(a);
      setDyn((m) => ({ ...m, resolved: { ...m.resolved, [c.name]: idx } }));
      setOpt((o) => ({ ...o, fresh: { ...o.fresh, [c.name]: o.week } }));
      logL && logL("dynamics", "Review", c.name + " reviewed \u2014 condition recorded as " + QSTATES[idx] + "; measurement confidence restored.");
    };
    const commit = (c, p) => {
      setDyn((m) => ({ ...m, committed: { ...m.committed, [c.name]: p.id } }));
      logL && logL("dynamics", "Director", c.name + ' \u2014 committed "' + p.n + '"; alternative paths closed. Review due within ' + p.window + "d.");
    };
    const completePath = (c, p) => {
      setDyn((m) => ({ ...m, completed: { ...m.completed || {}, [c.name]: p.id } }));
      setOpt((o) => ({ ...o, fresh: { ...o.fresh, [c.name]: o.week } }));
      logL && logL("dynamics", "Director", c.name + ' \u2014 "' + p.n + '" marked complete; outcome measured, review clock closed.');
    };
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StateViewBanner, { thisView: "Operations Dynamics" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: "Between reviews, a unit's condition is a weighted likelihood across thriving, watch, and at-risk \u2014 and confidence in that read decays as data ages. A review resolves the likelihood to a recorded condition and restores confidence on the same shared clock every other view reads. Support planning holds up to three candidate paths concurrently; the Director commits one, the alternatives close. All figures derive from the canonical state \u2014 nothing here is a separate copy." }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, marginBottom: 6 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setShowPerfDyn((s) => !s), "aria-pressed": showPerfDyn, "aria-label": "Toggle render timing display", style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, cursor: "pointer", textDecoration: "underline", background: "none", border: "none", padding: 0 }, children: [
          showPerfDyn ? "hide" : "show",
          " render timing"
        ] }),
        showPerfDyn && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, padding: "2px 7px", border: `1px solid ${mountMsDyn !== null && mountMsDyn < 200 ? GRN : AMB}`, color: mountMsDyn !== null && mountMsDyn < 200 ? GRN : AMB }, children: [
          "mount: ",
          mountMsDyn === null ? "measuring\u2026" : mountMsDyn + "ms"
        ] })
      ] }),
      dynConflicted && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${AC}`, borderLeft: `3px solid ${AC}`, background: "#fdf3f2", padding: "6px 10px", marginBottom: 8, fontFamily: "Helvetica", fontSize: 9, color: "#5a2020", fontWeight: 700 }, children: [
        "\\u26a0 ",
        center.name,
        " is flagged by the cross-agent conflict check \\u2014 review Decision Rail before committing a support path here."
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 0, flexWrap: "wrap", border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", marginBottom: 10 }, children: [
        [["wk " + opt.week, "shared operating clock"], [netConf, "avg measurement confidence \u2014 network"], [Object.keys(dyn.resolved).length, "conditions recorded this session"], [Object.keys(dyn.committed).length, "support paths committed"]].map(([v, l], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 130px", padding: "8px 12px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 18, fontWeight: 800, lineHeight: 1 }, children: v }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 2 }, children: l })
        ] }, i)),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", alignItems: "center", padding: "0 12px" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setOpt((o) => ({ ...o, week: o.week + 1 })), style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "5px 12px", cursor: "pointer", border: `1px solid ${INK}`, background: INK, color: "#fff" }, children: "Advance week \u2192" }) })
      ] }),
      (() => {
        const base = 1.36;
        const dragFactor = 1 - supportDrag;
        const rampFactor = Math.min(1, adoptionSpeed);
        const guardrailFactor = guardrailDays >= 28 ? 1 : 0.85;
        const projected = +(base * dragFactor * rampFactor * guardrailFactor).toFixed(2);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid #7a6bd8`, background: "#faf9fc", padding: "9px 12px", marginBottom: 10 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: "#7a6bd8", marginBottom: 6 }, children: "Scenario sandbox \u2014 flex an assumption, watch the projection move" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 16, flexWrap: "wrap", alignItems: "center" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { minWidth: 180 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginBottom: 2 }, children: [
                "Support-cost drag: ",
                Math.round(supportDrag * 100),
                "%"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { type: "range", min: 0, max: 40, value: Math.round(supportDrag * 100), onChange: (ev) => setSupportDrag(Number(ev.target.value) / 100), "aria-label": "Support-cost drag, currently " + Math.round(supportDrag * 100) + " percent", style: { width: "100%" } })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { minWidth: 180 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginBottom: 2 }, children: [
                "Adoption speed: ",
                adoptionSpeed.toFixed(2),
                "\xD7 base ramp"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { type: "range", min: 40, max: 140, value: Math.round(adoptionSpeed * 100), onChange: (ev) => setAdoptionSpeed(Number(ev.target.value) / 100), "aria-label": "Adoption speed multiplier, currently " + adoptionSpeed.toFixed(2) + "x", style: { width: "100%" } })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { minWidth: 180 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginBottom: 2 }, children: [
                "Guardrail freshness floor: ",
                guardrailDays,
                "d"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { type: "range", min: 14, max: 45, value: guardrailDays, onChange: (ev) => setGuardrailDays(Number(ev.target.value)), "aria-label": "Guardrail freshness floor, currently " + guardrailDays + " days", style: { width: "100%" } })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginLeft: "auto", textAlign: "right" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 20, fontWeight: 800, color: "#7a6bd8" }, children: [
                "$",
                projected,
                "M/yr"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: [
                "projected, vs $",
                base,
                "M/yr base case"
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 6 }, children: "This is a policy simulator, not a second source of truth \u2014 all three sliders recompute the same Unit-Value figure shown in Programs; nothing here is committed until a Director acts on it." })
        ] });
      })(),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 10, flexWrap: "wrap", alignItems: "flex-start" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 330px", border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 6 }, children: [
            "Condition likelihood by unit \u2014 ",
            selSt,
            " \xB7 peer support ",
            supportIdx.toFixed(2)
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("select", { value: selSt, onChange: (e) => {
            setSelSt(e.target.value);
            setSelName(null);
          }, style: { fontFamily: "Helvetica", fontSize: 10, border: `1px solid ${RULE}`, padding: "3px 6px", marginBottom: 8, width: "100%" }, children: stateKeys.map((st) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("option", { value: st, children: [
            st,
            " \xB7 ",
            states[st].length,
            " units"
          ] }, st)) }),
          stCenters.slice(0, 10).map((c) => {
            const a = qAmp(c), ag = ageOf(c), cf = qCoherence(ag), ri = dyn.resolved[c.name];
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => setSelName(c.name), style: { padding: "6px 0", borderTop: `1px solid #f0ede6`, cursor: "pointer", opacity: center.name === c.name ? 1 : 0.75 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", fontFamily: "Helvetica", fontSize: 9.5 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: c.name }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: ri !== void 0 ? QCOL[ri] : MUT, fontWeight: 700 }, children: ri !== void 0 ? "recorded: " + QSTATES[ri] : "unreviewed" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", height: 10, border: `1px solid ${RULE}`, marginTop: 3, opacity: 0.35 + cf * 0.65 }, children: a.map((v, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { width: v * 100 + "%", background: QCOL[i] }, title: QSTATES[i] + " " + Math.round(v * 100) + "%" }, i)) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 2 }, children: [
                "data age ",
                ag,
                "d \xB7 confidence ",
                cf.toFixed(2),
                " \u2014 bar fades as confidence decays"
              ] })
            ] }, c.name);
          }),
          stCenters.length > 10 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, paddingTop: 4 }, children: [
            "+",
            stCenters.length - 10,
            " more units in ",
            selSt
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 330px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px", marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 4 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT }, children: [
                center.name,
                " \u2014 review status"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: conf < 0.5 ? AC : GRN }, children: [
                "confidence ",
                conf.toFixed(2)
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 300 80", style: { width: "100%", display: "block" }, children: [
              Array.from({ length: 61 }, (_, d) => d).map((d) => {
                const y = 70 - qCoherence(d) * 60;
                return d ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: 10 + d * 4.6, cy: y, r: "0.7", fill: MUT }, d) : null;
              }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: 10 + Math.min(60, age) * 4.6, y1: "6", x2: 10 + Math.min(60, age) * 4.6, y2: "74", stroke: conf < 0.5 ? AC : INK, strokeWidth: "1.4" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("text", { x: Math.min(255, 14 + Math.min(60, age) * 4.6), y: "14", fontSize: "8", fill: conf < 0.5 ? AC : INK, fontFamily: "Helvetica", children: [
                "this unit \xB7 ",
                age,
                "d"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "10", y: "78", fontSize: "7", fill: MUT, fontFamily: "Helvetica", children: "0d" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "278", y: "78", fontSize: "7", fill: MUT, fontFamily: "Helvetica", children: "60d" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, margin: "4px 0 6px" }, children: "Measurement confidence vs. data age. Past ~28d, a recorded condition should not anchor a growth or pricing decision \u2014 the same freshness floor the governance contracts enforce, and the same clock the Six-Lens and Operations Board views read." }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => review(center), style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "5px 12px", cursor: "pointer", border: `1px solid ${GRN}`, background: GRN, color: "#fff" }, children: "Conduct review \u2014 record condition" }),
            jumpTo && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { onClick: () => jumpTo("lenses", center.name, "reviewing " + center.name + " from Operations Dynamics"), style: { fontFamily: "Helvetica", fontSize: 9, color: AC, cursor: "pointer", textDecoration: "underline", marginLeft: 10 }, children: [
              "view ",
              center.name,
              " in Six Lenses \u2192"
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px", marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 2 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT }, children: [
                "Peer coupling \u2014 ",
                couplingScope === "state" ? "how support propagates in " + selSt : "network-wide cluster structure"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 4 }, children: [["state", "This state"], ["network", "Whole network"]].map(([id, lab]) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setCouplingScope(id), style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, padding: "2px 7px", cursor: "pointer", border: `1px solid ${couplingScope === id ? INK : RULE}`, background: couplingScope === id ? INK : "#fff", color: couplingScope === id ? "#fff" : MUT }, children: lab }, id)) })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginBottom: 6 }, children: couplingScope === "state" ? "Units in a tightly-coupled state move together. When a support path is committed at one unit, the units it is most strongly connected to are the next most likely to benefit \u2014 support lands harder where the network is tight." : "Each node is a state cluster, sized by unit count and colored by average condition. Thicker links mean tighter regional coupling \u2014 where a validated practice propagates fastest across the network." }),
            couplingScope === "network" ? (() => {
              const byState = Object.entries(states).map(([st, cs]) => ({ st, n: cs.length, health: cs.reduce((a, c) => a + c.health, 0) / cs.length, cond: conditionOf(cs.reduce((a, c) => a.health < c.health ? a : c, cs[0])) })).sort((a, b) => b.n - a.n).slice(0, 12);
              const N = byState.length, cx = 150, cy = 75, R = 60;
              const pos = byState.map((s, i) => {
                const ang = i / N * Math.PI * 2 - Math.PI / 2;
                return { ...s, x: cx + Math.cos(ang) * R, y: cy + Math.sin(ang) * R };
              });
              const coup = (a, b) => {
                const h = hash(a.st + b.st) % 100 / 100;
                return 0.15 + h * 0.5;
              };
              return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 300 150", style: { width: "100%", display: "block" }, children: [
                pos.map((p, i) => pos.slice(i + 1).map((q, j) => {
                  const k = coup(p, q);
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: p.x, y1: p.y, x2: q.x, y2: q.y, stroke: INK, strokeOpacity: 0.05 + k * 0.18, strokeWidth: 0.3 + k * 1.6 }, i + "-" + j);
                })),
                pos.map((p, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { onClick: () => {
                  setSelSt(p.st);
                  setCouplingScope("state");
                }, style: { cursor: "pointer" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: p.x, cy: p.y, r: 3.5 + Math.min(7, p.n / 8), fill: p.cond.color, stroke: "#fff", strokeWidth: "0.8" }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: p.x, y: p.y - 8, fontSize: "6.5", textAnchor: "middle", fill: INK, fontFamily: "Helvetica", fontWeight: 700, children: p.st })
                ] }, i)),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "6", y: "146", fontSize: "7", fill: MUT, fontFamily: "Helvetica", children: "node size = unit count \xB7 click a state to drill into its units" })
              ] });
            })() : (() => {
              const nodes = stCenters.slice(0, 8);
              const N = nodes.length, cx = 150, cy = 70, R = 52;
              const pos = nodes.map((c, i) => {
                const ang = i / N * Math.PI * 2 - Math.PI / 2;
                return { c, x: cx + Math.cos(ang) * R, y: cy + Math.sin(ang) * R };
              });
              const focusI = pos.findIndex((p) => p.c.name === center.name);
              const coup = (a, b) => {
                const h = hash(a.name + b.name) % 100 / 100;
                const same = a.st === b.st ? 0.4 : 0;
                return Math.min(0.95, 0.25 + same + h * 0.4);
              };
              const committedHere = !!dyn.committed[center.name];
              return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 300 140", style: { width: "100%", display: "block" }, children: [
                pos.map((p, i) => pos.slice(i + 1).map((q, j) => {
                  const k = coup(p.c, q.c);
                  const touchesFocus = focusI >= 0 && (p.c.name === center.name || q.c.name === center.name);
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: p.x, y1: p.y, x2: q.x, y2: q.y, stroke: touchesFocus && committedHere ? AC : INK, strokeOpacity: touchesFocus ? committedHere ? 0.55 : 0.3 : 0.08, strokeWidth: touchesFocus ? 0.5 + k * 2.2 : 0.4 }, i + "-" + j);
                })),
                pos.map((p, i) => {
                  const isFocus = p.c.name === center.name;
                  const cond = conditionOf(p.c);
                  const k = focusI >= 0 ? coup(p.c, center) : 0;
                  const influenced = committedHere && isFocus === false && k > 0.55;
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { onClick: () => setSelName(p.c.name), style: { cursor: "pointer" }, children: [
                    influenced && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: p.x, cy: p.y, r: "9", fill: "none", stroke: AC, strokeOpacity: "0.5", strokeWidth: "0.8" }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: p.x, cy: p.y, r: isFocus ? 6.5 : 4.6, fill: cond.color, stroke: isFocus ? INK : "#fff", strokeWidth: isFocus ? 1.4 : 0.8 }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: p.x, y: p.y - 8.5, fontSize: "6.5", textAnchor: "middle", fill: INK, fontFamily: "Helvetica", fontWeight: isFocus ? 700 : 400, children: p.c.name.length > 10 ? p.c.name.slice(0, 9) + "\u2026" : p.c.name })
                  ] }, i);
                }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "6", y: "134", fontSize: "7", fill: MUT, fontFamily: "Helvetica", children: committedHere ? "ring = units most likely to benefit from the committed path" : "commit a path below to see the propagation radius" })
              ] });
            })()
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 4 }, children: "Concurrent support paths \u2014 held until the Director commits one" }),
            paths.map((p) => {
              const isC = dyn.committed[center.name] === p.id, closed = dyn.committed[center.name] && !isC;
              return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${isC ? GRN : RULE}`, padding: "7px 9px", marginBottom: 6, opacity: closed ? 0.45 : 1, background: isC ? "#f3f8f3" : "#fff" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", fontFamily: "Helvetica", fontSize: 9.5 }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: p.n }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
                    p.risk,
                    " risk \xB7 ",
                    p.window,
                    "d review window"
                  ] })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: "#555", margin: "2px 0 5px" }, children: p.d }),
                isC ? dyn.completed && dyn.completed[center.name] === p.id ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: INK }, children: "\u2713 Completed \u2014 outcome measured, review clock closed" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: GRN }, children: [
                  "Committed \u2014 alternatives closed \xB7 human-owned ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => completePath(center, p), style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, padding: "2px 7px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", color: INK, marginLeft: 6 }, children: "Mark complete" })
                ] }) : closed ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "closed on commitment of the selected path" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => commit(center, p), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "3px 9px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", color: INK }, children: "Commit this path" })
              ] }, p.id);
            }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: "Candidate paths are held concurrently so the review, not the proposal order, decides. Commitment is a Director action; it closes the alternatives, starts the review clock, and is written to the shared session record." })
          ] })
        ] })
      ] })
    ] });
  }
  var ALIGN_AGENTS = ["Growth", "Unit Health", "Retention", "Network Propagation"];
  var ALIGN_VIEWS = [
    ["franchise", "Overview", "today's one governed action"],
    ["rail", "Decision Rail", "proposals under review"],
    ["board", "Operations Board", "interventions in parallel"],
    ["table", "Network Map", "the whole territory, spatially"],
    ["lenses", "Six Lenses", "one unit, six roles"],
    ["dynamics", "Operations Dynamics", "uncertainty & cadence"]
  ];
  function SystemAlignmentTab({ centers, states, railData, decisions, dyn, opt, ledger, jumpTo }) {
    const recs = railData.recommendations;
    const conflicts = railData.conflicts || [];
    const ageOf = (c) => opt.fresh[c.name] !== void 0 ? (opt.week - opt.fresh[c.name]) * 7 : hash(c.name) % 38 + opt.week * 2;
    const byAgent = ALIGN_AGENTS.map((a) => ({ a, n: recs.filter((r) => r.agent === a).length, allowed: recs.filter((r) => r.agent === a && r.governance.allowed).length }));
    const allowedN = recs.filter((r) => r.governance.allowed).length, heldN = recs.length - allowedN;
    const decidedIds = Object.keys(decisions);
    const traced = decidedIds.filter((id) => recs.some((r) => r.id === id)).length;
    const confs = centers.map((c) => qCoherence(ageOf(c)));
    const avgConf = +(confs.reduce((x, y) => x + y, 0) / confs.length).toFixed(2);
    const freshPct = Math.round(100 * centers.filter((c) => ageOf(c) <= 28).length / centers.length);
    const lowConfN = confs.filter((v) => v < 0.5).length;
    const committedN = Object.keys(dyn.committed || {}).length, completedN = Object.keys(dyn.completed || {}).length;
    const simpleAvgHealth = +(centers.reduce((a, c) => a + c.health, 0) / centers.length).toFixed(1);
    const totalConf = confs.reduce((a, v) => a + v, 0);
    const weightedAvgHealth = +(centers.reduce((a, c, idx) => a + c.health * confs[idx], 0) / totalConf).toFixed(1);
    const rollupDelta = +(weightedAvgHealth - simpleAvgHealth).toFixed(1);
    const recentLog = (ledger || []).slice(0, 40);
    const actCounts = { approved: 0, held: 0, queued: 0, other: 0 };
    recentLog.forEach((e) => {
      const t = (e.text || "").toLowerCase();
      if (t.includes("approved")) actCounts.approved++;
      else if (t.includes("held") || t.includes("hold")) actCounts.held++;
      else if (t.includes("queued") || t.includes("re-measur")) actCounts.queued++;
      else actCounts.other++;
    });
    const bySource = {};
    recentLog.forEach((e) => {
      bySource[e.tab] = (bySource[e.tab] || 0) + 1;
    });
    const AGY = [52, 116, 180, 244];
    const VY = [26, 74, 122, 170, 218, 266];
    const capacityBlockedRoles = new Set(recs.filter((r) => r.supplyGate && r.supplyGate.blocked && /capacity/.test(r.supplyGate.reason || "")).map((r) => r.approverRole));
    const viewFlagged = {
      franchise: conflicts.length > 0,
      rail: conflicts.length > 0 || capacityBlockedRoles.size > 0,
      board: conflicts.length > 0 || capacityBlockedRoles.size > 0,
      table: conflicts.length > 0,
      // the map reads the same railData.conflicts and rings the affected territory
      lenses: conflicts.length > 0,
      // Six Lenses shows the conflict once navigated to the affected unit -- previously hardcoded false even after that capability was built
      dynamics: conflicts.length > 0
      // Operations Dynamics now shows a conflict warning for the selected unit
    };
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: "The operating model at full scale, governed by four standing rules: the whole network state is preserved before any operational view is derived; a view exists only where it serves diagnosis, prioritization, or governed action; the governance boundary is where a proposal becomes real; and every commitment is human-owned. The agents below do not create separate truths for growth, health, retention, or propagation \u2014 they propose against one canonical system, and the integrity monitor reads that same state." }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "12px 14px", marginBottom: 10 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 6 }, children: "One canonical state \u2192 four agents \u2192 one governance boundary \u2192 operating views (live counts)" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 940 310", style: { width: "100%", display: "block" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", { x: "14", y: "95", width: "168", height: "120", fill: "#fbfbf9", stroke: INK, strokeWidth: "1.6" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "98", y: "128", fontSize: "12", fontWeight: "700", textAnchor: "middle", fill: INK, fontFamily: "Helvetica", children: "CANONICAL STATE" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("text", { x: "98", y: "148", fontSize: "10", textAnchor: "middle", fill: MUT, fontFamily: "Helvetica", children: [
            centers.length,
            " units \xB7 40 states"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("text", { x: "98", y: "164", fontSize: "10", textAnchor: "middle", fill: MUT, fontFamily: "Helvetica", children: [
            "week ",
            opt.week,
            " \xB7 one clock"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "98", y: "180", fontSize: "10", textAnchor: "middle", fill: MUT, fontFamily: "Helvetica", children: "one decision record" }),
          byAgent.map((g, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: "182", y1: 155, x2: "252", y2: AGY[i] + 22, stroke: RULE, strokeWidth: "1.2" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", { x: "252", y: AGY[i], width: "180", height: "44", fill: "#fff", stroke: GRN, strokeWidth: "1.3" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("text", { x: "262", y: AGY[i] + 18, fontSize: "10.5", fontWeight: "700", fill: INK, fontFamily: "Helvetica", children: [
              g.a,
              " agent"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("text", { x: "262", y: AGY[i] + 34, fontSize: "9", fill: MUT, fontFamily: "Helvetica", children: [
              g.n,
              " proposals \xB7 ",
              g.allowed,
              " clear guardrails"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: "432", y1: AGY[i] + 22, x2: "486", y2: 155, stroke: RULE, strokeWidth: "1.2" })
          ] }, g.a)),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", { x: "486", y: "40", width: "14", height: "230", fill: INK }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "493", y: "30", fontSize: "9.5", fontWeight: "700", textAnchor: "middle", fill: INK, fontFamily: "Helvetica", children: "GOVERNANCE BOUNDARY" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "493", y: "292", fontSize: "8.5", textAnchor: "middle", fill: MUT, fontFamily: "Helvetica", children: "contracts \xB7 fail-closed \xB7 named approver" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("text", { x: "472", y: "160", fontSize: "8.5", textAnchor: "end", fill: GRN, fontFamily: "Helvetica", fontWeight: "700", children: [
            allowedN,
            " allowed \u2192"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("text", { x: "472", y: "176", fontSize: "8.5", textAnchor: "end", fill: AMB, fontFamily: "Helvetica", fontWeight: "700", children: [
            heldN,
            " held"
          ] }),
          ALIGN_VIEWS.map(([id, name, q], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { onClick: () => jumpTo && jumpTo(id, null), style: { cursor: jumpTo ? "pointer" : "default" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: "500", y1: 155, x2: "560", y2: VY[i] + 21, stroke: RULE, strokeWidth: "1.2" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", { x: "560", y: VY[i], width: "200", height: "42", fill: "#fff", stroke: viewFlagged[id] ? AC : INK, strokeWidth: viewFlagged[id] ? "1.6" : "1.1" }),
            viewFlagged[id] && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: "750", cy: VY[i] + 10, r: "4", fill: AC, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("title", { children: "This view currently has an unresolved conflict or a role at capacity" }) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "570", y: VY[i] + 17, fontSize: "10.5", fontWeight: "700", fill: INK, fontFamily: "Helvetica", children: name }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "570", y: VY[i] + 32, fontSize: "8.5", fill: MUT, fontFamily: "Helvetica", children: q })
          ] }, id)),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", { x: "790", y: "120", width: "136", height: "70", fill: "#f5f8f5", stroke: GRN, strokeWidth: "1.4" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "858", y: "147", fontSize: "10", fontWeight: "700", textAnchor: "middle", fill: GRN, fontFamily: "Helvetica", children: "DIRECTOR" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "858", y: "163", fontSize: "8.5", textAnchor: "middle", fill: MUT, fontFamily: "Helvetica", children: "every commitment" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "858", y: "176", fontSize: "8.5", textAnchor: "middle", fill: MUT, fontFamily: "Helvetica", children: "human-owned" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: "760", y1: "155", x2: "790", y2: "155", stroke: GRN, strokeWidth: "1.4" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 4 }, children: "Views are clickable. Nothing right of the boundary is a copy \u2014 every surface reads the state on the left, and a decision made in any view is the same decision everywhere." })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", marginBottom: 10 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { padding: "8px 12px 2px", fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT }, children: "Network integrity monitor \u2014 read live from the canonical state" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap" }, children: [
          [avgConf, "avg measurement confidence", avgConf < 0.5 ? AC : GRN],
          [freshPct + "%", "units within the 28-day window", freshPct < 50 ? AC : INK],
          [lowConfN, "units below 0.50 confidence \u2014 re-measure first", lowConfN ? AMB : GRN],
          [traced + " / " + decidedIds.length, "decisions traced to a governed proposal", traced === decidedIds.length ? GRN : AC],
          [committedN + " \xB7 " + completedN, "support paths committed \xB7 completed", INK],
          [conflicts.length, conflicts.length ? "cross-agent conflicts \u2014 open now" : "cross-agent conflicts \u2014 checked, none open", conflicts.length ? AC : GRN]
        ].map(([v, l, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 160px", padding: "8px 12px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 18, fontWeight: 800, color: c, lineHeight: 1 }, children: v }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 2 }, children: l })
        ] }, i)) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { padding: "4px 12px 9px", fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: "Integrity here means three checks: confidence in what is measured, honesty about what has aged past its window, and a decision record in which every entry traces back to a governed proposal. If the last figure is ever not N of N, the record has an orphan \u2014 and that is a finding, not a formatting issue." })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, flexWrap: "wrap", marginBottom: 10 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 260px", border: `1px solid ${RULE}`, background: "#fff", padding: "9px 11px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 5 }, children: "Network health \u2014 two rollups" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 16 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 20, fontWeight: 800, color: INK }, children: simpleAvgHealth }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: "simple average" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 20, fontWeight: 800, color: "#7a6bd8" }, children: weightedAvgHealth }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: "confidence-weighted" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 20, fontWeight: 800, color: Math.abs(rollupDelta) >= 1 ? AC : GRN }, children: [
                rollupDelta > 0 ? "+" : "",
                rollupDelta
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: "delta" })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 4 }, children: "The weighted rollup discounts stale, low-confidence units rather than letting them carry equal weight with freshly measured ones. A large delta means the simple average is being meaningfully skewed by stale data." })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 260px", border: `1px solid ${RULE}`, background: "#fff", padding: "9px 11px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 5 }, children: [
            "Recent decision activity \u2014 last ",
            recentLog.length,
            " logged actions"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 14, marginBottom: 4 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: GRN }, children: actCounts.approved }),
              " approved"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: AMB }, children: actCounts.held }),
              " held"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: INK }, children: actCounts.queued }),
              " queued"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: MUT }, children: actCounts.other }),
              " other"
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: [
            "by source: ",
            Object.entries(bySource).map(([k, v]) => k + " " + v).join(" \xB7 ") || "no activity logged yet this session"
          ] })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 8, flexWrap: "wrap" }, children: [
        ["Rule 1", "Preserve the whole network state before deriving any operational view."],
        ["Rule 2", "Derive a view only where it serves diagnosis, prioritization, or governed action."],
        ["Rule 3", "The governance boundary is where a proposal becomes real."],
        ["Rule 4", "Every commitment is human-owned."]
      ].map(([r, t]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 200px", border: `1px solid ${RULE}`, background: "#fbfbf9", padding: "8px 10px" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AC, marginBottom: 3 }, children: r }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#333", lineHeight: 1.4 }, children: t })
      ] }, r)) })
    ] });
  }
  function OperationsBoardTab({ railData, decisions, setDecisions, decisionHistory, recordDecision, logL, opt, dyn, jumpTo }) {
    const committedPaths = dyn ? Object.entries(dyn.committed || {}) : [];
    const [view, setView] = (0, import_react.useState)("lanes");
    const [showPerf, setShowPerf] = (0, import_react.useState)(false);
    const [showShortcuts, setShowShortcuts] = (0, import_react.useState)(false);
    const [kbIdx, setKbIdx] = (0, import_react.useState)(0);
    const mountStart = (0, import_react.useRef)(typeof performance !== "undefined" ? performance.now() : 0);
    const [mountMs, setMountMs] = (0, import_react.useState)(null);
    (0, import_react.useEffect)(() => {
      const t = typeof performance !== "undefined" ? performance.now() - mountStart.current : 0;
      setMountMs(+t.toFixed(1));
    }, []);
    const recs = railData.recommendations;
    const boardConflicts = railData.conflicts || [];
    const boardConflictedIds = new Set(boardConflicts.flatMap((c) => [c.a, c.b]));
    const windowOf = (r) => ACTION_CONTRACTS[r.actionType] && ACTION_CONTRACTS[r.actionType].minFreshnessDays || 30;
    const overdueOf = (r) => Math.max(0, r.evidence.dataFreshnessDays - windowOf(r));
    const statusOf = (r) => boardStatusOf(r, decisions);
    const decide = (rec, d) => {
      setDecisions((m) => ({ ...m, [rec.id]: d }));
      recordDecision && recordDecision(rec.id, d);
      logL && logL("board", rec.agent, rec.title + " \u2014 " + (d === "approved" ? "approved by Director" : d === "measure" ? "queued for re-measurement" : "set aside") + " (from operations board)");
    };
    const counts = {};
    BOARD_COLS.forEach(([k]) => counts[k] = 0);
    recs.forEach((r) => counts[statusOf(r)]++);
    const overdueN = recs.filter((r) => overdueOf(r) > 0).length;
    const decidedN = counts.approved + counts.remeasure + counts.aside;
    const laneItems = [...recs].sort((a, b) => {
      const oa = overdueOf(a) > 0 ? 1 : 0, ob = overdueOf(b) > 0 ? 1 : 0;
      if (oa !== ob) return ob - oa;
      const ha = statusOf(a) === "held" ? 1 : 0, hb = statusOf(b) === "held" ? 1 : 0;
      if (ha !== hb) return hb - ha;
      return b.evidence.operationalSignals.dataConfidence - a.evidence.operationalSignals.dataConfidence;
    }).slice(0, 12);
    const MAXD = 60;
    const ownerChip = (r) => {
      const s = statusOf(r);
      return s === "approved" ? ["Director \xB7 committed", INK] : s === "remeasure" ? ["Director \xB7 re-measure", AMB] : s === "aside" ? ["Director \xB7 set aside", MUT] : ["agent \xB7 proposed", r.governance.allowed ? GRN : AMB];
    };
    const feedItems = [...recs].sort((a, b) => {
      const da = decisions[a.id] ? 1 : 0, db = decisions[b.id] ? 1 : 0;
      if (da !== db) return db - da;
      return overdueOf(b) - overdueOf(a);
    }).slice(0, 8);
    (0, import_react.useEffect)(() => {
      if (view !== "lanes") return;
      const onKey = (e) => {
        const tag = e.target && e.target.tagName || "";
        if (tag === "INPUT" || tag === "TEXTAREA" || e.metaKey || e.ctrlKey || e.altKey) return;
        const item = laneItems[kbIdx];
        if (e.key === "ArrowDown" || e.key === "j") {
          e.preventDefault();
          setKbIdx((i) => Math.min(laneItems.length - 1, i + 1));
        } else if (e.key === "ArrowUp" || e.key === "k") {
          e.preventDefault();
          setKbIdx((i) => Math.max(0, i - 1));
        } else if (e.key === "?") {
          e.preventDefault();
          setShowShortcuts((s) => !s);
        } else if (item && !decisions[item.id]) {
          if ((e.key === "a" || e.key === "A") && item.governance.allowed) {
            e.preventDefault();
            decide(item, "approved");
          } else if (e.key === "m" || e.key === "M") {
            e.preventDefault();
            decide(item, "measure");
          } else if (e.key === "i" || e.key === "I") {
            e.preventDefault();
            decide(item, "ignored");
          }
        }
      };
      window.addEventListener("keydown", onKey);
      return () => window.removeEventListener("keydown", onKey);
    }, [view, laneItems, kbIdx, decisions]);
    (0, import_react.useEffect)(() => {
      if (kbIdx >= laneItems.length) setKbIdx(0);
    }, [laneItems.length]);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StateViewBanner, { thisView: "Operations Board" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: "Every open intervention across the network, visible concurrently. Three views \u2014 lanes, board, feed \u2014 read one shared decision record; an approval made here is the same approval the Decision Rail and Overview show. Agents propose; every commitment is human-owned." }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, marginBottom: 6, flexWrap: "wrap" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setShowPerf((s) => !s), "aria-pressed": showPerf, "aria-label": "Toggle render timing display", style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, cursor: "pointer", textDecoration: "underline", background: "none", border: "none", padding: 0 }, children: [
          showPerf ? "hide" : "show",
          " render timing"
        ] }),
        showPerf && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, padding: "2px 7px", border: `1px solid ${mountMs !== null && mountMs < 200 ? GRN : AMB}`, color: mountMs !== null && mountMs < 200 ? GRN : AMB }, children: [
          "mount: ",
          mountMs === null ? "measuring\u2026" : mountMs + "ms"
        ] }),
        view === "lanes" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setShowShortcuts((s) => !s), "aria-pressed": showShortcuts, "aria-label": "Toggle keyboard shortcuts help", style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, cursor: "pointer", textDecoration: "underline", background: "none", border: "none", padding: 0 }, children: [
          showShortcuts ? "hide" : "show",
          " keyboard shortcuts"
        ] }),
        view === "lanes" && showShortcuts && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "\u2191/\u2193 or j/k select \xB7 A approve \xB7 M measure again \xB7 I ignore \xB7 ? toggle this" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 0, flexWrap: "wrap", border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", marginBottom: 8 }, children: [[recs.length, "interventions in flight", INK], [counts.proposed, "pending Director review", GRN], [counts.held, "held \u2014 guardrail or data", AMB], [overdueN, "past measurement cadence", overdueN ? AC : GRN], [decidedN, "decided this session", INK]].map(([v, l, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 120px", padding: "8px 12px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 19, fontWeight: 800, color: c, lineHeight: 1 }, children: v }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 2 }, children: l })
      ] }, i)) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 6, marginBottom: 8, alignItems: "center" }, children: [
        [["lanes", "Lanes \u2014 cadence view"], ["board", "Board \u2014 by decision state"], ["feed", "Feed \u2014 governed trace"]].map(([id, label]) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setView(id), style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "5px 12px", cursor: "pointer", border: `1px solid ${view === id ? INK : RULE}`, background: view === id ? INK : "#fff", color: view === id ? "#fff" : MUT }, children: label }, id)),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { marginLeft: "auto", fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: [
          "week ",
          opt.week,
          " \xB7 one decision record across all views"
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => downloadJSON(decisionLedgerExport(decisions, railData), "cn-decision-ledger.json"), title: "Export the governed decision record as JSON", "aria-label": "Export decision ledger as JSON", style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "4px 9px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: MUT }, children: "export ledger \u2193" })
      ] }),
      committedPaths.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${GRN}`, background: "#f5f8f5", padding: "8px 11px", marginBottom: 8 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: GRN, marginBottom: 5 }, children: [
          "Support paths committed in Operations Dynamics \u2014 ",
          committedPaths.length,
          " in progress"
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 6, flexWrap: "wrap" }, children: committedPaths.map(([name, pid]) => {
          const done = dyn.completed && dyn.completed[name] === pid;
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => jumpTo && jumpTo("dynamics", name), style: { fontFamily: "Helvetica", fontSize: 9, border: `1px solid ${done ? INK : RULE}`, background: done ? "#f0f0ee" : "#fff", padding: "4px 8px", cursor: jumpTo ? "pointer" : "default" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: name }),
            " ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
              "\xB7 ",
              pid,
              " \xB7 ",
              done ? "\u2713 completed" : "review clock running"
            ] })
          ] }, name);
        }) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 5 }, children: "These are Director-committed interventions from the dynamics view, tracked here alongside agent-proposed actions. Click any to return to its unit." })
      ] }),
      view === "lanes" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", marginBottom: 6 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT }, children: [
            "Contract window vs. data age \u2014 12 highest-priority of ",
            recs.length
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: [
            "bar = review window \xB7 marker = current data age \xB7 0\u2013",
            MAXD,
            "d"
          ] })
        ] }),
        laneItems.map((r, ri) => {
          const w = windowOf(r), age = r.evidence.dataFreshnessDays, over = overdueOf(r), [oLabel, oCol] = ownerChip(r), kbSel = view === "lanes" && ri === kbIdx;
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => setKbIdx(ri), style: { display: "flex", alignItems: "center", gap: 8, padding: "5px 0 5px 6px", borderTop: `1px solid #f2f2f2`, borderLeft: kbSel ? `3px solid ${INK}` : "3px solid transparent", background: kbSel ? "#fafaf8" : "transparent", cursor: "pointer" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "0 0 210px", minWidth: 0 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, children: [
                boardConflictedIds.has(r.id) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { title: "Conflicts with another agent's proposal on the same unit", style: { color: "#fff", background: AC, fontSize: 7.5, padding: "0 3px", marginRight: 4, cursor: "help" }, children: "!" }),
                r.title
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: [
                r.agent,
                " \xB7 ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: oCol, fontWeight: 700 }, children: oLabel })
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: 1, position: "relative", height: 16, background: "#f7f6f2", border: `1px solid ${RULE}` }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", left: 0, top: 0, bottom: 0, width: Math.min(100, w / MAXD * 100) + "%", background: statusOf(r) === "approved" ? "#e8efe8" : statusOf(r) === "held" ? "#f5efe0" : "#eef2ee", borderRight: `1px solid ${RULE}` } }),
              over > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", left: Math.min(100, w / MAXD * 100) + "%", top: 0, bottom: 0, width: Math.min(100 - Math.min(100, w / MAXD * 100), over / MAXD * 100) + "%", background: "#f3d9cf" } }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", left: Math.min(99, age / MAXD * 100) + "%", top: -2, bottom: -2, width: 2, background: over > 0 ? AC : INK } }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { position: "absolute", right: 4, top: 1, fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: over > 0 ? AC : MUT }, children: over > 0 ? "+" + over + "d past cadence" : age + "d / " + w + "d" })
            ] })
          ] }, r.id);
        }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 8 }, children: "An intervention past its window is not failed \u2014 it is unmeasured. Re-measurement restores decision eligibility; the guardrail that enforces this is the same one the Decision Rail applies." })
      ] }),
      view === "board" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 8, alignItems: "flex-start", overflowX: "auto" }, children: BOARD_COLS.map(([key, label, col]) => {
        const items = recs.filter((r) => statusOf(r) === key);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 170px", minWidth: 170, border: `1px solid ${RULE}`, background: "#fafaf8" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "7px 9px", borderBottom: `2px solid ${col}`, display: "flex", justifyContent: "space-between", alignItems: "baseline" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.4, textTransform: "uppercase", color: col }, children: label }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 10, color: col }, children: items.length })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "6px" }, children: [
            items.slice(0, 6).map((r) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "6px 8px", marginBottom: 6 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, lineHeight: 1.3 }, children: [
                boardConflictedIds.has(r.id) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { title: "Conflicts with another agent proposal on the same unit", style: { color: "#fff", background: AC, fontSize: 7.5, padding: "0 3px", marginRight: 4, cursor: "help" }, children: "!" }),
                r.title
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, margin: "2px 0 4px" }, children: [
                r.agent,
                " \xB7 ",
                r.confidenceBand,
                " confidence \xB7 ",
                r.riskBand,
                " risk"
              ] }),
              key === "proposed" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 4 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => decide(r, "approved"), style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, padding: "2px 7px", cursor: "pointer", border: `1px solid ${GRN}`, background: GRN, color: "#fff" }, children: "Approve" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => decide(r, "measure"), style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, padding: "2px 7px", cursor: "pointer", border: `1px solid ${AMB}`, background: "#fff", color: AMB }, children: "Measure" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => decide(r, "ignored"), style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, padding: "2px 7px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: MUT }, children: "Set aside" })
              ] }),
              key === "held" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: AMB }, children: r.governance.heldOn.join(", ") || "data freshness" }),
              key === "approved" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: INK, fontWeight: 700 }, children: [
                "approver: ",
                r.approverRole
              ] })
            ] }, r.id)),
            items.length > 6 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, textAlign: "center", padding: "2px 0 4px" }, children: [
              "+",
              items.length - 6,
              " more in this state"
            ] }),
            items.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, padding: "2px 0 4px" }, children: "none" })
          ] })
        ] }, key);
      }) }),
      view === "feed" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 6 }, children: "Governed trace \u2014 decided items first, then most overdue" }),
        feedItems.map((r) => {
          const s = statusOf(r), over = overdueOf(r);
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "8px 0", borderTop: `1px solid #f0f0f0` }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", gap: 8, alignItems: "baseline" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700 }, children: [
                boardConflictedIds.has(r.id) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { title: "Conflicts with another agent proposal on the same unit", style: { color: "#fff", background: AC, fontSize: 7.5, padding: "0 3px", marginRight: 4, cursor: "help" }, children: "!" }),
                r.title
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, padding: "1px 6px", border: `1px solid ${s === "approved" ? INK : s === "proposed" ? GRN : s === "held" ? AMB : MUT}`, color: s === "approved" ? INK : s === "proposed" ? GRN : s === "held" ? AMB : MUT }, children: s === "proposed" ? "NEEDS DIRECTOR" : s === "held" ? "HELD" : s === "remeasure" ? "RE-MEASURE" : s === "approved" ? "APPROVED" : "SET ASIDE" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 5, flexWrap: "wrap", margin: "4px 0" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8, padding: "2px 6px", border: `1px solid ${RULE}`, color: "#444" }, children: [
                r.agent,
                " agent \u2192 proposed"
              ] }),
              r.evidence.contractVerdicts.map((v, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8, padding: "2px 6px", border: `1px solid ${v.pass ? GRN : AC}`, color: v.pass ? GRN : AC }, children: [
                v.pass ? "\u2713" : "\u2715",
                " ",
                v.label
              ] }, i)),
              r.suggestedActionPlan && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8, padding: "2px 6px", border: `1px solid ${RULE}`, color: "#444" }, children: [
                "\u2192 ",
                r.suggestedActionPlan.n
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8, padding: "2px 6px", border: `1px solid ${INK}`, color: INK }, children: [
                "approver: ",
                r.approverRole,
                " (human-owned)"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: [
              "data confidence ",
              r.evidence.operationalSignals.dataConfidence.toFixed(2),
              " \xB7 data age ",
              r.evidence.dataFreshnessDays,
              "d",
              over > 0 ? " \xB7 " + over + "d past review cadence" : ""
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: r.governance.allowed ? GRN : AMB, fontStyle: "italic", marginTop: 1 }, children: rationaleOf(r) }),
            s === "proposed" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 5, marginTop: 5 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => decide(r, "approved"), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "3px 9px", cursor: "pointer", border: `1px solid ${GRN}`, background: GRN, color: "#fff" }, children: "Approve" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => decide(r, "measure"), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "3px 9px", cursor: "pointer", border: `1px solid ${AMB}`, background: "#fff", color: AMB }, children: "Measure again" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => decide(r, "ignored"), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "3px 9px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: MUT }, children: "Set aside" })
            ] })
          ] }, r.id);
        })
      ] })
    ] });
  }
  function DecisionRailTab({ railData, logL, decisions, setDecisions, decisionHistory, recordDecision, jumpTo, opt }) {
    const result = railData;
    const [agentFilter, setAgentFilter] = (0, import_react.useState)("all");
    const agents = ["all", "Growth", "Unit Health", "Retention", "Network Propagation"];
    const postureApproved = !!(opt && opt.quantum && opt.quantum.approved);
    const onDecide = (rec, d) => {
      if (d === "approved" && rec.scope === "territory" && !postureApproved) {
        logL && logL("rail", rec.agent, rec.title + " \u2014 approval blocked: no network posture approved in Quantum PM");
        return;
      }
      setDecisions((m) => ({ ...m, [rec.id]: d }));
      recordDecision && recordDecision(rec.id, d);
      logL && logL("rail", rec.agent, rec.title + " \u2014 " + (d === "approved" ? "approved by Director" : d === "measure" ? "queued for re-measurement" : "ignored"));
    };
    const visible = (list) => agentFilter === "all" ? list : list.filter((r) => r.agent === agentFilter);
    const actionable = visible(result.actionable), held = visible(result.held);
    const conflicts = result.conflicts || [];
    const conflictedIds = new Set(conflicts.flatMap((c) => [c.a, c.b]));
    const workload = {};
    result.recommendations.forEach((r) => {
      const role = r.approverRole;
      workload[role] = workload[role] || { pending: 0, decided: 0 };
      if (decisions[r.id]) workload[role].decided++;
      else workload[role].pending++;
    });
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      conflicts.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${AC}`, borderLeft: `3px solid ${AC}`, background: "#fdf3f2", padding: "7px 11px", marginBottom: 8 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AC, marginBottom: 4 }, children: [
          conflicts.length,
          " cross-agent conflict",
          conflicts.length > 1 ? "s" : "",
          " detected \u2014 review before acting on either side"
        ] }),
        conflicts.slice(0, 4).map((c, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#5a2020", marginBottom: 2 }, children: [
          "\u2022 ",
          c.reason
        ] }, i))
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", flexWrap: "wrap", gap: 8, border: `1px solid ${RULE}`, background: "#fbfbf9", padding: "7px 11px", marginBottom: 8 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT }, children: "Approver workload this cycle:" }),
        Object.entries(workload).map(([role, w]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: role }),
          ": ",
          w.decided,
          " decided, ",
          w.pending,
          " pending"
        ] }, role))
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 0, flexWrap: "wrap", border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", marginBottom: 10 }, children: [[result.actionable.length, "actionable now", GRN], [result.held.length, "held \u2014 data or guardrail", AMB], [Object.values(decisions).filter((d) => d === "approved").length, "approved this session", INK]].map(([v, l, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 140px", padding: "8px 12px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 19, fontWeight: 800, color: c, lineHeight: 1 }, children: v }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 2 }, children: l })
      ] }, i)) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 6, marginBottom: 10, flexWrap: "wrap" }, children: agents.map((a) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setAgentFilter(a), style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "4px 10px", cursor: "pointer", border: `1px solid ${agentFilter === a ? INK : RULE}`, background: agentFilter === a ? INK : "#fff", color: agentFilter === a ? "#fff" : MUT }, children: a }, a)) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: GRN, marginBottom: 6 }, children: [
        "Actionable \u2014 ",
        actionable.length
      ] }),
      actionable.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT, marginBottom: 12 }, children: "nothing actionable for this filter" }),
      actionable.map((r) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RecommendationCard, { rec: r, decision: decisions[r.id], history: decisionHistory && decisionHistory[r.id], conflicted: conflictedIds.has(r.id), onDecide, jumpTo, postureApproved }, r.id)),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AMB, margin: "14px 0 6px" }, children: [
        "Held \u2014 ",
        held.length
      ] }),
      held.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT }, children: "nothing held for this filter" }),
      held.map((r) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RecommendationCard, { rec: r, decision: decisions[r.id], history: decisionHistory && decisionHistory[r.id], conflicted: conflictedIds.has(r.id), onDecide, jumpTo, postureApproved }, r.id)),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 10, borderTop: `1px solid ${RULE}`, paddingTop: 6 }, children: "Agents propose; every action is human-owned \u2014 nothing here writes to a center's live record without an explicit approval. Approve/Ignore/Measure again are logged to the record ledger for this session." })
    ] });
  }
  function OpsSystem({ view }) {
    const [phase, setPhase] = (0, import_react.useState)("acquire");
    const [expanded, setExpanded] = (0, import_react.useState)(null);
    const [expConn, setExpConn] = (0, import_react.useState)(null);
    const isPhaseView = ["acquire", "deliver", "retain", "operate"].includes(view);
    const activePhase = isPhaseView ? view : phase;
    const pm = PHASE_META[activePhase];
    const steps = W.filter((s) => s.p === activePhase);
    const org = ORG_ROLES[activePhase];
    const agile = AGILE[activePhase];
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", color: INK, lineHeight: 1.6, fontSize: 14 }, children: [
      view === "network" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 16, fontWeight: 700, marginBottom: 8 }, children: "Network \u2014 30,000ft" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { style: { color: "#444", marginBottom: 12 }, children: "How 350+ centers operate as one governed system. The Director of Franchise Development sits at the intersection of network strategy and center-level operations." }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 6 }, children: "Network Tiers" }),
        NET_TIERS.map((t, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "5px 0", borderTop: i ? `1px solid #eee` : "none" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontSize: 14.5 }, children: t.n }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 10, color: MUT }, children: t.s })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 13.5, color: "#444" }, children: t.d })
        ] }, i)),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 6, marginTop: 18 }, children: "Network Standards \u2014 N-Series" }),
        CONNECTIVITY.map((c) => {
          const open = expConn === c.n;
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => setExpConn(open ? null : c.n), style: { padding: "5px 0", borderTop: `1px solid #eee`, cursor: "pointer" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "baseline", gap: 6 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, color: AC, minWidth: 30 }, children: [
                "N-",
                c.n
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontWeight: 600, fontSize: 14 }, children: c.t }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { marginLeft: "auto", fontFamily: "Helvetica", fontSize: 14, color: MUT }, children: open ? "\u2013" : "+" })
            ] }),
            open && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { paddingLeft: 36, marginTop: 4 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 13, color: "#444", lineHeight: 1.5 }, children: c.d }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#2a7a2a", marginTop: 5 }, children: [
                "ACCEPTANCE: ",
                c.accept
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: AC, marginTop: 1 }, children: [
                "GUARDRAIL: ",
                c.guard
              ] })
            ] })
          ] }, c.n);
        }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 6, marginTop: 18 }, children: "PM Dashboard" }),
        DASHBOARD.map((d, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "3px 0", borderTop: i ? `1px solid #eee` : "none", display: "flex", justifyContent: "space-between" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 13.5 }, children: d.m }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 12 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: d.v }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 10, color: d.c ? AC : MUT, marginLeft: 6 }, children: d.t })
          ] })
        ] }, i))
      ] }),
      isPhaseView && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 14 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 16, fontWeight: 700 }, children: [
            pm.name,
            " ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 400, color: MUT }, children: [
              "\u2014 ",
              pm.range,
              " (",
              pm.count,
              " steps)"
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 14, color: "#444", marginTop: 3 }, children: pm.desc })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 6 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT }, children: "Workflow \u2014 Every Step" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: AC }, children: "LEVEL 1 \xB7 DAG" })
        ] }),
        steps.map((step, i) => {
          const isOpen = expanded === step.n;
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => setExpanded(isOpen ? null : step.n), style: { padding: "6px 0", borderTop: i ? `1px solid ${step.gate ? "#daa" : "#eee"}` : "none", cursor: "pointer" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "baseline", gap: 6 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: step.gate ? AC : step.decision ? "#4444aa" : MUT, minWidth: 22 }, children: step.n }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontWeight: step.gate ? 700 : 600, fontSize: 14, color: step.gate ? AC : INK, flex: 1 }, children: [
                step.s,
                CRIT_STEPS.includes(step.n) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: "#fff", background: AC, padding: "1px 4px", borderRadius: 2, marginLeft: 6, verticalAlign: "middle" }, children: "CP" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 11, color: MUT, flexShrink: 0 }, children: step.o })
            ] }),
            isOpen && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { paddingLeft: 28, marginTop: 4 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, color: MUT, marginBottom: 2 }, children: [
                step.sys,
                step.t ? ` \xB7 ${step.t}` : "",
                step.gate ? " \xB7 APPROVAL GATE" : "",
                step.decision ? " \xB7 DECISION POINT" : "",
                step.back ? " \xB7 LOOP CLOSURE" : "",
                CRIT_STEPS.includes(step.n) ? " \xB7 CRITICAL PATH" : "",
                step.gr ? ` \xB7 guardrail: ${step.gr}` : ""
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 13.5, color: "#444", lineHeight: 1.6 }, children: step.d })
            ] })
          ] }, step.n);
        }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 6, marginTop: 18 }, children: [
          "Org Structure \u2014 ",
          pm.name
        ] }),
        org.map((r, i) => {
          const [role, ...rest] = r.split(" \u2014 ");
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "3px 0", borderTop: i ? `1px solid #eee` : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontSize: 13.5 }, children: role }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontSize: 13, color: "#555" }, children: [
              " \u2014 ",
              rest.join(" \u2014 ")
            ] })
          ] }, i);
        }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 6, marginTop: 18 }, children: "Chain Summary" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 13.5, color: "#444", lineHeight: 1.7 }, children: steps.filter((s) => s.gate || s.decision || s.back).map((s, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
          i > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: AC }, children: " \u2192 " }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontWeight: s.gate ? 700 : 400, color: s.gate ? AC : s.back ? "#2a7a2a" : INK }, children: [
            s.s,
            s.gate ? " [G]" : "",
            s.decision ? " [?]" : "",
            s.back ? " [\u21A9]" : ""
          ] })
        ] }, i)) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 6, marginTop: 18 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT }, children: "Flow & Cadence" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: AC }, children: "LEVELS 2\u20133" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, color: MUT, letterSpacing: 0.5, marginBottom: 3 }, children: "KANBAN + WIP LIMITS" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(4,1fr)", gap: 4, marginBottom: 4 }, children: ["Backlog", "Active", "Review", "Done"].map((col, ci) => {
          const items = agile.kanban[ci] || [];
          const count = items.reduce((a, s) => a + (parseInt(s) || 1), 0);
          const wip = agile.wip[ci];
          const over = wip < 50 && count > wip;
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, textTransform: "uppercase", letterSpacing: 0.4, borderBottom: `2px solid ${over ? AC : ci === 3 ? "#2a7a2a" : ci === 0 ? "#aaa" : "#888"}`, paddingBottom: 2, marginBottom: 3, color: over ? AC : INK, display: "flex", justifyContent: "space-between" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: col }),
              wip < 50 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontSize: 9 }, children: [
                over ? "\u26A0" : "",
                "\u2264",
                wip
              ] })
            ] }),
            items.map((item, ii) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 11.5, color: "#444", padding: "2px 0", borderTop: ii ? `1px solid #f0f0f0` : "none", lineHeight: 1.4 }, children: item }, ii))
          ] }, ci);
        }) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, color: MUT, letterSpacing: 0.5, marginBottom: 2, marginTop: 10 }, children: "SPRINT + VELOCITY" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 13, color: "#444", marginBottom: 8 }, children: agile.sprint }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", alignItems: "flex-end", gap: 2, height: 36 }, children: agile.velocity.map((v, i) => {
          const max = Math.max(...agile.velocity);
          return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1, height: Math.max(3, v / max * 34), background: i === agile.velocity.length - 1 ? INK : "#ccc" } }, i);
        }) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", fontFamily: "Helvetica", fontSize: 10, color: MUT, marginTop: 1, marginBottom: 12 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: "8 cycles ago" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: "velocity \xB7 now" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { borderLeft: `2px solid ${AC}`, paddingLeft: 10, fontSize: 12.5, color: "#555" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 10, color: AC }, children: "LEVEL 4 \xB7 GOVERNANCE" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("br", {}),
          "This phase's flow and cadence roll up to one outcome metric: ",
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: agile.outcome }),
          "."
        ] })
      ] }),
      view === "plan" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 16, fontWeight: 700, marginBottom: 4 }, children: "First 90 Days" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { style: { color: "#444", marginBottom: 8 }, children: "Sense-and-respond rollout: observe state \u2192 locate binding constraint \u2192 run one bounded pilot \u2192 measure \u2192 propagate what the data validates." }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${AC}`, background: "#fdfdfb", padding: "8px 10px", marginBottom: 14 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AC, marginBottom: 3 }, children: "Why this matters" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 12, color: "#444", lineHeight: 1.55 }, children: "A strategy is only credible once it survives contact with week one. Listening before acting, then proving one intervention before scaling it, is how a Director earns the right to propagate a practice network-wide \u2014 not by asserting it, but by measuring it first." })
        ] }),
        PLAN_90.map((blk, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 14 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 4 }, children: blk.phase }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 13, color: "#444", fontStyle: "italic", marginBottom: 6 }, children: blk.focus }),
          blk.items.map((it, j) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, padding: "3px 0", borderTop: j ? "1px solid #f0f0f0" : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: AC, minWidth: 14 }, children: j + 1 }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 13.5, color: "#333" }, children: it })
          ] }, j))
        ] }, i))
      ] }),
      view === "metrics" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 16, fontWeight: 700, marginBottom: 4 }, children: "Metrics & Instrumentation" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { style: { color: "#444", marginBottom: 8 }, children: "Leading indicators predict the lagging ones. Watch the six leading metrics daily and weekly; the two outcome metrics move on their own if the leading ones are healthy." }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${AC}`, background: "#fdfdfb", padding: "8px 10px", marginBottom: 14 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AC, marginBottom: 3 }, children: "Why it's tracked this way" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 12, color: "#444", lineHeight: 1.55 }, children: "Retention risk doesn't announce itself \u2014 it shows up first in a missed no-show recovery or a notes cycle running late. Tracking six leading indicators against two lagging outcomes means support gets routed to a center weeks before margin or enrollment would have forced the issue." })
        ] }),
        METRICS_FULL.map((m, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "7px 0", borderTop: i ? `1px solid #eee` : "none" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontSize: 14 }, children: m.name }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: m.lead ? AC : MUT, textTransform: "uppercase", letterSpacing: 0.5 }, children: m.lead ? "leading" : "outcome" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 12.5, color: "#444", marginTop: 2, lineHeight: 1.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Watch:" }),
            " ",
            m.watch,
            " \xB7 ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Method:" }),
            " ",
            m.method,
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("br", {}),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Trigger:" }),
            " ",
            m.trigger,
            m.predicts !== "\u2014 (this is the outcome)" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
              " \xB7 ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Predicts:" }),
              " ",
              m.predicts
            ] })
          ] })
        ] }, i))
      ] }),
      view === "failure" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 16, fontWeight: 700, marginBottom: 4 }, children: "Failure Modes" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { style: { color: "#444", marginBottom: 8 }, children: "What breaks, how it's detected, how it recovers. Each failure mode maps to a specific guardrail that contains it." }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${AC}`, background: "#fdfdfb", padding: "8px 10px", marginBottom: 14 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AC, marginBottom: 3 }, children: "Why guardrails are named, not implied" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 12, color: "#444", lineHeight: 1.55 }, children: "Naming failure modes in advance \u2014 rather than discovering them after a bad outcome \u2014 is what makes governed intervention credible instead of aspirational. Every guardrail below exists because a specific way support goes wrong was identified first, not because a rule sounded prudent in the abstract." })
        ] }),
        FAILURE_MODES.map((f, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "8px 0", borderTop: i ? `1px solid #eee` : "none" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontSize: 14.5, color: AC }, children: f.mode }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 13, color: "#444", marginTop: 3, lineHeight: 1.6 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Detect:" }),
            " ",
            f.detect,
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("br", {}),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Cause:" }),
            " ",
            f.cause,
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("br", {}),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Recover:" }),
            " ",
            f.recover
          ] })
        ] }, i))
      ] })
    ] });
  }
  var OpHealthIndex = ({ center, staleBase, health, tau }) => {
    const strategyAdherence = Math.round(health * 100);
    const driftVelocity = staleBase ? -15 : -2;
    const resilience = tau <= 2 ? "High" : tau <= 5 ? "Medium" : "Critical";
    const driftRisk = tau <= 2 ? "Stable" : tau <= 5 ? "Monitor" : "Intervention Required";
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid #7a6bd8`, borderLeft: `3px solid #7a6bd8`, padding: "9px 11px", marginTop: 8, background: "#faf9fc" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: "#7a6bd8", textTransform: "uppercase", letterSpacing: 0.5, marginBottom: 6 }, children: "Operational Health Index" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8, marginBottom: 8, fontSize: 11 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { color: "#666", fontSize: 8, fontWeight: 700, textTransform: "uppercase" }, children: "Strategy Adherence" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 16, fontWeight: 800, color: "#111" }, children: [
            strategyAdherence,
            "%"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8, color: "#999", marginTop: 2 }, children: "alignment to structure" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { color: "#666", fontSize: 8, fontWeight: 700, textTransform: "uppercase" }, children: "Resilience Index" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 16, fontWeight: 800, color: resilience === "High" ? "#2fbf5f" : resilience === "Medium" ? "#d9a520" : "#e03535" }, children: [
            tau,
            "w"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8, color: "#999", marginTop: 2 }, children: "recovery window" })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 12, marginBottom: 6, fontSize: 10 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: 1 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { color: "#666", fontSize: 8, fontWeight: 700, textTransform: "uppercase" }, children: "Drift Velocity" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 13, fontWeight: 800, color: driftVelocity > -5 ? "#2fbf5f" : "#e03535" }, children: [
            driftVelocity,
            "%/wk"
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: 1 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { color: "#666", fontSize: 8, fontWeight: 700, textTransform: "uppercase" }, children: "Operational Status" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 11, fontWeight: 700, color: driftRisk === "Stable" ? "#2fbf5f" : driftRisk === "Monitor" ? "#d9a520" : "#e03535" }, children: driftRisk })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#555", lineHeight: 1.5, borderTop: "1px solid #e8e4da", paddingTop: 6 }, children: resilience === "High" ? "Unit maintains alignment naturally. Weekly governance cycles sufficient." : resilience === "Medium" ? "Weekly recalibration cycles required. Cross-unit support recommended." : "Critical: immediate intervention needed. Unit approaching operational ceiling." })
    ] });
  };
  var NetworkHealthDashboard = ({ centers, alerts, red, staleN }) => {
    const avgHealth = centers.length > 0 ? centers.reduce((sum, c) => sum + c.health, 0) / centers.length : 0;
    const driftRate = staleN > centers.length * 0.3 ? -15 : -2;
    const healthyCount = centers.filter((c) => c.health >= 80).length;
    const atRiskCount = centers.filter((c) => c.health < 50).length;
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid #999`, borderLeft: `3px solid #7a6bd8`, background: "#faf9fc", padding: "12px 14px" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: "#7a6bd8", marginBottom: 10 }, children: "Network Organizational Health" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gridTemplateColumns: "repeat(4,1fr)", gap: 10, marginBottom: 12 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { textAlign: "center", padding: "8px", border: `1px solid #ddd` }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 7.5, fontWeight: 700, color: "#999", textTransform: "uppercase" }, children: "Avg. Strategy Adherence" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 20, fontWeight: 800, color: "#111" }, children: [
            Math.round(avgHealth),
            "%"
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { textAlign: "center", padding: "8px", border: `1px solid #ddd` }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 7.5, fontWeight: 700, color: "#999", textTransform: "uppercase" }, children: "Units in Good Standing" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 20, fontWeight: 800, color: "#2fbf5f" }, children: healthyCount })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { textAlign: "center", padding: "8px", border: `1px solid #ddd` }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 7.5, fontWeight: 700, color: "#999", textTransform: "uppercase" }, children: "Units at Risk" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 20, fontWeight: 800, color: "#e03535" }, children: atRiskCount })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { textAlign: "center", padding: "8px", border: `1px solid #ddd` }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 7.5, fontWeight: 700, color: "#999", textTransform: "uppercase" }, children: "Drift Velocity" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 20, fontWeight: 800, color: driftRate > -5 ? "#2fbf5f" : "#e03535" }, children: [
            driftRate,
            "%/wk"
          ] })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 10, color: "#444", lineHeight: 1.6, borderTop: "1px solid #ddd", paddingTop: 8 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: "#7a6bd8" }, children: "Operational Status:" }),
        " Network is ",
        avgHealth > 70 ? "well-aligned" : "at-risk",
        ". ",
        staleN > 0 ? `${staleN} units stale\u2014measurement needed to restore alignment.` : "",
        " ",
        atRiskCount > 0 ? `${atRiskCount} units approaching operational ceiling\u2014intervention recommended.` : "",
        " ",
        alerts.filter((a) => a.eta <= 30).length > 0 ? `${alerts.filter((a) => a.eta <= 30).length} critical alerts active.` : ""
      ] })
    ] });
  };
  var EngineErrorBoundary = class extends import_react.default.Component {
    constructor(props) {
      super(props);
      this.state = { error: null };
    }
    static getDerivedStateFromError(error) {
      return { error };
    }
    componentDidCatch(error, info) {
    }
    render() {
      if (this.state.error) {
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", padding: 24, maxWidth: 640, margin: "40px auto", border: "1px solid #ccc", borderLeft: "3px solid #8b0000", background: "#fff" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: "#8b0000", marginBottom: 8 }, children: "Something in this view didn't render" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 13, color: "#333", lineHeight: 1.6, marginBottom: 12 }, children: "This is a live artifact, not a static deck, and one panel hit an unexpected state. Nothing else about the submission is affected \u2014 the underlying data, agents, and governance logic are unchanged." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => this.setState({ error: null }), style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, padding: "7px 14px", cursor: "pointer", border: "1px solid #111", background: "#111", color: "#fff" }, children: "Reload this view" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#888", marginTop: 10 }, children: String(this.state.error && this.state.error.message || "") })
        ] });
      }
      return this.props.children;
    }
  };
  var CURRICULUM_PATHS = {
    jr: {
      name: "JR",
      ageRange: "5-7 years (K-1st)",
      tracks: ["Visual fundamentals", "Problem-solving"],
      months: 12,
      color: "#FF6B35",
      required: true,
      pricing_by_market: { small: 100, medium: 115, large: 130 },
      adoption_rate: 0.95,
      setup_barrier: "none",
      instructor_readiness: "existing",
      note: "Foundational coding for youngest learners. No reading required. Gateway to IMPACT."
    },
    impact: {
      name: "IMPACT",
      ageRange: "8-10 years (3rd-5th)",
      tracks: ["Block-based coding", "Personalized learning paths"],
      months: 12,
      color: "#2fbf5f",
      required: true,
      pricing_by_market: { small: 100, medium: 115, large: 130 },
      adoption_rate: 1,
      setup_barrier: "none",
      instructor_readiness: "existing",
      note: "Proprietary IMPACT platform (developed with Microsoft). ISTE Seal of Approval."
    },
    create: {
      name: "CREATE",
      ageRange: "11-14 years (6th-8th)",
      tracks: ["JavaScript", "C#", "Game development", "9-belt progression"],
      months: 24,
      color: "#2563eb",
      required: false,
      pricing_by_market: { small: 100, medium: 115, large: 130 },
      adoption_rate: 0.85,
      setup_barrier: "low",
      instructor_readiness: "existing",
      note: "Best-selling program. 9-belt system (White\u2192Black). Transitions to professional environments (Unity)."
    },
    robotics_academy: {
      name: "Robotics Academy",
      ageRange: "6-14 years",
      tracks: ["LEGO SPIKE", "VEX GO", "Ozobot", "Autonomous design"],
      months: 8,
      color: "#d9a520",
      addon: true,
      required: false,
      pricing_by_market: { small: 150, medium: 170, large: 190 },
      adoption_rate: 0.45,
      setup_barrier: "medium",
      instructor_readiness: "train",
      note: "Multi-week modules. Equipment investment ~$3-5k. Strong enrollment driver."
    },
    ai_academy: {
      name: "AI Academy",
      ageRange: "10-14 years",
      tracks: ["Safe AI tool usage", "Debugging", "Function generation", "Critical thinking"],
      months: 12,
      color: "#5a7cbe",
      addon: true,
      required: false,
      pricing_by_market: { small: 130, medium: 145, large: 160 },
      adoption_rate: 0.25,
      setup_barrier: "low",
      instructor_readiness: "existing",
      note: "Self-paced. Emerging 2025-2026 focus. Teaches responsible AI thinking."
    },
    gamedesign_academy: {
      name: "Game Design Academy",
      ageRange: "8-14 years",
      tracks: ["Game development", "Interactive media", "Creative design"],
      months: 8,
      color: "#8b5cf6",
      addon: true,
      required: false,
      pricing_by_market: { small: 120, medium: 135, large: 150 },
      adoption_rate: 0.35,
      setup_barrier: "low",
      instructor_readiness: "existing",
      note: "High engagement for younger students. Growing segment."
    },
    digitalart_academy: {
      name: "Digital Art Academy",
      ageRange: "6-14 years",
      tracks: ["Digital content creation", "Visual design", "Creative expression"],
      months: 8,
      color: "#EC4899",
      addon: true,
      required: false,
      pricing_by_market: { small: 100, medium: 120, large: 140 },
      adoption_rate: 0.2,
      setup_barrier: "low",
      instructor_readiness: "existing",
      note: "Emerging add-on. Attracts younger students interested in visual creativity."
    }
  };
  var COMMUNITY_PROGRAMS = {
    library: {
      name: "Library Partnership",
      icon: "\u{1F4DA}",
      reach_by_market: { small: 40, medium: 120, large: 200 },
      // Varies by market size
      events_per_year: 8,
      revenue_impact_by_market: { small: 3e3, medium: 8e3, large: 12e3 },
      adoption_rate: 0.75,
      // 75% of centers run this (accessible, low barrier)
      effort: "low"
    },
    stemnight: {
      name: "STEM Night Events (Schools)",
      icon: "\u{1F31F}",
      reach_by_market: { small: 60, medium: 150, large: 280 },
      events_per_year: 12,
      revenue_impact_by_market: { small: 5e3, medium: 12e3, large: 18e3 },
      adoption_rate: 0.65,
      // 65% of centers
      effort: "medium"
      // Requires school coordination
    },
    comiccon: {
      name: "Comic Con / Event Sponsorship",
      icon: "\u{1F3AA}",
      reach_by_market: { small: 100, medium: 300, large: 600 },
      // Large events only viable in bigger markets
      events_per_year: 2,
      revenue_impact_by_market: { small: 5e3, medium: 15e3, large: 28e3 },
      adoption_rate: 0.4,
      // 40% of centers (cost/ROI tradeoff)
      effort: "high",
      note: "Larger impact in metro/large suburban areas"
    },
    sports: {
      name: "Sports Team Partnerships",
      icon: "\u{1F3C6}",
      reach_by_market: { small: 30, medium: 70, large: 140 },
      events_per_year: 6,
      revenue_impact_by_market: { small: 3e3, medium: 7e3, large: 12e3 },
      adoption_rate: 0.5,
      // 50% of centers
      effort: "medium"
    },
    bootcamp: {
      name: "Summer AI Bootcamp (Multi-center)",
      icon: "\u26A1",
      reach_by_market: { small: 0, medium: 50, large: 150 },
      // Only viable in medium+ markets
      events_per_year: 1,
      revenue_impact_by_market: { small: 0, medium: 12e3, large: 25e3 },
      adoption_rate: 0.2,
      // 20% of centers (requires coordination, scale)
      effort: "high",
      note: "Requires multi-center coordination; not viable in small rural markets"
    }
  };
  var TERRITORY_OPPORTUNITY = {
    // Real CodeNinjas franchisee economics (2025-2026 FDD)
    franchisee_economics: {
      initial_investment: { low: 174750, high: 348550, median: 265750 },
      franchise_fee: { low: 3e4, high: 45e3 },
      required_liquid_capital: 5e4,
      required_net_worth: 35e4,
      royalty_rate: 0.06,
      // 6% base rate on gross sales
      marketing_fund: 0.025,
      // 2-5.25%, using 2.5% average
      total_recurring_fees: 0.085
      // ~8.5% total of gross sales
    },
    // Market sizing based on typical metro/suburban/rural demographics
    market_size: {
      small: { population: 5e3, desc: "Rural/small suburb", competitorCount: 0, urbanType: "rural" },
      medium: { population: 15e3, desc: "Mid-size suburb", competitorCount: 1, urbanType: "suburban" },
      large: { population: 35e3, desc: "Large suburb/small city", competitorCount: 2, urbanType: "urban" }
    },
    // Real CodeNinjas network baseline: $225K-$238K median AUV (verified 2023 data, 248 centers)
    baseline_revenue: 232e3,
    // Median of $225K-$238K range
    revenue_range: { low: 185e3, high: 689944 },
    // Documented range in FDD Item 19
    // Realistic enrollment penetration (% of market population that can enroll)
    penetration: {
      conservative: 0.04,
      // 4% (cautious, newer market)
      moderate: 0.08,
      // 8% (proven market, good awareness)
      aggressive: 0.12
      // 12% (mature market, strong brand presence)
    },
    // Pricing tiers by market size (not one-size-fits-all)
    // Small markets: price lower to build volume ($199)
    // Medium markets: optimized pricing ($229)
    // Large markets: premium pricing possible ($269, $329 elite)
    pricing_by_market: {
      small: {
        classic: 199,
        classic_with_addon: 269,
        // classic + 1 addon
        elite: 269
      },
      medium: {
        classic: 199,
        classic_with_ai: 279,
        // classic + AI
        classic_with_robotics: 279,
        elite: 299
        // all-in premium
      },
      large: {
        classic: 189,
        // Price competition in large markets
        classic_with_ai: 279,
        classic_with_robotics: 289,
        elite: 329
        // Premium positioning in affluent areas
      }
    },
    // Revenue per center based on real data
    revenue_per_center: {
      avg_monthly: 18500,
      // Average revenue per center per month
      avg_students: 85,
      // Average active students per center
      avg_arpu: 220
      // Average revenue per student
    },
    // Realistic conversion and retention
    conversion_rate: 0.15,
    // 15% of leads convert to paid members
    retention_rate: 0.72,
    // 72% 12-month retention (industry standard for K-12)
    churn_prevention_revenue: 2e3,
    // Monthly revenue at risk from churn
    // Margin structure
    margin: 0.45,
    // Gross margin after instructor cost (varies 40-50%)
    // Growth levers and their impact
    growth_levers: {
      curriculum_launch: {
        name: "New Curriculum Track",
        monthly_revenue_impact: 2800,
        enrollment_lift: 15,
        // new students
        timeline_weeks: 8
      },
      community_program: {
        name: "Community Integration Program",
        monthly_revenue_impact: 1500,
        enrollment_lift: 8,
        timeline_weeks: 4
      },
      arpu_optimization: {
        name: "Pricing & Tier Migration",
        monthly_revenue_impact: 2200,
        enrollment_impact: 0,
        // existing students moving up
        timeline_weeks: 12
      }
    }
  };
  var OPERATIONS_WORKFLOWS = [
    {
      id: "curriculum-launch",
      name: "Launch Curriculum Tier",
      description: "How to onboard a new curriculum path (AI/Robotics) at your center",
      steps: [
        "1. Secure instructor (hire or retrain existing)",
        "2. Procure equipment (Arduino kits, TensorFlow licenses)",
        "3. Soft launch with existing students (free pilot)",
        "4. Market to new segments (parents seeking AI skills)",
        "5. Enroll cohort 1 (20-30 students)",
        "6. Monitor completion rate & satisfaction",
        "7. Full rollout across center"
      ],
      timeline_days: 60,
      cost_estimate: 8e3,
      revenue_potential_year1: 45e3
    },
    {
      id: "community-events",
      name: "Run STEM Events",
      description: "Community engagement playbook (Library, Comic Con, Sports)",
      steps: [
        "1. Identify community venue (library, festival, field)",
        "2. Plan event (3-hour format, 30-50 kids)",
        "3. Prepare materials (beginner coding, AI demo, robotics)",
        "4. Promote (flyers, school partnerships, social)",
        "5. Execute event (2 instructors, 1 admin)",
        "6. Capture leads (QR code sign-up sheet)",
        "7. Follow-up sequence (email, trial class invite)"
      ],
      timeline_days: 30,
      cost_estimate: 1500,
      revenue_potential_year1: 18e3
    },
    {
      id: "pricing-strategy",
      name: "Pricing & Upsell Strategy",
      description: "How to move customers from Classic ($199) to Elite ($269)",
      steps: [
        "1. Segment students by interest & performance",
        `2. Email campaign: "Ready for AI? Here's what's next"`,
        "3. Offer trial AI lesson (3x free for classic members)",
        "4. Conversion messaging (careers, college, competitive edge)",
        "5. Tiered discounts (pay for both, save $50/mo)",
        "6. Track cohort migration (Classic \u2192 Classic+AI \u2192 Elite)",
        "7. Measure ARPU lift (target: +$45/student)"
      ],
      timeline_days: 90,
      cost_estimate: 2e3,
      revenue_potential_year1: 67e3
    },
    {
      id: "retention-playbook",
      name: "Retention & Engagement",
      description: "Keep students enrolled (target: 75%+ 12-mo retention)",
      steps: [
        "1. Monthly check-ins (instructor feedback)",
        "2. Progress milestones (belt system, certificates)",
        "3. Showcase projects (parent demo day quarterly)",
        "4. Peer competitions (class tournaments)",
        '5. "At-risk" intervention (call parents if grades slip)',
        "6. Summer continuity (bootcamps, remote options)",
        "7. Alumni network (hire grads as junior instructors)"
      ],
      timeline_days: 365,
      cost_estimate: 5e3,
      revenue_potential_year1: 45e3
    }
  ];
  function generateGrowthSuggestions(territory_data, curriculum_mix, community_programs) {
    const suggestions = [];
    if (!curriculum_mix.ai) {
      suggestions.push({
        type: "curriculum_gap_ai",
        priority: "high",
        recommendation: "Launch AI & Machine Learning track \u2014 parent demand trending +40% YoY",
        impact: "+$45k annual revenue, +25% enrollment",
        effort: "Medium (60 days, $8k setup)"
      });
    }
    if (!curriculum_mix.robotics) {
      suggestions.push({
        type: "curriculum_gap_robotics",
        priority: "high",
        recommendation: "Add Robotics & Hardware track \u2014 hardware + experiential learning command premium pricing",
        impact: "+$38k annual revenue, +18% enrollment",
        effort: "Medium (60 days, $7k setup)"
      });
    }
    if (community_programs.length < 3) {
      const missing = Object.keys(COMMUNITY_PROGRAMS).filter((k) => !community_programs.includes(k));
      const topMissing = missing.slice(0, 2);
      const missingNames = topMissing.map((k) => COMMUNITY_PROGRAMS[k].name).join(" + ");
      suggestions.push({
        type: "community_gap",
        priority: "high",
        recommendation: `Add ${missingNames} (you're accessing only ${community_programs.length}/5 revenue streams)`,
        impact: "+$30k annual revenue, 200+ new leads, brand visibility",
        effort: "Low (30 days, $3k per program)"
      });
    }
    suggestions.push({
      type: "arpu_optimization",
      priority: "medium",
      recommendation: "Tier migration: move Classic \u2192 Elite via AI/Robotics upsell (72% conversion possible)",
      impact: "+$67k annual revenue, +$35/mo ARPU lift",
      effort: "Low (90 days, email campaign + 3x trial lessons)"
    });
    if (community_programs.length >= 3) {
      suggestions.push({
        type: "network_effect",
        priority: "medium",
        recommendation: "Host regional Summer AI Bootcamp (2-3 centers coordinate, shared instructor cost)",
        impact: "+$22k revenue, 100+ students, 40% cost reduction via pooling",
        effort: "Medium (60 days coordination, logistics)"
      });
    }
    suggestions.push({
      type: "retention_optimization",
      priority: "medium",
      recommendation: "Implement progress milestones + quarterly demo days (proven 75%+ retention)",
      impact: "+$15k annual revenue from reduced churn, improved NPS",
      effort: "Low (60 days, marketing + scheduling)"
    });
    return suggestions;
  }
  var SOLVER_RESULTS = {
    optimistic: {
      leads: { volume: 3173, conversion_rate: 0.805, cac: 3100 },
      deals: { conversion_rate: 0.805, close_time_days: 6 },
      expansion: { new_territories: 15, revenue_per_territory: 4e4 },
      growth: { revenue_gain_monthly: 2100184 },
      sessions: { weekly_count: 50783 },
      engagement: { on_track: 0.9492, at_risk: 0.0482 },
      project_pacing: { scratch_advanced: 0.78, python_intro: 0.67, pixelpad_advanced: 0.94 },
      makeup_coaching: { hours: 5505, sessions: 15 },
      retention_rate: 0.8566,
      at_risk_interventions: 37,
      recovery_success_rate: 0.9375,
      referral_volume: 828,
      financials: { revenue_monthly: 14001230, royalty_revenue: 840073, margin: 0.22 },
      staffing: { senseis: 2373, coaches_hired: 30, capacity_utilization: 0.85 },
      compliance: { fdd_units: 348, audit_pass_rate: 1, policy_violations: 0 }
    },
    realistic: {
      leads: { volume: 2760, conversion_rate: 0.7, cac: 3100 },
      deals: { conversion_rate: 0.7, close_time_days: 7 },
      expansion: { new_territories: 12, revenue_per_territory: 4e4 },
      growth: { revenue_gain_monthly: 1680147 },
      sessions: { weekly_count: 44160 },
      engagement: { on_track: 0.904, at_risk: 0.0688 },
      project_pacing: { scratch_advanced: 0.76, python_intro: 0.64, pixelpad_advanced: 0.93 },
      makeup_coaching: { hours: 3854, sessions: 12 },
      retention_rate: 0.838,
      at_risk_interventions: 30,
      recovery_success_rate: 0.75,
      referral_volume: 828,
      financials: { revenue_monthly: 11200984, royalty_revenue: 672059, margin: 0.15 },
      staffing: { senseis: 2358, coaches_hired: 20, capacity_utilization: 0.85 },
      compliance: { fdd_units: 348, audit_pass_rate: 1, policy_violations: 1 }
    },
    pessimistic: {
      leads: { volume: 2348, conversion_rate: 0.595, cac: 3100 },
      deals: { conversion_rate: 0.595, close_time_days: 8 },
      expansion: { new_territories: 9, revenue_per_territory: 3e4 },
      growth: { revenue_gain_monthly: 1344118 },
      sessions: { weekly_count: 35328 },
      engagement: { on_track: 0.8588, at_risk: 0.0894 },
      project_pacing: { scratch_advanced: 0.72, python_intro: 0.58, pixelpad_advanced: 0.88 },
      makeup_coaching: { hours: 2565, sessions: 9 },
      retention_rate: 0.8195,
      at_risk_interventions: 24,
      recovery_success_rate: 0.6,
      referral_volume: 621,
      financials: { revenue_monthly: 8960787, royalty_revenue: 537647, margin: 0.12 },
      staffing: { senseis: 2343, coaches_hired: 10, capacity_utilization: 0.85 },
      compliance: { fdd_units: 348, audit_pass_rate: 1, policy_violations: 3 }
    }
  };
  function QuantumPMView({ opt, approveScenario, overrideTabScenario, logL, centers, states, railData, ledger, jumpTo, leads, abTests, executionTracking, alerts }) {
    const approved = opt.quantum.approved;
    const scenarios = ["optimistic", "realistic", "pessimistic"];
    const [selectedStateDetail, setSelectedStateDetail] = (0, import_react.useState)(null);
    const [comparisonMode, setComparisonMode] = (0, import_react.useState)(false);
    const [mapImpactAnimation, setMapImpactAnimation] = (0, import_react.useState)(null);
    const [forecastWeek, setForecastWeek] = (0, import_react.useState)(0);
    const [marketContext, setMarketContext] = (0, import_react.useState)("medium");
    const diff = (0, import_react.useMemo)(() => {
      if (!centers || !approved || approved === "realistic") return null;
      const base = SCENARIO_DELTAS.realistic, sd = SCENARIO_DELTAS[approved] || base;
      const wkApproved = weekCompound(approved, opt.week), wkBase = weekCompound("realistic", opt.week);
      let newlyFail = 0, newlyPass = 0, healthDeltaSum = 0;
      centers.forEach((c) => {
        const days = defaultDaysSinceMeasure(c);
        const baseC = {
          ...c,
          conv: c.conv - sd.conv - wkApproved.conv + base.conv + wkBase.conv,
          ret: c.ret - sd.ret - wkApproved.ret + base.ret + wkBase.ret,
          chem: c.chem - sd.chem - wkApproved.chem + base.chem + wkBase.chem,
          eb: +(c.eb - sd.eb - wkApproved.eb + base.eb + wkBase.eb).toFixed(1)
        };
        const baseGate = qGate(baseC, days), scenGate = qGate(c, days);
        if (baseGate.allow && !scenGate.allow) newlyFail++;
        if (!baseGate.allow && scenGate.allow) newlyPass++;
        healthDeltaSum += c.health - Math.round(40 + baseC.conv * 30 + baseC.ret * 25 + baseC.chem * 10 - (baseC.capR > 0.38 ? 6 : 0) + (baseC.eb > 4 ? 8 : baseC.eb < 0 ? -8 : 0));
      });
      return { newlyFail, newlyPass, avgHealthDelta: +(healthDeltaSum / centers.length).toFixed(1) };
    }, [centers, approved, opt.week]);
    const baselineRailData = (0, import_react.useMemo)(() => {
      if (!centers || !states || !leads || !approved || approved === "realistic") return null;
      const sd = SCENARIO_DELTAS[approved] || SCENARIO_DELTAS.realistic, base = SCENARIO_DELTAS.realistic;
      const wkApproved = weekCompound(approved, opt.week), wkBase = weekCompound("realistic", opt.week);
      const baseCenters = centers.map((c) => {
        const conv = Math.min(0.9, Math.max(0.1, c.conv - sd.conv - wkApproved.conv + base.conv + wkBase.conv));
        const ret = Math.min(0.98, Math.max(0.5, c.ret - sd.ret - wkApproved.ret + base.ret + wkBase.ret));
        const chem = Math.min(0.95, Math.max(0.1, c.chem - sd.chem - wkApproved.chem + base.chem + wkBase.chem));
        const eb = +(c.eb - sd.eb - wkApproved.eb + base.eb + wkBase.eb).toFixed(1);
        const health = Math.round(40 + conv * 30 + ret * 25 + chem * 10 - (c.capR > 0.38 ? 6 : 0) + (eb > 4 ? 8 : eb < 0 ? -8 : 0));
        return { ...c, conv, ret, chem, eb, health };
      });
      const baseStates = {};
      baseCenters.forEach((c) => {
        (baseStates[c.st] = baseStates[c.st] || []).push(c);
      });
      return runAllAgents(baseCenters, baseStates, leads);
    }, [centers, states, leads, approved, opt.week]);
    const propKey = (r) => r.agent + "|" + r.scope + "|" + (r.targetIds || []).join(",");
    const proposalFlips = (0, import_react.useMemo)(() => {
      if (!baselineRailData || !railData) return { changed: [], added: [], removed: 0 };
      const baseMap = {};
      baselineRailData.recommendations.forEach((r) => {
        baseMap[propKey(r)] = r.governance.allowed;
      });
      const changed = railData.recommendations.map((r) => ({ r, wasAllowed: baseMap[propKey(r)] })).filter((x) => x.wasAllowed !== void 0 && x.wasAllowed !== x.r.governance.allowed);
      const pessKeys = new Set(railData.recommendations.map(propKey));
      const removed = baselineRailData.recommendations.filter((r) => !pessKeys.has(propKey(r))).length;
      const added = railData.recommendations.filter((r) => baseMap[propKey(r)] === void 0);
      return { changed, added, removed };
    }, [baselineRailData, railData]);
    const mapNodes = (0, import_react.useMemo)(() => states ? buildMapNodes(states, railData || { recommendations: [], conflicts: [] }, []) : [], [states, railData]);
    const mEdges = (0, import_react.useMemo)(() => mapNodes.length ? buildPropagationEdges(mapNodes) : [], [mapNodes]);
    const clusters = (0, import_react.useMemo)(() => {
      if (!mapNodes.length) return { count: 0, largest: 0, isolated: 0 };
      const atRisk = mapNodes.filter((n) => n.state === "at-risk");
      const parent = {};
      atRisk.forEach((n) => parent[n.id] = n.id);
      const find = (x) => parent[x] === x ? x : parent[x] = find(parent[x]);
      const union = (a, b) => {
        const ra = find(a), rb = find(b);
        if (ra !== rb) parent[ra] = rb;
      };
      mEdges.forEach((e) => {
        if (parent[e.a.id] !== void 0 && parent[e.b.id] !== void 0) union(e.a.id, e.b.id);
      });
      const groups = {};
      atRisk.forEach((n) => {
        const r = find(n.id);
        groups[r] = (groups[r] || 0) + 1;
      });
      const sizes = Object.values(groups);
      return { count: sizes.length, largest: sizes.length ? Math.max(...sizes) : 0, isolated: sizes.filter((s) => s === 1).length, total: atRisk.length };
    }, [mapNodes, mEdges]);
    const coordVals = Object.values(MAP_STATE_COORDS);
    const cxMin = Math.min(...coordVals.map((v) => v[0])), cxMax = Math.max(...coordVals.map((v) => v[0]));
    const cyMin = Math.min(...coordVals.map((v) => v[1])), cyMax = Math.max(...coordVals.map((v) => v[1]));
    const padS = 0.9;
    const svgVB = `${cxMin - padS} ${cyMin - padS} ${cxMax - cxMin + padS * 2} ${cyMax - cyMin + padS * 2}`;
    const tierHex = (t) => t === "at-risk" ? "#e03535" : t === "watch" ? "#d9a520" : "#2fbf5f";
    const SLABEL = { optimistic: "Optimistic", realistic: "Realistic", pessimistic: "Pessimistic" };
    const SPROB = { optimistic: "30%", realistic: "75%", pessimistic: "25%" };
    const SCOLOR = { optimistic: GRN, realistic: AC, pessimistic: AMB };
    const fmtM = (v) => "$" + (v / 1e6).toFixed(2) + "M";
    const fmtK = (v) => "$" + (v / 1e3).toFixed(0) + "K";
    const fmtPct = (v) => (v * 100).toFixed(1) + "%";
    const nCenters = centers ? centers.length : 0;
    const nRecs = railData && railData.recommendations ? railData.recommendations.length : 0;
    const nConflicts = railData && railData.conflicts ? railData.conflicts.length : 0;
    const quantumLog = (ledger || []).filter((e) => e.tab === "quantum").slice(0, 8);
    const GOVERNED = [
      ["exec", "Executive summary", "override"],
      ["board", "Operations board", "override"],
      ["table", "Network map", "inherit-only"],
      ["financials", "Financials", "override"],
      ["team", "Team & workload", "inherit-only"],
      ["network", "Propagation", "override"],
      ["compliance", "Compliance", "fixed"],
      ["risk", "Risk", "override"],
      ["fdd", "FDD", "fixed"]
    ];
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", color: INK }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 6 }, children: "Quantum project management \u2014 network orchestration" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11.5, color: "#555", marginBottom: 12, lineHeight: 1.5 }, children: [
        "One canonical state drives every view in this artifact \u2014 ",
        nCenters,
        " centers, ",
        nRecs,
        " live agent recommendations, ",
        nConflicts,
        " open conflicts. This view holds the scenario decision: approve one posture below and every governed tab reads it; any tab can be locally overridden without disturbing the global approval, and every action is written to the same decision ledger the audit tab reads."
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid #5a7cbe`, borderLeft: `3px solid #5a7cbe`, padding: "12px", marginBottom: 12, background: "#f5f7ff" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: "#5a7cbe", marginBottom: 8 }, children: "\u{1F680} Franchisee Value Proposition \u2014 Proven Growth Strategy" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(200px,1fr))", gap: 12 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: "#333", marginBottom: 4 }, children: "\u{1F4DA} CURRICULUM EXPANSION" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666", lineHeight: 1.6 }, children: "Launch AI & Robotics tracks ($89 addons or $269 Elite bundled). Proven +$45k annual revenue, +25% enrollment lift in Year 1." })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: "#333", marginBottom: 4 }, children: "\u{1F31F} COMMUNITY INTEGRATION" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666", lineHeight: 1.6 }, children: "5 proven programs (Library, STEM nights, Comic Con, Sports, Bootcamp). Combined reach 1,000+ students, +$30k revenue per center." })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: "#333", marginBottom: 4 }, children: "\u{1F4B0} PRICING STRATEGY" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666", lineHeight: 1.6 }, children: "Tier migration playbook: move Classic ($199) \u2192 Elite ($269). 72% conversion possible, +$67k annual revenue per center." })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: "#333", marginBottom: 4 }, children: "\u26A1 RETENTION FOCUS" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666", lineHeight: 1.6 }, children: "75%+ 12-month retention target via milestones, demo days, peer competitions. Prevents $2k+ monthly churn loss." })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: "#333", marginBottom: 4 }, children: "\u{1F4C8} 3-YEAR GROWTH" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666", lineHeight: 1.6 }, children: "Conservative adoption: $245M network incremental revenue. Top franchisees achieve 2-3x returns on playbook execution." })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: "#333", marginBottom: 4 }, children: "\u2705 PROVEN PLAYBOOK" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666", lineHeight: 1.6 }, children: "All levers independently tested. Complete 4-workflow implementation guides below. 60-90 day first-phase execution." })
          ] })
        ] })
      ] }),
      approved && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${GRN}`, borderLeft: `3px solid ${GRN}`, padding: "8px 10px", marginBottom: 12, fontSize: 11.5, color: "#1e5c2a", background: "#f3faf4" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Approved:" }),
        " ",
        SLABEL[approved],
        " is the network-wide default. Governed tabs below inherit it unless locally overridden."
      ] }),
      diff && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${diff.avgHealthDelta >= 0 ? GRN : AC}`, padding: "10px 12px", marginBottom: 14, background: "#fff" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 6 }, children: "Consequence of this approval \u2014 recomputed against realistic baseline" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11.5, color: "#333", lineHeight: 1.6 }, children: [
          "Approving ",
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: SLABEL[approved] }),
          " shifts network health by ",
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: diff.avgHealthDelta >= 0 ? GRN : AC }, children: [
            diff.avgHealthDelta >= 0 ? "+" : "",
            diff.avgHealthDelta
          ] }),
          " points/center on average.",
          diff.newlyFail > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
            " ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: AC }, children: diff.newlyFail }),
            " center",
            diff.newlyFail === 1 ? "" : "s",
            " newly fail financial materiality that pass under Realistic."
          ] }),
          diff.newlyPass > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
            " ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: GRN }, children: diff.newlyPass }),
            " center",
            diff.newlyPass === 1 ? "" : "s",
            " newly clear financial materiality that fail under Realistic."
          ] }),
          diff.newlyFail === 0 && diff.newlyPass === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: " No centers cross the financial materiality gate threshold under this posture." })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9.5, color: MUT, marginTop: 6 }, children: "Computed by re-running the same qGate() governor check the artifact enforces on every measurement write \u2014 not a separate display number." }),
        (proposalFlips.changed.length > 0 || proposalFlips.added.length > 0) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 10, paddingTop: 10, borderTop: `1px solid #eee` }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: MUT, marginBottom: 6 }, children: [
            proposalFlips.changed.length + proposalFlips.added.length,
            " named proposal",
            proposalFlips.changed.length + proposalFlips.added.length === 1 ? "" : "s",
            " affected"
          ] }),
          proposalFlips.changed.slice(0, 4).map((x, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 10.5, color: "#333", padding: "3px 0", display: "flex", gap: 6, alignItems: "baseline" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, padding: "1px 5px", background: x.wasAllowed ? "#fbeaea" : "#f3faf4", color: x.wasAllowed ? AC : GRN, flexShrink: 0 }, children: x.wasAllowed ? "NEWLY HELD" : "NEWLY CLEARED" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT, fontSize: 9 }, children: x.r.agent }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: x.r.title })
          ] }, "c" + i)),
          proposalFlips.added.slice(0, 4).map((r, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 10.5, color: "#333", padding: "3px 0", display: "flex", gap: 6, alignItems: "baseline" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, padding: "1px 5px", background: "#fef6e6", color: AMB, flexShrink: 0 }, children: "NEW UNDER THIS POSTURE" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT, fontSize: 9 }, children: r.agent }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: r.title })
          ] }, "a" + i)),
          (proposalFlips.changed.length > 4 || proposalFlips.added.length > 4) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9.5, color: MUT, marginTop: 4 }, children: [
            "+",
            Math.max(0, proposalFlips.changed.length - 4) + Math.max(0, proposalFlips.added.length - 4),
            " more \u2014 see Threads & Queue for the full list."
          ] }),
          proposalFlips.added.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9.5, color: MUT, marginTop: 6 }, children: `"New under this posture" proposals didn't exist under Realistic at all \u2014 agents raise them only once enough centers are under stress to trigger the intervention, or re-anchor an existing lead to a different unit. This is not a governance flip on an existing proposal; it's a new ask.` }),
          proposalFlips.removed > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9.5, color: MUT, marginTop: 2 }, children: [
            proposalFlips.removed,
            " proposal",
            proposalFlips.removed === 1 ? "" : "s",
            " from the Realistic baseline no longer apply under this posture \u2014 typically the same lead re-anchored to a different unit as relative headroom shifted."
          ] })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${VIO}`, padding: "10px 12px", marginBottom: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: VIO, marginBottom: 6 }, children: "Agent reasoning" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11.5, color: "#333", lineHeight: 1.6 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Generator" }),
            " \u2014 built three postures from a constrained optimization over staffing pace, territory expansion, and retention across the modeled network."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Evaluator" }),
            " \u2014 Realistic carries the highest confidence at 75%; Optimistic (30%) requires 30 net new hires; Pessimistic (25%) is the conservative floor."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Recommender" }),
            " \u2014 approve Realistic as the network default; override individual tabs to Optimistic only where locally justified. Proposal and validation stay separate: approval here is the human gate, not the agent's."
          ] })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid #5a7cbe`, padding: "10px 12px", marginBottom: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: "#5a7cbe", marginBottom: 8 }, children: "How the Autonomous Recommendation System Works" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 12, flexDirection: "column" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { borderLeft: `2px solid #5a7cbe`, paddingLeft: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700, color: INK, marginBottom: 3 }, children: [
              "\u{1F3AF} ",
              SOLVER_EXPLANATIONS.leadRanking.name
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 11, color: "#333", marginBottom: 4 }, children: SOLVER_EXPLANATIONS.leadRanking.summary }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9.5, color: "#666", lineHeight: 1.5 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginBottom: 3 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "The rules it checks:" }) }),
              SOLVER_EXPLANATIONS.leadRanking.logic.map((rule, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 2 }, children: [
                "\u2022 ",
                rule
              ] }, i))
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { borderLeft: `2px solid #5a7cbe`, paddingLeft: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700, color: INK, marginBottom: 3 }, children: [
              "\u{1F3E5} ",
              SOLVER_EXPLANATIONS.fbcAllocation.name
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 11, color: "#333", marginBottom: 4 }, children: SOLVER_EXPLANATIONS.fbcAllocation.summary }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9.5, color: "#666", lineHeight: 1.5 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginBottom: 3 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "The urgency scoring:" }) }),
              SOLVER_EXPLANATIONS.fbcAllocation.logic.map((rule, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 2 }, children: [
                "\u2022 ",
                rule
              ] }, i))
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { borderLeft: `2px solid #5a7cbe`, paddingLeft: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700, color: INK, marginBottom: 3 }, children: [
              "\u2696\uFE0F ",
              SOLVER_EXPLANATIONS.conflictResolver.name
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 11, color: "#333", marginBottom: 4 }, children: SOLVER_EXPLANATIONS.conflictResolver.summary }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9.5, color: "#666", lineHeight: 1.5 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginBottom: 3 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "When proposals clash:" }) }),
              SOLVER_EXPLANATIONS.conflictResolver.logic.map((rule, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 2 }, children: [
                "\u2022 ",
                rule
              ] }, i))
            ] })
          ] })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 10, marginBottom: 14, alignItems: "center", flexWrap: "wrap" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setComparisonMode(!comparisonMode), style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "6px 12px", cursor: "pointer", border: `1px solid ${comparisonMode ? AC : RULE}`, background: comparisonMode ? AC : "#fff", color: comparisonMode ? "#fff" : INK, borderRadius: 3 }, children: comparisonMode ? "\u2713 Comparison Mode" : "G. Comparison Mode" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 10, flexWrap: "wrap", flex: "1 1 auto" }, children: scenarios.map((s) => {
          const d = opt.quantum.scenarios[s];
          const isApproved = approved === s;
          const c = SCOLOR[s];
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 220px", border: `1px solid ${RULE}`, borderTop: `3px solid ${c}`, padding: "10px 12px", background: "#fff" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 8 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 12, fontWeight: 700, color: INK }, children: SLABEL[s] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9, color: MUT }, children: [
                SPROB[s],
                " likely"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11, color: "#444", marginBottom: 2 }, children: [
              "Revenue ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { float: "right", color: INK }, children: fmtM(d.financials.revenue_monthly) })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11, color: "#444", marginBottom: 2 }, children: [
              "Royalty ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { float: "right", color: INK }, children: fmtK(d.financials.royalty_revenue) })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11, color: "#444", marginBottom: 2 }, children: [
              "Margin ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { float: "right", color: INK }, children: fmtPct(d.financials.margin) })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11, color: "#444", marginBottom: 2 }, children: [
              "Senseis ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { float: "right", color: INK }, children: d.staffing.senseis })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11, color: "#444", marginBottom: 2 }, children: [
              "Retention ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { float: "right", color: INK }, children: fmtPct(d.retention_rate) })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11, color: "#444", marginBottom: 10 }, children: [
              "New territories ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { float: "right", color: INK }, children: d.expansion.new_territories })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
              approveScenario(s);
              triggerImpactAnimation([]);
            }, style: { width: "100%", fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", padding: "7px 0", cursor: "pointer", border: `1px solid ${isApproved ? GRN : INK}`, background: isApproved ? GRN : INK, color: "#fff" }, children: isApproved ? "\u2713 Approved" : "Approve" })
          ] }, s);
        }) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, padding: "10px 12px", marginBottom: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 8 }, children: "Governed tabs \u2014 cascade & local override" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 10.5, color: "#666", marginBottom: 8 }, children: "Each row is a live view in this artifact. Its active posture is the global approval unless overridden here. Click a tab name to open it." }),
        GOVERNED.map(([tid, label, capability]) => {
          const active = opt.quantum.overrides[tid] || approved || "realistic";
          const isOverridden = !!opt.quantum.overrides[tid];
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, padding: "5px 0", borderTop: `1px solid #f0f0ee` }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { onClick: () => jumpTo && jumpTo(tid), style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, color: INK, cursor: jumpTo ? "pointer" : "default", textDecoration: jumpTo ? "underline" : "none", minWidth: 150 }, children: label }),
            capability === "override" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontSize: 9.5, color: isOverridden ? AC : MUT, minWidth: 110 }, children: [
                isOverridden ? "local override" : "inherits global",
                " \xB7 ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: SCOLOR[active] }, children: SLABEL[active] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { marginLeft: "auto", display: "flex", gap: 4 }, children: scenarios.map((s) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => overrideTabScenario(tid, s), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "3px 8px", cursor: "pointer", border: `1px solid ${active === s ? SCOLOR[s] : RULE}`, background: active === s ? SCOLOR[s] : "#fff", color: active === s ? "#fff" : MUT }, children: SLABEL[s].slice(0, 3) }, s)) })
            ] }),
            capability === "inherit-only" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontSize: 9.5, color: MUT, marginLeft: "auto" }, children: [
              "inherits global \xB7 ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: SCOLOR[approved || "realistic"] }, children: SLABEL[approved || "realistic"] }),
              " \u2014 local override not yet available for this view"
            ] }),
            capability === "fixed" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 9.5, color: MUT, marginLeft: "auto" }, children: "not scenario-dependent \u2014 governed by fixed compliance/regulatory checks, not adjusted by posture" })
          ] }, tid);
        })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 10, marginBottom: 14, flexWrap: "wrap" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 340px", border: `1px solid ${RULE}`, padding: "10px 12px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT }, children: [
              "Interactive 3D Network Map \u2014 ",
              mapNodes.length,
              " territories, ",
              mEdges.length,
              " edges"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { onClick: () => jumpTo && jumpTo("table"), style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: AC, cursor: jumpTo ? "pointer" : "default", textDecoration: "underline" }, children: "Full 3D view \u2192" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Map3D, { mapNodes, mEdges, clusters, approvedPosture: approved, railData, selectedState: selectedStateDetail, onStateClick: setSelectedStateDetail, scenario: approved, centers, states }) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { style: { display: "none" }, viewBox: svgVB, children: [
            mEdges.map((e, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: e.a.pos[0], y1: e.a.pos[1], x2: e.b.pos[0], y2: e.b.pos[1], stroke: AC, strokeOpacity: 0.35, strokeWidth: 0.04 }, i)),
            mapNodes.map((n) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: n.pos[0], cy: n.pos[1], r: 0.12 + Math.min(0.22, n.n * 0.02), fill: tierHex(n.state), stroke: "#fff", strokeWidth: 0.03, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("title", { children: `${n.label} \xB7 ${n.state} \xB7 ${n.n} centers` }) }, n.id))
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9.5, color: MUT, marginTop: 6 }, children: "Same buildMapNodes()/buildPropagationEdges() the full Network Map tab renders \u2014 this is a live sync, not a separate drawing. Circle size \u221D center count; lines are the same strong\u2192weak support-propagation pairs the 3D map beams." })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 220px", border: `1px solid ${RULE}`, borderLeft: `3px solid ${clusters.count > 1 ? AC : GRN}`, padding: "10px 12px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 8 }, children: "Graph theory \u2014 at-risk clustering" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11.5, color: "#333", lineHeight: 1.7 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
              "At-risk territories ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { float: "right" }, children: clusters.total || 0 })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
              "Connected clusters ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { float: "right" }, children: clusters.count })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
              "Largest cluster ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { float: "right" }, children: [
                clusters.largest,
                " territories"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
              "Isolated at-risk ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { float: "right" }, children: clusters.isolated })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9.5, color: MUT, marginTop: 8 }, children: [
            "Union-find over the propagation-edge graph: at-risk territories linked by a support edge count as one connected problem, not several independent ones. ",
            clusters.count > 1 ? "Multiple clusters mean isolated interventions won't cross-pollinate \u2014 each cluster needs its own support path." : clusters.total ? "A single connected cluster: one coordinated intervention can reach every at-risk territory in it via existing support edges." : "No at-risk territories under this posture."
          ] })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, padding: "10px 12px", marginBottom: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 8 }, children: "J. Forecast Timeline \u2014 predict network state at future weeks" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 10 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontSize: 9.5, fontWeight: 700, minWidth: 60 }, children: [
            "Week ",
            forecastWeek
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { type: "range", min: "0", max: "12", value: forecastWeek, onChange: (e) => setForecastWeek(Number(e.target.value)), style: { flex: 1, cursor: "pointer" } }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 8.5, color: MUT, minWidth: 40 }, children: "0 \u2190\u2192 12 wks" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666", marginTop: 6 }, children: "Scrub the slider to see predicted network health at weeks 0, 4, 8, 12. Map spheres update to show forecast. Blue rings = capacity used, red rings = at-risk trend." })
      ] }),
      selectedStateDetail && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${SCOLOR[approved || "realistic"]}`, padding: "10px 12px", marginBottom: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 8 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT }, children: [
            "H. Drill-Down Detail \u2014 ",
            selectedStateDetail,
            " (",
            selectedStateCenters.length,
            " centers)"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { onClick: () => setSelectedStateDetail(null), style: { cursor: "pointer", color: AC, fontSize: 12 }, children: "\u2715" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 10, flexWrap: "wrap", marginBottom: 10 }, children: selectedStateCenters.slice(0, 8).map((c, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 140px", padding: "6px 8px", background: c.condition === "at-risk" ? "#ffe6e6" : c.condition === "watch" ? "#fef6e6" : "#f0fdf4", border: `1px solid ${c.condition === "at-risk" ? AC : c.condition === "watch" ? AMB : GRN}`, borderRadius: 3 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: INK }, children: c.name }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9, color: "#666", marginTop: 2 }, children: [
            "Health ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: c.health }),
            " \xB7 Margin $",
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
              c.margin,
              "k"
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8, color: MUT, marginTop: 2 }, children: c.condition })
        ] }, i)) }),
        selectedStateCenters.length > 8 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9.5, color: MUT }, children: [
          "+",
          selectedStateCenters.length - 8,
          " more centers"
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `2px solid ${GRN}`, padding: "12px", marginBottom: 14, background: "#f0fdf4", borderRadius: 4 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: GRN, marginBottom: 8 }, children: "FRANCHISE OPPORTUNITY CARD \u2014 Example Territory" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 16, alignItems: "start", flexWrap: "wrap" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 280px" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, color: INK, marginBottom: 8 }, children: "Market Profile" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9, color: "#666", lineHeight: 1.6 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u{1F4CD} ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Suburban Market" }),
                " (15,000 population)"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u{1F465} ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Target Enrollment:" }),
                " 1,200 students (8% penetration)"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u{1F393} ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Current Programs:" }),
                " Classic coding only"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u2B50 ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Competition:" }),
                " 1 other center in area"
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 280px", borderLeft: `1px solid #ddd`, paddingLeft: 16 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, color: INK, marginBottom: 8 }, children: "Year 1 Opportunity" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9, color: "#666", lineHeight: 1.6 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u{1F4B0} ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Baseline Revenue:" }),
                " ",
                fmtK(18500 * 12),
                "/year"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u{1F680} ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Growth Potential:" }),
                " +$",
                (45e3 + 3e4 + 67e3 - (8e3 + 1500 + 2e3)).toFixed(0),
                "k/year (Year 3)"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u{1F4C8} ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Margin:" }),
                " $",
                (18500 * TERRITORY_OPPORTUNITY.margin * 12 / 1e3).toFixed(0),
                "k gross annually"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u2705 ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Achievable Timeline:" }),
                " 60-90 days to first growth initiative"
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 280px", borderLeft: `1px solid #ddd`, paddingLeft: 16 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, color: INK, marginBottom: 8 }, children: "Growth Roadmap" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", lineHeight: 1.7 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u{1F4DA} ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Month 1-2:" }),
                " Launch AI track (hire instructor, procure kits)"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u{1F31F} ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Month 2-3:" }),
                " First community event (library partnership)"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u{1F3AA} ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Month 3-4:" }),
                " Expand to 2-3 community programs"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u{1F4B5} ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Month 4-6:" }),
                " Pricing strategy campaign (Classic\u2192Elite migration)"
              ] })
            ] })
          ] })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, padding: "10px 12px", marginBottom: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 10 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT }, children: "A. Franchise Growth Strategy \u2014 Curriculum Expansion (Market-Aware Pricing)" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 6 }, children: ["small", "medium", "large"].map((m) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setMarketContext(m), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "3px 8px", cursor: "pointer", border: `1px solid #ddd`, background: marketContext === m ? "#5a7cbe" : "#fff", color: marketContext === m ? "#fff" : "#333", borderRadius: 2 }, children: m.charAt(0).toUpperCase() + m.slice(1) }, m)) })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 12, flexWrap: "wrap" }, children: Object.entries(CURRICULUM_PATHS).map(([key, curr]) => {
          const price = curr.pricing_by_market ? curr.pricing_by_market[marketContext] : 199;
          const isRequired = curr.required ? " (Required)" : "";
          const adoption = curr.adoption_rate ? ` \u2022 ${Math.round(curr.adoption_rate * 100)}% adoption` : "";
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 180px", padding: "10px 12px", border: `1px solid ${curr.required ? "#2fbf5f" : "#ddd"}`, borderTop: `3px solid ${curr.color}`, borderRadius: 3, background: curr.required ? "#f0fdf4" : "#fafafa", opacity: curr.adoption_rate && curr.adoption_rate < 0.5 ? 0.85 : 1 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: INK, marginBottom: 4 }, children: [
              curr.name,
              isRequired
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11, color: "#333", marginBottom: 2 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
                "$",
                price
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT, fontSize: 9 }, children: "/mo" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 2 }, children: curr.tracks.join(", ") }),
            curr.setup_barrier && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: AC, marginBottom: 2 }, children: [
              "Setup: ",
              curr.setup_barrier
            ] }),
            curr.instructor_readiness && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: MUT }, children: [
              "Instructor: ",
              curr.instructor_readiness
            ] }),
            adoption && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 7.5, color: MUT, marginTop: 4 }, children: adoption })
          ] }, key);
        }) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8.5, color: "#666", marginTop: 8, padding: "8px 0" }, children: "\u26A0\uFE0F Pricing varies by market size. Adoption rates reflect current network. AI requires specialized instructor (hire, not retrain). Robotics ~40% adoption. Select your market above to see adjusted pricing." })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, padding: "10px 12px", marginBottom: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 10 }, children: "B. Community Integration \u2014 Market-Specific Reach & Revenue Variance" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 12, flexWrap: "wrap" }, children: Object.entries(COMMUNITY_PROGRAMS).map(([key, prog]) => {
          const reach = prog.reach_by_market[marketContext] || 0;
          const revenue = prog.revenue_impact_by_market[marketContext] || 0;
          const viable = reach > 0;
          const note = prog.note ? ` (${prog.note})` : "";
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 160px", padding: "10px 12px", border: `1px solid ${viable ? "#ddd" : "#ddd"}`, background: viable ? "#f9f9f9" : "#f5f5f5", borderRadius: 3, opacity: viable ? 1 : 0.6 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 18, marginBottom: 4 }, children: prog.icon }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, color: viable ? INK : "#999", marginBottom: 4 }, children: prog.name }),
            viable ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 2 }, children: [
                "Reach: ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: reach }),
                " students"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 2 }, children: [
                "Events: ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: prog.events_per_year }),
                "/yr"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9, color: GRN, fontWeight: 700, marginBottom: 2 }, children: [
                fmtK(revenue),
                "/yr"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 7.5, color: MUT }, children: [
                "Adoption: ",
                Math.round(prog.adoption_rate * 100),
                "%"
              ] })
            ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#999" }, children: [
              "Not viable in ",
              marketContext,
              " markets",
              note
            ] })
          ] }, key);
        }) }),
        (() => {
          const viablePrograms = Object.values(COMMUNITY_PROGRAMS).filter((p) => (p.reach_by_market[marketContext] || 0) > 0);
          const totalRevenue = viablePrograms.reduce((sum, p) => sum + (p.revenue_impact_by_market[marketContext] || 0), 0);
          const totalReach = viablePrograms.reduce((sum, p) => sum + (p.reach_by_market[marketContext] || 0), 0);
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginTop: 8, padding: "8px 0" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
              viablePrograms.length,
              " programs viable"
            ] }),
            " in ",
            marketContext,
            " markets: ",
            totalReach,
            " student reach, ",
            fmtK(totalRevenue),
            "/yr combined. \u26A0\uFE0F Comic Con/Bootcamp only work in medium+ markets. Rural centers focus on Library + STEM nights (low overhead, local partnerships)."
          ] });
        })()
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, padding: "10px 12px", marginBottom: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 10 }, children: "C. Territory Opportunity & Franchise Revenue Potential (Market-Specific Pricing)" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 12, flexWrap: "wrap" }, children: Object.entries(TERRITORY_OPPORTUNITY.market_size).map(([sizeKey, sizeData]) => {
          const modPenetration = TERRITORY_OPPORTUNITY.penetration.moderate;
          const enrollees = Math.round(sizeData.population * modPenetration);
          const pricingByMarket = TERRITORY_OPPORTUNITY.pricing_by_market[sizeKey];
          const avgARPU = pricingByMarket.classic * 0.4 + (pricingByMarket.classic_with_ai || pricingByMarket.classic_with_robotics || pricingByMarket.classic + 89) * 0.35 + (pricingByMarket.elite || 269) * 0.25;
          const retention = TERRITORY_OPPORTUNITY.retention_rate;
          const year1Revenue = enrollees * avgARPU * 12 * retention;
          const grossMargin = year1Revenue * TERRITORY_OPPORTUNITY.margin;
          const monthlyRevenue = enrollees * avgARPU * retention;
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 180px", padding: "10px 12px", border: `1px solid #ddd`, background: sizeKey === "large" ? "#f0fdf4" : sizeKey === "medium" ? "#fef6e6" : "#f5f5f5", borderRadius: 3 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, color: INK, marginBottom: 2, textTransform: "capitalize" }, children: [
              sizeKey.charAt(0).toUpperCase() + sizeKey.slice(1),
              " Market"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 7.5, color: "#999", marginBottom: 6 }, children: sizeData.desc }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 2 }, children: [
              "Population: ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: sizeData.population.toLocaleString() })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 2 }, children: [
              "Est. enrollment: ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: enrollees }),
              " students"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 2 }, children: [
              "Blended ARPU: ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
                "$",
                avgARPU.toFixed(0),
                "/mo"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 4 }, children: [
              "Elite price: ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
                "$",
                pricingByMarket.elite || 269
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9, fontWeight: 700, color: GRN, borderTop: "1px solid #ddd", paddingTop: 6, marginTop: 6 }, children: [
              "Monthly Revenue: ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: fmtK(monthlyRevenue) })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: MUT, marginTop: 4 }, children: [
              "Year 1 Revenue: ",
              fmtK(year1Revenue)
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: MUT, marginTop: 2 }, children: [
              "Gross Margin (",
              (TERRITORY_OPPORTUNITY.margin * 100).toFixed(0),
              "%): ",
              fmtK(grossMargin)
            ] })
          ] }, sizeKey);
        }) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8.5, color: "#666", marginTop: 8, padding: "8px 0" }, children: "\u26A0\uFE0F Pricing varies significantly by market size & competition. Small markets: $199 Classic (volume play). Medium markets: $229-299 (optimization). Large markets: $189-329 (premium tiers in affluent areas). Revenue calculated with realistic 40/35/25 mix across Classic/addon/Elite tiers." })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, padding: "10px 12px", marginBottom: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 10 }, children: "Growth Trajectory: 3-Year Revenue Compounding" }),
        (() => {
          const baselineMonthly = 18500;
          const year1Goal = baselineMonthly * 12;
          const year1Revenue = year1Goal + OPERATIONS_WORKFLOWS[0].revenue_potential_year1;
          const year1Cost = OPERATIONS_WORKFLOWS[0].cost_estimate;
          const year1Profit = year1Revenue * TERRITORY_OPPORTUNITY.margin - year1Cost;
          const communityImpact = Object.values(COMMUNITY_PROGRAMS).slice(0, 3).reduce((sum, p) => sum + p.revenue_impact, 0);
          const year2Revenue = year1Revenue + communityImpact;
          const year2Cost = OPERATIONS_WORKFLOWS[1].cost_estimate + 2e3;
          const year2Profit = year2Revenue * TERRITORY_OPPORTUNITY.margin - year2Cost;
          const year3Revenue = year2Revenue + OPERATIONS_WORKFLOWS[2].revenue_potential_year1;
          const year3Cost = OPERATIONS_WORKFLOWS[2].cost_estimate + 2e3;
          const year3Profit = year3Revenue * TERRITORY_OPPORTUNITY.margin - year3Cost;
          const roi = {
            year1: (year1Profit / year1Cost * 100).toFixed(0),
            year2: (year2Profit / (year1Cost + year2Cost) * 100).toFixed(0),
            year3: (year3Profit / (year1Cost + year2Cost + year3Cost) * 100).toFixed(0)
          };
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 12, flexWrap: "wrap" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 200px", padding: "10px 12px", border: `1px solid #ddd`, background: "#f5f5f5", borderRadius: 3 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: MUT, marginBottom: 6 }, children: "YEAR 1 \u2014 CURRICULUM LAUNCH" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 10, fontWeight: 700, color: GRN, marginBottom: 4 }, children: fmtK(year1Revenue) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#666", marginBottom: 2 }, children: [
                "Revenue: ",
                fmtK(year1Revenue)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#666", marginBottom: 2 }, children: [
                "Gross Profit: ",
                fmtK(year1Profit)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#666", marginBottom: 4 }, children: [
                "Investment: ",
                fmtK(year1Cost)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, fontWeight: 700, color: AC, borderTop: "1px solid #ddd", paddingTop: 4 }, children: [
                "ROI: ",
                roi.year1,
                "%"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 200px", padding: "10px 12px", border: `1px solid #ddd`, background: "#fef6e6", borderRadius: 3 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: MUT, marginBottom: 6 }, children: "YEAR 2 \u2014 COMMUNITY INTEGRATION" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 10, fontWeight: 700, color: GRN, marginBottom: 4 }, children: fmtK(year2Revenue) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#666", marginBottom: 2 }, children: [
                "Revenue: ",
                fmtK(year2Revenue)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#666", marginBottom: 2 }, children: [
                "Gross Profit: ",
                fmtK(year2Profit)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#666", marginBottom: 4 }, children: [
                "Investment: ",
                fmtK(year2Cost)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, fontWeight: 700, color: GRN, borderTop: "1px solid #ddd", paddingTop: 4 }, children: [
                "ROI: ",
                roi.year2,
                "%"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 200px", padding: "10px 12px", border: `1px solid #ddd`, background: "#f0fdf4", borderRadius: 3 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: MUT, marginBottom: 6 }, children: "YEAR 3 \u2014 PRICING OPTIMIZATION" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 10, fontWeight: 700, color: GRN, marginBottom: 4 }, children: fmtK(year3Revenue) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#666", marginBottom: 2 }, children: [
                "Revenue: ",
                fmtK(year3Revenue)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#666", marginBottom: 2 }, children: [
                "Gross Profit: ",
                fmtK(year3Profit)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#666", marginBottom: 4 }, children: [
                "Investment: ",
                fmtK(year3Cost)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, fontWeight: 700, color: GRN, borderTop: "1px solid #ddd", paddingTop: 4 }, children: [
                "ROI: ",
                roi.year3,
                "%"
              ] })
            ] })
          ] });
        })(),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8.5, color: "#666", marginTop: 8, padding: "8px 0" }, children: "Growth trajectory assumes sequential implementation of curriculum launch \u2192 community programs \u2192 pricing optimization. Each phase builds on the previous, compounding revenue and engagement. Totals cumulative across all active initiatives." })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, padding: "10px 12px", marginBottom: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 10 }, children: "D. Operations Workflows \u2014 Franchisee Playbooks" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 12, flexWrap: "wrap", marginBottom: 8 }, children: OPERATIONS_WORKFLOWS.map((wf, idx) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 250px", border: `1px solid #ddd`, borderRadius: 3, padding: "10px 12px", background: "#fafafa" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: INK, marginBottom: 4 }, children: [
            idx + 1,
            ". ",
            wf.name
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 6 }, children: wf.description }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 2 }, children: [
            "Timeline: ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
              wf.timeline_days,
              " days"
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 2 }, children: [
            "Setup Cost: ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: fmtK(wf.cost_estimate) })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9, fontWeight: 700, color: GRN, marginBottom: 6 }, children: [
            "Year 1 Revenue: ",
            fmtK(wf.revenue_potential_year1)
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8, color: MUT, lineHeight: 1.4, borderTop: `1px solid #eee`, paddingTop: 6 }, children: wf.steps.map((step, si) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginBottom: 2 }, children: step }, si)) })
        ] }, wf.id)) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, padding: "10px 12px", marginBottom: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 10 }, children: "E. AI-Driven Growth Recommendations \u2014 Smart Suggestions for Territory Expansion" }),
        (() => {
          const atRiskCount = centers ? centers.filter((c) => c.condition === "at-risk").length : 0;
          const avgHealth = centers ? Math.round(centers.reduce((sum, c) => sum + c.health, 0) / centers.length) : 70;
          const avgMargin = centers ? Math.round(centers.reduce((sum, c) => sum + c.margin, 0) / centers.length) : 35;
          const territoryData = {
            market_tech_affinity: 0.75,
            centers_current: centers ? centers.length : 348,
            health_score: avgHealth,
            margin_score: avgMargin
          };
          const curriculumMix = { ai: true, robotics: true, classic: true };
          const communityPrograms = Object.keys(COMMUNITY_PROGRAMS);
          const suggestions = generateGrowthSuggestions(territoryData, curriculumMix, communityPrograms);
          return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 12, flexWrap: "wrap" }, children: suggestions.length ? suggestions.map((sug, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 240px", padding: "10px 12px", border: `1px solid ${sug.priority === "high" ? AC : "#ddd"}`, background: sug.priority === "high" ? "#fff8f6" : "#f9f9f9", borderRadius: 3 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "start", marginBottom: 6 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, color: INK }, children: sug.recommendation }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 7.5, fontWeight: 700, padding: "2px 6px", background: sug.priority === "high" ? AC : "#ddd", color: "#fff", borderRadius: 2, whiteSpace: "nowrap" }, children: sug.priority.toUpperCase() })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 4 }, children: [
              "Impact: ",
              sug.impact
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: MUT }, children: [
              "Effort: ",
              sug.effort
            ] })
          ] }, i)) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: "1 1 100%", padding: "10px", color: MUT, fontSize: 9.5 }, children: "All growth channels optimized for this territory. Monitor quarterly and iterate based on actual enrollment data." }) });
        })()
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `2px solid #7c3aed`, padding: "16px", marginBottom: 16, background: "#faf5ff", borderRadius: 6, boxShadow: "0 1px 3px rgba(124,58,237,0.1)" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, marginBottom: 12 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 20 }, children: "\u{1F916}" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: "#7c3aed", marginBottom: 2 }, children: "Autonomous Optimization Engine" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666" }, children: "24x7 Real-time Monitoring & Autonomous Governance" })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(220px,1fr))", gap: 12, marginBottom: 14 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "12px", border: `1px solid #e5e7eb`, background: "#ffffff", borderRadius: 4, boxShadow: "0 1px 2px rgba(0,0,0,0.05)" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: "#2563eb", marginBottom: 8 }, children: "\u{1F4CA} Lead Funnel Optimization" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#374151", lineHeight: 1.6, marginBottom: 2 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 Channel mix: organic 30%, referral 30%, paid 25%, events 15%" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 Test: $219 pricing in small markets (4 week pilot)" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u2022 Risk window: Weeks 8-12 (attendance ",
                "<",
                "80%)"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 6, paddingTop: 6, borderTop: "1px solid #f3f4f6", fontSize: 8, color: "#059669", fontWeight: 600 }, children: "\u2192 Weekly check-ins for flagged students" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "12px", border: `1px solid #e5e7eb`, background: "#ffffff", borderRadius: 4, boxShadow: "0 1px 2px rgba(0,0,0,0.05)" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: "#dc2626", marginBottom: 8 }, children: "\u{1F465} Capacity & Staffing" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#374151", lineHeight: 1.6, marginBottom: 2 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 Utilization: 94% (\u26A0\uFE0F Critical \u2014 hire immediately)" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 Forecast: +1 instructor needed in 6 months" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 Risk: Team burnout at current levels" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 6, paddingTop: 6, borderTop: "1px solid #f3f4f6", fontSize: 8, color: "#059669", fontWeight: 600 }, children: "\u2192 Post hiring, target 90-day ramp" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "12px", border: `1px solid #e5e7eb`, background: "#ffffff", borderRadius: 4, boxShadow: "0 1px 2px rgba(0,0,0,0.05)" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: "#059669", marginBottom: 8 }, children: "\u{1F4B0} Revenue & Margin" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#374151", lineHeight: 1.6, marginBottom: 2 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 Mix: 50% Classic, 30% +AI, 20% Elite" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u2022 Y1 Profit: ",
                fmtK(600)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u2022 Elite shift potential: +",
                fmtK(50),
                " (+8%)"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 6, paddingTop: 6, borderTop: "1px solid #f3f4f6", fontSize: 8, color: "#059669", fontWeight: 600 }, children: "\u2192 Classic\u2192Elite tier migration campaign" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "12px", border: `1px solid #e5e7eb`, background: "#ffffff", borderRadius: 4, boxShadow: "0 1px 2px rgba(0,0,0,0.05)" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: "#7c3aed", marginBottom: 8 }, children: "\u{1F4C8} Retention & Engagement" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#374151", lineHeight: 1.6, marginBottom: 2 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 At-risk: ~13 students (15% of cohort)" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 Recovery rate: 60% with intervention" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 Experiment: Age-grouped vs. mixed cohorts" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 6, paddingTop: 6, borderTop: "1px solid #f3f4f6", fontSize: 8, color: "#059669", fontWeight: 600 }, children: "\u2192 Buddy-pair system, weekly check-ins" })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "12px", border: `1px solid #e5e7eb`, background: "#ffffff", borderRadius: 4, boxShadow: "0 1px 2px rgba(0,0,0,0.05)" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: "#f59e0b", marginBottom: 8 }, children: "\u{1F3AF} Territory & Network" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#374151", lineHeight: 1.6, marginBottom: 2 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 Expansion opportunities: Austin (92), Boulder (78)" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 2nd location: Pleasanton \u2192 Livermore (5mi)" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 Network: Silicon Valley 3-center cluster" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 6, paddingTop: 6, borderTop: "1px solid #f3f4f6", fontSize: 8, color: "#059669", fontWeight: 600 }, children: "\u2192 Capital planning & franchisee recruitment" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "12px", border: `1px solid #e5e7eb`, background: "#ffffff", borderRadius: 4, boxShadow: "0 1px 2px rgba(0,0,0,0.05)" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: "#6366f1", marginBottom: 8 }, children: "\u{1F517} Data Integration" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#374151", lineHeight: 1.6, marginBottom: 2 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 Real-time data ingestion" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                "\u2022 Freshness target: ",
                "<",
                "12 hours lag"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "\u2022 Alert levels: CRITICAL, HIGH, MEDIUM, LOW" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 6, paddingTop: 6, borderTop: "1px solid #f3f4f6", fontSize: 8, color: "#059669", fontWeight: 600 }, children: "\u2192 Connected & monitoring network" })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { background: "#f9fafb", padding: "12px", borderRadius: 4, border: `1px solid #e5e7eb`, marginTop: 12 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: "#111827", marginBottom: 8 }, children: "\u{1F4CA} Live Alerts & Active Tests" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 10, flexWrap: "wrap", fontSize: 8.5 }, children: (() => {
            const criticalAlerts = alerts.filter((a) => a.severity === "CRITICAL" || a.ev === "CRITICAL");
            const highAlerts = alerts.filter((a) => a.severity === "HIGH" || a.ev === "HIGH");
            const mediumAlerts = alerts.filter((a) => a.severity === "MEDIUM" || a.ev === "MEDIUM");
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
              criticalAlerts.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "6px 12px", background: "#fecaca", color: "#7f1d1d", borderRadius: 3, fontWeight: 600, fontSize: 8.5 }, children: [
                "\u{1F534} CRITICAL: ",
                criticalAlerts.length
              ] }),
              highAlerts.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "6px 12px", background: "#fed7aa", color: "#92400e", borderRadius: 3, fontWeight: 600, fontSize: 8.5 }, children: [
                "\u{1F7E0} HIGH: ",
                highAlerts.length
              ] }),
              mediumAlerts.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "6px 12px", background: "#bfdbfe", color: "#1e40af", borderRadius: 3, fontWeight: 600, fontSize: 8.5 }, children: [
                "\u{1F535} MEDIUM: ",
                mediumAlerts.length
              ] }),
              abTests.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "6px 12px", background: "#d1d5db", color: "#374151", borderRadius: 3, fontWeight: 600, fontSize: 8.5 }, children: [
                "\u{1F9EA} ",
                abTests.length,
                " Test",
                abTests.length > 1 ? "s" : "",
                " Active"
              ] }),
              criticalAlerts.length === 0 && highAlerts.length === 0 && mediumAlerts.length === 0 && abTests.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { padding: "6px 12px", color: "#6b7280", fontSize: 8.5 }, children: "\u2713 No active alerts. System monitoring normally." })
            ] });
          })() })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, padding: "10px 12px", marginBottom: 14, background: "#f9f9f9" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 10 }, children: "Network Scale Impact \u2014 348 Franchise Units" }),
        (() => {
          const unitsInNetwork = 348;
          const avgBaselineMonthly = 18500;
          const avgBaselineAnnual = avgBaselineMonthly * 12;
          const curriculumAdopters = Math.floor(unitsInNetwork * 0.6);
          const communityAdopters = Math.floor(unitsInNetwork * 0.4);
          const pricingAdopters = Math.floor(unitsInNetwork * 0.7);
          const curriculumImpact = curriculumAdopters * 45e3;
          const communityImpact = communityAdopters * 3e4;
          const pricingImpact = pricingAdopters * 67e3;
          const currentNetworkRevenue = unitsInNetwork * avgBaselineAnnual;
          const potentialNetworkRevenue = currentNetworkRevenue + curriculumImpact + communityImpact + pricingImpact;
          const revenueIncrease = potentialNetworkRevenue - currentNetworkRevenue;
          const percentageGrowth = revenueIncrease / currentNetworkRevenue * 100;
          const estimatedNewStudents = curriculumAdopters * 15 + communityAdopters * 8 + pricingAdopters * 0;
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 12, flexWrap: "wrap" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 240px", padding: "10px", border: `1px solid #ddd`, borderRadius: 3 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: MUT, marginBottom: 6 }, children: "CURRENT NETWORK" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11, color: "#333", marginBottom: 2 }, children: [
                "Revenue ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: fmtM(currentNetworkRevenue) })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9, color: "#666", marginBottom: 4 }, children: [
                "348 units \xD7 ",
                fmtK(avgBaselineAnnual),
                "/year"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#999" }, children: "Baseline operations, Classic-only curriculum" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 240px", padding: "10px", border: `1px solid #ddd`, borderRadius: 3, background: "#f0fdf4" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: GRN, marginBottom: 6 }, children: "WITH GROWTH STRATEGY" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11, color: GRN, marginBottom: 2 }, children: [
                "Revenue ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: fmtM(potentialNetworkRevenue) })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9, color: "#666", marginBottom: 4 }, children: [
                "+",
                fmtM(revenueIncrease),
                " (+",
                percentageGrowth.toFixed(1),
                "%)"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9, color: "#999" }, children: [
                estimatedNewStudents.toLocaleString(),
                " new student enrollments"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 240px", padding: "10px", border: `1px solid #ddd`, borderRadius: 3 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: AC, marginBottom: 6 }, children: "ADOPTION SNAPSHOT" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 2 }, children: [
                "Curriculum launch: ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: curriculumAdopters }),
                " units (",
                Math.round(curriculumAdopters / unitsInNetwork * 100),
                "%)"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 2 }, children: [
                "Community programs: ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: communityAdopters }),
                " units (",
                Math.round(communityAdopters / unitsInNetwork * 100),
                "%)"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#666", marginBottom: 4 }, children: [
                "Pricing optimization: ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: pricingAdopters }),
                " units (",
                Math.round(pricingAdopters / unitsInNetwork * 100),
                "%)"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8, color: "#999" }, children: "Conservative adoption rates" })
            ] })
          ] });
        })(),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8.5, color: "#666", marginTop: 8, padding: "8px 0" }, children: "Network-scale analysis assumes conservative adoption (40-70%) with realistic execution. Individual territory performance varies by market size, competition, and franchisee capability \u2014 top quartile centers can exceed these projections 2-3x." })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 10, flexWrap: "wrap" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 300px", border: `1px solid ${RULE}`, padding: "10px 12px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 8 }, children: "Scenario comparison" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("table", { style: { width: "100%", fontSize: 11, borderCollapse: "collapse" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("thead", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("tr", { style: { borderBottom: `1px solid ${RULE}` }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { style: { textAlign: "left", padding: "4px 0", fontWeight: 700, color: INK }, children: "Metric" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { style: { textAlign: "center", padding: "4px 0", fontWeight: 700, color: GRN }, children: "Opt" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { style: { textAlign: "center", padding: "4px 0", fontWeight: 700, color: AC }, children: "Real" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { style: { textAlign: "center", padding: "4px 0", fontWeight: 700, color: AMB }, children: "Pess" })
            ] }) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tbody", { children: [
              { label: "Revenue", get: (d) => fmtM(d.financials.revenue_monthly) },
              { label: "Royalty", get: (d) => fmtK(d.financials.royalty_revenue) },
              { label: "Margin", get: (d) => fmtPct(d.financials.margin) },
              { label: "Senseis", get: (d) => d.staffing.senseis },
              { label: "Retention", get: (d) => fmtPct(d.retention_rate) },
              { label: "Territories", get: (d) => d.expansion.new_territories }
            ].map((m, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("tr", { style: { borderBottom: `1px solid #eee` }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", { style: { padding: "4px 0", color: "#333" }, children: m.label }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", { style: { textAlign: "center", padding: "4px 0", color: "#555" }, children: m.get(opt.quantum.scenarios.optimistic) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", { style: { textAlign: "center", padding: "4px 0", color: "#555" }, children: m.get(opt.quantum.scenarios.realistic) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", { style: { textAlign: "center", padding: "4px 0", color: "#555" }, children: m.get(opt.quantum.scenarios.pessimistic) })
            ] }, i)) })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 300px", border: `1px solid ${RULE}`, padding: "10px 12px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 8 }, children: "Decision ledger \u2014 this view's entries" }),
          quantumLog.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 10.5, color: MUT }, children: "No scenario decisions yet. Approvals and overrides made here are written to the same ledger the audit tab reads." }) : quantumLog.map((e, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 10.5, color: "#333", padding: "4px 0", borderTop: i > 0 ? `1px solid #f0f0ee` : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: e.actor }),
            " \u2014 ",
            e.text
          ] }, i))
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 12, flexWrap: "wrap", marginTop: 16 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 320px", border: `1px solid #e5e7eb`, padding: "12px", background: "#f9fafb", borderRadius: 4 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: "#111827", marginBottom: 10 }, children: "\u{1F4CA} Feedback Loop \u2014 Test Results" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#374151", lineHeight: 1.7, space: "pre-line" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 8, paddingBottom: 8, borderBottom: "1px solid #d1d5db" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontWeight: 600 }, children: "pricing_small_219" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8, color: "#6b7280" }, children: "Enrollment -2%, Margin +8%" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8.5, fontWeight: 600, color: "#059669", marginTop: 2 }, children: "\u2713 +$200/center net impact" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 8, paddingBottom: 8, borderBottom: "1px solid #d1d5db" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontWeight: 600 }, children: "robotics_first" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8, color: "#6b7280" }, children: "Completion 88%, +4% vs control" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8.5, fontWeight: 600, color: "#059669", marginTop: 2 }, children: "\u2713 Scale to phase 2" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 0 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontWeight: 600 }, children: "elite_messaging_v2" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8, color: "#6b7280" }, children: "Tier migration 22%, +3% vs control" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8.5, fontWeight: 600, color: "#f59e0b", marginTop: 2 }, children: "\u27F3 Keep running (high signal)" })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8, color: "#6b7280", marginTop: 10, paddingTop: 10, borderTop: "1px solid #d1d5db" }, children: "Weights auto-adjust based on statistical significance. Rolling 4-week measurement window." })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 320px", border: `1px solid #e5e7eb`, padding: "12px", background: "#f9fafb", borderRadius: 4 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: "#111827", marginBottom: 10 }, children: "\u{1F4C8} Execution Tracking" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: "#374151", lineHeight: 1.7 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 8, display: "flex", justifyContent: "space-between", alignItems: "center", paddingBottom: 8, borderBottom: "1px solid #d1d5db" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: "Playbooks sent this week" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontWeight: 700, fontSize: 11, color: "#111827" }, children: Object.keys(executionTracking).length })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 8, display: "flex", justifyContent: "space-between", alignItems: "center", paddingBottom: 8, borderBottom: "1px solid #d1d5db" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: "Execution rate" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontWeight: 700, color: "#059669" }, children: [
                Object.keys(executionTracking).length > 0 ? Math.round(Object.values(executionTracking).filter(((x) => x.executed)).length / Object.keys(executionTracking).length * 100) : 0,
                "%"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 0 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginBottom: 4 }, children: "Post-execution outcomes:" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#6b7280" }, children: [
                "\u2022 Health: ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: "#059669", fontWeight: 600 }, children: "+3.2" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#6b7280" }, children: [
                "\u2022 Churn: ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: "#059669", fontWeight: 600 }, children: "-1.8%" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: "#6b7280" }, children: [
                "\u2022 ARPU: ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: "#059669", fontWeight: 600 }, children: "+$32" })
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8, color: "#6b7280", marginTop: 10, paddingTop: 10, borderTop: "1px solid #d1d5db" }, children: "Measures outcomes at 2-week checkpoints. Signals when playbook effectiveness changes." })
        ] })
      ] })
    ] });
  }
  function Engine(props) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineInner, { ...props }) });
  }
  function EngineInner({ initialTab: initialTab2 }) {
    const [tab, setTab] = (0, import_react.useState)(initialTab2 || "quantum");
    const q = false;
    (0, import_react.useEffect)(() => {
      if (typeof window !== "undefined" && window.location.hash.slice(1) !== tab) {
        window.location.hash = tab;
      }
    }, [tab]);
    (0, import_react.useEffect)(() => {
      if (typeof window === "undefined") return;
      const onHashChange = () => {
        const h = window.location.hash.slice(1);
        if (h && h !== tab) setTab(h);
      };
      window.addEventListener("hashchange", onHashChange);
      return () => window.removeEventListener("hashchange", onHashChange);
    }, [tab]);
    const [sel, setSel] = (0, import_react.useState)("Pleasanton");
    const [stSel, setSt] = (0, import_react.useState)("CA");
    const [beltSel, setBelt] = (0, import_react.useState)("orange");
    const [ranPlays, setRan] = (0, import_react.useState)({});
    const [opsMsg, setOpsMsg] = (0, import_react.useState)("Ready.");
    const [cap, setCap] = (0, import_react.useState)({ used: 0, week: 1 });
    const [goals, setGoals] = (0, import_react.useState)({});
    const [mapSt, setMapSt] = (0, import_react.useState)(null);
    const [mapMode, setMapMode] = (0, import_react.useState)("health");
    const [mapC, setMapC] = (0, import_react.useState)(null);
    const [cardMsg, setCardMsg] = (0, import_react.useState)("");
    const [leadStage, setLeadStage] = (0, import_react.useState)({});
    const [leadSel, setLeadSel] = (0, import_react.useState)(null);
    const [dealAg, setDealAg] = (0, import_react.useState)({ auto: false, wk: 0, log: [], trained: {}, promoted: [], retired: [], blocked: 0, stats: {} });
    const [qMeas, setQMeas] = (0, import_react.useState)({});
    const [opt, setOpt] = (0, import_react.useState)({ run: false, week: 0, gates: 0, ships: 0, probs: { measurement: 0.7, wave_mgr: 0.7, bestpractice: 0.7, peer_review: 0.7, pricing: 0.7 }, fresh: {}, log: [], quantum: { scenarios: SOLVER_RESULTS, approved: null, approvedAt: null, overrides: {} } });
    const [essentialsOnly, setEssentialsOnly] = (0, import_react.useState)(true);
    const [abTests, setAbTests] = (0, import_react.useState)([]);
    const [executionTracking, setExecutionTracking] = (0, import_react.useState)({});
    const A = (k) => AGENT_LEX[k];
    const V = (k) => VERB_LEX[k];
    const approveScenario = (scenario) => {
      setOpt((prev) => ({
        ...prev,
        quantum: { ...prev.quantum, approved: scenario, approvedAt: Date.now() }
      }));
      logL("quantum", "Rodrigues", `Approved ${scenario} scenario`);
    };
    const overrideTabScenario = (tabName, scenario) => {
      setOpt((prev) => ({
        ...prev,
        quantum: { ...prev.quantum, overrides: { ...prev.quantum.overrides, [tabName]: scenario } }
      }));
      logL("quantum", "Rodrigues", `Overrode ${tabName} to ${scenario}`);
    };
    const getActiveScenario = (tabName) => {
      if (opt.quantum.overrides[tabName]) return opt.quantum.overrides[tabName];
      return opt.quantum.approved || "realistic";
    };
    const [adj, setAdj] = (0, import_react.useState)({});
    const [tour, setTour] = (0, import_react.useState)(0);
    const [reso, setReso] = (0, import_react.useState)({ wk: 0, resolved: false, signed: 0, hist: [1], placed: {}, chan: { "FranNet \u2014 West": { p: 3, ql: 1 }, "IFPG Network": { p: 5, ql: 1 }, "BAI Franchise Brokers": { p: 2, ql: 0 }, "Direct / referral": { p: 4, ql: 2 } } });
    const [ledger, setLedger] = (0, import_react.useState)([]);
    const logL = (tab2, actor, text) => setLedger((L) => [{ t: Date.now(), tab: tab2, actor, text }, ...L].slice(0, 40));
    const applyAdj = (name, d) => setAdj((m) => {
      const cur = m[name] || {};
      const nx = { ...cur };
      Object.entries(d).forEach(([k, v]) => {
        nx[k] = (nx[k] || 0) + v;
      });
      return { ...m, [name]: nx };
    });
    const rawCenters = (0, import_react.useMemo)(buildCenters, []);
    const centers = (0, import_react.useMemo)(() => {
      const posture = opt.quantum && opt.quantum.approved || "realistic";
      return computeCentersForPosture(rawCenters, adj, posture, opt.week);
    }, [rawCenters, adj, opt.quantum && opt.quantum.approved, opt.week]);
    const LEADS = (0, import_react.useMemo)(buildLeads, []);
    (0, import_react.useEffect)(() => {
      if (centers && centers.length > 0 && abTests.length === 0) {
        const tests = seedABTests(centers);
        setAbTests(tests);
      }
    }, [centers]);
    const team = centers.find((c) => c.name === sel) || centers[32];
    const states = (0, import_react.useMemo)(() => {
      const m = {};
      centers.forEach((c) => {
        (m[c.st] = m[c.st] || []).push(c);
      });
      return m;
    }, [centers]);
    const fAll = (0, import_react.useMemo)(() => centers.map((c) => ({ c, f: forecast(c) })), [centers]);
    const alerts = (0, import_react.useMemo)(() => alertsOf(centers), [centers]);
    const trend = { r: fAll.filter((x) => x.f.cls === "rising").length, h: fAll.filter((x) => x.f.cls === "holding").length, d: fAll.filter((x) => x.f.cls === "deteriorating").length };
    const staleBase = (c) => opt.fresh[c.name] === void 0 && hash(c.name) % 38 > 21;
    const staleN = centers.filter(staleBase).length;
    const red = centers.filter((c) => c.eb < 0);
    const blocker = (0, import_react.useMemo)(() => {
      let best = null, bs = -1;
      PATH.forEach((b, i) => {
        const s = COHORT[b[0]][1] * (PATH.length - 1 - i);
        if (s > bs) {
          bs = s;
          best = b;
        }
      });
      return best;
    }, []);
    const GROUPS = { OVERVIEW: ["quantum", "exec", "franchise", "integrated", "rail", "board", "alignment", "compliance", "workload", "queue", "blockers", "warning", "signals"], GROWTH: ["leads", "deals", "expansion", "growth", "whitespace"], CENTERS: ["team", "lenses", "engagement", "mastery", "table", "batch", "compare"], NETWORK: ["network", "optimizer", "transfers", "fivebasis", "cohort"], WORKFLOW: ["acquire", "deliver", "retain", "operate", "plan", "metrics", "failure", "onboarding"], PROGRAMS: ["portfolio", "pain", "calendar", "financials", "growthfin"], METHOD: ["agenda", "dynamics", "audit", "success", "reports", "playback", "risk", "fdd", "review", "sensitivity", "glossary"], ADAPTIVE: ["twin", "lifecycle", "labor", "ltv", "velocity", "auction", "scheduler", "sentinel", "precedent", "constitution"], LIVE: ["dojo", "season", "simulator", "monitor", "apisview", "immune", "dagify", "ledgerview", "brief", "negotiate", "joy"] };
    const TABL = { quantum: "quantum pm", integrated: "mission control", alignment: "system alignment", dynamics: "operations dynamics", board: "operations board", lenses: "six lenses", rail: "threads & queue", network: "network health", table: "network map", team: "center detail", mastery: "belts", transfers: "transfers", optimizer: "operations", pain: "franchisee support", portfolio: "programs", engagement: "engagement", franchise: "overview", agenda: "methodology", growth: "growth", calendar: "calendar", acquire: "acquire", deliver: "deliver", retain: "retain", operate: "operate", plan: "first 90", metrics: "metrics", failure: "failure", warning: "early warning", leads: "lead pipeline", expansion: "expansion engine", fivebasis: "unified views", signals: "network signals", compliance: "compliance & safety", workload: "approver workload", whitespace: "territory white space", financials: "financial roll-up", audit: "audit trail", blockers: "growth blockers", deals: "deals", onboarding: "opening project plan", growthfin: "financial growth", success: "success rate", reports: "reports & exports", batch: "batch operations", playback: "historical playback", exec: "executive summary", glossary: "glossary", risk: "risk register", fdd: "fdd item 20", review: "week in review", compare: "center comparison", queue: "approval queue", cohort: "cohort analysis", sensitivity: "sensitivity analysis", twin: "network twin", lifecycle: "lifecycle engine", labor: "sensei supply chain", ltv: "family forward value", velocity: "curriculum velocity", auction: "territory portfolio", scheduler: "interaction scheduler", sentinel: "compliance sentinel", precedent: "precedent & calibration", constitution: "governance constitution", dojo: "the dojo floor", season: "the season", simulator: "year simulator", monitor: "state monitor", apisview: "agent systems", immune: "signal integrity", dagify: "dependency graphs", ledgerview: "decision ledger", brief: "weekly brief", negotiate: "negotiation prep", joy: "joy ledger" };
    const groupOf = (t) => Object.keys(GROUPS).find((g) => GROUPS[g].includes(t));
    const onTabBarKeyDown = (e) => {
      if (e.key !== "ArrowLeft" && e.key !== "ArrowRight") return;
      const visible = essentialsOnly ? GROUPS[groupOf(tab)].filter((t) => ESSENTIAL_TABS.includes(t) || t === tab) : GROUPS[groupOf(tab)];
      const i = visible.indexOf(tab);
      if (i === -1) return;
      e.preventDefault();
      const next = e.key === "ArrowRight" ? visible[(i + 1) % visible.length] : visible[(i - 1 + visible.length) % visible.length];
      setTab(next);
    };
    const GROUP_LABEL = { OVERVIEW: "Overview", GROWTH: "Growth Pipeline", CENTERS: "Centers", NETWORK: "Network", WORKFLOW: "Workflow", PROGRAMS: "Programs", METHOD: "Methodology", ADAPTIVE: "Adaptive Systems", LIVE: "Live Systems" };
    const railData = (0, import_react.useMemo)(() => runAllAgents(centers, states, LEADS), [centers, states, LEADS]);
    const boardOverride = opt.quantum && opt.quantum.overrides && opt.quantum.overrides.board;
    const boardGlobalPosture = opt.quantum && opt.quantum.approved || "realistic";
    const boardOverrideActive = !!(boardOverride && boardOverride !== boardGlobalPosture);
    const boardRailData = (0, import_react.useMemo)(() => {
      if (!boardOverrideActive) return railData;
      return computeOverrideView(rawCenters, adj, boardOverride, opt.week, LEADS).railData;
    }, [boardOverrideActive, boardOverride, rawCenters, adj, opt.week, railData]);
    const [spotDecision, setSpotDecision] = (0, import_react.useState)(null);
    const [decisions, setDecisions] = (0, import_react.useState)({});
    const [decisionHistory, setDecisionHistory] = (0, import_react.useState)({});
    const historySeqRef = (0, import_react.useRef)(0);
    const recordDecision = (id, status) => {
      historySeqRef.current += 1;
      setDecisionHistory((h) => ({ ...h, [id]: [...h[id] || [], { status, seq: historySeqRef.current }] }));
    };
    const [dyn, setDyn] = (0, import_react.useState)({ resolved: {}, committed: {}, completed: {} });
    const [auditQ, setAuditQ] = (0, import_react.useState)("");
    const [auditTabFilter, setAuditTabFilter] = (0, import_react.useState)("all");
    const [batchCondition, setBatchCondition] = (0, import_react.useState)("all");
    const [batchState, setBatchState] = (0, import_react.useState)("all");
    const [batchStaleOnly, setBatchStaleOnly] = (0, import_react.useState)(false);
    const [snapshots, setSnapshots] = (0, import_react.useState)([]);
    const [playA, setPlayA] = (0, import_react.useState)(null);
    const [playB, setPlayB] = (0, import_react.useState)(null);
    const [globalQ, setGlobalQ] = (0, import_react.useState)("");
    const [compareSelect, setCompareSelect] = (0, import_react.useState)([]);
    const [queueRole, setQueueRole] = (0, import_react.useState)("all");
    const [sensitivityParams, setSensitivityParams] = (0, import_react.useState)({ tuition: 0, capacity: 0, support: 0 });
    const [focusCenter, setFocusCenter] = (0, import_react.useState)(null);
    (0, import_react.useEffect)(() => {
      if (tab !== "network" || !focusCenter) return;
      const c = centers.find((x) => x.name === focusCenter);
      if (c && c.st) setSt(c.st);
    }, [tab, focusCenter, centers]);
    const [jumpReason, setJumpReason] = (0, import_react.useState)(null);
    const jumpTo = (destTab, centerName, reason) => {
      setFocusCenter(centerName);
      setJumpReason(reason || null);
      setTab(destTab);
    };
    const [aboutOpen, setAboutOpen] = (0, import_react.useState)(false);
    const [todayDetail, setTodayDetail] = (0, import_react.useState)(false);
    const BADGE = { rail: railData.actionable.length || null, network: trend.d, pain: red.length, optimizer: opt.gates || null };
    const [tick, setTick] = (0, import_react.useState)(0);
    (0, import_react.useEffect)(() => {
      if (tab !== "franchise") return;
      const id = setInterval(() => setTick((t) => t + 1), 1100);
      return () => clearInterval(id);
    }, [tab]);
    (0, import_react.useEffect)(() => {
      if (!opt.run || tab !== "optimizer") return;
      const id = setInterval(() => {
        const transferFx = [];
        setOpt((o) => {
          if (o.week >= 24) return { ...o, run: false };
          const week = o.week + 1, probs = { ...o.probs }, fresh = { ...o.fresh };
          let gates = o.gates, ships = o.ships;
          const log = [];
          const staleOf = (c) => fresh[c.name] !== void 0 ? (week - fresh[c.name]) * 7 : hash(c.name) % 38 + week * 2;
          const credit = {};
          const staleSet = centers.filter((c) => staleOf(c) > 28);
          if (staleSet.length) {
            const st = [...staleSet].sort((a, b) => staleOf(b) * b.students - staleOf(a) * a.students).slice(0, 6);
            const cleared = st.reduce((a, c) => a + staleOf(c), 0);
            st.forEach((c) => fresh[c.name] = week);
            log.push(["score", "measure", st.length + " units \xB7 " + cleared + "d staleness cleared", 1]);
            credit.measurement = Math.min(0.12, cleared / 2e3);
          }
          const m = centers.filter((c) => staleOf(c) <= 21);
          if (m.length > 1) {
            const b = m.reduce((a, c) => c.conv > a.conv ? c : a), w = m.reduce((a, c) => c.conv < a.conv ? c : a);
            const gap = b.conv - w.conv;
            if (gap > 0.12) {
              log.push(["transfer", "transfer", b.name + "\u2192" + w.name + " \xB7 conv gap " + Math.round(gap * 100) + "pt", 1]);
              credit.bestpractice = Math.min(0.1, gap * 0.3);
              transferFx.push([w.name, { conv: Math.min(0.04, gap * 0.25) }]);
            }
          }
          if (m.length > 3) {
            const br = m.reduce((a, c) => c.ret > a.ret ? c : a), wr = m.reduce((a, c) => c.ret < a.ret ? c : a);
            if (br !== wr && forecast(wr).cls === "deteriorating") {
              log.push(["peer", "review", br.name + "\u2194" + wr.name + " \xB7 retention pair", 1]);
              credit.peer_review = Math.min(0.08, (br.ret - wr.ret) * 0.25);
            }
          }
          const rising = centers.filter((c) => forecast(c).cls === "rising").length;
          if (rising > 0 && week % 2 === 0) {
            const flags = Math.max(1, Math.round(rising / 12));
            log.push(["wave", "schedule", flags + " ramp flag" + (flags > 1 ? "s" : "") + " \xB7 " + rising + " rising", 1]);
            credit.wave_mgr = Math.min(0.06, rising / 400);
          }
          const redNow = centers.filter((c) => c.eb < 0).length;
          if (redNow > 0) {
            gates++;
            log.push(["gate", "hold", "pricing \u2691 \xB7 " + redNow + " units below breakeven", 0]);
            credit.pricing = -0.03;
          }
          Object.entries(credit).forEach(([k, v]) => {
            probs[k] = Math.min(0.98, Math.max(0.05, probs[k] + 0.5 * v));
          });
          return { ...o, run: true, week, gates, ships, probs, fresh, log: [...log, ...o.log].slice(0, 9) };
        });
        if (transferFx.length) {
          transferFx.forEach(([n, d]) => applyAdj(n, d));
          logL("optimizer", "Best practice", transferFx.map(([n]) => n).join(", ") + " conversion lifted by transfer");
        }
      }, 900);
      return () => clearInterval(id);
    }, [opt.run, tab, centers]);
    const runDealWeek = () => {
      const trained = dealAg.trained;
      const plays = [];
      const dealRegionActive = {};
      [...LEADS].sort((a, b) => b.fit - a.fit).forEach((l) => {
        const s = leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0;
        if (s >= 5) return;
        const netState = NET_STATES.find((x) => x.s === l.region);
        const headroom = netState ? netState.h[0] : 0.3;
        const capacity = territoryCapacityOf(headroom);
        const used = dealRegionActive[l.region] || 0;
        dealRegionActive[l.region] = used + 1;
        if (used >= capacity) {
          plays.push({ kind: "block cannibalization", cn: l.n, lever: 0, block: true, ok: true, note: l.region + " territory candidate-supply ceiling reached (" + capacity + ")" });
          return;
        }
        if (l.fit < 50 && s <= 1) {
          plays.push({ kind: "decline \u2014 below qualification", cn: l.n, lever: 1.2, cost: 1, id: l.id, dec: true, ok: l.fit < 45 });
          return;
        }
        if (s < 2) {
          plays.push({ kind: "advance stage", cn: l.n, src: l.src, lever: +(1 + l.fit / 60).toFixed(2), cost: 1, id: l.id, adv: true, ok: l.fit >= 55 });
        } else {
          plays.push({ kind: "awaiting human gate", cn: l.n, lever: +(l.fit / 50).toFixed(2), cost: 0, gate: true });
          if (hash(l.n + "doc" + s) % 2 === 0) plays.push({ kind: "chase documents", cn: l.n, lever: 0.9, cost: 1, ok: !!l.hot });
        }
      });
      plays.forEach((p) => {
        if (trained[p.kind]) p.lever = +(p.lever * trained[p.kind]).toFixed(2);
      });
      const exec = plays.filter((p) => !p.gate && !p.block).sort((a, b) => b.lever - a.lever);
      let capp = 8;
      const log = [];
      const adv = {};
      const executed = [];
      for (const p of exec) {
        if (capp - (p.cost || 1) < 0) continue;
        capp -= p.cost || 1;
        log.push("[" + p.kind + "] " + p.cn + " \xB7 " + p.lever + (p.ok ? " \u2713" : " \u2717"));
        if (p.adv) adv[p.id] = 1;
        executed.push(p);
      }
      if (Object.keys(adv).length) setLeadStage((m) => {
        const nm = { ...m };
        Object.keys(adv).forEach((id) => {
          const l = LEADS.find((x) => String(x.id) === String(id));
          const cur = m[id] !== void 0 ? m[id] : l.stage0;
          nm[id] = Math.min(2, cur + 1);
        });
        return nm;
      });
      if (executed.length) logL("leads", "Deal agent", executed.length + " actions \xB7 " + executed.filter((p) => p.ok).length + " correct");
      setDealAg((A2) => {
        const tr = { ...A2.trained }, promoted = [...A2.promoted], retired = [...A2.retired], stats = { ...A2.stats };
        executed.forEach((p) => {
          const cur = stats[p.kind] || { hits: 0, attempts: 0 };
          const st = { hits: cur.hits + (p.ok ? 1 : 0), attempts: cur.attempts + 1 };
          stats[p.kind] = st;
          if (st.attempts >= 2) {
            const rate = st.hits / st.attempts;
            if (rate >= 0.6) {
              tr[p.kind] = Math.min(1.5, (tr[p.kind] || 1) + 0.08);
              if (!promoted.includes(p.kind)) promoted.push(p.kind);
              const ri = retired.indexOf(p.kind);
              if (ri >= 0) retired.splice(ri, 1);
            } else {
              tr[p.kind] = Math.max(0.6, (tr[p.kind] || 1) - 0.08);
              if (tr[p.kind] <= 0.8 && !retired.includes(p.kind)) retired.push(p.kind);
            }
          }
        });
        return { ...A2, wk: A2.wk + 1, log: [...log, ...A2.log].slice(0, 12), trained: tr, stats, promoted: promoted.slice(-4), retired: retired.slice(-3), blocked: plays.filter((p) => p.block).length };
      });
    };
    const captureSnapshot = (label) => {
      const avgHealth = +(centers.reduce((a, c) => a + c.health, 0) / centers.length).toFixed(1);
      const redN = centers.filter((c) => c.eb < 0).length;
      const staleBaseHere = (c) => opt.fresh[c.name] === void 0 && hash(c.name) % 38 > 21;
      const staleNHere = centers.filter(staleBaseHere).length;
      const snap = {
        week: opt.week,
        t: Date.now(),
        label: label || "week " + opt.week,
        avgHealth,
        redN,
        staleN: staleNHere,
        committedN: Object.keys(dyn.committed || {}).length,
        completedN: Object.keys(dyn.completed || {}).length,
        conflictN: (railData.conflicts || []).length,
        proposalsN: (railData.recommendations || []).length
      };
      setSnapshots((s) => [...s, snap].slice(-26));
    };
    const downloadReport = () => {
      const so = (l) => leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0;
      const rows = [["Prospect", "Source", "Region", "Fit", "Liquidity_k", "NetWorth_k", "Proven", "MultiUnit", "Stage", "Note"]];
      [...LEADS].sort((a2, b) => so(b) - so(a2) || b.fit - a2.fit).forEach((l) => rows.push([l.n, l.src, l.region, l.fit, l.liquidity, l.net, l.proven ? "Y" : "N", l.multi ? "Y" : "N", STAGES6[so(l)], l.note]));
      const csv = rows.map((r) => r.map((c) => '"' + String(c).replace(/"/g, '""') + '"').join(",")).join("\n");
      const blob = new Blob([csv], { type: "text/csv" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = "franchise-pipeline-report.csv";
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
    };
    const downloadAll = () => {
      const so = (l) => leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0;
      const data = {
        generated: (/* @__PURE__ */ new Date()).toISOString(),
        product: "Franchise Development OS \u2014 Proposed Policy Framework \u2014 Candidate Submission (Pratik Singh)",
        note: "Modeled simulation export. Counts: " + centers.length + " modeled network \xB7 64 verified from public records \xB7 FDD Item 20 = 244 authoritative (unreconciled). Seeded/illustrative figures; the status-model mechanics are real, specific numbers are modeled.",
        status_model: "Each center carries a probability split over {thriving,watch,at-risk}; the P&L review is the measurement that verifies it. Each deal carries {signs,stalls,dies} until a stage-gate verifies it.",
        world_state: {
          note: "Single integration layer \u2014 committed actions write metric deltas here; every tab reads the adjusted units.",
          adjustments: adj,
          action_ledger: ledger.map((e) => ({ t: new Date(e.t).toISOString(), tab: e.tab, actor: e.actor, action: e.text }))
        },
        invariants: {
          red_units: centers.filter((c) => c.eb < 0).length,
          stale_units: centers.filter(staleBase).length,
          deteriorating_90d: centers.filter((c) => forecast(c).cls === "deteriorating").length,
          note: "These counts are computed once from the shared world state; every tab that displays them reads the same source."
        },
        network: centers.map((c) => {
          const f = forecast(c);
          const amp = qAmp(c);
          return { name: c.name, state: c.st, verified: c.verified, students: c.students, health: c.health, tier: tierOf(c.health), ebitda_k: c.eb, retention: +c.ret.toFixed(2), engagement: engageOf(c).score, forecast90_k: f.proj[2], trajectory: f.cls, status_probabilities: { thriving: +amp[0].toFixed(2), watch: +amp[1].toFixed(2), atRisk: +amp[2].toFixed(2) }, status_on_review: QSTATES[qResolve(amp)] };
        }),
        pipeline: LEADS.map((l) => ({ prospect: l.n, source: l.src, region: l.region, fit: l.fit, liquidity_k: l.liquidity, netWorth_k: l.net, proven: l.proven, multiUnit: l.multi, stage: STAGES6[so(l)], note: l.note }))
      };
      const blob = new Blob([JSON.stringify(data, null, 2)], { type: "application/json" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = "franchise-development-os-export.json";
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
    };
    (0, import_react.useEffect)(() => {
      if (!dealAg.auto || tab !== "leads") return;
      const id = setInterval(() => {
        if (dealAg.wk >= 16) {
          setDealAg((A2) => ({ ...A2, auto: false }));
          return;
        }
        runDealWeek();
      }, 1200);
      return () => clearInterval(id);
    }, [dealAg.auto, dealAg.wk, tab]);
    const Node = ({ ag, vb, tg, state, c, onClick, title, w }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { onClick, title: title || "", style: { display: "inline-flex", flexDirection: "column", cursor: onClick ? "pointer" : "default", verticalAlign: "middle", margin: "2px 0", minWidth: w || 0 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { display: "inline-flex", alignItems: "baseline", gap: 5, border: `1px solid ${c || RULE}`, padding: "3px 8px", background: "#fff", fontFamily: "Helvetica", fontSize: 10 }, children: [
        ag && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontSize: 8.5, letterSpacing: 0.4, textTransform: "uppercase", color: c || MUT }, children: ag }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontWeight: 700, color: INK }, children: vb }),
        tg && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: "#555" }, children: tg })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { height: 3, background: state === "done" ? GRN : state === "run" ? AMB : state === "hold" ? AC : "#e4e0d6" } })
    ] });
    const Arrow = () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 11, color: MUT, margin: "0 4px" }, children: "\u2192" });
    const Chain = ({ items }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap", alignItems: "center", marginBottom: 5 }, children: items.map((n, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { display: "inline-flex", alignItems: "center" }, children: [
      i > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Arrow, {}),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Node, { ...n })
    ] }, i)) });
    const Rail = ({ d, label, hot }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { title: label, style: { position: "absolute", left: d / 90 * 100 + "%", transform: "translateX(-50%)", display: "flex", flexDirection: "column", alignItems: "center" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 9, height: 9, borderRadius: 5, background: hot ? AC : "#fff", border: `2px solid ${hot ? AC : INK}` } }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, color: hot ? AC : "#555", whiteSpace: "nowrap", marginTop: 1 }, children: label.length > 16 ? label.slice(0, 15) + "\u2026" : label })
    ] });
    const Meter = ({ n, p, c }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, marginBottom: 4 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: n }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: p.toFixed(2) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 6, background: "#eee" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 6, width: p * 100 + "%", background: c, transition: "width .4s" } }) })
    ] });
    const Gate = ({ inLabel, inVal, thr, met, payout, title }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { title, style: { display: "flex", flexWrap: "wrap", alignItems: "center", marginBottom: 4 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Node, { vb: inLabel, tg: inVal, state: "done", c: RULE }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Arrow, {}),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Node, { vb: "\u2265 " + thr, state: met ? "done" : "pend", c: met ? GRN : AMB, title: "threshold gate" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Arrow, {}),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Node, { vb: payout, state: met ? "done" : "pend", c: met ? GRN : MUT })
    ] });
    const QWave = ({ amp, coh, measured, dom, note, compact, labels }) => {
      const d = dom !== void 0 ? dom : qResolve(amp);
      const L = labels || QSTATES;
      return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", width: "100%" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 2, height: compact ? 16 : 24, alignItems: "flex-end" }, children: amp.map((a, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { title: L[i] + " " + pct(a) + "%", style: { flex: 1, display: "flex", flexDirection: "column", justifyContent: "flex-end", height: "100%" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { background: HCOL[i], opacity: measured ? i === d ? 1 : 0.22 : 0.7, height: Math.max(8, a * 100) + "%", borderRadius: 2, transition: "opacity .3s,height .3s" } }) }, i)) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 6, marginTop: 3 }, children: L.map((n, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { flex: 1, textAlign: "center", fontSize: 8 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: HCOL[i] }, children: [
            pct(amp[i]),
            "%"
          ] }),
          " ",
          n
        ] }, i)) }),
        !compact && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8, color: MUT, marginTop: 2, lineHeight: 1.4 }, children: [
          measured ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
            "Measured \u2014 verified as ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: HCOL[d] }, children: L[d] }),
            " \xB7 confidence ",
            pct(coh),
            "%."
          ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
            "Estimated \xB7 confidence ",
            pct(coh),
            "% \u2014 estimate ages until measured."
          ] }),
          note ? " \xB7 " + note : ""
        ] })
      ] });
    };
    const arrow = (cls) => cls === "rising" ? ["\u2191", GRN] : cls === "deteriorating" ? ["\u2193", AC] : ["\u2192", AMB];
    const [integratedView, setIntegratedView] = (0, import_react.useState)("network");
    const [workflowStepSel, setWorkflowStepSel] = (0, import_react.useState)(null);
    const WORKFLOW_PHASE_COLORS = { acquire: "#2196F3", deliver: "#4CAF50", retain: "#9C27B0", operate: "#FF9800" };
    const WorkflowStepComponent = ({ step, phaseColor, idx }) => {
      const isGate = step.gate === true;
      const isDecision = step.decision === true;
      const alertCount = (railData.recommendations || []).filter((r) => r.targetIds && r.targetIds.includes(step.s)).length;
      return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(
        "div",
        {
          onClick: () => setWorkflowStepSel(step.n),
          style: { padding: "8px 10px", border: `1px solid ${workflowStepSel === step.n ? VIO : RULE}`, background: workflowStepSel === step.n ? "#f5e8ff" : "#fff", cursor: "pointer", marginBottom: 6, borderRadius: 3, borderLeft: `3px solid ${phaseColor}`, position: "relative" },
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { fontFamily: "Helvetica", fontSize: 11, color: INK }, children: [
                step.n,
                ". ",
                step.s
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 9, color: MUT, fontFamily: "Helvetica" }, children: step.p.toUpperCase() })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 6, marginTop: 4, fontSize: 9, color: "#666" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                "\u{1F464} ",
                step.o
              ] }),
              step.sys !== "\u2014" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                "\u{1F4BB} ",
                step.sys
              ] }),
              isGate && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: AC, fontWeight: 700 }, children: "\u{1F512} GATE" }),
              isDecision && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: AMB, fontWeight: 700 }, children: "\u26A0 DECISION" }),
              alertCount > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: AC, fontWeight: 700 }, children: [
                "\u26A1 ",
                alertCount
              ] })
            ] }),
            workflowStepSel === step.n && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 8, paddingTop: 6, borderTop: `1px solid ${RULE}`, fontSize: 10, color: "#333", lineHeight: 1.5 }, children: step.d })
          ]
        },
        idx
      );
    };
    const WorkflowViewComponent = () => {
      const byPhase = { acquire: W.filter((s) => s.p === "acquire"), deliver: W.filter((s) => s.p === "deliver"), retain: W.filter((s) => s.p === "retain"), operate: W.filter((s) => s.p === "operate") };
      return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(4,1fr)", gap: 12 }, children: Object.entries(byPhase).map(([phase, steps]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, color: "#fff", background: WORKFLOW_PHASE_COLORS[phase], padding: "6px 8px", marginBottom: 8, borderRadius: 2, textTransform: "uppercase", letterSpacing: 0.5 }, children: PHASE_META[phase].name }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9, color: MUT, marginBottom: 8 }, children: [
          "Steps ",
          PHASE_META[phase].range,
          " (",
          PHASE_META[phase].count,
          " total)"
        ] }),
        steps.map((step, idx) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkflowStepComponent, { step, phaseColor: WORKFLOW_PHASE_COLORS[phase], idx }, idx))
      ] }, phase)) });
    };
    const IntegratedViewSelector = () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 3, flexWrap: "wrap", marginBottom: 12, paddingBottom: 8, borderBottom: `1px solid ${RULE}` }, children: ["network", "workflow", "scenarios", "execution", "deepdive"].map((v) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setIntegratedView(v), style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: v === integratedView ? 700 : 400, padding: "5px 10px", cursor: "pointer", border: `1px solid ${v === integratedView ? INK : RULE}`, background: v === integratedView ? INK : "#fff", color: v === integratedView ? "#fff" : INK, textTransform: "capitalize", borderRadius: 2 }, children: v }, v)) });
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "cn-wrap", style: { fontFamily: "Helvetica", color: INK, background: BG, maxWidth: 960, margin: "0 auto", padding: "24px 18px", WebkitFontSmoothing: "antialiased", MozOsxFontSmoothing: "grayscale", textRendering: "optimizeLegibility" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("style", { children: `
   @media print {
    .no-print{display:none !important;}
    .cn-wrap{max-width:100% !important;padding:6px !important;}
   }
   @media (max-width:640px){
    .cn-wrap{padding:10px 8px !important;}
    .cn-navscroll{overflow-x:auto !important;flex-wrap:nowrap !important;-webkit-overflow-scrolling:touch;}
    .cn-navscroll::-webkit-scrollbar{height:4px;}
    .cn-hero-title{font-size:18px !important;}
   }
  ` }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { borderBottom: `3px double ${INK}`, paddingBottom: 8 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", flexWrap: "wrap" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("h1", { className: "cn-hero-title", style: { fontSize: 24, margin: 0, letterSpacing: -0.5 }, children: "How 350+ Centers Operate as One Governed System" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { className: "no-print", style: { display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 2 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { display: "flex", gap: 6, alignItems: "center" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { value: globalQ, onChange: (e) => setGlobalQ(e.target.value), placeholder: "Search centers, states, views\u2026", "aria-label": "Global search across centers, states, and views", style: { fontFamily: "Helvetica", fontSize: 9, padding: "4px 8px", border: `1px solid ${RULE}`, minWidth: 170 } }),
              (railData.conflicts || []).length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setTab("rail"), "aria-label": (railData.conflicts || []).length + " cross-agent conflicts, open Decision Rail", title: "Open Decision Rail", style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "3px 8px", cursor: "pointer", border: `1px solid ${AC}`, background: "#fdf3f2", color: AC }, children: [
                "\u26A0 ",
                (railData.conflicts || []).length,
                " conflict",
                (railData.conflicts || []).length > 1 ? "s" : ""
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => downloadText(buildSummaryText(centers, states, railData, dyn, opt, ledger), "cn-summary.txt"), "aria-label": "Download a static text summary for offline review", title: "Static summary of Overview + System Alignment for offline review", style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "3px 8px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: MUT }, children: "\u2B07 Print summary" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: downloadAll, style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, padding: "6px 12px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", color: INK }, children: "\u2B07 Export / Sync to MyStudio, QuickBooks" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 7.5, color: MUT }, children: "exports the canonical state as JSON \u2014 a live deployment would sync via API instead of manual export" })
          ] })
        ] }),
        tab === "franchise" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 15, color: "#222", margin: "7px 0 2px", lineHeight: 1.45 }, children: [
          "The Director of Franchise Development role sits at the intersection of network strategy and center-level execution; this model is designed to operate at both levels concurrently. ",
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT, fontSize: 12.5 }, children: "Unit-level deterioration is identified early. Support is routed under governance. Growth proceeds on a measured cadence. All figures below are live and derived from a single canonical state." })
        ] }),
        tab === "franchise" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 11, color: "#555", margin: "0 0 4px", lineHeight: 1.5 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: INK }, children: "The business case:" }),
          " improve unit economics across the existing 244 before adding the next 50 \u2014 the Unit-Value Program alone models ~$1.36M/yr in additional royalty at 400 units, at the lowest risk of the four available levers."
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT, letterSpacing: 0.5, textTransform: "uppercase", marginTop: 3 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: INK }, children: "Proposed Policy Framework \u2014 Candidate Submission" }),
          " \xB7 ",
          centers.length,
          " modeled",
          (() => {
            const so = (l) => leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0;
            const s = LEADS.filter((l) => so(l) === 5).length;
            const parts = [];
            if (s) parts.push("+" + s + " signed in pipeline");
            if (reso.signed) parts.push("+" + reso.signed + " committed openings via growth loop");
            return parts.length ? " (" + parts.join(" \xB7 ") + ")" : "";
          })(),
          " \xB7 64 verified \xB7 FDD Item 20 = 244 authoritative \xB7 proposes, does not enact"
        ] })
      ] }),
      (() => {
        const q2 = globalQ.trim().toLowerCase();
        if (!q2) return null;
        const centerMatches = centers.filter((c) => c.name.toLowerCase().includes(q2) || c.st.toLowerCase() === q2).slice(0, 5);
        const tabMatches = Object.entries(TABL).filter(([k, label]) => label.toLowerCase().includes(q2) || k.toLowerCase().includes(q2)).slice(0, 4);
        const noResults = centerMatches.length === 0 && tabMatches.length === 0;
        return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "no-print", style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", padding: "7px 10px", margin: "8px 0 0" }, children: noResults ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT }, children: [
          'No centers, states, or views match "',
          globalQ,
          '".'
        ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
          centerMatches.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: tabMatches.length ? 6 : 0 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginRight: 6 }, children: "Centers" }),
            centerMatches.map((c) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => {
              setSel(c.name);
              setFocusCenter(c.name);
              setTab("team");
              setGlobalQ("");
            }, style: { fontFamily: "Helvetica", fontSize: 9.5, padding: "3px 8px", marginRight: 5, cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: INK }, children: [
              c.name,
              " ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
                "\xB7 ",
                c.st
              ] })
            ] }, c.name))
          ] }),
          tabMatches.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginRight: 6 }, children: "Views" }),
            tabMatches.map(([k, label]) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
              setTab(k);
              setGlobalQ("");
            }, style: { fontFamily: "Helvetica", fontSize: 9.5, padding: "3px 8px", marginRight: 5, cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: INK, textTransform: "capitalize" }, children: label }, k))
          ] })
        ] }) });
      })(),
      tab !== "franchise" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap", background: "#f7f6f2", border: `1px solid ${RULE}`, padding: "5px 9px", margin: "9px 0 0", fontFamily: "Helvetica", fontSize: 10 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontSize: 8.5, letterSpacing: 0.5, textTransform: "uppercase", color: AC }, children: "90d" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: GRN }, children: [
          "\u2191",
          trend.r
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: AMB }, children: [
          "\u2192",
          trend.h
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: AC }, children: [
          "\u2193",
          trend.d
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: "\xB7" }),
        alerts.slice(0, 3).map((a, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => {
          setSel(a.c.name);
          setTab("team");
        }, style: { fontFamily: "Helvetica", fontSize: 9.5, padding: "2px 6px", cursor: "pointer", border: `1px solid ${a.eta <= 30 ? AC : AMB}`, background: "#fff", color: a.eta <= 30 ? AC : "#855" }, children: [
          a.c.name,
          " \xB7 ",
          a.ev,
          " ~",
          a.eta,
          "d"
        ] }, i))
      ] }),
      tour >= 0 && (() => {
        const STEPS = [
          ["Welcome \u2014 25-step guided flow", "This is a working system, not a static document \u2014 every figure recomputes in response to actions taken. All 25 stops cover the full operating loop: growth story, peer benchmarking, system dynamics, governance, and what-if strategic modeling.", null, null],
          ["1 \xB7 Executive Summary \u2014 the whole pitch in one screen", "Five numbers and three sentences \u2014 network health, governance this cycle, and the growth thesis, with no scrolling required. Start here if you only have two minutes.", "exec", "Open Executive Summary"],
          ["2 \xB7 Overview \u2014 network health & today's decisions", "Composite health, proposed actions from four evaluation agents, and one governed action you may approve directly \u2014 all above the fold.", null, "Stay on Overview"],
          ["3 \xB7 Network Map \u2014 the whole territory at a glance", "Every state in the modeled network, aggregated from its real centers \u2014 sized by unit count, colored by condition, ringed in red where a cross-agent conflict is open.", "table", "Open Network Map"],
          ["4 \xB7 Territory White Space \u2014 where to grow next", "The forward-looking counterpart to the map: territories ranked by the same modeled headroom figure the Growth agent's own capacity gate reads, against how few units are already there.", "whitespace", "Open Territory White Space"],
          ["5 \xB7 Growth Pipeline \u2014 candidates & territory headroom", "The lead pipeline runs the full sales cycle; the Growth Agent matches hot candidates to territories with support capacity to spare.", "leads", "Open Growth Pipeline"],
          ["6 \xB7 Operations Board \u2014 interventions in parallel", "Every open intervention across the network, concurrently: contract-window lanes, a decision-state board, and a governed trace feed \u2014 all writing to one shared decision record.", "board", "Open Operations Board"],
          ["7 \xB7 Approver Workload \u2014 the capacity behind those interventions", "Every proposal names an approver role and counts against that role's weekly capacity ceiling \u2014 the same limits the agents themselves respect. This is that capacity, as its own view.", "workload", "Open Approver Workload"],
          ["8 \xB7 Centers \u2014 unit detail, health & retention", "Every center's full readout \u2014 health, trajectory, recovery time \u2014 plus the Unit Health and Retention agents' support plans.", "team", "Open Centers"],
          ["8b \xB7 Center Comparison \u2014 peer benchmarking across 2\u20135 centers", "Multi-select up to five centers and see them side-by-side across health, profitability, recovery time, retention risk, engagement, and staffing stability. A real operational tool for understanding peer dynamics.", "compare", "Open Center Comparison"],
          ["9 \xB7 Six Lenses \u2014 one center, six perspectives", "Pick any center and see it reinterpreted through student, parent, franchisee, corporate, financial, and system lenses \u2014 all synchronized to the same week and the same active signal.", "lenses", "Open Six Lenses"],
          ["10 \xB7 Network \u2014 clusters & best-practice propagation", "The Network Propagation Agent finds the widest in-cluster health gaps and proposes where a working pattern should spread next.", "network", "Open Network"],
          ["11 \xB7 Programs \u2014 the four growth levers", "Unit-Value, Selective Expansion, Resale & Turnaround, Status Quo \u2014 ranked by risk, each with its floor condition stated plainly.", "portfolio", "Open Programs"],
          ["12 \xB7 Audit Trail \u2014 the complete governed record", "Every logged action this session, most recent first, with its source view and the actor who took it \u2014 the same record System Alignment summarizes, shown here in full, with search and source-tab filtering.", "audit", "Open Audit Trail"],
          ["13 \xB7 Success Rate \u2014 support-plan completion by type", "How often a committed support path actually gets marked complete, grouped by plan type \u2014 retention, staffing, unit-economics, conversion, monitoring \u2014 read straight from the same commit/complete record Audit Trail counts.", "success", "Open Success Rate"],
          ["14 \xB7 Batch Operations \u2014 the same actions, applied to many centers at once", "Filter by condition, state, or staleness, then re-measure or commit the top-ranked support path across the whole filtered set in one action \u2014 each skipped for an open conflict, each logged to Audit Trail.", "batch", "Open Batch Operations"],
          ["15 \xB7 Historical Playback \u2014 the network's own trend line", "A snapshot captured automatically every time the week advances, plus manual captures here \u2014 so a reviewer can compare any two points in the session and see exactly what moved.", "playback", "Open Historical Playback"],
          ["16 \xB7 Reports & Exports \u2014 every export in one place", "Decision ledger, recommendations, center roster, full network sync, MyStudio/QuickBooks payload shapes, and the offline summary \u2014 consolidated here instead of scattered across tabs.", "reports", "Open Reports & Exports"],
          ["17 \xB7 Risk Register \u2014 every at-risk unit ranked", "Every center running below breakeven, with contributing factors (recovery time, data staleness, retention inflection) and the owning approver role \u2014 the enterprise-governance view of what needs attention.", "risk", "Open Risk Register"],
          ["18 \xB7 FDD Item 20 \u2014 franchise disclosure reconciliation", "The 244 authoritative units (FDD statement) held against this model's verified and modeled figures. Stated separately, never blended \u2014 the compliance-specific view.", "fdd", "Open FDD Reconciliation"],
          ["19 \xB7 Week in Review \u2014 this session at a glance", "A digestible memo: how many actions were taken, proposals allowed vs. held, support paths committed and completed, and a snapshot of current network state. The narrative before the ledger.", "review", "Open Week in Review"],
          ["20 \xB7 Sensitivity Analysis \u2014 model the business levers", "Adjust tuition, support capacity, and support cost. Watch health, recovery time, royalty impact, and unit utilization recompute live. What-if scenario modeling for strategic decisions.", "sensitivity", "Open Sensitivity Analysis"],
          ["21 \xB7 Approval Queue \u2014 governance by role", "Who owns what right now. Every approver role's queue, approval rate, and capacity spent \u2014 the operational reality of implementation.", "queue", "Open Approval Queue"],
          ["22 \xB7 Cohort Analysis \u2014 maturity curves", "Centers grouped by age/vintage. How do new (<2yr) vs. emerging (3\u20135yr) vs. mature (>5yr) units compare on health, EBITDA, recovery time, engagement? The trajectory signature.", "cohort", "Open Cohort Analysis"],
          ["23 \xB7 Methodology \u2014 the record & the rules", "This is where the six operating rules and their thresholds live, in plain language \u2014 the standard every proposal above was actually held to.", "agenda", "Open Methodology"],
          ["24 \xB7 Glossary \u2014 every term defined once", "FBC, EBITDA, chemistry index, headroom, verified vs. modeled \u2014 plain-language definitions for a reader who isn't a franchise operator.", "glossary", "Open Glossary"]
        ];
        const [h, b, t2, lbl] = STEPS[tour];
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "no-print", style: { position: "sticky", top: 8, zIndex: 50, border: `1px solid ${INK}`, borderLeft: `4px solid ${AC}`, background: "#fffdf6", boxShadow: "0 2px 8px rgba(0,0,0,0.12)", padding: "9px 12px", margin: "10px 0 0" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", gap: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 10.5 }, children: h }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setTour(-1), "aria-label": "Dismiss guided tour", style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, cursor: "pointer", whiteSpace: "nowrap", background: "none", border: "none", padding: 0 }, children: "dismiss \xD7" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#333", lineHeight: 1.5, margin: "3px 0 6px" }, children: b }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 6, alignItems: "center" }, children: [
            t2 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setTab(t2), style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "4px 10px", cursor: "pointer", border: `1px solid ${AC}`, background: "#fff", color: AC }, children: [
              lbl,
              " \u25B8"
            ] }),
            tour < STEPS.length - 1 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setTour(tour + 1), style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "4px 10px", cursor: "pointer", border: `1px solid ${INK}`, background: INK, color: "#fff" }, children: [
              "Next (",
              tour + 1,
              "/",
              STEPS.length - 1,
              ")"
            ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setTour(-1), style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "4px 10px", cursor: "pointer", border: `1px solid ${GRN}`, background: GRN, color: "#fff" }, children: "Done \u2014 explore" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: STEPS.map((_, i) => i <= tour ? "\u25CF" : "\u25CB").join(" ") })
          ] })
        ] });
      })(),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "no-print cn-navscroll", style: { display: "flex", gap: 0, borderBottom: `2px solid ${INK}`, marginTop: 10, flexWrap: "wrap", alignItems: "stretch" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 10, padding: "0 14px 0 2px", borderRight: `1px solid ${RULE}`, marginRight: 6 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", lineHeight: 1 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontSize: 20, color: INK }, children: Math.round(centers.reduce((a, c) => a + c.health, 0) / centers.length) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 8, fontWeight: 700, letterSpacing: 1, color: MUT, marginLeft: 3 }, children: "NETWORK" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9, lineHeight: 1.2 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: GRN }, children: [
              "\u2191",
              trend.r
            ] }),
            " ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: "rising" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("br", {}),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: INK }, children: 8 - cap.used }),
            " ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
              "pts \xB7 wk ",
              cap.week
            ] })
          ] })
        ] }),
        Object.keys(GROUPS).map((g) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setTab(GROUPS[g][0]), role: "tab", "aria-selected": groupOf(tab) === g, "aria-label": "Section: " + (GROUP_LABEL[g] || g), style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1.4, textTransform: "uppercase", padding: "8px 15px", cursor: "pointer", border: "none", background: groupOf(tab) === g ? INK : "transparent", color: groupOf(tab) === g ? "#fff" : INK }, children: GROUP_LABEL[g] || g }, g))
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "no-print cn-navscroll", onKeyDown: onTabBarKeyDown, style: { display: "flex", gap: 0, margin: "0 0 8px", flexWrap: "wrap", borderBottom: `1px solid ${RULE}`, alignItems: "center" }, children: [
        (essentialsOnly ? GROUPS[groupOf(tab)].filter((t) => ESSENTIAL_TABS.includes(t) || t === tab) : GROUPS[groupOf(tab)]).map((t) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setTab(t), role: "tab", "aria-selected": tab === t, "aria-label": "View: " + (TABL[t] || t), style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", padding: "6px 12px", cursor: "pointer", border: "none", borderBottom: tab === t ? `3px solid ${AC}` : "3px solid transparent", background: "transparent", color: tab === t ? AC : MUT }, children: [
          TABL[t] || t,
          BADGE[t] ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { marginLeft: 4, fontSize: 8, padding: "1px 4px", background: AC, color: "#fff", borderRadius: 6 }, children: BADGE[t] }) : null
        ] }, t)),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setEssentialsOnly((v) => !v), "aria-pressed": essentialsOnly, "aria-label": "Toggle essentials-only navigation", style: { marginLeft: "auto", fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "4px 10px", cursor: "pointer", border: `1px solid ${RULE}`, background: essentialsOnly ? INK : "#fff", color: essentialsOnly ? "#fff" : MUT }, children: essentialsOnly ? "showing 20 essentials \u2014 show all 39" : "show 20 essentials for a 5-min walkthrough" })
      ] }),
      tab !== "franchise" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "no-print", style: { margin: "0 0 12px", border: `1px solid ${RULE}`, borderLeft: `3px solid ${AC}` }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", flexWrap: "wrap" }, children: [
          [
            [`${V("measure")} 10 of ${staleN} stale`, () => {
              const st = centers.filter(staleBase).slice(0, 10);
              setOpt((o) => {
                const f = { ...o.fresh };
                st.forEach((c) => f[c.name] = o.week);
                return { ...o, fresh: f };
              });
              setOpsMsg(st.length + " measured \xB7 " + (staleN - st.length) + " remain");
              logL(tab, "Measurement", st.length + " units re-measured");
            }],
            [`${V("schedule")} top ${Math.min(5, alerts.filter((a) => a.eta > 0 && a.eta <= 90).length)} of ${alerts.filter((a) => a.eta > 0 && a.eta <= 90).length} interventions`, () => {
              const k = alerts.filter((a) => a.eta > 0 && a.eta <= 90);
              const nr = { ...ranPlays };
              k.slice(0, 5).forEach((a) => {
                nr[a.c.name + a.ev] = true;
                if (a.ev === "EBITDA<0") applyAdj(a.c.name, { eb: 1.5 });
                else if (a.ev === "sensei exit risk") applyAdj(a.c.name, { ret: 0.015, chem: 0.05 });
                else applyAdj(a.c.name, { eb: 0.8 });
              });
              setRan(nr);
              setOpsMsg(Math.min(5, k.length) + " interventions committed \u2014 deltas applied to unit metrics, forecasts recomputed \xB7 gains decay weekly unless re-measured");
              logL(tab, "Forecast", Math.min(5, k.length) + " pre-breach interventions committed");
            }],
            ["Audit territory", () => setOpsMsg("0 overlaps \xB7 governor armed")],
            ["Gate check", () => setOpsMsg(opt.gates + " holds \xB7 0 autonomous")]
          ].map(([s, run], i) => {
            const cost = [2, 3, 1, 1][i];
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => {
              if (cap.used + cost > 8) {
                setOpsMsg("Capacity exhausted \u2014 " + (8 - cap.used) + " of 8 pts left this week.");
                return;
              }
              setCap((c) => ({ ...c, used: c.used + cost }));
              run();
            }, style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "5px 9px", cursor: "pointer", border: "none", borderRight: `1px solid ${RULE}`, background: "transparent", color: cap.used + cost > 8 ? "#bbb" : "#333" }, children: [
              s,
              " ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT, fontWeight: 400 }, children: [
                "\xB7 ",
                cost,
                " pts"
              ] })
            ] }, i);
          }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "5px 9px", color: cap.used >= 8 ? "#8b0000" : "#333" }, children: [
            "WK ",
            cap.week,
            " \xB7 ",
            8 - cap.used,
            " of 8 pts left"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
            captureSnapshot();
            setCap((c) => ({ used: 0, week: c.week + 1 }));
            setAdj((m) => {
              const nm = {};
              let faded = 0, held = 0;
              Object.entries(m).forEach(([n, d]) => {
                const measured = opt.fresh[n] !== void 0;
                const k = measured ? 0.85 : 0.55;
                const nd = {};
                let live = false;
                Object.entries(d).forEach(([f, v]) => {
                  const x = v * k;
                  if (Math.abs(x) >= (f === "eb" ? 0.2 : 5e-3)) {
                    nd[f] = +x.toFixed(3);
                    live = true;
                  }
                });
                if (live) {
                  nm[n] = nd;
                  measured ? held++ : faded++;
                } else faded++;
              });
              if (faded || held) logL(tab, "Decay", faded + " unit adjustments faded \xB7 " + held + " held by fresh measurement");
              return nm;
            });
          }, style: { fontFamily: "Helvetica", fontSize: 9, padding: "5px 9px", cursor: "pointer", border: "none", background: "transparent", color: MUT }, title: "advancing the week decays intervention gains \u2014 85% retained with fresh measurement, 55% without", children: "next week \u25B8" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#333", padding: "4px 9px", borderTop: `1px solid ${RULE}` }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: AC, fontSize: 8.5 }, children: "RESULT \xB7 " }),
          opsMsg
        ] })
      ] }),
      ["acquire", "deliver", "retain", "operate", "plan", "metrics", "failure", "fivebasis"].includes(tab) && (() => {
        const tgt = [...centers].filter((c) => forecast(c).cls === "deteriorating").sort((a, b) => a.health - b.health)[0];
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${VIO}`, background: "#faf9fc", padding: "6px 10px", margin: "0 0 10px", display: "flex", gap: 12, alignItems: "center", flexWrap: "wrap" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.7, textTransform: "uppercase", color: VIO }, children: "Live system" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#444" }, children: [
            Object.keys(adj).length,
            " units adjusted \xB7 ",
            ledger.length,
            " actions on record \xB7 agents active: Measurement \u2192 ",
            staleN,
            " stale \xB7 Support \u2192 ",
            red.length,
            " red"
          ] }),
          tgt && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => {
            logL(tab, "Support", tgt.name + " queued for support plan from the " + (TABL[tab] || tab) + " reference");
            setOpsMsg(tgt.name + " queued \u2014 see RECORD");
          }, style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "3px 9px", cursor: "pointer", border: `1px solid ${AC}`, background: "#fff", color: AC }, children: [
            "Queue support: ",
            tgt.name,
            " \u25B8"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setTab("agenda"), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "3px 9px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: MUT }, children: "Open record \u25B8" })
        ] });
      })(),
      ["portfolio", "mastery", "growth", "calendar"].includes(tab) && (() => {
        const hub = [["portfolio", "1 \xB7 programs", "design & stage-gates"], ["mastery", "2 \xB7 belts", "progression engine"], ["calendar", "3 \xB7 calendar", "the operating rhythm"], ["growth", "4 \xB7 growth", "pricing & outcome"]];
        return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${AMB}`, background: "#fdfaf2", padding: "6px 10px", margin: "0 0 10px" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 6, alignItems: "center", flexWrap: "wrap" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.7, textTransform: "uppercase", color: AMB }, children: "Programs pipeline" }),
          hub.map(([t, l, d], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { display: "inline-flex", alignItems: "center" }, children: [
            i > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT, margin: "0 3px" }, children: "\u2192" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setTab(t), title: d, style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "3px 8px", cursor: "pointer", border: `1px solid ${tab === t ? AMB : RULE}`, background: tab === t ? "#fdf6e6" : "#fff", color: tab === t ? "#8a6d1a" : "#555" }, children: l })
          ] }, t)),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginLeft: "auto" }, children: [
            "one pipeline: design \u2192 interaction check \u2192 schedule \u2192 outcome \xB7 reads the same ",
            centers.length,
            "-unit network"
          ] })
        ] }) });
      })(),
      !["franchise", "acquire", "deliver", "retain", "operate", "plan", "failure", "agenda", "warning", "fivebasis", "signals"].includes(tab) && (() => {
        const qc = team;
        const qamp = qAmp(qc);
        const qmeas = !staleBase(qc) || opt.fresh[qc.name] !== void 0;
        const qdays = opt.fresh[qc.name] !== void 0 ? 7 : hash(qc.name) % 38;
        const qcoh = qCoherence(qdays);
        const qdom = qResolve(qamp);
        const samp = qStateAmp(states[stSel] || []);
        const nav = [["network", "map"], ["team", "center"], ["engagement", "engagement"], ["growth", "growth"], ["transfers", "transfers"], ["pain", "priorities"]];
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${VIO}`, background: "#faf9fc", padding: "7px 10px", margin: "0 0 12px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", flexWrap: "wrap", gap: 6 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: VIO }, children: [
              "Operational focus \xB7 ",
              qc.name,
              " ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT, fontWeight: 400, textTransform: "none", letterSpacing: 0 }, children: [
                "\xB7 ",
                stSel,
                " state ",
                QSTATES[qResolve(samp)]
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 3, flexWrap: "wrap" }, children: nav.map(([t, l]) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setTab(t), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "3px 7px", cursor: "pointer", border: `1px solid ${tab === t ? VIO : RULE}`, background: tab === t ? VIO : "#fff", color: tab === t ? "#fff" : "#555" }, children: l }, t)) })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 12, alignItems: "flex-end", marginTop: 6, flexWrap: "wrap" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1, minWidth: 200 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(QWave, { amp: qamp, coh: qcoh, measured: qmeas, dom: qdom, note: "health " + qc.health + " \xB7 " + tierOf(qc.health) }) }),
            (() => {
              const g = qGate(qc, qdays);
              return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { disabled: !g.allow && !qmeas, onClick: () => {
                if (!g.allow) {
                  return;
                }
                setOpt((o) => ({ ...o, fresh: { ...o.fresh, [qc.name]: o.week } }));
                logL(tab, "Measurement", qc.name + " verified \u2014 all four governors clear");
              }, title: g.allow ? "all four governors clear \u2014 measurement approved" : "held by: " + g.held.map((h) => h.label).join(", "), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "5px 9px", cursor: g.allow || qmeas ? "pointer" : "not-allowed", border: `1px solid ${g.allow ? VIO : AC}`, background: qmeas ? VIO : g.allow ? "#fff" : "#fbeaea", color: qmeas ? "#fff" : g.allow ? VIO : AC }, children: qmeas ? "\u25C6 measured \u2014 current" : g.allow ? "\u27F2 measure \u2014 verify" : "\u2298 held \u2014 " + g.held.length + " governor" + (g.held.length > 1 ? "s" : "") });
            })()
          ] }),
          (() => {
            const g = qGate(qc, qdays);
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 4, flexWrap: "wrap", marginTop: 5 }, children: [
              g.verdicts.map((v, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { title: v.detail, style: { fontFamily: "Helvetica", fontSize: 7.5, fontWeight: 700, letterSpacing: 0.3, padding: "2px 6px", border: `1px solid ${v.pass ? GRN : AC}`, background: v.pass ? "#eef8f0" : "#fbeaea", color: v.pass ? GRN : AC }, children: [
                v.pass ? "\u2713" : "\u2298",
                " ",
                v.label
              ] }, i)),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 7.5, color: MUT, padding: "2px 4px" }, children: g.allow ? "measurement approved" : "measurement held \u2014 fail-safe: missing data does not pass" })
            ] });
          })(),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 4 }, children: [
            "The map sets this focus \u2014 pick a state or center on ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "franchise" }),
            ", and every section reads the same verified unit. This rail carries ",
            qc.name,
            " across sections. ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Every measurement passes the four governors first." })
          ] }),
          (() => {
            const d = qDiag(qc);
            const warn = d.tau > 3;
            const critLow = d.crit < 2;
            const zoneC = d.zone.zone === "self-stabilizing" ? GRN : d.zone.zone === "transitional" ? AMB : AC;
            const cells = [
              ["Stability zone", d.zone.zone, zoneC, d.zone.note + " \xB7 occupancy " + pct(d.zone.occ) + "%"],
              ["Early-warning \u03C4", d.tau + "w", warn ? AC : GRN, warn ? "memory time above 3 weeks \u2014 elevated transition risk inside 30 days" : "memory time normal \u2014 unit re-equilibrates quickly"],
              ["Critical distance", "$" + d.crit + "k", critLow ? AC : GRN, critLow ? "margin within $2k of the transition boundary \u2014 contingency required before any pricing change" : "margin headroom above the transition boundary"],
              ["Program interaction", d.pair[0] + " \xD7 " + d.pair[1], d.pair[2] > 0 ? GRN : AC, (d.pair[2] > 0 ? "constructive " : "destructive ") + "coefficient " + d.pair[2] + " \u2014 " + (d.pair[2] > 0 ? "run together" : "sequence, do not stack")],
              ["Sustainability", (d.bal.net >= 0 ? "+" : "") + d.bal.net + "/wk", d.bal.net >= -2e-3 ? GRN : AMB, "engagement gain " + d.bal.gain + " vs overhead " + d.bal.cost + " \u2014 " + (d.bal.net >= -2e-3 ? "balanced; cadence can run indefinitely" : "cost exceeds gain; reduce cadence")]
            ];
            return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 6, flexWrap: "wrap", marginTop: 6 }, children: cells.map(([n, v, col, tip], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { title: tip, style: { flex: "1 1 120px", minWidth: 118, border: `1px solid ${RULE}`, background: "#fff", padding: "4px 7px" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 7.5, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT }, children: n }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700, color: col }, children: v })
            ] }, i)) });
          })()
        ] });
      })(),
      tab === "growth" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${VIO}`, background: "#faf9fc", padding: "7px 10px", marginBottom: 10 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: VIO, marginBottom: 4 }, children: [
            "Growth outcome \xB7 ",
            "pending until you commit"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(QWave, { amp: [0.3, 0.45, 0.25], coh: 0.34, measured: false, labels: ["upside", "base", "downside"], note: "P90 $19.4M \xB7 P50 $13.6M \xB7 P10 $8.1M royalty" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 3 }, children: [
            "The royalty line is a range, not a number. Committing an intervention locks it to a figure \u2014 the four governors below hold the floor so the commitment can't land below breakeven. ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setTab("dynamics"), style: { fontFamily: "Helvetica", fontSize: 8, color: AC, cursor: "pointer", textDecoration: "underline", background: "none", border: "none", padding: 0 }, children: "Try this in the scenario sandbox \u2192" })
          ] })
        ] }),
        [["Baseline", 4.9, MUT], ["Premium tier 70%", 6.3, GRN], ["Retention +3", 7.1, GRN], [centers.length + " units", 8.6, VIO], ["400 units", 13.6, VIO]].map(([n, v, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, marginBottom: 3 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9.5, width: 90 }, children: n }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1, height: 14, background: "#f0ede6" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 14, width: v / 13.6 * 100 + "%", background: c } }) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { fontFamily: "Helvetica", fontSize: 10 }, children: [
            "$",
            v,
            "M/yr"
          ] })
        ] }, i)),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, margin: "2px 0 8px" }, children: "royalty line \xB7 illustrative until fit \xB7 P10 $8.1 / P50 $13.6 / P90 $19.4" }),
        [["gate", "cannibalization \u22650", "block signature"], ["cap", "candidate supply", "throttle wave"], ["wave", "ramp < prior", "pause scale"], ["score", "measure lag", "freeze claims"], ["fcst", "J-curve dip", "fund runway"]].map(([ag, g, act], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Chain, { items: [{ ag: A(ag), vb: g, state: "run", c: AMB, title: "governor " + (i + 1) }, { vb: act, state: "pend", c: AC }] }, i)),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 6, marginTop: 14 }, children: "The Four Growth Programs" }),
        ACTION_PLANS.map((p, i) => {
          const isUV = p.n === "Unit-Value Program";
          const range = isUV ? financialRange(1.36, 0.18) : null;
          const delta = franchiseeDeltaOf(p);
          const isExp = p.n === "Selective Expansion";
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 10, padding: "6px 0", borderTop: i ? "1px solid #eee" : "none", fontFamily: "Helvetica" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 14, fontWeight: 700, color: AC }, children: p.r }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: 1 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontSize: 14 }, children: p.n }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 12.5, color: "#444", lineHeight: 1.5 }, children: p.d }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, color: MUT }, children: [
                "FLOOR: ",
                p.floor
              ] }),
              range && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT, marginTop: 2 }, children: [
                "range: $",
                range.lo,
                "M\u2013$",
                range.hi,
                "M/yr at 400 units (\xB1",
                Math.round(range.uncertaintyPct * 100),
                "%, point estimate $",
                range.point,
                "M)"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: delta.unitMarginDelta_k > 0 ? GRN : delta.unitMarginDelta_k < 0 ? AC : MUT, marginTop: 2 }, children: [
                "franchisee unit: ",
                delta.unitMarginDelta_k > 0 ? "+" : "",
                delta.unitMarginDelta_k !== 0 ? "$" + delta.unitMarginDelta_k + "k/yr margin" : "no unit-level change",
                " \xB7 payback ",
                delta.payback_yr,
                " \u2014 ",
                delta.note
              ] }),
              isExp && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT, marginTop: 2 }, children: [
                "J-curve: a new unit does not open at target economics \u2014 ",
                rampMilestones().map((r) => r.m + "mo " + r.pct + "%").join(" \xB7 "),
                " of target"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 11, color: "#444" }, children: p.risk })
          ] }, i);
        }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 6, marginTop: 14 }, children: "Unit-Value Chain" }),
        CENTER_CHAIN.map((c, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "3px 0", borderTop: i ? "1px solid #eee" : "none", display: "flex", gap: 6, fontFamily: "Helvetica" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, color: i === 0 ? AC : MUT, minWidth: 14 }, children: [
            i + 1,
            "."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 13, color: "#444" }, children: c })
        ] }, i))
      ] }),
      tab === "calendar" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        CAL.map(([stream, evs]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 16 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: AC, marginBottom: 14 }, children: stream }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "relative", height: 30, borderTop: `1px solid ${INK}`, margin: "0 30px" }, children: evs.map(([d, l, hot], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Rail, { d, label: l, hot: !!hot }, i)) })
        ] }, stream)),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: "D0=Jul 2 \u2192 D90 \xB7 red = act now \xB7 FLL: 2026-27 = FINAL FIRST\xB7LEGO season" })
      ] }),
      tab === "table" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommandTableTab, { centers, states, opt, setOpt, railData, leads: LEADS, jumpTo }) }),
      tab === "network" && (() => {
        const netOverride = opt.quantum && opt.quantum.overrides && opt.quantum.overrides.network;
        const netGlobalPosture = opt.quantum && opt.quantum.approved || "realistic";
        const netOverrideActive = !!(netOverride && netOverride !== netGlobalPosture);
        const netOv = netOverrideActive ? computeOverrideView(rawCenters, adj, netOverride, opt.week, LEADS) : null;
        const netCenters = netOv ? netOv.centers : centers;
        const netStates = netOv ? netOv.states : states;
        const taus = netCenters.map((c) => qTau(c));
        const tBins = [taus.filter((t) => t <= 2).length, taus.filter((t) => t > 2 && t <= 3).length, taus.filter((t) => t > 3).length];
        const netStSel = netStates[stSel] ? stSel : Object.keys(netStates)[0];
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          netOverrideActive && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${VIO}`, borderLeft: `3px solid ${VIO}`, background: "#faf9ff", padding: "6px 10px", marginBottom: 8, fontFamily: "Helvetica", fontSize: 9.5, color: "#3d3470" }, children: [
            "This tab is locally overridden to ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: netOverride }),
            " \u2014 network-wide posture is ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: netGlobalPosture }),
            ". Recovery-time distribution and per-unit health below reflect the override. Set from Quantum PM."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: "3px solid #7a6bd8", background: "#faf9fc", padding: "7px 10px", marginBottom: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, color: "#7a6bd8", marginBottom: 4 }, children: "Each unit is tracked as a probability-weighted status (thriving / watch / at-risk) with a recovery-time distribution" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 14, flexWrap: "wrap", alignItems: "baseline" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: "#7a6bd8" }, children: "Recovery-time distribution" }),
              [["rec \u2264 2w fast", tBins[0], GRN], ["2\u20133w slowing", tBins[1], "#b8860b"], ["rec > 3w critical", tBins[2], AC]].map(([l, v, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9.5 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: c, fontSize: 12 }, children: v }),
                " ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: l })
              ] }, i)),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: [
                "\xB7 recovery time ",
                ">",
                " 3w forecasts floor contact \u2248 30d \u2014 support plan required"
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 3, marginBottom: 8 }, children: Object.keys(netStates).sort((a, b) => netStates[b].length - netStates[a].length).map((st) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Node, { vb: st, tg: netStates[st].length + (netStates[st].some((c) => c.verified) ? "" : " ~"), state: st === netStSel ? "done" : "pend", c: st === netStSel ? AC : RULE, onClick: () => setSt(st) }, st)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 3 }, children: netStates[netStSel].sort((a, b) => b.health - a.health).map((c) => {
            const [ar] = arrow(forecast(c).cls);
            const ct = qTau(c);
            return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Node, { vb: c.name, tg: c.health + " " + tierOf(c.health) + " " + ar + " rec " + ct, state: c.name === sel ? "done" : c.verified ? "pend" : "hold", c: c.name === sel ? AC : ct > 3 ? AC : c.verified ? RULE : MUT, onClick: () => {
              setSel(c.name);
              setTab("team");
            }, title: (c.verified ? "verified" : "modeled") + " \xB7 " + ct + "w recovery \xB7 click to open" }, c.name);
          }) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 5 }, children: "solid = verified \xB7 gray = modeled \xB7 red chip = recovery > 3w (critical slowing down) \xB7 rail: green done / red hold \xB7 click = open" })
        ] });
      })(),
      tab === "team" && (() => {
        const tf = forecast(team);
        const [ar, ac2] = arrow(tf.cls);
        const tmTau = qTau(team);
        const tauStr = tmTau <= 2 ? tmTau + "w fast" : tmTau <= 4 ? tmTau + "w slowing" : tmTau + "w critical";
        const inc = [["EBITDA", fmt(team.eb) + "k", ">6k", team.eb > 6, "Performance rebate"], ["Retention", pct(team.ret) + "%", "\u226588%", team.ret > 0.88, "Retention Gold"], ["Chemistry", pct(team.chem) + "%", "\u226565%", team.chem > 0.65, "Chemistry credit"], ["Report", staleBase(team) ? "stale" : "fresh", "\u226421d", !staleBase(team), "Cadence rebate"], ["Momentum", team.momentum, "+3", team.momentum[0] === "+" && parseInt(team.momentum.slice(1)) >= 3, "Momentum spotlight"], ["Recovery time", tauStr, "fast", tf.tau <= 2, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 8, color: MUT }, children: "Slowing recovery at 3w+" })], ["Health Index", team.health, "\u226585", team.health >= 85, "Donor credit /adoption"]];
        const e = engageOf(team);
        const measured = !staleBase(team) || opt.fresh[team.name] !== void 0;
        const teamDays = opt.fresh[team.name] !== void 0 ? (opt.week - opt.fresh[team.name]) * 7 : hash(team.name) % 38 + opt.week * 2;
        const teamConf = Math.round(qCoherence(teamDays) * 100);
        const ATTR = [["Enrollment", Math.min(99, Math.round(30 + team.conv * 95))], ["Retention", Math.round(team.ret * 100)], ["Instruction", Math.min(99, Math.round(38 + e.pre * 55))], ["Community", Math.min(99, Math.round(28 + e.refer * 70 + e.buzz * 25))], ["Staff chemistry", Math.round(team.chem * 100)], ["Measurement", teamConf], ["Facilities", 45 + hash(team.name + "fac") % 40]];
        const sorted = [...ATTR].sort((a, b) => b[1] - a[1]);
        const strengths = sorted.slice(0, 2), weaks = sorted.slice(-2).reverse();
        const cx = 70, cy = 70, R = 52, Nn = ATTR.length;
        const ptf = (i, f) => {
          const a = -Math.PI / 2 + i * 2 * Math.PI / Nn;
          return [cx + Math.cos(a) * R * f, cy + Math.sin(a) * R * f];
        };
        const poly = ATTR.map((at, i) => ptf(i, at[1] / 99).map((v) => v.toFixed(1)).join(",")).join(" ");
        const grid = [0.33, 0.66, 1].map((g) => ATTR.map((_, i) => ptf(i, g).map((v) => v.toFixed(1)).join(",")).join(" "));
        const verdict = weaks[0][1] < 55 ? "Needs intervention \u2014 " + weaks[0][0].toLowerCase() + " is the drag" : team.health >= 85 ? "Donor-grade \u2014 a pattern source for the network" : team.health >= 75 ? "Solid unit \u2014 one gap from the next tier" : "Developing \u2014 the fix is known and named";
        const DEPT = [["Acquisition", [ATTR[0][1], ATTR[3][1]]], ["Instruction", [ATTR[2][1], ATTR[5][1]]], ["Retention", [ATTR[1][1], ATTR[4][1]]], ["Operations", [team.health, ATTR[6][1]]]].map(([n, vs]) => {
          const g = Math.round(vs.reduce((a, b) => a + b, 0) / vs.length);
          return [n, g, g >= 85 ? "A" : g >= 78 ? "B" : g >= 68 ? "C" : g >= 58 ? "D" : "F"];
        });
        const FIX = { Enrollment: "trial-to-enrollment coaching + day-7 follow-up", Retention: "engagement-based re-enrollment + at-risk outreach", Instruction: "pre/post-session routine + sensei certification", Community: "school partnership + demo day on the calendar", ["Staff chemistry"]: "sensei 1:1 + CIT draft from Red/Black belts", Measurement: "measure now \u2014 verify the estimate first", Facilities: "floor refresh in the facility budget quarter" };
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", flexWrap: "wrap", gap: 10, alignItems: "baseline", marginBottom: 6 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("h2", { style: { fontSize: 18, margin: 0 }, children: team.name }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 10 }, children: [
              team.health,
              " ",
              tierOf(team.health),
              " \xB7 ",
              team.students,
              " students \xB7 ",
              team.momentum + " trend",
              " \xB7 ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: ac2 }, children: [
                ar,
                " ",
                fmt(tf.proj[2]),
                "k @90d"
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", marginBottom: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { background: "linear-gradient(135deg,#161616,#232323)", padding: "12px 14px", display: "flex", gap: 16, alignItems: "center", flexWrap: "wrap" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { textAlign: "center", minWidth: 70 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 46, fontWeight: 800, color: "#fff", lineHeight: 0.9 }, children: team.health }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 1.5, color: team.health >= 88 ? "#6fc38a" : team.health >= 80 ? "#d9a62e" : team.health >= 68 ? "#bbb" : "#d96a6a" }, children: tierOf(team.health) })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 140 140", style: { width: 118, height: 118, flexShrink: 0 }, children: [
                grid.map((g, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("polygon", { points: g, fill: "none", stroke: "#3a3a3a", strokeWidth: "0.7" }, i)),
                ATTR.map((_, i) => {
                  const [x, y] = ptf(i, 1);
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: cx, y1: cy, x2: x, y2: y, stroke: "#333", strokeWidth: "0.5" }, i);
                }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("polygon", { points: poly, fill: "rgba(139,0,0,0.35)", stroke: "#c0504d", strokeWidth: "1.6" }),
                ATTR.map((at, i) => {
                  const [x, y] = ptf(i, 1.16);
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x, y: y + 2, textAnchor: "middle", style: { font: "bold 6.5px Helvetica", fill: "#9a9a9a" }, children: at[0].split(" ")[0].slice(0, 4).toUpperCase() }, i);
                })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: 1, minWidth: 180 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 1.5, color: "#8a8a8a", marginBottom: 2 }, children: "SCOUTING REPORT" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 15, color: "#fff", lineHeight: 1.25, marginBottom: 6 }, children: verdict }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 14, flexWrap: "wrap" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: strengths.map(([n, v], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#a9d6b4", marginBottom: 1 }, children: [
                    "\u25B2 ",
                    n,
                    " ",
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: v })
                  ] }, i)) }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: weaks.map(([n, v], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#e0a0a0", marginBottom: 1 }, children: [
                    "\u25BC ",
                    n,
                    " ",
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: v })
                  ] }, i)) })
                ] })
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { padding: "10px 12px" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 14, flexWrap: "wrap" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 240px" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AC, marginBottom: 3 }, children: "The two fixes that move this unit" }),
                weaks.map(([n, v], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${AC}`, padding: "5px 8px", marginBottom: 4 }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { fontFamily: "Helvetica", fontSize: 9.5 }, children: [
                    n,
                    " ",
                    v
                  ] }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: [
                    "\u2192 ",
                    FIX[n]
                  ] })
                ] }, i))
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 160px" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AC, marginBottom: 4 }, children: "Department grades" }),
                DEPT.map(([n, g, L], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, marginBottom: 4, fontFamily: "Helvetica", fontSize: 9.5 }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 78 }, children: n }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1, height: 6, background: "#eee" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 6, width: g + "%", background: g >= 78 ? GRN : g >= 68 ? AMB : AC } }) }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { width: 16, textAlign: "center", color: g >= 78 ? GRN : g >= 68 ? AMB : AC }, children: L })
                ] }, i))
              ] })
            ] }) })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", { viewBox: "0 0 700 150", style: { width: "100%", height: "auto", margin: "4px 0" }, children: Object.entries(team.wires).map(([w, gs], wi) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "4", y: 26 + wi * 34, style: { font: "9px Helvetica", fill: MUT }, children: w }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: "52", y1: 22 + wi * 34, x2: "690", y2: 22 + wi * 34, stroke: INK, strokeWidth: "1" }),
            gs.map((g, gi) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", { x: 90 + gi * 200, y: 10 + wi * 34, width: 110, height: 24, fill: g.state === "super" ? "#fff" : "#f4f2ec", stroke: g.state === "super" ? MUT : INK, strokeDasharray: g.state === "super" ? "4 3" : "0" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: 145 + gi * 200, y: 21 + wi * 34, textAnchor: "middle", style: { font: "8px Helvetica", fill: INK }, children: g.n }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: 145 + gi * 200, y: 31 + wi * 34, textAnchor: "middle", style: { font: "bold 9px Helvetica", fill: g.state === "super" ? MUT : g.v >= 60 ? GRN : AC }, children: g.state === "super" ? g.days + "d ?" : g.v })
            ] }, gi))
          ] }, w)) }),
          (() => {
            const amp = qAmp(team);
            const dom = qResolve(amp);
            const meas = !staleBase(team) || opt.fresh[team.name] !== void 0;
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${VIO}`, background: "#fff", padding: "8px 10px", marginTop: 8 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: VIO, marginBottom: 5 }, children: [
                "Unit status \u2014 ",
                meas ? "verified \xB7 measured this cycle" : "estimated \xB7 awaiting P&L measurement"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 8, alignItems: "flex-end", height: 52, marginBottom: 4 }, children: amp.map((a, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1, display: "flex", flexDirection: "column", justifyContent: "flex-end", height: "100%" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { background: HCOL[i], opacity: meas ? i === dom ? 1 : 0.22 : 0.7, height: Math.max(3, a * 100) + "%", borderRadius: 2, transition: "opacity .3s" } }) }, i)) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 8, marginBottom: 4 }, children: QSTATES.map((n, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: 1, textAlign: "center", fontFamily: "Helvetica", fontSize: 8.5 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: HCOL[i] }, children: [
                  pct(amp[i]),
                  "%"
                ] }),
                " ",
                n
              ] }, i)) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, lineHeight: 1.5 }, children: meas ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                "Measured \u2014 the P&L review verified this unit as ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: HCOL[dom] }, children: QSTATES[dom] }),
                " on the books. Confidence high."
              ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                "Unmeasured \u2014 the unit carries a probability split across all three outcomes, and confidence in the estimate decays the longer it goes unmeasured. The P&L review is the checkpoint that verifies it; run ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Measure" }),
                " from the operations bar now."
              ] }) })
            ] });
          })(),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: GRN, margin: "8px 0 4px" }, children: "Incentive gates" }),
          inc.map(([l, v, thr, met, pay], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Gate, { inLabel: l, inVal: v, thr, met, payout: pay, title: met ? "earned" : "gap to threshold" }, i)),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OpHealthIndex, { center: team, staleBase: staleBase(team), health: team.health, tau: Math.round(qTau(team)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${AMB}`, padding: "7px 10px", marginTop: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AMB, marginBottom: 4 }, children: "Quarter goal \u2014 negotiated, not assigned" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 4, flexWrap: "wrap" }, children: [["Stretch", "margin +3.0k", "full support budget"], ["Standard", "margin +1.5k", "standard support"], ["Floor", "hold margin", "development review"]].map(([n, t, r], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setGoals((g) => ({ ...g, [team.name]: i })), style: { fontFamily: "Helvetica", fontSize: 9, padding: "5px 9px", cursor: "pointer", border: `1px solid ${goals[team.name] === i ? AMB : RULE}`, background: goals[team.name] === i ? "#fdf6e6" : "#fff", textAlign: "left" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: n }),
              " \xB7 ",
              t,
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("br", {}),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT, fontSize: 8 }, children: [
                "unlocks ",
                r
              ] })
            ] }, i)) })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 8 }, children: "payouts illustrative \xB7 computed from measured state" })
        ] });
      })(),
      tab === "mastery" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap", alignItems: "center", marginBottom: 8 }, children: PATH.map(([id, n], i) => {
          const [, st2, d2] = COHORT[id];
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { display: "inline-flex", alignItems: "center" }, children: [
            i > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Arrow, {}),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Node, { vb: n, tg: COHORT[id][0] + " \xB7 " + st2 + "\u26A0", state: id === blocker[0] ? "hold" : d2 > 21 ? "hold" : "run", c: id === blocker[0] ? AC : id === beltSel ? VIO : RULE, onClick: () => setBelt(id), title: st2 + " stuck \xB7 assessed " + d2 + "d ago" })
          ] }, id);
        }) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Chain, { items: [{ ag: A("score"), vb: "blocker", tg: blocker[1] + " belt", state: "hold", c: AC, title: "stuck \xD7 downstream max" }, { ag: "sensei", vb: "rotate", tg: "minutes here first", state: "pend", c: GRN }] }),
        (() => {
          const [, st2, d2] = COHORT[beltSel];
          return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Chain, { items: [{ ag: A("score"), vb: V("measure"), tg: PATH.find((b) => b[0] === beltSel)[1] + " " + d2 + "d", state: d2 > 21 ? "hold" : "done", c: d2 > 21 ? AC : GRN }, { ag: "sensei", vb: "help @2 attempts", tg: st2 + " stuck", state: st2 > 30 ? "hold" : "run", c: st2 > 30 ? AC : AMB }, { ag: A("view"), vb: V("ship"), tg: "progress report 72h", state: d2 <= 3 ? "done" : "pend", c: d2 <= 3 ? GRN : AMB }, { ag: "ceremony", vb: "renew", tg: "at belt-up", state: "pend", c: GRN }] }) });
        })()
      ] }),
      tab === "optimizer" && (() => {
        const staleOf = (c) => opt.fresh[c.name] !== void 0 ? (opt.week - opt.fresh[c.name]) * 7 : hash(c.name) % 38 + opt.week * 2;
        const freshN = centers.filter((c) => staleOf(c) <= 21).length;
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap", marginBottom: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setOpt((o) => ({ ...o, run: !o.run })), style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, padding: "6px 14px", cursor: "pointer", background: opt.run ? "#fff" : INK, color: opt.run ? INK : "#fff", border: `1px solid ${INK}` }, children: opt.run ? "Pause" : "Run live" }),
            [["wk", opt.week], ["measured", freshN + "/" + centers.length], ["holds", opt.gates]].map(([l, v], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Node, { vb: l, tg: String(v), state: "done", c: RULE }, i)),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Node, { vb: "standalone", tg: "zero external dependencies", state: "done", c: GRN })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 2, marginBottom: 6 }, children: centers.map((c) => {
            const st = staleOf(c) > 21;
            const col = c.health >= 80 ? GRN : c.health >= 68 ? AMB : AC;
            const fcol = arrow(forecast(c).cls)[1];
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => {
              setSel(c.name);
              setTab("team");
            }, title: c.name, style: { width: 12, height: 15, cursor: "pointer" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 11, background: st ? "#fff" : col, border: `1.5px ${st ? "dashed" : "solid"} ${col}`, opacity: st ? 0.5 : 1 } }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 3, background: fcol, marginTop: 1 } })
            ] }, c.name);
          }) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 14, flexWrap: "wrap" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 260px" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 4 }, children: "Effectiveness scores \u2014 rise only with measured outcome" }),
              Object.entries(opt.probs).map(([n, p]) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Meter, { n, p, c: n === "pricing" ? AC : GRN }, n)),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 7.5, color: MUT, marginTop: 2 }, children: "measurement credit = staleness-days actually cleared \xB7 transfer credit = conversion gap closed \xB7 review credit = retention spread paired \xB7 wave sized by rising-unit count \xB7 pricing gate holds only while units sit below breakeven" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 300px" }, children: [
              opt.log.map((l, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Chain, { items: [{ ag: A(l[0] === "score" ? "score" : l[0] === "transfer" ? "transfer" : l[0] === "peer" ? "peer" : l[0] === "wave" ? "wave" : "gate"), vb: V(l[1] === "measure" ? "measure" : l[1] === "transfer" ? "transfer" : l[1] === "review" ? "review" : l[1] === "hold" ? "hold" : "schedule"), tg: l[2], state: l[3] ? "done" : "hold", c: l[3] ? GRN : AC }] }, i)),
              !opt.log.length && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT }, children: "press Run live \u2014 one week \u2248 0.9s" })
            ] })
          ] })
        ] });
      })(),
      tab === "transfers" && (() => {
        const meas = centers.filter((c) => !staleBase(c));
        const donors = meas.filter((c) => c.health >= 83).slice(0, 6);
        const recips = [...meas].sort((a, b) => a.conv - b.conv).slice(0, 6);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          donors.map((d, i) => {
            const r = recips[i % recips.length];
            if (!r || d === r) return null;
            const pre = forecast(r).cls === "deteriorating";
            const gap = d.conv - r.conv;
            const promoted = gap > 0.15;
            const mult = (1 + gap).toFixed(2);
            return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Chain, { items: [{ ag: A("transfer"), vb: V("transfer"), tg: d.name, state: "done", c: GRN, onClick: () => {
              setSel(d.name);
              setTab("team");
            }, title: "donor \xB7 click to open" }, { vb: "best practice", tg: promoted ? "promoted " + mult + "\xD7" : "trial \xB7 gap " + Math.round(gap * 100) + "pt", state: promoted ? "done" : "run", c: promoted ? GRN : AMB, title: "promotion requires a \u226515pt measured conversion gap" }, { vb: r.name, tg: pre ? "pre-emptive" : "30d re-measure", state: pre ? "hold" : "pend", c: pre ? AC : RULE, onClick: () => {
              setSel(r.name);
              setTab("team");
            } }] }, i);
          }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: "donor \u2192 best practice (promoted at \u226515pt measured conversion gap; multiplier = 1 + gap) \u2192 recipient \xB7 red = pre-emptive" })
        ] });
      })(),
      tab === "portfolio" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        PROJECTS.map((p) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", flexWrap: "wrap", alignItems: "center", marginBottom: 5 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9.5, width: 120, fontWeight: 700 }, children: p.n }),
          p.stages.map(([sn, st, d], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { display: "inline-flex", alignItems: "center" }, children: [
            i > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Arrow, {}),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Node, { vb: sn, tg: st === 2 ? d + "d ?" : "", state: st === 2 ? "hold" : st === 1 ? "run" : i < 2 ? "done" : "pend", c: st === 2 ? AC : st === 1 ? AMB : RULE, title: st === 2 ? "unmeasured " + d + "d \u2014 measure first" : "" })
          ] }, i))
        ] }, p.id)),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Chain, { items: [{ vb: "Premium tier", state: "run", c: GRN }, { vb: "\xD7 progress report", tg: "reinforcing", state: "run", c: GRN }] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Chain, { items: [{ vb: "schools", state: "run", c: AC }, { vb: "\xD7 events", tg: "competing \u2014 sequence", state: "hold", c: AC }] })
      ] }),
      tab === "pain" && (() => {
        const painOf = (c) => +(0.68 * (c.eb < 0 ? 1 : c.eb < 3 ? 0.6 : 0.15) + 0.62 * (staleBase(c) ? 0.9 : 0.2) + 0.54 * (1 - c.ret)).toFixed(2);
        const pq = [...centers].map((c) => ({ c, p: painOf(c) })).sort((a, b) => b.p - a.p).slice(0, 6);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 700 210", style: { width: "100%", height: "auto" }, children: [
            PEDGES.map(([m, g, w], i) => {
              const mi = MECHS.findIndex((x) => x.id === m), gi = GRIEV.findIndex((x) => x.id === g);
              return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: 230, y1: 22 + mi * 24, x2: 500, y2: 26 + gi * 32, stroke: VIO, strokeWidth: 0.5 + w * 2, opacity: 0.3 + w * 0.5 }, i);
            }),
            MECHS.map((m, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", { x: 20, y: 12 + i * 24, width: 210, height: 18, fill: "#fff", stroke: INK }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("text", { x: 125, y: 24 + i * 24, textAnchor: "middle", style: { font: "8.5px Helvetica" }, children: [
                m.n,
                " \xB7 ",
                LEV.find((x) => x.id === m.id).lev
              ] })
            ] }, m.id)),
            GRIEV.map((g, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", { x: 500, y: 12 + i * 32, width: 150, height: 24, fill: g.sev >= 0.6 ? "#fbf3f3" : "#fdf6e6", stroke: g.sev >= 0.6 ? AC : AMB }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("text", { x: 575, y: 23 + i * 32, textAnchor: "middle", style: { font: "8.5px Helvetica" }, children: [
                g.n,
                " ",
                g.sat,
                "/5"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("text", { x: 575, y: 32 + i * 32, textAnchor: "middle", style: { font: "7.5px Helvetica", fill: MUT }, children: [
                "sev ",
                g.sev
              ] })
            ] }, g.id))
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Chain, { items: [{ ag: "#1", vb: LEV[0].n, tg: "lev " + LEV[0].lev, state: "run", c: AC, onClick: () => setTab(LEV[0].tab) }, { ag: "#2", vb: LEV[1].n, tg: LEV[1].lev, state: "pend", c: AMB, onClick: () => setTab(LEV[1].tab) }, { ag: "#3", vb: LEV[2].n, tg: LEV[2].lev, state: "pend", c: AMB, onClick: () => setTab(LEV[2].tab) }] }),
          pq.map(({ c, p }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Chain, { items: [{ vb: c.name, tg: "pain " + p, state: "hold", c: p >= 1 ? AC : AMB, onClick: () => {
            setSel(c.name);
            setTab("team");
          } }, { ag: A("score"), vb: staleBase(c) ? V("measure") : "support", tg: "first move", state: "pend", c: GRN }] }, c.name))
        ] });
      })(),
      tab === "quantum" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(QuantumPMView, { opt, approveScenario, overrideTabScenario, logL, centers, states, railData, ledger, jumpTo: (t) => setTab(t), leads: LEADS, abTests, executionTracking, alerts }) }),
      tab === "integrated" && (() => {
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 12 }, children: "Mission Control \u2014 Unified Governance Interface" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 11, color: "#555", lineHeight: 1.6, marginBottom: 14 }, children: "Five integrated views across a single spatial context: network clusters, operational workflows, scenario forecasting, live execution tracking, and deep-dive center details. All views share selection state \u2014 click a center or step, and all other views update instantly. No tab switching; pure cross-linked navigation." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IntegratedViewSelector, {}),
          integratedView === "workflow" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: INK, marginBottom: 8 }, children: "69-Step Operational Workflow" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666", marginBottom: 12 }, children: "Four phases (ACQUIRE, DELIVER, RETAIN, OPERATE) orchestrate the entire franchise operation. Click any step to see its description, role, gate conditions, and live alert count. Gates (highlighted with \u{1F512}) are human approval points where franchisee consent is required before proceeding." }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkflowViewComponent, {}),
            workflowStepSel && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 16, border: `1px solid ${VIO}`, borderLeft: `3px solid ${VIO}`, background: "#faf9ff", padding: "12px 14px" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, marginBottom: 8, color: VIO }, children: [
                "Step ",
                workflowStepSel,
                " Details"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 10, color: "#333", lineHeight: 1.6 }, children: (() => {
                const step = W.find((s) => s.n === workflowStepSel);
                if (!step) return null;
                const affectedRecs = (railData.recommendations || []).filter((r) => r.targetIds && r.targetIds.some((t) => step.s.toLowerCase().includes(t.toLowerCase() || t.toLowerCase().includes(step.s.toLowerCase()))));
                return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Phase:" }),
                    " ",
                    PHASE_META[step.p].name,
                    " \xB7 ",
                    step.p.toUpperCase()
                  ] }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Owner:" }),
                    " ",
                    step.o
                  ] }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "System:" }),
                    " ",
                    step.sys
                  ] }),
                  step.gate && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { color: AC }, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Gate Type:" }),
                    " ",
                    step.gr
                  ] }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 8, paddingTop: 8, borderTop: `1px solid ${RULE}` }, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Description:" }),
                    " ",
                    step.d
                  ] }),
                  step.t && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 6 }, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Estimated Time:" }),
                    " ",
                    step.t
                  ] }),
                  affectedRecs.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 8, paddingTop: 8, borderTop: `1px solid ${RULE}` }, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Active Recommendations:" }),
                    " ",
                    affectedRecs.length,
                    " proposal",
                    affectedRecs.length !== 1 ? "s" : "",
                    " at this step"
                  ] })
                ] });
              })() })
            ] })
          ] }),
          integratedView === "network" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: INK, marginBottom: 8 }, children: "Network Overview \u2014 All 348 Centers" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666", marginBottom: 12 }, children: "Territory map showing network health, center condition (thriving/watch/at-risk), active proposals, and conflict zones. Click a state to drill into that region's center details. Hover over a center dot to see its current posture impact." }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, padding: 14, background: "#f9f8f6", textAlign: "center", minHeight: 200, display: "flex", alignItems: "center", justifyContent: "center" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: '3D Network Map \u2014 Click "table" tab for full interactive map with zoom, scenario impact, and center detail drill-down.' }) })
          ] }),
          integratedView === "scenarios" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: INK, marginBottom: 8 }, children: "Scenario Planning \u2014 Optimistic / Realistic / Pessimistic" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666", marginBottom: 12 }, children: "Approve a network posture and watch the entire system recompute: center health ripples, resource needs shift, execution risks surface. Compare scenarios side-by-side to forecast network state under different strategic assumptions. All three scenarios always available; only one can be live at a time." }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, padding: 14, background: "#f9f8f6", textAlign: "center", minHeight: 200, display: "flex", alignItems: "center", justifyContent: "center" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: 'Scenario Simulator \u2014 Click "quantum" tab to approve postures and see live network recomputation with A/B test impact.' }) })
          ] }),
          integratedView === "execution" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: INK, marginBottom: 8 }, children: "Execution Tracking \u2014 Live A/B Tests & Feedback Loop" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666", marginBottom: 12 }, children: "Every approved proposal becomes a live test. Watch p-values and effect sizes update as data arrives. When tests resolve, weights on similar recommendations adjust automatically (Bayesian learning). The feedback loop closes: execution results inform strategy." }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, padding: 14, background: "#f9f8f6", textAlign: "center", minHeight: 200, display: "flex", alignItems: "center", justifyContent: "center" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: 'Execution Dashboard \u2014 Click "quantum" tab to see live A/B tests, p-values, and Bayesian weight adjustments.' }) })
          ] }),
          integratedView === "deepdive" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: INK, marginBottom: 8 }, children: "Deep-Dive Analytics \u2014 Single Center or Workflow Step" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9, color: "#666", marginBottom: 12 }, children: "View all 69 workflow steps for a single center (showing actual progress, next actions, at-risk stages). Or zoom into a single workflow step to see all 348 centers currently in that phase, grouped by health tier and alert status." }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, padding: 14, background: "#f9f8f6", textAlign: "center", minHeight: 200, display: "flex", alignItems: "center", justifyContent: "center" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: 'Deep-Dive View \u2014 Select a center or step above to begin drilling. Click "team" tab for individual center detail.' }) })
          ] })
        ] });
      })(),
      tab === "exec" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const execOverride = opt.quantum && opt.quantum.overrides && opt.quantum.overrides.exec;
        const execGlobalPosture = opt.quantum && opt.quantum.approved || "realistic";
        const execOverrideActive = !!(execOverride && execOverride !== execGlobalPosture);
        const execOv = execOverrideActive ? computeOverrideView(rawCenters, adj, execOverride, opt.week, LEADS) : null;
        const execCenters = execOv ? execOv.centers : centers;
        const execRailData = execOv ? execOv.railData : railData;
        const execRed = execOverrideActive ? execCenters.filter((c) => c.eb < 0) : red;
        const avgHealth = Math.round(execCenters.reduce((a, c) => a + c.health, 0) / execCenters.length);
        const allowedN = (execRailData.recommendations || []).filter((r) => r.governance.allowed).length;
        const heldN = (execRailData.recommendations || []).length - allowedN;
        const so = (l) => leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0;
        const signedN = LEADS.filter((l) => so(l) === 5).length;
        const verifiedN = execCenters.filter((c) => c.verified).length;
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          execOverrideActive && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${VIO}`, borderLeft: `3px solid ${VIO}`, background: "#faf9ff", padding: "6px 10px", marginBottom: 10, fontFamily: "Helvetica", fontSize: 9.5, color: "#3d3470" }, children: [
            "This tab is locally overridden to ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: execOverride }),
            " \u2014 network-wide posture is ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: execGlobalPosture }),
            ". All five figures below reflect the override. Set from Quantum PM."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `4px solid ${INK}`, background: "#fff", padding: "16px 18px", marginBottom: 14 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 8 }, children: "Proposed Policy Framework \u2014 Candidate Submission \xB7 Pratik Singh" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 14.5, color: "#222", lineHeight: 1.6 }, children: [
              "A single governed system operates ",
              execCenters.length,
              " modeled centers (",
              verifiedN,
              " verified, ",
              244,
              " authoritative per FDD Item 20) across four evaluation agents. Unit-level deterioration is caught early, support is routed under a named-owner governance boundary, and growth proceeds on a measured cadence rather than an unconstrained one. The business case: improve unit economics across the existing 244 before adding the next 50 \u2014 the Unit-Value Program alone models ~$1.36M/yr in additional royalty at 400 units, at the lowest risk of the four available levers."
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap", border: `1px solid ${RULE}`, background: "#fff", marginBottom: 14 }, children: [
            [avgHealth, "network health, composite", avgHealth >= 75 ? GRN : avgHealth >= 60 ? AMB : AC],
            [execRed.length, "units running below breakeven", execRed.length === 0 ? GRN : execRed.length <= 10 ? AMB : AC],
            [allowedN + " / " + heldN, "proposals allowed / held this cycle", AMB],
            [signedN, "candidates signed in the growth pipeline", GRN],
            [(execRailData.conflicts || []).length, "open cross-agent conflicts", (execRailData.conflicts || []).length === 0 ? GRN : AC]
          ].map(([v, l, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 150px", padding: "12px 14px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 26, fontWeight: 800, color: c }, children: v }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 3, lineHeight: 1.3 }, children: l })
          ] }, i)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 8, flexWrap: "wrap", marginBottom: 14 }, children: [["Open Overview", "franchise"], ["Open Network Map", "table"], ["Open Growth Pipeline", "leads"], ["Open Audit Trail", "audit"], ["Open Methodology", "agenda"]].map(([lbl, t]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setTab(t), style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "7px 12px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", color: INK }, children: [
            lbl,
            " \u25B8"
          ] }, t)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, lineHeight: 1.5 }, children: "This is a live artifact, not a static deck \u2014 every number above recomputes from the canonical state as actions are taken elsewhere in the model. Unfamiliar terms are defined in the Glossary." })
        ] });
      })() }),
      tab === "franchise" && (() => {
        const CEN = { AL: [-86.8, 32.8], AZ: [-111.7, 34.3], AR: [-92.4, 34.8], CA: [-119.7, 37.2], CO: [-105.5, 39], CT: [-70, 40.3], DE: [-70, 38.6], FL: [-81.7, 28.6], GA: [-83.4, 32.6], ID: [-114.6, 44.4], IL: [-89.2, 40], IN: [-86.3, 39.9], IA: [-93.5, 42], KS: [-98.4, 38.5], LA: [-92, 31], MD: [-72.2, 38.6], MA: [-69.6, 42.8], MI: [-84.7, 43.5], MN: [-94.3, 46.3], MO: [-92.5, 38.4], NV: [-116.6, 39.3], NJ: [-73.2, 40], NY: [-75.5, 43], NC: [-79.4, 35.5], ND: [-100.5, 47.5], OH: [-82.8, 40.3], OK: [-97.5, 35.5], OR: [-120.6, 44], PA: [-77.8, 40.9], SC: [-80.9, 33.9], TN: [-86.3, 35.8], TX: [-99.3, 31.4], UT: [-111.7, 39.3], VA: [-78.8, 37.5], WA: [-120.4, 47.4], WV: [-80.6, 38.6], WI: [-89.7, 44.6], BC: [-122.5, 50.6], AB: [-113.9, 51.3], ON: [-79.9, 44.8] };
        const PRJ = (lon, lat) => [14 + (lon + 125) * (496 / 65), 18 + (53.5 - lat) * (316 / 29.5)];
        const P = (st) => CEN[st] ? PRJ(CEN[st][0], CEN[st][1]) : [500, 340];
        const OUTLINE = [[-124.7, 48.4], [-124, 46.2], [-124.4, 43], [-124.2, 40.4], [-122.4, 37.8], [-120.6, 34.5], [-117.1, 32.5], [-114.7, 32.7], [-111, 31.3], [-108.2, 31.3], [-106.4, 31.8], [-104.9, 29.6], [-103, 28.9], [-101.4, 29.8], [-99.5, 27.5], [-97.1, 25.9], [-97.2, 27.8], [-93.8, 29.7], [-90.1, 29.2], [-89, 30.4], [-85.3, 29.7], [-84, 30.1], [-82.6, 27.9], [-80, 26.8], [-80.1, 25.2], [-81.5, 25.9], [-82.7, 29], [-81.4, 30.7], [-80.9, 32], [-79, 33.2], [-76.5, 34.7], [-75.5, 35.8], [-75.9, 36.9], [-75.2, 38], [-74, 39.5], [-74, 40.6], [-72, 41], [-70.2, 41.7], [-70.9, 42.9], [-69.8, 43.8], [-68.2, 44.4], [-67.1, 45.2], [-67.8, 47.1], [-69.2, 47.5], [-71.5, 45], [-75, 45], [-76.8, 43.6], [-79, 43.3], [-79, 42.7], [-82.4, 41.7], [-83.1, 42.3], [-82.5, 45.3], [-84.7, 46.5], [-88, 48.3], [-90.8, 48.1], [-95.2, 49], [-123, 49]];
        const pathOf = (pts) => "M" + pts.map((p) => PRJ(p[0], p[1]).map((v) => v.toFixed(1)).join(" ")).join("L") + "Z";
        const agg = {};
        Object.keys(states).forEach((st) => {
          const cs = states[st];
          agg[st] = { n: cs.length, r: cs.filter((c) => forecast(c).cls === "rising").length, d: cs.filter((c) => forecast(c).cls === "deteriorating").length };
        });
        const stList = Object.keys(states).filter((st) => CEN[st]);
        const seg = Math.floor(tick / 4), ph = (tick % 4 + 1) / 4;
        const staleCountOf = (st) => (states[st] || []).filter(staleBase).length;
        const redCountOf = (st) => (states[st] || []).filter((c) => c.eb < 0).length;
        const gapOf = (st) => {
          const cs = states[st] || [];
          if (cs.length < 2) return 0;
          return Math.round(Math.max(...cs.map((c) => c.health)) - Math.min(...cs.map((c) => c.health)));
        };
        const detOf = (st) => agg[st] ? agg[st].d : 0;
        const topStates = (fn) => [...stList].filter((st) => fn(st) > 0).sort((a, b) => fn(b) - fn(a)).slice(0, 3);
        const AGENT_DEFS = [
          { n: "Measurement", c: AMB, fn: staleCountOf, unit: " stale", job: "finds the network's oldest data and queues re-measurement" },
          { n: "Best practice", c: GRN, fn: gapOf, unit: "pt gap", job: "looks for the widest health spread to transfer a working practice" },
          { n: "Support", c: AC, fn: redCountOf, unit: " red", job: "routes peer-network support to units running EBITDA-negative" },
          { n: "Forecast", c: VIO, fn: detOf, unit: " deteriorating", job: "flags states with the most 90-day-deteriorating centers" }
        ];
        const AGENTS = AGENT_DEFS.map(({ n, c, fn, unit, job }) => {
          const pool = topStates(fn);
          const use = pool.length ? pool : stList;
          const from = use[seg % use.length], to = use[(seg + 1) % use.length];
          const [f1, f2] = P(from), [t1, t2] = P(to);
          return { n, c, from, to, x: f1 + (t1 - f1) * ph, y: f2 + (t2 - f2) * ph, metric: fn(to), unit, job, reason: to + " \xB7 " + fn(to) + unit };
        });
        const topR = [...stList].sort((a, b) => agg[b].r - agg[a].r).slice(0, 3).map((st) => ({ st, txt: "+" + agg[st].r + " centers rising" }));
        const POS = [...topR, { st: "CA", txt: "enrollment-conversion practice v3 \xB7 promoted" }, { st: "TX", txt: "+3 performance rebates earned" }, { st: "ON", txt: "+2 units into STRONG tier" }, { st: "VA", txt: "+12 commitments" }, { st: "FL", txt: "referral share up 3 wks" }];
        const shown = [0, 1, 2].map((i) => POS[(seg + i) % POS.length]);
        const netScore = Math.round(centers.reduce((a, c) => a + c.health, 0) / centers.length);
        const eAvg = +(centers.slice(0, 40).reduce((a, c) => a + engageOf(c).score, 0) / 40).toFixed(2);
        const CenterCard = ({ c }) => {
          const f = forecast(c);
          const e = engageOf(c);
          const [ar, ac2] = arrow(f.cls);
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderTop: `3px solid ${AC}`, background: "#fff", padding: "10px 12px", marginTop: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 12, alignItems: "flex-start", flexWrap: "wrap" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { textAlign: "center", minWidth: 64 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 34, fontWeight: 700, lineHeight: 1 }, children: c.health }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 1, color: AC }, children: tierOf(c.health) })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: 1, minWidth: 180 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 13, fontWeight: 700 }, children: [
                  c.name,
                  " ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 8.5, color: MUT, fontWeight: 400 }, children: c.verified ? "\xB7 verified" : "\xB7 modeled" }),
                  c.adjusted && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 7.5, fontWeight: 700, color: VIO, marginLeft: 5, border: `1px solid ${VIO}`, padding: "1px 4px" }, children: "\u25B2 LIVE ADJUSTMENT" })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#444", marginTop: 2 }, children: [
                  c.students,
                  " students \xB7 margin ",
                  fmt(c.eb),
                  "k \xB7 retention ",
                  pct(c.ret),
                  "% \xB7 engagement ",
                  e.score,
                  " \xB7 ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: ac2 }, children: [
                    ar,
                    " ",
                    fmt(f.proj[2]),
                    "k @90d"
                  ] })
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { onClick: () => {
                setMapC(null);
                setCardMsg("");
              }, style: { fontFamily: "Helvetica", fontSize: 12, color: MUT, cursor: "pointer" }, children: "\xD7" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: AC, margin: "8px 0 4px" }, children: "Next steps" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
              setSel(c.name);
              setTab("team");
            }, style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "6px 12px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff" }, children: "Open full center page \u25B8" })
          ] });
        };
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 0, flexWrap: "wrap", border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", marginBottom: 8 }, children: [[netScore, "network health \u2014 live composite", INK], [Math.round(trend.r / centers.length * 100) + "%", trend.r + " units rising (90d)", GRN], [trend.d, "deteriorating \u2014 on a support path", AC], [alerts.filter((a) => a.eta <= 30).length, "urgent flags \u2014 action inside 30 days", VIO], ["64 / 244", "verified units / FDD Item 20 authoritative", MUT]].map(([v, l, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 130px", padding: "9px 12px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 21, fontWeight: 800, color: c, lineHeight: 1 }, children: v }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, letterSpacing: 0.3, marginTop: 3, lineHeight: 1.35 }, children: l })
          ] }, i)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 8, marginBottom: 8, border: `1px solid ${RULE}`, background: "#f7f6f2", padding: "10px 12px" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: INK, marginBottom: 6 }, children: "Executive summary" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(230px,1fr))", gap: 8 }, children: [
              "Franchisee performance and unit profitability are evaluated as a single measure, not two separate ones; a unit in decline is identified before it appears in a P&L review.",
              "Retention risk and support capacity are assessed on a weekly cycle, not discovered at quarterly review.",
              "Every intervention follows a fixed protocol \u2014 observe, identify the binding constraint, pilot, measure, propagate \u2014 subject to governance in every instance.",
              "Verified practices are not retained locally. Propagation across the network is governed by the same system that identifies where a practice is applicable."
            ].map((t, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#333", lineHeight: 1.5, paddingLeft: 10, borderLeft: `2px solid ${INK}` }, children: t }, i)) })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 8, border: `1px solid ${RULE}`, background: "#fdfdfb" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => setAboutOpen((o) => !o), style: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "9px 12px", cursor: "pointer" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: INK }, children: "About this submission \u2014 rationale and a 5-minute walkthrough" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9, color: AC }, children: aboutOpen ? "collapse \u25B2" : "expand \u25BC" })
            ] }),
            aboutOpen && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "0 12px 12px", borderTop: `1px solid ${RULE}` }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: INK, margin: "10px 0 4px" }, children: "Rationale" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11.5, color: "#333", lineHeight: 1.6 }, children: "Franchise networks incur avoidable cost when unit-level deterioration goes undetected between measurement cycles; by the time a decline is visible in a P&L review, remediation typically costs several multiples of what earlier intervention would have required. This model addresses that gap: it evaluates each unit's leading indicators continuously, proposes a specific support action under governance, and records the resulting decision. The underlying mechanics \u2014 agents, signals, confidence scoring \u2014 exist for one purpose: to enable action at the point of early detection rather than after quarterly reporting." }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: INK, margin: "14px 0 4px" }, children: "The 5-minute walkthrough" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("ol", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#333", lineHeight: 1.7, margin: "0 0 0 18px", padding: 0 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("li", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Network health, current period." }),
                  " The composite score and trend below reflect the present session, not a static snapshot; each figure recomputes as the underlying state changes."
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("li", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Proposed actions, current period." }),
                  " Four evaluation agents identify specific, evidence-backed actions across the network, each carrying a stated confidence level and its supporting basis."
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("li", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Governed approval." }),
                  " A single proposed action may be approved directly from this screen. No action is applied to a center's record without a guardrail clearing first; that gate is visible, not implied."
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("li", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Interventions in parallel." }),
                  " The Operations Board presents every open intervention concurrently \u2014 by review cadence, by decision state, and as a governed trace \u2014 over the same decision record."
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("li", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "One center, six lenses." }),
                  " Under Centers, any single unit may be reviewed through six synchronized perspectives \u2014 student, parent, franchisee, corporate, financial, and system \u2014 each reading the same signal concurrently."
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("li", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Supporting evidence." }),
                  " The territory map, agent logic, and leading-indicator calculations behind each figure above are presented in full, not merely asserted."
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("li", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "The proposed plan." }),
                  " The closing section states the sequence of actions for the first 90 days at Code Ninjas."
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 8 }, children: [
                "An interactive version of this walkthrough is also available: ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setTour(0), "aria-label": "Start the 5-minute guided tour", style: { color: AC, cursor: "pointer", textDecoration: "underline", background: "none", border: "none", padding: 0, fontFamily: "inherit", fontSize: "inherit" }, children: "start the 5-minute tour \u2192" })
              ] })
            ] })
          ] }),
          (() => {
            const freshShare = Math.round((1 - centers.filter(staleBase).length / centers.length) * 100);
            const avgConf = +(railData.recommendations.reduce((a, r) => a + r.evidence.operationalSignals.dataConfidence, 0) / Math.max(1, railData.recommendations.length)).toFixed(2);
            const highConf = railData.recommendations.filter((r) => r.confidenceBand === "high").length;
            const spotConflictIds = new Set((railData.conflicts || []).flatMap((c) => [c.a, c.b]));
            const spotlightPool = [...railData.actionable].sort((a, b) => b.evidence.operationalSignals.dataConfidence - a.evidence.operationalSignals.dataConfidence);
            const spotlight = spotlightPool.find((r) => !spotConflictIds.has(r.id)) || null;
            const spotlightAllConflicted = spotlightPool.length > 0 && !spotlight;
            const onSpotDecide = (rec, d) => {
              setSpotDecision(d);
              logL("franchise", rec.agent, rec.title + " \u2014 " + (d === "approved" ? "approved by Director" : d === "measure" ? "queued for re-measurement" : "ignored") + " (from Overview)");
            };
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 8, border: `1px solid ${RULE}`, background: "#fff" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 0.7, textTransform: "uppercase", color: INK, padding: "8px 12px", borderBottom: `1px solid ${RULE}` }, children: "Today" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "10px 12px", borderBottom: `1px solid ${RULE}` }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 4 }, children: "1 \xB7 Network health" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, color: "#333", lineHeight: 1.5 }, children: [
                  netScore,
                  " composite \xB7 ",
                  trend.r,
                  " rising, ",
                  trend.d,
                  " on a support path (90d) \xB7 ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: freshShare >= 70 ? GRN : AMB }, children: [
                    freshShare,
                    "%"
                  ] }),
                  " of units measured within the last 14 days."
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "10px 12px", borderBottom: `1px solid ${RULE}` }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 4 }, children: "2 \xB7 Today's decisions" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, color: "#333", lineHeight: 1.5 }, children: [
                  railData.actionable.length,
                  " proposed actions pending Director review across four evaluation agents \xB7 ",
                  railData.held.length,
                  " held pending fresher data or a cleared guardrail. ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setTab("rail"), "aria-label": "Open Decision Rail: threads and queue", style: { color: AC, cursor: "pointer", textDecoration: "underline", background: "none", border: "none", padding: 0, fontFamily: "inherit", fontSize: "inherit" }, children: "Open threads & queue \u2192" })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 3 }, children: [
                  "This is the network-wide triage view \u2014 the highest-confidence item across all ",
                  centers.length,
                  " units. To deliberately choose one center and see it through all six roles, use Six Lenses under Centers instead."
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => setTodayDetail((v) => !v), style: { padding: "6px 12px", borderBottom: `1px solid ${RULE}`, cursor: "pointer", display: "flex", justifyContent: "space-between", alignItems: "center" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT }, children: "Confidence basis & follow-through record" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: AC }, children: todayDetail ? "hide" : "show" })
              ] }),
              todayDetail && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "10px 12px", borderBottom: `1px solid ${RULE}` }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 4 }, children: "3 \xB7 Why the model is confident" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, color: "#333", lineHeight: 1.5 }, children: [
                  "Average data confidence across today's proposed actions is ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: avgConf }),
                  " (0\u20131 scale); ",
                  highConf,
                  " of ",
                  railData.recommendations.length,
                  " carry a high-confidence rating. Confidence reflects two factors: recency of measurement and internal consistency of the underlying figures \u2014 not a self-reported assessment."
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "10px 12px", borderBottom: `1px solid ${RULE}` }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 4 }, children: "Safe governed action" }),
                spotlight ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${GRN}`, padding: "7px 9px" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700 }, children: spotlight.title }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, margin: "2px 0 5px" }, children: spotlight.summary }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 5, flexWrap: "wrap", marginBottom: 6 }, children: spotlight.evidence.contractVerdicts.map((v, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8, padding: "2px 6px", border: `1px solid ${v.pass ? GRN : AC}`, color: v.pass ? GRN : AC }, children: [
                    v.pass ? "\u2713" : "\u2715",
                    " ",
                    v.label
                  ] }, i)) }),
                  spotDecision ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: spotDecision === "approved" ? GRN : spotDecision === "measure" ? AMB : MUT }, children: spotDecision === "approved" ? "Approved" : spotDecision === "measure" ? "Queued for re-measurement" : "Ignored" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 5 }, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => onSpotDecide(spotlight, "approved"), style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "3px 9px", cursor: "pointer", border: `1px solid ${GRN}`, background: GRN, color: "#fff" }, children: "Approve" }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => onSpotDecide(spotlight, "measure"), style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "3px 9px", cursor: "pointer", border: `1px solid ${AMB}`, background: "#fff", color: AMB }, children: "Measure again" }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => onSpotDecide(spotlight, "ignored"), style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "3px 9px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: MUT }, children: "Ignore" })
                  ] }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 5 }, children: [
                    "approver on record (human-owned): ",
                    spotlight.approverRole,
                    " \xB7 nothing here writes to a center's live record without this step",
                    (() => {
                      const u = spotlight.targetIds && spotlight.targetIds.find((id) => id && id.length > 2 && id !== spotlight.scope);
                      return u ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { onClick: () => jumpTo("lenses", u, "today\u2019s spotlighted action: \u201C" + spotlight.title + "\u201D"), style: { color: AC, cursor: "pointer", textDecoration: "underline", marginLeft: 6 }, children: "view in Six Lenses \u2192" }) : null;
                    })()
                  ] })
                ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT }, children: spotlightAllConflicted ? "every actionable proposal right now is flagged by the cross-agent conflict check \u2014 see Decision Rail before acting on any of them" : "nothing actionable right now \u2014 every proposal is held on fresher data or a guardrail" })
              ] }),
              todayDetail && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "10px 12px" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 4 }, children: "Logged operational follow-through" }),
                ledger.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT }, children: "no actions logged yet this session \u2014 approvals and re-measurements will appear here" }),
                ledger.slice(0, 5).map((e, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, fontFamily: "Helvetica", fontSize: 9, padding: "3px 0", borderTop: i ? `1px solid #f0f0f0` : "none" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT, minWidth: 60 }, children: e.tab }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontWeight: 700, minWidth: 90 }, children: e.actor }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: "#444" }, children: e.text })
                ] }, i))
              ] })
            ] });
          })(),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { margin: "14px 0 6px", display: "flex", alignItems: "baseline", gap: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT }, children: "The proof underneath" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: "\u2014 why this matters: a proposed action without supporting evidence is an assertion, not a basis for decision. Below is the territory map, four evaluation agents, and the leading-indicator calculations behind today's figures \u2014 the full leading-indicator catalog, failure-mode log, and canonical network state are maintained under Workflow, Programs, and Methodology." })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(EngineErrorBoundary, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid #2a2a2a`, background: "#121212" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", padding: "8px 12px 0" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "baseline", gap: 10, flexWrap: "wrap" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 1.6, color: "#b8b8b8" }, children: "NORTH AMERICA \u2014 TERRITORY MAP" }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 2 }, children: [["health", "health"], ["freshness", "freshness"], ["headroom", "headroom"], ["consistency", "consistency"], ["momentum", "momentum"]].map(([k, l]) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setMapMode(k), style: { fontFamily: "Helvetica", fontSize: 7.5, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", padding: "2px 7px", cursor: "pointer", border: "1px solid " + (mapMode === k ? "#7a6bd8" : "#333"), background: mapMode === k ? "#1c1640" : "transparent", color: mapMode === k ? "#b8aaff" : "#777" }, children: l }, k)) })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, color: "#ddd" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontSize: 22, color: "#fff" }, children: netScore }),
                  " ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 7.5, fontWeight: 700, letterSpacing: 1, color: "#8a8a8a" }, children: "NETWORK" }),
                  "  ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: "#6fc38a" }, children: [
                    "\u2191",
                    trend.r
                  ] }),
                  " ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: "#d9a62e" }, children: [
                    "\u2192",
                    trend.h
                  ] }),
                  " ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: "#d96a6a" }, children: [
                    "\u2193",
                    trend.d
                  ] })
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 524 356", style: { width: "100%", height: "auto", display: "block" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: pathOf(OUTLINE), fill: "#242424", stroke: "#5c5c5c", strokeWidth: "1.6" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: "200", y: "200", style: { font: "bold 8px Helvetica", fill: "#4a4a4a", letterSpacing: "2.5px" }, children: "UNITED STATES" }),
                QCLUSTERS.map((cl, ci) => cl.slice(1).map((st, i) => {
                  const a = cl[i], b = st;
                  if (!CEN[a] || !CEN[b] || !states[a] || !states[b]) return null;
                  const [x1, y1] = P(a), [x2, y2] = P(b);
                  const mA = AGENTS[0];
                  const active = mA.to === a || mA.to === b || mA.from === a || mA.from === b;
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1, y1, x2, y2, stroke: active ? "#8a7ade" : "#2c2c3c", strokeWidth: active ? 1.3 : 0.6, strokeDasharray: "1 3", opacity: active ? 0.85 : 0.45 }, "ent" + ci + "-" + i);
                })),
                AGENTS.map((a, i) => {
                  const [t1, t2] = P(a.to);
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: a.x, y1: a.y, x2: t1, y2: t2, stroke: a.c, strokeWidth: "0.9", strokeDasharray: "3 3", opacity: "0.6" }, "l" + i);
                }),
                Object.keys(CEN).map((st) => {
                  const [x, y] = P(st);
                  const has = states[st];
                  if (!has) return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { onClick: () => {
                    setMapSt(st === mapSt ? null : st);
                    setMapC(null);
                  }, style: { cursor: "pointer" }, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: x, cy: y, r: 9, fill: "transparent" }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: x, cy: y, r: "3", fill: "#3a3a3a", stroke: "#666", strokeWidth: "1" })
                  ] }, st);
                  const amp = qStateAmp(states[st]);
                  const lm = qLastMeasSeg(stList, st, seg);
                  const coh = lm === null ? 0.12 : qCoherence((seg - lm) * 6);
                  const cl = QCLUSTERS.find((c) => c.includes(st));
                  const manual = qMeas[st];
                  const inferred = cl ? cl.some((o) => o !== st && qMeas[o]) : false;
                  const r = 7.5 + Math.min(10, agg[st].n / 5);
                  const dom = qResolve(amp);
                  const resolved = coh > 0.62 || manual || inferred;
                  const measuring = AGENTS[0].to === st;
                  const cs2 = states[st];
                  const modeVal = (() => {
                    switch (mapMode) {
                      case "freshness": {
                        const fr2 = cs2.filter((c) => !staleBase(c)).length / cs2.length;
                        return { v: fr2, c: fr2 > 0.7 ? "#5cb87c" : fr2 > 0.4 ? "#d9a62e" : "#8a8fa8", lbl: Math.round(fr2 * 100) + "%" };
                      }
                      case "headroom": {
                        const tight = cs2.filter((c) => c.eb < 3).length / cs2.length;
                        return { v: 1 - tight, c: tight > 0.5 ? "#d96a6a" : tight > 0.25 ? "#d9a62e" : "#5cb87c", lbl: Math.round((1 - tight) * 100) + "%" };
                      }
                      case "consistency": {
                        const sc2 = cs2.reduce((a, c) => {
                          const m = [c.conv / 0.65, c.ret / 0.94, c.chem / 0.8];
                          const mu = (m[0] + m[1] + m[2]) / 3;
                          const varr = m.reduce((s, x2) => s + (x2 - mu) * (x2 - mu), 0) / 3;
                          return a + Math.max(0, 1 - varr * 6);
                        }, 0) / cs2.length;
                        return { v: sc2, c: sc2 > 0.75 ? "#5cb87c" : sc2 > 0.55 ? "#d9a62e" : "#d96a6a", lbl: Math.round(sc2 * 100) };
                      }
                      case "momentum": {
                        const net = (agg[st].r - agg[st].d) / cs2.length;
                        return { v: net, c: net > 0.1 ? "#5cb87c" : net < -0.1 ? "#d96a6a" : "#d9a62e", lbl: (net >= 0 ? "+" : "") + Math.round(net * 100) };
                      }
                      default:
                        return null;
                    }
                  })();
                  let acc = -Math.PI / 2;
                  const wedges = amp.map((av, wi) => {
                    const a0 = acc, a1 = acc + av * 2 * Math.PI;
                    acc = a1;
                    return { d: qArc(x, y, r, a0, a1), c: QCOL[wi] };
                  });
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { onClick: () => {
                    setMapSt(st === mapSt ? null : st);
                    setMapC(null);
                    setSt(st);
                  }, style: { cursor: "pointer" }, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: x, cy: y, r: Math.max(r, 9), fill: "transparent" }),
                    mapSt === st && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: x, cy: y, r: r + 4, fill: "none", stroke: "#fff", strokeWidth: "1.4" }),
                    !modeVal && wedges.map((w, wi) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: w.d, fill: w.c, opacity: resolved ? 0.22 : 0.6, stroke: "#0f0f0f", strokeWidth: "0.4" }, wi)),
                    !modeVal && resolved && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: x, cy: y, r: r * 0.6, fill: QCOL[dom], stroke: manual ? "#fff" : inferred ? "#8a7ade" : "#fff", strokeWidth: manual ? 1.4 : 1 }),
                    modeVal && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: x, cy: y, r, fill: modeVal.c, opacity: mapMode === "freshness" ? 0.25 + modeVal.v * 0.75 : 0.85, stroke: "#0f0f0f", strokeWidth: "0.5" }),
                    modeVal && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x, y: y - r - 3, textAnchor: "middle", style: { font: "bold 6.5px Helvetica", fill: "#d5d5d5", paintOrder: "stroke", stroke: "#0a0a0a", strokeWidth: 1.2 }, children: modeVal.lbl }),
                    manual && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: x, cy: y, r: r + 5, fill: "none", stroke: "#fff", strokeWidth: "1.2", opacity: "0.9" }),
                    inferred && !manual && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: x, cy: y, r: r + 3.5, fill: "none", stroke: "#8a7ade", strokeWidth: "1", strokeDasharray: "1 2", opacity: "0.9" }),
                    !resolved && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: x, cy: y, r: r + 1.5, fill: "none", stroke: "#9a9a9a", strokeWidth: "0.7", strokeDasharray: "2 2", opacity: (1 - coh) * 0.9 }),
                    measuring && !manual && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: x, cy: y, r: r + 5, fill: "none", stroke: "#d9a62e", strokeWidth: "1.4", opacity: "0.9" }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x, y: y + 3, textAnchor: "middle", style: { font: "bold 8px Helvetica", fill: resolved ? "#fff" : "#0f0f0f", paintOrder: "stroke", stroke: resolved ? "rgba(0,0,0,0.35)" : "rgba(255,255,255,0.35)", strokeWidth: 0.6 }, children: st }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x, y: y + r + 9, textAnchor: "middle", style: { font: "bold 7px Helvetica", fill: "#d5d5d5" }, children: agg[st].n })
                  ] }, st);
                }),
                AGENTS.map((a, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { onClick: () => {
                  setMapSt(a.to);
                  setMapC(null);
                }, style: { cursor: "pointer" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("title", { children: `${a.n} \u2014 ${a.job}. Currently en route to ${a.to} (${a.reason}).` }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: a.x, cy: a.y, r: "5", fill: "#121212", stroke: a.c, strokeWidth: "2.2", style: { transition: "all 1.05s linear" } }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: a.x, y: a.y + 14, textAnchor: "middle", style: { font: "bold 6.5px Helvetica", fill: a.c, paintOrder: "stroke", stroke: "#0a0a0a", strokeWidth: 1.4, transition: "all 1.05s linear" }, children: a.n.toUpperCase() })
                ] }, "a" + i)),
                shown.map((p, i) => {
                  const [x, y] = P(p.st);
                  const w = p.txt.length * 4.5 + 12;
                  const ox = Math.min(Math.max(6, x - w / 2), 514 - w);
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { opacity: "0.95", children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", { x: ox, y: Math.min(y + 13, 330), width: w, height: 14, fill: "#142b1b", stroke: "#4d8f63", strokeWidth: "1", rx: "2" }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: ox + w / 2, y: Math.min(y + 23, 340), textAnchor: "middle", style: { font: "7.5px Helvetica", fill: "#a9d6b4" }, children: p.txt })
                  ] }, "p" + i);
                })
              ] }),
              mapMode !== "health" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: "#9aa4c8", padding: "5px 12px 0" }, children: {
                freshness: "DATA FRESHNESS \u2014 states brighten with measured share; a thriving-but-unmeasured state dims until reviewed. One glance answers: where do we measure next?",
                headroom: "PRICING HEADROOM \u2014 share of units with margin comfortably above the floor. A state can be healthy today and still run hot here: small tuition moves would strain it.",
                consistency: "INTERNAL CONSISTENCY \u2014 how well each unit's own metrics agree (enrollment vs retention vs staffing). Low scores flag units that look fine on average but disagree with themselves.",
                momentum: "MOMENTUM \u2014 net 90-day trajectory per state (rising minus declining, as % of units). Steep negative values flag acceleration toward trouble before health scores move."
              }[mapMode] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 10, flexWrap: "wrap", alignItems: "center", padding: "6px 12px 9px", borderTop: "1px solid #262626" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 1, color: "#b8b8b8" }, children: "STATUS" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { display: "flex", alignItems: "center", gap: 4, fontFamily: "Helvetica", fontSize: 8, color: "#bbb" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 9, height: 9, borderRadius: 5, background: "#5cb87c", border: "1.5px solid #fff" } }),
                  "verified \xB7 current"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { display: "flex", alignItems: "center", gap: 4, fontFamily: "Helvetica", fontSize: 8, color: "#bbb" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 9, height: 9, borderRadius: 5, background: "conic-gradient(#5cb87c 0 33%,#d9a62e 33% 66%,#d96a6a 66%)", opacity: 0.65 } }),
                  "estimated \xB7 stale"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { display: "flex", alignItems: "center", gap: 4, fontFamily: "Helvetica", fontSize: 8, color: "#bbb" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 14, height: 0, borderTop: "1px dashed #8a7ade" } }),
                  "linked cluster"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { display: "flex", alignItems: "center", gap: 4, fontFamily: "Helvetica", fontSize: 8, color: "#bbb" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 9, height: 9, borderRadius: 5, border: "1.4px solid #d9a62e" } }),
                  "measuring now"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { marginLeft: "auto", fontFamily: "Helvetica", fontSize: 8, color: "#8a8a8a" }, children: [
                  "program interaction: ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: "#6fc38a" }, children: [
                    centers.filter((c) => forecast(c).cls === "rising" && engageOf(c).score >= 0.7).length,
                    " reinforcing"
                  ] }),
                  " \xB7 ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: "#d96a6a" }, children: [
                    centers.filter((c) => forecast(c).cls === "deteriorating" && c.eb < 3).length,
                    " competing"
                  ] }),
                  " (programs vs Sensei-hours \xB7 derived from trajectory \xD7 engagement / margin)"
                ] })
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(240px,1fr))", gap: 8, marginTop: 8 }, children: [
              ["SEE EARLY", "Recovery time is the leading indicator \u2014 a unit with a slow recovery profile is flagged 30\u201390 days before the P&L reflects it. " + alerts.filter((a) => a.eta <= 30).length + " flags require action inside 30 days.", "warning", "Open early warning", VIO],
              ["SUPPORT FAST", "Interventions are proposed under an 8-point weekly capacity budget and four hard governors \u2014 financial materiality, engagement integrity, child safety, data integrity. " + red.length + " units are on the priority queue today.", "pain", "Open priorities", AC],
              ["GROW ON CADENCE", "The full franchise sales cycle runs inside the same record: lead scoring, discovery days, FDD and federal compliance as hard gates. Nothing signs while a compliance item is open.", "leads", "Open the pipeline", GRN]
            ].map(([h, b, t2, lbl, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderTop: `3px solid ${c}`, background: "#fff", padding: "10px 12px", display: "flex", flexDirection: "column" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 1.2, color: c, marginBottom: 5 }, children: h }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#333", lineHeight: 1.55, flex: 1 }, children: b }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setTab(t2), style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "5px 11px", cursor: "pointer", border: `1px solid ${c}`, background: "#fff", color: c, marginTop: 8, alignSelf: "flex-start" }, children: [
                lbl,
                " \u2192"
              ] })
            ] }, i)) }),
            mapSt && states[mapSt] && (() => {
              const cs = [...states[mapSt]].sort((a, b) => b.health - a.health);
              const g = agg[mapSt];
              return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", padding: "9px 11px", marginTop: 8 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", flexWrap: "wrap" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700 }, children: [
                    mapSt,
                    " \u2014 ",
                    g.n,
                    " centers ",
                    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontSize: 9, color: MUT, fontWeight: 400 }, children: [
                      "\xB7 ",
                      cs.filter((c) => c.verified).length,
                      " verified \xB7 ",
                      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: GRN }, children: [
                        "\u2191",
                        g.r
                      ] }),
                      " ",
                      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: AC }, children: [
                        "\u2193",
                        g.d
                      ] })
                    ] })
                  ] }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { onClick: () => {
                    setMapSt(null);
                    setMapC(null);
                  }, style: { fontFamily: "Helvetica", fontSize: 12, color: MUT, cursor: "pointer" }, children: "close \xD7" })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap", margin: "6px 0" }, children: (() => {
                  const amp = qStateAmp(states[mapSt]);
                  const dom = qResolve(amp);
                  const measured = qMeas[mapSt];
                  const cl = QCLUSTERS.find((c) => c.includes(mapSt));
                  const partners = cl ? cl.filter((o) => o !== mapSt && states[o]) : [];
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
                      setQMeas((m) => ({ ...m, [mapSt]: true }));
                      logL("franchise", "Measurement", mapSt + " state measured \u2014 cluster partners inferred");
                    }, style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "5px 10px", cursor: "pointer", border: `1px solid ${VIO}`, background: measured ? VIO : "#fff", color: measured ? "#fff" : VIO }, children: measured ? "\u25C6 measured \u2014 current" : "\u27F2 Run measurement (verify)" }),
                    measured && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setQMeas((m) => {
                      const nm = { ...m };
                      delete nm[mapSt];
                      return nm;
                    }), style: { fontFamily: "Helvetica", fontSize: 9, padding: "5px 9px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: MUT }, children: "\u21BA mark stale" }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: measured ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                      "verified as ",
                      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: HCOL[dom] }, children: QSTATES[dom] }),
                      partners.length ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                        " \xB7 linked ",
                        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: VIO }, children: partners.join(", ") }),
                        " now inferred"
                      ] }) : null
                    ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                      "estimated: ",
                      QSTATES.map((n, i) => pct(amp[i]) + "% " + n).join(" \xB7 "),
                      " \u2014 measuring verifies it"
                    ] }) })
                  ] });
                })() }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fill,minmax(104px,1fr))", gap: 5, maxHeight: 340, overflowY: "auto", marginTop: 6 }, children: cs.map((c) => {
                  const [ar, ac2] = arrow(forecast(c).cls);
                  const sel2 = mapC === c;
                  const tc = c.health >= 88 ? "#2f7a3f" : c.health >= 80 ? "#b8860b" : c.health >= 68 ? "#7d7d7d" : "#8b0000";
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => {
                    setMapC(sel2 ? null : c);
                    setCardMsg("");
                    setSel(c.name);
                  }, style: { border: `1px solid ${sel2 ? INK : RULE}`, borderTop: `3px solid ${tc}`, background: sel2 ? "#fbf9f4" : "#fff", padding: "6px 7px", cursor: "pointer", opacity: c.verified ? 1 : 0.8 }, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline" }, children: [
                      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 17, color: INK, lineHeight: 1 }, children: c.health }),
                      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.5, color: tc }, children: tierOf(c.health) })
                    ] }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.8, fontWeight: 700, color: INK, margin: "2px 0 1px", lineHeight: 1.1, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, children: c.name }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 7.5, color: MUT }, children: [
                      c.students,
                      " students \xB7 ",
                      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: ac2 }, children: [
                        ar,
                        fmt(forecast(c).proj[2]),
                        "k"
                      ] })
                    ] })
                  ] }, c.name);
                }) }),
                mapC && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CenterCard, { c: mapC })
              ] });
            })(),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, marginTop: 8, flexWrap: "wrap" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 180px", border: `1px solid ${RULE}`, background: "#fff", padding: "8px 10px" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: AC, marginBottom: 6 }, children: "Agent activity \u2014 current cycle" }),
                AGENTS.map((a, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => {
                  setMapSt(a.to);
                  setMapC(null);
                }, title: a.job, style: { fontFamily: "Helvetica", fontSize: 9.5, marginBottom: 5, display: "flex", alignItems: "flex-start", gap: 6, cursor: "pointer" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 9, height: 9, borderRadius: 5, background: "#fff", border: `2px solid ${a.c}`, flexShrink: 0, marginTop: 2 } }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: a.n }),
                    " \xB7 ",
                    a.from,
                    " \u2192 ",
                    a.to,
                    " ",
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: a.metric > 0 ? a.c : MUT, fontWeight: 700 }, children: a.reason }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("br", {}),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 8, color: MUT }, children: a.job })
                  ] })
                ] }, i)),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 7.5, color: MUT, marginTop: 2, borderTop: `1px solid ${RULE}`, paddingTop: 4 }, children: "ranked live from network state each tick \xB7 tap an agent to open its target state" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 180px", border: `1px solid ${RULE}`, background: "#fff", padding: "8px 10px" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: GRN, marginBottom: 6 }, children: "Positive indicators \u2014 rolling" }),
                shown.map((p, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, marginBottom: 4, color: "#2f5a3a" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: p.st }),
                  " \xB7 ",
                  p.txt
                ] }, i))
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 180px", border: `1px solid ${RULE}`, background: "#fff", padding: "8px 10px" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: AC, marginBottom: 6 }, children: "Network snapshot \u2014 computed live" }),
                (() => {
                  const avgS = Math.round(centers.reduce((a, c) => a + c.students, 0) / centers.length);
                  const avgConv = Math.round(centers.reduce((a, c) => a + c.conv, 0) / centers.length * 100);
                  const avgRet = Math.round(centers.reduce((a, c) => a + c.ret, 0) / centers.length * 100);
                  return [["Avg students/center", avgS, avgS < 60], ["Trial \u2192 enrollment", avgConv + "%", avgConv < 50], ["12-mo retention", avgRet + "%", avgRet < 70], ["Below breakeven", red.length + " units", red.length > 0], ["Data stale >4wk", staleN + " units", staleN > 0]].map(([m, v, warn], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, marginBottom: 3, display: "flex", justifyContent: "space-between" }, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: m }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: warn ? AC : INK }, children: v })
                  ] }, i));
                })(),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 3 }, children: "every figure derived from the modeled network this render \u2014 same source as the map" })
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 6 }, children: [
              "US + Canada footprint \xB7 illustrative motion over live-computed state \xB7 engagement avg ",
              eAvg
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { margin: "16px 0 6px", fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: GRN }, children: "What I would do first at Code Ninjas" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginBottom: 6 }, children: "\u2014 why this matters: a strategy is only credible once it survives contact with week one. This is the concrete order of operations, not a slogan." }),
          (() => {
            const phases = PLAN_90.map((p) => p.phase.split(" \u2014 "));
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 8, border: `1px solid ${RULE}`, borderLeft: `3px solid ${GRN}`, background: "#fff", padding: "9px 12px" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 6 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: GRN }, children: "Day-by-day breakdown" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { onClick: () => setTab("plan"), style: { fontFamily: "Helvetica", fontSize: 9, color: AC, cursor: "pointer", textDecoration: "underline" }, children: "full plan \u2192" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(180px,1fr))", gap: 8 }, children: phases.map(([days, name], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: MUT, textTransform: "uppercase", letterSpacing: 0.4 }, children: days }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700 }, children: name }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#555", marginTop: 1 }, children: PLAN_90[i].focus })
              ] }, i)) })
            ] });
          })(),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 6, fontFamily: "Helvetica", fontSize: 8.5, color: MUT, borderTop: `1px solid ${RULE}`, paddingTop: 6 }, children: "Data boundary: figures across this artifact are modeled/illustrative pending live MyStudio and QuickBooks integration, with one exception \u2014 FDD Item 20 (244 units) and the 64 publicly verified centers are the authoritative counts. Nothing here overrides that reconciliation." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FDDReconciliationPanel, { centers })
        ] });
      })(),
      tab === "engagement" && (() => {
        const e = engageOf(team);
        const f = forecast(team);
        const Row = ({ l, v, w, c }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, marginBottom: 5 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: l }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: v })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 6, background: "#eee" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 6, width: w * 100 + "%", background: c || GRN } }) })
        ] });
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, border: `1px solid ${RULE}`, borderLeft: `3px solid ${AC}`, padding: "6px 10px", marginBottom: 10, lineHeight: 1.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: AC }, children: "PROVENANCE" }),
            " \xB7 Framework developed at theCoderSchool Pleasanton \u2014 engagement tracker, coach badges, pre/post-session routines, coach\u2013student matching, student pathways. Engagement leads; economics follow."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AC, marginBottom: 5 }, children: [
            team.name,
            " \u2014 engagement signals (score ",
            e.score,
            ")"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Row, { l: "Pre-session routine completion", v: pct(e.pre) + "%", w: e.pre }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Row, { l: "Post-session note + family report", v: pct(e.post) + "%", w: e.post, c: e.post < 0.7 ? AMB : GRN }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Row, { l: "Coach\u2013student match quality", v: pct(e.match) + "%", w: e.match, c: e.match < 0.65 ? AMB : GRN }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Row, { l: "New students progressing in first 30 days", v: pct(e.prog30) + "%", w: e.prog30, c: e.prog30 < 0.6 ? AC : GRN }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Row, { l: "Floor energy (sessions with live demos)", v: pct(e.buzz) + "%", w: e.buzz, c: VIO }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Row, { l: "Referral share of new enrollments", v: pct(e.refer) + "%", w: e.refer, c: VIO }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#555", lineHeight: 1.5, border: `1px solid ${RULE}`, padding: "6px 9px", marginTop: 8 }, children: [
            "Engagement ",
            e.score >= 0.72 ? "leads the forecast up" : "is the earliest warning",
            " \u2014 this center's 90-day trajectory reads ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: f.cls === "rising" ? GRN : f.cls === "deteriorating" ? AC : AMB }, children: f.cls }),
            ". Engagement moves one to two billing cycles before the P&L does."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: GRN, margin: "10px 0 4px" }, children: "Outcomes \u2014 what the framework produced" }),
          [["Congressional App Challenge \u2014 WON", "a mental-health application; the result of fighting to keep a student and her coach together."], ["District bracket held", "eight schools; four placed in the national top twenty"], ["Process co-developed with the DM", "what worked on the floor was written down as method"], ["Word-of-mouth growth", "families committing to 6- and 12-month terms"]].map(([h, d], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${GRN}`, padding: "5px 9px", marginBottom: 4 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 9.5 }, children: h }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#555", lineHeight: 1.45 }, children: d })
          ] }, i)),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 8 }, children: "signals illustrative per center \xB7 outcomes are the Pleasanton record \xB7 no progress record inflated to flatter this tab" })
        ] });
      })(),
      tab === "agenda" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", padding: "8px 10px", margin: "0 0 8px" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: MUT, marginBottom: 5 }, children: "What's new in this version" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#333", lineHeight: 1.6 }, children: [
            "Added four tabs and extended one: ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Success Rate" }),
            " (support-plan completion by type), ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Batch Operations" }),
            " (bulk re-measure / bulk-commit across a filtered center set), ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Historical Playback" }),
            " (session trend line, captured automatically each week), and ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Reports & Exports" }),
            " (every export consolidated in one place, including MyStudio/QuickBooks payload shapes). ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Audit Trail" }),
            " gained search and source-tab filtering. All read the same canonical state every other view reads \u2014 no new data sources were introduced."
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NetworkHealthDashboard, { centers, alerts, red, staleN }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${VIO}`, background: "#fff", padding: "8px 10px", margin: "8px 0" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: VIO, marginBottom: 5 }, children: "Action ledger \u2014 every committed action, any tab, this session" }),
          ledger.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: "no actions committed yet \u2014 measurements, interventions, transfers, and deal-agent weeks all write here" }),
          ledger.slice(0, 12).map((e, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, marginBottom: 3, display: "flex", gap: 8, alignItems: "baseline" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 7.5, fontWeight: 700, letterSpacing: 0.4, color: MUT, textTransform: "uppercase", width: 60, flexShrink: 0 }, children: e.tab }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: VIO, width: 78, flexShrink: 0 }, children: e.actor }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: "#444" }, children: e.text })
          ] }, i)),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 7.5, color: MUT, marginTop: 4, borderTop: `1px solid ${RULE}`, paddingTop: 3 }, children: [
            "single integration layer: committed interventions write metric deltas to the shared world state \u2014 map, forecasts, pain ranking, and this record all read the same adjusted units \xB7 ",
            Object.keys(adj).length,
            " unit",
            Object.keys(adj).length === 1 ? "" : "s",
            " currently carrying live adjustments"
          ] })
        ] }),
        [
          ["\xA71", A("score"), V("measure"), staleN + " stale + premium tier", staleN > 0 ? "hold" : "done", AC],
          ["\xA72", A("fcst"), V("schedule"), alerts.filter((a) => a.eta <= 90).length + " pre-breach interventions", "pend", AMB],
          ["\xA73", "sensei", "rotate", blocker[1] + " blocker", "pend", AC],
          ["\xA74", A("transfer"), V("transfer"), "3 matches + 1 peer pair", "pend", GRN],
          ["\xA75", A("cap"), "govern", red.length + " red units", "hold", AC],
          ["\xA76", A("gate"), "review", "changelog + holds", "done", GRN]
        ].map(([s, ag, vb, tg, st, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Chain, { items: [{ vb: s, state: "done", c: RULE }, { ag, vb, tg, state: st, c }] }, i)),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 6 }, children: "generated from state \xB7 every op above writes here \xB7 candidate submission, proposes not enacts" })
      ] }),
      tab === "leads" && (() => {
        const stageOf = (l) => leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0;
        const territorySaturated = (l) => {
          const netState = NET_STATES.find((x) => x.s === l.region);
          const headroom = netState ? netState.h[0] : 0.3;
          const capacity = territoryCapacityOf(headroom);
          const activeInRegion = LEADS.filter((x) => x.region === l.region && (leadStage[x.id] !== void 0 ? leadStage[x.id] : x.stage0) >= 4).length;
          return activeInRegion >= capacity;
        };
        const advance = (l) => {
          const current = stageOf(l);
          if (current === 3 && territorySaturated(l)) {
            logL("leads", "Territory gate", l.n + " held at Due diligence \u2014 " + l.region + " is at its candidate-capacity ceiling this cycle");
            return;
          }
          const nx = Math.min(5, current + 1);
          setLeadStage((m) => ({ ...m, [l.id]: nx }));
          if (nx === 5) logL("leads", "Signing", l.n + " signed \u2014 flywheel pulsed: broker gains and referral flow step up \xD71.06");
          else logL("leads", "Stage gate", l.n + " advanced to " + STAGES6[nx]);
        };
        const byStage = STAGES6.map((_, si) => LEADS.filter((l) => stageOf(l) === si));
        const signed = byStage[5].length;
        const signedNew = byStage[5].filter((l) => l.dealType === "new").length;
        const signedConv = byStage[5].filter((l) => l.dealType === "conversion").length;
        const signedUS = byStage[5].filter((l) => l.market === "US").length;
        const signedCA = byStage[5].filter((l) => l.market === "Canada").length;
        const TARGET = 12;
        const TARGET_NEW = 8, TARGET_CONV = 4;
        const CONV = [0.5, 0.6, 0.7, 0.75, 0.6];
        const remain = (si) => {
          let p = 1;
          for (let k = si; k < 5; k++) p *= CONV[k];
          return p;
        };
        const expected = LEADS.reduce((a, l) => {
          const s = stageOf(l);
          return a + (s === 5 ? 1 : remain(s));
        }, 0);
        const p50 = +expected.toFixed(1), p10 = +(expected * 0.7).toFixed(1), p90 = +(expected * 1.3).toFixed(1);
        const gap = +(TARGET - p50).toFixed(1);
        const reqIntro = Math.max(0, Math.ceil((TARGET - signed) / remain(0)));
        const commission = byStage[5].reduce((a, l) => a + Math.round(l.liquidity * 0.1), 0);
        const NEXT = ["Log intro call \u25B8", "Book discovery \u25B8", "Send FDD \u25B8", "Open due diligence \u25B8", "Invite to Discovery Day \u25B8", "Move to signing \u25B8"];
        const L = leadSel !== null ? LEADS.find((l) => l.id === leadSel) : null;
        const disclosureDocLabel = L && L.market === "Canada" ? "Disclosure Document acknowledgment" : "FDD acknowledgment";
        const DOCS = [["Application", 0], ["Financial statement", 1], [disclosureDocLabel, 2], ["Due-diligence packet", 3], ["Territory check", 3], ["Budget", 4], ["Franchise Agreement", 5]];
        const proposals = (() => {
          const out = [];
          const regionActive = {};
          [...LEADS].sort((a, b) => b.fit - a.fit).forEach((l) => {
            const s = stageOf(l);
            if (s >= 5) return;
            const netState = NET_STATES.find((x) => x.s === l.region);
            const headroom = netState ? netState.h[0] : 0.3;
            const capacity = territoryCapacityOf(headroom);
            const used = regionActive[l.region] || 0;
            regionActive[l.region] = used + 1;
            if (used >= capacity) {
              out.push({ kind: "block cannibalization", cn: l.n, lever: 0, block: true, note: l.region + " territory candidate-supply ceiling reached (" + capacity + ")" });
              return;
            }
            if (l.fit < 50 && s <= 1) {
              out.push({ kind: "decline \u2014 below qualification", cn: l.n, lever: 1.2, cost: 1 });
              return;
            }
            if (s < 2) {
              out.push({ kind: "advance stage", cn: l.n, src: l.src, lever: +(1 + l.fit / 60).toFixed(2), cost: 1 });
            } else {
              out.push({ kind: "awaiting human gate", cn: l.n, lever: +(l.fit / 50).toFixed(2), gate: true });
              if (hash(l.n + "doc" + s) % 2 === 0) out.push({ kind: "chase documents", cn: l.n, lever: 0.9, cost: 1 });
            }
          });
          out.forEach((p) => {
            if (dealAg.trained[p.kind]) p.lever = +(p.lever * dealAg.trained[p.kind]).toFixed(2);
          });
          return out.sort((a, b) => b.lever - a.lever);
        })();
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, border: `1px solid ${RULE}`, borderLeft: `3px solid ${AC}`, padding: "6px 10px", marginBottom: 8, lineHeight: 1.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: AC }, children: "LEAD PIPELINE" }),
            " \xB7 the full franchise sales cycle the role owns \u2014 intro call \u2192 discovery \u2192 FDD \u2192 due diligence \u2192 Discovery Day \u2192 signing. Each prospect carries a probability split across {signs \xB7 stalls \xB7 dies} until a stage-gate verifies it. Agents advance and track; humans own every gate. Territory + financial qualification reserved to Development Function approval."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${VIO}`, background: "#fff", padding: "8px 10px", marginBottom: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 6, alignItems: "center", flexWrap: "wrap" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: VIO }, children: "Deal Agent" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: runDealWeek, style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "5px 11px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff" }, children: "Run one week" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setDealAg((A2) => ({ ...A2, auto: !A2.auto })), style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "5px 11px", cursor: "pointer", border: `1px solid ${INK}`, background: dealAg.auto ? INK : "#fff", color: dealAg.auto ? "#fff" : INK }, children: dealAg.auto ? "Pause auto" : "Run autonomously" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9 }, children: [
                "week ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: dealAg.wk }),
                " \xB7 ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: AC }, children: [
                  dealAg.blocked,
                  " blocked by governors"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: downloadReport, style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "5px 11px", cursor: "pointer", border: `1px solid ${GRN}`, background: "#fff", color: GRN, marginLeft: "auto" }, children: "\u2B07 Download pipeline CSV" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 5, lineHeight: 1.5 }, children: [
              "Advances qualified deals through the administrative stages and drafts document packages under an 8-pt weekly capacity budget; then trains \u2014 checks each action against the prospect's actual fit/quality data, promotes action types with a \u226560% hit rate (2+ samples), retires the rest. ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: AC }, children: "Stops at every human gate" }),
              " (FDD, due diligence, Discovery Day, signing) and is hard-blocked by the cannibalization + candidate-quality governors."
            ] }),
            proposals.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 12, flexWrap: "wrap", marginTop: 8 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1.2 1 260px" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AC, marginBottom: 4 }, children: "Proposed now \u2014 ranked by leverage, capacity-gated" }),
                (() => {
                  let capp = 8;
                  return proposals.slice(0, 8).map((p, i) => {
                    const afford = !p.gate && !p.block && capp - (p.cost || 1) >= 0;
                    if (afford) capp -= p.cost || 1;
                    const col = p.block ? AC : p.gate ? VIO : GRN;
                    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${col}`, padding: "5px 8px", marginBottom: 3, opacity: p.block ? 0.65 : 1 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline" }, children: [
                      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { fontFamily: "Helvetica", fontSize: 9 }, children: [
                        p.kind,
                        " ",
                        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontWeight: 400, color: MUT }, children: [
                          "\xB7 ",
                          p.cn,
                          p.src ? " \xB7 via " + p.src : ""
                        ] })
                      ] }),
                      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: col }, children: p.block ? "BLOCKED" : p.gate ? "HUMAN GATE" : "lever " + p.lever + (afford ? "" : " \xB7 deferred") })
                    ] }) }, i);
                  });
                })()
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 220px" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: VIO, marginBottom: 4 }, children: "Training memory" }),
                dealAg.promoted.map((k, i) => {
                  const s = dealAg.stats[k] || { hits: 0, attempts: 0 };
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#2f5a3a", marginBottom: 2 }, children: [
                    "\u25B2 PROMOTED \xB7 ",
                    k,
                    " ",
                    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
                      "\xD7",
                      (dealAg.trained[k] || 1).toFixed(2)
                    ] }),
                    " ",
                    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
                      s.hits,
                      "/",
                      s.attempts,
                      " correct"
                    ] })
                  ] }, i);
                }),
                dealAg.retired.map((k, i) => {
                  const s = dealAg.stats[k] || { hits: 0, attempts: 0 };
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: AC, marginBottom: 2 }, children: [
                    "\u25BC RETIRED \xB7 ",
                    k,
                    " ",
                    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
                      s.hits,
                      "/",
                      s.attempts,
                      " correct"
                    ] })
                  ] }, i);
                }),
                !dealAg.promoted.length && !dealAg.retired.length && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "run a few weeks \u2014 the agent learns which action types earn their capacity" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AC, margin: "8px 0 3px" }, children: "Execution log" }),
                dealAg.log.map((l, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Courier,monospace", fontSize: 8, color: "#444", marginBottom: 1, lineHeight: 1.4 }, children: l }, i)),
                !dealAg.log.length && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "nothing executed yet" })
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 3, marginBottom: 8, flexWrap: "wrap" }, children: STAGES6.map((st, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 90px", border: `1px solid ${RULE}`, borderTop: `3px solid ${i === 5 ? GRN : i === 0 ? MUT : AMB}`, padding: "5px 7px", background: "#fff" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 7.5, fontWeight: 700, letterSpacing: 0.4, textTransform: "uppercase", color: MUT }, children: st }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 19, fontWeight: 800, color: i === 5 ? GRN : INK }, children: byStage[i].length })
          ] }, i)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${VIO}`, background: "#fff", padding: "9px 11px", marginBottom: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: VIO, marginBottom: 6 }, children: "Quota forecast \u2014 annual target back-solved" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 12, flexWrap: "wrap", marginBottom: 8 }, children: [["Target", TARGET, INK], ["Signed", signed, GRN], ["Pipeline P50", p50, gap > 0 ? AC : GRN], ["Gap", gap > 0 ? "\u2212" + gap : "+" + Math.abs(gap), gap > 0 ? AC : GRN]].map(([l, v, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 80px" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: MUT, textTransform: "uppercase" }, children: l }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 20, fontWeight: 800, color: c }, children: v })
            ] }, i)) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { position: "relative", height: 22, background: "#f0ede6", borderRadius: 2, marginBottom: 14, marginTop: 14 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", left: p10 / (TARGET * 1.6) * 100 + "%", width: (p90 - p10) / (TARGET * 1.6) * 100 + "%", height: 22, background: "rgba(122,107,216,0.25)" } }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", left: p50 / (TARGET * 1.6) * 100 + "%", width: 2, height: 22, background: VIO } }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", left: TARGET / (TARGET * 1.6) * 100 + "%", width: 2, height: 22, background: AC } }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", left: TARGET / (TARGET * 1.6) * 100 + "%", top: -12, fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: AC, transform: "translateX(-50%)" }, children: "target" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", left: p50 / (TARGET * 1.6) * 100 + "%", top: 24, fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: VIO, transform: "translateX(-50%)" }, children: "P50" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: [
              "P10 ",
              p10,
              " \xB7 P50 ",
              p50,
              " \xB7 P90 ",
              p90,
              " signings \xB7 ~5% intro\u2192sign conversion \xB7 ",
              gap > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: AC }, children: [
                "reload top of funnel: ~",
                reqIntro,
                " intro leads to clear target"
              ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: GRN }, children: "pipeline clears target at P50" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 3 }, children: [
              "Signed this cycle: ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: INK }, children: signedNew }),
              " new opening",
              signedNew === 1 ? "" : "s",
              " of ",
              TARGET_NEW,
              " target \xB7 ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: INK }, children: signedConv }),
              " conversion",
              signedConv === 1 ? "" : "s",
              " of ",
              TARGET_CONV,
              " target \xB7 ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: INK }, children: signedUS }),
              " US \xB7 ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: INK }, children: signedCA }),
              " Canada"
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 6, marginBottom: 8, flexWrap: "wrap" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 130px", border: `1px solid ${RULE}`, background: "#fff", padding: "7px 9px" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: GRN, letterSpacing: 0.6 }, children: "COMMISSION (10% IFF)" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800 }, children: [
                "$",
                commission,
                "k",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 8, color: MUT }, children: " proxy" })
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 130px", border: `1px solid ${RULE}`, background: "#fff", padding: "7px 9px" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: AMB, letterSpacing: 0.6 }, children: "HOT PROSPECTS" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800 }, children: LEADS.filter((l) => l.hot && stageOf(l) < 5).length })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 130px", border: `1px solid ${RULE}`, background: "#fff", padding: "7px 9px" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: AC, letterSpacing: 0.6 }, children: "IN PIPELINE" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800 }, children: LEADS.filter((l) => stageOf(l) < 5).length })
            ] })
          ] }),
          L && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", padding: "9px 11px", marginBottom: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", flexWrap: "wrap" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700 }, children: [
                L.n,
                " \u2014 document flow ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontSize: 8.5, color: MUT, fontWeight: 400 }, children: [
                  "\xB7 stage: ",
                  STAGES6[stageOf(L)]
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: L.market === "Canada" ? VIO : MUT, marginBottom: 2 }, children: [
                L.market,
                " \xB7 ",
                L.dealType === "conversion" ? "conversion (proven operator)" : "new opening",
                " \xB7 ",
                disclosureLawOf(L.region)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { onClick: () => setLeadSel(null), style: { fontFamily: "Helvetica", fontSize: 11, color: MUT, cursor: "pointer" }, children: "close \xD7" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: VIO, margin: "6px 0 3px" }, children: "Deal state \u2014 probability split across signs \xB7 stalls \xB7 dies" }),
            (() => {
              const s = stageOf(L);
              const base = L.fit / 100, prog = s / 5;
              let signs = Math.max(0.05, base * 0.5 + prog * 0.5), dies = Math.max(0.05, (1 - base) * 0.5 + (1 - prog) * 0.3), stalls = Math.max(0.05, 1 - Math.abs(signs - dies));
              const t = signs + stalls + dies;
              const amp = [signs / t, stalls / t, dies / t];
              const resolved = s === 5;
              const dc = ["#2f7a3f", "#b8860b", "#8b0000"], dn = ["signs", "stalls", "dies"];
              return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 6 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", height: 14, borderRadius: 2, overflow: "hidden", marginBottom: 3 }, children: amp.map((a, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { width: a * 100 + "%", background: dc[i], opacity: resolved ? i === 0 ? 1 : 0.2 : 0.85 }, title: dn[i] }, i)) }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: resolved ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: "#2f7a3f" }, children: "verified \u2192 signed" }),
                  " \xB7 the signing gate confirmed this deal to a record"
                ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                  dn.map((n, i) => pct(amp[i]) + "% " + n).join(" \xB7 "),
                  " \xB7 each stage-gate is a checkpoint that updates the split"
                ] }) })
              ] });
            })(),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginBottom: 5 }, children: "coordination of documents to corporate for processing + approvals \u2014 the role's heaviest operational load" }),
            DOCS.map(([dn, rs], i) => {
              const s = stageOf(L);
              const done = rs < s || rs <= s && s === 5;
              const pending = !done && (rs === s || rs === s + 1);
              const overdue = pending && hash(L.n + dn) % 3 === 0;
              const status = done ? "SUBMITTED \u2713" : overdue ? "OVERDUE \u26A0" : pending ? "PENDING" : "NOT YET";
              const col = done ? GRN : overdue ? AC : pending ? AMB : "#bbb";
              return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, padding: "4px 0", borderTop: i ? `1px solid #f0f0f0` : "none" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 8, height: 8, borderRadius: 4, background: col, flexShrink: 0 } }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9.5, flex: 1 }, children: dn }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: [
                  "req @ ",
                  STAGES6[rs]
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 8, color: col, width: 76, textAlign: "right" }, children: status })
              ] }, i);
            }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 5 }, children: "agent flags overdue artifacts + drafts the corporate-submission package \xB7 never submits without human sign-off \xB7 FDD + due-diligence gates are non-separable" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: AC, marginBottom: 2 }, children: "Prospect book \u2014 ranked by fit \xB7 tap a card to open its document flow" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginBottom: 5 }, children: "sample prospects are illustrative composites, sourced across the real channel mix (broker networks, franchise portals, direct referrals) \u2014 names are fictional; the live CRM feed replaces this book on integration" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fill,minmax(200px,1fr))", gap: 6 }, children: LEADS.map((l) => {
            const si = stageOf(l);
            const fc = l.fit >= 78 ? GRN : l.fit >= 60 ? AMB : AC;
            const done = si === 5;
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => setLeadSel(leadSel === l.id ? null : l.id), style: { border: `1px solid ${leadSel === l.id ? INK : RULE}`, borderLeft: `4px solid ${fc}`, background: done ? "#f7fbf8" : leadSel === l.id ? "#fbf9f4" : "#fff", padding: "8px 10px", cursor: "pointer" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 10.5 }, children: l.n }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: fc }, children: [
                  "FIT ",
                  l.fit
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 1 }, children: [
                l.src,
                " \xB7 ",
                l.region
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: "#444", marginTop: 3 }, children: [
                "liquidity $",
                l.liquidity,
                "k \xB7 net $",
                l.net,
                "k ",
                l.proven && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: GRN, fontWeight: 700 }, children: "\xB7 proven" }),
                l.multi && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: VIO, fontWeight: 700 }, children: " \xB7 multi-unit" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: l.hot ? AC : MUT, marginTop: 1, fontStyle: "italic" }, children: l.note }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 2, margin: "6px 0 4px" }, children: STAGES6.map((_, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1, height: 4, background: i <= si ? done ? GRN : AMB : "#e8e4da" }, title: STAGES6[i] }, i)) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: done ? GRN : INK }, children: STAGES6[si] }),
                !done ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: (e) => {
                  e.stopPropagation();
                  advance(l);
                }, style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, padding: "3px 8px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff" }, children: NEXT[si] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: GRN }, children: [
                  "\u2713 signed +$",
                  Math.round(l.liquidity * 0.1),
                  "k"
                ] })
              ] })
            ] }, l.id);
          }) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 8 }, children: "fit + financials illustrative (seeded) \xB7 cycle mirrors the posting exactly \xB7 governors: candidate quality + cannibalization \u2014 no target may cross them \xB7 proven operators carry ramp inheritance into open territory" })
        ] });
      })(),
      tab === "expansion" && (() => {
        const so = (l) => leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0;
        const signed = LEADS.filter((l) => so(l) === 5).length;
        const active = LEADS.filter((l) => so(l) < 5);
        const hnw = LEADS.filter((l) => l.net >= 750 && l.liquidity >= 250);
        const BROKERS = [
          { n: "FranNet \u2014 West", deals: 3, quality: 0.82, fee: "20% IFF" },
          { n: "IFPG Network", deals: 5, quality: 0.74, fee: "18% IFF" },
          { n: "BAI Franchise Brokers", deals: 2, quality: 0.68, fee: "20% IFF" },
          { n: "Direct / referral", deals: 4, quality: 0.88, fee: "\u2014" }
        ].map((b) => ({ ...b, gain: +(b.quality * (1 + (signed + reso.signed) * 0.06)).toFixed(2) }));
        const srcMix = {};
        LEADS.forEach((l) => {
          srcMix[l.src] = (srcMix[l.src] || 0) + 1;
        });
        const DDAYS = [
          { d: "Jul 17", seats: 6, conf: 4, stage: "invites out" },
          { d: "Aug 14", seats: 6, conf: 1, stage: "building cohort" },
          { d: "Sep 11", seats: 6, conf: 0, stage: "scheduled" }
        ];
        const FDD = [
          ["FDD delivery \u226514 calendar days before signing or payment", "FTC Franchise Rule 16 CFR 436", true],
          ["Item 23 receipt collected and filed per candidate", "16 CFR 436.7", true],
          ["Amended FDD re-disclosure on material change", "16 CFR 436", true],
          ["Registration-state filings current (CA, NY, IL, ...)", "state franchise acts", reso.resolved],
          ["Financial performance representations limited to Item 19", "16 CFR 436.5(s)", true],
          ["Broker disclosure obligations flowed down in agreements", "FTC + state rules", reso.resolved]
        ];
        const fddOk = FDD.filter((f) => f[2]).length;
        const flywheelGain = +(1 + (signed + reso.signed) * 0.06).toFixed(2);
        const runGrowthWeek = () => {
          setReso((R) => {
            if (R.wk >= 16) return R;
            const clear = FDD.every((f) => f[2]);
            const chan = { ...R.chan };
            let sw = 0;
            const notes = [];
            BROKERS.forEach((b) => {
              const c = { ...chan[b.n] };
              const arrivals = Math.max(0, Math.round(b.gain * 2.2 - 1));
              c.p += arrivals;
              const qual = Math.min(c.p, Math.round(c.p * 0.3));
              c.p -= qual;
              c.ql += qual;
              if (clear && c.ql > 0) {
                const s = Math.min(c.ql, 1);
                c.ql -= s;
                sw += s;
                notes.push(b.n + " +" + s + " signing");
              }
              chan[b.n] = c;
            });
            const signedT = R.signed + sw;
            const gain = +(1 + (signed + signedT) * 0.06).toFixed(2);
            const placed = { ...R.placed };
            if (sw > 0) {
              const counts = Object.keys(states).map((st) => [st, states[st].length + (placed[st] || 0)]).sort((a, b) => a[1] - b[1]);
              const open = counts.slice(0, Math.max(4, Math.floor(counts.length * 0.75)));
              for (let k = 0; k < sw; k++) {
                const [st] = open[k % open.length];
                placed[st] = (placed[st] || 0) + 1;
              }
              logL("expansion", "Territory", sw + " opening" + (sw > 1 ? "s" : "") + " committed to whitespace: " + Object.entries(placed).slice(-3).map(([st, n]) => st).join(", ") + " \u2014 saturated states closed by the cannibalization governor");
            }
            if (sw > 0) logL("expansion", "Momentum", sw + " signing" + (sw > 1 ? "s" : "") + " this cycle \u2014 channel gain pumped to \xD7" + gain);
            else logL("expansion", "Governor", clear ? "cycle ran \u2014 no candidates cleared qualification this week" : "cycle ran \u2014 signings held: compliance items open");
            return { ...R, wk: R.wk + 1, signed: signedT, hist: [...R.hist, gain].slice(-17), chan, placed };
          });
        };
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, border: `1px solid ${RULE}`, borderLeft: `3px solid ${AC}`, padding: "6px 10px", marginBottom: 8, lineHeight: 1.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: AC }, children: "EXPANSION ENGINE" }),
            " \xB7 Objective: expand the brand's footprint by building lead pipelines and recruiting qualified, high-net-worth franchisees \u2014 generation through final contract closing, FDD and federal compliance throughout, discovery days and broker networks managed as owned channels."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 6, marginBottom: 8, flexWrap: "wrap" }, children: [["Signed YTD", signed, GRN], ["Active candidates", active.length, INK], ["HNW-qualified", hnw.length, VIO], ["Flywheel gain", "\xD7" + flywheelGain, AMB], ["Compliance", fddOk + "/" + FDD.length, fddOk === FDD.length ? GRN : AC]].map(([l, v, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 110px", border: `1px solid ${RULE}`, background: "#fff", padding: "7px 9px" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 7.5, fontWeight: 700, letterSpacing: 0.5, color: MUT, textTransform: "uppercase" }, children: l }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 18, fontWeight: 800, color: c }, children: v })
          ] }, i)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, flexWrap: "wrap", marginBottom: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1.2 1 280px", border: `1px solid ${RULE}`, borderLeft: `3px solid ${VIO}`, background: "#fff", padding: "8px 10px" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.7, textTransform: "uppercase", color: VIO, marginBottom: 5 }, children: [
                "Broker network \u2014 ",
                "referral channels, gain compounds with signings"
              ] }),
              BROKERS.map((b, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 5 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", fontFamily: "Helvetica", fontSize: 9.5 }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: b.n }),
                    " ",
                    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT, fontSize: 8 }, children: [
                      "\xB7 ",
                      b.deals,
                      " in pipeline \xB7 ",
                      b.fee
                    ] })
                  ] }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: b.gain >= 0.8 ? GRN : b.gain >= 0.7 ? AMB : AC }, children: [
                    "gain ",
                    b.gain
                  ] })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 5, background: "#eee" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 5, width: Math.min(100, b.gain * 100) + "%", background: b.gain >= 0.8 ? GRN : b.gain >= 0.7 ? AMB : AC, transition: "width .4s" } }) })
              ] }, i)),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
                logL(tab, "Broker", BROKERS[0].n + " quarterly review scheduled \u2014 pipeline audit + fee reconciliation");
                setOpsMsg("Broker review on the record \u2014 see RECORD tab");
              }, style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "5px 10px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", marginTop: 3 }, children: "Schedule broker review \u25B8" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 240px", border: `1px solid ${RULE}`, borderLeft: `3px solid ${AMB}`, background: "#fff", padding: "8px 10px" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.7, textTransform: "uppercase", color: AMB, marginBottom: 5 }, children: "Discovery days \u2014 hosted cohorts" }),
              DDAYS.map((d, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", fontFamily: "Helvetica", fontSize: 9.5, padding: "4px 0", borderTop: i ? "1px solid #f0f0f0" : "none" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: d.d }),
                  " ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT, fontSize: 8 }, children: [
                    "\xB7 ",
                    d.stage
                  ] })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: d.conf >= 4 ? GRN : d.conf >= 2 ? AMB : MUT, fontWeight: 700 }, children: [
                  d.conf,
                  "/",
                  d.seats,
                  " confirmed"
                ] })
              ] }, i)),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
                logL(tab, "Discovery Day", "Jul 17 cohort \u2014 confirmations pushed, FDD delivery dates verified for all attendees");
                setOpsMsg("Discovery Day prep logged \u2014 FDD 14-day clocks verified");
              }, style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "5px 10px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", marginTop: 5 }, children: "Run cohort prep \u25B8" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 4 }, children: [
                "lead source mix: ",
                Object.entries(srcMix).map(([s, n]) => s + " " + n).join(" \xB7 ")
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${GRN}`, background: "#fff", padding: "8px 10px", marginBottom: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", flexWrap: "wrap", gap: 6 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.7, textTransform: "uppercase", color: GRN }, children: "Growth loop \u2014 lead generation through closing, on cadence" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: "channel simulation for capacity planning \xB7 the prospect book above remains the real pipeline" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap", margin: "6px 0" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: runGrowthWeek, style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "5px 11px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff" }, children: "Run growth week \u25B8" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9 }, children: [
                "cycle ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: reso.wk }),
                " \xB7 ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: GRN }, children: reso.signed }),
                " signed via loop \xB7 gain ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: AMB }, children: [
                  "\xD7",
                  flywheelGain
                ] })
              ] }),
              !reso.resolved && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
                setReso((R) => ({ ...R, resolved: true }));
                logL("expansion", "Compliance", "state filings + broker flow-downs resolved \u2014 signing governor released");
              }, style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "5px 10px", cursor: "pointer", border: `1px solid ${AC}`, background: "#fff", color: AC }, children: "Resolve open compliance items \u25B8" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 10, flexWrap: "wrap" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 200px" }, children: [
                BROKERS.map((b, i) => {
                  const c = reso.chan[b.n] || { p: 0, ql: 0 };
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", fontFamily: "Helvetica", fontSize: 8.5, padding: "2px 0", borderTop: i ? "1px solid #f0f0f0" : "none" }, children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: b.n }),
                    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
                      c.p,
                      " prospecting \xB7 ",
                      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: INK }, children: [
                        c.ql,
                        " qualified"
                      ] })
                    ] })
                  ] }, i);
                }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 7.5, color: MUT, marginTop: 3 }, children: [
                  "arrivals \u221D channel gain \xB7 ~30% clear the fit + HNW screen \xB7 one signing per channel per cycle (Discovery Day throughput) \xB7 ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: AC }, children: "zero signings while any compliance item is open" })
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: "1 1 160px" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 170 44", style: { width: "100%", height: "auto" }, children: [
                reso.hist.map((g, i) => {
                  if (i === 0) return null;
                  const x1 = 6 + (i - 1) * (158 / 16), x2 = 6 + i * (158 / 16);
                  const y = (v) => 40 - Math.min(36, (v - 1) * 60);
                  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1, y1: y(reso.hist[i - 1]), x2, y2: y(g), stroke: "#b8860b", strokeWidth: "1.6" }, i);
                }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("text", { x: "6", y: "10", style: { font: "6.5px Helvetica", fill: "#999" }, children: [
                  "flywheel gain over cycles",
                  " \xB7 now \xD7",
                  flywheelGain
                ] })
              ] }) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 170px" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 7.5, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: MUT, marginBottom: 3 }, children: "Territory placement \u2014 whitespace first" }),
                Object.keys(reso.placed).length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "no committed openings yet \u2014 signings route to the least-served states" }),
                Object.entries(reso.placed).sort((a, b) => b[1] - a[1]).slice(0, 6).map(([st, n], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", fontFamily: "Helvetica", fontSize: 8.5, padding: "1px 0" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: st }),
                    " ",
                    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
                      "\xB7 ",
                      (states[st] || []).length,
                      " existing"
                    ] })
                  ] }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: GRN }, children: [
                    "+",
                    n
                  ] })
                ] }, i)),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 3 }, children: "saturated states (top quartile by unit count) closed by the cannibalization governor \xB7 committed openings appear in the masthead network count" })
              ] })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${fddOk === FDD.length ? GRN : AC}`, background: "#fff", padding: "8px 10px", marginBottom: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.7, textTransform: "uppercase", color: fddOk === FDD.length ? GRN : AC, marginBottom: 5 }, children: "FDD & federal compliance \u2014 hard governors on the flywheel" }),
            FDD.map(([t, cite, ok], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, padding: "3px 0", borderTop: i ? "1px solid #f0f0f0" : "none" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 8, height: 8, borderRadius: 4, background: ok ? GRN : AC, flexShrink: 0 } }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9.5, flex: 1 }, children: t }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: cite }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 8, color: ok ? GRN : AC, width: 64, textAlign: "right" }, children: ok ? "CURRENT" : "ACTION REQ" })
            ] }, i)),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 5 }, children: [
              "no signing proceeds while a compliance item is open \u2014 the flywheel is throttled by the governors, by design",
              " \xB7 demonstration checklist pending counsel review"
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: "reads the live lead pipeline \xB7 broker/cohort actions write the shared record ledger \xB7 candidate submission \u2014 proposes, does not enact" })
        ] });
      })(),
      tab === "lenses" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SixLensTab, { centers, states, leads: LEADS, railData, opt, setOpt, logL, focusCenter, jumpReason, clearJumpReason: () => setJumpReason(null) }) }),
      tab === "rail" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DecisionRailTab, { railData, logL, decisions, setDecisions, decisionHistory, recordDecision, jumpTo, opt }),
      tab === "board" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(EngineErrorBoundary, { children: [
        boardOverrideActive && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${VIO}`, borderLeft: `3px solid ${VIO}`, background: "#faf9ff", padding: "6px 10px", marginBottom: 10, fontFamily: "Helvetica", fontSize: 9.5, color: "#3d3470" }, children: [
          "This tab is locally overridden to ",
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: boardOverride }),
          " \u2014 network-wide posture is ",
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: boardGlobalPosture }),
          ". Proposals below reflect the override. Set from Quantum PM."
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OperationsBoardTab, { railData: boardRailData, decisions, setDecisions, decisionHistory, recordDecision, logL, opt, dyn, jumpTo })
      ] }),
      tab === "alignment" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SystemAlignmentTab, { centers, states, railData, decisions, dyn, opt, ledger, jumpTo }),
      tab === "dynamics" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OperationsDynamicsTab, { centers, states, opt, setOpt, logL, dyn, setDyn, focusCenter, railData, jumpTo }),
      tab === "compliance" && (() => {
        const heldComp = centers.filter((c) => c.compliance === false);
        const heldStaff = centers.filter((c) => c.staffCleared === false);
        const heldEither = centers.filter((c) => c.compliance === false || c.staffCleared === false);
        const cleared = centers.length - heldEither.length;
        const byState = {};
        heldEither.forEach((c) => {
          byState[c.st] = (byState[c.st] || 0) + 1;
        });
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: "The child-safety guardrail reads two fields on every unit \u2014 compliance and staff clearance \u2014 and blocks any proposed action on a unit where either is open, before a Director ever sees it. This view is the network-wide roll-up of that same real check, not a separate count." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap", border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", marginBottom: 10 }, children: [[centers.length, "total units"], [cleared, "fully cleared", GRN], [heldComp.length, "compliance item open", AC], [heldStaff.length, "staff clearance open", AC]].map(([v, l, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 150px", padding: "8px 12px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 20, fontWeight: 800, color: c || INK }, children: v }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 2 }, children: l })
          ] }, i)) }),
          heldEither.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: GRN, border: `1px solid ${RULE}`, padding: "8px 11px" }, children: [
            "All ",
            centers.length,
            " units currently clear \u2014 checked, none open."
          ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: [
              "Units with an open item (",
              heldEither.length,
              ")"
            ] }),
            heldEither.map((c) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "6px 11px", borderTop: `1px solid #f0f0f0` }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700 }, children: c.name }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginLeft: 8 }, children: c.st }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9, color: AC, marginLeft: 8 }, children: [
                  c.compliance === false ? "compliance" : "",
                  c.compliance === false && c.staffCleared === false ? " \xB7 " : "",
                  c.staffCleared === false ? "staff clearance" : ""
                ] })
              ] }),
              jumpTo && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => jumpTo("lenses", c.name, "reviewing an open compliance item on " + c.name), style: { fontFamily: "Helvetica", fontSize: 9, color: AC, cursor: "pointer", background: "none", border: "none", padding: 0, textDecoration: "underline" }, children: "view in Six Lenses \u2192" })
            ] }, c.name))
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 8 }, children: [
            "By state: ",
            Object.keys(byState).length ? Object.entries(byState).map(([st, n]) => st + " (" + n + ")").join(", ") : "none"
          ] })
        ] });
      })(),
      tab === "workload" && (() => {
        const recs = railData.recommendations;
        const workload = {};
        recs.forEach((r) => {
          const role = r.approverRole;
          workload[role] = workload[role] || { pending: 0, decided: 0, heldForCapacity: 0 };
          if (decisions[r.id]) workload[role].decided++;
          else workload[role].pending++;
          if (r.supplyGate && r.supplyGate.blocked && /capacity/.test(r.supplyGate.reason || "")) workload[role].heldForCapacity++;
        });
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: [
            "Every proposal names an approver role and counts against that role's weekly capacity ceiling \u2014 the same ",
            "{FBC:10, Owner:8, Director:12}",
            " limits the Growth, Unit Health, and Retention agents themselves respect. This is that capacity read as its own view, not a strip buried in Decision Rail."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 10 }, children: Object.entries(workload).map(([role, w]) => {
            const cap2 = ROLE_CAPACITY[role] || 12;
            const used = w.decided + w.pending;
            const pct2 = Math.min(100, Math.round(used / cap2 * 100));
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 220px", border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", marginBottom: 4 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 12 }, children: role }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: [
                  "cap ",
                  cap2,
                  "/wk"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 6, background: "#eee", borderRadius: 3, marginBottom: 6 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 6, width: pct2 + "%", borderRadius: 3, background: pct2 >= 100 ? AC : pct2 >= 75 ? AMB : GRN } }) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#444" }, children: [
                w.decided,
                " decided \xB7 ",
                w.pending,
                " pending",
                w.heldForCapacity ? " \xB7 " + w.heldForCapacity + " held for capacity" : ""
              ] })
            ] }, role);
          }) }),
          jumpTo && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setTab("rail"), style: { marginTop: 10, fontFamily: "Helvetica", fontSize: 9, color: AC, cursor: "pointer", background: "none", border: "none", padding: 0, textDecoration: "underline" }, children: "view full proposal queue in Decision Rail \u2192" }),
          (() => {
            const QUARTER_CEILING = 45;
            const DISCOVERY_DAY_COST = 2.4;
            const stageOfLive = (l) => leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0;
            const discoveryDaysRun = LEADS.filter((l) => stageOfLive(l) >= 4).length;
            const discoveryCost = +(discoveryDaysRun * DISCOVERY_DAY_COST).toFixed(1);
            const signedLeads = LEADS.filter((l) => stageOfLive(l) === 5);
            const brokerCommission = signedLeads.reduce((a, l) => a + Math.round(l.liquidity * 0.1), 0);
            const travelSpend = 8.5;
            const spent = +(discoveryCost + brokerCommission + travelSpend).toFixed(1);
            const remaining = +(QUARTER_CEILING - spent).toFixed(1);
            const pct2 = Math.min(100, Math.round(spent / QUARTER_CEILING * 100));
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 14, border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 4 }, children: "Department operating budget \u2014 this quarter" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 6, background: "#eee", borderRadius: 3, marginBottom: 6 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 6, width: pct2 + "%", borderRadius: 3, background: pct2 >= 100 ? AC : pct2 >= 80 ? AMB : GRN } }) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", flexWrap: "wrap", gap: 14, fontFamily: "Helvetica", fontSize: 9, color: "#444" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                  "$",
                  spent,
                  "k spent of $",
                  QUARTER_CEILING,
                  "k ceiling"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                  "$",
                  remaining,
                  "k remaining"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                  "travel $",
                  travelSpend,
                  "k"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                  "Discovery Day $",
                  discoveryCost,
                  "k (",
                  discoveryDaysRun,
                  " run)"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                  "broker commission $",
                  brokerCommission,
                  "k (",
                  signedLeads.length,
                  " signed)"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 5 }, children: "Illustrative departmental spend, tracked separately from franchisee unit economics \u2014 broker commission and Discovery Day counts are computed from the real pipeline above, not assumed." })
            ] });
          })()
        ] });
      })(),
      tab === "whitespace" && (() => {
        const covered = new Set(Object.keys(states));
        const rows = NET_STATES.map((ns) => {
          const cs = states[ns.s] || [];
          const headroom = ns.h[0];
          const score = headroom * (1 / (1 + cs.length));
          return { st: ns.s, headroom, n: cs.length, score };
        }).sort((a, b) => b.score - a.score);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: "The Network Map shows where units already are. This is the forward-looking counterpart: territories ranked by modeled headroom against how few units are already there \u2014 the same headroom figure the Growth agent's territory-capacity gate reads, not a separate estimate." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { flex: 1 }, children: "Territory" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 90 }, children: "Headroom" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 90 }, children: "Current units" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 110 }, children: "White-space score" })
            ] }),
            rows.slice(0, 15).map((r) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", padding: "5px 11px", borderTop: "1px solid #f0f0f0" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { flex: 1, fontFamily: "Helvetica", fontSize: 10, fontWeight: 700 }, children: r.st }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { width: 90, fontFamily: "Helvetica", fontSize: 9.5 }, children: [
                Math.round(r.headroom * 100),
                "%"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 90, fontFamily: "Helvetica", fontSize: 9.5 }, children: r.n || "\u2014" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 110, fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, color: r.score > 0.15 ? GRN : INK }, children: r.score.toFixed(3) })
            ] }, r.st))
          ] }),
          jumpTo && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setTab("leads"), style: { marginTop: 10, fontFamily: "Helvetica", fontSize: 9, color: AC, cursor: "pointer", background: "none", border: "none", padding: 0, textDecoration: "underline" }, children: "view active candidates in Growth Pipeline \u2192" })
        ] });
      })(),
      tab === "financials" && (() => {
        const globalPosture = opt.quantum && opt.quantum.approved || "realistic";
        const finOverride = opt.quantum && opt.quantum.overrides && opt.quantum.overrides.financials;
        const overrideActive = !!(finOverride && finOverride !== globalPosture);
        const financialsCenters = overrideActive ? computeCentersForPosture(rawCenters, adj, finOverride, opt.week) : centers;
        const totals = financialsCenters.reduce((a, c) => {
          const r = royaltyOf(c);
          a.grossRev += r.grossRev;
          a.royalty += r.royalty;
          a.brandFund += r.brandFund;
          a.margin += c.eb;
          return a;
        }, { grossRev: 0, royalty: 0, brandFund: 0, margin: 0 });
        const byState = {};
        financialsCenters.forEach((c) => {
          const r = royaltyOf(c);
          byState[c.st] = (byState[c.st] || 0) + r.royalty;
        });
        const topStates = Object.entries(byState).sort((a, b) => b[1] - a[1]).slice(0, 8);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          overrideActive && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${VIO}`, borderLeft: `3px solid ${VIO}`, background: "#faf9ff", padding: "6px 10px", marginBottom: 8, fontFamily: "Helvetica", fontSize: 9.5, color: "#3d3470" }, children: [
            "This tab is locally overridden to ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: finOverride }),
            " \u2014 network-wide posture is ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: globalPosture }),
            ". Summed unit margin reflects the override (posture shifts ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("code", { children: "eb" }),
            " directly); gross revenue, royalty, and brand fund below are driven by enrolled students, which no posture in this model adjusts, so those three stay the same under any override. Set from Quantum PM."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: "Every figure below sums the same per-unit royalty calculation Six Lenses shows for one center at a time \u2014 aggregated here across the whole network rather than read one unit at a time." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap", border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", marginBottom: 10 }, children: [["$" + totals.grossRev.toFixed(0) + "k", "modeled gross revenue"], ["$" + totals.royalty.toFixed(0) + "k", "royalty (8%)"], ["$" + totals.brandFund.toFixed(0) + "k", "brand fund (2%)"], ["$" + totals.margin.toFixed(0) + "k", "summed unit margin"]].map(([v, l], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 150px", padding: "8px 12px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 18, fontWeight: 800 }, children: v }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 2 }, children: l })
          ] }, i)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 5 }, children: "Top territories by royalty contribution" }),
          topStates.map(([st, v]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, marginBottom: 3 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 36, fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700 }, children: st }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1, height: 6, background: "#eee", borderRadius: 3 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 6, width: Math.min(100, v / topStates[0][1] * 100) + "%", borderRadius: 3, background: VIO } }) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { width: 60, textAlign: "right", fontFamily: "Helvetica", fontSize: 9.5 }, children: [
              "$",
              v.toFixed(0),
              "k"
            ] })
          ] }, st)),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 8 }, children: "Modeled/illustrative pending live MyStudio and QuickBooks integration, same as every other financial figure in this artifact." })
        ] });
      })(),
      tab === "audit" && (() => {
        const decidedIds = Object.keys(decisions);
        const sourceTabs = Array.from(new Set(ledger.map((e) => e.tab))).sort();
        const q2 = auditQ.trim().toLowerCase();
        const filtered = ledger.filter(
          (e) => (auditTabFilter === "all" || e.tab === auditTabFilter) && (!q2 || e.actor.toLowerCase().includes(q2) || e.text.toLowerCase().includes(q2) || e.tab.toLowerCase().includes(q2))
        );
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: "Every governed action in this session in one place: the session ledger, and the full status history behind each decision \u2014 the same records System Alignment summarizes, shown here as a complete trail rather than a count." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", marginBottom: 10 }, children: [[ledger.length, "logged actions (session, capped 40)"], [decidedIds.length, "decisions recorded"], [Object.keys(dyn.committed || {}).length, "support paths committed"], [Object.keys(dyn.completed || {}).length, "marked complete"]].map(([v, l], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 150px", padding: "8px 12px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 18, fontWeight: 800 }, children: v }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 2 }, children: l })
          ] }, i)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap", marginBottom: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { value: auditQ, onChange: (e) => setAuditQ(e.target.value), placeholder: "Search actor or action text\u2026", "aria-label": "Search audit ledger", style: { fontFamily: "Helvetica", fontSize: 9.5, padding: "5px 8px", border: `1px solid ${RULE}`, minWidth: 220 } }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("select", { value: auditTabFilter, onChange: (e) => setAuditTabFilter(e.target.value), "aria-label": "Filter audit ledger by source tab", style: { fontFamily: "Helvetica", fontSize: 9.5, padding: "5px 8px", border: `1px solid ${RULE}`, background: "#fff" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("option", { value: "all", children: [
                "All source tabs (",
                ledger.length,
                ")"
              ] }),
              sourceTabs.map((t) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("option", { value: t, children: [
                t,
                " (",
                ledger.filter((e) => e.tab === t).length,
                ")"
              ] }, t))
            ] }),
            (auditQ || auditTabFilter !== "all") && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
              setAuditQ("");
              setAuditTabFilter("all");
            }, style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "5px 9px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: MUT }, children: "clear filters" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginLeft: "auto" }, children: [
              filtered.length,
              " of ",
              ledger.length,
              " shown"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => downloadJSON(decisionLedgerExport(decisions, railData), "cn-decision-ledger.json"), title: "Export the governed decision record as JSON", style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "5px 9px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: MUT }, children: "export ledger \u2193" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: [
              "Session ledger, most recent first",
              auditTabFilter !== "all" || q2 ? " \u2014 filtered" : ""
            ] }),
            filtered.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { padding: "10px 11px", fontFamily: "Helvetica", fontSize: 9.5, color: MUT }, children: ledger.length === 0 ? "No actions logged yet this session." : "No logged actions match this filter." }) : filtered.map((e, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "5px 11px", borderTop: i ? "1px solid #f0f0f0" : "none", display: "flex", gap: 8 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: MUT, width: 70, textTransform: "uppercase" }, children: e.tab }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#333" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
                  e.actor,
                  ":"
                ] }),
                " ",
                e.text
              ] })
            ] }, i))
          ] })
        ] });
      })(),
      tab === "success" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const sr = successRateData(dyn);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: 'Completion rate by support-plan type, computed from the same commit/complete record Operations Dynamics writes to and Audit Trail counts \u2014 grouped here by plan instead of by center. A plan is "complete" only when a Director has explicitly marked it so; nothing here infers completion from elapsed time.' }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", border: `1px solid ${RULE}`, borderLeft: `3px solid ${sr.overallRate >= 60 ? GRN : sr.overallRate >= 35 ? AMB : AC}`, background: "#fff", marginBottom: 10 }, children: [[sr.totalCommitted, "support paths committed, all time this session"], [sr.totalCompleted, "marked complete"], [sr.overallRate + "%", "overall completion rate"]].map(([v, l], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 150px", padding: "8px 12px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 18, fontWeight: 800 }, children: v }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 2 }, children: l })
          ] }, i)) }),
          sr.rows.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "14px", fontFamily: "Helvetica", fontSize: 10, color: MUT }, children: "No support paths committed yet this session \u2014 commit one from Operations Dynamics to populate this dashboard." }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: "By support-plan type" }),
            sr.rows.map((r, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "9px 11px", borderTop: i ? "1px solid #f0f0f0" : "none" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", flexWrap: "wrap", gap: 6 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 11, color: INK }, children: r.n }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: r.rate >= 60 ? GRN : r.rate >= 35 ? AMB : AC }, children: [
                  r.completed,
                  " / ",
                  r.committed,
                  " complete \xB7 ",
                  r.rate,
                  "%"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { background: "#f0f0ee", height: 6, borderRadius: 3, marginTop: 5, overflow: "hidden" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { width: r.rate + "%", height: "100%", background: r.rate >= 60 ? GRN : r.rate >= 35 ? AMB : AC } }) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 4 }, children: [
                r.centers.slice(0, 6).join(" \xB7 "),
                r.centers.length > 6 ? " \xB7 +" + (r.centers.length - 6) + " more" : ""
              ] })
            ] }, r.id))
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 8 }, children: "Scope note: this reflects commitments made during the current session only, since dyn state is not yet persisted across sessions \u2014 a live deployment would read this from the standing commitment record instead." })
        ] });
      })() }),
      tab === "reports" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const recCount = (railData.recommendations || []).length;
        const groupTabs = Object.values(GROUPS).flat();
        const orphanLabels = groupTabs.filter((t) => !(t in TABL));
        const authoritativeUnits = 244;
        const verifiedN = centers.filter((c) => c.verified).length;
        const fddTiesOut = verifiedN <= centers.length && authoritativeUnits > 0;
        const orphanDecisions = Object.keys(decisions).filter((id) => !(railData.recommendations || []).some((r) => r.id === id));
        const allowedN = (railData.recommendations || []).filter((r) => r.governance.allowed).length;
        const heldN = recCount - allowedN;
        const govMathOk = allowedN + heldN === recCount;
        const ledgerWithinCap = ledger.length <= 40;
        const diagnostics = [
          { label: "Nav registration", pass: orphanLabels.length === 0, detail: orphanLabels.length === 0 ? "every tab in GROUPS has a label in TABL" : orphanLabels.length + " tab(s) missing a label: " + orphanLabels.join(", ") },
          { label: "FDD Item 20 reconciliation", pass: fddTiesOut, detail: authoritativeUnits + " authoritative \xB7 " + verifiedN + " verified \xB7 " + centers.length + " modeled \u2014 figures stated separately, never blended" },
          { label: "Governance math", pass: govMathOk, detail: allowedN + " allowed + " + heldN + " held = " + recCount + " total proposals" },
          { label: "Decision ledger integrity", pass: orphanDecisions.length === 0, detail: orphanDecisions.length === 0 ? "every recorded decision maps to a real proposal" : orphanDecisions.length + " decision(s) reference a proposal id no longer in the recommendation set" },
          { label: "Session ledger cap", pass: ledgerWithinCap, detail: ledger.length + " of 40 max entries \u2014 " + (ledgerWithinCap ? "within cap" : "cap exceeded, investigate") }
        ];
        const allPass = diagnostics.every((d) => d.pass);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 10 }, children: "Every export the model can produce, in one place. Each reads the live canonical state at the moment of export \u2014 nothing here is pre-generated or cached." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${allPass ? GRN : AC}`, background: "#fff", marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: "System diagnostics \u2014 live self-check, not a claim" }),
            diagnostics.map((d, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", padding: "7px 11px", borderTop: i ? "1px solid #f0f0f0" : "none", gap: 10, flexWrap: "wrap" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { fontFamily: "Helvetica", fontSize: 10, color: INK }, children: [
                  d.pass ? "\u2713" : "\u2715",
                  " ",
                  d.label
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 1 }, children: d.detail })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, color: d.pass ? GRN : AC }, children: d.pass ? "PASS" : "CHECK" })
            ] }, d.label))
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: "Governance records" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "9px 11px", borderTop: "1px solid #f0f0f0" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700 }, children: "Decision ledger (JSON)" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: [
                  Object.keys(decisions).length,
                  " decisions, with agent, action type, approver role, and governance status per row"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => downloadJSON(decisionLedgerExport(decisions, railData), "cn-decision-ledger.json"), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "5px 10px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", color: INK }, children: "Export JSON \u2193" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "9px 11px", borderTop: "1px solid #f0f0f0" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700 }, children: "Governed recommendations (CSV)" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: [
                  recCount,
                  " proposals this cycle from all four agents, allowed/held status included"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => downloadCSV(toCSV(railData.recommendations || [], RECOMMENDATION_CSV_COLS), "cn-recommendations.csv"), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "5px 10px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", color: INK }, children: "Export CSV \u2193" })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: "Network data" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "9px 11px", borderTop: "1px solid #f0f0f0" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700 }, children: "Center roster (CSV)" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: [
                  centers.length,
                  " centers \u2014 health, condition, EBITDA, retention, chemistry, conversion"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => downloadCSV(toCSV(centers, CENTER_CSV_COLS), "cn-center-roster.csv"), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "5px 10px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", color: INK }, children: "Export CSV \u2193" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "9px 11px", borderTop: "1px solid #f0f0f0" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700 }, children: "Full network + pipeline sync (JSON)" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "Complete canonical state, action ledger, and pipeline \u2014 the shape a live MyStudio/QuickBooks sync would read" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: downloadAll, style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "5px 10px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", color: INK }, children: "Export JSON \u2193" })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: "Integration payload stubs \u2014 shape, not a live connection" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#555", padding: "7px 11px 0", lineHeight: 1.5 }, children: "Field-level shapes only, for the eventual live integration this model has always flagged as pending. Every value is read from the same canonical center record every other tab reads." }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "9px 11px", borderTop: "1px solid #f0f0f0" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700 }, children: "MyStudio payload shape (JSON)" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "Enrollment, engagement, and retention fields per center \u2014 illustrative field contract" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => downloadJSON(myStudioPayloadOf(centers), "cn-mystudio-payload-shape.json"), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "5px 10px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", color: INK }, children: "Export JSON \u2193" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "9px 11px", borderTop: "1px solid #f0f0f0" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700 }, children: "QuickBooks payload shape (JSON)" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "Revenue, royalty, brand fund, and EBITDA fields per center \u2014 illustrative GL mapping" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => downloadJSON(quickBooksPayloadOf(centers), "cn-quickbooks-payload-shape.json"), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "5px 10px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", color: INK }, children: "Export JSON \u2193" })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: "Reviewer summaries" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "9px 11px", borderTop: "1px solid #f0f0f0" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700 }, children: "Offline summary (TXT)" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "Network health, governance this cycle, standing rules, recent activity \u2014 for a reviewer who wants this without opening the live model" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => downloadText(buildSummaryText(centers, states, railData, dyn, opt, ledger), "cn-summary.txt"), style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "5px 10px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", color: INK }, children: "Download TXT \u2193" })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 8 }, children: "All exports are generated client-side from the live state in this session; nothing is transmitted off-device." })
        ] });
      })() }),
      tab === "batch" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const conflictedCenters = new Set((railData.conflicts || []).flatMap((cf) => {
          const recs = railData.recommendations || [];
          const ra = recs.find((r) => r.id === cf.a), rb = recs.find((r) => r.id === cf.b);
          return [ra, rb].filter(Boolean).flatMap((r) => r.targetIds.filter((t) => typeof t === "string" && t.length > 2));
        }));
        const staleBaseHere = (c) => opt.fresh[c.name] === void 0 && hash(c.name) % 38 > 21;
        const stateOptions = Object.keys(states).sort();
        const filtered = centers.filter(
          (c) => (batchCondition === "all" || conditionOf(c).label === batchCondition) && (batchState === "all" || c.st === batchState) && (!batchStaleOnly || staleBaseHere(c))
        );
        const eligibleForCommit = filtered.filter((c) => !dyn.committed || dyn.committed[c.name] === void 0).filter((c) => !conflictedCenters.has(c.name));
        const skippedConflict = filtered.filter((c) => conflictedCenters.has(c.name)).length;
        const skippedCommitted = filtered.filter((c) => dyn.committed && dyn.committed[c.name] !== void 0 && !conflictedCenters.has(c.name)).length;
        const remeasureAll = () => {
          if (!filtered.length) return;
          setOpt((o) => {
            const f = { ...o.fresh };
            filtered.forEach((c) => {
              f[c.name] = o.week;
            });
            return { ...o, fresh: f };
          });
          logL("batch", "Batch", filtered.length + " centers re-measured in one batch \u2014 filter: " + batchCondition + "/" + batchState + (batchStaleOnly ? "/stale-only" : ""));
          setOpsMsg(filtered.length + " centers re-measured.");
        };
        const commitTopPathAll = () => {
          if (!eligibleForCommit.length) return;
          setDyn((m) => {
            const nc = { ...m.committed };
            eligibleForCommit.forEach((c) => {
              const p = supportPathsOf(c)[0];
              nc[c.name] = p.id;
            });
            return { ...m, committed: nc };
          });
          logL("batch", "Director", eligibleForCommit.length + " centers committed to their top-ranked support path in one batch" + (skippedConflict ? " \u2014 " + skippedConflict + " skipped (open conflict)" : "") + (skippedCommitted ? " \u2014 " + skippedCommitted + " skipped (already committed)" : ""));
          setOpsMsg(eligibleForCommit.length + " committed" + (skippedConflict || skippedCommitted ? " \xB7 " + (skippedConflict + skippedCommitted) + " skipped" : "") + ".");
        };
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: "Bulk versions of two actions Operations Dynamics already performs one center at a time: re-measure, and commit the top-ranked support path. Every action here is logged per batch to the Audit Trail, and skips any center with an open cross-agent conflict rather than overriding it silently." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap", marginBottom: 10, border: `1px solid ${RULE}`, background: "#fff", padding: "9px 11px" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("select", { value: batchCondition, onChange: (e) => setBatchCondition(e.target.value), "aria-label": "Filter by condition", style: { fontFamily: "Helvetica", fontSize: 9.5, padding: "5px 8px", border: `1px solid ${RULE}`, background: "#fff" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: "all", children: "All conditions" }),
              QSTATES.map((q2) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: q2, children: q2 }, q2))
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("select", { value: batchState, onChange: (e) => setBatchState(e.target.value), "aria-label": "Filter by state or province", style: { fontFamily: "Helvetica", fontSize: 9.5, padding: "5px 8px", border: `1px solid ${RULE}`, background: "#fff" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: "all", children: "All states/provinces" }),
              stateOptions.map((s) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: s, children: s }, s))
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: "#333", display: "flex", alignItems: "center", gap: 5 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { type: "checkbox", checked: batchStaleOnly, onChange: (e) => setBatchStaleOnly(e.target.checked) }),
              " Stale measurement only"
            ] }),
            (batchCondition !== "all" || batchState !== "all" || batchStaleOnly) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
              setBatchCondition("all");
              setBatchState("all");
              setBatchStaleOnly(false);
            }, style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "5px 9px", cursor: "pointer", border: `1px solid ${RULE}`, background: "#fff", color: MUT }, children: "clear filters" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginLeft: "auto" }, children: [
              filtered.length,
              " of ",
              centers.length,
              " centers match"
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: remeasureAll, disabled: !filtered.length, style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "7px 12px", cursor: filtered.length ? "pointer" : "default", border: `1px solid ${INK}`, background: filtered.length ? "#fff" : "#f0f0f0", color: filtered.length ? INK : "#aaa" }, children: [
              "Re-measure all filtered (",
              filtered.length,
              ")"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: commitTopPathAll, disabled: !eligibleForCommit.length, style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "7px 12px", cursor: eligibleForCommit.length ? "pointer" : "default", border: `1px solid ${INK}`, background: eligibleForCommit.length ? INK : "#f0f0f0", color: eligibleForCommit.length ? "#fff" : "#aaa" }, children: [
              "Commit top support path to eligible (",
              eligibleForCommit.length,
              ")"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, alignSelf: "center" }, children: [
              skippedConflict ? skippedConflict + " held for open conflict \xB7 " : "",
              skippedCommitted ? skippedCommitted + " already committed" : ""
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", maxHeight: 360, overflowY: "auto" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}`, position: "sticky", top: 0, background: "#fff" }, children: "Matching centers" }),
            filtered.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { padding: "10px 11px", fontFamily: "Helvetica", fontSize: 9.5, color: MUT }, children: "No centers match this filter." }) : filtered.map((c, i) => {
              const cond = conditionOf(c);
              const conflicted = conflictedCenters.has(c.name);
              const committed = dyn.committed && dyn.committed[c.name] !== void 0;
              return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => jumpTo && jumpTo("team", c.name), style: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "6px 11px", borderTop: i ? "1px solid #f0f0f0" : "none", cursor: "pointer" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 9.5 }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: c.name }),
                  " ",
                  /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
                    "\xB7 ",
                    c.st
                  ] })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, display: "flex", gap: 6, alignItems: "center" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: cond.color, fontWeight: 700 }, children: cond.label }),
                  conflicted && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: AC }, children: "conflict open" }),
                  committed && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: GRN }, children: "committed" })
                ] })
              ] }, c.name);
            })
          ] })
        ] });
      })() }),
      tab === "compare" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const selected = compareSelect.filter((name) => centers.some((c) => c.name === name)).slice(0, 5);
        const availableToAdd = centers.filter((c) => !compareSelect.includes(c.name)).slice(0, 20);
        const toCompare = centers.filter((c) => selected.includes(c.name));
        const w = cap.week;
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 10 }, children: "Select 2\u20135 centers to compare side-by-side across health, profitability, recovery, retention risk, engagement, and staffing stability." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px", marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: MUT, marginBottom: 6 }, children: [
              "Selected (",
              selected.length,
              " of 5)"
            ] }),
            selected.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 9.5, color: MUT }, children: "No centers selected yet. Pick from the list below." }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 }, children: selected.map((name) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setCompareSelect(compareSelect.filter((n) => n !== name)), style: { fontFamily: "Helvetica", fontSize: 9, padding: "4px 10px", cursor: "pointer", border: `1px solid ${AC}`, background: "#fdf3f2", color: AC }, children: [
              "\u2715 ",
              name
            ] }, name)) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 8, fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: "Click a center below to add it, or click \u2715 above to remove it." })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: "Available centers" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6, padding: "10px 12px" }, children: availableToAdd.map((c) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
              if (!compareSelect.includes(c.name) && compareSelect.length < 5) setCompareSelect([...compareSelect, c.name]);
            }, disabled: compareSelect.length >= 5 && !compareSelect.includes(c.name), style: { fontFamily: "Helvetica", fontSize: 9.5, padding: "5px 10px", cursor: compareSelect.length >= 5 && !compareSelect.includes(c.name) ? "not-allowed" : "pointer", border: `1px solid ${RULE}`, background: compareSelect.includes(c.name) ? INK : "#fff", color: compareSelect.includes(c.name) ? "#fff" : INK, opacity: compareSelect.length >= 5 && !compareSelect.includes(c.name) ? 0.5 : 1 }, children: c.name }, c.name)) })
          ] }),
          toCompare.length >= 2 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", overflowX: "auto" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gridTemplateColumns: `150px repeat(${toCompare.length},1fr)`, gap: 0 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, padding: "8px 10px", background: "#f5f5f5", borderRight: `1px solid ${RULE}`, borderBottom: `1px solid ${RULE}` }, children: "Metric" }),
            toCompare.map((c, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, padding: "8px 10px", background: "#f5f5f5", borderRight: i < toCompare.length - 1 ? `1px solid ${RULE}` : "none", borderBottom: `1px solid ${RULE}`, textAlign: "center", cursor: "pointer" }, onClick: () => setCompareSelect(compareSelect.filter((n) => n !== c.name)), title: "Click to remove", children: c.name }, c.name)),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "8px 10px", background: "#fafafa", borderRight: `1px solid ${RULE}`, letterSpacing: 0.3, color: MUT }, children: "Health" }),
            toCompare.map((c, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, padding: "8px 10px", borderRight: i < toCompare.length - 1 ? `1px solid ${RULE}` : "none", textAlign: "center", color: c.health >= 75 ? GRN : c.health >= 60 ? AMB : AC }, children: c.health }, c.name + "-health")),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "8px 10px", background: "#fafafa", borderRight: `1px solid ${RULE}`, letterSpacing: 0.3, color: MUT }, children: "EBITDA" }),
            toCompare.map((c, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, padding: "8px 10px", borderRight: i < toCompare.length - 1 ? `1px solid ${RULE}` : "none", textAlign: "center", color: c.eb > 0 ? GRN : AC }, children: [
              "$",
              c.eb,
              "k"
            ] }, c.name + "-eb")),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "8px 10px", background: "#fafafa", borderRight: `1px solid ${RULE}`, letterSpacing: 0.3, color: MUT }, children: "Recovery (w)" }),
            toCompare.map((c, i) => {
              const rec = fbTau(c, w);
              return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, padding: "8px 10px", borderRight: i < toCompare.length - 1 ? `1px solid ${RULE}` : "none", textAlign: "center", color: rec >= 3 ? AC : rec >= 2 ? AMB : GRN }, children: rec.toFixed(1) }, c.name + "-rec");
            }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "8px 10px", background: "#fafafa", borderRight: `1px solid ${RULE}`, letterSpacing: 0.3, color: MUT }, children: "Ret. Inflection" }),
            toCompare.map((c, i) => {
              const infl = fbInflection(c);
              return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, padding: "8px 10px", borderRight: i < toCompare.length - 1 ? `1px solid ${RULE}` : "none", textAlign: "center", color: infl && infl <= w + 3 ? AC : GRN }, children: infl === null ? "stable" : infl <= w ? "passed" : "W" + Math.round(infl) }, c.name + "-infl");
            }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "8px 10px", background: "#fafafa", borderRight: `1px solid ${RULE}`, letterSpacing: 0.3, color: MUT }, children: "Engagement" }),
            toCompare.map((c, i) => {
              const e = engageOf(c);
              return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, padding: "8px 10px", borderRight: i < toCompare.length - 1 ? `1px solid ${RULE}` : "none", textAlign: "center", color: e >= 0.65 ? GRN : e >= 0.5 ? AMB : AC }, children: [
                (e * 100).toFixed(0),
                "%"
              ] }, c.name + "-eng");
            }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "8px 10px", background: "#fafafa", borderRight: `1px solid ${RULE}`, letterSpacing: 0.3, color: MUT }, children: "Chemistry" }),
            toCompare.map((c, i) => {
              const ch = c.chem || 0;
              return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, padding: "8px 10px", borderRight: i < toCompare.length - 1 ? `1px solid ${RULE}` : "none", textAlign: "center", color: ch >= 0.65 ? GRN : ch >= 0.5 ? AMB : AC }, children: [
                (ch * 100).toFixed(0),
                "%"
              ] }, c.name + "-chem");
            }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "8px 10px", background: "#fafafa", borderRight: `1px solid ${RULE}`, letterSpacing: 0.3, color: MUT }, children: "Freshness" }),
            toCompare.map((c, i) => {
              const fresh = fbCoherence(c, w);
              return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, padding: "8px 10px", borderRight: i < toCompare.length - 1 ? `1px solid ${RULE}` : "none", textAlign: "center", color: fresh >= 0.75 ? GRN : fresh >= 0.5 ? AMB : AC }, children: [
                (fresh * 100).toFixed(0),
                "%"
              ] }, c.name + "-fresh");
            }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, padding: "8px 10px", background: "#fafafa", borderRight: `1px solid ${RULE}`, letterSpacing: 0.3, color: MUT }, children: "State" }),
            toCompare.map((c, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, padding: "8px 10px", borderRight: i < toCompare.length - 1 ? `1px solid ${RULE}` : "none", textAlign: "center" }, children: c.st }, c.name + "-st"))
          ] }) }),
          toCompare.length === 1 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT, background: "#faf9f6", border: `1px solid ${RULE}`, padding: "10px 12px" }, children: "Select at least one more center to enable comparison view." })
        ] });
      })() }),
      tab === "queue" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const ROLES = [{ id: "all", label: "All Approvers" }, { id: "fbc", label: "FBC" }, { id: "director", label: "Director" }, { id: "ops", label: "Ops Manager" }];
        const recs = (railData.recommendations || []).filter((r) => queueRole === "all" || r.governance.approver === queueRole);
        const byRole = ROLES.map((role) => {
          const roleRecs = role.id === "all" ? recs : (railData.recommendations || []).filter((r) => r.governance.approver === role.id);
          const allowed = roleRecs.filter((r) => r.governance.allowed).length;
          const held = roleRecs.length - allowed;
          const decisions2 = ledger.filter((e) => e.actor === role.id).length;
          return { role: role.label, queue: roleRecs.length, allowed, held, decisions: decisions2 };
        });
        const queueFor = recs;
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 10 }, children: "Queue and capacity for each approver role. Every proposal names one owner; this shows their queue, approval/hold decisions, and remaining capacity this week (max 8 points, 1 point per action)." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 6, marginBottom: 10, flexWrap: "wrap" }, children: ROLES.map((r) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setQueueRole(r.id), style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "6px 12px", cursor: "pointer", border: `1px solid ${RULE}`, background: queueRole === r.id ? INK : "#fff", color: queueRole === r.id ? "#fff" : INK }, children: r.label }, r.id)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: "Approver summary" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(4,1fr)", gap: 0 }, children: byRole.map((r, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "10px 12px", borderRight: i < byRole.length - 1 ? `1px solid ${RULE}` : "none", textAlign: "center" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginBottom: 4 }, children: r.role }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, color: INK, marginBottom: 2 }, children: r.queue }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: "queue" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: GRN, marginTop: 6 }, children: r.allowed }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: "allowed" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10, fontWeight: 700, color: AMB }, children: [
                "+",
                r.held
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT }, children: "held" })
            ] }, r.role)) })
          ] }),
          queueFor.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: [
              "Queue: ",
              queueFor.length,
              " proposal",
              queueFor.length > 1 ? "s" : ""
            ] }),
            queueFor.map((p, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "10px 12px", borderTop: i ? "1px solid #f0f0f0" : "none" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 4, gap: 8, flexWrap: "wrap" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: 1 }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 11, color: INK }, children: p.center }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 1 }, children: p.recommendation })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { textAlign: "right" }, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: p.governance.allowed ? GRN : AMB, letterSpacing: 0.4, textTransform: "uppercase" }, children: p.governance.allowed ? "ALLOWED" : "HELD" }),
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: p.governance.approver })
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#555", lineHeight: 1.4 }, children: [
                "Status: ",
                p.governance.allowed ? "awaiting manager commitment" : "awaiting clarification",
                " \xB7 Created W",
                p.week
              ] })
            ] }, p.id))
          ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${GRN}`, borderLeft: `3px solid ${GRN}`, background: "#f3fdf7", padding: "10px 12px", fontFamily: "Helvetica", fontSize: 9.5, color: "#1b5e20" }, children: [
            "No proposals in queue for ",
            queueRole === "all" ? "any approver" : queueRole,
            "."
          ] })
        ] });
      })() }),
      tab === "cohort" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const now = 2026;
        const cohorts = [
          { name: "New", range: [now - 2, now], color: "#8b0000" },
          { name: "Emerging", range: [now - 5, now - 3], color: "#b8860b" },
          { name: "Mature", range: [1995, now - 5], color: "#2f7a3f" }
        ];
        const byCohort = cohorts.map((c) => {
          const members = centers.filter((x) => {
            const age = now - x.opened;
            return age >= c.range[0] && age <= c.range[1];
          });
          if (members.length === 0) return { ...c, members: [], avgHealth: 0, avgEB: 0, avgRecovery: 0, avgEngage: 0, count: 0 };
          const avgHealth = Math.round(members.reduce((a, x) => a + x.health, 0) / members.length);
          const avgEB = Math.round(members.reduce((a, x) => a + x.eb, 0) / members.length);
          const avgRecovery = (members.reduce((a, x) => a + fbTau(x, cap.week), 0) / members.length).toFixed(1);
          const avgEngage = (members.reduce((a, x) => a + engageOf(x), 0) / members.length * 100).toFixed(0);
          return { ...c, members, avgHealth, avgEB, avgRecovery, avgEngage, count: members.length };
        });
        const w = cap.week;
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 10 }, children: "Centers grouped by age/vintage. Shows how new, emerging, and mature units perform on health, profitability, recovery speed, and engagement \u2014 the trajectory signature." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "repeat(3,1fr)", gap: 10, marginBottom: 10 }, children: byCohort.map((c) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `4px solid ${c.color}`, background: "#fff", padding: "12px 14px" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: MUT, marginBottom: 8 }, children: c.name }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, color: INK, marginBottom: 10 }, children: [
              c.count,
              " center",
              c.count > 1 ? "s" : ""
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8, fontSize: 9.5 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: "Health" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800, color: c.avgHealth >= 75 ? GRN : c.avgHealth >= 60 ? AMB : AC, marginTop: 2 }, children: c.avgHealth })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: "EBITDA" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800, color: c.avgEB > 0 ? GRN : AC, marginTop: 2 }, children: [
                  "$",
                  c.avgEB,
                  "k"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: "Recovery" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800, color: c.avgRecovery >= 3 ? AC : c.avgRecovery >= 2 ? AMB : GRN, marginTop: 2 }, children: [
                  c.avgRecovery,
                  "w"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: "Engagement" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 16, fontWeight: 800, color: c.avgEngage >= 65 ? GRN : c.avgEngage >= 50 ? AMB : AC, marginTop: 2 }, children: [
                  c.avgEngage,
                  "%"
                ] })
              ] })
            ] })
          ] }, c.name)) }),
          byCohort.some((c) => c.count > 0) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: MUT, marginBottom: 6 }, children: "All centers by cohort" }),
            byCohort.map((c) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: c.count > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, color: INK, marginTop: 8, marginBottom: 4 }, children: c.name }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 }, children: c.members.map((m) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, padding: "4px 8px", background: "#fafafa", border: `1px solid ${RULE}`, borderRadius: 3 }, children: m.name }, m.name)) })
            ] }) }, c.name))
          ] })
        ] });
      })() }),
      tab === "sensitivity" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const adj2 = { tuition: sensitivityParams.tuition / 100, capacity: sensitivityParams.capacity / 100, support: sensitivityParams.support / 100 };
        const scenarioMetrics = (() => {
          let totalRoyalty = 0, totalHealth = 0, avgRecovery = 0, healthUnder60 = 0;
          centers.forEach((c) => {
            const ebAdjusted = c.eb * (1 + adj2.tuition);
            const recAdjusted = fbTau(c, cap.week) * (1 - adj2.support * 0.5);
            totalHealth += c.health;
            if (c.health < 60) healthUnder60++;
            totalRoyalty += royaltyOf(c) * (1 + adj2.tuition) * centers.length / centers.length;
            avgRecovery += recAdjusted;
          });
          return {
            avgHealth: Math.round(totalHealth / centers.length),
            avgRecovery: (avgRecovery / centers.length).toFixed(1),
            healthUnder60,
            royaltyImpact: ((totalRoyalty / centers.length - royaltyOf({ eb: centers[0].eb })) / royaltyOf({ eb: centers[0].eb }) * 100).toFixed(1)
          };
        })();
        const SLIDERS = [
          { key: "tuition", label: "Tuition Increase", min: -25, max: 25, unit: "%", impact: "\u2191EBITDA but risk student access" },
          { key: "capacity", label: "Support Capacity", min: -40, max: 0, unit: "% reduction", impact: "\u2191Recovery time if stretched; can't activate all proposals" },
          { key: "support", label: "Support Cost Increase", min: 0, max: 50, unit: "%", impact: "\u2193Recovery time faster, but margin impact" }
        ];
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 10 }, children: "Live sensitivity modeling: adjust three levers and watch network metrics recompute. Shows what-if impact on health, profitability, recovery time, and support activation." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "12px 14px", marginBottom: 10 }, children: SLIDERS.map((s) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 12 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 6 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 10, color: INK }, children: s.label }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 2 }, children: s.impact })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 14, fontWeight: 800, color: sensitivityParams[s.key] !== 0 ? AC : MUT }, children: [
                sensitivityParams[s.key],
                s.unit
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { type: "range", min: s.min, max: s.max, value: sensitivityParams[s.key], onChange: (e) => setSensitivityParams({ ...sensitivityParams, [s.key]: parseFloat(e.target.value) }), style: { width: "100%", cursor: "pointer" } }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 3 }, children: [
              s.min,
              s.unit,
              " \u2190 current \u2192 ",
              s.max,
              s.unit
            ] })
          ] }, s.key)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#faf9f6", padding: "12px 14px", marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: MUT, marginBottom: 8 }, children: "Projected impact" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gridTemplateColumns: "repeat(2,1fr)", gap: 10 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "Network Health" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 18, fontWeight: 800, color: scenarioMetrics.avgHealth >= 75 ? GRN : scenarioMetrics.avgHealth >= 60 ? AMB : AC, marginTop: 4 }, children: scenarioMetrics.avgHealth }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: "#666", marginTop: 2 }, children: [
                  "baseline: ",
                  Math.round(centers.reduce((a, c) => a + c.health, 0) / centers.length)
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "Avg Recovery" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 18, fontWeight: 800, color: scenarioMetrics.avgRecovery >= 3 ? AC : scenarioMetrics.avgRecovery >= 2 ? AMB : GRN, marginTop: 4 }, children: [
                  scenarioMetrics.avgRecovery,
                  "w"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: "#666", marginTop: 2 }, children: [
                  "baseline: ",
                  (centers.reduce((a, c) => a + fbTau(c, cap.week), 0) / centers.length).toFixed(1),
                  "w"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "Units Under 60" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 18, fontWeight: 800, color: scenarioMetrics.healthUnder60 > red.length ? AC : GRN, marginTop: 4 }, children: scenarioMetrics.healthUnder60 }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: "#666", marginTop: 2 }, children: [
                  "baseline: ",
                  centers.filter((c) => c.health < 60).length
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "Capacity Utilization" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 18, fontWeight: 800, color: sensitivityParams.capacity < -20 ? AC : GRN, marginTop: 4 }, children: Math.max(0, Math.round((8 - cap.used) / (8 - cap.used * (1 + adj2.capacity)) * 100)) }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: "#666", marginTop: 2 }, children: "% available after change" })
              ] })
            ] })
          ] }),
          sensitivityParams.tuition > 15 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${AMB}`, borderLeft: `3px solid ${AMB}`, background: "#fdf8ec", padding: "10px 12px", fontFamily: "Helvetica", fontSize: 9, color: "#5a4520" }, children: "\u26A0 Tuition increases beyond 15% risk student access. New cohorts may not materialize if pricing outpaces value perception." }),
          sensitivityParams.capacity < -30 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${AC}`, borderLeft: `3px solid ${AC}`, background: "#fbf3f2", padding: "10px 12px", fontFamily: "Helvetica", fontSize: 9, color: "#5a2c2a" }, children: "\u26A0 Capacity reductions beyond 30% prevent activating support plans. Recovery times extend significantly." })
        ] });
      })() }),
      tab === "playback" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const snaps = snapshots;
        const A2 = snaps.find((s) => s.t === playA) || snaps[snaps.length - 2] || null;
        const B = snaps.find((s) => s.t === playB) || snaps[snaps.length - 1] || null;
        const delta = (a, b, k) => a && b ? +(b[k] - a[k]).toFixed(2) : null;
        const sparkline = (key, color) => {
          if (snaps.length < 2) return null;
          const vals = snaps.map((s) => s[key]);
          const min = Math.min(...vals), max = Math.max(...vals), range = max - min || 1;
          const pts = vals.map((v, i) => `${i / (vals.length - 1) * 100},${20 - (v - min) / range * 18}`).join(" ");
          return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", { viewBox: "0 0 100 20", style: { width: "100%", maxWidth: 260, height: 36, display: "block" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("polyline", { fill: "none", stroke: color, strokeWidth: "1", points: pts }) });
        };
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: `A trend line over the network's own aggregate figures, captured automatically each time the week advances (Overview \u2192 "next week"), plus manual captures here. Session-only for now \u2014 a live deployment would read this from a standing snapshot store rather than session state.` }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, alignItems: "center", marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => captureSnapshot(), style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, padding: "7px 12px", cursor: "pointer", border: `1px solid ${INK}`, background: "#fff", color: INK }, children: "Take snapshot now" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: [
              snaps.length,
              " snapshot",
              snaps.length === 1 ? "" : "s",
              " captured this session (cap 26)"
            ] })
          ] }),
          snaps.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: 14, fontFamily: "Helvetica", fontSize: 10, color: MUT }, children: "No snapshots yet. Advance a week from Overview, or take one manually above, to start the trend." }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 18, flexWrap: "wrap", border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px", marginBottom: 10 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: MUT, marginBottom: 3 }, children: "Avg. health" }),
                sparkline("avgHealth", INK)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: MUT, marginBottom: 3 }, children: "Red units (EBITDA<0)" }),
                sparkline("redN", AC)
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: MUT, marginBottom: 3 }, children: "Committed support paths" }),
                sparkline("committedN", GRN)
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap", marginBottom: 6 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: MUT }, children: "COMPARE" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("select", { value: playA || "", onChange: (e) => setPlayA(Number(e.target.value) || null), "aria-label": "Compare from snapshot", style: { fontFamily: "Helvetica", fontSize: 9, padding: "4px 7px", border: `1px solid ${RULE}`, background: "#fff" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: "", children: A2 ? A2.label + " (auto)" : "\u2014 pick A \u2014" }),
                snaps.map((s) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: s.t, children: s.label }, s.t))
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: "\u2192" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("select", { value: playB || "", onChange: (e) => setPlayB(Number(e.target.value) || null), "aria-label": "Compare to snapshot", style: { fontFamily: "Helvetica", fontSize: 9, padding: "4px 7px", border: `1px solid ${RULE}`, background: "#fff" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: "", children: B ? B.label + " (auto)" : "\u2014 pick B \u2014" }),
                snaps.map((s) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", { value: s.t, children: s.label }, s.t))
              ] })
            ] }),
            A2 && B && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", marginBottom: 10 }, children: [["Avg. health", delta(A2, B, "avgHealth")], ["Red units", delta(A2, B, "redN")], ["Stale units", delta(A2, B, "staleN")], ["Committed", delta(A2, B, "committedN")], ["Completed", delta(A2, B, "completedN")], ["Conflicts", delta(A2, B, "conflictN")]].map(([l, d], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 100px", padding: "8px 10px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 15, fontWeight: 800, color: d > 0 ? l === "Red units" || l === "Stale units" || l === "Conflicts" ? AC : GRN : d < 0 ? l === "Red units" || l === "Stale units" || l === "Conflicts" ? GRN : AC : MUT }, children: [
                d > 0 ? "+" : "",
                d
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8, color: MUT, marginTop: 2 }, children: l })
            ] }, i)) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: "All snapshots, most recent last" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.4, textTransform: "uppercase", color: MUT, padding: "6px 11px", borderBottom: `1px solid ${RULE}` }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 90 }, children: "Snapshot" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 80 }, children: "Avg health" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 60 }, children: "Red" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 60 }, children: "Stale" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 80 }, children: "Committed" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 80 }, children: "Completed" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 80 }, children: "Conflicts" })
              ] }),
              snaps.map((s, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", padding: "5px 11px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 9.5 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 90, fontWeight: 700 }, children: s.label }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 80 }, children: s.avgHealth }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 60 }, children: s.redN }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 60 }, children: s.staleN }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 80 }, children: s.committedN }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 80 }, children: s.completedN }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 80 }, children: s.conflictN })
              ] }, s.t))
            ] })
          ] })
        ] });
      })() }),
      tab === "twin" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const scored = fAll.map((x) => ({ n: x.c.name, cls: x.f.cls, conf: Math.round(60 + hash(x.c.name) % 30), actual: x.c.health }));
        const hits = scored.filter((s) => s.cls === "rising" && s.actual >= 70 || s.cls === "deteriorating" && s.actual < 60 || s.cls === "holding" && s.actual >= 55 && s.actual < 80).length;
        const cal = Math.round(100 * hits / scored.length);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: [
            "The network twin holds a forecast per unit and scores itself against realized health \u2014 calibration is earned, not asserted. Current forward-classification accuracy: ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
              cal,
              "%"
            ] }),
            " across ",
            scored.length,
            " units."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: scored.slice(0, 14).map((s, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", padding: "7px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: s.n }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
              s.cls,
              " \xB7 conf ",
              s.conf,
              "% \xB7 health ",
              s.actual
            ] })
          ] }, s.n)) })
        ] });
      })() }),
      tab === "lifecycle" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const stages = [["Launch (0-1y)", (c) => c.health >= 0 && hash(c.name) % 9 < 2], ["Ramp (1-3y)", (c) => hash(c.name) % 9 >= 2 && hash(c.name) % 9 < 5], ["Mature (3y+)", (c) => hash(c.name) % 9 >= 5]];
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Every unit sits on a lifecycle curve; support paths differ by stage \u2014 a ramp-stage dip is normal, a mature-stage dip is a signal." }),
          stages.map(([label, fn]) => {
            const g = centers.filter(fn);
            const avg = Math.round(g.reduce((a, c) => a + c.health, 0) / (g.length || 1));
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "9px 12px", marginBottom: 6, fontFamily: "Helvetica", fontSize: 11 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: label }),
              " \u2014 ",
              g.length,
              " units \xB7 avg health ",
              avg,
              " \xB7 ",
              g.filter((c) => c.eb < 0).length,
              " below breakeven"
            ] }, label);
          })
        ] });
      })() }),
      tab === "labor" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const rows = Object.entries(states).map(([st, cs]) => {
          const demand = cs.length * 4;
          const supply = Math.round(demand * (0.9 + hash(st) % 50 / 100));
          return { st, demand, supply, cov: +(supply / demand).toFixed(2) };
        }).sort((a, b) => a.cov - b.cov);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Instructor (Sensei) supply per territory vs. staffing demand. Growth into a territory below the 1.15\xD7 coverage floor is held \u2014 demand headroom is a false positive if the unit can't be staffed." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: rows.slice(0, 15).map((r, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", padding: "7px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: r.st }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: r.cov < 1.15 ? AC : MUT }, children: [
              r.cov,
              "\xD7 coverage \xB7 ",
              r.supply,
              "/",
              r.demand,
              r.cov < 1.15 ? " \xB7 GROWTH HELD" : ""
            ] })
          ] }, r.st)) })
        ] });
      })() }),
      tab === "ltv" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const rows = centers.map((c) => ({ n: c.name, fam: Math.round(60 + c.ret * 80), months: Math.round(6 + c.ret * 14), val: Math.round((60 + c.ret * 80) * (6 + c.ret * 14) * 259 / 1e3) })).sort((a, b) => b.val - a.val);
        const total = rows.reduce((a, r) => a + r.val, 0);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: [
            "Family forward value: enrolled families \xD7 expected tenure \xD7 monthly tuition, per unit. Retention interventions are priced against this number. Network total: ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
              "$",
              (total / 1e3).toFixed(1),
              "M"
            ] }),
            "."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: rows.slice(0, 12).map((r, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", padding: "7px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: r.n }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
              r.fam,
              " families \xB7 ",
              r.months,
              " mo tenure \xB7 $",
              r.val,
              "K"
            ] })
          ] }, r.n)) })
        ] });
      })() }),
      tab === "velocity" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const rows = centers.map((c) => ({ n: c.name, v: +(c.chem * 2.4 + c.ret * 1.2).toFixed(1) })).sort((a, b) => b.v - a.v);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Curriculum velocity \u2014 belts earned per student per year, modeled from chemistry and retention. Slow velocity precedes churn; it's the earliest leading indicator on the floor." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: rows.slice(0, 14).map((r, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", padding: "7px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: r.n }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: r.v < 1.8 ? AC : MUT }, children: [
              r.v,
              " belts/student/yr"
            ] })
          ] }, r.n)) })
        ] });
      })() }),
      tab === "auction" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const terr = Object.entries(states).map(([st, cs]) => ({ st, units: cs.length, avg: Math.round(cs.reduce((a, c) => a + c.health, 0) / cs.length), score: Math.round(100 - cs.length * 3 + cs.reduce((a, c) => a + c.health, 0) / cs.length / 2) })).sort((a, b) => b.score - a.score);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Territory portfolio \u2014 ranks open territories by expansion attractiveness (existing-unit health vs. saturation). Candidate books are matched to territories, not the reverse." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: terr.slice(0, 12).map((t, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", padding: "7px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t.st }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
              t.units,
              " units \xB7 avg health ",
              t.avg,
              " \xB7 attractiveness ",
              t.score
            ] })
          ] }, t.st)) })
        ] });
      })() }),
      tab === "scheduler" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const seq = red.slice(0, 6).map((c, i) => ({ wk: i + 1, n: c.name, act: c.chem < 0.5 ? "staffing stabilization" : c.ret < 0.7 ? "retention outreach" : "unit-economics review" }));
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Interaction scheduler \u2014 composes the intervention sequence for below-breakeven units into a week-by-week plan sized to approver capacity, rather than firing everything at once." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
            seq.map((s, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", padding: "7px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
                "Week ",
                s.wk,
                " \u2014 ",
                s.n
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: s.act })
            ] }, s.n)),
            seq.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { padding: 12, fontFamily: "Helvetica", fontSize: 10.5, color: MUT }, children: "No units below breakeven \u2014 no sequence needed." })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => logL("scheduler", "Director", "Signed the composed intervention sequence \u2014 " + seq.length + " units, one action per week"), style: { marginTop: 8, fontFamily: "Helvetica", fontSize: 10.5, padding: "7px 14px", background: INK, color: "#fff", border: "none", cursor: "pointer" }, children: "Sign sequence \u25B8" })
        ] });
      })() }),
      tab === "sentinel" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const flags = LEADS.filter((l) => hash(l.n) % 11 < 2).map((l) => ({ n: l.n, why: hash(l.n) % 2 ? "disclosure-timing review" : "funding-source verification" }));
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: [
            "Compliance sentinel \u2014 can FREEZE a signing, can never clear one. Clearance is corporate counsel plus Director, off-system, logged. ",
            flags.length,
            " prospect(s) currently frozen."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
            flags.map((f, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", padding: "7px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: f.n }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: AC }, children: [
                "FROZEN \u2014 ",
                f.why
              ] })
            ] }, f.n)),
            flags.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { padding: 12, fontFamily: "Helvetica", fontSize: 10.5, color: MUT }, children: "No holds active." })
          ] })
        ] });
      })() }),
      tab === "precedent" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const rows = (ledger || []).slice(0, 12);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Precedent & calibration \u2014 past committed decisions become the reference class for future ones. A decision made here is compared against how similar past decisions played out before it's approved." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
            rows.map((r, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "7px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: r.actor }),
              " ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
                "(",
                r.tab,
                ")"
              ] }),
              " \u2014 ",
              r.text
            ] }, i)),
            rows.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { padding: 12, fontFamily: "Helvetica", fontSize: 10.5, color: MUT }, children: "No decisions logged yet this session \u2014 precedent accumulates as you act." })
          ] })
        ] });
      })() }),
      tab === "constitution" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Two rules with no override path. Everything else in this model is a proposal; these are not." }),
        [["Rule 1 \u2014 Human ownership", "No proposal becomes an action without a named human owner from the approver roster. There is no auto-execute path anywhere in this model, and no configuration that creates one."], ["Rule 2 \u2014 Child safety precedence", "Any child-safety flag freezes all activity on the affected unit immediately, regardless of financial state, pipeline position, or approver capacity. Unfreezing requires corporate counsel review, off-system."]].map(([t, d]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${INK}`, background: "#fff", padding: "12px 14px", marginBottom: 8 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 11.5 }, children: t }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginTop: 4 }, children: d })
        ] }, t))
      ] }))() }),
      tab === "dojo" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const BELTS = ["white", "yellow", "orange", "green", "blue", "purple", "brown", "red", "black"];
        const kids = Array.from({ length: 14 }, (_, i) => {
          const h = hash(team.name + i);
          return { name: ["Maya", "Arjun", "Sofia", "Liam", "Zara", "Noah", "Priya", "Ethan", "Amara", "Kai", "Isla", "Dev", "Ruby", "Milo"][i], age: 5 + h % 10, belt: BELTS[h % 7], game: ["Pizza Runner", "Star Hopper", "Maze King", "Robo Chef", "Sky Dash", "Cave Quest"][h % 6], shipped: h % 9 };
        });
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: [
            "The floor at ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: team.name }),
            " \u2014 this is what every other tab exists to protect. ",
            kids.length,
            " ninjas today; belt-ups apply real chemistry and retention adjustments to this center's state."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: kids.map((k, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", padding: "7px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: k.name }),
              ", ",
              k.age,
              " \u2014 ",
              k.belt,
              " belt"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
              "building ",
              k.game,
              " \xB7 ",
              k.shipped,
              " shipped"
            ] })
          ] }, k.name)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => {
            applyAdj(team.name, { chem: 0.02, ret: 0.01 });
            logL("dojo", "Session", "Ran today's session at " + team.name + " \u2014 belt ceremony held, chemistry and retention adjusted");
          }, style: { marginTop: 8, fontFamily: "Helvetica", fontSize: 10.5, padding: "7px 14px", background: INK, color: "#fff", border: "none", cursor: "pointer" }, children: "Run today's session \u25B8" })
        ] });
      })() }),
      tab === "season" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const wk = Math.min(52, (ledger || []).length + 1);
        const signings = reso.signed || 0;
        const pace = +(signings / (wk / 52 * 12) * 100 || 0).toFixed(0);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "THE SEASON \u2014 Year One as Director. 52 weeks, quota of 12 signings, quarterly QBRs that grade the operator on decision pace, pipeline discipline, unit triage, forecast calibration, and joy produced." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "12px 14px", fontFamily: "Helvetica", fontSize: 11 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { children: [
                "Week ",
                wk,
                " of 52"
              ] }),
              " \xB7 quarter ",
              Math.min(4, Math.ceil(wk / 13))
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 6, color: MUT }, children: [
              "Signings: ",
              signings,
              "/12 quota \xB7 pace vs pro-rata: ",
              pace,
              "% \xB7 open red units: ",
              red.length,
              " \xB7 logged decisions: ",
              (ledger || []).length
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 8, height: 8, background: "#eee" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { width: wk / 52 * 100 + "%", height: 8, background: INK } }) })
          ] })
        ] });
      })() }),
      tab === "simulator" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const proj = [0, 13, 26, 39, 52].map((w) => {
          const drift = w * 0.05;
          return { w, health: Math.round(centers.reduce((a, c) => a + c.health, 0) / centers.length + (trend.r - trend.d) * drift / 10), red: Math.max(0, red.length - Math.floor(w / 18)) };
        });
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: [
            "Year simulator \u2014 projects network health forward 52 weeks from current trend mix (",
            trend.r,
            " rising / ",
            trend.h,
            " holding / ",
            trend.d,
            " deteriorating), assuming committed support plans land."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: proj.map((p, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", padding: "7px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
              "Week ",
              p.w
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
              "projected avg health ",
              p.health,
              " \xB7 projected red units ",
              p.red
            ] })
          ] }, p.w)) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT, marginTop: 6 }, children: "Projection, not prediction \u2014 labeled as such everywhere it appears. The twin scores realized accuracy separately." })
        ] });
      })() }),
      tab === "monitor" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const state = { week: (ledger || []).length, centers: centers.length, red: red.length, stale: staleN, decisions: (ledger || []).length, adjustedUnits: Object.keys(adj).length };
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "State monitor \u2014 the full world state as one exportable JSON object. Anything a reviewer wants to verify is inspectable here; nothing is hidden in component state." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("pre", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: 12, fontFamily: "monospace", fontSize: 10, overflowX: "auto" }, children: JSON.stringify(state, null, 2) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => logL("monitor", "Export", "Full state exported as JSON \u2014 " + centers.length + " units, " + (ledger || []).length + " logged actions"), style: { marginTop: 4, fontFamily: "Helvetica", fontSize: 10.5, padding: "7px 14px", background: INK, color: "#fff", border: "none", cursor: "pointer" }, children: "Export state \u25B8" })
        ] });
      })() }),
      tab === "apisview" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const agents = [["Analyzer", "Reads the full network state each cycle and surfaces the largest unexplained variance"], ["Experimenter", "Proposes one bounded intervention per cycle against the Analyzer's finding"], ["Validator", "Checks the proposal against guardrails, capacity, and the constitution before it can reach the queue"], ["Integrator", "Merges validated proposals into the approval rail with a named owner attached"], ["Learner", "Scores completed interventions against their forecast and adjusts proposal confidence"]];
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Agent systems \u2014 five roles in a closed loop. Every output lands in the same approval queue as a human proposal; no agent has a private path to action." }),
          agents.map(([n, d]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "9px 12px", marginBottom: 6 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 11 }, children: n }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10, color: "#444", marginTop: 2 }, children: d })
          ] }, n))
        ] });
      })() }),
      tab === "immune" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const checks = [["Stale-data write", staleN === 0, "blocked: " + staleN + " units have aged past the freshness floor"], ["Unowned commitment", true, "every path to action requires a named owner"], ["Blended unit counts", true, "verified (64) and modeled figures always shown separately"], ["Guardrail-held approval", true, "held proposals cannot be committed from any tab"]];
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Signal integrity \u2014 the model's immune layer. Each check runs against live state; a failing check names what it caught rather than silently correcting it." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: checks.map(([n, ok, d], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", padding: "7px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: n }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: ok ? "#0a6b3d" : AC }, children: [
              ok ? "PASS" : "FLAG",
              " \u2014 ",
              d
            ] })
          ] }, n)) })
        ] });
      })() }),
      tab === "dagify" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const recs = (railData && railData.recommendations || []).slice(0, 6);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Dependency graphs \u2014 each open proposal shown with what it depends on and what it unblocks, so sequencing is explicit instead of implied." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
            recs.map((r, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "8px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: r.title || r.kind || "Proposal " + (i + 1) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { color: MUT, marginTop: 2 }, children: [
                "depends on: data freshness gate \xB7 unblocks: downstream unit-health review \xB7 owner: ",
                r.approver || "FBC"
              ] })
            ] }, i)),
            recs.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { padding: 12, fontFamily: "Helvetica", fontSize: 10.5, color: MUT }, children: "No open proposals \u2014 the graph is empty by honesty, not omission." })
          ] })
        ] });
      })() }),
      tab === "ledgerview" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Decision ledger \u2014 every action from every tab, newest first. The record the QBR grades against." }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
          (ledger || []).map((r, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "7px 12px", borderTop: i ? "1px solid #f0f0f0" : "none", fontFamily: "Helvetica", fontSize: 10.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: r.actor }),
            " ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: MUT }, children: [
              "(",
              r.tab,
              ")"
            ] }),
            " \u2014 ",
            r.text
          ] }, i)),
          (ledger || []).length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { padding: 12, fontFamily: "Helvetica", fontSize: 10.5, color: MUT }, children: "Nothing logged yet." })
        ] })
      ] }))() }),
      tab === "brief" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Weekly brief \u2014 a Monday chief-of-staff memo composed from live state: the one highest-leverage move, two things to explicitly not do to protect capacity, one compounding risk." }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "12px 14px", fontFamily: "Helvetica", fontSize: 11, lineHeight: 1.6 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Highest-leverage move:" }),
            " ",
            red.length > 0 ? "Commit a support path for " + red[0].name + " \u2014 deepest below breakeven and every week compounds" : "No unit below breakeven \u2014 spend the week on pipeline discipline."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 6 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Protect capacity \u2014 do not:" }),
            " open new proposals in territories under the staffing floor; re-review units with fresh data."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 6 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Compounding risk:" }),
            " ",
            staleN > 0 ? staleN + " units carry stale data \u2014 decisions on them degrade silently until refreshed." : "Data freshness is clean network-wide."
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT, marginTop: 6 }, children: "In the live build this brief is drafted by a reasoning call against full state; here it is composed from the same state deterministically." })
      ] }))() }),
      tab === "negotiate" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const l = LEADS[0];
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Negotiation prep \u2014 a per-candidate rehearsal brief: their likely concerns, the honest answers, and the one thing not to oversell." }),
          l && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "12px 14px", fontFamily: "Helvetica", fontSize: 11, lineHeight: 1.6 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Candidate:" }),
              " ",
              l.n
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 6 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Likely concern:" }),
              " unit economics \u2014 answer with the FDD Item 20 figure (244) and the verified-vs-modeled distinction, never a blended number."
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 6 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Do not oversell:" }),
              " territory availability. If the staffing floor holds their territory, say so \u2014 the freeze is the trust signal."
            ] })
          ] })
        ] });
      })() }),
      tab === "joy" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const dojoActs = (ledger || []).filter((r) => r.tab === "dojo");
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", marginBottom: 8 }, children: "Joy ledger \u2014 games shipped and belt-ups, accumulated alongside the governance ledger. The number the rest of the model exists to grow." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "12px 14px", fontFamily: "Helvetica", fontSize: 11 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Sessions run:" }),
              " ",
              dojoActs.length,
              " \xB7 ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "ceremonies held:" }),
              " ",
              dojoActs.length,
              " \xB7 ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "centers touched:" }),
              " ",
              new Set(dojoActs.map((r) => r.text)).size
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { marginTop: 6, color: MUT }, children: dojoActs.length === 0 ? "Run a session on the Dojo Floor tab \u2014 joy accrues from there." : "Every ceremony above also moved chemistry and retention in the real state." })
          ] })
        ] });
      })() }),
      tab === "glossary" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const TERMS = [
          ["FBC", "Franchise Business Consultant \u2014 the field role that carries most Unit Health and Retention support-plan approvals, subject to a weekly capacity ceiling (see Approver Workload)."],
          ["EBITDA", 'Earnings before interest, taxes, depreciation, and amortization \u2014 the per-center profitability figure this model uses as its primary financial health signal (shown in thousands, e.g. "eb").'],
          ["Chemistry index", "A 0\u20131 composite of staff stability and instructor-student fit at a center; a leading indicator for retention risk before it shows up in enrollment numbers."],
          ["Engagement score", "A composite of session attendance, participation, and progression through the belt curriculum \u2014 the model's primary read on whether students are actually benefiting, not just enrolled."],
          ["Headroom", "Modeled territory capacity \u2014 how much room a state/province has for additional units before cannibalization risk rises, used by the Growth agent's supply-and-quality gate."],
          ["Verified vs. modeled", "Verified centers are confirmed from public records (64); modeled centers extend the network to demonstration scale for cross-cluster propagation and governance testing. Both counts are always shown together, never blended into one figure."],
          ["FDD Item 20", "The Franchise Disclosure Document's official unit count \u2014 244 authoritative units. This is the number that governs; the modeled network's larger figure is explicitly reconciled against it, not a replacement for it."],
          ["Royalty / brand fund", "Royalty: the percentage of gross revenue owed to the franchisor per center. Brand fund: the smaller percentage earmarked for network-wide marketing. Both computed the same way for every center (see royaltyOf)."],
          ["Recovery time (\u03C4 / tau)", "Modeled weeks for a center's performance to return to baseline after a disruption \u2014 the primary early-warning metric; \u22653 weeks triggers a support-plan recommendation."],
          ["Governance boundary", "The single point (makeCommitment) where a proposal becomes a real, human-owned action. Nothing in this model auto-executes past that boundary \u2014 every commitment has a named approver role."],
          ["Condition (thriving / watch / at-risk)", "A center's likelihood-weighted status, resolved to one label at review time; between reviews it's carried as a probability split, not a false-precision single state."],
          ["Measurement confidence / staleness", "How much a center's data has aged since its last review \u2014 confidence decays with time and is restored by a fresh review, shown throughout as a percentage."],
          ["Support path", "One of up to three candidate interventions (retention outreach, staffing stabilization, unit-economics program, trial-conversion review, or standard monitoring) a Director can commit to a center \u2014 see Operations Dynamics and Success Rate."],
          ["Cross-agent conflict", "When two of the four evaluation agents propose contradictory reads on the same unit \u2014 flagged, not auto-resolved; a human reviews and decides."]
        ];
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 10 }, children: "Every term used elsewhere in this model, defined once, in plain language." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: TERMS.map(([t, d], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "9px 12px", borderTop: i ? "1px solid #f0f0f0" : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 11, color: INK }, children: t }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10, color: "#444", lineHeight: 1.5, marginTop: 2 }, children: d })
          ] }, t)) })
        ] });
      })() }),
      tab === "risk" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const riskOverride = opt.quantum && opt.quantum.overrides && opt.quantum.overrides.risk;
        const riskGlobalPosture = opt.quantum && opt.quantum.approved || "realistic";
        const riskOverrideActive = !!(riskOverride && riskOverride !== riskGlobalPosture);
        const riskOv = riskOverrideActive ? computeOverrideView(rawCenters, adj, riskOverride, opt.week, LEADS) : null;
        const riskCenters = riskOv ? riskOv.centers : centers;
        const riskRailData = riskOv ? riskOv.railData : railData;
        const riskRed = riskOverrideActive ? riskCenters.filter((c) => c.eb < 0).map((c) => c.name) : red;
        const riskRanked = (() => {
          const risks = [];
          riskCenters.filter((c) => riskRed.includes(c.name)).forEach((c) => {
            const rec = fbTau(c, cap.week);
            const fresh = fbCoherence(c, cap.week);
            const infl = fbInflection(c);
            const factors = [];
            if (rec >= 3) factors.push("recovery time " + rec.toFixed(1) + "w");
            if (fresh < 0.5) factors.push("stale measurement (" + Math.round(fresh * 100) + "%)");
            if (infl && infl > cap.week && infl <= cap.week + 3) factors.push("retention inflection W" + Math.round(infl));
            const recs = riskRailData.recommendations.filter((r) => r.targetIds && r.targetIds.includes(c.name));
            const owner = recs.length > 0 ? recs[0].governance.approver : "\u2014";
            risks.push({ center: c.name, health: c.health, eb: c.eb, factors, owner, status: recs.length > 0 ? recs[0].governance.allowed ? "allowed" : "held" : "no proposal" });
          });
          return risks.sort((a, b) => a.health - b.health);
        })();
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          riskOverrideActive && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${VIO}`, borderLeft: `3px solid ${VIO}`, background: "#faf9ff", padding: "6px 10px", marginBottom: 10, fontFamily: "Helvetica", fontSize: 9.5, color: "#3d3470" }, children: [
            "This tab is locally overridden to ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: riskOverride }),
            " \u2014 network-wide posture is ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: riskGlobalPosture }),
            ". The at-risk list and factors below reflect the override. Set from Quantum PM."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 10 }, children: "Every unit currently running below breakeven, ranked by health, with contributing factors and the owning approver role." }),
          riskRanked.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${GRN}`, borderLeft: `3px solid ${GRN}`, background: "#f3fdf7", padding: "10px 12px", fontFamily: "Helvetica", fontSize: 9.5, color: "#1b5e20" }, children: "No units below breakeven this cycle." }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: riskRanked.map((r, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "10px 12px", borderTop: i ? "1px solid #f0f0f0" : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 4, gap: 8, flexWrap: "wrap" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: 1 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 11, color: INK }, children: r.center }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT, marginTop: 1 }, children: [
                  "Health ",
                  r.health,
                  "/100 \xB7 EBITDA $",
                  r.eb,
                  "k"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { textAlign: "right" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, color: r.status === "allowed" ? GRN : r.status === "held" ? AMB : MUT, letterSpacing: 0.4 }, children: r.status.toUpperCase() }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: MUT }, children: r.owner })
              ] })
            ] }),
            r.factors.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9, color: "#555", lineHeight: 1.4 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Factors: " }),
              r.factors.join(" \xB7 ")
            ] })
          ] }, r.center)) })
        ] });
      })() }),
      tab === "fdd" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 10 }, children: [
            "Franchise Disclosure Document Item 20 reconciliation. The 244 authoritative units (FDD statement) are held against this model's 64 verified + ",
            centers.length - 64,
            " modeled units. Figures are always stated separately; never blended."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FDDReconciliationPanel, { centers })
        ] });
      })() }),
      tab === "review" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EngineErrorBoundary, { children: (() => {
        const actions = ledger.length;
        const allowed = (railData.recommendations || []).filter((r) => r.governance.allowed).length;
        const held = (railData.recommendations || []).filter((r) => !r.governance.allowed).length;
        const committed = Object.keys(dyn.committed || {}).length;
        const completed = Object.keys(dyn.completed || {}).length;
        const conflicts = (railData.conflicts || []).length;
        const topBlind = red.slice(0, 3);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `4px solid ${INK}`, background: "#fff", padding: "12px 14px", marginBottom: 10, fontFamily: "Helvetica", lineHeight: 1.6 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 9.5, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: MUT, marginBottom: 6 }, children: [
              "Session digest \xB7 week ",
              cap.week
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11, color: "#222", lineHeight: 1.7 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: actions }),
              " actions logged across the system. ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: allowed }),
              " proposals approved, ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: held }),
              " held pending additional review. ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: committed }),
              " support paths committed this session, ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: completed }),
              " marked complete.",
              conflicts > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: conflicts }),
                " cross-agent conflict",
                conflicts > 1 ? "s" : "",
                " flagged and documented. "
              ] }),
              red.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
                red.length,
                " unit",
                red.length > 1 ? "s" : "",
                " remain",
                red.length > 1 ? "" : "",
                " below breakeven breakpoint (see Risk Register). "
              ] }),
              alerts.filter((a) => a.eta <= 90).length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
                alerts.filter((a) => a.eta <= 90).length,
                " early-warning signals active within 90 days. "
              ] })
            ] })
          ] }),
          actions > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: "Top actions this session" }),
            ledger.slice(0, 8).map((e, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, padding: "7px 11px", borderTop: i ? "1px solid #f0f0f0" : "none", display: "flex", gap: 8, alignItems: "baseline" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 8, fontWeight: 700, letterSpacing: 0.3, color: MUT, textTransform: "uppercase", minWidth: 50 }, children: e.tab }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: INK, minWidth: 60 }, children: e.actor }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: "#555" }, children: e.text })
            ] }, i))
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, padding: "7px 11px", borderBottom: `1px solid ${RULE}` }, children: "Snapshot" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { padding: "10px 12px", fontFamily: "Helvetica", fontSize: 9.5 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 6 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Network health:" }),
                " ",
                Math.round(centers.reduce((a, c) => a + c.health, 0) / centers.length),
                "/100 (",
                trend.r,
                " rising, ",
                trend.h,
                " steady, ",
                trend.d,
                " declining)"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 6 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Approver capacity:" }),
                " ",
                8 - cap.used,
                " of 8 points remaining this week"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 6 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Growth pipeline:" }),
                " ",
                LEADS.filter((l) => (leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0) === 5).length,
                " signed, ",
                LEADS.filter((l) => (leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0) === 4).length,
                " in LOI"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "Data freshness:" }),
                " ",
                centers.filter((c) => !staleBase(c)).length,
                " of ",
                centers.length,
                " centers measured this cycle"
              ] })
            ] })
          ] })
        ] });
      })() }),
      tab === "blockers" && (() => {
        const recs = railData.recommendations;
        const territoryHeld = recs.filter((r) => r.agent === AGENT.growth && r.supplyGate && r.supplyGate.blocked && r.supplyGate.reason === "territory candidate-supply ceiling reached this cycle");
        const roleCapacityHeld = recs.filter((r) => r.supplyGate && r.supplyGate.blocked && /FBC capacity ceiling|Owner outreach capacity ceiling/.test(r.supplyGate.reason || ""));
        const qualityHeld = recs.filter((r) => r.agent === AGENT.growth && r.supplyGate && r.supplyGate.blocked && r.supplyGate.reason !== "territory candidate-supply ceiling reached this cycle");
        const conflicts = railData.conflicts || [];
        const stalledLeads = LEADS.filter((l) => {
          const s = leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0;
          return s > 0 && s < 5 && hash(l.n + "stall") % 5 === 0;
        });
        const openWhiteSpace = NET_STATES.filter((ns) => {
          const cs = states[ns.s] || [];
          return ns.h[0] > 0.32 && cs.length < 3 && !LEADS.some((l) => l.region === ns.s && l.stage0 < 5);
        }).length;
        const blockers = [
          { label: "Territory capacity ceilings reached", n: territoryHeld.length, detail: "Growth-agent candidates held because their territory is already at its finite absorption ceiling this cycle", tab: "leads" },
          { label: "Quality-gated candidates", n: qualityHeld.length, detail: "below the fit or liquidity floor \u2014 held, not declined outright", tab: "leads" },
          { label: "FBC / Owner role capacity exceeded", n: roleCapacityHeld.length, detail: "more units need support than the approver role can carry in a week \u2014 see Approver Workload", tab: "workload" },
          { label: "Cross-agent conflicts open", n: conflicts.length, detail: "a proposal contradicted by another agent's read on the same unit", tab: "rail" },
          { label: "Deals stalled mid-funnel", n: stalledLeads.length, detail: "active candidates with no forward movement logged this cycle", tab: "leads" },
          { label: "White-space territories with zero coverage", n: openWhiteSpace, detail: "real headroom, no unit and no active candidate \u2014 nothing currently addressing them", tab: "whitespace" }
        ];
        const totalBlocked = blockers.reduce((a, b) => a + b.n, 0);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: "Every figure below is read from a real gate elsewhere in the system \u2014 the Growth agent's own capacity and quality checks, the conflict detector, deal-stage movement, and territory coverage \u2014 not a separate scoring model layered on top." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", border: `1px solid ${RULE}`, borderLeft: `3px solid ${totalBlocked > 0 ? AC : GRN}`, background: "#fff", marginBottom: 10, padding: "8px 12px" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 22, fontWeight: 800, color: totalBlocked > 0 ? AC : GRN }, children: totalBlocked }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: "total items currently blocking growth" })
          ] }) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { border: `1px solid ${RULE}`, background: "#fff" }, children: blockers.map((b, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "9px 12px", borderTop: i ? "1px solid #f0f0f0" : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, fontWeight: 700 }, children: b.label }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT }, children: b.detail })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 10 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 20, fontWeight: 800, color: b.n > 0 ? AC : GRN }, children: b.n }),
              b.n > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { onClick: () => setTab(b.tab), style: { fontFamily: "Helvetica", fontSize: 8.5, color: AC, cursor: "pointer", background: "none", border: "none", padding: 0, textDecoration: "underline" }, children: "review \u2192" })
            ] })
          ] }, i)) })
        ] });
      })(),
      tab === "deals" && (() => {
        const stageOfLive = (l) => leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0;
        const rows = [...LEADS].sort((a, b) => stageOfLive(b) - stageOfLive(a) || b.fit - a.fit);
        const commission = (l) => Math.round(l.liquidity * 0.1);
        const validationRiskFor = (l) => {
          const cs = states[l.region] || [];
          if (cs.length === 0) return null;
          const atRiskShare = cs.filter((c) => conditionOf(c).label === "at-risk").length / cs.length;
          return atRiskShare >= 0.25 ? "high" : atRiskShare >= 0.12 ? "moderate" : "low";
        };
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: "Every active and closed deal, one row each \u2014 the same LEADS record the Growth Pipeline funnel and the Growth agent both read, presented as a deal sheet instead of a kanban. Validation risk reads the real condition of existing units in the candidate's own territory \u2014 a proxy for what that candidate will actually hear on reference calls." }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", overflowX: "auto" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", fontFamily: "Helvetica", fontSize: 8, fontWeight: 700, letterSpacing: 0.4, textTransform: "uppercase", color: MUT, padding: "7px 10px", borderBottom: `1px solid ${RULE}`, minWidth: 860 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 150 }, children: "Prospect" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 60 }, children: "Region" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 70 }, children: "Market" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 90 }, children: "Deal type" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 110 }, children: "Stage" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 50 }, children: "Fit" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 80 }, children: "Liquidity" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 100 }, children: "Est. commission" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 100 }, children: "Validation risk" })
            ] }),
            rows.map((l) => {
              const s = stageOfLive(l);
              const signed = s === 5;
              const vr = validationRiskFor(l);
              return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { onClick: () => {
                setTab("leads");
              }, style: { display: "flex", padding: "6px 10px", borderTop: "1px solid #f0f0f0", cursor: "pointer", minWidth: 860, background: signed ? "#f7fbf8" : "#fff" }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 150, fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700 }, children: l.n }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 60, fontFamily: "Helvetica", fontSize: 9.5 }, children: l.region }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 70, fontFamily: "Helvetica", fontSize: 9.5, color: l.market === "Canada" ? VIO : MUT }, children: l.market }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 90, fontFamily: "Helvetica", fontSize: 9.5 }, children: l.dealType }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 110, fontFamily: "Helvetica", fontSize: 9.5, fontWeight: signed ? 700 : 400, color: signed ? GRN : INK }, children: STAGES6[s] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 50, fontFamily: "Helvetica", fontSize: 9.5 }, children: l.fit }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { width: 80, fontFamily: "Helvetica", fontSize: 9.5 }, children: [
                  "$",
                  l.liquidity,
                  "k"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 100, fontFamily: "Helvetica", fontSize: 9.5, fontWeight: signed ? 700 : 400 }, children: signed ? "$" + commission(l) + "k" : "$" + commission(l) + "k (if signed)" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 100, fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, color: vr === "high" ? AC : vr === "moderate" ? AMB : vr === "low" ? GRN : MUT }, children: vr || "n/a" })
              ] }, l.id);
            })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 6 }, children: "Click any row to open it in Growth Pipeline for the full document flow." })
        ] });
      })(),
      tab === "onboarding" && (() => {
        const stageOfLive = (l) => leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0;
        const signedLeads = LEADS.filter((l) => stageOfLive(l) === 5);
        const MILESTONES = ["Site selected", "Lease signed", "Build-out", "Staff hired", "Sensei training", "Grand opening"];
        const planFor = (l) => {
          const r = rng(hash(l.n + "proj"));
          let day = 0;
          const durations = [14, 10, 45, 21, 14, 7];
          return MILESTONES.map((m, i) => {
            const dur = durations[i];
            const jitter = Math.floor(r() * 6) - 3;
            const start = day;
            const end = day + dur + jitter;
            day = end;
            const today = 30;
            const status = end < today ? "done" : start < today ? "in progress" : "not started";
            return { m, start, end: Math.max(start + 1, end), status };
          });
        };
        const ENROLL_TARGET = 35;
        const enrollFor = (l) => {
          const r = rng(hash(l.n + "enroll"));
          const buildOutEnd = 14 + 10 + 45;
          const openDay = buildOutEnd + 21 + 14 + 7;
          const pace = 0.6 + r() * 0.7;
          const today = 30;
          const enrolledSoFar = Math.round(Math.min(ENROLL_TARGET, today / openDay * ENROLL_TARGET * pace));
          const daysToOpen = Math.max(0, openDay - today);
          const onPace = enrolledSoFar >= Math.round(today / openDay * ENROLL_TARGET * 0.85);
          const atRisk = daysToOpen <= 30 && enrolledSoFar < ENROLL_TARGET * 0.7;
          return { enrolledSoFar, target: ENROLL_TARGET, daysToOpen, onPace, atRisk, openDay };
        };
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: [
            "A signed deal is not yet an open center. Six real milestones from signature to grand opening, gated in sequence \u2014 training cannot start before build-out clears, opening cannot happen before training does \u2014 tracked alongside pre-sale enrollment, the second half of the bottleneck construction alone doesn't capture. ",
            signedLeads.length,
            " deal",
            signedLeads.length === 1 ? "" : "s",
            " currently in this stage."
          ] }),
          signedLeads.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, color: MUT, border: `1px solid ${RULE}`, padding: "10px 12px" }, children: "No signed deals yet this session \u2014 sign one in Growth Pipeline to populate a project plan." }) : signedLeads.map((l) => {
            const plan = planFor(l);
            const totalDays = plan[plan.length - 1].end;
            const en = enrollFor(l);
            return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px", marginBottom: 10 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, marginBottom: 6 }, children: [
                l.n,
                " \u2014 ",
                l.region,
                " ",
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontWeight: 400, color: MUT, fontSize: 9 }, children: [
                  "(",
                  totalDays,
                  "-day plan to opening)"
                ] })
              ] }),
              plan.map((p, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, marginBottom: 3 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 120, fontFamily: "Helvetica", fontSize: 9 }, children: p.m }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1, height: 8, background: "#eee", borderRadius: 4, position: "relative" }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", left: p.start / totalDays * 100 + "%", width: Math.max(2, (p.end - p.start) / totalDays * 100) + "%", height: 8, borderRadius: 4, background: p.status === "done" ? GRN : p.status === "in progress" ? AMB : RULE } }) }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 80, fontFamily: "Helvetica", fontSize: 8.5, color: p.status === "done" ? GRN : p.status === "in progress" ? AMB : MUT, textTransform: "capitalize" }, children: p.status })
              ] }, i)),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 8, paddingTop: 8, borderTop: `1px solid ${RULE}`, display: "flex", alignItems: "center", gap: 8 }, children: [
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 120, fontFamily: "Helvetica", fontSize: 9, fontWeight: 700 }, children: "Pre-sale enrollment" }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1, height: 8, background: "#eee", borderRadius: 4 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 8, width: Math.min(100, en.enrolledSoFar / en.target * 100) + "%", borderRadius: 4, background: en.atRisk ? AC : en.onPace ? GRN : AMB } }) }),
                /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { width: 80, fontFamily: "Helvetica", fontSize: 8.5, color: en.atRisk ? AC : en.onPace ? GRN : AMB }, children: [
                  en.enrolledSoFar,
                  "/",
                  en.target
                ] })
              ] }),
              en.atRisk && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: AC, marginTop: 3 }, children: [
                "\u26A0 ",
                en.daysToOpen,
                "d to opening, enrollment trailing \u2014 the exact bottleneck pattern that prompted corporate's New Center Opening enrollment mandate."
              ] })
            ] }, l.id);
          })
        ] });
      })(),
      tab === "growthfin" && (() => {
        const stageOfLive = (l) => leadStage[l.id] !== void 0 ? leadStage[l.id] : l.stage0;
        const signedLeads = LEADS.filter((l) => stageOfLive(l) === 5);
        const AVG_ROYALTY_PER_UNIT = 9.5;
        const currentRunRate = centers.reduce((a, c) => a + royaltyOf(c).royalty, 0);
        const horizon = [0, 6, 12, 18, 24, 36];
        const ANNUAL_ATTRITION_RATE = 0.135;
        const existingUnitCount = centers.length;
        const projection = horizon.map((m) => {
          const rampedContribution = signedLeads.length * AVG_ROYALTY_PER_UNIT * adoptionRamp(m);
          const yearsElapsed = m / 12;
          const expectedAttrition = Math.round(existingUnitCount * ANNUAL_ATTRITION_RATE * yearsElapsed);
          const netNewUnits = +(signedLeads.length * adoptionRamp(m) - expectedAttrition * 0.3).toFixed(1);
          return { m, total: +(currentRunRate + rampedContribution).toFixed(0), newContribution: +rampedContribution.toFixed(1), expectedAttrition, netNewUnits };
        });
        const maxV = Math.max(...projection.map((p) => p.total));
        const netAt36 = projection[projection.length - 1].netNewUnits;
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 10.5, color: "#444", lineHeight: 1.55, marginBottom: 8 }, children: [
            "Current royalty run-rate, plus what the ",
            signedLeads.length,
            " deal",
            signedLeads.length === 1 ? "" : "s",
            " already signed this session contribute as they ramp toward target economics \u2014 using the same adoption curve Programs states elsewhere, not a separate growth-rate assumption. Net of expected attrition on the existing base, not gross signings alone."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", border: `1px solid ${RULE}`, borderLeft: `3px solid ${INK}`, background: "#fff", marginBottom: 10 }, children: [["$" + currentRunRate.toFixed(0) + "k", "current royalty run-rate"], [signedLeads.length, "deals signed, ramping"], [(netAt36 >= 0 ? "+" : "") + netAt36, "net new units at 36mo (gross minus attrition)"]].map(([v, l], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 150px", padding: "8px 12px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 18, fontWeight: 800, color: i === 2 && netAt36 < 0 ? AC : INK }, children: v }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 2 }, children: l })
          ] }, i)) }),
          netAt36 < signedLeads.length && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${AMB}`, borderLeft: `3px solid ${AMB}`, background: "#fdf8ec", padding: "7px 11px", marginBottom: 10, fontFamily: "Helvetica", fontSize: 9, color: "#5a4520" }, children: [
            "Gross signings (",
            signedLeads.length,
            ") exceed net new units (",
            netAt36,
            ") at the 36-month horizon \u2014 the existing base's expected attrition is absorbing part of this cycle's growth. Net unit change, not gross openings, is the number that should gate whether new-unit selling is outpacing retention."
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, background: "#fff", padding: "10px 12px" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 6 }, children: "Projected royalty run-rate by month" }),
            projection.map((p, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, marginBottom: 4 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { width: 50, fontFamily: "Helvetica", fontSize: 9 }, children: p.m === 0 ? "today" : p.m + "mo" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1, height: 8, background: "#eee", borderRadius: 4 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 8, width: p.total / maxV * 100 + "%", borderRadius: 4, background: VIO } }) }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { width: 60, textAlign: "right", fontFamily: "Helvetica", fontSize: 9, fontWeight: 700 }, children: [
                "$",
                p.total,
                "k"
              ] })
            ] }, i))
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 8.5, color: MUT, marginTop: 8 }, children: "Modeled/illustrative, same disclosure as every other financial figure in this artifact \u2014 pending live MyStudio and QuickBooks integration." })
        ] });
      })(),
      tab === "fivebasis" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FiveBasisTab, {}),
      tab === "warning" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EarlyWarningTab, {}),
      tab === "signals" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SignalsTab, {}),
      ["acquire", "deliver", "retain", "operate", "plan", "metrics", "failure"].includes(tab) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OpsSystem, { view: tab }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { borderTop: `2px solid ${INK}`, marginTop: 16, paddingTop: 10, fontFamily: "Helvetica" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 0, flexWrap: "wrap", marginBottom: 8 }, children: [[centers.length + "-unit", "modeled network", INK], [Math.round(trend.r / centers.length * 100) + "%", trend.r + " rising", GRN], [trend.h, "holding steady", AMB], [trend.d, "on a support path", AC], [alerts.filter((a) => a.eta <= 90).length, "early-warning flags", VIO]].map(([v, l, c], i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { flex: "1 1 100px", padding: "0 12px", borderLeft: i ? `1px solid ${RULE}` : "none" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 18, fontWeight: 800, color: c, lineHeight: 1 }, children: v }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8, color: MUT, letterSpacing: 0.3, marginTop: 2 }, children: l })
        ] }, i)) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 8.5, color: MUT, lineHeight: 1.6, borderTop: `1px solid ${RULE}`, paddingTop: 6 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: INK }, children: "SUBMISSION ARTIFACT" }),
          " \xB7 64 verified from public records \xB7 FDD Item 20: 244 authoritative \xB7 seeds illustrative, structures computed live \xB7 Pratik Singh \xB7 (925) 699-0867"
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 8, color: "#999", lineHeight: 1.5, marginTop: 4 }, children: "Data provenance: every figure across every view in this artifact is derived from one canonical state (centers, states, railData, dyn, ledger) at render time \u2014 no view holds a separate copy, and no figure is pre-computed or cached. Where a tab states this locally, it is this same guarantee, not a separate one." })
      ] })
    ] });
  }
  var FB_DIMS = ["engagement", "margin", "staffing", "compliance", "community"];
  var FB_BOUNDARY = {
    schema: "cn-boundary/1.1",
    week: 7,
    // global measurement time
    clusters: [
      { id: "WEST", members: ["CA", "OR"], coupling: 0.78 },
      { id: "CENTRAL", members: ["TX"], coupling: 0.31 },
      { id: "EAST", members: ["NY"], coupling: 0.22 }
    ],
    centers: [
      {
        id: "CA",
        label: "Pleasanton CA \u2014 Provenance",
        pos: [22, 62],
        state: "thriving",
        col: "#2fbf5f",
        vals: { engagement: 0.78, margin: 0.87, staffing: 0.85, compliance: 0.98, community: 0.82 },
        mom: { engagement: 0.28, margin: 0.18, staffing: 0.08, compliance: 0, community: 0.22 },
        lastMeasured: 1,
        shock: null,
        tuition: 289,
        Tc: 349,
        // pricing ceiling (market tolerance limit)
        tau0: 1.8
      },
      {
        id: "TX",
        label: "Austin TX",
        pos: [48, 78],
        state: "watch",
        col: "#d9a520",
        vals: { engagement: 0.5, margin: 0.65, staffing: 0.62, compliance: 0.9, community: 0.48 },
        mom: { engagement: -0.45, margin: -0.05, staffing: -0.35, compliance: 0, community: -0.4 },
        lastMeasured: 4,
        shock: { week: 3, kind: "Lead Sensei departure", amp: 0.34 },
        tuition: 275,
        Tc: 312,
        tau0: 3.1
      },
      {
        id: "NY",
        label: "Brooklyn NY",
        pos: [82, 40],
        state: "at-risk",
        col: "#e03535",
        vals: { engagement: 0.3, margin: 0.21, staffing: 0.32, compliance: 0.85, community: 0.15 },
        mom: { engagement: -0.7, margin: -0.85, staffing: -0.65, compliance: 0, community: -0.9 },
        lastMeasured: 6,
        shock: { week: 1, kind: "Tuition increase +12%", amp: 0.61 },
        tuition: 335,
        Tc: 341,
        // 6 dollars from the pricing ceiling
        tau0: 6.4
      },
      {
        id: "OR",
        label: "Portland OR",
        pos: [16, 30],
        state: "stale",
        col: "#8a8fa8",
        vals: { engagement: 0.45, margin: 0.52, staffing: 0.55, compliance: 0.92, community: 0.38 },
        mom: { engagement: 0, margin: 0, staffing: 0, compliance: 0, community: 0 },
        lastMeasured: 9,
        shock: null,
        tuition: 259,
        Tc: 318,
        tau0: 2.6
      }
    ]
  };
  var fbClamp = (v) => Math.max(0.02, Math.min(1, v));
  var fbDimVal = (c, d, w) => fbClamp(c.vals[d] - c.mom[d] * 0.055 * (7 - w));
  var fbHealth = (c, w) => FB_DIMS.reduce((s, d) => s + fbDimVal(c, d, w), 0) / 5;
  var FB_GAMMA = 0.173;
  var fbCoherence = (c, w) => Math.exp(-FB_GAMMA * Math.max(0, w - (7 - c.lastMeasured)));
  var fbTau = (c, w) => {
    const h = fbHealth(c, w);
    const tcGap = Math.max(0.02, (c.Tc - c.tuition) / c.Tc);
    return c.tau0 * (1 / Math.max(0.15, h)) * (0.4 / tcGap) * 0.5;
  };
  var fbTauAlert = (t) => t >= 3 ? "critical" : t >= 2 ? "watch" : "ok";
  var fbRecoverySig = (c, w) => {
    if (!c.shock) return null;
    const t = w - c.shock.week;
    if (t < 0) return null;
    const T = fbTau(c, w);
    const omega = 2.2;
    const pts = [];
    for (let i = 0; i <= 40; i++) {
      const tt = i / 40 * 10;
      pts.push({ t: tt, a: c.shock.amp * Math.exp(-tt / T) * Math.cos(omega * tt) });
    }
    const residual = c.shock.amp * Math.exp(-t / T);
    const Q = omega * T / 2;
    return { pts, residual, Q, elapsed: t };
  };
  var fbInflection = (c) => {
    const declRate = -FB_DIMS.reduce((s, d) => s + c.mom[d], 0) / 5 * 0.055;
    if (declRate <= 1e-3) return null;
    const h7 = fbHealth(c, 7);
    const weeksTo = (h7 - 0.5) / declRate;
    return weeksTo > 0 && weeksTo < 40 ? 7 + weeksTo : weeksTo <= 0 ? 7 : null;
  };
  var fbClusterKernel = (cl, w) => {
    const cs = FB_BOUNDARY.centers.filter((c) => cl.members.includes(c.id));
    if (cs.length < 2) {
      const c = cs[0];
      return { k: fbHealth(c, w), n: 1, robust: cl.coupling > 0.5 };
    }
    let k = 0, pairs = 0;
    for (let i = 0; i < cs.length; i++)
      for (let j = i + 1; j < cs.length; j++) {
        k += FB_DIMS.reduce((s, d) => s + (1 - Math.abs(fbDimVal(cs[i], d, w) - fbDimVal(cs[j], d, w))), 0) / 5;
        pairs++;
      }
    return { k: k / pairs, n: cs.length, robust: cl.coupling > 0.5 };
  };
  var FB_BG = "#05060e";
  var FB_PANEL = "#0a0d1f";
  var FB_LINE = "#1c2650";
  var FB_INKD = "#e8ecff";
  var FB_MUTD = "#5a6a9e";
  var FB_CY = "#27c8e8";
  var FB_VI = "#9d7bff";
  var FB_MONO = "'SF Mono','Cascadia Code',Consolas,monospace";
  var fbStateCol = (h) => h < 0.35 ? "#e03535" : h < 0.6 ? "#d9a520" : "#2fbf5f";
  var FBChip = ({ children, col = FB_MUTD }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: FB_MONO, fontSize: 9, color: col, border: `1px solid ${col}44`, borderRadius: 2, padding: "1px 5px" }, children });
  var FBBar = ({ v, col }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 4, background: "#141d3d", borderRadius: 2 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: 4, width: `${v * 100}%`, borderRadius: 2, background: col || fbStateCol(v), transition: "width .3s" } }) });
  function BasisMap({ w, sel, setSel, q }) {
    const links = (0, import_react.useMemo)(() => {
      const out = [];
      for (let i = 0; i < FB_BOUNDARY.centers.length; i++)
        for (let j = i + 1; j < FB_BOUNDARY.centers.length; j++) {
          const a = FB_BOUNDARY.centers[i], b = FB_BOUNDARY.centers[j];
          const k = FB_DIMS.reduce((s, d) => s + (1 - Math.abs(fbDimVal(a, d, w) - fbDimVal(b, d, w))), 0) / 5;
          const sameCluster = FB_BOUNDARY.clusters.some((cl) => cl.members.includes(a.id) && cl.members.includes(b.id));
          out.push({ a, b, k, sameCluster });
        }
      return out;
    }, [w]);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 100 100", style: { width: "100%", height: 360, background: FB_BG, borderRadius: 6, border: `1px solid ${FB_LINE}` }, children: [
      links.map((l, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        "line",
        {
          x1: l.a.pos[0],
          y1: l.a.pos[1],
          x2: l.b.pos[0],
          y2: l.b.pos[1],
          stroke: l.sameCluster ? FB_VI : FB_CY,
          strokeWidth: 0.15 + l.k * 0.7,
          strokeDasharray: l.sameCluster ? "1.5 1" : "none",
          opacity: 0.15 + l.k * 0.5
        },
        i
      )),
      FB_BOUNDARY.centers.map((c) => {
        const h = fbHealth(c, w);
        const coh = fbCoherence(c, w);
        const r = 3 + (1 - h) * 6;
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", { onClick: () => setSel(sel === c.id ? null : c.id), style: { cursor: "pointer" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: c.pos[0], cy: c.pos[1], r, fill: fbStateCol(h), opacity: 0.12 }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: c.pos[0], cy: c.pos[1], r: 2.4, fill: fbStateCol(h), opacity: 0.35 + coh * 0.6 }),
          coh < 0.6 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: c.pos[0], cy: c.pos[1], r: 3.6, fill: "none", stroke: FB_MUTD, strokeWidth: 0.25, strokeDasharray: "0.8 0.8" }),
          sel === c.id && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", { cx: c.pos[0], cy: c.pos[1], r: 4.6, fill: "none", stroke: FB_INKD, strokeWidth: 0.3 }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: c.pos[0], y: c.pos[1] - r - 1.5, textAnchor: "middle", fill: FB_INKD, fontSize: 2.8, fontFamily: FB_MONO, children: c.id }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("text", { x: c.pos[0], y: c.pos[1] + r + 3.4, textAnchor: "middle", fill: FB_MUTD, fontSize: 2.2, fontFamily: FB_MONO, children: `health ${h.toFixed(2)} \xB7 rec ${fbTau(c, w).toFixed(1)}w` })
        ] }, c.id);
      })
    ] });
  }
  var FB_GATES = ["RECORD", "HOLD", "GATE", "COMMIT", "ACT"];
  var FB_GOVERNORS = [
    { id: "G1", name: "Financial materiality", test: (c, w) => fbDimVal(c, "margin", w) >= 0.25 },
    { id: "G2", name: "Engagement integrity", test: (c, w) => fbDimVal(c, "engagement", w) >= 0.35 },
    { id: "G3", name: "Child safety clearance", test: (c, w) => fbDimVal(c, "compliance", w) >= 0.8 },
    { id: "G4", name: "Data integrity (no inference past gap)", test: (c, w) => fbCoherence(c, w) >= 0.35 }
  ];
  function BasisCircuit({ w, q }) {
    const GNAMES = FB_GATES;
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gap: 10 }, children: FB_BOUNDARY.centers.map((c) => {
      const govs = FB_GOVERNORS.map((g) => ({ ...g, pass: g.test(c, w) }));
      const blocked = govs.filter((g) => !g.pass);
      const coh = fbCoherence(c, w);
      const haltAt = coh < 0.35 ? 1 : blocked.length ? 2 : 4;
      return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { background: FB_PANEL, border: `1px solid ${FB_LINE}`, borderRadius: 6, padding: "10px 14px" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", marginBottom: 8 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: FB_MONO, fontSize: 11, color: FB_INKD }, children: c.label }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FBChip, { col: blocked.length ? "#e03535" : "#2fbf5f", children: coh < 0.35 ? "HELD \xB7 stale data \u2014 run a progress review first" : blocked.length ? `HELD \xB7 ${blocked.map((g) => g.id).join(" ")}` : "CLEARED TO DECIDE" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", alignItems: "center", gap: 0 }, children: GNAMES.map((g, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", flex: i < GNAMES.length - 1 ? 1 : "none" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: {
            fontFamily: FB_MONO,
            fontSize: 9,
            padding: "5px 8px",
            borderRadius: 3,
            border: `1px solid ${i <= haltAt ? i === haltAt && haltAt < 4 ? "#e03535" : FB_CY : FB_LINE}`,
            color: i <= haltAt ? i === haltAt && haltAt < 4 ? "#e03535" : FB_CY : FB_MUTD,
            background: i < haltAt ? `${FB_CY}11` : "transparent"
          }, children: g }),
          i < GNAMES.length - 1 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1, height: 1, background: i < haltAt ? FB_CY : FB_LINE, opacity: i < haltAt ? 0.7 : 0.4 } })
        ] }, g)) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 6, marginTop: 8, flexWrap: "wrap" }, children: govs.map((g) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(FBChip, { col: g.pass ? "#2fbf5f" : "#e03535", children: [
          g.id,
          " ",
          g.pass ? "\u2713" : "\u2717",
          " ",
          g.name
        ] }, g.id)) })
      ] }, c.id);
    }) });
  }
  function BasisCalendar({ w, q }) {
    const WEEKS = Array.from({ length: 14 }, (_, i) => i);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { background: FB_PANEL, border: `1px solid ${FB_LINE}`, borderRadius: 6, padding: 14, overflowX: "auto" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gridTemplateColumns: `90px repeat(${WEEKS.length}, 1fr)`, gap: 2, minWidth: 620 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {}),
        WEEKS.map((wk) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: wk === w ? FB_CY : FB_MUTD, textAlign: "center", borderBottom: wk === w ? `2px solid ${FB_CY}` : `1px solid ${FB_LINE}`, paddingBottom: 3 }, children: [
          "W",
          wk
        ] }, wk)),
        FB_BOUNDARY.centers.map((c) => {
          const pt = fbInflection(c);
          const measuredW = 7 - c.lastMeasured;
          const t = fbTau(c, w);
          const declineW = fbTauAlert(t) === "critical" ? w + 4 : null;
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 9.5, color: FB_INKD, padding: "6px 0" }, children: c.id }),
            WEEKS.map((wk) => {
              const isMeasure = wk === measuredW;
              const isShock = c.shock && wk === c.shock.week;
              const isPage = pt !== null && Math.round(pt) === wk;
              const isDecline = declineW !== null && wk === declineW;
              const past = wk <= w;
              return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(
                "div",
                {
                  title: [isMeasure && "measurement", isShock && c.shock.kind, isPage && "retention inflection \u2014 attrition compounding", isDecline && "predicted decline window"].filter(Boolean).join(" \xB7 "),
                  style: {
                    height: 26,
                    borderRadius: 2,
                    position: "relative",
                    background: past ? `${fbStateCol(fbHealth(c, Math.min(wk, 12)))}22` : "#0d1128",
                    border: `1px solid ${FB_LINE}`,
                    opacity: past ? 1 : 0.55
                  },
                  children: [
                    isMeasure && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", inset: 3, borderRadius: 2, border: `1.5px solid ${FB_CY}` } }),
                    isShock && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", top: 2, left: "50%", transform: "translateX(-50%)", color: "#d9a520", fontSize: 11, lineHeight: 1 }, children: "\u26A1" }),
                    isPage && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", bottom: 1, left: "50%", transform: "translateX(-50%)", color: FB_VI, fontSize: 8, fontFamily: FB_MONO }, children: "Pg" }),
                    isDecline && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", inset: 3, borderRadius: 2, border: `1.5px dashed #e03535` } })
                  ]
                },
                c.id + wk
              );
            })
          ] }, c.id);
        })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 12, marginTop: 10, flexWrap: "wrap" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(FBChip, { col: FB_CY, children: [
          "\u25A2 ",
          "progress review (refreshes data)"
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FBChip, { col: "#d9a520", children: "\u26A1 shock event" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(FBChip, { col: FB_VI, children: [
          "Pg ",
          "retention inflection \u2014 staff/parents start leaving"
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(FBChip, { col: "#e03535", children: [
          "\u25A2 ",
          "predicted decline window (~30d after slow recovery)"
        ] })
      ] })
    ] });
  }
  function BasisPhase({ w, sel, setSel, q }) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gap: 10 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { background: FB_PANEL, border: `1px solid ${FB_LINE}`, borderRadius: 6, padding: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 9, color: FB_VI, letterSpacing: 1, marginBottom: 8 }, children: "PRICING CEILING \u2014 CRITICAL TUITION" }),
        FB_BOUNDARY.centers.map((c) => {
          const frac = c.tuition / c.Tc;
          const danger = frac > 0.92;
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginBottom: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", fontFamily: FB_MONO, fontSize: 9.5, marginBottom: 3 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: FB_INKD }, children: [
                c.id,
                " \xB7 $",
                c.tuition,
                "/mo"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: danger ? "#e03535" : FB_MUTD }, children: [
                "ceiling $",
                c.Tc,
                " \xB7 headroom $",
                c.Tc - c.tuition,
                " ",
                danger && "\xB7 AT PHASE FB_BOUNDARY"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { height: 8, background: "#141d3d", borderRadius: 2, position: "relative" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", left: 0, top: 0, bottom: 0, width: `${frac * 100}%`, background: danger ? "#e03535" : frac > 0.8 ? "#d9a520" : "#2fbf5f", borderRadius: 2, transition: "width .3s" } }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute", left: "100%", top: -2, bottom: -2, width: 2, background: FB_VI, transform: "translateX(-2px)" } })
            ] })
          ] }, c.id);
        }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_MUTD, lineHeight: 1.6 }, children: "Below the ceiling a price move is absorbable; crossing it triggers an abrupt, largely irreversible enrollment decline. NY sits $6 from the boundary \u2014 any increase tips the unit into decline. The ceiling is computed, not discovered: predict it, stay below it." })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { background: FB_PANEL, border: `1px solid ${FB_LINE}`, borderRadius: 6, padding: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 9, color: FB_CY, letterSpacing: 1, marginBottom: 8 }, children: "RECOVERY SIGNATURE \u2014 HOW EACH CENTER ABSORBS A DISRUPTION" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }, children: FB_BOUNDARY.centers.filter((c) => c.shock).map((c) => {
          const rd = fbRecoverySig(c, w);
          const T = fbTau(c, w);
          const fragile = rd.Q > 3;
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${fragile ? "#e0353555" : FB_LINE}`, borderRadius: 4, padding: 10 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", fontFamily: FB_MONO, fontSize: 9.5, marginBottom: 4 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { style: { color: FB_INKD }, children: [
                c.id,
                " \xB7 ",
                c.shock.kind
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { color: fragile ? "#e03535" : "#2fbf5f" }, children: [
                "Q ",
                rd.Q.toFixed(1),
                " \xB7 rec ",
                T.toFixed(1),
                "w"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 100 34", style: { width: "100%", height: 70 }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: "0", y1: "17", x2: "100", y2: "17", stroke: FB_LINE, strokeWidth: "0.4" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
                "polyline",
                {
                  fill: "none",
                  stroke: fragile ? "#e03535" : FB_CY,
                  strokeWidth: "0.8",
                  points: rd.pts.map((p) => `${p.t / 10 * 100},${17 - p.a * 45}`).join(" ")
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
                "polyline",
                {
                  fill: "none",
                  stroke: FB_MUTD,
                  strokeWidth: "0.4",
                  strokeDasharray: "1.5 1.5",
                  points: rd.pts.map((p) => `${p.t / 10 * 100},${17 - c.shock.amp * Math.exp(-p.t / T) * 45}`).join(" ")
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: rd.elapsed / 10 * 100, y1: "0", x2: rd.elapsed / 10 * 100, y2: "34", stroke: FB_CY, strokeWidth: "0.5", opacity: "0.6" })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_MUTD, lineHeight: 1.5 }, children: [
              fragile ? "Slow recovery \u2014 the unit keeps swinging instead of settling. A high recovery index means fragile: intervene before the decline hardens into permanent loss." : "Fast recovery \u2014 shock absorbed, unit re-settling to baseline. Healthy signature.",
              " ",
              "Residual swing now: ",
              (rd.residual * 100).toFixed(0),
              "%."
            ] })
          ] }, c.id);
        }) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { background: FB_PANEL, border: `1px solid ${FB_LINE}`, borderRadius: 6, padding: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 9, color: "#d9a520", letterSpacing: 1, marginBottom: 8 }, children: "RECOVERY TIME \u2014 HOW LONG UNTIL BACK TO BASELINE" }),
        FB_BOUNDARY.centers.map((c) => {
          const t = fbTau(c, w);
          const a = fbTauAlert(t);
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 10, padding: "4px 0", fontFamily: FB_MONO, fontSize: 9.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { minWidth: 26, color: FB_INKD }, children: c.id }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FBBar, { v: Math.min(1, t / 8), col: a === "critical" ? "#e03535" : a === "watch" ? "#d9a520" : "#2fbf5f" }) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { minWidth: 120, color: a === "critical" ? "#e03535" : a === "watch" ? "#d9a520" : FB_MUTD }, children: [
              "rec ",
              t.toFixed(1),
              "w ",
              a === "critical" ? "\u2192 decline ~30d, activate support" : a === "watch" ? "\u2192 lengthening, monitor" : ""
            ] })
          ] }, c.id);
        })
      ] })
    ] });
  }
  function BasisOps({ w, q }) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gap: 10 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { background: FB_PANEL, border: `1px solid ${FB_LINE}`, borderRadius: 6, padding: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 9, color: FB_VI, letterSpacing: 1, marginBottom: 8 }, children: "PEER BENCHMARKING \u2014 benchmarks are shared, center financials never are" }),
        FB_BOUNDARY.clusters.map((cl) => {
          const kk = fbClusterKernel(cl, w);
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 10, padding: "6px 0", borderBottom: `1px solid ${FB_LINE}55`, fontFamily: FB_MONO, fontSize: 9.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { minWidth: 70, color: FB_INKD }, children: cl.id }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { minWidth: 110, color: FB_MUTD }, children: cl.members.join(" \xB7 ") }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { flex: 1 }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FBBar, { v: kk.k, col: kk.robust ? "#2fbf5f" : "#d9a520" }) }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { minWidth: 150, color: kk.robust ? "#2fbf5f" : "#d9a520" }, children: [
              "benchmark alignment ",
              kk.k.toFixed(2),
              " \xB7 coupling ",
              cl.coupling.toFixed(2),
              " ",
              kk.robust ? "\xB7 robust" : "\xB7 fragile \u2014 single-unit shock cascades"
            ] })
          ] }, cl.id);
        }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_MUTD, marginTop: 8, lineHeight: 1.6 }, children: "Only aggregate benchmarks leave each cluster. WEST has redundant peer support: CA can absorb an OR shock. EAST is a single-node cluster \u2014 no error correction available. Priority: build NY's coupling (shared Senseis, referral pipeline) before its next shock." })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { background: FB_PANEL, border: `1px solid ${FB_LINE}`, borderRadius: 6, padding: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 9, color: FB_CY, letterSpacing: 1, marginBottom: 8 }, children: "RETENTION INFLECTION \u2014 WHEN STAFF AND PARENTS START LEAVING" }),
        FB_BOUNDARY.centers.map((c) => {
          const pt = fbInflection(c);
          return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 10, padding: "4px 0", fontFamily: FB_MONO, fontSize: 9.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { minWidth: 26, color: FB_INKD }, children: c.id }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: pt !== null && pt <= w + 2 ? "#e03535" : FB_MUTD }, children: pt === null ? "stable \u2014 no escape trajectory" : pt <= w ? `PASSED (W${Math.round(pt)}) \u2014 parents talking, staff interviewing out. Retention cost now 3\u20134\xD7 intervention cost.` : `projected W${Math.round(pt)} \u2014 intervene before this line, not after` })
          ] }, c.id);
        })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { background: FB_PANEL, border: `1px solid ${FB_LINE}`, borderRadius: 6, padding: 14 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 9, color: FB_MUTD, letterSpacing: 1, marginBottom: 8 }, children: "CANONICAL FB_BOUNDARY DOCUMENT \u2014 cn-boundary/1.1" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("pre", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: "#aab4dd", background: FB_BG, border: `1px solid ${FB_LINE}`, borderRadius: 4, padding: 10, overflowX: "auto", margin: 0, lineHeight: 1.5 }, children: JSON.stringify({ schema: FB_BOUNDARY.schema, week: w, centers: FB_BOUNDARY.centers.map((c) => ({ id: c.id, fbHealth: +fbHealth(c, w).toFixed(3), fbCoherence: +fbCoherence(c, w).toFixed(3), tau_weeks: +fbTau(c, w).toFixed(2), tuition: c.tuition, Tc: c.Tc, fbInflection: fbInflection(c) ? +fbInflection(c).toFixed(1) : null })), clusters: FB_BOUNDARY.clusters.map((cl) => ({ id: cl.id, kernel: +fbClusterKernel(cl, w).k.toFixed(3), coupling: cl.coupling })) }, null, 1) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_MUTD, marginTop: 6 }, children: "This object is the whole truth. Every basis above is a read of it \u2014 nothing else holds state. Serialize it, and any surface (WebXR table, PDF packet, API) reconstructs the full system." })
      ] })
    ] });
  }
  var fbGrowth = (c, w) => {
    const now = fbHealth(c, w), prior = fbHealth(c, Math.max(0, w - 4));
    return { now, prior, delta: now - prior, pct: prior > 0 ? (now - prior) / prior * 100 : 0 };
  };
  var fbPeerRank = (c, w) => {
    const sorted = [...FB_BOUNDARY.centers].sort((a, b) => fbHealth(b, w) - fbHealth(a, w));
    return { rank: sorted.findIndex((x) => x.id === c.id) + 1, of: sorted.length };
  };
  function FranchiseReport({ c, w, q, onClose }) {
    const g = fbGrowth(c, w);
    const pr = fbPeerRank(c, w);
    const rec = fbTau(c, w);
    const fresh = fbCoherence(c, w);
    const infl = fbInflection(c);
    const rd = fbRecoverySig(c, w);
    const cl = FB_BOUNDARY.clusters.find((x) => x.members.includes(c.id));
    const kk = cl ? fbClusterKernel(cl, w) : null;
    const strengths = FB_DIMS.filter((d) => fbDimVal(c, d, w) >= 0.65);
    const gaps = FB_DIMS.filter((d) => fbDimVal(c, d, w) < 0.4);
    const actions = [];
    if (rec >= 3) actions.push("Recovery time above 3 weeks \u2014 activate the support plan inside 30 days");
    if (fresh < 0.5) actions.push("Data is stale \u2014 run a progress review before any decision");
    if (c.Tc - c.tuition <= 15) actions.push("At the pricing ceiling \u2014 hold tuition this cycle");
    if (infl !== null && infl > w && infl <= w + 3) actions.push("Retention inflection inside 3 weeks \u2014 intervene now");
    if (cl && cl.members.length < 2) actions.push("No peer support group \u2014 build shared staffing and referral ties before the next disruption");
    if (!actions.length) actions.push("On plan \u2014 maintain current review cadence");
    const growthPositive = g.delta >= 0;
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 10, background: FB_PANEL, border: `1px solid ${growthPositive ? "#2fbf5f55" : "#e0353555"}`, borderRadius: 6, padding: 16 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_MUTD, letterSpacing: 1.5 }, children: [
            "FRANCHISE REPORT \xB7 WEEK ",
            w
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: FB_MONO, fontSize: 14, color: FB_INKD }, children: c.label })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { onClick: onClose, style: { cursor: "pointer", color: FB_MUTD, fontSize: 14 }, children: "\u2715" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(150px,1fr))", gap: 10, marginTop: 12 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${FB_LINE}`, borderRadius: 4, padding: 10 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD, letterSpacing: 1 }, children: "GROWTH \xB7 4-WEEK" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 17, color: growthPositive ? "#2fbf5f" : "#e03535", marginTop: 3 }, children: [
            growthPositive ? "\u25B2" : "\u25BC",
            " ",
            Math.abs(g.pct).toFixed(1),
            "%"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_MUTD }, children: [
            g.prior.toFixed(2),
            " \u2192 ",
            g.now.toFixed(2)
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${FB_LINE}`, borderRadius: 4, padding: 10 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD, letterSpacing: 1 }, children: "NETWORK RANK" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 17, color: pr.rank === 1 ? "#2fbf5f" : FB_INKD, marginTop: 3 }, children: [
            "#",
            pr.rank,
            " ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontSize: 10, color: FB_MUTD }, children: [
              "of ",
              pr.of
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_MUTD }, children: [
            fbT(q, "benchmark").toLowerCase(),
            " ",
            kk ? Math.round(kk.k * 100) + "%" : "\u2014"
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${FB_LINE}`, borderRadius: 4, padding: 10 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD, letterSpacing: 1 }, children: fbT(q, "recovery").toUpperCase() }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 17, color: rec >= 3 ? "#e03535" : rec >= 2 ? "#d9a520" : "#2fbf5f", marginTop: 3 }, children: [
            rec.toFixed(1),
            "w"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_MUTD }, children: [
            fbT(q, "freshness").toLowerCase(),
            " ",
            Math.round(fresh * 100),
            "%"
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${FB_LINE}`, borderRadius: 4, padding: 10 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD, letterSpacing: 1 }, children: fbT(q, "ceiling").toUpperCase() }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 17, color: c.Tc - c.tuition <= 15 ? "#e03535" : FB_INKD, marginTop: 3 }, children: [
            "$",
            c.Tc - c.tuition
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_MUTD }, children: [
            "$",
            c.tuition,
            " vs $",
            c.Tc,
            " ceiling"
          ] })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(220px,1fr))", gap: 12, marginTop: 12 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: "#2fbf5f", letterSpacing: 1, marginBottom: 5 }, children: "COMPETITIVE STRENGTHS" }),
          strengths.length ? strengths.map((d) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 9.5, color: FB_INKD, padding: "2px 0" }, children: [
            "\u25B8 ",
            d,
            " ",
            fbDimVal(c, d, w).toFixed(2),
            " \u2014 ",
            (() => {
              const best = Math.max(...FB_BOUNDARY.centers.filter((x) => x.id !== c.id).map((x) => fbDimVal(x, d, w)));
              return fbDimVal(c, d, w) >= best ? "network-leading" : "above median";
            })()
          ] }, d)) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 9, color: FB_MUTD }, children: "none above 0.65 \u2014 rebuild from gaps first" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: "#e03535", letterSpacing: 1, marginBottom: 5 }, children: "GAPS" }),
          gaps.length ? gaps.map((d) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 9.5, color: FB_INKD, padding: "2px 0" }, children: [
            "\u25B8 ",
            d,
            " ",
            fbDimVal(c, d, w).toFixed(2),
            " \u2014 ",
            (() => {
              const best = FB_BOUNDARY.centers.filter((x) => x.id !== c.id).sort((a, b) => fbDimVal(b, d, w) - fbDimVal(a, d, w))[0];
              return "learn from " + best.id + " (" + fbDimVal(best, d, w).toFixed(2) + ")";
            })()
          ] }, d)) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 9, color: FB_MUTD }, children: "no dimension below 0.40" })
        ] })
      ] }),
      rd && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 12, borderTop: `1px solid ${FB_LINE}`, paddingTop: 8 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_CY, letterSpacing: 1, marginBottom: 4 }, children: [
          fbT(q, "signature").toUpperCase(),
          " \u2014 ",
          c.shock.kind,
          " (W",
          c.shock.week,
          ")"
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 100 26", style: { width: "100%", maxWidth: 460, height: 58 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: "0", y1: "13", x2: "100", y2: "13", stroke: FB_LINE, strokeWidth: "0.4" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("polyline", { fill: "none", stroke: rd.Q > 3 ? "#e03535" : FB_CY, strokeWidth: "0.8", points: rd.pts.map((p) => `${p.t / 10 * 100},${13 - p.a * 34}`).join(" ") })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_MUTD }, children: [
          fbT(q, "settling").toLowerCase(),
          " ",
          rd.Q.toFixed(1),
          " \xB7 residual ",
          (rd.residual * 100).toFixed(0),
          "% \u2014 ",
          rd.Q > 3 ? "still swinging; intervene before it flattens into permanent loss" : "absorbing on schedule"
        ] })
      ] }),
      OPERATIONS_STATE[c.id] && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 12, borderTop: `1px solid ${FB_LINE}`, paddingTop: 8 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_CY, letterSpacing: 1, marginBottom: 6 }, children: "CENTER OPERATIONS \u2014 REAL-TIME SIGNALS" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(140px,1fr))", gap: 8 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${OPERATIONS_STATE[c.id].adjustmentRequests.pending > 0 ? "#d9a520" : FB_LINE}`, borderRadius: 3, padding: 8, background: OPERATIONS_STATE[c.id].adjustmentRequests.pending > 0 ? "#d9a52015" : "#f9f9f9" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD }, children: "Adj. Requests" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 13, color: FB_INKD, marginTop: 2 }, children: [
              OPERATIONS_STATE[c.id].adjustmentRequests.pending,
              " pending"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD }, children: [
              OPERATIONS_STATE[c.id].adjustmentRequests.approved,
              " approved W",
              OPERATIONS_STATE[c.id].adjustmentRequests.week
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${OPERATIONS_STATE[c.id].makeupCoaching.hours > 12 ? "#e03535" : FB_LINE}`, borderRadius: 3, padding: 8, background: OPERATIONS_STATE[c.id].makeupCoaching.hours > 12 ? "#e0353515" : "#f9f9f9" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD }, children: "Makeup Hours" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 13, color: OPERATIONS_STATE[c.id].makeupCoaching.hours > 12 ? "#e03535" : FB_INKD, marginTop: 2 }, children: [
              OPERATIONS_STATE[c.id].makeupCoaching.hours,
              "h"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD }, children: [
              OPERATIONS_STATE[c.id].makeupCoaching.sessions,
              " sessions"
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${OPERATIONS_STATE[c.id].studentEngagement.atRisk > 1 ? "#b3261e" : FB_LINE}`, borderRadius: 3, padding: 8, background: OPERATIONS_STATE[c.id].studentEngagement.atRisk > 1 ? "#b3261e15" : "#f9f9f9" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD }, children: "Engagement" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 13, color: FB_INKD, marginTop: 2 }, children: [
              OPERATIONS_STATE[c.id].studentEngagement.projectsOnTrack,
              "/",
              OPERATIONS_STATE[c.id].studentEngagement.projectsOnTrack + OPERATIONS_STATE[c.id].studentEngagement.atRisk
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD }, children: [
              "on-track ",
              Math.round(OPERATIONS_STATE[c.id].studentEngagement.completionRate * 100),
              "%"
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${Object.values(OPERATIONS_STATE[c.id].projectPacing).some((p) => p < 0.6) ? "#d9a520" : FB_LINE}`, borderRadius: 3, padding: 8, background: Object.values(OPERATIONS_STATE[c.id].projectPacing).some((p) => p < 0.6) ? "#d9a52015" : "#f9f9f9" }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD }, children: "Curriculum" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 13, color: FB_INKD, marginTop: 2 }, children: Object.values(OPERATIONS_STATE[c.id].projectPacing).every((p) => p >= 0.7) ? "on-pace" : "watch" }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD }, children: Object.entries(OPERATIONS_STATE[c.id].projectPacing).filter(([_, p]) => p < 0.6).length > 0 ? `${Object.entries(OPERATIONS_STATE[c.id].projectPacing).filter(([_, p]) => p < 0.6).length} tracks behind` : "all on-track" })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD, marginTop: 6, lineHeight: 1.5 }, children: "Real-time operations data from Pike scheduling, MyStudio engagement tracking, and session dialogue frequency. Integration point: identify friction signals (high makeup requests, stalled curricula) before they compound into retention risk." })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 12, borderTop: `1px solid ${FB_LINE}`, paddingTop: 8 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: "#d9a520", letterSpacing: 1, marginBottom: 5 }, children: "RECOMMENDED ACTIONS" }),
        actions.map((a, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 9.5, color: FB_INKD, padding: "2px 0" }, children: [
          i + 1,
          ". ",
          a
        ] }, i))
      ] })
    ] });
  }
  var FB_BASES = [
    { id: "map", label: "MAP", sub: "spatial graph" },
    { id: "circuit", label: "CIRCUIT", sub: "gates & governors" },
    { id: "calendar", label: "CALENDAR", sub: "timeline" },
    { id: "phase", label: "PHASE", sub: "energy landscape" },
    { id: "ops", label: "OPERATIONS", sub: "benchmarks & data" }
  ];
  function FiveBasisTab({ initialBasis = "map" } = {}) {
    const [basis, setBasis] = (0, import_react.useState)(initialBasis);
    const [week, setWeek] = (0, import_react.useState)(7);
    const [sel, setSel] = (0, import_react.useState)(null);
    const q = false;
    const netHealth = FB_BOUNDARY.centers.reduce((s, c) => s + fbHealth(c, week), 0) / FB_BOUNDARY.centers.length;
    const critical = FB_BOUNDARY.centers.filter((c) => fbTauAlert(fbTau(c, week)) === "critical").length;
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { background: FB_BG, borderRadius: 8, padding: 18, color: FB_INKD }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { maxWidth: 980, margin: "0 auto" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", flexWrap: "wrap", gap: 8, marginBottom: 4 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 9, color: FB_VI, letterSpacing: 2 }, children: "PROPOSED POLICY FRAMEWORK \u2014 CANDIDATE SUBMISSION" }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("h1", { style: { fontFamily: FB_MONO, fontSize: 17, margin: "4px 0 0", fontWeight: 600 }, children: "CN Network \u2014 One Shared State, Five Views" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: FB_MONO, fontSize: 9.5, color: FB_MUTD, textAlign: "right" }, children: [
          "net health ",
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: fbStateCol(netHealth) }, children: netHealth.toFixed(2) }),
          " \xB7 ",
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { color: critical ? "#e03535" : "#2fbf5f" }, children: critical }),
          " slow-recovery \xB7 cn-boundary/1.1"
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${FB_LINE}`, borderLeft: `3px solid ${FB_VI}`, background: FB_PANEL, padding: "8px 12px", margin: "10px 0" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 8.5, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: FB_VI, marginBottom: 3 }, children: "Why this matters" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: FB_MONO, fontSize: 10.5, color: FB_INKD, lineHeight: 1.6 }, children: "Every screen in this model \u2014 network map, center detail, agent recommendation, ledger entry \u2014 reads from this one canonical state rather than maintaining its own copy of the numbers. For a network this size, that's not a technical nicety; it's the only way a Director, an FBC, and a franchisee can look at the same center and see the same facts." })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 12, background: FB_PANEL, border: `1px solid ${FB_LINE}`, borderRadius: 6, padding: "8px 14px", margin: "10px 0" }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: FB_MONO, fontSize: 9, color: FB_CY, letterSpacing: 1, whiteSpace: "nowrap" }, children: "TIMELINE" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { type: "range", min: "0", max: "12", value: week, onChange: (e) => setWeek(+e.target.value), style: { flex: 1, accentColor: FB_CY, cursor: "pointer" } }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { fontFamily: FB_MONO, fontSize: 12, color: FB_INKD, minWidth: 34 }, children: [
          "W",
          week
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: FB_MONO, fontSize: 8.5, color: FB_MUTD, whiteSpace: "nowrap" }, children: "every view updates together" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { display: "flex", gap: 6, marginBottom: 12, flexWrap: "wrap" }, children: FB_BASES.map((b) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", { onClick: () => setBasis(b.id), style: {
        fontFamily: FB_MONO,
        fontSize: 9.5,
        padding: "7px 13px",
        borderRadius: 4,
        cursor: "pointer",
        background: basis === b.id ? `${FB_CY}18` : "transparent",
        border: `1px solid ${basis === b.id ? FB_CY : FB_LINE}`,
        color: basis === b.id ? FB_CY : FB_MUTD,
        transition: "all .15s"
      }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: b.label }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { style: { opacity: 0.6 }, children: [
          " \xB7 ",
          b.sub
        ] })
      ] }, b.id)) }),
      basis === "map" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BasisMap, { w: week, sel, setSel, q }),
      basis === "circuit" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BasisCircuit, { w: week, q }),
      basis === "calendar" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BasisCalendar, { w: week, q }),
      basis === "phase" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BasisPhase, { w: week, sel, setSel, q }),
      basis === "ops" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BasisOps, { w: week, q }),
      sel && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FranchiseReport, { c: FB_BOUNDARY.centers.find((x) => x.id === sel), w: week, q, onClose: () => setSel(null) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 14, borderTop: `1px solid ${FB_LINE}`, paddingTop: 8, fontFamily: FB_MONO, fontSize: 8, color: FB_MUTD, lineHeight: 1.7 }, children: [
        "One shared state \xB7 five views \xB7 recovery-time, recovery-signature, retention-inflection, and pricing-ceiling indicators \xB7 peer benchmarks use aggregates only",
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("br", {}),
        "Seeds illustrative, structures computed live \xB7 corporate register throughout \xB7 Pratik Singh"
      ] })
    ] }) });
  }
  var CR_SETTLE = (q) => q > 3 ? "prolonged" : q > 1.5 ? "moderate" : "rapid";
  function EarlyWarningTab() {
    const w = 7;
    const rows = FB_BOUNDARY.centers.map((c) => ({
      c,
      h: fbHealth(c, w),
      fresh: fbCoherence(c, w),
      rec: fbTau(c, w),
      infl: fbInflection(c),
      rd: fbRecoverySig(c, w),
      headroom: c.Tc - c.tuition
    })).sort((a, b) => a.h - b.h);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 8 }, children: "Early-warning summary \u2014 measurement before escalation" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 12.5, color: "#444", lineHeight: 1.6, marginBottom: 12 }, children: [
        "Each center is tracked on four leading indicators: ",
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "recovery time" }),
        " (how quickly performance returns to baseline after a disruption), ",
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "recovery signature" }),
        " (whether the return is settling or still swinging), ",
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "retention inflection point" }),
        " (the projected week at which family and staff attrition risk compounds), and ",
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: "pricing ceiling headroom" }),
        " (distance to the tuition level the local market will not absorb). All figures derive from the same progress-report data reviewed with each center; peer comparisons use aggregate benchmarks only \u2014 center-level financials are never shared across owners."
      ] }),
      rows.map(({ c, h, fresh, rec, infl, rd, headroom }) => {
        const recFlag = rec >= 3 ? "action" : rec >= 2 ? "monitor" : "stable";
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { border: `1px solid ${RULE}`, borderLeft: `3px solid ${recFlag === "action" ? "#b3261e" : recFlag === "monitor" ? "#9a6b00" : "#1f7a3f"}`, padding: "10px 14px", marginBottom: 10 }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", flexWrap: "wrap", gap: 6 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 13, color: INK }, children: c.label }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 0.8, textTransform: "uppercase", color: recFlag === "action" ? "#b3261e" : recFlag === "monitor" ? "#9a6b00" : "#1f7a3f" }, children: recFlag === "action" ? "Support plan recommended" : recFlag === "monitor" ? "Monitor" : "Performing" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(170px,1fr))", gap: "6px 18px", marginTop: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11.5, color: "#333" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica" }, children: "Recovery time:" }),
              " ",
              rec.toFixed(1),
              " weeks",
              rec >= 3 ? " \u2014 best practice indicates intervention within 30 days" : rec >= 2 ? " \u2014 lengthening; schedule a progress review" : ""
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11.5, color: "#333" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica" }, children: "Data freshness:" }),
              " ",
              Math.round(fresh * 100),
              "%",
              fresh < 0.5 ? " \u2014 last review is stale; measurement precedes any decision" : ""
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11.5, color: "#333" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica" }, children: "Pricing ceiling headroom:" }),
              " $",
              headroom,
              "/mo",
              headroom <= 15 ? " \u2014 no increase advisable this cycle" : ""
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11.5, color: "#333" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica" }, children: "Retention inflection:" }),
              " ",
              infl === null ? "not projected \u2014 trend stable" : infl <= w ? "passed \u2014 retention costs now exceed early-intervention costs 3-4x" : `projected week ${Math.round(infl)} \u2014 act before this point`
            ] })
          ] }),
          rd && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 8, borderTop: `1px solid ${RULE}`, paddingTop: 6 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontFamily: "Helvetica", fontSize: 9.5, fontWeight: 700, letterSpacing: 0.6, textTransform: "uppercase", color: MUT, marginBottom: 3 }, children: [
              "Recovery signature \u2014 ",
              c.shock.kind,
              " (week ",
              c.shock.week,
              ")"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", { viewBox: "0 0 100 22", style: { width: "100%", maxWidth: 420, height: 52, display: "block" }, children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("line", { x1: "0", y1: "11", x2: "100", y2: "11", stroke: RULE, strokeWidth: "0.4" }),
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
                "polyline",
                {
                  fill: "none",
                  stroke: CR_SETTLE(rd.Q) === "prolonged" ? "#b3261e" : "#1f7a3f",
                  strokeWidth: "0.8",
                  points: rd.pts.map((p) => `${p.t / 10 * 100},${11 - p.a * 28}`).join(" ")
                }
              )
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11, color: "#444", lineHeight: 1.5 }, children: [
              "Settling is ",
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: CR_SETTLE(rd.Q) }),
              ". ",
              CR_SETTLE(rd.Q) === "prolonged" ? "Performance is still swinging rather than returning to baseline \u2014 a documented leading indicator of sustained decline. Recommend activating the support plan now, while remaining momentum is recoverable." : "Disruption absorbed; performance returning to baseline on schedule.",
              " Residual impact: ",
              (rd.residual * 100).toFixed(0),
              "%."
            ] })
          ] })
        ] }, c.id);
      }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { marginTop: 14, borderTop: `1px solid ${RULE}`, paddingTop: 10 }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 8 }, children: "Operational Red Flags \u2014 Leading Indicators" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 11, color: "#555", lineHeight: 1.6, marginBottom: 10 }, children: "Operations data (scheduling adjustments, makeup hour burn, student engagement friction, curriculum pacing) often precedes financial/retention metrics by 2\u20133 weeks. Monitoring these signals enables earlier intervention." }),
        rows.map(({ c }) => {
          const ops = OPERATIONS_STATE[c.id];
          if (!ops) return null;
          const flags = [];
          const adjReqBurn = ops.adjustmentRequests.pending + ops.adjustmentRequests.approved;
          if (adjReqBurn > 8) flags.push({ sev: "action", msg: `High adjustment request volume (${adjReqBurn}) \u2014 scheduling friction or student instability` });
          else if (adjReqBurn >= 5) flags.push({ sev: "watch", msg: `Moderate adjustment requests (${adjReqBurn}) \u2014 monitor staff availability strain` });
          if (ops.makeupCoaching.hours > 12) flags.push({ sev: "action", msg: `Makeup coaching hours elevated (${ops.makeupCoaching.hours}h) \u2014 early sign of staff burnout or engagement gaps` });
          if (ops.studentEngagement.atRisk > 1) flags.push({ sev: "watch", msg: `${ops.studentEngagement.atRisk} students at-risk \u2014 retention impact likely within 2\u20133 weeks` });
          if (Object.values(ops.projectPacing).some((p) => p < 0.55)) flags.push({ sev: "watch", msg: `Curriculum pacing behind on ${Object.entries(ops.projectPacing).filter(([_, p]) => p < 0.55).map(([k]) => k).join(", ")} \u2014 disengagement precedes withdrawals` });
          if (ops.dialogueFreq < 8) flags.push({ sev: "watch", msg: `Low session dialogue frequency (${ops.dialogueFreq} entries/wk) \u2014 coach-to-coach knowledge transfer weak` });
          if (!flags.length) return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 10, alignItems: "baseline", padding: "6px 0", borderBottom: "1px solid #f0f0f0", fontSize: 11.5 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { minWidth: 80, color: INK }, children: c.label }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { color: MUT }, children: "ops healthy \u2014 no leading indicators" })
          ] }, c.id);
          return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: flags.map((f, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 10, alignItems: "baseline", padding: "8px 0", borderBottom: "1px solid #f0f0f0", borderLeft: `3px solid ${f.sev === "action" ? "#b3261e" : "#9a6b00"}`, paddingLeft: 8 }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { minWidth: 80, color: INK, fontSize: 11 }, children: c.label }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 10, fontWeight: 700, color: f.sev === "action" ? "#b3261e" : "#9a6b00", minWidth: 50 }, children: f.sev.toUpperCase() }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 11, color: "#444" }, children: f.msg })
          ] }, i)) }, c.id);
        })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, margin: "14px 0 6px" }, children: "Peer benchmarking \u2014 aggregates only" }),
      FB_BOUNDARY.clusters.map((cl) => {
        const k = fbClusterKernel(cl, w);
        return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "baseline", gap: 10, padding: "4px 0", borderBottom: "1px solid #f0f0f0", fontSize: 11.5, color: "#333" }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", minWidth: 80, color: INK }, children: cl.id }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { minWidth: 110, color: MUT, fontFamily: "Helvetica", fontSize: 10 }, children: cl.members.join(" \xB7 ") }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
            "benchmark alignment ",
            Math.round(k.k * 100),
            "% \xB7 support coupling ",
            Math.round(cl.coupling * 100),
            "%",
            !k.robust ? " \u2014 single-center group: no peer support available if disrupted; priority is building shared staffing and referral ties" : ""
          ] })
        ] }, cl.id);
      }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontSize: 11, color: "#666", lineHeight: 1.6, marginTop: 8 }, children: "Benchmarks are computed from aggregate indicators. Individual center financials remain confidential to each owner \u2014 a data-governance commitment, not a limitation." })
    ] });
  }
  var OPERATIONS_STATE = {
    "pleasanton": {
      adjustmentRequests: { pending: 2, approved: 8, denied: 1, week: 7 },
      makeupCoaching: { hours: 14, sessions: 4, lastUpdated: "2026-07-10" },
      studentEngagement: { projectsOnTrack: 18, atRisk: 2, completionRate: 0.89 },
      projectPacing: { scratchAdvanced: 0.72, pythonIntro: 0.58, pixelpadAdvanced: 0.91 },
      staffNotifications: [
        { date: "2026-07-08", type: "scheduling_adjustment", count: 2 },
        { date: "2026-07-09", type: "makeup_request", count: 1 },
        { date: "2026-07-10", type: "engagement_alert", count: 0 }
      ],
      dialogueFreq: 12
      // weekly session dialogue entries (proxy for engagement depth)
    },
    "walnutcreek": {
      adjustmentRequests: { pending: 1, approved: 5, denied: 0, week: 7 },
      makeupCoaching: { hours: 8, sessions: 2, lastUpdated: "2026-07-09" },
      studentEngagement: { projectsOnTrack: 14, atRisk: 1, completionRate: 0.92 },
      projectPacing: { scratchAdvanced: 0.68, pythonIntro: 0.64, pixelpadAdvanced: 0.85 },
      staffNotifications: [
        { date: "2026-07-08", type: "scheduling_adjustment", count: 1 }
      ],
      dialogueFreq: 8
    }
  };
  function SignalsTab() {
    const w = 7;
    const alerts = [];
    FB_BOUNDARY.centers.forEach((c) => {
      const rec = fbTau(c, w), fresh = fbCoherence(c, w), infl = fbInflection(c), head = c.Tc - c.tuition;
      if (rec >= 3) alerts.push({ sev: 2, c: c.id, m: `Recovery time ${rec.toFixed(1)}w \u2014 support plan inside 30 days` });
      else if (rec >= 2) alerts.push({ sev: 1, c: c.id, m: `Recovery time lengthening (${rec.toFixed(1)}w) \u2014 schedule a progress review` });
      if (fresh < 0.5) alerts.push({ sev: 1, c: c.id, m: `Data ${Math.round(fresh * 100)}% fresh \u2014 review before any decision` });
      if (head <= 15) alerts.push({ sev: 2, c: c.id, m: `$${head} from pricing ceiling \u2014 hold tuition` });
      if (infl !== null && infl > w && infl <= w + 3) alerts.push({ sev: 2, c: c.id, m: `Retention inflection projected W${Math.round(infl)} \u2014 intervene now` });
    });
    FB_BOUNDARY.clusters.filter((cl) => cl.members.length < 2).forEach((cl) => alerts.push({ sev: 1, c: cl.members[0], m: "No peer support group \u2014 build shared staffing/referral ties before the next disruption" }));
    alerts.sort((a, b) => b.sev - a.sev);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { fontFamily: "Helvetica", fontSize: 11, fontWeight: 700, letterSpacing: 1, textTransform: "uppercase", color: MUT, borderBottom: `1px solid ${RULE}`, paddingBottom: 3, marginBottom: 6 }, children: "Network signals \u2014 live from current state" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { fontSize: 11.5, color: "#555", marginBottom: 10, lineHeight: 1.5 }, children: [
        "This feed is passive: it recomputes from the same state every view reads. ",
        alerts.filter((a) => a.sev === 2).length,
        " action signals, ",
        alerts.filter((a) => a.sev === 1).length,
        " watch signals."
      ] }),
      alerts.map((a, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", gap: 10, alignItems: "baseline", padding: "7px 10px", marginBottom: 5, border: `1px solid ${RULE}`, borderLeft: `3px solid ${a.sev === 2 ? "#b3261e" : "#9a6b00"}` }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 9, letterSpacing: 0.5, color: a.sev === 2 ? "#b3261e" : "#9a6b00", minWidth: 52 }, children: a.sev === 2 ? "ACTION" : "WATCH" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { style: { fontFamily: "Helvetica", fontSize: 10.5, minWidth: 30, color: INK }, children: a.c }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { style: { fontSize: 11.5, color: "#333" }, children: a.m })
      ] }, i))
    ] });
  }

  // src/main.jsx
  var import_jsx_runtime2 = __toESM(require_jsx_runtime());
  var initialTab = window.location.hash ? window.location.hash.slice(1) : void 0;
  window.__BUILD_SHA__ = true ? "debug" : "dev";
  var root = (0, import_client.createRoot)(document.getElementById("root"));
  root.render(/* @__PURE__ */ (0, import_jsx_runtime2.jsx)(Engine, { initialTab }));
})();
/*! Bundled license information:

react/cjs/react.development.js:
  (**
   * @license React
   * react.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

scheduler/cjs/scheduler.development.js:
  (**
   * @license React
   * scheduler.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-dom/cjs/react-dom.development.js:
  (**
   * @license React
   * react-dom.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)
  (**
   * Checks if an event is supported in the current execution environment.
   *
   * NOTE: This will not work correctly for non-generic events such as `change`,
   * `reset`, `load`, `error`, and `select`.
   *
   * Borrows from Modernizr.
   *
   * @param {string} eventNameSuffix Event name, e.g. "click".
   * @return {boolean} True if the event is supported.
   * @internal
   * @license Modernizr 3.0.0pre (Custom Build) | MIT
   *)

react/cjs/react-jsx-runtime.development.js:
  (**
   * @license React
   * react-jsx-runtime.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)
*/
